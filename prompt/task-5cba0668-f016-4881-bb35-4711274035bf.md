---
task_id: 5cba0668-f016-4881-bb35-4711274035bf
title: Inspector Bridge Script در frontend/index.html و App.jsx دارای WebSocket URL سخت‌کد شده و غیرفعال است
type: bug
priority: high
execution_priority: 2000
status: suggested
external_status: pending
verification_status: pending
watched_id: 6b04f8bd-b562-4fb9-9cd9-0c27458ced59
project: mahdighandi1989/language
created_at: '2026-05-16T07:42:12.782873+00:00'
updated_at: '2026-05-29T20:15:54.329785+00:00'
target_files:
- frontend/src/App.jsx
- frontend/src/App.jsx
- frontend/index.html
---

# Inspector Bridge Script در frontend/index.html و App.jsx دارای WebSocket URL سخت‌کد شده و غیرفعال است

## Raw Idea

در frontend/index.html (خط 8) و frontend/src/App.jsx (خط 8) یک WebSocket URL سخت‌کد شده به آدرس 'wss://ai-creator-backend-q677.onrender.com/api/render/ws/bridge/gh_mahdighandi1989_language' وجود دارد. در App.jsx خط 17 یک شرط if وجود دارد که اگر این URL دقیقاً برابر این مقدار باشد، اتصال WebSocket برقرار نمی‌شود ('if (!WS_URL || WS_URL === 'wss://...') return;'). این باعث می‌شود کل Inspector Bridge عملاً غیرفعال باشد و هیچ رویدادی (کلیک، اسکرول، تایپ) به backend ارسال نشود. همچنین این یک security issue است چون کلید API Firebase در index.html به صورت plain text در خط 12 قرار دارد.
---
[scan #2 at 2026-05-23T07:36:07.798552+00:00]
در frontend/src/App.jsx خط 8، WebSocket URL به آدرس 'wss://ai-creator-backend-q677.onrender.com/api/render/ws/bridge/gh_mahdighandi1989_language' تنظیم شده است که به یک سرور خارجی (احتمالاً متعلق به یک پلتفرم دیگر) اشاره می‌کند. این آدرس با پروژه فعلی (mahdighandi1989/language) مرتبط نیست و باعث اتص
---
[scan #3 at 2026-05-23T07:36:07.831705+00:00]
اسکریپت Inspector Bridge در frontend/index.html (خطوط 32-201) و frontend/src/App.jsx (خطوط 2-126) به صورت مجزا و با منطق متفاوت قرار گرفته است. نسخه index.html از postMessage و نسخه App.jsx از WebSocket استفاده می‌کند. این دو نسخه با یکدیگر تداخل دارند و باعث ارسال پیام‌های تکراری و مصرف بی‌رویه پهن
---
[scan #4 at 2026-05-23T07:36:07.869056+00:00]
اسکریپت Inspector Bridge در frontend/index.html (خطوط 32-201) و همچنین در frontend/src/App.jsx (خطوط 2-126) به‌طور کامل تکرار شده است. هر دو اسکریپت event listenerهای یکسانی برای click, input, scroll ثبت می‌کنند. این باعث می‌شود هر رویداد کاربر دو بار به Inspector ارسال شود (duplicate messages). همچ
---
[scan #5 at 2026-05-23T07:36:33.745101+00:00]
در frontend/src/App.jsx خط 8، Inspector Bridge به WebSocket endpoint 'wss://ai-creator-backend-q677.onrender.com/api/render/ws/bridge/gh_mahdighandi1989_language' متصل می‌شود. اما در backend/server.js، تنها یک WebSocket endpoint در خط 45 تعریف شده است: '/ws/live'. هیچ endpoint با مسیر '/api/render/w

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
Inspector Bridge Script در frontend/index.html و App.jsx دارای WebSocket URL سخت‌کد شده و غیرفعال است

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/App.jsx:8-8` — `WS_URL` — URL سخت‌کد شده که باید به env منتقل شود
  ```jsx
  const WS_URL = 'wss://ai-creator-backend-q677.onrender.com/api/render/ws/bridge/gh_mahdighandi1989_language';
  ```
- `frontend/src/App.jsx:17-17` — `connectWS` — شرطی که کل Inspector Bridge را غیرفعال می‌کند
  ```jsx
  if (!WS_URL || WS_URL === 'wss://ai-creator-backend-q677.onrender.com/api/render/ws/bridge/gh_mahdighandi1989_language') return;
  ```
- `frontend/index.html:11-18` — `__firebase_config` — Firebase credentials در HTML به صورت plain text
  ```
  var __firebase_config = JSON.stringify({
    apiKey: "AIzaSyAMFtV0zIMKKe0bIr68Z1wTORQ1jQpsv9Q",
    authDomain: "labaneseapp.firebaseapp.com",
    ...
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
React 18 + Vite + Firebase + WebSocket

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `frontend/.env.example` (سطر 1) — برای اضافه کردن متغیرهای محیطی جدید
- `frontend/vite.config.js` (سطر 1) — برای پشتیبانی از env variables

## 🌐 نقشهٔ وابستگی‌ها
Inspector Bridge در دو مکان (index.html و App.jsx) تعریف شده و هر دو از یک URL سخت‌کد شده استفاده می‌کنند. غیرفعال بودن آن باعث می‌شود قابلیت ریموت inspect و debug از کار بیفتد.

## 🔍 Context و وضعیت فعلی
در frontend/index.html (خط 8) و frontend/src/App.jsx (خط 8) یک WebSocket URL سخت‌کد شده به آدرس 'wss://ai-creator-backend-q677.onrender.com/api/render/ws/bridge/gh_mahdighandi1989_language' وجود دارد. در App.jsx خط 17 یک شرط if وجود دارد که اگر این URL دقیقاً برابر این مقدار باشد، اتصال WebSocket برقرار نمی‌شود ('if (!WS_URL || WS_URL === 'wss://...') return;'). این باعث می‌شود کل Inspector Bridge عملاً غیرفعال باشد و هیچ رویدادی (کلیک، اسکرول، تایپ) به backend ارسال نشود. همچنین این یک security issue است چون کلید API Firebase در index.html به صورت plain text در خط 12 قرار دارد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] WebSocket URL از متغیر محیطی VITE_WS_URL خوانده شود
- [ ] شرط غیرفعال‌سازی در خط 17 حذف شود
- [ ] Firebase config از index.html حذف و از env خوانده شود
- [ ] Inspector Bridge رویدادهای کلیک و اسکرول را به backend ارسال کند
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. WebSocket URL را به یک متغیر محیطی (VITE_WS_URL) منتقل کنید و شرط غیرفعال‌سازی را حذف کنید. همچنین Firebase config را از index.html به یک فایل env منتقل کرده و از VITE_FIREBASE_CONFIG استفاده کنید.

## 💡 نمونه‌های قبل/بعد
**WebSocket URL**

_قبل:_
```
const WS_URL = 'wss://ai-creator-backend-q677.onrender.com/api/render/ws/bridge/gh_mahdighandi1989_language';
```

_بعد:_
```
const WS_URL = import.meta.env.VITE_WS_URL || '';
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `grep -r 'ai-creator-backend-q677' frontend/`
- `grep -r 'AIzaSyAMFtV0zIMKKe0bIr68Z1wTORQ1jQpsv9Q' frontend/`

## ⚠️ ریسک‌ها و موارد احتیاط
کم - فقط تغییر مسیر خواندن config

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: bug
- اولویت: high
- تخمین زمان: small

## Acceptance Criteria

1. WebSocket URL از متغیر محیطی VITE_WS_URL خوانده شود _(verify: static)_
2. شرط غیرفعال‌سازی در خط 17 حذف شود _(verify: static)_
3. Firebase config از index.html حذف و از env خوانده شود _(verify: static)_
4. Inspector Bridge رویدادهای کلیک و اسکرول را به backend ارسال کند _(verify: ui_interaction)_

## Task Steps

### Step 1: انتقال WebSocket URL از سخت‌کد شده به متغیر محیطی در App.jsx
**Status:** `pending` (0%)
**Scope:** این مرحله شامل جایگزینی WebSocket URL سخت‌کد شده در frontend/src/App.jsx با یک متغیر محیطی (VITE_WS_URL) است. فایل .env.example باید به‌روزرسانی شود تا شامل این متغیر جدید باشد. همچنین vite.config.js باید بررسی شود تا از پشتیبانی env variables اطمینان حاصل شود. خارج از این مرحله: تغییر در index.html، اصلاح شرط if در خط 17، یا رفع security issue Firebase.
**Excerpt:**
```
در frontend/index.html (خط 8) و frontend/src/App.jsx (خط 8) یک WebSocket URL سخت‌کد شده به آدرس 'wss://ai-creator-backend-q677.onrender.com/api/render/ws/bridge/gh_mahdighandi1989_language' وجود دارد.
```

### Step 2: اصلاح شرط if غیرفعال‌کننده WebSocket در App.jsx
**Status:** `pending` (0%)
**Scope:** این مرحله شامل اصلاح شرط if در خط 17 App.jsx است که Inspector Bridge را غیرفعال می‌کند. شرط فعلی 'if (!WS_URL || WS_URL === 'wss://...') return;' باید به شرطی تغییر کند که فقط در صورت نبودن URL (null/undefined) از اتصال جلوگیری کند، نه اینکه URL خاصی را مسدود کند. خارج از این مرحله: تغییر در index.html، انتقال URL به env (مرحله قبل)، یا رفع security issue Firebase.
**Excerpt:**
```
در App.jsx خط 17 یک شرط if وجود دارد که اگر این URL دقیقاً برابر این مقدار باشد، اتصال WebSocket برقرار نمی‌شود ('if (!WS_URL || WS_URL === 'wss://...') return;'). این باعث می‌شود کل Inspector Bridge عملاً غیرفعال باشد و هیچ رویدادی (کلیک، اسکرول، تایپ) به backend ارسال نشود.
```

### Step 3: انتقال WebSocket URL از سخت‌کد شده به متغیر محیطی در index.html
**Status:** `pending` (0%)
**Scope:** این مرحله شامل جایگزینی WebSocket URL سخت‌کد شده در frontend/index.html (خط 8) با یک متغیر محیطی است. از آنجایی که index.html مستقیماً به env variables دسترسی ندارد، باید از یک اسکریپت inline استفاده شود که مقدار را از window.__ENV یا از یک endpoint دریافت کند. همچنین باید یک فایل env.js یا اسکریپت مشابه برای تزریق env variables در build time ایجاد شود. خارج از این مرحله: تغییر در App.jsx (مراحل 1 و 2) یا رفع security issue Firebase.
**Excerpt:**
```
در frontend/index.html (خط 8) و frontend/src/App.jsx (خط 8) یک WebSocket URL سخت‌کد شده به آدرس 'wss://ai-creator-backend-q677.onrender.com/api/render/ws/bridge/gh_mahdighandi1989_language' وجود دارد.
```

### Step 4: رفع security issue: انتقال Firebase API Key از index.html به متغیر محیطی
**Status:** `pending` (0%)
**Scope:** این مرحله شامل حذف Firebase credentials (apiKey, authDomain, projectId, ...) از frontend/index.html (خط 12) و انتقال آن‌ها به متغیرهای محیطی است. باید یک فایل env.js یا اسکریپت مشابه ایجاد شود که این مقادیر را از env variables خوانده و به window.__FIREBASE_CONFIG تزریق کند. فایل .env.example باید شامل متغیرهای Firebase جدید باشد. خارج از این مرحله: تغییر در App.jsx یا اصلاح WebSocket URL.
**Excerpt:**
```
همچنین این یک security issue است چون کلید API Firebase در index.html به صورت plain text در خط 12 قرار دارد. var __firebase_config = JSON.stringify({ apiKey: "AIzaSyAMFtV0zIMKKe0bIr68Z1wTORQ1jQpsv9Q", authDomain: "labaneseapp.firebaseapp.com", ...
```
