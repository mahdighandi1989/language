---
task_id: dc721af2-f84f-424d-a491-91e8ab3624c0
title: میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار (6 مرحله)
type: other
priority: medium
execution_priority: 3000
status: pending
external_status: pending
verification_status: pending
watched_id: 6b04f8bd-b562-4fb9-9cd9-0c27458ced59
project: mahdighandi1989/language
created_at: '2026-05-12T11:13:22.960230+00:00'
updated_at: '2026-06-01T20:10:21.707105+00:00'
target_files:
- backend/server.js
- backend/.env.example
- frontend/index.html
- backend/package.json
---

# میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار (6 مرحله)

## Raw Idea

میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم و از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

📖 **خواندن کامل + اجرای مو-به-مو (بسیار مهم):**

این پرامپت — از این یادداشت تا انتها — یک سند واحد است که هر بخشش
حاوی الزام یا context منحصربه‌فرد است. خواندن سطحی یا skim کردن **ممنوع**
است.

- پرامپت را **سطر به سطر** بخوان، نه head/tail/فقط-بخش-اصلی.
- اگر بخشی به‌نظر طولانی یا تکراری آمد، **حتماً** بخوان — تفاوت‌های
  ریز ممکن است در آن جا اساسی باشند.
- هر جمله، URL، نام فایل، نام تابع، یا مقدار عددی که در پرامپت آمده،
  دقیقاً همان است که کاربر می‌خواهد — تغییرش نده، رندش نکن، خلاصه‌اش
  نکن.
- اگر پرامپت چندین درخواست/مرحله/زیرتسک دارد، **همه** را پیاده کن. حتی
  یکی را نه به‌عنوان "خارج از scope" حذف کن.

❌ ممنوعات صریح:
- خلاصه‌سازی متن کاربر در commit message یا response
- "این بخش اصلی نیست، رد می‌کنم"
- "کاربر احتمالاً منظورش این بود..." — منظورش همان است که نوشته
- "این URL/نام به نظر قدیمی است، آپدیتش کردم" — تغییر بدون درخواست ممنوع
- پیاده‌سازی فقط بخشی از پرامپت و تظاهر به کامل بودن
- "همه آیتم‌های لیست A را بررسی کردم، B و C مشابه بودند" — نه؛
  هرکدام را جداگانه

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

🔗 **وابستگی‌ها و همگام‌سازی (بسیار حیاتی — هرگز skip نکن):**

این بخش از همهٔ بخش‌های دیگرِ این یادداشت **مهم‌تر** است. اگر نقض شود،
نتیجهٔ کار ممکن است مشروع به‌نظر برسد ولی در عمل بخش‌های دیگر سیستم را عقب
بیندازد، broken reference تولید کند، یا منجر به data corruption شود.

پیش از و حین تغییر، تمام وابستگی‌ها را در **چهار جهت** به‌طور **کامل و
بدون هیچ خلاصه‌سازی** شناسایی و همگام کن:

**۱. وابستگی‌های upstream (این تسک به چه چیزهایی متکی است):**
- چه فایل‌ها، توابع، کلاس‌ها، API endpoint ها، schema های دیتابیس،
  env vars، یا config هایی که این تسک نیاز دارد؟
- آیا قرار است چیزی را ویرایش/حذف کنی که جای دیگر (signature، رفتار،
  return type، side effect) از آن انتظار خاصی می‌رود؟
- اگر dependency جدیدی اضافه می‌کنی، آیا با dependencyهای موجود تداخل
  دارد (نسخه، compat، lock file)؟

**۲. وابستگی‌های downstream (چه چیزهایی به این تسک متکی‌اند):**
- چه فایل‌ها، توابع، تست‌ها، migrations، docs، یا UI component هایی از
  کدی که داری ویرایش/اضافه/حذف می‌کنی **استفاده می‌کنند**؟
- با grep و reference search **همه‌ی** call sites، importها، subclassها،
  reference های مستقیم و غیرمستقیم را پیدا کن — نه فقط چند مورد اصلی.
- خصوصاً برای حذف یا rename: هیچ broken reference نباید باقی بماند.

**۳. وابستگی‌های cross-tier (بسیار مهم — هرگز فقط یک لایه را نبین):**

تسک شما ممکن است از backend، frontend، database، worker، یا هر tier
دیگری شروع شده باشد. ولی تغییرات تقریباً همیشه روی tier های دیگر هم
اثر می‌گذارند. **مستقل از اینکه تسک از کدام tier است**، این چک‌های دو
طرفه را همیشه انجام بده:

🔁 **اگر backend را تغییر دادی** (API، service، model، route):
  → frontend: کدام component/page/hook این endpoint یا data shape را
    مصرف می‌کند؟ type definition، state shape، error handling، loading
    state، form validation، URL routing همگی باید همگام شوند.
  → mobile/SDK/client library (اگر پروژه دارد): همان داستان frontend.
  → database: آیا migration لازم است؟ آیا rollback امن است؟
  → background workers: آیا event producer/consumer ها تحت تأثیرند؟
  → rate limit، auth، CORS، CSP: آیا رفتار جدید پشتیبانی می‌شود؟

🔁 **اگر frontend را تغییر دادی** (component، form، state، route):
  → backend: آیا endpoint جدید/تغییریافته لازم است؟ آیا data shape ای
    که ارسال می‌شود با schema سرور سازگار است؟
  → backend validation: آیا برای ورودی‌های جدید UI کافی است؟
  → permissions/RBAC: آیا feature جدید نیاز به role check جدید دارد؟
  → analytics/tracking: آیا event های جدید باید در backend log شوند؟
  → SEO/SSR: آیا تغییر route نیاز به sitemap/meta tags جدید دارد؟

🔁 **اگر database/migration را تغییر دادی**:
  → backend models (ORM، Pydantic، dataclasses) همگی به‌روزند؟
  → query های raw SQL یا ORM queries با schema جدید سازگارند؟
  → seed data، fixtures، factory functions تست‌ها به‌روزند؟
  → frontend: آیا data shape جدید در UI به‌درستی render می‌شود؟
  → rollback migration نوشته شده و امن است؟

🔁 **اگر API contract یا event schema را تغییر دادی** (REST، GraphQL،
   WebSocket، gRPC، Kafka، …):
  → OpenAPI/GraphQL schema/proto file آپدیت شد؟
  → همه‌ی consumer ها (client، subscriber، webhook، external API
    user) با version جدید سازگارند؟
  → backward compatibility حفظ شده یا migration path روشن است؟
  → versioning header/path اگر breaking change است؟

🔁 **اگر infrastructure یا config را تغییر دادی** (Dockerfile، CI، Render
   config، env، secrets):
  → README setup/installation section به‌روزه؟
  → `.env.example` با env vars جدید آپدیت شد؟
  → deploy script یا CI workflow هم تغییر کرد؟
  → docs/architecture یا diagram های infrastructure به‌روزند؟

⚠️ **هرگز فقط یک tier را تغییر نده و فرض کنی بقیه خودکار همگام می‌شوند.**
   حتی برای تغییرات به‌ظاهر «کوچک»، چک کن.

**۴. وابستگی‌های جانبی (artifacts که همیشه چک شوند):**

تغییرات کد همیشه روی این artifact ها اثر دارند. **همه را** بررسی و
به‌روز کن — مستندات اولویت **بالا** دارد چون فراموش‌شدنی‌ترین است.

  📝 **مستندات** (همیشه چک کن — حتی برای تغییر کوچک کد):
    - README.md (شرح، setup، نمونه‌های استفاده، badge ها)
    - CHANGELOG.md / RELEASE_NOTES.md
    - docs/ folder (architecture، API reference، user guides، runbooks)
    - inline docstrings/کامنت‌های توابع و کلاس‌های تغییریافته
    - OpenAPI/Swagger annotations، JSDoc/TSDoc
    - architecture diagrams (اگر component اضافه/حذف شد)
    - migration guides (اگر breaking change است)

  🌍 **مستندات کاربر**:
    - i18n files و translation keys
    - UI labels، tooltip ها، help text، error messages
    - in-app onboarding (اگر flow جدید است)

  🧪 **تست‌ها**:
    - unit tests (همه‌ی فایل‌های مرتبط — حتی اگر «بی‌ربط» به‌نظر می‌رسد)
    - integration tests
    - e2e tests (Playwright/Cypress/Selenium)
    - snapshot tests (اگر UI تغییر کرد)
    - contract tests (Pact یا مشابه)
    - performance benchmarks (اگر behavior performance-sensitive تغییر کرد)

  🧬 **type definitions و contracts**:
    - .d.ts files
    - Pydantic models، dataclasses
    - Protobuf/Avro/Thrift schemas
    - GraphQL schema definitions
    - JSON Schemas

  🏗 **infrastructure و config**:
    - Dockerfile، docker-compose.yml
    - Kubernetes manifests
    - Render/Vercel/Netlify config
    - GitHub Actions / GitLab CI workflows
    - environment templates (.env.example، .env.sample)
    - feature flags (LaunchDarkly، GrowthBook، config)

  📊 **monitoring و observability**:
    - logging keys (اگر اضافه/حذف شد، log parser ها هم به‌روز شوند)
    - metric names (Prometheus، Datadog)
    - tracing spans
    - alert rules و dashboards
    - error tracking (Sentry rules، groupings)

  🔐 **security**:
    - auth rules (rate limit، CORS، CSP، HSTS)
    - permissions/RBAC config
    - secrets rotation policies
    - audit log events (اگر action جدید اضافه شد)

  💾 **caches و serialization**:
    - cache keys و TTL (اگر data shape یا lifecycle تغییر کرد)
    - serializer formats (Redis، session storage)
    - browser storage (localStorage، IndexedDB schemas)

**قانون مطلق همگام‌سازی:**
- هر چیزی که در (۱)، (۲)، (۳)، یا (۴) شناسایی شد، در **همان workflow
  این تسک** همگام و به‌روز شود. هرگز برای بعد رها نکن.
- اگر یک فایل/تست/docs نسبت به تغییر شما عقب بماند، در بهترین حالت bug،
  در بدترین حالت مشکل امنیتی یا data corruption تولید می‌کند.
- تغییرات همگام‌سازی می‌توانند در commit جداگانه باشند (در همان task)،
  ولی نباید skip شوند یا به «refactor آینده» سپرده شوند.

**هرگز این جمله‌ها قابل قبول نیست:**
- ❌ «بعداً پیداش می‌کنم»
- ❌ «احتمالاً جای دیگه‌ای استفاده نمی‌شه»
- ❌ «این یه refactor جداگانه‌ست — out of scope»
- ❌ «فقط فایل‌های اصلی رو بررسی کردم»
- ❌ «حدس می‌زنم چیزی بهش وابسته نیست»
- ❌ «دامنه‌ی وابستگی‌ها رو خلاصه کردم» — هرگز خلاصه نکن
- ❌ «این task فقط backend است؛ frontend مشکل خودش» — هرگز
- ❌ «این task فقط frontend است؛ backend از قبل کار می‌کند» — هرگز ثابت نکرده
- ❌ «مستندات بعداً به‌روز می‌شن» — همیشه same-task همگام شوند
- ❌ «testها رو نگاه نکردم چون فقط یه تغییر کوچیک بود»

**در commit message یا PR description**، دامنهٔ وابستگی‌های شناسایی‌شده و
همگام‌شده را به‌طور explicit و **per-tier** بنویس. مثال:
```
Dependencies synced:
- upstream: User model schema, auth middleware
- downstream: 3 API endpoints, 5 frontend components, 12 tests
- cross-tier (backend → frontend): UserProfile.tsx, useUser.ts hook,
  api-types.ts (TS definitions)
- cross-tier (backend → infra): .env.example added NEW_AUTH_SCOPES
- side artifacts: OpenAPI spec, README API section, i18n keys for
  new errors, Sentry alert rule for new error code
```
اگر هیچ وابستگی پیدا نکردی در هر کدام از چهار جهت، صریحاً بنویس:
«بررسی شد — هیچ وابستگی upstream / downstream / cross-tier (backend↔
frontend↔db↔infra) / side شناسایی نشد» تا مشخص باشد بررسی **انجام شده**
نه اینکه فراموش شده.

📋 **مدیریت TO-DO برای اقدامات دستی کاربر (همیشه چک کن):**

⚠️ **هشدار بحرانی — قاعدهٔ ضد-فرار:** TO-DO فقط برای کارهایی است که
**واقعاً غیرممکن** برای agent است (نیاز به انسان مطلق)، نه برای کارهایی
که «بزرگ‌اند»، «وقت می‌برند»، یا «نیازمند fixture/setup» هستند. اگر یک
agent در یک سشن بیش از **۲۰٪ از تسک‌ها** را با TO-DO ببندد، یعنی از کار
فرار می‌کند — این الگو در سشن‌های قبلی **مشاهده** شده و الان ممنوع است.

✅ **فقط برای این موارد TO-DO بساز** (لیست بسته — هرچه خارج این لیست
ممنوع است):

  ۱. **Credential/secret که فقط کاربر دارد**:
     - تنظیم API key واقعی در پنل ادمین خارجی (Render، AWS، Stripe، …)
     - تأیید OAuth client روی console آن سرویس
     - paste کردن webhook secret که فقط بعد از ساخت در dashboard ظاهر می‌شود

  ۲. **Account/billing روی سرویس خارجی که کاربر باید عضو شود**:
     - ساخت account جدید روی Stripe/SendGrid/Twilio/Google Cloud
     - تأیید verification شماره یا ID
     - فعال‌سازی subscription پولی

  ۳. **داده/asset خصوصی که فقط کاربر دارد**:
     - آپلود لوگو/تصویر/فونت برند
     - paste کردن داده‌ای که در محل کار کاربر است
     - import داده‌ای که فقط روی device کاربر است

  ۴. **تصمیم سلیقه‌ای/حقوقی/کسب‌وکار**:
     - انتخاب رنگ‌بندی نهایی یا تم
     - متن دقیق Terms of Service / Privacy Policy
     - تعرفهٔ قیمت‌گذاری
     - نام نهایی برند یا دامنه

⛔ **هرگز TO-DO نکن برای** (لیست سیاه — هر چیزی که در این لیست است
**قابل اجرا** توسط agent است، حتی اگر بزرگ یا چندبخشی باشد):

  ❌ UI component / page / dashboard (هر فریم‌ورک: React, Vue, Angular,
     Svelte، حتی اگر معماری بزرگ دارد) — می‌توانی stub اولیه + state
     management + layout + استایل بسازی
  ❌ "نیازمند Google Drive / Stripe / Twilio API" — می‌توانی **client
     stub** با abstraction layer بسازی که با env var واقعی plug-in شود؛
     کد integration یعنی پیاده‌سازی، نه TO-DO
  ❌ "feature بزرگ، چند روز کار می‌برد" — اندازه دلیل defer نیست؛ کوچک
     شروع کن، iterate کن، در همین سشن کامل کن
  ❌ Celery / background worker / scheduler — یک task ساده + register
     می‌توانی بسازی
  ❌ Migration / model / schema — حتی اگر فیلد جدید نیاز دارد، اضافه کن
  ❌ REST endpoint / GraphQL resolver / WebSocket route — هرگز TO-DO
  ❌ test (unit/integration/e2e) — همیشه قابل نوشتن
  ❌ Documentation / README / API docs — همیشه قابل نوشتن
  ❌ Config file / .env.example / Dockerfile / CI workflow — همیشه قابل
     نوشتن
  ❌ "می‌توانستی .tsx ولی repo .jsx است" — از .jsx استفاده کن، TO-DO نکن
  ❌ "نیازمند فیلد X در مدل دیگر" — اضافه کن فیلد را، TO-DO نکن
  ❌ "تصمیم admin-vs-user-scoped" — پرامپت اولیه scope را معلوم کرده،
     یا با محتاطانه‌ترین تفسیر پیش برو
  ❌ "credential در production هنوز ست نیست" — این TO-DO ساده برای
     تنظیم env var است (مورد ۱ بالا)، نه دلیل برای defer کردن کد
  ❌ "نیازمند verification از کاربر" — اگر اقدام واقعی غیرممکن نیست،
     پیش برو
  ❌ هر چیزی که در یک کامنت `# TODO` معمولی نوشته می‌شد — این فایل
     TO-DO نیست، کامنت inline است

🔬 **قاعدهٔ «حداقل تلاش» قبل از TO-DO**: قبل از TO-DO کردن یک AC، **اثبات
کن** که قابل انجام نیست:

  ۱. آیا می‌توانم یک stub/placeholder بسازم که با env واقعی plug-in شود؟
     → اگر بله، بساز و TO-DO نکن
  ۲. آیا می‌توانم برای این بخش یک test (حتی mock-based) بنویسم؟
     → اگر بله، بنویس و TO-DO نکن
  ۳. آیا می‌توانم abstraction/interface را تعریف کنم، حتی اگر backend
     واقعی نیست؟ → اگر بله، تعریف کن و TO-DO نکن
  ۴. آیا فقط یک حالت سلیقه‌ای/decision کاربر در میان است؟
     → فقط آن یک decision را TO-DO کن، نه کل feature را

اگر یکی از این چهار راه‌حل ممکن بود ولی به TO-DO رفتی، **اعتبار شما از
بین می‌رود**.

📊 **آستانهٔ TO-DO per session**: در یک حلقهٔ اجرای N تسک، اگر بیشتر از
**۲۰٪** تسک‌ها فایل TO-DO ساختی، خودت در گزارش پایانی صریحاً اعلام کن:

  "⚠️ نسبت TO-DO من {K}/{N} = {%} است که از آستانهٔ ۲۰٪ بالاتر است.
   احتمالاً برخی از این TO-DO ها قابل اجرا بودند ولی من فرار کردم.
   لیست TO-DO ها را کاربر باید بازبینی کند که آیا واقعاً Manual-required
   بودند یا agent ضعیف کار کرده."

**یادآوری همیشگی:** اگر در آینده قابلیت‌های شما گسترش پیدا کرد و توانستید
یکی از موارد لیست سفید را خودکار انجام دهید (مثلاً managed credential
injection، یا integration پولی automate شود)، انجام دهید و TO-DO نسازید.
لیست سفید بسته است ولی **بسته از پایین** (می‌تواند کوچک‌تر شود اگر
قابلیت‌ها رشد کنند، ولی هرگز بزرگ‌تر نشود برای فرار).

**اگر هیچ بخش Manual-required نبود (تمام تسک Auto-capable است)**:
  → فایل TO-DO **نساز**. فولدر TO-DO/ باید پاک و معنادار بماند.
  → اگر برای این task از قبل `TO-DO/todo-task-{task_id_first_8}.md` بود
     (یعنی در run قبلی نیاز به دخالت کاربر بود ولی الان نه): فایل قدیمی
     را پاک کن و entry را از `TO-DO/_index.json` حذف کن.

**اگر بخش Manual-required دارد** (همه‌جانبه یا hybrid):
  1. فولدر TO-DO/ را در ریشه ریپو ایجاد کن اگر نیست
  2. فایل `TO-DO/todo-task-{task_id_first_8}.md` بساز با front-matter
     شامل: task_id, task_title, execution_priority, created_at,
     updated_at, status: "pending"
     و در بدنه: «چرا این فایل ساخته شد»، «وضعیت بخش‌های خودکار»
     (commit ها reference)، «کارهایی که باید انجام دهی» با اولویت
     بالا/متوسط/پایین به ترتیب، «وقتی این کارها را تمام کردی»
  3. `TO-DO/_index.json` را با **merge** آپدیت کن (نه overwrite):
     - فایل موجود را بخوان
     - entry های orphan (فایلشان پاک شده) را حذف کن
     - entry این task را اضافه/replace کن
     - بر اساس execution_priority صعودی مرتب کن
     - ساختار: `{"version":1, "generated_at": ISO, "total": N, "items": [...]}`
  4. این تغییرات TO-DO را در **همان commit کد** شامل کن (نه commit جداگانه)

⛔ **ممنوعات مطلق TO-DO**:
  ❌ ساختن TO-DO برای کاری که می‌توانستی خودت انجام دهی (شلوغی فولدر)
  ❌ overwrite کردن `TO-DO/_index.json` بدون merge (data loss)
  ❌ نگه‌داشتن entry هایی که فایل‌شان پاک شده (broken reference)
  ❌ فراموش کردن نوشتن «خروجی مورد انتظار» در هر آیتم TO-DO

این بخش الزامی است. حتی اگر فکر می‌کنی "این تسک کاملاً auto است و نیازی
به TO-DO نیست"، صریحاً در commit message یا report بنویس:
"بررسی شد — این تسک هیچ بخش Manual-required ندارد، TO-DO ساخته نشد."

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

🔁 **Commit + Push فوری per-task (بسیار مهم برای جریان کار صحیح):**

پس از اتمام پیاده‌سازی این تسک، **بلافاصله** commit کن و **همان موقع**
به default branch (main/master) push کن. سپس به تسک بعدی برو.

✓ چرا این قانون حیاتی است:
  - تسک‌های بعدی ممکن است به فایل‌ها/تغییراتی که این تسک ایجاد کرده
    نیاز داشته باشند. اگر push نکنی، `git pull` بعدی آن‌ها را نمی‌بیند.
  - جمع‌کردن تغییرات چند تسک منجر به conflict های بزرگ می‌شود.
  - اگر در میانه fail کنی، task های push شده ضایع نمی‌شوند.

⛔ ممنوع: "همه task ها را تمام می‌کنم بعد یک‌جا push می‌زنم"
⛔ ممنوع: branch جدا برای task — مستقیم به default branch
⛔ ممنوع: task بعدی بدون push کامل task قبلی

---


---

## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند.)_

```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم و از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....
```

## 📋 چک‌لیست مراحل (15 مرحله)

این تسک به مراحل کوچک‌تر تقسیم شده. **در هر verify خودکار، وضعیت هر مرحله به‌صورت `[ ]` (انجام نشده)، `[~]` (ناقص)، یا `[x]` (انجام شده) به‌روز می‌شود.**
وقتی تمام مراحل `[x]` شدند، تسک به‌طور خودکار به «انجام شده» منتقل می‌شود.

- [ ] **مرحله 1: ایجاد ساختار پایه برای اتصال تلگرام دوطرفه** — این مرحله شامل ایجاد یک ماژول پایه برای اتصال به تلگرام با استفاده از کتابخانه python-telegram-bot (یا aiogram) است. باید یک bot token از BotFather گرفته شود و یک handler ساده برای دریافت پیام‌های متنی و ارسال پاسخ اولیه پیاده‌سازی شود. خارج از این مرحله: پیاده‌سازی منطق کسب‌وکار، اتصال به دیتابیس، 
- [ ] **مرحله 2: پیاده‌سازی سیستم نوتیفیکیشن تلگرام برای رویدادهای سیستم** — این مرحله شامل ایجاد یک سرویس نوتیفیکیشن است که رویدادهای مشخصی از سیستم (مثلاً ثبت‌نام کاربر جدید، تکمیل تمرین، خطاهای سیستمی) را به کاربران مشخصی از طریق تلگرام ارسال کند. باید یک صف پیام یا callback برای رویدادها ایجاد شود. خارج از این مرحله: تعیین اینکه کدام رویدادها نوتیفیکیشن شوند (بعداً confi
- [ ] **مرحله 3: پیاده‌سازی احراز هویت و اتصال کاربران سیستم به حساب تلگرام** — این مرحله شامل ایجاد مکانیزمی برای اتصال حساب کاربری سیستم به حساب تلگرام است. کاربر باید بتواند از طریق یک کد یکتا یا لینک، حساب خود را به ربات متصل کند. باید جدول یا دیتابیسی برای ذخیره mapping بین user_id سیستم و chat_id تلگرام ایجاد شود. خارج از این مرحله: پیاده‌سازی دستورات کنترلی. نکته حیاتی: 
- [ ] **مرحله 4: ایجاد دستورات پایه کنترلی تلگرام (مشاهده وضعیت سیستم)** — این مرحله شامل پیاده‌سازی دستورات پایه تلگرام برای کاربران احراز هویت شده است: /status (وضعیت سیستم)، /help (راهنما)، /profile (مشاهده پروفایل). باید handlerهای مربوطه ایجاد شود و پاسخ‌های مناسب از سیستم گرفته شود. خارج از این مرحله: دستورات مدیریت محتوا یا تمرین. نکته حیاتی: فقط کاربران احراز هویت 
- [ ] **مرحله 5: ایجاد دستورات مدیریت محتوای دروس (مشاهده لیست دروس)** — این مرحله شامل پیاده‌سازی دستور /courses برای مشاهده لیست دروس موجود در سیستم است. باید از دیتابیس دروس خوانده شود و به صورت فرمت‌شده (با شماره و عنوان) نمایش داده شود. خارج از این مرحله: مشاهده محتوای یک درس خاص یا آپلود. نکته حیاتی: باید pagination برای لیست طولانی دروس در نظر گرفته شود.
- [ ] **مرحله 6: مشاهده محتوای یک درس خاص از طریق تلگرام** — این مرحله شامل پیاده‌سازی دستور /course <id> برای مشاهده جزئیات یک درس خاص (توضیحات، لیست جلسات، فایل‌ها) است. باید از دیتابیس اطلاعات درس و جلسات آن خوانده شود و با دکمه‌های inline برای انتخاب جلسات نمایش داده شود. خارج از این مرحله: آپلود محتوا یا مشاهده تمرینات. نکته حیاتی: باید از inline keyboar
- [ ] **مرحله 7: آپلود فایل‌های درسی از طریق تلگرام** — این مرحله شامل پیاده‌سازی قابلیت آپلود فایل (PDF، تصویر، ویدئو) برای یک درس خاص از طریق تلگرام است. کاربر باید بتواند فایلی را به ربات ارسال کند و ربات آن را در مسیر مشخصی ذخیره کرده و به درس مربوطه متصل کند. باید دستور /upload <course_id> برای شروع فرآیند آپلود ایجاد شود. خارج از این مرحله: مدیریت 
- [ ] **مرحله 8: ایجاد دستورات مدیریت تمرینات (مشاهده لیست تمرینات)** — این مرحله شامل پیاده‌سازی دستور /exercises برای مشاهده لیست تمرینات موجود در سیستم است. باید تمرینات به همراه وضعیت (انجام شده/انجام نشده) نمایش داده شوند. خارج از این مرحله: انجام تمرین یا اتصال به هوش مصنوعی. نکته حیاتی: باید از دیتابیس وضعیت هر کاربر برای نمایش وضعیت تمرینات استفاده شود.
- [ ] **مرحله 9: اتصال به ماژول 'تمرین با استاد هوش مصنوعی' از طریق تلگرام** — این مرحله شامل ایجاد دستور /ai_exercise <id> برای شروع یک جلسه تمرین با استاد هوش مصنوعی از طریق تلگرام است. باید یک session جدید در ماژول AI ایجاد شود و کاربر به آن متصل شود. خارج از این مرحله: پیاده‌سازی خود هوش مصنوعی یا مدیریت مکالمه. نکته حیاتی: باید state management برای session‌های همزمان در 
- [ ] **مرحله 10: مدیریت موضوعات تمرین از طریق تلگرام (انتخاب و تغییر موضوع)** — این مرحله شامل پیاده‌سازی قابلیت انتخاب و تغییر موضوع تمرین در session AI از طریق تلگرام است. کاربر باید بتواند با دستور /topic <id> موضوع جدیدی انتخاب کند یا با /topics لیست موضوعات موجود را ببیند. خارج از این مرحله: مکالمه با هوش مصنوعی. نکته حیاتی: باید موضوعات از ماژول AI دریافت شوند و session ب
- [ ] **مرحله 11: پیاده‌سازی مکالمه متنی با هوش مصنوعی از طریق تلگرام** — این مرحله شامل اتصال پیام‌های متنی تلگرام به ماژول AI است. هر پیام متنی کاربر در session فعال به AI ارسال شده و پاسخ دریافت و به کاربر نمایش داده می‌شود. خارج از این مرحله: پیام‌های صوتی. نکته حیاتی: باید context مکالمه حفظ شود و session timeout مدیریت گردد.
- [ ] **مرحله 12: پیاده‌سازی دریافت و ارسال پیام صوتی به هوش مصنوعی** — این مرحله شامل قابلیت دریافت پیام صوتی از کاربر تلگرام، ارسال آن به ماژول AI برای تحلیل و استخراج متن، و دریافت پاسخ صوتی از AI و ارسال آن به کاربر است. باید از کتابخانه‌های تبدیل صوت به متن (مثلاً Whisper) و متن به صوت استفاده شود. خارج از این مرحله: ذخیره‌سازی فایل‌های صوتی. نکته حیاتی: باید فرمت‌
- [ ] **مرحله 13: همگام‌سازی دوطرفه پیام‌ها بین تلگرام و چت وب** — این مرحله شامل ایجاد مکانیزمی برای همگام‌سازی پیام‌ها بین تلگرام و چت وب است. هر پیام ارسال شده در تلگرام باید در چت وب نیز نمایش داده شود و بالعکس. باید از WebSocket یا polling برای实时 همگام‌سازی استفاده شود. خارج از این مرحله: مدیریت فایل‌های رسانه‌ای. نکته حیاتی: باید از conflict avoidance و dedup
- [ ] **مرحله 14: مدیریت خطاها و لاگینگ در سیستم تلگرام** — این مرحله شامل پیاده‌سازی سیستم مدیریت خطا برای ربات تلگرام است. خطاهای رایج (اتصال، API، دیتابیس) باید به صورت دوستانه به کاربر نمایش داده شود و در لاگ‌ها ثبت شود. باید از logging module با سطوح مختلف (INFO, ERROR, DEBUG) استفاده شود. خارج از این مرحله: مانیتورینگ پیشرفته. نکته حیاتی: خطاهای حساس (
- [ ] **مرحله 15: تست و دیباگ کامل سیستم تلگرام دوطرفه** — این مرحله شامل تست کامل تمامی قابلیت‌های پیاده‌سازی شده است: ارسال و دریافت پیام، نوتیفیکیشن، احراز هویت، دستورات کنترلی، مدیریت محتوا، اتصال به AI، و همگام‌سازی. باید تست‌های unit و integration نوشته شود. خارج از این مرحله: تست load یا performance. نکته حیاتی: باید سناریوهای edge case (مثل ارسال هم

---

# 🔹 مرحله 1: ایجاد ساختار پایه برای اتصال تلگرام دوطرفه

**Scope:** این مرحله شامل ایجاد یک ماژول پایه برای اتصال به تلگرام با استفاده از کتابخانه python-telegram-bot (یا aiogram) است. باید یک bot token از BotFather گرفته شود و یک handler ساده برای دریافت پیام‌های متنی و ارسال پاسخ اولیه پیاده‌سازی شود. خارج از این مرحله: پیاده‌سازی منطق کسب‌وکار، اتصال به دیتابیس، یا مدیریت کاربران. نکته حیاتی: حتماً از webhook یا polling استفاده شود و امنیت token رعایت گردد.
**Key terms:** python-telegram-bot, aiogram, BotFather, bot token, webhook, polling

**بخش مربوط از متن کاربر:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد ماژول پایه اتصال دوطرفه تلگرام

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:38-48` — `app setup` — محل فعلی تنظیمات سرور Express — پس از این بخش می‌توان endpoint جدید /api/telegram/status را اضافه کرد
  ```jsx
  const app = express();
  const PORT = process.env.PORT || 3001;
  
  // Create HTTP server
  const server = createServer(app);
  
  // Create WebSocket server
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  
  app.use(cors());
  app.use(express.json({ limit: '10mb' }));
  ```
- `backend/server.js:166-169` — `health check endpoint` — الگوی endpoint ساده برای کپی کردن و ایجاد /api/telegram/status — مشابه health check
  ```jsx
  // Health check
  app.get('/api/health', (req, res) => {
    res.json({ status: 'ok', timestamp: new Date().toISOString() });
  });
  ```
- `backend/.env.example:1-8` — `environment variables` — فایل env.example باید با اضافه کردن TELEGRAM_BOT_TOKEN به‌روزرسانی شود
  ```
  # Gemini API Key - Get from https://aistudio.google.com/apikey
  GEMINI_API_KEY=your_gemini_api_key_here
  
  # Port (optional, default 3001)
  PORT=3001
  
  # Firebase config (optional - for data persistence)
  # VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/package.json` (سطر 1) — برای اضافه کردن وابستگی python-telegram-bot (اگر پروژه از Python استفاده می‌کند) یا ایجاد فایل requirements.txt جدید — backend فعلاً Node.js است، بنابراین باید تصمیم گرفته شود که ماژول تلگرام با Python نوشته شود یا Node.js
- `backend/server.js` (سطر 45) — فایل اصلی سرور که باید endpoint جدید /api/telegram/status به آن اضافه شود و در صورت نیاز WebSocket برای ارتباط با ماژول تلگرام
- `frontend/src/App.jsx` (سطر 1) — در آینده ممکن است نیاز به نمایش وضعیت تلگرام در UI باشد — اما خارج از scope این تسک
- `.github/workflows/claude-auto-task.yml` (سطر 1) — فایل CI/CD که ممکن است نیاز به به‌روزرسانی برای نصب وابستگی‌های جدید تلگرام داشته باشد

## 🌐 نقشهٔ وابستگی‌ها
این تسک یک ماژول جدید (telegram_bot) به پروژه اضافه می‌کند که مستقل از backend/server.js فعلی است. backend/server.js باید یک endpoint GET /api/telegram/status داشته باشد که وضعیت اتصال تلگرام را برگرداند. backend/.env.example باید با TELEGRAM_BOT_TOKEN به‌روزرسانی شود. backend/package.json یا یک فایل requirements.txt جدید باید وابستگی python-telegram-bot را شامل شود. frontend/src/App.jsx در این مرحله تغییری نمی‌کند. فایل .github/workflows/claude-auto-task

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد یک ماژول پایه برای اتصال دوطرفه به تلگرام را دارد. این ماژول باید با استفاده از کتابخانه python-telegram-bot یا aiogram پیاده‌سازی شود. یک bot token از BotFather گرفته شود و یک handler ساده برای دریافت پیام‌های متنی و ارسال پاسخ اولیه پیاده‌سازی گردد. خارج از این مرحله: پیاده‌سازی منطق کسب‌وکار، اتصال به دیتابیس، یا مدیریت کاربران. نکته حیاتی: حتماً از webhook یا polling استفاده شود و امنیت token رعایت گردد. بخش مربوط از درخواست اصلی کاربر: 'میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم'. کلیدواژه‌ها: python-telegram-bot, aiogram, BotFather, bot token, webhook, polling. پروژه فعلی یک اپلیکیشن آموزش لهجه لبنانی با React (frontend) و Express.js (backend) است که از Gemini API استفاده می‌کند. backend/server.js شامل WebSocket (خط 45)، APIهای مختلف Gemini (خطوط 56-240)، و تحلیل فایل (خطوط 698+) است. backend/package.json شامل وابستگی‌های express, cors, dotenv, ws, multer, fluent-ffmpeg است. frontend با React و Vite ساخته شده. این تسک یک feature جدید است که نیاز به ایجاد فایل‌های جدید در backend دارد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/services/telegram_bot.py با کلاس TelegramBot که از python-telegram-bot استفاده می‌کند. 2. پیاده‌سازی متد start_polling() برای شروع polling با استفاده از Application.builder().token().build() از کتابخانه python-telegram-bot. 3. پیاده‌سازی handler برای دستور /start که پیام خوش‌آمدگویی ارسال کند. 4. پیاده‌سازی handler برای پیام‌های متنی که پاسخ اولیه 'پیام شما دریافت شد' را برگرداند. 5. ایجاد فایل backend/config/telegram.py برای ذخیره bot token از متغیر محیطی TELEGRAM_BOT_TOKEN. 6. به‌روزرسانی backend/.env.example برای اضافه کردن TELEGRAM_BOT_TOKEN. 7. اضافه کردن وابستگی python-telegram-bot به backend/requirements.txt یا backend/package.json (بسته به زبان). 8. ایجاد endpoint GET /api/telegram/status در backend/server.js برای بررسی وضعیت اتصال تلگرام. 9. اطمینان از استفاده از webhook یا polling با رعایت امنیت token.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 2: پیاده‌سازی سیستم نوتیفیکیشن تلگرام برای رویدادهای سیستم

**Scope:** این مرحله شامل ایجاد یک سرویس نوتیفیکیشن است که رویدادهای مشخصی از سیستم (مثلاً ثبت‌نام کاربر جدید، تکمیل تمرین، خطاهای سیستمی) را به کاربران مشخصی از طریق تلگرام ارسال کند. باید یک صف پیام یا callback برای رویدادها ایجاد شود. خارج از این مرحله: تعیین اینکه کدام رویدادها نوتیفیکیشن شوند (بعداً configurable می‌شود). نکته حیاتی: باید از async استفاده شود تا ارسال پیام‌ها blocking نباشد.
**Key terms:** notification service, event queue, async, telegram bot, message sender

**بخش مربوط از متن کاربر:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی سرویس نوتیفیکیشن تلگرام با صف رویداد async

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:1-12` — `import section` — محل افزودن import سرویس نوتیفیکیشن تلگرام و event queue. باید بعد از خط ۱۲ import جدید اضافه شود.
  ```jsx
  import express from 'express';
  import cors from 'cors';
  import { fileURLToPath } from 'url';
  import { dirname, join } from 'path';
  import dotenv from 'dotenv';
  import { createServer } from 'http';
  import { WebSocketServer, WebSocket } from 'ws';
  import multer from 'multer';
  import fs from 'fs';
  import os from 'os';
  import ffmpeg from 'fluent-ffmpeg';
  import ffmpegStatic from 'ffmpeg-static';
  ```
- `backend/server.js:38-48` — `app initialization and middleware` — محل افزودن راه‌اندازی TelegramNotificationService بعد از ایجاد WebSocket server (بعد از خط ۴۵). باید سرویس با token از env مقداردهی شود.
  ```jsx
  const app = express();
  const PORT = process.env.PORT || 3001;
  
  // Create HTTP server
  const server = createServer(app);
  
  // Create WebSocket server
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  
  app.use(cors());
  app.use(express.json({ limit: '10mb' }));
  ```
- `backend/server.js:166`

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی سیستم نوتیفیکیشن تلگرام برای رویدادهای سیستم را دارد. متن خام کاربر: «میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته». کلیدواژه‌های کاربر: notification service, event queue, async, telegram bot, message sender. این مرحله شامل ایجاد یک سرویس نوتیفیکیشن است که رویدادهای مشخصی از سیستم (مثلاً ثبت‌نام کاربر جدید، تکمیل تمرین، خطاهای سیستمی) را به کاربران مشخصی از طریق تلگرام ارسال کند. باید یک صف پیام یا callback برای رویدادها ایجاد شود. خارج از این مرحله: تعیین اینکه کدام رویدادها نوتیفیکیشن شوند (بعداً configurable می‌شود). نکته حیاتی: باید از async استفاده شود تا ارسال پیام‌ها blocking نباشد. در کد فعلی پروژه (backend/server.js) هیچ سرویس نوتیفیکیشن یا یکپارچه‌سازی با تلگرام وجود ندارد. پروژه از Express.js (backend/server.js خط ۱-۴۸)، WebSocket (خط ۴۵)، و Gemini API (خطوط ۵۶-۱۶۴) استفاده می‌کند. فایل backend/package.json شامل وابستگی‌های express, cors, dotenv, ws, multer, fluent-ffmpeg, ffmpeg-static است. فایل frontend/package.json شامل react, firebase, lucide-react است. هیچ کتابخانه‌ای برای Telegram Bot API (node-telegram-bot-api یا telegraf) در وابستگی‌ها موجود نیست. ساختار پروژه شامل backend/server.js به عنوان entry point backend و frontend/src/App.jsx به عنوان کامپوننت اصلی frontend است. فایل .github/workflows/claude-auto-task.yml workflow خودکار Claude Code را نشان می‌دهد. برای پیاده‌سازی این درخواست، باید یک سرویس جدید در backend ایجاد شود که از کتابخانه node-telegram-bot-api یا telegraf استفاده کند، یک صف رویداد (Event Queue) مبتنی بر async/await یا Bull/BullMQ ایجاد کند، و endpointهایی برای ثبت و مدیریت رویدادها فراهم کند. همچنین باید قابلیت دوطرفه (ارسال و دریافت پیام از تلگرام) پیاده‌سازی شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ۱. ایجاد فایل جدید backend/services/telegramNotificationService.js با کلاس TelegramNotificationService که شامل: (الف) متد initialize(botToken) برای راه‌اندازی بات تلگرام با استفاده از کتابخانه node-telegram-bot-api، (ب) متد sendMessage(chatId, message) برای ارسال پیام async، (ج) EventQueue داخلی مبتنی بر Map یا Bull برای ذخیره رویدادها، (د) متد enqueueEvent(eventType, payload) برای افزودن رویداد به صف، (ه) متد processQueue() که به صورت async رویدادها را پردازش می‌کند، (و) متد registerHandler(eventType, handlerFunction) برای ثبت هندلرهای رویداد. ۲. ایجاد فایل backend/routes/telegramRoutes.js با endpointهای: POST /api/telegram/send برای ارسال پیام، POST /api/telegram/register-event برای ثبت رویداد جدید، GET /api/telegram/status برای بررسی وضعیت بات. ۳. تغییر backend/server.js: import سرویس جدید در خطوط ابتدایی (بعد از خط ۱۲)، راه‌اندازی سرویس در بخش setup (بعد از خط ۴۵)، افزودن routeهای تلگرام (بعد از خط ۱۶۴). ۴. نصب وابستگی جدید: npm install node-telegram-bot-api در backend. ۵. افزودن متغیر محیطی TELEGRAM_BOT_TOKEN به backend/.env.example. ۶. ایجاد فایل backend/services/eventQueue.js برای مدیریت صف رویداد با قابلیت priority و retry. ۷. ایجاد تست واحد برای سرویس در backend/tests/telegramNotification.test.js.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 3: پیاده‌سازی احراز هویت و اتصال کاربران سیستم به حساب تلگرام

**Scope:** این مرحله شامل ایجاد مکانیزمی برای اتصال حساب کاربری سیستم به حساب تلگرام است. کاربر باید بتواند از طریق یک کد یکتا یا لینک، حساب خود را به ربات متصل کند. باید جدول یا دیتابیسی برای ذخیره mapping بین user_id سیستم و chat_id تلگرام ایجاد شود. خارج از این مرحله: پیاده‌سازی دستورات کنترلی. نکته حیاتی: امنیت این اتصال بسیار مهم است و باید از روش‌های امن مثل OTP استفاده شود.
**Key terms:** user authentication, chat_id mapping, OTP, database table, user_id

**بخش مربوط از متن کاربر:**
```
بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن احراز هویت تلگرام با OTP و mapping کاربر

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:38-48` — `app initialization` — محل افزودن middleware جدید و endpointهای احراز هویت تلگرام. بعد از خط 48 (app.use(express.json)) و قبل از خط 56 (Gemini Chat API) endpointهای جدید اضافه شوند.
  ```jsx
  const app = express();
  const PORT = process.env.PORT || 3001;
  
  // Create HTTP server
  const server = createServer(app);
  
  // Create WebSocket server
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  
  app.use(cors());
  app.use(express.json({ limit: '10mb' }));
  ```
- `backend/server.js:53-54` — `GEMINI_API_KEY constant` — الگوی مشابه برای خواندن TELEGRAM_BOT_TOKEN از environment variables. یک const TELEGRAM_BOT_TOKEN = process.env.TELEGRAM_BOT_TOKEN; در کنار این خط اضافه شود.
  ```jsx
  const GEMINI_API_KEY = process.env.GEMINI_API_KEY;
  ```
- `backend/.env.example:1-8` — `environment variables` — افزودن متغیر محیطی TELEGRAM_BOT_TOKEN به این فایل. این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند.
  ```
  # Gemini API Key - Get from https://aistudio.google.com/apikey
  GEMINI_API_KEY=your_gemini_api_key_here
  
  # Port (optional, default 3001)
  PORT=3001
  
  # Firebase config (optional - for data persistence)
  # VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/package.json` (سطر 1) — برای افزودن وابستگی‌های جدید (مثلاً node-telegram-bot-api یا axios برای ارتباط با Telegram Bot API) باید این فایل به‌روز شود.
- `frontend/index.html` (سطر 11) — Firebase config در خطوط 11-18 موجود است. اگر از Firebase Firestore برای ذخیره mapping استفاده شود، این config مورد نیاز است.
- `.github/workflows/claude-auto-task.yml` (سطر 1) — اگر نیاز به نصب وابستگی‌های جدید باشد، workflow ممکن است نیاز به به‌روزرسانی داشته باشد (مثلاً npm install در backend).
- `backend/server.js` (سطر 56) — فایل اصلی backend که endpointهای جدید به آن اضافه می‌شود. خطوط 56-114 (Gemini Chat API) و 117-164 (Gemini TTS API) الگوی endpointهای موجود را نشان می‌دهند.

## 🌐 نقشهٔ وابستگی‌ها
این قابلیت به فایل جدید backend/services/telegramAuth.js وابسته است که شامل توابع generateOTP, verifyOTP, saveMapping, getChatIdByUserId, getUserIdByChatId خواهد بود. endpointهای جدید در backend/server.js (بعد از خط 48) به این سرویس وابسته هستند. برای ذخیره‌سازی mapping، یا از فایل JSON (backend/data/telegram-mappings.json) استفاده می‌ش

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی احراز هویت و اتصال حساب کاربری سیستم به حساب تلگرام را دارد. این مرحله شامل ایجاد مکانیزمی برای اتصال حساب کاربری سیستم به حساب تلگرام است. کاربر باید بتواند از طریق یک کد یکتا یا لینک، حساب خود را به ربات متصل کند. باید جدول یا دیتابیسی برای ذخیره mapping بین user_id سیستم و chat_id تلگرام ایجاد شود. خارج از این مرحله: پیاده‌سازی دستورات کنترلی. نکته حیاتی: امنیت این اتصال بسیار مهم است و باید از روش‌های امن مثل OTP استفاده شود. بخش مربوط از درخواست اصلی کاربر: 'بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم'. کلیدواژه‌ها: user authentication, chat_id mapping, OTP, database table, user_id. در کد فعلی پروژه (backend/server.js) هیچ endpoint یا سرویسی برای احراز هویت تلگرام وجود ندارد. پروژه از Express.js در backend و React در frontend استفاده می‌کند. فایل backend/server.js شامل APIهای Gemini (chat در خط 56، TTS در خط 117، تحلیل فایل در خط 698) و health check (خط 167) است. فایل frontend/index.html شامل Firebase config (خطوط 11-18) است. فایل .github/workflows/claude-auto-task.yml شامل CI/CD خودکار است. backend/package.json وابستگی‌های express, cors, dotenv, ws, multer, fluent-ffmpeg, ffmpeg-static را دارد. frontend/package.json وابستگی‌های firebase, lucide-react, react, react-dom را دارد. هیچ جدول دیتابیس یا mapping user_id به chat_id در کد فعلی وجود ندارد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک فایل جدید backend/services/telegramAuth.js برای مدیریت احراز هویت تلگرام. 2. در این فایل، یک تابع generateOTP(userId) ایجاد کن که یک کد 6 رقمی OTP تولید کرده و در حافظه موقت (Map) با زمان انقضا 5 دقیقه ذخیره کند. 3. یک endpoint جدید POST /api/telegram/request-otp در backend/server.js اضافه کن که userId را دریافت کرده و OTP برگرداند. 4. یک endpoint جدید POST /api/telegram/verify-otp اضافه کن که userId, otp, chatId را دریافت کرده و mapping را ذخیره کند. 5. یک جدول/دیتابیس ساده با استفاده از فایل JSON (backend/data/telegram-mappings.json) یا Firebase Firestore (با توجه به Firebase config در frontend/index.html خطوط 11-18) برای ذخیره mapping بین user_id سیستم و chat_id تلگرام ایجاد کن. 6. یک تابع getChatIdByUserId(userId) و getUserIdByChatId(chatId) برای جستجوی mapping ایجاد کن. 7. لاگ‌گیری مناسب برای تمام عملیات OTP و mapping اضافه کن. 8. مستندات API در backend/README.md یا فایل جداگانه اضافه کن.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 4: ایجاد دستورات پایه کنترلی تلگرام (مشاهده وضعیت سیستم)

**Scope:** این مرحله شامل پیاده‌سازی دستورات پایه تلگرام برای کاربران احراز هویت شده است: /status (وضعیت سیستم)، /help (راهنما)، /profile (مشاهده پروفایل). باید handlerهای مربوطه ایجاد شود و پاسخ‌های مناسب از سیستم گرفته شود. خارج از این مرحله: دستورات مدیریت محتوا یا تمرین. نکته حیاتی: فقط کاربران احراز هویت شده باید به این دستورات دسترسی داشته باشند.
**Key terms:** /status, /help, /profile, command handler, authentication middleware

**بخش مربوط از متن کاربر:**
```
بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی دستورات پایه تلگرام: /status، /help، /profile با احراز هویت

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:167-169` — `GET /api/health` — این endpoint برای دستور /status استفاده می‌شود تا وضعیت سلامت سیستم را برگرداند.
  ```jsx
  app.get('/api/health', (req, res) => {
    res.json({ status: 'ok', timestamp: new Date().toISOString() });
  });
  ```
- `backend/server.js:195-240` — `GET /api/test-gemini` — این endpoint برای دستور /status استفاده می‌شود تا وضعیت API key را بررسی کند.
  ```jsx
  app.get('/api/test-gemini', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured', keyExists: false });
    }
    ...
    res.json({
      success: true,
      text: text,
      keyPrefix: GEMINI_API_KEY.substring(0, 10) + '...'
    });
  });
  ```
- `backend/.env.example:1-8` — `Environment variables` — به این فایل باید متغیرهای TELEGRAM_BOT_TOKEN و TELEGRAM_ALLOWED_USERS اضافه شود.
  ```
  # Gemini API Key - Get from https://aistudio.google.com/apikey
  GEMINI_API_KEY=your_gemini_api_key_here
  
  # Port (optional, default 3001)
  PORT=3001
  
  # Firebase config (optional - for data persistence)
  # VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/package.json` (سطر 10) — برای اضافه کردن وابستگی node-telegram-bot-api یا کتابخانه مشابه نیاز به بروزرسانی دارد.
- `backend/server.js` (سطر 698) — برای اضافه کردن endpoint POST /api/telegram/webhook و import سرویس تلگرام نیاز به تغییر دارد.
- `backend/.env.example` (سطر 1) — برای اضافه کردن متغیرهای محیطی جدید تلگرام نیاز به بروزرسانی دارد.
- `frontend/src/App.jsx`

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد دستورات پایه کنترلی تلگرام برای مشاهده وضعیت سیستم را داده است. این مرحله شامل پیاده‌سازی دستورات پایه تلگرام برای کاربران احراز هویت شده است: /status (وضعیت سیستم)، /help (راهنما)، /profile (مشاهده پروفایل). باید handlerهای مربوطه ایجاد شود و پاسخ‌های مناسب از سیستم گرفته شود. خارج از این مرحله: دستورات مدیریت محتوا یا تمرین. نکته حیاتی: فقط کاربران احراز هویت شده باید به این دستورات دسترسی داشته باشند. بخش مربوط از درخواست اصلی کاربر: "بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم". کلیدواژه‌ها: /status, /help, /profile, command handler, authentication middleware. در کد فعلی پروژه (backend/server.js) هیچ endpoint یا middleware مرتبط با تلگرام وجود ندارد. پروژه از Express.js در backend و React در frontend استفاده می‌کند. فایل .github/workflows/claude-auto-task.yml نشان‌دهنده CI/CD خودکار است. backend/.env.example حاوی متغیرهای محیطی برای GEMINI_API_KEY و Firebase است. backend/package.json شامل وابستگی‌های express, cors, dotenv, ws, multer, fluent-ffmpeg, pdf-parse است. frontend/package.json شامل react, firebase, lucide-react است. برای پیاده‌سازی این تسک، نیاز به ایجاد یک سرویس تلگرام جدید (مثلاً backend/services/telegram.js) و اضافه کردن webhook endpoint به backend/server.js است. همچنین نیاز به middleware احراز هویت برای بررسی کاربران مجاز و handlerهای جداگانه برای هر دستور است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/services/telegram.js شامل: (الف) کلاس TelegramBot با متدهای handleCommand, authenticateUser, sendMessage. (ب) متد handleStatus برای دستور /status که وضعیت سیستم (uptime, API key status, last analysis time) را از backend/server.js endpoints (GET /api/health در خط 167-169 و GET /api/test-gemini در خط 195-240) می‌گیرد. (ج) متد handleHelp برای دستور /help که لیست دستورات مجاز را برمی‌گرداند. (د) متد handleProfile برای دستور /profile که اطلاعات کاربر احراز هویت شده را از Firebase یا یک دیتابیس محلی می‌خواند. (ه) middleware احراز هویت که chatId کاربر را با لیست کاربران مجاز (که می‌تواند در متغیر محیطی TELEGRAM_ALLOWED_USERS یا یک فایل JSON ذخیره شود) مقایسه می‌کند. 2. اضافه کردن endpoint جدید POST /api/telegram/webhook به backend/server.js (حدود خط 698) که پیام‌های دریافتی از تلگرام را پردازش کند. 3. اضافه کردن متغیرهای محیطی TELEGRAM_BOT_TOKEN و TELEGRAM_ALLOWED_USERS به backend/.env.example. 4. نصب کتابخانه node-telegram-bot-api (یا استفاده از fetch خام برای Webhook). 5. ایجاد فایل تست backend/tests/telegram.test.js برای تست handlerها.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 5: ایجاد دستورات مدیریت محتوای دروس (مشاهده لیست دروس)

**Scope:** این مرحله شامل پیاده‌سازی دستور /courses برای مشاهده لیست دروس موجود در سیستم است. باید از دیتابیس دروس خوانده شود و به صورت فرمت‌شده (با شماره و عنوان) نمایش داده شود. خارج از این مرحله: مشاهده محتوای یک درس خاص یا آپلود. نکته حیاتی: باید pagination برای لیست طولانی دروس در نظر گرفته شود.
**Key terms:** /courses, course list, database query, pagination, telegram keyboard

**بخش مربوط از متن کاربر:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد دستور /courses برای مشاهده لیست دروس با pagination

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:51-52` — `express.static middleware` — بعد از این خط، route جدید /api/courses باید اضافه شود. همچنین نیاز به import فایل routes/courses.js در ابتدای فایل (بعد از خط 12).
  ```jsx
  app.use(express.static(join(__dirname, '../frontend/dist')));
  ```
- `backend/server.js:56-114` — `app.post('/api/gemini/chat')` — الگوی endpoint موجود برای ساخت endpoint جدید /api/courses با متد GET. ساختار async handler و error handling مشابه استفاده شود.
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => { ... });
  ```
- `frontend/index.html:11-18` — `Firebase Configuration` — این config نشان می‌دهد که Firebase در frontend تنظیم شده است. برای backend نیز می‌توان از Firebase Admin SDK استفاده کرد یا یک دیتابیس ساده‌تر مثل SQLite (با better-sqlite3) پیاده‌سازی کرد.
  ```
  var __firebase_config = JSON.stringify({ apiKey: "AIzaSyAMFtV0zIMKKe0bIr68Z1wTORQ1jQpsv9Q", authDomain: "labaneseapp.firebaseapp.com", projectId: "labaneseapp", storageBucket: "labaneseapp.firebasestorage.app", messagingSenderId: "951874597795", appId: "1:951874597795:web:00745327993adad760a016" });
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack شناسایی‌شده

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/.env.example` (سطر 8) — شامل Firebase config نمونه است. برای راه‌اندازی دیتابیس Firebase در backend نیاز به تنظیم متغیرهای محیطی مشابه (FIREBASE_SERVICE_ACCOUNT) خواهد بود.
- `backend/package.json` (سطر 1) — برای اضافه کردن وابستگی‌های جدید مانند firebase-admin یا better-sqlite3 باید این فایل به‌روزرسانی شود.
- `frontend/src/App.jsx` (سطر 1) — اگر قرار باشد لیست دروس در frontend نیز نمایش داده شود، این کامپوننت باید از API جدید /api/courses استفاده کند. اما در این مرحله فقط backend مد نظر است.

## 🌐 نقشهٔ وابستگی‌ها
این تسک به فایل‌های backend/server.js (برای اضافه کردن route جدید)، backend/.env.example (برای تنظیمات دیتابیس)، backend/package.json (برای اضافه کردن وابستگی firebase-admin یا better-sqlite3)، و backend/services/courseService.js (فایل جدید برای منطق دیتابیس) وابسته است. همچنین frontend/index.html حاوی Firebase config است که می‌تواند برای اتصال backend به Firebase استفاده شود. اگر از Firebase استفاده شود، نیاز به فایل credentials (مثل firebase-service-account.json) خواهد بود که در .gitignore اضافه شود.

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد دستور مدیریت محتوای دروس با دستور /courses برای مشاهده لیست دروس موجود در سیستم را دارد. این دستور باید از دیتابیس دروس خوانده شود و به صورت فرمت‌شده (با شماره و عنوان) نمایش داده شود. خارج از این مرحله: مشاهده محتوای یک درس خاص یا آپلود. نکته حیاتی: باید pagination برای لیست طولانی دروس در نظر گرفته شود. بخش مربوط از درخواست اصلی کاربر: 'و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...'. کلیدواژه‌ها: /courses, course list, database query, pagination, telegram keyboard. با بررسی کد واقعی پروژه در backend/server.js (خطوط 1-1403) مشخص شد که این فایل شامل APIهای مربوط به Gemini (Chat, TTS, File Analysis) و WebSocket است، اما هیچ endpoint یا منطق دیتابیسی برای مدیریت دروس (courses) وجود ندارد. همچنین frontend/index.html شامل Firebase Config (apiKey: AIzaSyAMFtV0zIMKKe0bIr68Z1wTORQ1jQpsv9Q, authDomain: labaneseapp.firebaseapp.com, projectId: labaneseapp) است که می‌توان از Firebase Firestore به عنوان دیتابیس استفاده کرد. فایل backend/.env.example نیز اشاره به Firebase config دارد. بنابراین نیاز به ایجاد یک سیستم دیتابیس (احتمالاً Firebase Firestore یا SQLite) و یک endpoint جدید برای /courses با پشتیبانی از pagination است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک فایل جدید backend/services/courseService.js برای مدیریت عملیات دیتابیس دروس (خواندن لیست با pagination). 2. ایجاد یک فایل جدید backend/routes/courses.js برای تعریف route /api/courses با متد GET که پارامترهای page و limit را دریافت کند. 3. در backend/server.js، import و استفاده از route جدید (app.use('/api/courses', courseRoutes)) در خطوط بعد از خط 51 (بعد از express.static). 4. پیاده‌سازی تابع getCourses(page, limit) در courseService.js که از Firebase Firestore (با توجه به config در frontend/index.html خطوط 11-18) یا یک دیتابیس ساده JSON استفاده کند. 5. اضافه کردن pagination metadata در پاسخ (total, page, limit, totalPages). 6. ایجاد یک endpoint آزمایشی GET /api/courses برای تست با curl یا مرورگر. 7. مستندسازی API در docs.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 6: مشاهده محتوای یک درس خاص از طریق تلگرام

**Scope:** این مرحله شامل پیاده‌سازی دستور /course <id> برای مشاهده جزئیات یک درس خاص (توضیحات، لیست جلسات، فایل‌ها) است. باید از دیتابیس اطلاعات درس و جلسات آن خوانده شود و با دکمه‌های inline برای انتخاب جلسات نمایش داده شود. خارج از این مرحله: آپلود محتوا یا مشاهده تمرینات. نکته حیاتی: باید از inline keyboards برای ناوبری استفاده شود.
**Key terms:** /course, inline keyboard, course details, sessions list, database

**بخش مربوط از متن کاربر:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن دستور /course برای مشاهده جزئیات درس از طریق تلگرام

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:38-42` — `app, server, PORT` — اینجا باید ربات تلگرام به سرور اضافه شود — یا webhook روی همین سرور یا polling جداگانه.
  ```jsx
  const app = express();
  const PORT = process.env.PORT || 3001;
  
  // Create HTTP server
  const server = createServer(app);
  ```
- `backend/server.js:56-114` — `app.post('/api/gemini/chat', ...)` — الگوی endpointهای موجود برای ساختاردهی handlerهای جدید قابل استفاده است.
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
      ...
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack شناسایی‌شده: JavaScript (Node.js backend با Express، React frontend با Vite، Firebase). کتابخانه‌های مرتبط: node-telegram-bot-api یا telegraf برای تلگرام، firebase-admin برای دیتابیس (در صورت استفاده از Firebase).

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/package.json` (سطر 1) — برای افزودن وابستگی node-telegram-bot-api یا telegraf نیاز به تغییر دارد.
- `backend/.env.example` (سطر 1) — برای افزودن متغیر محیطی TELEGRAM_BOT_TOKEN نیاز به به‌روزرسانی دارد.
- `frontend/src/App.jsx` (سطر 1) — اگر دیتابیس مشترک باشد (مثلاً Firebase)، ممکن است نیاز به هماهنگی ساختار داده‌ها داشته باشد.

## 🌐 نقشهٔ وابستگی‌ها
این feature جدید به فایل‌های backend/server.js (برای ثبت ربات تلگرام و webhook)، backend/package.json (برای افزودن وابستگی تلگرام)، backend/.env.example (برای افزودن TELEGRAM_BOT_TOKEN)، و یک فایل جدید backend/services/telegramBot.js و backend/services/database.js وابسته است. همچنین نیاز به دیتابیس (Firebase یا SQLite) برای ذخیره دروس و جلسات دارد که ممکن است با frontend/src/App.jsx اشتراک داده شود.

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی دستور /course <id> در تلگرام برای مشاهده جزئیات یک درس خاص (توضیحات، لیست جلسات، فایل‌ها) را دارد. این دستور باید از دیتابیس اطلاعات درس و جلسات آن را بخواند و با دکمه‌های inline keyboard برای انتخاب جلسات نمایش دهد. کاربر تأکید کرده که آپلود محتوا یا مشاهده تمرینات خارج از این مرحله است و نکته حیاتی استفاده از inline keyboards برای ناوبری است. همچنین کاربر اشاره کرده: 'و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...'. کلیدواژه‌ها: /course, inline keyboard, course details, sessions list, database. در کد فعلی پروژه (backend/server.js) هیچ endpoint یا handler مربوط به تلگرام یا دیتابیس دروس وجود ندارد. فایل‌های backend/server.js (خطوط 1-1403) شامل APIهای مربوط به Gemini (Chat, TTS, File Analysis) و health check است. فایل frontend/index.html شامل Firebase config و Inspector Bridge است. فایل .github/workflows/claude-auto-task.yml مربوط به CI/CD است. فایل‌های package.json و package-lock.json وابستگی‌های پروژه را نشان می‌دهند. هیچ فایل تلگرام بات یا دیتابیس در پروژه وجود ندارد — این یک feature جدید است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک فایل جدید backend/services/telegramBot.js برای مدیریت ربات تلگرام. 2. ایجاد یک فایل جدید backend/services/database.js برای اتصال به دیتابیس (مثلاً Firebase یا SQLite) و ذخیره/بازیابی اطلاعات دروس و جلسات. 3. پیاده‌سازی تابع handleCourseCommand در telegramBot.js که دستور /course <id> را پردازش کند: از دیتابیس درس با id مشخص را بخواند، اگر درس وجود داشت، پیامی شامل توضیحات درس و لیست جلسات با دکمه‌های inline keyboard برای هر جلسه ارسال کند. 4. پیاده‌سازی تابع handleSessionCallback برای مدیریت کلیک روی دکمه‌های جلسات (نمایش جزئیات جلسه). 5. ثبت webhook یا polling برای ربات تلگرام در backend/server.js (خطوط 38-42). 6. استفاده از کتابخانه node-telegram-bot-api یا telegraf برای ارتباط با Telegram API. 7. افزودن وابستگی به package.json backend. 8. ایجاد دیتابیس نمونه (مثلاً فایل JSON یا Firebase collection) با ساختار courses و sessions.

## 💡 نمونه‌های قبل/بعد
**افزودن ربات تلگرام به backend/server.js**

_قبل:_
```
const app = express();
const PORT = process.env.PORT || 3001;
const server = createServer(app);
```

_بعد:_
```
const app = express();
const PORT = process.env.PORT || 3001;
const server = createServer(app);

// Telegram Bot
import { startTelegramBot } from './services/telegramBot.js';
const bot = startTelegramBot(process.env.TELEGRAM_BOT_TOKEN);
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 7: آپلود فایل‌های درسی از طریق تلگرام

**Scope:** این مرحله شامل پیاده‌سازی قابلیت آپلود فایل (PDF، تصویر، ویدئو) برای یک درس خاص از طریق تلگرام است. کاربر باید بتواند فایلی را به ربات ارسال کند و ربات آن را در مسیر مشخصی ذخیره کرده و به درس مربوطه متصل کند. باید دستور /upload <course_id> برای شروع فرآیند آپلود ایجاد شود. خارج از این مرحله: مدیریت دسته‌بندی فایل‌ها. نکته حیاتی: محدودیت حجم فایل (حداکثر ۵۰MB) و نوع فایل‌های مجاز باید بررسی شود.
**Key terms:** /upload, file handler, file storage, course attachment, size limit

**بخش مربوط از متن کاربر:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن قابلیت آپلود فایل درسی از طریق تلگرام با دستور /upload

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:25-33` — `multer storage configuration` — تنظیمات فعلی multer با محدودیت 500MB. برای تلگرام باید محدودیت به 50MB کاهش یابد و مسیر ذخیره به backend/uploads/courses/<course_id>/files/ تغییر کند.
  ```jsx
  const storage = multer.diskStorage({
    destination: (req, file, cb) => cb(null, uploadDir),
    filename: (req, file, cb) => cb(null, `${Date.now()}-${file.originalname}`)
  });
  
  const upload = multer({
    storage: storage,
    limits: { fileSize: 500 * 1024 * 1024 } // 500MB max per file
  });
  ```
- `backend/server.js:698-715` — `app.post('/api/analyze-files')` — این endpoint فعلی برای تحلیل فایل با Gemini است. endpoint جدید تلگرام باید مشابه این ساختار اما با محدودیت 50MB و ذخیره در مسیر درسی باشد.
  ```jsx
  app.post('/api/analyze-files', upload.array('files', 10), handleMulterError, async (req, res) => {
    console.log('Received file analysis request');
  
    if (!GEMINI_API_KEY) {
      console.error('API key not configured');
      return res.status(500).json({ error: 'API key not configured' });
    }
  
    try {
      const files = req.files || [];
      const textContent = req.body.textContent || '';
      const userInstructions = req.body.userInstructions || '';
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/.env.example` (سطر 1) — نیازمند افزودن متغیرهای محیطی جدید: TELEGRAM_BOT_TOKEN, TELEGRAM_WEBHOOK_URL
- `frontend/index.html` (سطر 11) — شامل Firebase config (خطوط 11-18) که می‌توان برای ذخیره‌سازی metadata فایل‌ها استفاده کرد
- `backend/package.json` (سطر 1) — نیازمند افزودن وابستگی جدید: node-telegram-bot-api یا axios برای Webhook تلگرام
- `frontend/src/App.jsx` — باید صفحه‌ای برای مشاهده فایل‌های آپلودشده هر درس اضافه شود (بخش 'بتونم محتوای دروس رو هم

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی قابلیت آپلود فایل (PDF، تصویر، ویدئو) برای یک درس خاص از طریق تلگرام را دارد. کاربر باید بتواند فایلی را به ربات ارسال کند و ربات آن را در مسیر مشخصی ذخیره کرده و به درس مربوطه متصل کند. باید دستور /upload <course_id> برای شروع فرآیند آپلود ایجاد شود. خارج از این مرحله: مدیریت دسته‌بندی فایل‌ها. نکته حیاتی: محدودیت حجم فایل (حداکثر ۵۰MB) و نوع فایل‌های مجاز باید بررسی شود. بخش مربوط از درخواست اصلی کاربر: "و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ..." کلیدواژه‌ها: /upload, file handler, file storage, course attachment, size limit. در کد فعلی پروژه، backend/server.js از multer برای آپلود فایل استفاده می‌کند (خطوط 25-33) با محدودیت 500MB و ذخیره در مسیر tmp. endpoint /api/analyze-files در خط 698 فایل‌ها را دریافت و پردازش می‌کند. frontend/index.html شامل Firebase config است (خطوط 11-18) که می‌توان از آن برای ذخیره‌سازی استفاده کرد. backend/.env.example نشان می‌دهد که Firebase به‌عنوان option برای persistence در نظر گرفته شده. پروژه از Express.js در backend و React/Vite در frontend استفاده می‌کند. هیچ endpoint یا ربات تلگرامی در کد فعلی وجود ندارد — این یک feature جدید است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک ربات تلگرام جدید (یا استفاده از ربات موجود) با Webhook به backend. 2. افزودن endpoint جدید در backend/server.js برای دریافت Webhook تلگرام: POST /api/telegram/webhook. 3. پیاده‌سازی هندلر برای دستور /upload <course_id> که: (الف) course_id را validate کند، (ب) از کاربر بخواهد فایل را ارسال کند، (ج) فایل دریافتی را با multer ذخیره کند (با محدودیت 50MB به‌جای 500MB فعلی)، (د) نوع فایل را بررسی کند (PDF, image, video)، (ه) فایل را به درس مربوطه متصل کند (ذخیره مسیر و metadata در یک فایل JSON یا Firebase). 4. ایجاد یک storage structure جدید: backend/uploads/courses/<course_id>/files/. 5. افزودن validation برای نوع فایل‌های مجاز (PDF, image/*, video/*). 6. ذخیره metadata فایل شامل: course_id, filename, originalname, mimetype, size, uploadDate, filePath. 7. بازگرداندن پیام تأیید به کاربر تلگرام با شناسه فایل. 8. افزودن endpoint GET /api/courses/:courseId/files برای مشاهده فایل‌های آپلودشده (برای بخش 'بتونم محتوای دروس رو هم از اینجا ببینم').

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 8: ایجاد دستورات مدیریت تمرینات (مشاهده لیست تمرینات)

**Scope:** این مرحله شامل پیاده‌سازی دستور /exercises برای مشاهده لیست تمرینات موجود در سیستم است. باید تمرینات به همراه وضعیت (انجام شده/انجام نشده) نمایش داده شوند. خارج از این مرحله: انجام تمرین یا اتصال به هوش مصنوعی. نکته حیاتی: باید از دیتابیس وضعیت هر کاربر برای نمایش وضعیت تمرینات استفاده شود.
**Key terms:** /exercises, exercise list, status, user progress, database

**بخش مربوط از متن کاربر:**
```
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد دستور /exercises برای مشاهده لیست تمرینات با وضعیت کاربر

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:56-114` — `app.post('/api/gemini/chat', ...)` — الگوی endpoint موجود در backend/server.js که باید برای ایجاد GET /api/exercises دنبال شود. در خطوط 167-169 نیز GET /api/health وجود دارد.
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
  ```
- `frontend/index.html:11-18` — `__firebase_config` — Firebase config موجود در frontend که برای اتصال به Firestore استفاده می‌شود. projectId = labaneseapp
  ```
  var __firebase_config = JSON.stringify({
    apiKey: "AIzaSyAMFtV0zIMKKe0bIr68Z1wTORQ1jQpsv9Q",
    authDomain: "labaneseapp.firebaseapp.com",
    projectId: "labaneseapp",
    storageBucket: "labaneseapp.firebasestorage.app",
    messagingSenderId: "951874597795",
    appId: "1:951874597795:web:00745327993adad760a016"
  });
  ```
- `backend/package.json:1-15` — `dependencies` — وابستگی‌های فعلی backend که فاقد firebase-admin هستند. باید firebase-admin به این لیست اضافه شود.
  ```json
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "ws": "^8.19.0"
  }
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: JavaScript (Node.js backend با Express.js، React frontend با Vite). کتابخانه‌های

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `frontend/src/App.jsx` — کامپوننت اصلی React که احتمالاً صفحه تمرینات را نمایش می‌دهد. باید برای نمایش لیست تمرینات از endpoint جدید استفاده کند.
- `frontend/src/main.jsx` — نقطه ورود React که Firebase را initialize می‌کند. ممکن است نیاز به تغییر در نحوه اتصال به Firebase داشته باشد.
- `frontend/package.json` — شامل وابستگی firebase است که برای ارتباط با Firestore از سمت کلاینت استفاده می‌شود.
- `backend/.env.example` — فایل نمونه env که باید شامل متغیرهای Firebase (FIREBASE_PROJECT_ID, FIREBASE_CLIENT_EMAIL, FIREBASE_PRIVATE_KEY) برای احراز هویت سرور شود.
- `prompt/_index.json` — فایل index تسک‌ها که پس از ایجاد تسک جدید باید به‌روزرسانی شود تا توسط Claude Auto Task Runner شناسایی شود.

## 🌐 نقشهٔ وابستگی‌ها
این تسک به فایل‌های backend/server.js (برای افزودن endpoint جدید)، frontend/index.html (برای استفاده از Firebase config موجود)، backend/package.json (برای افزودن وابستگی firebase-admin)، frontend/src/App.jsx (برای نمایش لیست تمرینات در UI)، frontend/src/main.jsx (برای initialize Firebase) و backend/.env.example (برای افزودن متغیرهای Firebase) وابسته است. همچنین نیاز به ایجاد دیتابیس Firebase Firestore با کلکسیون‌های exercises و user_exercises دارد. فایل frontend/package.json شامل firebase SDK است که برای ارتباط کلاینت با Firestore کافی است.

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد دستور /exercises برای مشاهده لیست تمرینات موجود در سیستم را دارد. این دستور باید تمرینات را به همراه وضعیت (انجام شده/انجام نشده) نمایش دهد. خارج از این مرحله: انجام تمرین یا اتصال به هوش مصنوعی. نکته حیاتی: باید از دیتابیس وضعیت هر کاربر برای نمایش وضعیت تمرینات استفاده شود. کاربر اشاره کرده: 'و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم'. کلیدواژه‌ها: /exercises, exercise list, status, user progress, database. در کد فعلی پروژه (backend/server.js) هیچ endpoint یا دیتابیسی برای مدیریت تمرینات وجود ندارد. پروژه از Express.js در backend و React در frontend استفاده می‌کند. فایل backend/server.js شامل endpointهای /api/gemini/chat، /api/gemini/tts، /api/health، /api/list-models، /api/test-gemini و /api/analyze-files است. هیچ دیتابیسی در پروژه تعریف نشده (فایل backend/package.json فقط وابستگی‌های cors, dotenv, express, ws را دارد). فایل frontend/src/App.jsx و frontend/src/main.jsx در ساختار پروژه موجودند اما محتوایشان deep-read نشده. فایل frontend/package.json شامل react, firebase, lucide-react است. برای پیاده‌سازی این درخواست، نیاز به ایجاد دیتابیس (احتمالاً Firebase که در frontend/index.html کانفیگ شده)، مدل داده تمرینات، endpoint جدید و دستور /exercises است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد مدل داده تمرینات در Firebase Firestore با ساختار: { id, title, description, category, difficulty, createdAt }. 2. ایجاد کلکسیون user_exercises برای ذخیره وضعیت هر کاربر: { userId, exerciseId, completed: boolean, completedAt, score }. 3. افزودن endpoint GET /api/exercises در backend/server.js که لیست تمرینات را با وضعیت کاربر برگرداند. 4. پیاده‌سازی منطق خواندن از Firebase Firestore در endpoint جدید. 5. ایجاد دستور /exercises در ربات تلگرام (اگر وجود دارد) یا endpoint REST. 6. استفاده از Firebase config موجود در frontend/index.html (خطوط 11-18) برای اتصال به دیتابیس. 7. افزودن وابستگی firebase-admin به backend/package.json برای ارتباط با Firestore از سمت سرور.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 9: اتصال به ماژول 'تمرین با استاد هوش مصنوعی' از طریق تلگرام

**Scope:** این مرحله شامل ایجاد دستور /ai_exercise <id> برای شروع یک جلسه تمرین با استاد هوش مصنوعی از طریق تلگرام است. باید یک session جدید در ماژول AI ایجاد شود و کاربر به آن متصل شود. خارج از این مرحله: پیاده‌سازی خود هوش مصنوعی یا مدیریت مکالمه. نکته حیاتی: باید state management برای session‌های همزمان در نظر گرفته شود.
**Key terms:** /ai_exercise, AI session, state management, exercise module, session ID

**بخش مربوط از متن کاربر:**
```
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد دستور تلگرام /ai_exercise برای اتصال به ماژول تمرین با استاد هوش مصنوعی

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:44-45` — `WebSocketServer` — WebSocket موجود برای ارتباطات عمومی. باید یک WebSocket جدید با path '/ws/ai-exercise' اضافه شود یا این WebSocket توسعه یابد تا session‌های AI را نیز پشتیبانی کند.
  ```jsx
  // Create WebSocket server
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/server.js:56-114` — `app.post('/api/gemini/chat')` — این endpoint برای چت با Gemini استفاده می‌شود. می‌توان از آن برای ارسال پیام‌های کاربر در session AI استفاده کرد.
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
  
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
  ```
- `frontend/index.html:32-201` — `Inspector Bridge Script` — این اسکریپت برای ردیابی رویدادهای کاربر (کلیک، اسکرول، تایپ) و ارسال به پنل مدیریت استفاده می‌شود. می‌توان از آن برای ارسال رویدادهای session AI به پنل مدیریت استفاده کرد.
  ```
  <script>
  (function() {
    console.log('🌉 Inspector Bridge: Script starting...');
  
    // جلوگیری از اجرای چندباره
    if (window.__inspectorBridgeLoaded) {
      console.log('🌉 Inspector Bridge: Already loaded, skipping');
      return;
    }
    window.__inspectorBridgeLoaded = true;
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/package.json` (سطر 1) — برای اضافه کردن وابستگی‌های جدید مانند redis (برای state management پیشرفته) یا ws (که قبلاً وجود دارد) نیاز به تغییر دارد.
- `frontend/src/App.jsx` (سطر 1) — کامپوننت اصلی React که باید state management برای session‌های AI را مدیریت کند و WebSocket جدید را متصل کند.
- `backend/.env.example` (سطر 1) — برای اضافه کردن متغیرهای محیطی جدید مانند TELEGRAM_BOT_TOKEN و REDIS_URL نیاز به تغییر دارد.
- `.github/workflows/claude-auto-task.yml` (سطر 79) — برای اضافه کردن secrets جدید مانند TELEGRAM_BOT_TOKEN به workflow نیاز به تغییر دارد.

## 🌐 نقشهٔ وابستگی‌ها
این قابلیت به فایل‌های backend/server.js (برای WebSocket و endpointهای جدید)، backend/package.json (برای وابستگی‌های جدید)، frontend/src/App.jsx (برای state management سمت کلاینت)، frontend/index.html (برای Inspector Bridge)، و backend/.env.example (برای متغیرهای محیطی جدید) وابسته است. همچنین نیاز به ایجاد فایل‌های جدید backend/services/aiSessionManager.js (برای مدیریت session‌ها) و backend/telegram/b

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد یک دستور تلگرامی به نام /ai_exercise <id> را دارد که یک session جدید در ماژول AI ایجاد کرده و کاربر را به آن متصل کند. این درخواست برگرفته از متن کاربر است: 'اتصال به ماژول 'تمرین با استاد هوش مصنوعی' از طریق تلگرام. این مرحله شامل ایجاد دستور /ai_exercise <id> برای شروع یک جلسه تمرین با استاد هوش مصنوعی از طریق تلگرام است. باید یک session جدید در ماژول AI ایجاد شود و کاربر به آن متصل شود. خارج از این مرحله: پیاده‌سازی خود هوش مصنوعی یا مدیریت مکالمه. نکته حیاتی: باید state management برای session‌های همزمان در نظر گرفته شود.' همچنین کاربر اشاره کرده: 'و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم'. کلیدواژه‌های اصلی: /ai_exercise, AI session, state management, exercise module, session ID. در کد فعلی پروژه (backend/server.js) هیچ endpoint یا ماژولی برای مدیریت session‌های AI یا WebSocket مخصوص تمرین وجود ندارد. تنها WebSocket موجود در خط 45 با path '/ws/live' تعریف شده که برای ارتباطات عمومی است. همچنین در frontend/index.html یک Inspector Bridge Script (خطوط 32-201) برای ردیابی رویدادهای کاربر وجود دارد که می‌تواند برای مدیریت session‌ها مفید باشد. پروژه از Express.js (backend/server.js خط 38)، WebSocket (ws) (خط 7)، و React (frontend) استفاده می‌کند. برای پیاده‌سازی این قابلیت، نیاز به ایجاد یک WebSocket جدید یا复用 WebSocket موجود با مسیر اختصاصی برای session‌های AI، ایجاد state management برای session‌های همزمان (احتمالاً در backend با Map یا Redis)، و ایجاد endpoint REST برای مدیریت session‌ها (create, join, leave) است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک WebSocket جدید در backend/server.js با مسیر '/ws/ai-exercise' برای مدیریت session‌های تمرین با هوش مصنوعی. 2. ایجاد state management برای session‌های همزمان با استفاده از Map در backend (فایل جدید backend/services/aiSessionManager.js). 3. ایجاد endpoint REST در backend/server.js: POST /api/ai-exercise/session برای ایجاد session جدید با body شامل {userId, topicId}. 4. ایجاد endpoint GET /api/ai-exercise/session/:id برای دریافت وضعیت session. 5. ایجاد endpoint POST /api/ai-exercise/session/:id/join برای اتصال کاربر به session. 6. پیاده‌سازی منطق دستور /ai_exercise در یک فایل جدید backend/telegram/bot.js که با WebSocket ارتباط برقرار کند. 7. استفاده از Inspector Bridge Script موجود در frontend/index.html (خطوط 32-201) برای ارسال رویدادهای session به پنل مدیریت. 8. ذخیره session ID در localStorage یا state مدیریت شده برای بازیابی پس از قطع ارتباط.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 10: مدیریت موضوعات تمرین از طریق تلگرام (انتخاب و تغییر موضوع)

**Scope:** این مرحله شامل پیاده‌سازی قابلیت انتخاب و تغییر موضوع تمرین در session AI از طریق تلگرام است. کاربر باید بتواند با دستور /topic <id> موضوع جدیدی انتخاب کند یا با /topics لیست موضوعات موجود را ببیند. خارج از این مرحله: مکالمه با هوش مصنوعی. نکته حیاتی: باید موضوعات از ماژول AI دریافت شوند و session به‌روزرسانی شود.
**Key terms:** /topic, /topics, topic selection, AI session update, topic list

**بخش مربوط از متن کاربر:**
```
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن مدیریت موضوعات تمرین از طریق تلگرام

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:56-114` — `app.post('/api/gemini/chat')` — این endpoint اصلی چت با AI است. باید قابلیت دریافت topic از session و ارسال آن به عنوان context به Gemini اضافه شود.
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
      if (payload.contents) {
        payload.contents = payload.contents.map(content => ({
          role: content.role,
          parts: content.parts.map(part => {
            if (part.inline_data && includeAudio) {
              return { inline_data: part.inline_data };
            }
            if (part.text !== undefined) {
              return { text: part.text };
            }
            return null;
          }).filter(p => p !== null)
        }));
      }
  ```
- `backend/server.js:45` — `WebSocketServer` — WebSocket موجود برای اطلاع‌رسانی تغییر topic به frontend استفاده می‌شود.
  ```jsx
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/server.js:698-715` — `app.post('/api/analyze-files')` — این endpoint تحلیل فایل را انجام می‌دهد. topic انتخاب شده می‌تواند به عنوان system instruction اضافی به این endpoint ارسال شود.
  ```jsx
  app.post('/api/analyze-files', upload.array('files', 10), handleMulterError, async (req, res) => {
    console.log('Received file analysis request');
    if (!GEMINI_API_KEY) {
      console.error('API key not configured');
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const files = req.files || [];
      const textContent = req.body.textContent || '';
      const userInstructions = req.body.userInstructions || '';
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: JavaScript (Node.js backend با Express, WebSocket, multer, ffmpeg; frontend با React, Vite, Tailwind CSS). کتابخانه‌های مرتبط: ws (WebSocket), express, dotenv. برای ربات تلگرام: node-telegram-bot-api یا telegraf.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `frontend/src/App.jsx` (سطر 1) — کامپوننت اصلی frontend که باید تغییر topic را از WebSocket دریافت کرده و UI را به‌روزرسانی کند.
- `frontend/index.html` (سطر 205) — فایل HTML اصلی که اسکریپت inspector bridge در آن قرار دارد. ممکن است نیاز به اضافه کردن اسکریپت WebSocket client برای دریافت تغییرات topic باشد.
- `backend/package.json` (سطر 1) — برای اضافه کردن وابستگی‌های جدید مانند node-telegram-bot-api یا telegraf برای ربات تلگرام.
- `backend/.env.example` (سطر 1) — برای اضافه کردن متغیر محیطی TELEGRAM_BOT_TOKEN به فایل env.

## 🌐 نقشهٔ وابستگی‌ها
این قابلیت به فایل‌های backend/server.js (برای اضافه کردن endpoint جدید و WebSocket handler)، backend/services/telegram_bot.js (ماژول جدید برای ربات تلگرام)، frontend/src/App.jsx (برای نمایش topic جاری و دریافت تغییرات از WebSocket)، backend/package.json (برای اضافه کردن وابستگی node-telegram-bot-api یا telegraf)، و backend/.env.example (برای اضافه کردن TELEGRAM_BOT_TOKEN) وابسته است. همچنین نیاز به یک دیتابیس ساده (فایل JSON یا SQLite) برای ذخیره session کاربران دارد.

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی قابلیت انتخاب و تغییر موضوع تمرین در session AI از طریق تلگرام را دارد. کاربر باید بتواند با دستور /topic <id> موضوع جدیدی انتخاب کند یا با /topics لیست موضوعات موجود را ببیند. نکته حیاتی: باید موضوعات از ماژول AI دریافت شوند و session به‌روزرسانی شود. این قابلیت به بخش 'تمرین با استاد هوش مصنوعی' متصل می‌شود. کلیدواژه‌ها: /topic, /topics, topic selection, AI session update, topic list. در کد فعلی پروژه (backend/server.js) endpoint‌های مربوط به AI شامل /api/gemini/chat (خط 56) و /api/gemini/tts (خط 117) و /api/analyze-files (خط 698) وجود دارد اما هیچ endpoint یا handler ای برای مدیریت session یا topic از طریق تلگرام پیاده‌سازی نشده است. همچنین فایل frontend/src/App.jsx و frontend/index.html فاقد هرگونه قابلیت مرتبط با تلگرام هستند. فایل .github/workflows/claude-auto-task.yml نیز صرفاً برای اجرای خودکار تسک‌هاست و ربطی به این قابلیت ندارد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک ماژول جدید backend/services/telegram_bot.js برای مدیریت ربات تلگرام. 2. پیاده‌سازی handler برای دستور /topics که از API هوش مصنوعی (مثلاً endpoint /api/gemini/list-topics یا مشابه) لیست موضوعات را دریافت کرده و به کاربر نمایش دهد. 3. پیاده‌سازی handler برای دستور /topic <id> که موضوع انتخاب شده را در session کاربر ذخیره کرده و به AI backend ارسال کند تا session به‌روزرسانی شود. 4. ایجاد یک endpoint جدید در backend/server.js مانند /api/session/topic برای دریافت و به‌روزرسانی topic جلسه. 5. ذخیره‌سازی session کاربر (chat_id تلگرام) با topic انتخاب شده در یک دیتابیس ساده (مثلاً فایل JSON یا SQLite). 6. اتصال به WebSocket موجود (خط 45: wss = new WebSocketServer({ server, path: '/ws/live' })) برای اطلاع‌رسانی تغییر topic به frontend.

## 💡 نمونه‌های قبل/بعد
**اضافه کردن endpoint /api/session/topic به backend**

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 11: پیاده‌سازی مکالمه متنی با هوش مصنوعی از طریق تلگرام

**Scope:** این مرحله شامل اتصال پیام‌های متنی تلگرام به ماژول AI است. هر پیام متنی کاربر در session فعال به AI ارسال شده و پاسخ دریافت و به کاربر نمایش داده می‌شود. خارج از این مرحله: پیام‌های صوتی. نکته حیاتی: باید context مکالمه حفظ شود و session timeout مدیریت گردد.
**Key terms:** text message handler, AI API, conversation context, session timeout, response handler

**بخش مربوط از متن کاربر:**
```
و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
اتصال پیام‌های متنی تلگرام به ماژول AI با حفظ context

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:56-114` — `app.post('/api/gemini/chat')` — این endpoint اصلی برای ارسال پیام به Gemini API است. باید برای پشتیبانی از history مکالمه توسعه یابد یا یک endpoint جدید ایجاد شود.
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
      if (payload.contents) {
        payload.contents = payload.contents.map(content => ({
          role: content.role,
          parts: content.parts.map(part => {
            if (part.inline_data && includeAudio) {
              return { inline_data: part.inline_data };
            }
            if (part.text !== undefined) {
              return { text: part.text };
            }
            return null;
          }).filter(p => p !== null)
        }));
      }
      const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${GEMINI_API_KEY}`;
  ```
- `backend/server.js:44-45` — `WebSocketServer` — WebSocket موجود برای ارسال real-time پاسخ‌ها به کلاینت تلگرام قابل استفاده است.
  ```jsx
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `frontend/index.html:10-19` — `Firebase Configuration` — Firebase config موجود است. می‌توان از Firestore برای ذخیره session‌های
  ```
  var __firebase_config = JSON.stringify({
    apiKey: "AIzaSyAMFtV0zIMKKe0bIr68Z1wTORQ1jQpsv9Q",
    authDomain: "labaneseapp.firebaseapp.com",
    projectId: "labaneseapp",
    storageBucket: "labaneseapp.firebasestorage.app",
    messagingSenderId: "951874597795",
    appId: "1:951874597795:web:00745327993adad760a016"
  });
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی مکالمه متنی با هوش مصنوعی از طریق تلگرام را دارد. این مرحله شامل اتصال پیام‌های متنی تلگرام به ماژول AI است. هر پیام متنی کاربر در session فعال به AI ارسال شده و پاسخ دریافت و به کاربر نمایش داده می‌شود. خارج از این مرحله: پیام‌های صوتی. نکته حیاتی: باید context مکالمه حفظ شود و session timeout مدیریت گردد. بخش مربوط از درخواست اصلی کاربر: 'و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم'. کلیدواژه‌ها: text message handler, AI API, conversation context, session timeout, response handler. در کد فعلی backend/server.js، endpoint /api/gemini/chat (خط 56-114) برای ارسال پیام به Gemini API وجود دارد که از مدل gemini-2.0-flash استفاده می‌کند. این endpoint payload را دریافت کرده و پس از پاکسازی (حذف includeAudio و مدیریت inline_data)، به API گوگل ارسال می‌کند. همچنین endpoint /api/gemini/tts (خط 117-164) برای تبدیل متن به گفتار با مدل gemini-2.5-flash-preview-tts موجود است. پروژه از WebSocket (خط 45) برای ارتباط زنده استفاده می‌کند. frontend/index.html شامل Firebase config (apiKey: AIzaSyAMFtV0zIMKKe0bIr68Z1wTORQ1jQpsv9Q, authDomain: labaneseapp.firebaseapp.com, projectId: labaneseapp) است. backend/.env.example نشان می‌دهد که GEMINI_API_KEY باید تنظیم شود. backend/package.json شامل وابستگی‌های express, cors, dotenv, ws است. frontend/package.json شامل react, firebase, lucide-react است. برای پیاده‌سازی، نیاز به ایجاد یک endpoint جدید یا توسعه endpoint موجود برای مدیریت session و context مکالمه است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک فایل جدید backend/services/telegram-ai-service.js برای مدیریت session‌های مکالمه تلگرام با timeout. 2. در backend/server.js، یک endpoint جدید POST /api/telegram/chat ایجاد کن که: (الف) sessionId را از body دریافت کند، (ب) session را از یک Map (یا Redis در آینده) بازیابی کند، (ج) پیام جدید را به history session اضافه کند، (د) history را به همراه پیام جدید به /api/gemini/chat ارسال کند (با includeAudio=false)، (ه) پاسخ را به history اضافه کند و session را با timestamp جدید ذخیره کند، (و) session timeout را بررسی کند (مثلاً 30 دقیقه عدم فعالیت). 3. در backend/server.js، تابع middleware برای cleanup session‌های منقضی اضافه کن (setInterval هر 5 دقیقه). 4. در frontend/src/App.jsx، یک کامپوننت جدید TelegramChat ایجاد کن که از Firebase Realtime Database یا Firestore برای ذخیره session‌ها استفاده کند (با توجه به Firebase config در frontend/index.html). 5. از WebSocket موجود (wss در خط 45 backend/server.js) برای ارسال real-time پاسخ‌ها به کلاینت تلگرام استفاده کن. 6. context مکالمه را به صورت آرایه‌ای از {role, parts} ذخیره کن (مطابق ساختار payload در خطوط 68-81 backend/server.js).

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 12: پیاده‌سازی دریافت و ارسال پیام صوتی به هوش مصنوعی

**Scope:** این مرحله شامل قابلیت دریافت پیام صوتی از کاربر تلگرام، ارسال آن به ماژول AI برای تحلیل و استخراج متن، و دریافت پاسخ صوتی از AI و ارسال آن به کاربر است. باید از کتابخانه‌های تبدیل صوت به متن (مثلاً Whisper) و متن به صوت استفاده شود. خارج از این مرحله: ذخیره‌سازی فایل‌های صوتی. نکته حیاتی: باید فرمت‌های صوتی پشتیبانی شده (OGG, MP3, WAV) مدیریت شوند.
**Key terms:** voice message, speech-to-text, Whisper, text-to-speech, audio format, OGG, MP3

**بخش مربوط از متن کاربر:**
```
و از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن قابلیت دریافت و ارسال پیام صوتی به AI

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:56-114` — `app.post('/api/gemini/chat', ...)` — این endpoint برای ارسال متن به Gemini و دریافت پاسخ متنی/صوتی استفاده می‌شود. باید از آن برای ارسال متن استخراج‌شده از Whisper استفاده کرد.
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
      ...
    } catch (error) {
      console.error('Chat API error:', error);
      res.status(500).json({ error: error.message });
    }
  });
  ```
- `backend/server.js:117-164` — `app.post('/api/gemini/tts', ...)` — این endpoint برای تبدیل متن به صوت با Gemini TTS استفاده می‌شود. باید برای تولید پاسخ صوتی از آن استفاده کرد.
- `backend/server.js:790-800` — `else if (mimeType.startsWith('audio/'))` — این بخش از endpoint /api/analyze-files فایل‌های صوتی را پردازش می‌کند. می‌توان از منطق مشابه برای پردازش فایل‌های صوتی دریافتی از کاربر استفاده کرد.
  ```jsx
  } else if (mimeType.startsWith('audio/')) {
    console.log(`Audio file: ${fileName}, size: ${fileSizeMB.toFixed(2)} MB`);
    try {
      let result;
      if (isLargeFile) {
        console.log(`Using File API for large audio: ${fileName}`);
        const uploadedFileName = await uploadToGeminiFileAPI(filePath, mimeType, fileName, fileSize);
        ...
  ```
- `backend/server.js:30-33` — `const upload = multer({...})` — تنظیمات multer برای آپلود فایل. باید اطمینان حاصل شود که فرمت‌های OGG, MP3, WAV پشتیبانی می‌شوند (به صورت پیش‌فرض multer همه فرمت‌ها را قبول می‌کند).
  ```jsx
  const upload = multer({
    storage: storage,
    limits: { fileSize: 500 * 1024 * 1024 } // 500MB max per file
  });
  ```
- `backend/server.js`

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی قابلیت دریافت پیام صوتی از کاربر (تلگرام)، ارسال آن به ماژول AI برای تحلیل و استخراج متن، و دریافت پاسخ صوتی از AI و ارسال آن به کاربر را دارد. باید از کتابخانه‌های تبدیل صوت به متن (مثلاً Whisper) و متن به صوت استفاده شود. خارج از این مرحله: ذخیره‌سازی فایل‌های صوتی. نکته حیاتی: باید فرمت‌های صوتی پشتیبانی شده (OGG, MP3, WAV) مدیریت شوند. کاربر اشاره کرده: 'و از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد'. کلیدواژه‌ها: voice message, speech-to-text, Whisper, text-to-speech, audio format, OGG, MP3. در کد فعلی پروژه (backend/server.js) endpointهای /api/gemini/chat (خط 56) و /api/gemini/tts (خط 117) وجود دارند که می‌توانند برای پردازش متن و تولید صوت استفاده شوند. همچنین endpoint /api/analyze-files (خط 698) از فایل‌های صوتی پشتیبانی می‌کند (خط 790: else if (mimeType.startsWith('audio/'))). اما هیچ endpoint یا منطقی برای دریافت مستقیم پیام صوتی از کاربر (مثلاً از طریق WebSocket یا API جدید) و تبدیل آن به متن با Whisper وجود ندارد. همچنین هیچ WebSocket endpoint برای ارتباط بلادرنگ با تلگرام یا کلاینت تعبیه نشده است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک endpoint جدید WebSocket در backend/server.js (مثلاً '/ws/voice') برای دریافت استریم صوتی از کاربر. 2. پیاده‌سازی ماژول speech-to-text با استفاده از Whisper (کتابخانه openai-whisper یا API OpenAI Whisper) در یک فایل جدید backend/services/whisper_service.js. 3. ایجاد endpoint جدید POST /api/voice/transcribe در backend/server.js که فایل صوتی (OGG, MP3, WAV) را دریافت کرده، با Whisper به متن تبدیل کند و متن را برگرداند. 4. استفاده از endpoint موجود /api/gemini/chat (خط 56) برای ارسال متن استخراج‌شده به Gemini و دریافت پاسخ تحلیلی. 5. استفاده از endpoint موجود /api/gemini/tts (خط 117) برای تبدیل پاسخ متنی Gemini به صوت. 6. ارسال پاسخ صوتی (base64) و متن به کاربر از طریق WebSocket یا endpoint جدید. 7. مدیریت فرمت‌های صوتی OGG, MP3, WAV در multer (backend/server.js خط 30-33) و تبدیل به فرمت مناسب برای Whisper. 8. نصب وابستگی‌های جدید: openai (برای Whisper API) یا openai-whisper (برای مدل محلی) در backend/package.json.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 13: همگام‌سازی دوطرفه پیام‌ها بین تلگرام و چت وب

**Scope:** این مرحله شامل ایجاد مکانیزمی برای همگام‌سازی پیام‌ها بین تلگرام و چت وب است. هر پیام ارسال شده در تلگرام باید در چت وب نیز نمایش داده شود و بالعکس. باید از WebSocket یا polling برای实时 همگام‌سازی استفاده شود. خارج از این مرحله: مدیریت فایل‌های رسانه‌ای. نکته حیاتی: باید از conflict avoidance و deduplication استفاده شود.
**Key terms:** WebSocket, real-time sync, message queue, deduplication, chat synchronization

**بخش مربوط از متن کاربر:**
```
و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....
```

## 🎯 هدف (خلاصه ساختاریافته)
همگام‌سازی دوطرفه پیام‌ها بین تلگرام و چت وب

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:44-45` — `WebSocketServer` — WebSocket موجود صرفاً برای Inspector Bridge است. باید یک WebSocket جدید با path /ws/chat برای چت وب اضافه شود.
  ```jsx
  // Create WebSocket server
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/server.js:53` — `GEMINI_API_KEY` — الگوی مشابه برای خواندن TELEGRAM_BOT_TOKEN از environment variables باید اضافه شود.
  ```jsx
  const GEMINI_API_KEY = process.env.GEMINI_API_KEY;
  ```
- `backend/.env.example:1-8` — `GEMINI_API_KEY` — باید خط TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here به این فایل اضافه شود.
  ```
  # Gemini API Key - Get from https://aistudio.google.com/apikey
  GEMINI_API_KEY=your_gemini_api_key_here
  
  # Port (optional, default 3001)
  PORT=3001
  
  # Firebase config (optional - for data persistence)
  # VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}
  ```
- `backend/package.json:1-15` — `dependencies` — باید وابستگی "node-telegram-bot-api": "^0.64.0" به dependencies اضافه شود.
  ```json
  {
    "name": "lebanese-dialect-backend",
    "version": "1.0.0",
    "dependencies": {
      "cors": "^2.8.5",
      "dotenv": "^16.3.1",
      "express": "^4.18.2",
      "ws": "^8.19.0"
    }
  }
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `frontend/src/App.jsx` (سطر 1) — فایل اصلی React که چت وب در آن پیاده‌سازی شده است. باید WebSocket client برای /ws/chat به آن اضافه شود.
- `frontend/src/main.jsx` (سطر 1) — نقطه ورود React که App.jsx را رندر می‌کند. ممکن است نیاز به Provider یا context برای WebSocket داشته باشد.
- `frontend/vite.config.js` (سطر 8) — تنظیمات Vite که proxy /api را به backend دارد. ممکن است نیاز به proxy برای WebSocket نیز داشته باشد.
- `backend/package-lock.json` (سطر 1) — بعد از افزودن node-telegram-bot-api، این فایل به‌روزرسانی می‌شود.
- `frontend/index.html` (سطر 32) — Inspector Bridge در این فایل از WebSocket استفاده می‌کند. ممکن است نیاز به هماهنگی با WebSocket جدید چت باشد.

## 🌐 نقشهٔ وابستگی‌ها
این تسک به فایل‌های backend/server.js (WebSocket موجود و endpointهای API)، backend/.env.example (متغیرهای محیطی)، backend/package.json (وابستگی‌ها)، frontend/src/App.jsx (UI چت وب)، و frontend/vite.config.js (proxy) وابسته است. فایل‌های جدید backend/services/telegramBot.js و backend/services/chatSync.js ایجاد می‌شوند.

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد مکانیزمی برای همگام‌سازی دوطرفه پیام‌ها بین تلگرام و چت وب را دارد. هر پیام ارسال شده در تلگرام باید در چت وب نیز نمایش داده شود و بالعکس. باید از WebSocket یا polling برای real-time همگام‌سازی استفاده شود. مدیریت فایل‌های رسانه‌ای خارج از این مرحله است. نکته حیاتی: باید از conflict avoidance و deduplication استفاده شود. بخش مربوط از درخواست اصلی کاربر: 'و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....' کلیدواژه‌ها: WebSocket, real-time sync, message queue, deduplication, chat synchronization. در کد فعلی پروژه، backend/server.js از WebSocket (ws) برای اتصالات زنده استفاده می‌کند (خط 45: const wss = new WebSocketServer({ server, path: '/ws/live' })) اما این WebSocket صرفاً برای Inspector Bridge در frontend/index.html (خط 32-201) استفاده می‌شود و هیچ ارتباطی با تلگرام یا چت وب ندارد. frontend/src/App.jsx فایل اصلی React است که احتمالاً چت وب در آن پیاده‌سازی شده اما محتوای آن در deep context موجود نیست. backend/package.json شامل وابستگی ws است اما هیچ کتابخانه‌ای برای Telegram Bot API (مثل node-telegram-bot-api) در backend/package-lock.json دیده نمی‌شود. backend/.env.example شامل GEMINI_API_KEY و Firebase config است اما هیچ فیلدی برای Telegram Bot Token ندارد. بنابراین این تسک نیازمند افزودن Telegram Bot integration، ایجاد WebSocket room/namespace جدید برای چت، و پیاده‌سازی message queue با deduplication است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. افزودن وابستگی node-telegram-bot-api به backend/package.json و نصب آن. 2. ایجاد فایل جدید backend/services/telegramBot.js که با استفاده از Telegram Bot API پیام‌ها را دریافت و ارسال کند. 3. ایجاد فایل جدید backend/services/chatSync.js که message queue با deduplication (با استفاده از message ID یا hash) را مدیریت کند. 4. اصلاح backend/server.js برای: (الف) افزودن یک WebSocket namespace جدید (مثلاً /ws/chat) برای چت وب، (ب) اتصال Telegram Bot به WebSocket برای ارسال پیام‌های دریافتی از تلگرام به کلاینت‌های وب، (ج) دریافت پیام‌های وب از WebSocket و ارسال آن‌ها به تلگرام. 5. افزودن متغیر محیطی TELEGRAM_BOT_TOKEN به backend/.env.example. 6. ایجاد endpoint جدید POST /api/chat/send در backend/server.js برای ارسال پیام از وب به تلگرام (به عنوان fallback برای WebSocket). 7. پیاده‌سازی deduplication با نگهداری یک Set از message IDs در memory (یا Redis در آینده). 8. پیاده‌سازی conflict avoidance با timestamp-based ordering.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 14: مدیریت خطاها و لاگینگ در سیستم تلگرام

**Scope:** این مرحله شامل پیاده‌سازی سیستم مدیریت خطا برای ربات تلگرام است. خطاهای رایج (اتصال، API، دیتابیس) باید به صورت دوستانه به کاربر نمایش داده شود و در لاگ‌ها ثبت شود. باید از logging module با سطوح مختلف (INFO, ERROR, DEBUG) استفاده شود. خارج از این مرحله: مانیتورینگ پیشرفته. نکته حیاتی: خطاهای حساس (مثل token) نباید به کاربر نمایش داده شود.
**Key terms:** error handler, logging, user-friendly error, exception handling, log levels

**بخش مربوط از متن کاربر:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی مدیریت خطا و لاگینگ در ربات تلگرام

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:1-12` — `import section` — محل افزودن importهای جدید: logger و telegramBotHandler. همچنین باید import کتابخانه node-telegram-bot-api اضافه شود.
  ```jsx
  import express from 'express';
  import cors from 'cors';
  import { fileURLToPath } from 'url';
  import { dirname, join } from 'path';
  import dotenv from 'dotenv';
  import { createServer } from 'http';
  import { WebSocketServer, WebSocket } from 'ws';
  import multer from 'multer';
  import fs from 'fs';
  import os from 'os';
  import ffmpeg from 'fluent-ffmpeg';
  import ffmpegStatic from 'ffmpeg-static';
  ```
- `backend/server.js:53-54` — `GEMINI_API_KEY constant` — بعد از این خط، باید TelegramBotHandler راه‌اندازی شود: const telegramBot = new TelegramBotHandler(process.env.TELEGRAM_BOT_TOKEN);
  ```jsx
  const GEMINI_API_KEY = process.env.GEMINI_API_KEY;
  ```
- `backend/server.js:56-114` — `POST /api/gemini/chat` — نمونه endpoint که باید
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
  
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
  
      // Clean payload - preserve audio data if includeAudio flag is set
      if (payload.contents) {
        payload.contents = payload.contents.map(content => ({
          role: content.role,
          parts: content.parts.map(part => {
            // Keep inline_data (audio) if present
            if (part.inline_data && includeAudio) {
              return { inline_data: part.inline_data };
            }
            // Keep text if present
            if (part.text !== undefined) {
              return { text: part.text };
            }
            return null;
          }).filter(p => p !== null)
        }));
      }
  
      const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${GEMINI_API_KEY}`;
  
      const response = await fetch(apiUrl, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(payload)
      });
  
      if (!response.ok) {
        const errorData = await response.text();
        console.error('Gemini API error:', errorData);
        return res.status(response.status).json({ error: errorData });
      }
  
      const result = await response.json();
  
      // Return full result for audio requests, just text otherwise
      if (includeAudio) {
        res.json(result);
      } else {
        const text = result.candidates?.[0]?.content?.parts?.[0]?.text;
        if (!text) {
          return res.status(500).json({ error: 'No text in response' });
        }
        res.json({ text });
      }
    } catch (error) {
      console.error('Chat API error:', error);
      res.status(500).json({ error: error.message });
    }
  });
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
این تسک شامل پیاده‌سازی سیستم مدیریت خطا برای ربات تلگرام است. کاربر درخواست کرده: 'میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه'. خطاهای رایج (اتصال، API، دیتابیس) باید به صورت دوستانه به کاربر نمایش داده شود و در لاگ‌ها ثبت شود. باید از logging module با سطوح مختلف (INFO, ERROR, DEBUG) استفاده شود. خارج از این مرحله: مانیتورینگ پیشرفته. نکته حیاتی: خطاهای حساس (مثل token) نباید به کاربر نمایش داده شود. کلیدواژه‌ها: error handler, logging, user-friendly error, exception handling, log levels. در کد فعلی پروژه (backend/server.js) هیچ سیستم لاگینگ ساختاریافته‌ای وجود ندارد و خطاها فقط با console.error (خطوط 94, 111, 146, 161, 190, 237, 289, 322, 354, 417, 440, 487, 521, 550, 572, 577, 688, 702, 748) ثبت می‌شوند. همچنین endpoint یا سرویسی برای تلگرام وجود ندارد. فایل .github/workflows/claude-auto-task.yml نشان می‌دهد که پروژه از GitHub Actions برای اجرای خودکار تسک‌ها استفاده می‌کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/services/telegramBot.js با ساختار زیر: الف) import کتابخانه node-telegram-bot-api و logging module. ب) پیاده‌سازی کلاس TelegramBotHandler با متدهای: constructor(token) برای راه‌اندازی بات، sendNotification(chatId, message) برای ارسال نوتیفیکیشن، setupCommands() برای تعریف دستورات (/start, /help). ج) پیاده‌سازی errorHandler(err, chatId) که خطاها را بر اساس نوع (اتصال، API، دیتابیس) دسته‌بندی کرده و پیام دوستانه به کاربر نمایش دهد. د) استفاده از logging module با سطوح INFO, ERROR, DEBUG. ه) فیلتر کردن خطاهای حساس (token, API key) از نمایش به کاربر.
2. ایجاد فایل backend/utils/logger.js با تنظیمات logging module شامل: format, timestamp, log levels.
3. ایجاد فایل backend/config/telegram.js برای ذخیره تنظیمات تلگرام (token, chatId).
4. به‌روزرسانی backend/server.js: import کردن TelegramBotHandler و راه‌اندازی آن در خطوط ابتدایی (بعد از خط 53). افزودن middleware برای لاگ کردن درخواست‌ها. جایگزینی console.error با logger.error در تمام خطوط.
5. افزودن endpoint جدید POST /api/telegram/send-notification در backend/server.js برای ارسال نوتیفیکیشن از طریق API.
6. به‌روزرسانی backend/package.json برای افزودن وابستگی node-telegram-bot-api.
7. ایجاد فایل backend/tests/telegram.test.js برای تست واحد.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 15: تست و دیباگ کامل سیستم تلگرام دوطرفه

**Scope:** این مرحله شامل تست کامل تمامی قابلیت‌های پیاده‌سازی شده است: ارسال و دریافت پیام، نوتیفیکیشن، احراز هویت، دستورات کنترلی، مدیریت محتوا، اتصال به AI، و همگام‌سازی. باید تست‌های unit و integration نوشته شود. خارج از این مرحله: تست load یا performance. نکته حیاتی: باید سناریوهای edge case (مثل ارسال همزمان چند پیام) تست شود.
**Key terms:** unit test, integration test, edge case, test scenario, debug logging

**بخش مربوط از متن کاربر:**
```
و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....
```

## 🎯 هدف (خلاصه ساختاریافته)
تست و دیباگ کامل سیستم تلگرام دوطرفه

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:56-114` — `app.post('/api/gemini/chat')` — این endpoint برای اتصال به AI (Gemini) استفاده می‌شود. سیستم تلگرام باید بتواند پیام دریافتی از کاربر را به این endpoint ارسال کند و پاسخ را به تلگرام برگرداند.
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
      ...
    } catch (error) {
      console.error('Chat API error:', error);
      res.status(500).json({ error: error.message });
    }
  });
  ```
- `backend/server.js:117-164` — `app.post('/api/gemini/tts')` — این endpoint برای تبدیل متن به گفتار (TTS) استفاده می‌شود. در سناریوی تلگرام، می‌توان از آن برای ارسال پاسخ صوتی به کاربر استفاده کرد.
  ```jsx
  app.post('/api/gemini/tts', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const { prompt, voice = 'Kore' } = req.body;
      ...
    } catch (error) {
      console.error('TTS API error:', error);
      res.status(500).json({ error: error.message });
    }
  });
  ```
- `backend/server.js:698-715` — `app.post('/api/analyze-files')` — این endpoint برای تحلیل فایل‌ها (صوت، تصویر، ویدیو، PDF) استفاده می‌شود. در تلگرام، کاربر می‌تواند فایل ارسال کند و این endpoint آن را تحلیل کند.
  ```jsx
  app.post('/api/analyze-files', upload.array('files', 10), handleMulterError, async (req, res) => {
    console.log('Received file analysis request');
    if (!GEMINI_API_KEY) {
      console.error('API key not configured');
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const files = req.files || [];
      const textContent = req.body.textContent || '';
      const userInstructions = req.body.userInstructions || '';
      ...
    }
  });
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
JavaScript (Node.js), Express, node-telegram-bot-api, Gemini API, WebSocket (موجود در backend/server.js

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/package.json` (سطر 10) — برای اضافه کردن وابستگی node-telegram-bot-api به dependencies نیاز است.
- `backend/.env.example` (سطر 1) — برای اضافه کردن متغیرهای محیطی TELEGRAM_BOT_TOKEN و WEBHOOK_URL به عنوان نمونه نیاز است.
- `frontend/src/App.jsx` (سطر 1) — اگر قرار است وضعیت تلگرام در فرانت‌اند نمایش داده شود، این فایل نیاز به تغییر دارد. اما در این مرحله فقط backend تغییر می‌کند.

## 🌐 نقشهٔ وابستگی‌ها
این تسک وابسته به فایل‌های backend/server.js (برای استفاده از endpointهای AI), backend/package.json (برای اضافه کردن وابستگی), و backend/.env (برای ذخیره توکن‌ها) است. فایل‌های جدیدی که ایجاد می‌شوند: backend/services/telegramService.js (سرویس اصلی تلگرام), backend/routes/telegram.js (روتر تلگرام), backend/tests/telegram.test.js (تست‌ها). این فایل‌ها به express (موجود در backend/server.js خط 1) و node-telegram-bot-api وابسته هستند.

## 🔍 Context و وضعیت فعلی
کاربر درخواست تست کامل تمامی قابلیت‌های پیاده‌سازی شده سیستم تلگرام دوطرفه را دارد. این شامل: ارسال و دریافت پیام، نوتیفیکیشن، احراز هویت، دستورات کنترلی، مدیریت محتوا، اتصال به AI (که در backend/server.js از طریق endpoint /api/gemini/chat در خط 56 و /api/gemini/tts در خط 117 پیاده‌سازی شده)، و همگام‌سازی است. کاربر تأکید کرده که باید تست‌های unit و integration نوشته شود و خارج از این مرحله: تست load یا performance. نکته حیاتی: باید سناریوهای edge case (مثل ارسال همزمان چند پیام) تست شود. بخش مربوط از درخواست اصلی کاربر: 'و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....' کلیدواژه‌ها: unit test, integration test, edge case, test scenario, debug logging. با توجه به deep context موجود، پروژه فعلی یک اپلیکیشن آموزش لهجه لبنانی با React (frontend) و Express (backend) است و هیچ فایل یا کدی مرتبط با تلگرام در ساختار پروژه دیده نمی‌شود. بنابراین این تسک نیازمند ایجاد زیرساخت کامل تلگرام (بات، webhook، مدیریت پیام) و سپس تست آن است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/services/telegramService.js برای مدیریت اتصال به Telegram Bot API (webhook, sendMessage, receiveMessage). 2. ایجاد فایل جدید backend/routes/telegram.js برای endpointهای مربوط به تلگرام (POST /api/telegram/webhook برای دریافت پیام‌ها از تلگرام). 3. ایجاد فایل جدید backend/tests/telegram.test.js برای تست‌های unit و integration شامل: تست ارسال پیام به تلگرام، تست دریافت پیام از تلگرام، تست edge case ارسال همزمان چند پیام، تست احراز هویت (با استفاده از secret token), تست اتصال به AI از طریق endpoint /api/gemini/chat در backend/server.js خط 56. 4. اضافه کردن debug logging با استفاده از console.error (الگوی موجود در backend/server.js خط 94, 111, 146, 161). 5. به‌روزرسانی backend/package.json برای اضافه کردن وابستگی node-telegram-bot-api. 6. ایجاد فایل backend/.env برای ذخیره TELEGRAM_BOT_TOKEN و WEBHOOK_URL.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

## ✅ معیارهای پذیرش کلی (همهٔ مراحل)
- [ ] همهٔ مراحل بالا با موفقیت پیاده‌سازی شده‌اند
- [ ] تست‌های موجود pass می‌شوند
- [ ] هیچ regression رخ نداده

## Acceptance Criteria

1. بات تلگرام با موفقیت ایجاد شده و توکن آن در متغیر محیطی TELEGRAM_BOT_TOKEN ذخیره شود _(verify: static)_
2. endpoint POST /api/telegram/notify در backend/server.js پیاده‌سازی شود و پیام را به تلگرام ارسال کند _(verify: api_response)_
3. نوتیفیکیشن‌های خودکار برای خطاهای سیستم (مثلاً خطای Gemini API در خط 111) ارسال شوند _(verify: static)_
4. نوتیفیکیشن‌های وضعیت سیستم (مثلاً وضعیت WebSocket در خط 45) قابل ارسال باشند _(verify: static)_
5. متن پیام‌ها به صورت پویا و قابل تنظیم باشد _(verify: static)_
6. خطاهای ارسال نوتیفیکیشن به درستی لاگ شوند و باعث کرش سرور نشوند _(verify: static)_
7. یک endpoint جدید `POST /api/telegram/webhook` در `backend/server.js` ایجاد شود که قادر به دریافت Webhook از تلگرام باشد. _(verify: api_response)_
8. یک فایل جدید `backend/services/telegramBot.js` ایجاد شود که شامل منطق پردازش دستورات `/start`, `/status`, `/list-models` باشد. _(verify: static)_
9. دستور `/status` باید وضعیت سیستم را با فراخوانی `GET /api/health` برگرداند. _(verify: static)_
10. دستور `/list-models` باید لیست مدل‌های Gemini را با فراخوانی `GET /api/list-models` برگرداند. _(verify: static)_
11. متغیر محیطی `TELEGRAM_BOT_TOKEN` به `backend/.env.example` اضافه شود. _(verify: static)_
12. ربات تلگرام بتواند پیام تأیید دریافت دستور و نتیجه عملیات را به کاربر ارسال کند. _(verify: static)_
13. کاربر می‌تواند با ارسال دستور /start به ربات تلگرام، پیام خوش‌آمدگویی و راهنما دریافت کند. _(verify: static)_
14. کاربر می‌تواند با ارسال دستور /lessons، لیست تمام دروس موجود در Firebase Firestore (collection: lessons) را مشاهده کند. _(verify: static)_
15. کاربر می‌تواند با ارسال دستور /lesson <lessonId>، محتوای کامل یک درس خاص شامل title، content و لیست فایل‌های مرتبط را مشاهده کند. _(verify: static)_
16. کاربر می‌تواند با ارسال دستور /upload و سپس ارسال یک فایل (صوتی، تصویری، متنی)، فایل را در Firebase Storage در مسیر lessons/{lessonId}/{fileName} آپلود کند. _(verify: static)_
17. پس از آپلود فایل، یک پیام تأیید به کاربر در تلگرام ارسال شود و اطلاعات فایل در Firestore به‌روزرسانی شود. _(verify: static)_
18. WebSocket موجود در backend/server.js (path: /ws/live) یک رویداد new-file-uploaded را برای اطلاع‌رسانی به فرانت‌اند ارسال کند. _(verify: static)_
19. تمامی خطاها (مانند عدم وجود درس، خطای آپلود) با پیام مناسب به کاربر در تلگرام نمایش داده شود. _(verify: static)_
20. ربات تلگرام بتواند همزمان با چندین کاربر کار کند (state management برای هر کاربر). _(verify: static)_
21. یک WebSocket endpoint جدید با مسیر '/ws/topics' در backend/server.js ایجاد شود و به درستی کار کند. _(verify: api_response)_
22. یک ربات تلگرام با استفاده از کتابخانه 'node-telegram-bot-api' ایجاد شود و به WebSocket endpoint متصل شود. _(verify: static)_
23. کاربر بتواند از طریق ربات تلگرام، لیست موضوعات تمرین را مشاهده کند. _(verify: ui_interaction)_
24. کاربر بتواند از طریق ربات تلگرام، یک موضوع را برای تمرین انتخاب کند. _(verify: ui_interaction)_
25. موضوع انتخاب‌شده در Firebase Firestore ذخیره شود و در فرانت‌اند (کامپوننت TopicSelector) قابل مشاهده باشد. _(verify: api_response)_
26. کامپوننت TopicSelector در frontend/src/App.jsx اضافه شود و موضوعات را از WebSocket دریافت و نمایش دهد. _(verify: static)_
27. تغییرات در backend/server.js بر WebSocket endpoint فعلی ('/ws/live') تأثیری نگذارد. _(verify: static)_
28. یک ربات تلگرام با استفاده از BotFather ساخته شود و توکن آن در متغیر محیطی TELEGRAM_BOT_TOKEN ذخیره شود. _(verify: static)_
29. Webhook تلگرام در backend/server.js در مسیر POST /api/telegram/webhook پیاده‌سازی شود و پیام‌های دریافتی را پردازش کند. _(verify: api_response)_
30. پیام متنی ارسال شده به ربات تلگرام، به /api/gemini/chat ارسال شده و پاسخ متنی به تلگرام برگردانده شود. _(verify: api_response)_
31. پیام صوتی ارسال شده به ربات تلگرام، دانلود شده، با analyzeWithGemini آنالیز شود، و پاسخ صوتی با /api/gemini/tts تولید شده و به تلگرام ارسال شود. _(verify: api_response)_
32. پاسخ‌های صوتی و متنی همزمان در تلگرام و چت موجود در frontend/src/App.jsx نمایش داده شوند (با استفاده از WebSocket). _(verify: ui_interaction)_
33. کتابخانه node-telegram-bot-api به backend/package.json اضافه و نصب شود. _(verify: static)_
34. مستندات راه‌اندازی در backend/.env.example به‌روزرسانی شود. _(verify: static)_
35. 1. فایل `backend/services/telegramBot.js` ایجاد شود و شامل کلاس/ماژول TelegramBot با متدهای `handleUpdate`, `sendMessage`, `sendAudio`, `sendDocument` باشد. _(verify: static)_
36. 2. Webhook endpoint `POST /api/telegram/webhook` در `backend/server.js` اضافه شود و آپدیت‌های تلگرام را به درستی پردازش کند. _(verify: api_response)_
37. 3. هندلر دستور `/chat` در Telegram Bot پیاده‌سازی شود و متن پیام را به API `/api/gemini/chat` ارسال کند و پاسخ را به کاربر تلگرام برگرداند. _(verify: api_response)_
38. 4. هندلر دستور `/upload` پیاده‌سازی شود و فایل ارسالی از تلگرام را دریافت کرده، به File Analysis API ارسال کند و نتیجه را برگرداند. _(verify: api_response)_
39. 5. هندلر دستور `/lessons` پیاده‌سازی شود و لیست دروس را از Firebase (یا منبع داده) دریافت کرده و به کاربر نمایش دهد. _(verify: api_response)_
40. 6. ماژول نوتیفیکیشن `backend/services/notificationService.js` ایجاد شود و بتواند پیام‌های نوتیفیکیشن را به کاربران تلگرام ارسال کند. _(verify: static)_
41. 7. فایل تست `backend/tests/telegram.integration.test.js` ایجاد شود و شامل حداقل 5 تست یکپارچه‌سازی برای Webhook و هندلرهای مختلف باشد. _(verify: backend_test)_
42. 8. فایل تست `backend/tests/telegram.e2e.test.js` ایجاد شود و شامل حداقل 2 تست نهایی (End-to-End) با mock کردن Telegram API باشد. _(verify: backend_test)_
43. 9. تمام تست‌ها با دستور `npm run test` (یا معادل آن) اجرا شوند و پاس شوند. _(verify: backend_test)_
44. 10. متغیرهای محیطی `TELEGRAM_BOT_TOKEN` و `TELEGRAM_WEBHOOK_URL` به `backend/.env.example` اضافه شوند. _(verify: static)_

## Task Steps

### Step 1: ایجاد ساختار پایه برای اتصال تلگرام دوطرفه
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک ماژول پایه برای اتصال به تلگرام با استفاده از کتابخانه python-telegram-bot (یا aiogram) است. باید یک bot token از BotFather گرفته شود و یک handler ساده برای دریافت پیام‌های متنی و ارسال پاسخ اولیه پیاده‌سازی شود. خارج از این مرحله: پیاده‌سازی منطق کسب‌وکار، اتصال به دیتابیس، یا مدیریت کاربران. نکته حیاتی: حتماً از webhook یا polling استفاده شود و امنیت token رعایت گردد.
**Excerpt:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

### Step 2: پیاده‌سازی سیستم نوتیفیکیشن تلگرام برای رویدادهای سیستم
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک سرویس نوتیفیکیشن است که رویدادهای مشخصی از سیستم (مثلاً ثبت‌نام کاربر جدید، تکمیل تمرین، خطاهای سیستمی) را به کاربران مشخصی از طریق تلگرام ارسال کند. باید یک صف پیام یا callback برای رویدادها ایجاد شود. خارج از این مرحله: تعیین اینکه کدام رویدادها نوتیفیکیشن شوند (بعداً configurable می‌شود). نکته حیاتی: باید از async استفاده شود تا ارسال پیام‌ها blocking نباشد.
**Excerpt:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته
```

### Step 3: پیاده‌سازی احراز هویت و اتصال کاربران سیستم به حساب تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد مکانیزمی برای اتصال حساب کاربری سیستم به حساب تلگرام است. کاربر باید بتواند از طریق یک کد یکتا یا لینک، حساب خود را به ربات متصل کند. باید جدول یا دیتابیسی برای ذخیره mapping بین user_id سیستم و chat_id تلگرام ایجاد شود. خارج از این مرحله: پیاده‌سازی دستورات کنترلی. نکته حیاتی: امنیت این اتصال بسیار مهم است و باید از روش‌های امن مثل OTP استفاده شود.
**Excerpt:**
```
بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

### Step 4: ایجاد دستورات پایه کنترلی تلگرام (مشاهده وضعیت سیستم)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی دستورات پایه تلگرام برای کاربران احراز هویت شده است: /status (وضعیت سیستم)، /help (راهنما)، /profile (مشاهده پروفایل). باید handlerهای مربوطه ایجاد شود و پاسخ‌های مناسب از سیستم گرفته شود. خارج از این مرحله: دستورات مدیریت محتوا یا تمرین. نکته حیاتی: فقط کاربران احراز هویت شده باید به این دستورات دسترسی داشته باشند.
**Excerpt:**
```
بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

### Step 5: ایجاد دستورات مدیریت محتوای دروس (مشاهده لیست دروس)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی دستور /courses برای مشاهده لیست دروس موجود در سیستم است. باید از دیتابیس دروس خوانده شود و به صورت فرمت‌شده (با شماره و عنوان) نمایش داده شود. خارج از این مرحله: مشاهده محتوای یک درس خاص یا آپلود. نکته حیاتی: باید pagination برای لیست طولانی دروس در نظر گرفته شود.
**Excerpt:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

### Step 6: مشاهده محتوای یک درس خاص از طریق تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی دستور /course <id> برای مشاهده جزئیات یک درس خاص (توضیحات، لیست جلسات، فایل‌ها) است. باید از دیتابیس اطلاعات درس و جلسات آن خوانده شود و با دکمه‌های inline برای انتخاب جلسات نمایش داده شود. خارج از این مرحله: آپلود محتوا یا مشاهده تمرینات. نکته حیاتی: باید از inline keyboards برای ناوبری استفاده شود.
**Excerpt:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

### Step 7: آپلود فایل‌های درسی از طریق تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی قابلیت آپلود فایل (PDF، تصویر، ویدئو) برای یک درس خاص از طریق تلگرام است. کاربر باید بتواند فایلی را به ربات ارسال کند و ربات آن را در مسیر مشخصی ذخیره کرده و به درس مربوطه متصل کند. باید دستور /upload <course_id> برای شروع فرآیند آپلود ایجاد شود. خارج از این مرحله: مدیریت دسته‌بندی فایل‌ها. نکته حیاتی: محدودیت حجم فایل (حداکثر ۵۰MB) و نوع فایل‌های مجاز باید بررسی شود.
**Excerpt:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

### Step 8: ایجاد دستورات مدیریت تمرینات (مشاهده لیست تمرینات)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی دستور /exercises برای مشاهده لیست تمرینات موجود در سیستم است. باید تمرینات به همراه وضعیت (انجام شده/انجام نشده) نمایش داده شوند. خارج از این مرحله: انجام تمرین یا اتصال به هوش مصنوعی. نکته حیاتی: باید از دیتابیس وضعیت هر کاربر برای نمایش وضعیت تمرینات استفاده شود.
**Excerpt:**
```
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم
```

### Step 9: اتصال به ماژول 'تمرین با استاد هوش مصنوعی' از طریق تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد دستور /ai_exercise <id> برای شروع یک جلسه تمرین با استاد هوش مصنوعی از طریق تلگرام است. باید یک session جدید در ماژول AI ایجاد شود و کاربر به آن متصل شود. خارج از این مرحله: پیاده‌سازی خود هوش مصنوعی یا مدیریت مکالمه. نکته حیاتی: باید state management برای session‌های همزمان در نظر گرفته شود.
**Excerpt:**
```
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم
```

### Step 10: مدیریت موضوعات تمرین از طریق تلگرام (انتخاب و تغییر موضوع)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی قابلیت انتخاب و تغییر موضوع تمرین در session AI از طریق تلگرام است. کاربر باید بتواند با دستور /topic <id> موضوع جدیدی انتخاب کند یا با /topics لیست موضوعات موجود را ببیند. خارج از این مرحله: مکالمه با هوش مصنوعی. نکته حیاتی: باید موضوعات از ماژول AI دریافت شوند و session به‌روزرسانی شود.
**Excerpt:**
```
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم
```

### Step 11: پیاده‌سازی مکالمه متنی با هوش مصنوعی از طریق تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل اتصال پیام‌های متنی تلگرام به ماژول AI است. هر پیام متنی کاربر در session فعال به AI ارسال شده و پاسخ دریافت و به کاربر نمایش داده می‌شود. خارج از این مرحله: پیام‌های صوتی. نکته حیاتی: باید context مکالمه حفظ شود و session timeout مدیریت گردد.
**Excerpt:**
```
و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم
```

### Step 12: پیاده‌سازی دریافت و ارسال پیام صوتی به هوش مصنوعی
**Status:** `pending` (0%)
**Scope:** این مرحله شامل قابلیت دریافت پیام صوتی از کاربر تلگرام، ارسال آن به ماژول AI برای تحلیل و استخراج متن، و دریافت پاسخ صوتی از AI و ارسال آن به کاربر است. باید از کتابخانه‌های تبدیل صوت به متن (مثلاً Whisper) و متن به صوت استفاده شود. خارج از این مرحله: ذخیره‌سازی فایل‌های صوتی. نکته حیاتی: باید فرمت‌های صوتی پشتیبانی شده (OGG, MP3, WAV) مدیریت شوند.
**Excerpt:**
```
و از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....
```

### Step 13: همگام‌سازی دوطرفه پیام‌ها بین تلگرام و چت وب
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد مکانیزمی برای همگام‌سازی پیام‌ها بین تلگرام و چت وب است. هر پیام ارسال شده در تلگرام باید در چت وب نیز نمایش داده شود و بالعکس. باید از WebSocket یا polling برای实时 همگام‌سازی استفاده شود. خارج از این مرحله: مدیریت فایل‌های رسانه‌ای. نکته حیاتی: باید از conflict avoidance و deduplication استفاده شود.
**Excerpt:**
```
و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....
```

### Step 14: مدیریت خطاها و لاگینگ در سیستم تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی سیستم مدیریت خطا برای ربات تلگرام است. خطاهای رایج (اتصال، API، دیتابیس) باید به صورت دوستانه به کاربر نمایش داده شود و در لاگ‌ها ثبت شود. باید از logging module با سطوح مختلف (INFO, ERROR, DEBUG) استفاده شود. خارج از این مرحله: مانیتورینگ پیشرفته. نکته حیاتی: خطاهای حساس (مثل token) نباید به کاربر نمایش داده شود.
**Excerpt:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه
```

### Step 15: تست و دیباگ کامل سیستم تلگرام دوطرفه
**Status:** `pending` (0%)
**Scope:** این مرحله شامل تست کامل تمامی قابلیت‌های پیاده‌سازی شده است: ارسال و دریافت پیام، نوتیفیکیشن، احراز هویت، دستورات کنترلی، مدیریت محتوا، اتصال به AI، و همگام‌سازی. باید تست‌های unit و integration نوشته شود. خارج از این مرحله: تست load یا performance. نکته حیاتی: باید سناریوهای edge case (مثل ارسال همزمان چند پیام) تست شود.
**Excerpt:**
```
و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....
```
