---
task_id: dc721af2-f84f-424d-a491-91e8ab3624c0
title: میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار (6 مرحله)
type: other
priority: medium
execution_priority: 3000
status: pending
external_status: pending
verification_status: pending
watched_id: 6b04f8bd-b562-4fb9-9cd9-0c27458ced59
project: mahdighandi1989/language
created_at: '2026-05-12T11:13:22.960230+00:00'
updated_at: '2026-06-02T08:24:32.015871+00:00'
target_files:
- backend/server.js
- backend/.env.example
- backend/package.json
- frontend/index.html
---

# میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار (6 مرحله)

## Raw Idea

میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم و از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

📖 **خواندن کامل + اجرای مو-به-مو (بسیار مهم):**

این پرامپت — از این یادداشت تا انتها — یک سند واحد است که هر بخشش
حاوی الزام یا context منحصربه‌فرد است. خواندن سطحی یا skim کردن **ممنوع**
است.

- پرامپت را **سطر به سطر** بخوان، نه head/tail/فقط-بخش-اصلی.
- اگر بخشی به‌نظر طولانی یا تکراری آمد، **حتماً** بخوان — تفاوت‌های
  ریز ممکن است در آن جا اساسی باشند.
- هر جمله، URL، نام فایل، نام تابع، یا مقدار عددی که در پرامپت آمده،
  دقیقاً همان است که کاربر می‌خواهد — تغییرش نده، رندش نکن، خلاصه‌اش
  نکن.
- اگر پرامپت چندین درخواست/مرحله/زیرتسک دارد، **همه** را پیاده کن. حتی
  یکی را نه به‌عنوان "خارج از scope" حذف کن.

❌ ممنوعات صریح:
- خلاصه‌سازی متن کاربر در commit message یا response
- "این بخش اصلی نیست، رد می‌کنم"
- "کاربر احتمالاً منظورش این بود..." — منظورش همان است که نوشته
- "این URL/نام به نظر قدیمی است، آپدیتش کردم" — تغییر بدون درخواست ممنوع
- پیاده‌سازی فقط بخشی از پرامپت و تظاهر به کامل بودن
- "همه آیتم‌های لیست A را بررسی کردم، B و C مشابه بودند" — نه؛
  هرکدام را جداگانه

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

🔗 **وابستگی‌ها و همگام‌سازی (بسیار حیاتی — هرگز skip نکن):**

این بخش از همهٔ بخش‌های دیگرِ این یادداشت **مهم‌تر** است. اگر نقض شود،
نتیجهٔ کار ممکن است مشروع به‌نظر برسد ولی در عمل بخش‌های دیگر سیستم را عقب
بیندازد، broken reference تولید کند، یا منجر به data corruption شود.

پیش از و حین تغییر، تمام وابستگی‌ها را در **چهار جهت** به‌طور **کامل و
بدون هیچ خلاصه‌سازی** شناسایی و همگام کن:

**۱. وابستگی‌های upstream (این تسک به چه چیزهایی متکی است):**
- چه فایل‌ها، توابع، کلاس‌ها، API endpoint ها، schema های دیتابیس،
  env vars، یا config هایی که این تسک نیاز دارد؟
- آیا قرار است چیزی را ویرایش/حذف کنی که جای دیگر (signature، رفتار،
  return type، side effect) از آن انتظار خاصی می‌رود؟
- اگر dependency جدیدی اضافه می‌کنی، آیا با dependencyهای موجود تداخل
  دارد (نسخه، compat، lock file)؟

**۲. وابستگی‌های downstream (چه چیزهایی به این تسک متکی‌اند):**
- چه فایل‌ها، توابع، تست‌ها، migrations، docs، یا UI component هایی از
  کدی که داری ویرایش/اضافه/حذف می‌کنی **استفاده می‌کنند**؟
- با grep و reference search **همه‌ی** call sites، importها، subclassها،
  reference های مستقیم و غیرمستقیم را پیدا کن — نه فقط چند مورد اصلی.
- خصوصاً برای حذف یا rename: هیچ broken reference نباید باقی بماند.

**۳. وابستگی‌های cross-tier (بسیار مهم — هرگز فقط یک لایه را نبین):**

تسک شما ممکن است از backend، frontend، database، worker، یا هر tier
دیگری شروع شده باشد. ولی تغییرات تقریباً همیشه روی tier های دیگر هم
اثر می‌گذارند. **مستقل از اینکه تسک از کدام tier است**، این چک‌های دو
طرفه را همیشه انجام بده:

🔁 **اگر backend را تغییر دادی** (API، service، model، route):
  → frontend: کدام component/page/hook این endpoint یا data shape را
    مصرف می‌کند؟ type definition، state shape، error handling، loading
    state، form validation، URL routing همگی باید همگام شوند.
  → mobile/SDK/client library (اگر پروژه دارد): همان داستان frontend.
  → database: آیا migration لازم است؟ آیا rollback امن است؟
  → background workers: آیا event producer/consumer ها تحت تأثیرند؟
  → rate limit، auth، CORS، CSP: آیا رفتار جدید پشتیبانی می‌شود؟

🔁 **اگر frontend را تغییر دادی** (component، form، state، route):
  → backend: آیا endpoint جدید/تغییریافته لازم است؟ آیا data shape ای
    که ارسال می‌شود با schema سرور سازگار است؟
  → backend validation: آیا برای ورودی‌های جدید UI کافی است؟
  → permissions/RBAC: آیا feature جدید نیاز به role check جدید دارد؟
  → analytics/tracking: آیا event های جدید باید در backend log شوند؟
  → SEO/SSR: آیا تغییر route نیاز به sitemap/meta tags جدید دارد؟

🔁 **اگر database/migration را تغییر دادی**:
  → backend models (ORM، Pydantic، dataclasses) همگی به‌روزند؟
  → query های raw SQL یا ORM queries با schema جدید سازگارند؟
  → seed data، fixtures، factory functions تست‌ها به‌روزند؟
  → frontend: آیا data shape جدید در UI به‌درستی render می‌شود؟
  → rollback migration نوشته شده و امن است؟

🔁 **اگر API contract یا event schema را تغییر دادی** (REST، GraphQL،
   WebSocket، gRPC، Kafka، …):
  → OpenAPI/GraphQL schema/proto file آپدیت شد؟
  → همه‌ی consumer ها (client، subscriber، webhook، external API
    user) با version جدید سازگارند؟
  → backward compatibility حفظ شده یا migration path روشن است؟
  → versioning header/path اگر breaking change است؟

🔁 **اگر infrastructure یا config را تغییر دادی** (Dockerfile، CI، Render
   config، env، secrets):
  → README setup/installation section به‌روزه؟
  → `.env.example` با env vars جدید آپدیت شد؟
  → deploy script یا CI workflow هم تغییر کرد؟
  → docs/architecture یا diagram های infrastructure به‌روزند؟

⚠️ **هرگز فقط یک tier را تغییر نده و فرض کنی بقیه خودکار همگام می‌شوند.**
   حتی برای تغییرات به‌ظاهر «کوچک»، چک کن.

**۴. وابستگی‌های جانبی (artifacts که همیشه چک شوند):**

تغییرات کد همیشه روی این artifact ها اثر دارند. **همه را** بررسی و
به‌روز کن — مستندات اولویت **بالا** دارد چون فراموش‌شدنی‌ترین است.

  📝 **مستندات** (همیشه چک کن — حتی برای تغییر کوچک کد):
    - README.md (شرح، setup، نمونه‌های استفاده، badge ها)
    - CHANGELOG.md / RELEASE_NOTES.md
    - docs/ folder (architecture، API reference، user guides، runbooks)
    - inline docstrings/کامنت‌های توابع و کلاس‌های تغییریافته
    - OpenAPI/Swagger annotations، JSDoc/TSDoc
    - architecture diagrams (اگر component اضافه/حذف شد)
    - migration guides (اگر breaking change است)

  🌍 **مستندات کاربر**:
    - i18n files و translation keys
    - UI labels، tooltip ها، help text، error messages
    - in-app onboarding (اگر flow جدید است)

  🧪 **تست‌ها**:
    - unit tests (همه‌ی فایل‌های مرتبط — حتی اگر «بی‌ربط» به‌نظر می‌رسد)
    - integration tests
    - e2e tests (Playwright/Cypress/Selenium)
    - snapshot tests (اگر UI تغییر کرد)
    - contract tests (Pact یا مشابه)
    - performance benchmarks (اگر behavior performance-sensitive تغییر کرد)

  🧬 **type definitions و contracts**:
    - .d.ts files
    - Pydantic models، dataclasses
    - Protobuf/Avro/Thrift schemas
    - GraphQL schema definitions
    - JSON Schemas

  🏗 **infrastructure و config**:
    - Dockerfile، docker-compose.yml
    - Kubernetes manifests
    - Render/Vercel/Netlify config
    - GitHub Actions / GitLab CI workflows
    - environment templates (.env.example، .env.sample)
    - feature flags (LaunchDarkly، GrowthBook، config)

  📊 **monitoring و observability**:
    - logging keys (اگر اضافه/حذف شد، log parser ها هم به‌روز شوند)
    - metric names (Prometheus، Datadog)
    - tracing spans
    - alert rules و dashboards
    - error tracking (Sentry rules، groupings)

  🔐 **security**:
    - auth rules (rate limit، CORS، CSP، HSTS)
    - permissions/RBAC config
    - secrets rotation policies
    - audit log events (اگر action جدید اضافه شد)

  💾 **caches و serialization**:
    - cache keys و TTL (اگر data shape یا lifecycle تغییر کرد)
    - serializer formats (Redis، session storage)
    - browser storage (localStorage، IndexedDB schemas)

**قانون مطلق همگام‌سازی:**
- هر چیزی که در (۱)، (۲)، (۳)، یا (۴) شناسایی شد، در **همان workflow
  این تسک** همگام و به‌روز شود. هرگز برای بعد رها نکن.
- اگر یک فایل/تست/docs نسبت به تغییر شما عقب بماند، در بهترین حالت bug،
  در بدترین حالت مشکل امنیتی یا data corruption تولید می‌کند.
- تغییرات همگام‌سازی می‌توانند در commit جداگانه باشند (در همان task)،
  ولی نباید skip شوند یا به «refactor آینده» سپرده شوند.

**هرگز این جمله‌ها قابل قبول نیست:**
- ❌ «بعداً پیداش می‌کنم»
- ❌ «احتمالاً جای دیگه‌ای استفاده نمی‌شه»
- ❌ «این یه refactor جداگانه‌ست — out of scope»
- ❌ «فقط فایل‌های اصلی رو بررسی کردم»
- ❌ «حدس می‌زنم چیزی بهش وابسته نیست»
- ❌ «دامنه‌ی وابستگی‌ها رو خلاصه کردم» — هرگز خلاصه نکن
- ❌ «این task فقط backend است؛ frontend مشکل خودش» — هرگز
- ❌ «این task فقط frontend است؛ backend از قبل کار می‌کند» — هرگز ثابت نکرده
- ❌ «مستندات بعداً به‌روز می‌شن» — همیشه same-task همگام شوند
- ❌ «testها رو نگاه نکردم چون فقط یه تغییر کوچیک بود»

**در commit message یا PR description**، دامنهٔ وابستگی‌های شناسایی‌شده و
همگام‌شده را به‌طور explicit و **per-tier** بنویس. مثال:
```
Dependencies synced:
- upstream: User model schema, auth middleware
- downstream: 3 API endpoints, 5 frontend components, 12 tests
- cross-tier (backend → frontend): UserProfile.tsx, useUser.ts hook,
  api-types.ts (TS definitions)
- cross-tier (backend → infra): .env.example added NEW_AUTH_SCOPES
- side artifacts: OpenAPI spec, README API section, i18n keys for
  new errors, Sentry alert rule for new error code
```
اگر هیچ وابستگی پیدا نکردی در هر کدام از چهار جهت، صریحاً بنویس:
«بررسی شد — هیچ وابستگی upstream / downstream / cross-tier (backend↔
frontend↔db↔infra) / side شناسایی نشد» تا مشخص باشد بررسی **انجام شده**
نه اینکه فراموش شده.

📋 **مدیریت TO-DO برای اقدامات دستی کاربر (همیشه چک کن):**

⚠️ **هشدار بحرانی — قاعدهٔ ضد-فرار:** TO-DO فقط برای کارهایی است که
**واقعاً غیرممکن** برای agent است (نیاز به انسان مطلق)، نه برای کارهایی
که «بزرگ‌اند»، «وقت می‌برند»، یا «نیازمند fixture/setup» هستند. اگر یک
agent در یک سشن بیش از **۲۰٪ از تسک‌ها** را با TO-DO ببندد، یعنی از کار
فرار می‌کند — این الگو در سشن‌های قبلی **مشاهده** شده و الان ممنوع است.

✅ **فقط برای این موارد TO-DO بساز** (لیست بسته — هرچه خارج این لیست
ممنوع است):

  ۱. **Credential/secret که فقط کاربر دارد**:
     - تنظیم API key واقعی در پنل ادمین خارجی (Render، AWS، Stripe، …)
     - تأیید OAuth client روی console آن سرویس
     - paste کردن webhook secret که فقط بعد از ساخت در dashboard ظاهر می‌شود

  ۲. **Account/billing روی سرویس خارجی که کاربر باید عضو شود**:
     - ساخت account جدید روی Stripe/SendGrid/Twilio/Google Cloud
     - تأیید verification شماره یا ID
     - فعال‌سازی subscription پولی

  ۳. **داده/asset خصوصی که فقط کاربر دارد**:
     - آپلود لوگو/تصویر/فونت برند
     - paste کردن داده‌ای که در محل کار کاربر است
     - import داده‌ای که فقط روی device کاربر است

  ۴. **تصمیم سلیقه‌ای/حقوقی/کسب‌وکار**:
     - انتخاب رنگ‌بندی نهایی یا تم
     - متن دقیق Terms of Service / Privacy Policy
     - تعرفهٔ قیمت‌گذاری
     - نام نهایی برند یا دامنه

⛔ **هرگز TO-DO نکن برای** (لیست سیاه — هر چیزی که در این لیست است
**قابل اجرا** توسط agent است، حتی اگر بزرگ یا چندبخشی باشد):

  ❌ UI component / page / dashboard (هر فریم‌ورک: React, Vue, Angular,
     Svelte، حتی اگر معماری بزرگ دارد) — می‌توانی stub اولیه + state
     management + layout + استایل بسازی
  ❌ "نیازمند Google Drive / Stripe / Twilio API" — می‌توانی **client
     stub** با abstraction layer بسازی که با env var واقعی plug-in شود؛
     کد integration یعنی پیاده‌سازی، نه TO-DO
  ❌ "feature بزرگ، چند روز کار می‌برد" — اندازه دلیل defer نیست؛ کوچک
     شروع کن، iterate کن، در همین سشن کامل کن
  ❌ Celery / background worker / scheduler — یک task ساده + register
     می‌توانی بسازی
  ❌ Migration / model / schema — حتی اگر فیلد جدید نیاز دارد، اضافه کن
  ❌ REST endpoint / GraphQL resolver / WebSocket route — هرگز TO-DO
  ❌ test (unit/integration/e2e) — همیشه قابل نوشتن
  ❌ Documentation / README / API docs — همیشه قابل نوشتن
  ❌ Config file / .env.example / Dockerfile / CI workflow — همیشه قابل
     نوشتن
  ❌ "می‌توانستی .tsx ولی repo .jsx است" — از .jsx استفاده کن، TO-DO نکن
  ❌ "نیازمند فیلد X در مدل دیگر" — اضافه کن فیلد را، TO-DO نکن
  ❌ "تصمیم admin-vs-user-scoped" — پرامپت اولیه scope را معلوم کرده،
     یا با محتاطانه‌ترین تفسیر پیش برو
  ❌ "credential در production هنوز ست نیست" — این TO-DO ساده برای
     تنظیم env var است (مورد ۱ بالا)، نه دلیل برای defer کردن کد
  ❌ "نیازمند verification از کاربر" — اگر اقدام واقعی غیرممکن نیست،
     پیش برو
  ❌ هر چیزی که در یک کامنت `# TODO` معمولی نوشته می‌شد — این فایل
     TO-DO نیست، کامنت inline است

🔬 **قاعدهٔ «حداقل تلاش» قبل از TO-DO**: قبل از TO-DO کردن یک AC، **اثبات
کن** که قابل انجام نیست:

  ۱. آیا می‌توانم یک stub/placeholder بسازم که با env واقعی plug-in شود؟
     → اگر بله، بساز و TO-DO نکن
  ۲. آیا می‌توانم برای این بخش یک test (حتی mock-based) بنویسم؟
     → اگر بله، بنویس و TO-DO نکن
  ۳. آیا می‌توانم abstraction/interface را تعریف کنم، حتی اگر backend
     واقعی نیست؟ → اگر بله، تعریف کن و TO-DO نکن
  ۴. آیا فقط یک حالت سلیقه‌ای/decision کاربر در میان است؟
     → فقط آن یک decision را TO-DO کن، نه کل feature را

اگر یکی از این چهار راه‌حل ممکن بود ولی به TO-DO رفتی، **اعتبار شما از
بین می‌رود**.

📊 **آستانهٔ TO-DO per session**: در یک حلقهٔ اجرای N تسک، اگر بیشتر از
**۲۰٪** تسک‌ها فایل TO-DO ساختی، خودت در گزارش پایانی صریحاً اعلام کن:

  "⚠️ نسبت TO-DO من {K}/{N} = {%} است که از آستانهٔ ۲۰٪ بالاتر است.
   احتمالاً برخی از این TO-DO ها قابل اجرا بودند ولی من فرار کردم.
   لیست TO-DO ها را کاربر باید بازبینی کند که آیا واقعاً Manual-required
   بودند یا agent ضعیف کار کرده."

**یادآوری همیشگی:** اگر در آینده قابلیت‌های شما گسترش پیدا کرد و توانستید
یکی از موارد لیست سفید را خودکار انجام دهید (مثلاً managed credential
injection، یا integration پولی automate شود)، انجام دهید و TO-DO نسازید.
لیست سفید بسته است ولی **بسته از پایین** (می‌تواند کوچک‌تر شود اگر
قابلیت‌ها رشد کنند، ولی هرگز بزرگ‌تر نشود برای فرار).

**اگر هیچ بخش Manual-required نبود (تمام تسک Auto-capable است)**:
  → فایل TO-DO **نساز**. فولدر TO-DO/ باید پاک و معنادار بماند.
  → اگر برای این task از قبل `TO-DO/todo-task-{task_id_first_8}.md` بود
     (یعنی در run قبلی نیاز به دخالت کاربر بود ولی الان نه): فایل قدیمی
     را پاک کن و entry را از `TO-DO/_index.json` حذف کن.

**اگر بخش Manual-required دارد** (همه‌جانبه یا hybrid):
  1. فولدر TO-DO/ را در ریشه ریپو ایجاد کن اگر نیست
  2. فایل `TO-DO/todo-task-{task_id_first_8}.md` بساز با front-matter
     شامل: task_id, task_title, execution_priority, created_at,
     updated_at, status: "pending"
     و در بدنه: «چرا این فایل ساخته شد»، «وضعیت بخش‌های خودکار»
     (commit ها reference)، «کارهایی که باید انجام دهی» با اولویت
     بالا/متوسط/پایین به ترتیب، «وقتی این کارها را تمام کردی»
  3. `TO-DO/_index.json` را با **merge** آپدیت کن (نه overwrite):
     - فایل موجود را بخوان
     - entry های orphan (فایلشان پاک شده) را حذف کن
     - entry این task را اضافه/replace کن
     - بر اساس execution_priority صعودی مرتب کن
     - ساختار: `{"version":1, "generated_at": ISO, "total": N, "items": [...]}`
  4. این تغییرات TO-DO را در **همان commit کد** شامل کن (نه commit جداگانه)

⛔ **ممنوعات مطلق TO-DO**:
  ❌ ساختن TO-DO برای کاری که می‌توانستی خودت انجام دهی (شلوغی فولدر)
  ❌ overwrite کردن `TO-DO/_index.json` بدون merge (data loss)
  ❌ نگه‌داشتن entry هایی که فایل‌شان پاک شده (broken reference)
  ❌ فراموش کردن نوشتن «خروجی مورد انتظار» در هر آیتم TO-DO

این بخش الزامی است. حتی اگر فکر می‌کنی "این تسک کاملاً auto است و نیازی
به TO-DO نیست"، صریحاً در commit message یا report بنویس:
"بررسی شد — این تسک هیچ بخش Manual-required ندارد، TO-DO ساخته نشد."

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

🔁 **Commit + Push فوری per-task (بسیار مهم برای جریان کار صحیح):**

پس از اتمام پیاده‌سازی این تسک، **بلافاصله** commit کن و **همان موقع**
به default branch (main/master) push کن. سپس به تسک بعدی برو.

✓ چرا این قانون حیاتی است:
  - تسک‌های بعدی ممکن است به فایل‌ها/تغییراتی که این تسک ایجاد کرده
    نیاز داشته باشند. اگر push نکنی، `git pull` بعدی آن‌ها را نمی‌بیند.
  - جمع‌کردن تغییرات چند تسک منجر به conflict های بزرگ می‌شود.
  - اگر در میانه fail کنی، task های push شده ضایع نمی‌شوند.

⛔ ممنوع: "همه task ها را تمام می‌کنم بعد یک‌جا push می‌زنم"
⛔ ممنوع: branch جدا برای task — مستقیم به default branch
⛔ ممنوع: task بعدی بدون push کامل task قبلی

---


---

## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند.)_

```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم و از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....
```

## 📋 چک‌لیست مراحل (10 مرحله)

این تسک به مراحل کوچک‌تر تقسیم شده. **در هر verify خودکار، وضعیت هر مرحله به‌صورت `[ ]` (انجام نشده)، `[~]` (ناقص)، یا `[x]` (انجام شده) به‌روز می‌شود.**
وقتی تمام مراحل `[x]` شدند، تسک به‌طور خودکار به «انجام شده» منتقل می‌شود.

- [ ] **مرحله 1: ایجاد ساختار پایه برای نوتیفیکیشن تلگرام دوطرفه** — این مرحله شامل ایجاد یک ماژول پایه برای ارتباط با تلگرام (bot) است. باید یک bot تلگرام با استفاده از کتابخانه python-telegram-bot یا aiogram ایجاد شود که قابلیت دریافت و ارسال پیام را داشته باشد. این مرحله فقط شامل راه‌اندازی اولیه bot و اتصال به API تلگرام است. خارج از این مرحله: پیاده‌سازی منطق ار
- [ ] **مرحله 2: پیاده‌سازی ارسال نوتیفیکیشن از سیستم به تلگرام** — این مرحله شامل ایجاد یک سرویس یا تابع در سیستم است که بتواند پیام‌های نوتیفیکیشن را به تلگرام ارسال کند. باید مشخص شود چه رویدادهایی (مثلاً ثبت‌نام جدید، خطا، به‌روزرسانی) باعث ارسال پیام می‌شوند. خارج از این مرحله: دریافت دستورات از تلگرام برای کنترل سیستم. نکته حیاتی: پیام‌ها باید شامل اطلاعات مفی
- [ ] **مرحله 3: پیاده‌سازی دریافت و پردازش دستورات از تلگرام برای کنترل سیستم** — این مرحله شامل ایجاد handlerهای مختلف برای دستورات دریافتی از تلگرام است. باید دستورات پایه مانند /status، /help، /courses، /upload و ... تعریف شوند. خارج از این مرحله: اتصال به بخش محتوای دروس یا تمرین با استاد هوش مصنوعی. نکته حیاتی: دستورات باید به صورت ماژولار طراحی شوند تا بعداً به راحتی گسترش 
- [ ] **مرحله 4: ایجاد ماژول مشاهده و آپلود محتوای دروس از طریق تلگرام** — این مرحله شامل ایجاد قابلیت مشاهده لیست دروس، مشاهده جزئیات هر درس، و آپلود فایل‌های مرتبط با دروس از طریق تلگرام است. باید دستورات /courses، /course <id>، /upload_course <id> پیاده‌سازی شوند. خارج از این مرحله: مدیریت تمرین با استاد هوش مصنوعی. نکته حیاتی: فایل‌های آپلودی باید در سیستم ذخیره و ایند
- [ ] **مرحله 5: اتصال به بخش 'تمرین با استاد هوش مصنوعی' از طریق تلگرام** — این مرحله شامل ایجاد ارتباط بین bot تلگرام و بخش تمرین با استاد هوش مصنوعی است. باید بتوان از تلگرام موضوعات تمرین را مشاهده، انتخاب و مدیریت کرد. خارج از این مرحله: پیاده‌سازی خود چت با هوش مصنوعی. نکته حیاتی: باید API یا interface مناسبی برای ارتباط با بخش استاد هوش مصنوعی وجود داشته باشد.
- [ ] **مرحله 6: پیاده‌سازی چت دوطرفه با هوش مصنوعی از طریق تلگرام** — این مرحله شامل ایجاد قابلیت چت متنی با هوش مصنوعی موجود در بخش تمرین از طریق تلگرام است. باید پیام‌های متنی کاربر به هوش مصنوعی ارسال شده و پاسخ دریافت و در تلگرام نمایش داده شود. خارج از این مرحله: مدیریت پیام‌های صوتی. نکته حیاتی: باید state جلسه چت حفظ شود تا مکالمه پیوسته باشد.
- [ ] **مرحله 7: پیاده‌سازی ارسال و دریافت پیام صوتی از طریق تلگرام برای تعامل با هوش مصنوعی** — این مرحله شامل قابلیت دریافت پیام صوتی از کاربر در تلگرام، ارسال آن به سرویس تحلیل و استخراج متن (ASR)، ارسال متن استخراج‌شده به هوش مصنوعی، دریافت پاسخ صوتی از هوش مصنوعی و ارسال آن به کاربر در تلگرام است. خارج از این مرحله: ذخیره‌سازی فایل‌های صوتی. نکته حیاتی: باید از میکروفون تلگرام استفاده شود 
- [ ] **مرحله 8: یکپارچه‌سازی و همگام‌سازی وضعیت چت بین تلگرام و وب** — این مرحله شامل اطمینان از همگام‌سازی وضعیت چت بین تلگرام و وب‌سایت است. هر پیام و پاسخی که در یک پلتفرم رد و بدل می‌شود باید در پلتفرم دیگر نیز قابل مشاهده باشد. خارج از این مرحله: پیاده‌سازی features جدید. نکته حیاتی: باید از یک دیتابیس مشترک یا event-driven architecture استفاده شود.
- [ ] **مرحله 9: مدیریت خطا و لاگ‌گیری در ارتباط تلگرام** — این مرحله شامل پیاده‌سازی سیستم مدیریت خطا برای ارتباط با تلگرام است. باید خطاهای احتمالی (قطع ارتباط، timeout، خطای API) مدیریت شوند و لاگ مناسب ثبت شود. خارج از این مرحله: features جدید. نکته حیاتی: باید از retry mechanism و fallback استفاده شود.
- [ ] **مرحله 10: تست و اعتبارسنجی نهایی قابلیت‌های تلگرام** — این مرحله شامل تست تمام قابلیت‌های پیاده‌سازی‌شده (ارسال نوتیفیکیشن، دریافت دستورات، مدیریت دروس، چت با هوش مصنوعی، پیام صوتی) است. باید سناریوهای مختلف تست شوند. خارج از این مرحله: توسعه features جدید. نکته حیاتی: باید هم تست واحد و هم تست یکپارچه‌سازی انجام شود.

---

# 🔹 مرحله 1: ایجاد ساختار پایه برای نوتیفیکیشن تلگرام دوطرفه

**Scope:** این مرحله شامل ایجاد یک ماژول پایه برای ارتباط با تلگرام (bot) است. باید یک bot تلگرام با استفاده از کتابخانه python-telegram-bot یا aiogram ایجاد شود که قابلیت دریافت و ارسال پیام را داشته باشد. این مرحله فقط شامل راه‌اندازی اولیه bot و اتصال به API تلگرام است. خارج از این مرحله: پیاده‌سازی منطق ارسال نوتیفیکیشن از سیستم، دریافت دستورات خاص، یا اتصال به سایر بخش‌های پروژه. نکته حیاتی: باید از webhook یا polling استفاده شود و توکن bot در محیط امن (env) ذخیره شود.
**Key terms:** python-telegram-bot, aiogram, bot token, webhook, polling, Telegram API

**بخش مربوط از متن کاربر:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد ماژول پایه تلگرام بات دوطرفه با پشتیبانی webhook/polling

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:61-68` — `app و server و wss` — بعد از ایجاد WebSocket server (خط 68)، باید ماژول telegramBot را import کرده و startBot() را فراخوانی کرد. همچنین endpoint GET /api/telegram/status باید به مسیرهای موجود اضافه شود.
  ```jsx
  const app = express();
  const PORT = process.env.PORT || 3001;
  
  // Create HTTP server
  const server = createServer(app);
  
  // Create WebSocket server
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/.env.example:1-6` — `فایل env.example` — باید خط جدید # Telegram Bot Token - Get from https://t.me/BotFather و TELEGRAM_BOT_TOKEN=your_bot_token_here اضافه شود. همچنین می‌توان گزینه TELEGRAM_USE_WEBHOOK=false را برای انتخاب بین polling/webhook اضافه کرد.
  ```
  # Gemini API Key - Get from https://aistudio.google.com/apikey
  GEMINI_API_KEY=your_gemini_api_key_here
  
  # Port (optional, default 3001)
  PORT=3001
  
  # Firebase config (optional - for data persistence)
  # VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد یک ماژول پایه برای ارتباط دوطرفه با تلگرام (bot) را دارد. این ماژول باید با استفاده از کتابخانه python-telegram-bot یا aiogram ساخته شود و قابلیت دریافت و ارسال پیام را داشته باشد. کاربر تأکید کرده که این مرحله فقط شامل راه‌اندازی اولیه bot و اتصال به API تلگرام است و خارج از این مرحله: پیاده‌سازی منطق ارسال نوتیفیکیشن از سیستم، دریافت دستورات خاص، یا اتصال به سایر بخش‌های پروژه. نکته حیاتی: باید از webhook یا polling استفاده شود و توکن bot در محیط امن (env) ذخیره شود. کاربر همچنین خواسته که تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم. کلیدواژه‌های ذکر شده: python-telegram-bot, aiogram, bot token, webhook, polling, Telegram API. پروژه فعلی یک اپلیکیشن آموزش لهجه لبنانی با هوش مصنوعی است که از backend Express.js (backend/server.js) و frontend React (frontend/src/App.jsx) استفاده می‌کند. backend/server.js شامل APIهای مختلف از جمله /api/gemini/chat (خط 79)، /api/gemini/tts (خط 140)، /api/analyze-files (خط 727) و WebSocket در /ws/live (خط 68) است. فایل backend/.env.example نشان می‌دهد که متغیرهای محیطی شامل GEMINI_API_KEY و PORT هستند. فایل package.json در ریشه پروژه نشان می‌دهد که پروژه از npm scripts برای مدیریت backend و frontend استفاده می‌کند. فایل .github/workflows/claude-auto-task.yml شامل CI/CD با Claude Code است. برای پیاده‌سازی این ماژول تلگرام، باید یک فایل جدید مانند backend/services/telegramBot.js ایجاد شود که از کتابخانه node-telegram-bot-api (معادل پایتونی python-telegram-bot در Node.js) استفاده کند. این ماژول باید با استفاده از polling (ساده‌تر برای توسعه) یا webhook (برای production) کار کند و توکن bot را از متغیر محیطی TELEGRAM_BOT_TOKEN در فایل .env بخواند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/services/telegramBot.js با ساختار زیر:
   - import کتابخانه node-telegram-bot-api (ابتدا باید با npm install node-telegram-bot-api نصب شود)
   - خواندن توکن از process.env.TELEGRAM_BOT_TOKEN
   - ایجاد نمونه bot با polling mode (options: { polling: true })
   - ثبت handler برای پیام‌های متنی (on('message', ...)) که پیام دریافتی را لاگ کند و پاسخ ساده ارسال کند
   - تابع startBot() برای شروع bot
   - تابع stopBot() برای توقف graceful
   - export کردن توابع برای استفاده در backend/server.js
2. افزودن متغیر TELEGRAM_BOT_TOKEN به backend/.env.example
3. در backend/server.js، import کردن ماژول telegramBot و فراخوانی startBot() بعد از راه‌اندازی سرور (حدود خط 65-68)
4. ایجاد endpoint ساده GET /api/telegram/status برای بررسی وضعیت bot
5. افزودن handler برای دستور /start که پیام خوش‌آمدگویی ارسال کند
6. اطمینان از اینکه polling در محیط development و webhook در production قابل تنظیم است (با متغیر TELEGRAM_USE_WEBHOOK)
7. افزودن logging مناسب با console.log برای دیباگ

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 2: پیاده‌سازی ارسال نوتیفیکیشن از سیستم به تلگرام

**Scope:** این مرحله شامل ایجاد یک سرویس یا تابع در سیستم است که بتواند پیام‌های نوتیفیکیشن را به تلگرام ارسال کند. باید مشخص شود چه رویدادهایی (مثلاً ثبت‌نام جدید، خطا، به‌روزرسانی) باعث ارسال پیام می‌شوند. خارج از این مرحله: دریافت دستورات از تلگرام برای کنترل سیستم. نکته حیاتی: پیام‌ها باید شامل اطلاعات مفید و قابل فهم باشند و قابلیت شخصی‌سازی داشته باشند.
**Key terms:** notification service, send_message, event handler, message formatter

**بخش مربوط از متن کاربر:**
```
علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن سرویس نوتیفیکیشن تلگرام به بک‌اند

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:76-77` — `GEMINI_API_KEY` — محل فعلی خواندن API key از environment — الگویی که برای TELEGRAM_BOT_TOKEN و TELEGRAM_CHAT_ID نیز باید تکرار شود. این خط نشان می‌دهد که پروژه از process.env برای کانفیگ استفاده می‌کند.
  ```jsx
  const GEMINI_API_KEY = process.env.GEMINI_API_KEY;
  ```
- `backend/server.js:79-137` — `app.post('/api/gemini/chat')` — اولین نقطه فراخوانی notify — هم برای خطای API key not configured (خط 81) و هم برای catch error (خط 134). این endpoint پرکاربردترین API پروژه است.
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      ...
    } catch (error) {
      console.error('Chat API error:', error);
      res.status(500).json({ error: error.message });
    }
  });
  ```
- `backend/server.js:189-192` — `app.get('/api/health')` — الگوی endpoint ساده برای کپی کردن و ساخت /api/health/notify-test. همچنین می‌توان timestamp را در پیام‌های نوتیفیکیشن استفاده کرد.
  ```jsx
  app.get('/api/health', (req, res) => {
    res.json({ status: 'ok', timestamp: new Date().toISOString() });
  });
  ```
- `backend/.env.example`

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی ارسال نوتیفیکیشن از سیستم به تلگرام را داده است. این شامل ایجاد یک سرویس یا تابع در سیستم است که بتواند پیام‌های نوتیفیکیشن را به تلگرام ارسال کند. باید مشخص شود چه رویدادهایی (مثلاً ثبت‌نام جدید، خطا، به‌روزرسانی) باعث ارسال پیام می‌شوند. خارج از این مرحله: دریافت دستورات از تلگرام برای کنترل سیستم. نکته حیاتی: پیام‌ها باید شامل اطلاعات مفید و قابل فهم باشند و قابلیت شخصی‌سازی داشته باشند. کاربر تأکید کرده: 'علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته'. کلیدواژه‌های مشخص شده: notification service, send_message, event handler, message formatter. در کد فعلی پروژه (backend/server.js)، هیچ سرویس نوتیفیکیشن یا ارسال پیام به تلگرام وجود ندارد. ساختار فعلی شامل: endpointهای Gemini Chat (خط 79)، Gemini TTS (خط 140)، File Analysis (خط 727)، و WebSocket در /ws/live (خط 68) است. فایل backend/.env.example فقط شامل GEMINI_API_KEY و PORT و Firebase config است — هیچ متغیری برای Telegram Bot Token وجود ندارد. فایل backend/package.json وابستگی‌های فعلی شامل express, cors, dotenv, ws, multer, fluent-ffmpeg, ffmpeg-static است — هیچ کتابخانه‌ای برای Telegram API (مثل node-telegram-bot-api یا telegraf) نصب نیست. فایل frontend/index.html شامل Firebase config (apiKey, authDomain, projectId, storageBucket, messagingSenderId, appId) است که می‌تواند برای رویدادهای احراز هویت (ثبت‌نام جدید) استفاده شود. فایل .github/workflows/claude-auto-task.yml نشان می‌دهد که سیستم از oversight backend برای مدیریت تسک‌ها استفاده می‌کند — این می‌تواند منبع رویدادهای خطا و به‌روزرسانی باشد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ۱. ایجاد فایل جدید backend/services/telegramNotifier.js با ساختار زیر: (الف) تابع sendTelegramMessage(botToken, chatId, message) که با استفاده از fetch (موجود در Node.js 18+) به Telegram Bot API در آدرس https://api.telegram.org/bot${botToken}/sendMessage درخواست POST ارسال کند. (ب) تابع formatNotification(eventType, data) که بسته به نوع رویداد (registration, error, update) یک پیام خوش‌فرمت Markdown بسازد. (ج) تابع notify(eventType, data) که ابتدا formatNotification را صدا بزند، سپس با خواندن مقادیر از process.env (مثل TELEGRAM_BOT_TOKEN و TELEGRAM_CHAT_ID) پیام را ارسال کند. ۲. افزودن متغیرهای محیطی جدید به backend/.env.example: TELEGRAM_BOT_TOKEN و TELEGRAM_CHAT_ID. ۳. افزودن import سرویس در backend/server.js (حدود خط 12) و فراخوانی notify در نقاط استراتژیک: (الف) بعد از خط 81 (API key not configured) برای خطاهای کانفیگ. (ب) بعد از خط 134 (Chat API error) برای خطاهای API. (ج) بعد از خط 185 (TTS API error) برای خطاهای TTS. (د) در endpoint /api/analyze-files (خط 727) برای خطاهای تحلیل فایل. ۴. افزودن یک endpoint جدید GET /api/health/notify-test در backend/server.js برای تست دستی ارسال نوتیفیکیشن. ۵. به‌روزرسانی فایل backend/package.json برای افزودن وابستگی اختیاری node-telegram-bot-api (اگر نیاز به قابلیت‌های پیشرفته‌تر مثل کیبورد شناور باشد).

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 3: پیاده‌سازی دریافت و پردازش دستورات از تلگرام برای کنترل سیستم

**Scope:** این مرحله شامل ایجاد handlerهای مختلف برای دستورات دریافتی از تلگرام است. باید دستورات پایه مانند /status، /help، /courses، /upload و ... تعریف شوند. خارج از این مرحله: اتصال به بخش محتوای دروس یا تمرین با استاد هوش مصنوعی. نکته حیاتی: دستورات باید به صورت ماژولار طراحی شوند تا بعداً به راحتی گسترش یابند.
**Key terms:** command handler, /status, /help, /courses, /upload, message handler

**بخش مربوط از متن کاربر:**
```
بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی handlerهای دستورات تلگرام برای کنترل سیستم

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:65-68` — `server creation` — بعد از ایجاد WebSocket سرور، باید بوت تلگرام را راه‌اندازی کنیم. این نقطه مناسب برای افزودن کد راه‌اندازی bot.launch() است.
  ```jsx
  // Create HTTP server
  const server = createServer(app);
  
  // Create WebSocket server
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/server.js:76-77` — `GEMINI_API_KEY constant` — الگوی خواندن API key از env. برای تلگرام هم باید TELEGRAM_BOT_TOKEN را به همین شکل از process.env بخوانیم.
  ```jsx
  const GEMINI_API_KEY = process.env.GEMINI_API_KEY;
  ```
- `backend/.env.example:1-8` — `environment variables` — باید خط TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here به این فایل اضافه شود.
  ```
  # Gemini API Key - Get from https://aistudio.google.com/apikey
  GEMINI_API_KEY=your_gemini_api_key_here
  
  # Port (optional, default 3001)
  PORT=3001
  
  # Firebase config (optional - for data persistence)
  # VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}
  ```
- `backend/package.json:1-15` — `dependencies` — باید وابستگی telegraf به dependencies اضافه شود.
  ```json
  {
    "name": "lebanese-dialect-backend",
    "version": "1.0.0",
    "dependencies": {
      "cors": "^2.8.5",
      "dotenv": "^16.3.1",
      "express": "^4.18.2",
      "ws": "^8.19.0"
    }
  }
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/server.js` (سطر 65) — فایل اصلی سرور که بوت تلگرام باید از آنجا راه‌اندازی شود و به endpointهای موجود دسترسی داشته باشد
- `backend/.env.example` (سطر 1) — محل تعریف متغیرهای محیطی — باید TELEGRAM_BOT_TOKEN به آن اضافه شود
- `backend/package.json` (سطر 1) — محل تعریف وابستگی‌ها — باید telegraf به dependencies اضافه شود
- `backend/package-lock.json` (سطر 1) — بعد از نصب telegraf این فایل به‌روزرسانی می‌شود
- `frontend/src/App.jsx` — احتمالاً نیاز به نمایش وضعیت اتصال تلگرام در UI دارد — اما خارج از scope این تسک است

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی دریافت و پردازش دستورات از تلگرام برای کنترل سیستم را دارد. این شامل ایجاد handlerهای مختلف برای دستورات دریافتی از تلگرام است. دستورات پایه مانند /status، /help، /courses، /upload و ... باید تعریف شوند. خارج از این مرحله: اتصال به بخش محتوای دروس یا تمرین با استاد هوش مصنوعی. نکته حیاتی: دستورات باید به صورت ماژولار طراحی شوند تا بعداً به راحتی گسترش یابند. کاربر می‌خواهد 'بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم'. کلیدواژه‌ها: command handler, /status, /help, /courses, /upload, message handler. در کد فعلی پروژه (backend/server.js) هیچ اثری از Webhook تلگرام، کتابخانه‌ای مثل node-telegram-bot-api یا telegraf، و هیچ handlerای برای دستورات تلگرام وجود ندارد. پروژه در backend/server.js از Express (خط 1-2)، WebSocket (خط 7)، multer برای آپلود فایل (خط 8)، ffmpeg برای پردازش ویدیو (خط 11-12) و Gemini API (خط 76-452) استفاده می‌کند. endpointهای موجود شامل /api/gemini/chat (خط 79)، /api/gemini/tts (خط 140)، /api/analyze-files (خط 727)، /api/health (خط 190) و /api/list-models (خط 195) هستند. frontend/index.html یک SPA با React است که از Firebase (خط 11-18) استفاده می‌کند. برای پیاده‌سازی این قابلیت، باید یک ماژول مجزای تلگرام بوت در backend ایجاد شود که از کتابخانه‌ای مانند telegraf یا node-telegram-bot-api استفاده کند و handlerهای ماژولار برای دستورات /status، /help، /courses، /upload و ... تعریف شوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/telegram/bot.js به عنوان نقطه ورود بوت تلگرام با استفاده از کتابخانه telegraf. 2. ایجاد فایل backend/telegram/handlers/status.js برای دستور /status که وضعیت سیستم (uptime, API key status, disk usage) را برمی‌گرداند. 3. ایجاد فایل backend/telegram/handlers/help.js برای دستور /help که لیست دستورات موجود را نمایش می‌دهد. 4. ایجاد فایل backend/telegram/handlers/courses.js برای دستور /courses که لیست دوره‌های موجود را نمایش می‌دهد (placeholder). 5. ایجاد فایل backend/telegram/handlers/upload.js برای دستور /upload که راهنمای آپلود فایل را ارائه می‌دهد. 6. ایجاد فایل backend/telegram/index.js برای export همه handlerها به صورت ماژولار. 7. افزودن middleware در backend/server.js برای راه‌اندازی بوت تلگرام در زمان استارت سرور (حدود خط 65-68). 8. افزودن متغیر محیطی TELEGRAM_BOT_TOKEN به backend/.env.example. 9. نصب وابستگی telegraf در backend/package.json. 10. طراحی ساختار handlerها به صورت کلاس/تابع با interface یکسان (ctx => Promise<void>) برای قابلیت گسترش.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 4: ایجاد ماژول مشاهده و آپلود محتوای دروس از طریق تلگرام

**Scope:** این مرحله شامل ایجاد قابلیت مشاهده لیست دروس، مشاهده جزئیات هر درس، و آپلود فایل‌های مرتبط با دروس از طریق تلگرام است. باید دستورات /courses، /course <id>، /upload_course <id> پیاده‌سازی شوند. خارج از این مرحله: مدیریت تمرین با استاد هوش مصنوعی. نکته حیاتی: فایل‌های آپلودی باید در سیستم ذخیره و ایندکس شوند.
**Key terms:** /courses, /course, /upload_course, course content, file upload

**بخش مربوط از متن کاربر:**
```
بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد ماژول تلگرام برای مشاهده و آپلود دروس

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:19-56` — `multer configuration` — پیکربندی multer برای آپلود فایل‌ها - باید برای آپلود فایل‌های دروس از طریق تلگرام复用 شود
  ```jsx
  const uploadDir = join(os.tmpdir(), 'lebanese-uploads');
  if (!fs.existsSync(uploadDir)) {
    fs.mkdirSync(uploadDir, { recursive: true });
  }
  
  const storage = multer.diskStorage({
    destination: (req, file, cb) => cb(null, uploadDir),
    filename: (req, file, cb) => cb(null, `${Date.now()}-${file.originalname}`)
  });
  ```
- `backend/server.js:68` — `WebSocket server` — WebSocket برای اطلاع‌رسانی آپلودهای جدید به فرانت‌اند
  ```jsx
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/server.js:727` — `app.post('/api/analyze-files')` — الگوی endpoint آپلود فایل - مشابه برای /api/courses استفاده شود
  ```jsx
  app.post('/api/analyze-files', upload.array('files', MAX_FILES), handleMulterError, async (req, res) => {
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `frontend/src/App.jsx` (سطر 1) — کامپوننت اصلی فرانت‌اند که باید برای نمایش لیست دروس و جزئیات به‌روزرسانی شود
- `frontend/index.html` (سطر 11) — حاوی Firebase config (خطوط 11-18) که می‌تواند برای ذخیره‌سازی داده‌های دروس استفاده شود
- `frontend/vite.config.js` (سطر 8) — پروکسی /api به backend - نیاز به تنظیم خاصی ندارد اما باید مطمئن شد endpoint جدید را پروکسی کند
- `backend/package.json` (سطر 1) — نیاز به افزودن وابستگی node-telegram-bot-api و احتمالاً lowdb یا sqlite برای ذخیره‌سازی
- `backend/.env.example` — نیاز به افز

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد قابلیت مشاهده لیست دروس، مشاهده جزئیات هر درس، و آپلود فایل‌های مرتبط با دروس از طریق تلگرام را دارد. دستورات /courses، /course <id>، /upload_course <id> باید پیاده‌سازی شوند. نکته حیاتی: فایل‌های آپلودی باید در سیستم ذخیره و ایندکس شوند. خارج از این مرحله: مدیریت تمرین با استاد هوش مصنوعی. کاربر می‌خواهد 'بتونم محتوای دروس رو هم از اینجا ببینم یا آپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم'. پروژه فعلی (backend/server.js) از Express.js با WebSocket (ws) و multer برای آپلود فایل استفاده می‌کند. در backend/server.js خطوط 19-56 پیکربندی multer برای ذخیره فایل‌ها در os.tmpdir() انجام شده. خط 727 endpoint /api/analyze-files برای تحلیل فایل‌ها وجود دارد. خط 68 WebSocket در /ws/live فعال است. خط 70-71 CORS و JSON parser فعال. خط 74 static files از frontend/dist سرو می‌شود. خط 190-192 endpoint /api/health وجود دارد. خط 195-215 endpoint /api/list-models. خط 218-263 endpoint /api/test-gemini. خط 79-137 endpoint /api/gemini/chat. خط 140-187 endpoint /api/gemini/tts. فایل frontend/index.html حاوی Firebase config (خطوط 11-18) و Inspector Bridge Script (خطوط 32-201) است. فایل .github/workflows/claude-auto-task.yml workflow خودکار Claude Code را نشان می‌دهد. backend/package.json وابستگی‌های express, cors, dotenv, ws, multer, fluent-ffmpeg, ffmpeg-static, pdf-parse را دارد. frontend/package.json وابستگی‌های react, react-dom, firebase, lucide-react, vite, tailwindcss را دارد. frontend/vite.config.js پروکسی /api به localhost:3001 را تنظیم کرده. backend/.env.example متغیر GEMINI_API_KEY و PORT را نشان می‌دهد. این پروژه یک اپلیکیشن آموزش لهجه لبنانی با هوش مصنوعی است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/services/telegramBot.js برای مدیریت ربات تلگرام با کتابخانه node-telegram-bot-api. 2. پیاده‌سازی دستور /courses در تابع handleCoursesCommand که لیست دروس را از یک منبع داده (فایل JSON یا دیتابیس) خوانده و به صورت فرمت‌بندی شده ارسال کند. 3. پیاده‌سازی دستور /course <id> در تابع handleCourseDetailCommand که جزئیات یک درس خاص را با id مشخص شده برگرداند. 4. پیاده‌سازی دستور /upload_course <id> در تابع handleUploadCourseCommand که فایل ارسالی کاربر را دریافت کرده، با استفاده از multer (مشابه backend/server.js خطوط 25-56) در مسیر uploadDir ذخیره کند، و metadata فایل را در یک فایل index.json ثبت کند. 5. ایجاد فایل backend/data/courses.json برای ذخیره اطلاعات دروس (id, title, description, files[]). 6. ایجاد فایل backend/data/course_files_index.json برای ایندکس فایل‌های آپلودی. 7. افزودن endpoint جدید /api/courses در backend/server.js برای بازگرداندن لیست دروس (GET) و دریافت آپلود فایل درس (POST). 8. اتصال WebSocket در /ws/live (خط 68) برای اطلاع‌رسانی آپلودهای جدید. 9. به‌روزرسانی frontend/src/App.jsx برای نمایش لیست دروس و جزئیات. 10. افزودن دکمه آپلود فایل در فرانت‌اند. نکته حیاتی: فایل‌های آپلودی باید در سیستم ذخیره و ایندکس شوند.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 5: اتصال به بخش 'تمرین با استاد هوش مصنوعی' از طریق تلگرام

**Scope:** این مرحله شامل ایجاد ارتباط بین bot تلگرام و بخش تمرین با استاد هوش مصنوعی است. باید بتوان از تلگرام موضوعات تمرین را مشاهده، انتخاب و مدیریت کرد. خارج از این مرحله: پیاده‌سازی خود چت با هوش مصنوعی. نکته حیاتی: باید API یا interface مناسبی برای ارتباط با بخش استاد هوش مصنوعی وجود داشته باشد.
**Key terms:** /practice_topics, /select_topic, /practice_status, AI tutor, topic management

**بخش مربوط از متن کاربر:**
```
اگر به قسمت 'تمرین با استاد هوش مصنوعی' وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
اتصال تلگرام به بخش تمرین با استاد هوش مصنوعی

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:1-12` — `imports` — محل افزودن import کتابخانه telegraf و فایل telegramBot.js
  ```jsx
  import express from 'express';
  import cors from 'cors';
  import { fileURLToPath } from 'url';
  import { dirname, join } from 'path';
  import dotenv from 'dotenv';
  import { createServer } from 'http';
  import { WebSocketServer, WebSocket } from 'ws';
  import multer from 'multer';
  import fs from 'fs';
  import os from 'os';
  import ffmpeg from 'fluent-ffmpeg';
  import ffmpegStatic from 'ffmpeg-static';
  ```
- `backend/server.js:61-68` — `app initialization and WebSocket` — بعد از این بخش باید bot تلگرام راه‌اندازی شود
  ```jsx
  const app = express();
  const PORT = process.env.PORT || 3001;
  
  // Create HTTP server
  const server = createServer(app);
  
  // Create WebSocket server
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/server.js:189-192` — `Health check endpoint` — الگویی برای افزودن endpointهای جدید /api/practice/topics و /api/practice/select
  ```jsx
  // Health check
  app.get('/api/health', (req, res) => {
    res.json({ status: 'ok', timestamp: new Date().toISOString() });
  });
  ```
- `backend/.env.example:1-8` — `environment variables` — افزودن TELEGRAM_BOT_TOKEN به این فایل
  ```
  # Gemini API Key - Get from https://aistudio.google.com/apikey
  GEMINI_API_KEY=your_gemini_api_key_here
  
  # Port (optional, default 3001)
  PORT=3001
  
  # Firebase config (optional - for data persistence)
  # VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/package.json` (سطر 1) — نیاز به افزودن وابستگی telegraf در dependencies
- `frontend/src/App.jsx` — این فایل deep-read نشده ولی احتمالاً کامپوننت اصلی تمرین با استاد هوش مصنوعی را دارد — باید APIهای جدید را consume کند
- `backend/server.js` (سطر 727) — فایل اصلی backend که endpointهای جدید practice به آن اضافه می‌شود
- `prompt/task-31236cc5-eb3f-4b4e-be02-ee2362ecc8c4.md` — تسک قبلی مرتبط با File Analysis API — ممکن است ساختار مشابهی برای practice topics داشته باشد

## 🌐 نقشهٔ وابستگی‌ها
این تسک وابسته به backend/server.js است (برای افزودن endpointها)، backend/package.json (برای افزودن وابستگی telegraf)، backend/.env.example (برای افزودن TELEGRAM_BOT_TOKEN). فایل‌های جدید backend/services/telegramBot.js و backend/services/practiceService.js ایجاد می‌شوند. frontend/src/App.jsx احتمالاً برای نمایش موضوعات تمرین نیاز به تغییر دارد. هیچ دیتابیسی وجود ندارد — وضعیت کاربر در

## 🔍 Context و وضعیت فعلی
کاربر درخواست اتصال به بخش 'تمرین با استاد هوش مصنوعی' از طریق تلگرام را دارد. این مرحله شامل ایجاد ارتباط بین bot تلگرام و بخش تمرین با استاد هوش مصنوعی است. باید بتوان از تلگرام موضوعات تمرین را مشاهده، انتخاب و مدیریت کرد. خارج از این مرحله: پیاده‌سازی خود چت با هوش مصنوعی. نکته حیاتی: باید API یا interface مناسبی برای ارتباط با بخش استاد هوش مصنوعی وجود داشته باشد. کلیدواژه‌های مشخص شده: /practice_topics, /select_topic, /practice_status, AI tutor, topic management. در کد فعلی پروژه (backend/server.js) هیچ endpoint یا API برای مدیریت موضوعات تمرین (practice topics) وجود ندارد. فایل frontend/index.html فقط یک صفحه SPA ساده با Firebase config است و frontend/src/App.jsx (که deep-read نشده) احتمالاً کامپوننت‌های React را شامل می‌شود. backend/server.js شامل APIهای /api/gemini/chat (خط 79)، /api/gemini/tts (خط 140)، /api/analyze-files (خط 727) و endpoints آزمایشی است. هیچ WebSocket endpoint یا REST API برای مدیریت topic وجود ندارد. همچنین هیچ فایل bot تلگرام در ساختار پروژه دیده نمی‌شود. بنابراین نیاز به ایجاد یک bot تلگرام جدید (با کتابخانه‌ای مانند node-telegram-bot-api یا telegraf) و یک سری API جدید در backend برای مدیریت موضوعات تمرین است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک فایل جدید backend/services/telegramBot.js که bot تلگرام را با استفاده از کتابخانه telegraf راه‌اندازی کند. 2. تعریف هندلرهای دستورات: /start (خوش‌آمدگویی)، /practice_topics (لیست موضوعات تمرین)، /select_topic [id] (انتخاب موضوع)، /practice_status (وضعیت تمرین فعلی). 3. ایجاد یک فایل جدید backend/services/practiceService.js برای مدیریت موضوعات تمرین (CRUD) و وضعیت کاربر. 4. افزودن یک endpoint جدید در backend/server.js مانند GET /api/practice/topics که لیست موضوعات را برگرداند و POST /api/practice/select که موضوع انتخاب شده را ذخیره کند. 5. ذخیره‌سازی وضعیت کاربر در حافظه (یا فایل JSON ساده) چون دیتابیس وجود ندارد. 6. نصب کتابخانه telegraf در backend با npm install telegraf. 7. افزودن متغیر محیطی TELEGRAM_BOT_TOKEN به backend/.env.example. 8. اتصال bot تلگرام به APIهای backend از طریق fetch داخلی.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 6: پیاده‌سازی چت دوطرفه با هوش مصنوعی از طریق تلگرام

**Scope:** این مرحله شامل ایجاد قابلیت چت متنی با هوش مصنوعی موجود در بخش تمرین از طریق تلگرام است. باید پیام‌های متنی کاربر به هوش مصنوعی ارسال شده و پاسخ دریافت و در تلگرام نمایش داده شود. خارج از این مرحله: مدیریت پیام‌های صوتی. نکته حیاتی: باید state جلسه چت حفظ شود تا مکالمه پیوسته باشد.
**Key terms:** chat handler, AI API, session management, message streaming, conversation state

**بخش مربوط از متن کاربر:**
```
از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن چت دوطرفه تلگرام با AI و حفظ session

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:79-137` — `app.post('/api/gemini/chat')` — این endpoint اصلی چت با AI است که باید از طریق تلگرام فراخوانی شود. payload شامل role و parts است که برای session management باید history به آن اضافه شود.
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
      if (payload.contents) {
        payload.contents = payload.contents.map(content => ({
          role: content.role,
          parts: content.parts.map(part => {
            if (part.inline_data && includeAudio) {
              return { inline_data: part.inline_data };
            }
            if (part.text !== undefined) {
              return { text: part.text };
            }
            return null;
          }).filter(p => p !== null)
        }));
      }
      const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${GEMINI_API_KEY}`;
  ```
- `backend/server.js:296-317` — `analyzeWithGemini(parts, systemPrompt)` — این تابع کمکی برای ارسال مستقیم به Gemini API است. برای session management، باید history مکالمه به array contents اضافه شود.
  ```jsx
  async function analyzeWithGemini(parts, systemPrompt) {
    const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${GEMINI_API_KEY}`;
    const payload = {
      contents: [{ role: 'user', parts }],
      systemInstruction: { parts: [{ text: systemPrompt }] }
    };
    const response = await fetch(apiUrl, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(payload)
    });
    if (!response.ok) {
      const error = await response.text();
      throw new Error(`Gemini API error: ${error}`);
    }
    const result = await response.json();
    return result.candidates?.[0]?.content?.parts?.[0]?.text || '';
  }
  ```
- `backend/server.js:68` — `WebSocket server` — WebSocket موجود می‌تواند برای streaming پاسخ‌های AI به تلگرام استفاده شود، اما در این مرحله احتمالاً نیاز به webhook ساده‌تر است.
  ```jsx
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack تشخیص داده شده: JavaScript (Node.js backend با Express، frontend با React/Vite). کتابخانه‌های مرتبط: express (خط 1 backend/server.js)، ws (خط

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `frontend/index.html` (سطر 11) — حاوی Firebase config (خط 11-18) که می‌تواند برای ذخیره session کاربران تلگرام استفاده شود
- `backend/package.json` (سطر 1) — نیاز به اضافه کردن dependency جدید مانند node-telegram-bot-api یا axios برای ارتباط با Telegram API
- `backend/.env.example` (سطر 1) — نیاز به اضافه کردن متغیر محیطی TELEGRAM_BOT_TOKEN برای توکن ربات تلگرام
- `frontend/src/App.jsx` (سطر 1) — احتمالاً نیاز به نمایش وضعیت چت تلگرام در UI دارد (deep-read نشده)

## 🌐 نقشهٔ وابستگی‌ها
این قابلیت به backend/server.js وابسته است (endpoint /api/gemini/chat در خط 79 و تابع analyzeWithGemini در خط 296). نیاز به اضافه کردن dependency جدید (node-telegram-bot-api یا axios) به backend/package.json دارد. همچنین نیاز به متغیر محیطی TELEGRAM_BOT_TOKEN در backend/.env.example دارد. session management می‌تواند از Firebase config موجود در frontend/index.html (خط 11-18) استفاده کند یا به صورت in-memory با Map پیاده‌سازی شود.

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی چت دوطرفه با هوش مصنوعی از طریق تلگرام را دارد. این قابلیت باید از طریق تلگرام، پیام‌های متنی کاربر را به هوش مصنوعی موجود در بخش تمرین (که در backend/server.js از طریق endpoint /api/gemini/chat در خط 79 با مدل gemini-2.0-flash پیاده‌سازی شده) ارسال کرده و پاسخ را در تلگرام نمایش دهد. خارج از این مرحله: مدیریت پیام‌های صوتی. نکته حیاتی: باید state جلسه چت حفظ شود تا مکالمه پیوسته باشد. کلیدواژه‌های ذکر شده: chat handler, AI API, session management, message streaming, conversation state. در backend/server.js خط 79-137 endpoint /api/gemini/chat وجود دارد که payload شامل contents با role و parts را دریافت کرده و به Gemini API ارسال می‌کند. همچنین در خط 68 WebSocket در /ws/live برای ارتباطات زنده راه‌اندازی شده. frontend/index.html حاوی Firebase config (خط 11-18) است که می‌توان از آن برای احراز هویت تلگرام استفاده کرد. در حال حاضر هیچ handler یا endpoint مخصوص تلگرام در پروژه وجود ندارد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک endpoint جدید در backend/server.js مثلاً POST /api/telegram/webhook برای دریافت webhook از تلگرام. 2. در این endpoint، پیام متنی کاربر را استخراج کرده و با استفاده از تابع analyzeWithGemini (خط 296-317) یا endpoint /api/gemini/chat (خط 79) به AI ارسال کند. 3. پیاده‌سازی session management با استفاده از یک Map ساده یا دیتابیس (با توجه به Firebase config موجود در frontend/index.html خط 11-18) برای نگهداری history مکالمه هر کاربر تلگرام. 4. ارسال پاسخ دریافتی از AI به تلگرام از طریق Telegram Bot API. 5. اضافه کردن dependency تلگرام (node-telegram-bot-api یا axios برای API calls) به backend/package.json. 6. استفاده از WebSocket موجود در خط 68 برای streaming پاسخ‌ها در صورت نیاز.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 7: پیاده‌سازی ارسال و دریافت پیام صوتی از طریق تلگرام برای تعامل با هوش مصنوعی

**Scope:** این مرحله شامل قابلیت دریافت پیام صوتی از کاربر در تلگرام، ارسال آن به سرویس تحلیل و استخراج متن (ASR)، ارسال متن استخراج‌شده به هوش مصنوعی، دریافت پاسخ صوتی از هوش مصنوعی و ارسال آن به کاربر در تلگرام است. خارج از این مرحله: ذخیره‌سازی فایل‌های صوتی. نکته حیاتی: باید از میکروفون تلگرام استفاده شود و فایل صوتی به درستی پردازش شود.
**Key terms:** voice message, ASR, speech-to-text, text-to-speech, audio file, microphone

**بخش مربوط از متن کاربر:**
```
از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن Webhook تلگرام برای پیام صوتی و ASR/TTS

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:68-68` — `WebSocketServer` — WebSocket موجود می‌تواند برای ارسال لحظه‌ای وضعیت پردازش صوتی به فرانت‌اند استفاده شود.
  ```jsx
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/server.js:140-187` — `POST /api/gemini/tts` — این endpoint برای تبدیل متن پاسخ هوش مصنوعی به صوت استفاده می‌شود. باید از داخل telegramService.js فراخوانی شود.
  ```jsx
  app.post('/api/gemini/tts', async (req, res) => { ... const { prompt, voice = 'Kore' } = req.body; ... const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-preview-tts:generateContent?key=${GEMINI_API_KEY}`; ... });
  ```
- `backend/server.js:296-317` — `analyzeWithGemini` — این تابع برای ASR (تبدیل صوت به متن) استفاده می‌شود. فایل صوتی دانلودشده از تلگرام باید به صورت inline_data به parts اضافه شود.
  ```jsx
  async function analyzeWithGemini(parts, systemPrompt) { const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${GEMINI_API_KEY}`; ... }
  ```
- `backend/server.js:727-800` — `POST /api/analyze-files` — این endpoint می‌تواند برای پردازش فایل‌های صوتی دریافتی از تلگرام复用 شود، اما نیاز به تغییر مسیر فایل از Telegram به disk دارد.
  ```jsx
  app.post('/api/analyze-files', upload.array('files', MAX_FILES), handleMulterError, async (req, res) => { ... });
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/package.json` (سطر 1) — نیاز به افزودن وابستگی node-telegram-bot-api یا grammy
- `backend/.env.example` (سطر 1) — نیاز به افزودن متغیر TELEGRAM_BOT_TOKEN
- `frontend/src/App.jsx` (سطر 1) — ممکن است نیاز به نمایش وضعیت پیام‌های تلگرام در UI داشته باشد
- `frontend/index.html` — Firebase config موجود (خط 11-18)

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی ارسال و دریافت پیام صوتی از طریق تلگرام برای تعامل با هوش مصنوعی را دارد. این شامل: دریافت پیام صوتی از کاربر در تلگرام، ارسال آن به سرویس تحلیل و استخراج متن (ASR)، ارسال متن استخراج‌شده به هوش مصنوعی، دریافت پاسخ صوتی از هوش مصنوعی و ارسال آن به کاربر در تلگرام است. خارج از این مرحله: ذخیره‌سازی فایل‌های صوتی. نکته حیاتی: باید از میکروفون تلگرام استفاده شود و فایل صوتی به درستی پردازش شود. کاربر می‌گوید: 'از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد'. کلیدواژه‌ها: voice message, ASR, speech-to-text, text-to-speech, audio file, microphone. در کد فعلی backend/server.js، endpointهای موجود برای تحلیل فایل (POST /api/analyze-files در خط 727) و TTS (POST /api/gemini/tts در خط 140) و چت (POST /api/gemini/chat در خط 79) وجود دارند. همچنین WebSocket در خط 68 (wss = new WebSocketServer({ server, path: '/ws/live' })) پیاده‌سازی شده. فایل frontend/index.html شامل Firebase config (خط 11-18) و inspector bridge (خط 32-201) است. پروژه از Express.js، multer برای آپلود فایل (خط 44-56)، fluent-ffmpeg برای پردازش ویدیو/صوت (خط 11)، و Gemini API برای ASR/TTS استفاده می‌کند. نیاز به افزودن Webhook تلگرام (مثلاً با کتابخانه node-telegram-bot-api یا grammy) و مسیریابی پیام‌های صوتی به pipeline موجود است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. افزودن وابستگی node-telegram-bot-api یا grammy به backend/package.json. 2. ایجاد فایل جدید backend/services/telegramService.js شامل: (a) راه‌اندازی bot با توکن تلگرام از env، (b) هندلر برای پیام‌های صوتی (message.voice) که فایل صوتی را از Telegram File API دانلود می‌کند، (c) تابعی برای ارسال فایل صوتی به Gemini File API (مشابه uploadToGeminiFileAPI در خط 320 server.js) یا استفاده از analyzeWithGemini با inline_data (خط 296)، (d) ارسال متن استخراج‌شده به POST /api/gemini/chat (خط 79) برای دریافت پاسخ متنی، (e) ارسال متن پاسخ به POST /api/gemini/tts (خط 140) برای تولید صوت، (f) ارسال فایل صوتی پاسخ به کاربر در تلگرام. 3. افزودن endpoint جدید POST /api/telegram/webhook در backend/server.js برای دریافت webhook از تلگرام. 4. تنظیم webhook در تلگرام (setWebhook). 5. استفاده از multer موجود (خط 44) برای مدیریت فایل‌های دریافتی از تلگرام. 6. اطمینان از اینکه فایل صوتی از میکروفون تلگرام (فرمت OGG/OPUS) به درستی توسط Gemini پشتیبانی می‌شود (mimeType: audio/ogg). 7. افزودن مدیریت خطا و لاگینگ مشابه سایر endpointها.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 8: یکپارچه‌سازی و همگام‌سازی وضعیت چت بین تلگرام و وب

**Scope:** این مرحله شامل اطمینان از همگام‌سازی وضعیت چت بین تلگرام و وب‌سایت است. هر پیام و پاسخی که در یک پلتفرم رد و بدل می‌شود باید در پلتفرم دیگر نیز قابل مشاهده باشد. خارج از این مرحله: پیاده‌سازی features جدید. نکته حیاتی: باید از یک دیتابیس مشترک یا event-driven architecture استفاده شود.
**Key terms:** sync, real-time, WebSocket, database, event bus, message queue

**بخش مربوط از متن کاربر:**
```
هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد
```

## 🎯 هدف (خلاصه ساختاریافته)
همگام‌سازی وضعیت چت بین تلگرام و وب با WebSocket

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:68` — `WebSocketServer` — WebSocket موجود باید گسترش یابد تا رویدادهای چت (chat_message, chat_sync) را مدیریت کند و پیام‌ها را broadcast کند.
  ```jsx
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/server.js:79-137` — `app.post('/api/gemini/chat')` — این endpoint پاسخ‌های چت را تولید می‌کند. باید پس از دریافت پاسخ، آن را از طریق WebSocket به کلاینت‌ها broadcast کند و در Firebase ذخیره کند.
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => { ... });
  ```
- `frontend/index.html:54-71` — `sendToInspector` — این تابع از postMessage برای ارتباط با parent استفاده می‌کند. می‌توان از الگوی مشابه برای ارسال پیام‌های چت به WebSocket استفاده کرد.
  ```
  function sendToInspector(action, data) { ... window.parent.postMessage(message, '*'); ... }
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
JavaScript (Node.js), Express, WebSocket (ws), Firebase (Firestore/Realtime Database), Telegram Bot API, EventEmitter

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `frontend/src/App.jsx` (سطر 1) — کامپوننت اصلی فرانت‌اند که باید WebSocket را متصل کند و پیام‌های همگام‌سازی‌شده از تلگرام را نمایش دهد.
- `backend/.env.example` (سطر 1) — نیاز به افزودن متغیرهای محیطی جدید برای Telegram Bot Token (TELEGRAM_BOT_TOKEN) و Firebase Admin SDK credentials.
- `backend/package.json` (سطر 1) — نیاز به افزودن وابستگی‌های جدید: node-telegram-bot-api (برای ارتباط با Telegram Bot API) و firebase-admin (برای دسترسی به Firebase از سمت سرور).
- `frontend/package.json` (سطر 1) — نیاز به افزودن وابستگی firebase برای اتصال به Firebase Realtime Database یا Firestore از سمت کلاینت.
- `frontend/index.html` (سطر 11) — Firebase config موجود در frontend/index.html (__firebase_config) باید برای اتصال به دیتابیس مشترک استفاده شود.

## 🌐 نقشهٔ وابستگی‌ها
این تسک به شدت به WebSocket موجود در backend/server.js (خط 68) وابسته است. endpoint /api/gemini/chat (خط 79) باید پس از تولید پاسخ، آن را از طریق WebSocket broadcast کند. Firebase (frontend/index.html خطوط 11-18) به‌عنوان دیتابیس مشترک عمل می‌کند. یک سرویس جدید (backend/services/telegramService.js) باید ایجاد شود که با Telegram Bot API ارتباط برقرار کند. frontend/src/App.jsx باید WebSocket را متصل کند. backend/.env.example نیاز به متغیرهای جدید دارد. backend/package.json نیاز به وابستگی‌های جدید (node-telegram-bot-api, firebase-admin) دارد.

## 🔍 Context و وضعیت فعلی
کاربر درخواست یکپارچه‌سازی و همگام‌سازی وضعیت چت بین تلگرام و وب‌سایت را دارد. متن خام کاربر: «یکپارچه‌سازی و همگام‌سازی وضعیت چت بین تلگرام و وب. این مرحله شامل اطمینان از همگام‌سازی وضعیت چت بین تلگرام و وب‌سایت است. هر پیام و پاسخی که در یک پلتفرم رد و بدل می‌شود باید در پلتفرم دیگر نیز قابل مشاهده باشد. خارج از این مرحله: پیاده‌سازی features جدید. نکته حیاتی: باید از یک دیتابیس مشترک یا event-driven architecture استفاده شود. هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد. کلیدواژه‌ها: sync, real-time, WebSocket, database, event bus, message queue». در کد فعلی پروژه (backend/server.js خط 68) یک WebSocketServer روی path /ws/live ایجاد شده است (new WebSocketServer({ server, path: '/ws/live' })). همچنین frontend/index.html شامل یک Inspector Bridge Script است که از postMessage برای ارتباط با parent استفاده می‌کند (خطوط 54-71). backend/server.js شامل endpointهای /api/gemini/chat (خط 79) و /api/analyze-files (خط 727) است که پاسخ‌های چت را تولید می‌کنند. هیچ endpoint یا سرویس تلگرام در کد فعلی وجود ندارد. پروژه از Firebase (frontend/index.html خطوط 11-18) استفاده می‌کند که می‌تواند به‌عنوان دیتابیس مشترک عمل کند. نیاز به ایجاد یک سرویس تلگرام (Telegram Bot API) و اتصال آن به WebSocket موجود و/یا Firebase برای همگام‌سازی پیام‌ها است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک سرویس تلگرام جدید (backend/services/telegramService.js) که با Telegram Bot API ارتباط برقرار کند و پیام‌های دریافتی را به WebSocket و Firebase منتقل کند. 2. گسترش WebSocket موجود در backend/server.js (خط 68) برای پشتیبانی از رویدادهای چت (chat_message, chat_sync) و broadcast پیام‌ها به همه کلاینت‌های متصل. 3. ایجاد یک دیتابیس مشترک (Firebase Firestore) برای ذخیره تاریخچه چت و وضعیت آن. 4. افزودن یک endpoint جدید /api/telegram/webhook در backend/server.js برای دریافت webhook از Telegram Bot API. 5. اصلاح frontend/src/App.jsx برای اتصال به WebSocket و نمایش پیام‌های همگام‌سازی‌شده از تلگرام. 6. پیاده‌سازی event-driven architecture با استفاده از EventEmitter داخلی Node.js برای انتشار رویدادهای چت بین سرویس‌ها.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: high
- تخمین زمان: large

---

# 🔹 مرحله 9: مدیریت خطا و لاگ‌گیری در ارتباط تلگرام

**Scope:** این مرحله شامل پیاده‌سازی سیستم مدیریت خطا برای ارتباط با تلگرام است. باید خطاهای احتمالی (قطع ارتباط، timeout، خطای API) مدیریت شوند و لاگ مناسب ثبت شود. خارج از این مرحله: features جدید. نکته حیاتی: باید از retry mechanism و fallback استفاده شود.
**Key terms:** error handling, logging, retry, timeout, exception, fallback

**بخش مربوط از متن کاربر:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی سیستم مدیریت خطا و لاگ‌گیری برای ارتباط دوطرفه تلگرام

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:68-68` — `WebSocketServer` — این خط نشان‌دهنده سیستم ارتباطی فعلی پروژه است. برای تلگرام باید سیستم مشابهی با WebSocket ایجاد شود.
  ```jsx
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/server.js:79-137` — `app.post('/api/gemini/chat')` — این endpoint الگوی مناسبی برای ساختار error handling در endpoint تلگرام است. باید از همین الگوی try-catch با return error استفاده شود.
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      ...
  ```
- `backend/server.js:189-192` — `app.get('/api/health')` — این endpoint الگوی health check است. برای endpoint /api/telegram/status باید از همین الگو استفاده شود.

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: JavaScript (Node.js backend با Express, React frontend با Vite). کتابخانه‌های مرتبط: node-telegram-bot-api (برای ارتباط با Telegram Bot API)، winston یا pino (برای لاگ‌گیری حرفه‌ای).

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/package.json` (سطر 2) — برای اضافه کردن وابستگی node-telegram-bot-api به dependencies نیاز است
- `backend/.env.example` (سطر 1) — برای اضافه کردن متغیرهای محیطی TELEGRAM_BOT_TOKEN و TELEGRAM_WEBHOOK_URL نیاز است
- `frontend/src/App.jsx` (سطر 1) — ممکن است نیاز به نمایش وضعیت اتصال تلگرام در UI داشته باشد (فعلاً خارج از scope)
- `package.json` (سطر 2) — برای اضافه کردن اسکریپت‌های مدیریت تلگرام (اختیاری)

## 🌐 نقشهٔ وابستگی‌ها
این تسک وابسته به فایل‌های backend/server.js (برای اضافه کردن endpoint جدید)، backend/package.json (برای اضافه کردن وابستگی node-telegram-bot-api)، backend/.env.example (برای متغیرهای محیطی) است. فایل‌های جدید backend/services/telegramService.js و backend/utils/logger.js و backend/config/telegram.js ایجاد می‌شوند. فایل frontend/src/App.jsx ممکن است در آینده برای نمایش وضعیت تلگرام نیاز به تغییر داشته باشد (فعلاً خارج از scope).

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی سیستم مدیریت خطا برای ارتباط با تلگرام را دارد. این شامل مدیریت خطاهای احتمالی مانند قطع ارتباط (connection drop)، timeout، خطای API تلگرام است. باید لاگ مناسب ثبت شود و از retry mechanism و fallback استفاده شود. نکته حیاتی: کاربر تأکید دارد که خارج از این مرحله features جدید اضافه نشود. همچنین کاربر می‌خواهد تلگرام دو طرفه کار کند (two-way Telegram communication). کلیدواژه‌های اصلی: error handling, logging, retry, timeout, exception, fallback.

بر اساس کد واقعی پروژه (backend/server.js)، در حال حاضر هیچ سیستمی برای ارتباط با تلگرام وجود ندارد. پروژه فعلی از WebSocket (خط 68: wss = new WebSocketServer({ server, path: '/ws/live' })) برای ارتباط live استفاده می‌کند و APIهای مختلفی مانند /api/gemini/chat (خط 79)، /api/gemini/tts (خط 140)، /api/analyze-files (خط 727) دارد. فایل frontend/index.html شامل Firebase config (خطوط 11-18) و inspector bridge script (خطوط 32-201) است. backend/package.json (خط 2: name: "lebanese-dialect-backend") وابستگی‌های فعلی شامل express, cors, dotenv, ws است. package.json اصلی (خط 2: name: "lebanese-dialect-learning") شامل react, firebase, lucide-react است.

برای پیاده‌سازی این تسک، نیاز به ایجاد فایل جدید backend/services/telegramService.js برای مدیریت خطا و لاگ‌گیری تلگرام است. همچنین باید endpoint جدید در backend/server.js اضافه شود. retry mechanism باید با exponential backoff پیاده‌سازی شود. fallback strategy باید برای زمانی که تلگرام در دسترس نیست تعریف شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/services/telegramService.js با کلاس TelegramService شامل:
   - متد sendMessage(text, chatId) با error handling و retry mechanism
   - متد handleIncomingMessage(update) برای پردازش پیام‌های دریافتی (دو طرفه)
   - متد setupWebhook(url) برای تنظیم webhook تلگرام
   - پیاده‌سازی retry mechanism با exponential backoff (max 3 retry)
   - پیاده‌سازی fallback strategy (ذخیره پیام در صف محلی)
   - لاگ‌گیری با console.error و ذخیره در فایل لاگ

2. ایجاد فایل backend/utils/logger.js برای سیستم لاگ‌گیری متمرکز:
   - سطوح لاگ: error, warn, info, debug
   - نوشتن لاگ در فایل logs/telegram.log
   - فرمت JSON برای لاگ‌ها

3. اضافه کردن endpoint در backend/server.js:
   - POST /api/telegram/webhook برای دریافت webhook از تلگرام
   - GET /api/telegram/status برای بررسی وضعیت اتصال

4. به‌روزرسانی backend/package.json برای اضافه کردن وابستگی node-telegram-bot-api

5. ایجاد فایل backend/config/telegram.js برای تنظیمات:
   - BOT_TOKEN از environment variable
   - WEBHOOK_URL
   - RETRY_CONFIG (maxRetries, baseDelay, maxDelay)
   - TIMEOUT_CONFIG (requestTimeout, connectionTimeout)

## 💡 نمونه‌های قبل/بعد
**قبل: عدم وجود سیستم مدیریت خطا برای تلگرام**

_قبل:_
```
// backend/server.js - هیچ endpoint یا سرویسی برای تلگرام وجود ندارد
//
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 10: تست و اعتبارسنجی نهایی قابلیت‌های تلگرام

**Scope:** این مرحله شامل تست تمام قابلیت‌های پیاده‌سازی‌شده (ارسال نوتیفیکیشن، دریافت دستورات، مدیریت دروس، چت با هوش مصنوعی، پیام صوتی) است. باید سناریوهای مختلف تست شوند. خارج از این مرحله: توسعه features جدید. نکته حیاتی: باید هم تست واحد و هم تست یکپارچه‌سازی انجام شود.
**Key terms:** unit test, integration test, test scenario, end-to-end test, QA

**بخش مربوط از متن کاربر:**
```
و تا حد زیادی کار رو از اینجا پیش ببرم
```

## 🎯 هدف (خلاصه ساختاریافته)
تست و اعتبارسنجی نهایی قابلیت‌های تلگرام

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:79-137` — `app.post('/api/gemini/chat')`
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
      if (payload.contents) {
        payload.contents = payload.contents.map(content => ({
          role: content.role,
          parts: content.parts.map(part => {
            if (part.inline_data && includeAudio) {
              return { inline_data: part.inline_data };
            }
            if (part.text !== undefined) {
              return { text: part.text };
            }
            return null;
          }).filter(p => p !== null)
        }));
      }
      const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${GEMINI_API_KEY}`;
      const response = await fetch(apiUrl, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(payload)
      });
      if (!response.ok) {
        const errorData = await response.text();
        console.error('Gemini API error:', errorData);
        return res.status(response.status).json({ error: errorData });
      }
      const result = await response.json();
      if (includeAudio) {
        res.json(result);
      } else {
        const text = result.candidates?.[0]?.content?.parts?.[0]?.text;
        if (!text) {
          return res.status(500).json({ error: 'No text in response' });
        }
        res.json({ text });
      }
    } catch (error) {
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
این تسک شامل تست و اعتبارسنجی نهایی تمام قابلیت‌های پیاده‌سازی‌شده در پروژه است که کاربر به آن‌ها اشاره کرده: ارسال نوتیفیکیشن، دریافت دستورات، مدیریت دروس، چت با هوش مصنوعی، پیام صوتی. کاربر خواسته که سناریوهای مختلف تست شوند و خارج از این مرحله توسعه features جدید انجام نشود. نکته حیاتی: باید هم تست واحد و هم تست یکپارچه‌سازی انجام شود. کلیدواژه‌های کاربر: unit test, integration test, test scenario, end-to-end test, QA. با بررسی کد واقعی پروژه در فایل‌های deep-read شده (backend/server.js, frontend/index.html, .github/workflows/claude-auto-task.yml, backend/package-lock.json, frontend/package-lock.json, frontend/src/App.jsx, و تعدادی از prompt/archive files)، مشخص شد که پروژه یک اپلیکیشن آموزش لهجه لبنانی با هوش مصنوعی است که از Express.js در backend و React در frontend استفاده می‌کند. backend/server.js شامل endpointهای اصلی است: POST /api/gemini/chat (خط 79), POST /api/gemini/tts (خط 140), GET /api/health (خط 190), GET /api/list-models (خط 195), GET /api/test-gemini (خط 218), POST /api/analyze-files (خط 727). همچنین WebSocket در /ws/live (خط 68) پیاده‌سازی شده. frontend/index.html شامل Firebase config و Inspector Bridge Script است. با این حال، هیچ کد مرتبط با تلگرام (Telegram Bot API, telegram webhook, telegram notification) در فایل‌های deep-read شده یافت نشد. بنابراین این تسک نیازمند ایجاد زیرساخت تست برای قابلیت‌های تلگرام است که احتمالاً در فایل‌های خوانده‌نشده (مانند backend/services/telegram.js یا frontend/src/components/TelegramPanel.jsx) پیاده‌سازی شده‌اند. کاربر اشاره کرده که "و تا حد زیادی کار رو از اینجا پیش ببرم" که نشان می‌دهد بخشی از کار قبلاً انجام شده و این تسک برای تکمیل و تست نهایی است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. با توجه به عدم وجود کد تلگرام در فایل‌های deep-read شده، پیشنهاد می‌شود مراحل زیر به ترتیب انجام شود:
1. اسکن کامل پروژه برای یافتن فایل‌های مرتبط با تلگرام (جستجوی pattern‌های 'telegram', 'bot', 'notification', 'webhook' در کل پروژه).
2. ایجاد فایل تست واحد برای هر سرویس تلگرام: backend/tests/unit/telegramService.test.js برای تست توابع ارسال نوتیفیکیشن و دریافت دستورات.
3. ایجاد فایل تست یکپارچه‌سازی: backend/tests/integration/telegramFlow.test.js برای سناریوهای end-to-end شامل ارسال پیام از تلگرام، پردازش در backend، و بازگشت پاسخ.
4. برای قابلیت چت با هوش مصنوعی (که در backend/server.js خط 79-137 پیاده‌سازی شده)، تست‌های واحد برای endpoint /api/gemini/chat با mock کردن پاسخ Gemini API.
5. برای قابلیت پیام صوتی (که در backend/server.js خط 140-187 برای TTS و خط 296-317 برای تحلیل صوتی پیاده‌سازی شده)، تست‌های واحد با mock فایل صوتی.
6. برای مدیریت دروس، تست‌های واحد برای CRUD operations.
7. اجرای تست‌ها با دستورات npm test و بررسی پوشش کد.
8. ثبت نتایج و رفع باگ‌های احتمالی.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: medium
- تخمین زمان: large

---

## ✅ معیارهای پذیرش کلی (همهٔ مراحل)
- [ ] همهٔ مراحل بالا با موفقیت پیاده‌سازی شده‌اند
- [ ] تست‌های موجود pass می‌شوند
- [ ] هیچ regression رخ نداده

## Acceptance Criteria

1. فایل backend/services/telegramService.js ایجاد شده و تابع sendVoiceMessage را export کند _(verify: static)_

## Task Steps

### Step 1: ایجاد ساختار پایه برای نوتیفیکیشن تلگرام دوطرفه
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک ماژول پایه برای ارتباط با تلگرام (bot) است. باید یک bot تلگرام با استفاده از کتابخانه python-telegram-bot یا aiogram ایجاد شود که قابلیت دریافت و ارسال پیام را داشته باشد. این مرحله فقط شامل راه‌اندازی اولیه bot و اتصال به API تلگرام است. خارج از این مرحله: پیاده‌سازی منطق ارسال نوتیفیکیشن از سیستم، دریافت دستورات خاص، یا اتصال به سایر بخش‌های پروژه. نکته حیاتی: باید از webhook یا polling استفاده شود و توکن bot در محیط امن (env) ذخیره شود.
**Excerpt:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

### Step 2: پیاده‌سازی ارسال نوتیفیکیشن از سیستم به تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک سرویس یا تابع در سیستم است که بتواند پیام‌های نوتیفیکیشن را به تلگرام ارسال کند. باید مشخص شود چه رویدادهایی (مثلاً ثبت‌نام جدید، خطا، به‌روزرسانی) باعث ارسال پیام می‌شوند. خارج از این مرحله: دریافت دستورات از تلگرام برای کنترل سیستم. نکته حیاتی: پیام‌ها باید شامل اطلاعات مفید و قابل فهم باشند و قابلیت شخصی‌سازی داشته باشند.
**Excerpt:**
```
علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته
```

### Step 3: پیاده‌سازی دریافت و پردازش دستورات از تلگرام برای کنترل سیستم
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد handlerهای مختلف برای دستورات دریافتی از تلگرام است. باید دستورات پایه مانند /status، /help، /courses، /upload و ... تعریف شوند. خارج از این مرحله: اتصال به بخش محتوای دروس یا تمرین با استاد هوش مصنوعی. نکته حیاتی: دستورات باید به صورت ماژولار طراحی شوند تا بعداً به راحتی گسترش یابند.
**Excerpt:**
```
بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

### Step 4: ایجاد ماژول مشاهده و آپلود محتوای دروس از طریق تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد قابلیت مشاهده لیست دروس، مشاهده جزئیات هر درس، و آپلود فایل‌های مرتبط با دروس از طریق تلگرام است. باید دستورات /courses، /course <id>، /upload_course <id> پیاده‌سازی شوند. خارج از این مرحله: مدیریت تمرین با استاد هوش مصنوعی. نکته حیاتی: فایل‌های آپلودی باید در سیستم ذخیره و ایندکس شوند.
**Excerpt:**
```
بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم
```

### Step 5: اتصال به بخش 'تمرین با استاد هوش مصنوعی' از طریق تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد ارتباط بین bot تلگرام و بخش تمرین با استاد هوش مصنوعی است. باید بتوان از تلگرام موضوعات تمرین را مشاهده، انتخاب و مدیریت کرد. خارج از این مرحله: پیاده‌سازی خود چت با هوش مصنوعی. نکته حیاتی: باید API یا interface مناسبی برای ارتباط با بخش استاد هوش مصنوعی وجود داشته باشد.
**Excerpt:**
```
اگر به قسمت 'تمرین با استاد هوش مصنوعی' وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم
```

### Step 6: پیاده‌سازی چت دوطرفه با هوش مصنوعی از طریق تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد قابلیت چت متنی با هوش مصنوعی موجود در بخش تمرین از طریق تلگرام است. باید پیام‌های متنی کاربر به هوش مصنوعی ارسال شده و پاسخ دریافت و در تلگرام نمایش داده شود. خارج از این مرحله: مدیریت پیام‌های صوتی. نکته حیاتی: باید state جلسه چت حفظ شود تا مکالمه پیوسته باشد.
**Excerpt:**
```
از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم
```

### Step 7: پیاده‌سازی ارسال و دریافت پیام صوتی از طریق تلگرام برای تعامل با هوش مصنوعی
**Status:** `pending` (0%)
**Scope:** این مرحله شامل قابلیت دریافت پیام صوتی از کاربر در تلگرام، ارسال آن به سرویس تحلیل و استخراج متن (ASR)، ارسال متن استخراج‌شده به هوش مصنوعی، دریافت پاسخ صوتی از هوش مصنوعی و ارسال آن به کاربر در تلگرام است. خارج از این مرحله: ذخیره‌سازی فایل‌های صوتی. نکته حیاتی: باید از میکروفون تلگرام استفاده شود و فایل صوتی به درستی پردازش شود.
**Excerpt:**
```
از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد
```

### Step 8: یکپارچه‌سازی و همگام‌سازی وضعیت چت بین تلگرام و وب
**Status:** `pending` (0%)
**Scope:** این مرحله شامل اطمینان از همگام‌سازی وضعیت چت بین تلگرام و وب‌سایت است. هر پیام و پاسخی که در یک پلتفرم رد و بدل می‌شود باید در پلتفرم دیگر نیز قابل مشاهده باشد. خارج از این مرحله: پیاده‌سازی features جدید. نکته حیاتی: باید از یک دیتابیس مشترک یا event-driven architecture استفاده شود.
**Excerpt:**
```
هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد
```

### Step 9: مدیریت خطا و لاگ‌گیری در ارتباط تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی سیستم مدیریت خطا برای ارتباط با تلگرام است. باید خطاهای احتمالی (قطع ارتباط، timeout، خطای API) مدیریت شوند و لاگ مناسب ثبت شود. خارج از این مرحله: features جدید. نکته حیاتی: باید از retry mechanism و fallback استفاده شود.
**Excerpt:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه
```

### Step 10: تست و اعتبارسنجی نهایی قابلیت‌های تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل تست تمام قابلیت‌های پیاده‌سازی‌شده (ارسال نوتیفیکیشن، دریافت دستورات، مدیریت دروس، چت با هوش مصنوعی، پیام صوتی) است. باید سناریوهای مختلف تست شوند. خارج از این مرحله: توسعه features جدید. نکته حیاتی: باید هم تست واحد و هم تست یکپارچه‌سازی انجام شود.
**Excerpt:**
```
و تا حد زیادی کار رو از اینجا پیش ببرم
```
