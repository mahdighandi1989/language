---
task_id: dc721af2-f84f-424d-a491-91e8ab3624c0
title: میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار (6 مرحله)
type: other
priority: medium
execution_priority: 3000
status: pending
external_status: pending
verification_status: pending
watched_id: 6b04f8bd-b562-4fb9-9cd9-0c27458ced59
project: mahdighandi1989/language
created_at: '2026-05-12T11:13:22.960230+00:00'
updated_at: '2026-06-02T07:43:18.236437+00:00'
target_files:
- backend/server.js
- backend/.env.example
- render.yaml
- frontend/src/App.jsx
- frontend/index.html
---

# میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار (6 مرحله)

## Raw Idea

میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم و از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

📖 **خواندن کامل + اجرای مو-به-مو (بسیار مهم):**

این پرامپت — از این یادداشت تا انتها — یک سند واحد است که هر بخشش
حاوی الزام یا context منحصربه‌فرد است. خواندن سطحی یا skim کردن **ممنوع**
است.

- پرامپت را **سطر به سطر** بخوان، نه head/tail/فقط-بخش-اصلی.
- اگر بخشی به‌نظر طولانی یا تکراری آمد، **حتماً** بخوان — تفاوت‌های
  ریز ممکن است در آن جا اساسی باشند.
- هر جمله، URL، نام فایل، نام تابع، یا مقدار عددی که در پرامپت آمده،
  دقیقاً همان است که کاربر می‌خواهد — تغییرش نده، رندش نکن، خلاصه‌اش
  نکن.
- اگر پرامپت چندین درخواست/مرحله/زیرتسک دارد، **همه** را پیاده کن. حتی
  یکی را نه به‌عنوان "خارج از scope" حذف کن.

❌ ممنوعات صریح:
- خلاصه‌سازی متن کاربر در commit message یا response
- "این بخش اصلی نیست، رد می‌کنم"
- "کاربر احتمالاً منظورش این بود..." — منظورش همان است که نوشته
- "این URL/نام به نظر قدیمی است، آپدیتش کردم" — تغییر بدون درخواست ممنوع
- پیاده‌سازی فقط بخشی از پرامپت و تظاهر به کامل بودن
- "همه آیتم‌های لیست A را بررسی کردم، B و C مشابه بودند" — نه؛
  هرکدام را جداگانه

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

🔗 **وابستگی‌ها و همگام‌سازی (بسیار حیاتی — هرگز skip نکن):**

این بخش از همهٔ بخش‌های دیگرِ این یادداشت **مهم‌تر** است. اگر نقض شود،
نتیجهٔ کار ممکن است مشروع به‌نظر برسد ولی در عمل بخش‌های دیگر سیستم را عقب
بیندازد، broken reference تولید کند، یا منجر به data corruption شود.

پیش از و حین تغییر، تمام وابستگی‌ها را در **چهار جهت** به‌طور **کامل و
بدون هیچ خلاصه‌سازی** شناسایی و همگام کن:

**۱. وابستگی‌های upstream (این تسک به چه چیزهایی متکی است):**
- چه فایل‌ها، توابع، کلاس‌ها، API endpoint ها، schema های دیتابیس،
  env vars، یا config هایی که این تسک نیاز دارد؟
- آیا قرار است چیزی را ویرایش/حذف کنی که جای دیگر (signature، رفتار،
  return type، side effect) از آن انتظار خاصی می‌رود؟
- اگر dependency جدیدی اضافه می‌کنی، آیا با dependencyهای موجود تداخل
  دارد (نسخه، compat، lock file)؟

**۲. وابستگی‌های downstream (چه چیزهایی به این تسک متکی‌اند):**
- چه فایل‌ها، توابع، تست‌ها، migrations، docs، یا UI component هایی از
  کدی که داری ویرایش/اضافه/حذف می‌کنی **استفاده می‌کنند**؟
- با grep و reference search **همه‌ی** call sites، importها، subclassها،
  reference های مستقیم و غیرمستقیم را پیدا کن — نه فقط چند مورد اصلی.
- خصوصاً برای حذف یا rename: هیچ broken reference نباید باقی بماند.

**۳. وابستگی‌های cross-tier (بسیار مهم — هرگز فقط یک لایه را نبین):**

تسک شما ممکن است از backend، frontend، database، worker، یا هر tier
دیگری شروع شده باشد. ولی تغییرات تقریباً همیشه روی tier های دیگر هم
اثر می‌گذارند. **مستقل از اینکه تسک از کدام tier است**، این چک‌های دو
طرفه را همیشه انجام بده:

🔁 **اگر backend را تغییر دادی** (API، service، model، route):
  → frontend: کدام component/page/hook این endpoint یا data shape را
    مصرف می‌کند؟ type definition، state shape، error handling، loading
    state، form validation، URL routing همگی باید همگام شوند.
  → mobile/SDK/client library (اگر پروژه دارد): همان داستان frontend.
  → database: آیا migration لازم است؟ آیا rollback امن است؟
  → background workers: آیا event producer/consumer ها تحت تأثیرند؟
  → rate limit، auth، CORS، CSP: آیا رفتار جدید پشتیبانی می‌شود؟

🔁 **اگر frontend را تغییر دادی** (component، form، state، route):
  → backend: آیا endpoint جدید/تغییریافته لازم است؟ آیا data shape ای
    که ارسال می‌شود با schema سرور سازگار است؟
  → backend validation: آیا برای ورودی‌های جدید UI کافی است؟
  → permissions/RBAC: آیا feature جدید نیاز به role check جدید دارد؟
  → analytics/tracking: آیا event های جدید باید در backend log شوند؟
  → SEO/SSR: آیا تغییر route نیاز به sitemap/meta tags جدید دارد؟

🔁 **اگر database/migration را تغییر دادی**:
  → backend models (ORM، Pydantic، dataclasses) همگی به‌روزند؟
  → query های raw SQL یا ORM queries با schema جدید سازگارند؟
  → seed data، fixtures، factory functions تست‌ها به‌روزند؟
  → frontend: آیا data shape جدید در UI به‌درستی render می‌شود؟
  → rollback migration نوشته شده و امن است؟

🔁 **اگر API contract یا event schema را تغییر دادی** (REST، GraphQL،
   WebSocket، gRPC، Kafka، …):
  → OpenAPI/GraphQL schema/proto file آپدیت شد؟
  → همه‌ی consumer ها (client، subscriber، webhook، external API
    user) با version جدید سازگارند؟
  → backward compatibility حفظ شده یا migration path روشن است؟
  → versioning header/path اگر breaking change است؟

🔁 **اگر infrastructure یا config را تغییر دادی** (Dockerfile، CI، Render
   config، env، secrets):
  → README setup/installation section به‌روزه؟
  → `.env.example` با env vars جدید آپدیت شد؟
  → deploy script یا CI workflow هم تغییر کرد؟
  → docs/architecture یا diagram های infrastructure به‌روزند؟

⚠️ **هرگز فقط یک tier را تغییر نده و فرض کنی بقیه خودکار همگام می‌شوند.**
   حتی برای تغییرات به‌ظاهر «کوچک»، چک کن.

**۴. وابستگی‌های جانبی (artifacts که همیشه چک شوند):**

تغییرات کد همیشه روی این artifact ها اثر دارند. **همه را** بررسی و
به‌روز کن — مستندات اولویت **بالا** دارد چون فراموش‌شدنی‌ترین است.

  📝 **مستندات** (همیشه چک کن — حتی برای تغییر کوچک کد):
    - README.md (شرح، setup، نمونه‌های استفاده، badge ها)
    - CHANGELOG.md / RELEASE_NOTES.md
    - docs/ folder (architecture، API reference، user guides، runbooks)
    - inline docstrings/کامنت‌های توابع و کلاس‌های تغییریافته
    - OpenAPI/Swagger annotations، JSDoc/TSDoc
    - architecture diagrams (اگر component اضافه/حذف شد)
    - migration guides (اگر breaking change است)

  🌍 **مستندات کاربر**:
    - i18n files و translation keys
    - UI labels، tooltip ها، help text، error messages
    - in-app onboarding (اگر flow جدید است)

  🧪 **تست‌ها**:
    - unit tests (همه‌ی فایل‌های مرتبط — حتی اگر «بی‌ربط» به‌نظر می‌رسد)
    - integration tests
    - e2e tests (Playwright/Cypress/Selenium)
    - snapshot tests (اگر UI تغییر کرد)
    - contract tests (Pact یا مشابه)
    - performance benchmarks (اگر behavior performance-sensitive تغییر کرد)

  🧬 **type definitions و contracts**:
    - .d.ts files
    - Pydantic models، dataclasses
    - Protobuf/Avro/Thrift schemas
    - GraphQL schema definitions
    - JSON Schemas

  🏗 **infrastructure و config**:
    - Dockerfile، docker-compose.yml
    - Kubernetes manifests
    - Render/Vercel/Netlify config
    - GitHub Actions / GitLab CI workflows
    - environment templates (.env.example، .env.sample)
    - feature flags (LaunchDarkly، GrowthBook، config)

  📊 **monitoring و observability**:
    - logging keys (اگر اضافه/حذف شد، log parser ها هم به‌روز شوند)
    - metric names (Prometheus، Datadog)
    - tracing spans
    - alert rules و dashboards
    - error tracking (Sentry rules، groupings)

  🔐 **security**:
    - auth rules (rate limit، CORS، CSP، HSTS)
    - permissions/RBAC config
    - secrets rotation policies
    - audit log events (اگر action جدید اضافه شد)

  💾 **caches و serialization**:
    - cache keys و TTL (اگر data shape یا lifecycle تغییر کرد)
    - serializer formats (Redis، session storage)
    - browser storage (localStorage، IndexedDB schemas)

**قانون مطلق همگام‌سازی:**
- هر چیزی که در (۱)، (۲)، (۳)، یا (۴) شناسایی شد، در **همان workflow
  این تسک** همگام و به‌روز شود. هرگز برای بعد رها نکن.
- اگر یک فایل/تست/docs نسبت به تغییر شما عقب بماند، در بهترین حالت bug،
  در بدترین حالت مشکل امنیتی یا data corruption تولید می‌کند.
- تغییرات همگام‌سازی می‌توانند در commit جداگانه باشند (در همان task)،
  ولی نباید skip شوند یا به «refactor آینده» سپرده شوند.

**هرگز این جمله‌ها قابل قبول نیست:**
- ❌ «بعداً پیداش می‌کنم»
- ❌ «احتمالاً جای دیگه‌ای استفاده نمی‌شه»
- ❌ «این یه refactor جداگانه‌ست — out of scope»
- ❌ «فقط فایل‌های اصلی رو بررسی کردم»
- ❌ «حدس می‌زنم چیزی بهش وابسته نیست»
- ❌ «دامنه‌ی وابستگی‌ها رو خلاصه کردم» — هرگز خلاصه نکن
- ❌ «این task فقط backend است؛ frontend مشکل خودش» — هرگز
- ❌ «این task فقط frontend است؛ backend از قبل کار می‌کند» — هرگز ثابت نکرده
- ❌ «مستندات بعداً به‌روز می‌شن» — همیشه same-task همگام شوند
- ❌ «testها رو نگاه نکردم چون فقط یه تغییر کوچیک بود»

**در commit message یا PR description**، دامنهٔ وابستگی‌های شناسایی‌شده و
همگام‌شده را به‌طور explicit و **per-tier** بنویس. مثال:
```
Dependencies synced:
- upstream: User model schema, auth middleware
- downstream: 3 API endpoints, 5 frontend components, 12 tests
- cross-tier (backend → frontend): UserProfile.tsx, useUser.ts hook,
  api-types.ts (TS definitions)
- cross-tier (backend → infra): .env.example added NEW_AUTH_SCOPES
- side artifacts: OpenAPI spec, README API section, i18n keys for
  new errors, Sentry alert rule for new error code
```
اگر هیچ وابستگی پیدا نکردی در هر کدام از چهار جهت، صریحاً بنویس:
«بررسی شد — هیچ وابستگی upstream / downstream / cross-tier (backend↔
frontend↔db↔infra) / side شناسایی نشد» تا مشخص باشد بررسی **انجام شده**
نه اینکه فراموش شده.

📋 **مدیریت TO-DO برای اقدامات دستی کاربر (همیشه چک کن):**

⚠️ **هشدار بحرانی — قاعدهٔ ضد-فرار:** TO-DO فقط برای کارهایی است که
**واقعاً غیرممکن** برای agent است (نیاز به انسان مطلق)، نه برای کارهایی
که «بزرگ‌اند»، «وقت می‌برند»، یا «نیازمند fixture/setup» هستند. اگر یک
agent در یک سشن بیش از **۲۰٪ از تسک‌ها** را با TO-DO ببندد، یعنی از کار
فرار می‌کند — این الگو در سشن‌های قبلی **مشاهده** شده و الان ممنوع است.

✅ **فقط برای این موارد TO-DO بساز** (لیست بسته — هرچه خارج این لیست
ممنوع است):

  ۱. **Credential/secret که فقط کاربر دارد**:
     - تنظیم API key واقعی در پنل ادمین خارجی (Render، AWS، Stripe، …)
     - تأیید OAuth client روی console آن سرویس
     - paste کردن webhook secret که فقط بعد از ساخت در dashboard ظاهر می‌شود

  ۲. **Account/billing روی سرویس خارجی که کاربر باید عضو شود**:
     - ساخت account جدید روی Stripe/SendGrid/Twilio/Google Cloud
     - تأیید verification شماره یا ID
     - فعال‌سازی subscription پولی

  ۳. **داده/asset خصوصی که فقط کاربر دارد**:
     - آپلود لوگو/تصویر/فونت برند
     - paste کردن داده‌ای که در محل کار کاربر است
     - import داده‌ای که فقط روی device کاربر است

  ۴. **تصمیم سلیقه‌ای/حقوقی/کسب‌وکار**:
     - انتخاب رنگ‌بندی نهایی یا تم
     - متن دقیق Terms of Service / Privacy Policy
     - تعرفهٔ قیمت‌گذاری
     - نام نهایی برند یا دامنه

⛔ **هرگز TO-DO نکن برای** (لیست سیاه — هر چیزی که در این لیست است
**قابل اجرا** توسط agent است، حتی اگر بزرگ یا چندبخشی باشد):

  ❌ UI component / page / dashboard (هر فریم‌ورک: React, Vue, Angular,
     Svelte، حتی اگر معماری بزرگ دارد) — می‌توانی stub اولیه + state
     management + layout + استایل بسازی
  ❌ "نیازمند Google Drive / Stripe / Twilio API" — می‌توانی **client
     stub** با abstraction layer بسازی که با env var واقعی plug-in شود؛
     کد integration یعنی پیاده‌سازی، نه TO-DO
  ❌ "feature بزرگ، چند روز کار می‌برد" — اندازه دلیل defer نیست؛ کوچک
     شروع کن، iterate کن، در همین سشن کامل کن
  ❌ Celery / background worker / scheduler — یک task ساده + register
     می‌توانی بسازی
  ❌ Migration / model / schema — حتی اگر فیلد جدید نیاز دارد، اضافه کن
  ❌ REST endpoint / GraphQL resolver / WebSocket route — هرگز TO-DO
  ❌ test (unit/integration/e2e) — همیشه قابل نوشتن
  ❌ Documentation / README / API docs — همیشه قابل نوشتن
  ❌ Config file / .env.example / Dockerfile / CI workflow — همیشه قابل
     نوشتن
  ❌ "می‌توانستی .tsx ولی repo .jsx است" — از .jsx استفاده کن، TO-DO نکن
  ❌ "نیازمند فیلد X در مدل دیگر" — اضافه کن فیلد را، TO-DO نکن
  ❌ "تصمیم admin-vs-user-scoped" — پرامپت اولیه scope را معلوم کرده،
     یا با محتاطانه‌ترین تفسیر پیش برو
  ❌ "credential در production هنوز ست نیست" — این TO-DO ساده برای
     تنظیم env var است (مورد ۱ بالا)، نه دلیل برای defer کردن کد
  ❌ "نیازمند verification از کاربر" — اگر اقدام واقعی غیرممکن نیست،
     پیش برو
  ❌ هر چیزی که در یک کامنت `# TODO` معمولی نوشته می‌شد — این فایل
     TO-DO نیست، کامنت inline است

🔬 **قاعدهٔ «حداقل تلاش» قبل از TO-DO**: قبل از TO-DO کردن یک AC، **اثبات
کن** که قابل انجام نیست:

  ۱. آیا می‌توانم یک stub/placeholder بسازم که با env واقعی plug-in شود؟
     → اگر بله، بساز و TO-DO نکن
  ۲. آیا می‌توانم برای این بخش یک test (حتی mock-based) بنویسم؟
     → اگر بله، بنویس و TO-DO نکن
  ۳. آیا می‌توانم abstraction/interface را تعریف کنم، حتی اگر backend
     واقعی نیست؟ → اگر بله، تعریف کن و TO-DO نکن
  ۴. آیا فقط یک حالت سلیقه‌ای/decision کاربر در میان است؟
     → فقط آن یک decision را TO-DO کن، نه کل feature را

اگر یکی از این چهار راه‌حل ممکن بود ولی به TO-DO رفتی، **اعتبار شما از
بین می‌رود**.

📊 **آستانهٔ TO-DO per session**: در یک حلقهٔ اجرای N تسک، اگر بیشتر از
**۲۰٪** تسک‌ها فایل TO-DO ساختی، خودت در گزارش پایانی صریحاً اعلام کن:

  "⚠️ نسبت TO-DO من {K}/{N} = {%} است که از آستانهٔ ۲۰٪ بالاتر است.
   احتمالاً برخی از این TO-DO ها قابل اجرا بودند ولی من فرار کردم.
   لیست TO-DO ها را کاربر باید بازبینی کند که آیا واقعاً Manual-required
   بودند یا agent ضعیف کار کرده."

**یادآوری همیشگی:** اگر در آینده قابلیت‌های شما گسترش پیدا کرد و توانستید
یکی از موارد لیست سفید را خودکار انجام دهید (مثلاً managed credential
injection، یا integration پولی automate شود)، انجام دهید و TO-DO نسازید.
لیست سفید بسته است ولی **بسته از پایین** (می‌تواند کوچک‌تر شود اگر
قابلیت‌ها رشد کنند، ولی هرگز بزرگ‌تر نشود برای فرار).

**اگر هیچ بخش Manual-required نبود (تمام تسک Auto-capable است)**:
  → فایل TO-DO **نساز**. فولدر TO-DO/ باید پاک و معنادار بماند.
  → اگر برای این task از قبل `TO-DO/todo-task-{task_id_first_8}.md` بود
     (یعنی در run قبلی نیاز به دخالت کاربر بود ولی الان نه): فایل قدیمی
     را پاک کن و entry را از `TO-DO/_index.json` حذف کن.

**اگر بخش Manual-required دارد** (همه‌جانبه یا hybrid):
  1. فولدر TO-DO/ را در ریشه ریپو ایجاد کن اگر نیست
  2. فایل `TO-DO/todo-task-{task_id_first_8}.md` بساز با front-matter
     شامل: task_id, task_title, execution_priority, created_at,
     updated_at, status: "pending"
     و در بدنه: «چرا این فایل ساخته شد»، «وضعیت بخش‌های خودکار»
     (commit ها reference)، «کارهایی که باید انجام دهی» با اولویت
     بالا/متوسط/پایین به ترتیب، «وقتی این کارها را تمام کردی»
  3. `TO-DO/_index.json` را با **merge** آپدیت کن (نه overwrite):
     - فایل موجود را بخوان
     - entry های orphan (فایلشان پاک شده) را حذف کن
     - entry این task را اضافه/replace کن
     - بر اساس execution_priority صعودی مرتب کن
     - ساختار: `{"version":1, "generated_at": ISO, "total": N, "items": [...]}`
  4. این تغییرات TO-DO را در **همان commit کد** شامل کن (نه commit جداگانه)

⛔ **ممنوعات مطلق TO-DO**:
  ❌ ساختن TO-DO برای کاری که می‌توانستی خودت انجام دهی (شلوغی فولدر)
  ❌ overwrite کردن `TO-DO/_index.json` بدون merge (data loss)
  ❌ نگه‌داشتن entry هایی که فایل‌شان پاک شده (broken reference)
  ❌ فراموش کردن نوشتن «خروجی مورد انتظار» در هر آیتم TO-DO

این بخش الزامی است. حتی اگر فکر می‌کنی "این تسک کاملاً auto است و نیازی
به TO-DO نیست"، صریحاً در commit message یا report بنویس:
"بررسی شد — این تسک هیچ بخش Manual-required ندارد، TO-DO ساخته نشد."

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

🔁 **Commit + Push فوری per-task (بسیار مهم برای جریان کار صحیح):**

پس از اتمام پیاده‌سازی این تسک، **بلافاصله** commit کن و **همان موقع**
به default branch (main/master) push کن. سپس به تسک بعدی برو.

✓ چرا این قانون حیاتی است:
  - تسک‌های بعدی ممکن است به فایل‌ها/تغییراتی که این تسک ایجاد کرده
    نیاز داشته باشند. اگر push نکنی، `git pull` بعدی آن‌ها را نمی‌بیند.
  - جمع‌کردن تغییرات چند تسک منجر به conflict های بزرگ می‌شود.
  - اگر در میانه fail کنی، task های push شده ضایع نمی‌شوند.

⛔ ممنوع: "همه task ها را تمام می‌کنم بعد یک‌جا push می‌زنم"
⛔ ممنوع: branch جدا برای task — مستقیم به default branch
⛔ ممنوع: task بعدی بدون push کامل task قبلی

---


---

## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند.)_

```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم و از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....
```

## 📋 چک‌لیست مراحل (9 مرحله)

این تسک به مراحل کوچک‌تر تقسیم شده. **در هر verify خودکار، وضعیت هر مرحله به‌صورت `[ ]` (انجام نشده)، `[~]` (ناقص)، یا `[x]` (انجام شده) به‌روز می‌شود.**
وقتی تمام مراحل `[x]` شدند، تسک به‌طور خودکار به «انجام شده» منتقل می‌شود.

- [ ] **مرحله 1: راه‌اندازی ربات تلگرام و اتصال اولیه به سیستم** — ایجاد یک ربات تلگرام جدید از طریق BotFather، دریافت توکن API، و راه‌اندازی یک webhook یا polling برای دریافت پیام‌ها. این مرحله شامل اتصال پایه‌ای ربات به سرور پروژه است. خارج از این مرحله: پیاده‌سازی منطق دوطرفه، مدیریت محتوا، یا اتصال به هوش مصنوعی. نکته حیاتی: باید از کتابخانه python-telegram-bot
- [ ] **مرحله 2: پیاده‌سازی ارسال نوتیفیکیشن از سیستم به تلگرام** — ایجاد یک سرویس داخلی که رویدادهای مشخص سیستم (مثلاً ثبت سفارش، خطا، به‌روزرسانی) را به پیام تلگرام تبدیل کرده و به کاربر ارسال کند. شامل تعریف فرمت پیام‌ها، مدیریت خطا در ارسال، و صف‌بندی پیام‌ها. خارج از این مرحله: دریافت پیام از تلگرام یا کنترل سیستم. نکته حیاتی: باید از توکن ربات مرحله قبل استفاد
- [ ] **مرحله 3: پیاده‌سازی دریافت دستورات از تلگرام و اجرای عملیات روی سیستم** — تحلیل پیام‌های دریافتی از تلگرام، شناسایی دستورات (مثلاً /status، /restart، /report)، و اجرای عملیات متناظر روی سیستم. شامل تعریف لیست دستورات، اعتبارسنجی کاربر، و بازگرداندن نتیجه به تلگرام. خارج از این مرحله: مدیریت محتوای دروس یا اتصال به هوش مصنوعی. نکته حیاتی: باید احراز هویت کاربر تلگرام با کا
- [ ] **مرحله 4: ایجاد قابلیت مشاهده محتوای دروس از طریق تلگرام** — طراحی و پیاده‌سازی دستورات تلگرامی برای مشاهده لیست دروس، مشاهده جزئیات هر درس، و مشاهده فایل‌های ضمیمه (PDF، ویدئو، تصویر). شامل ارسال فایل‌ها از طریق تلگرام و مدیریت لینک‌ها. خارج از این مرحله: آپلود محتوا یا ویرایش. نکته حیاتی: باید از API دیتابیس دروس پروژه استفاده شود و فایل‌ها به صورت مستقیم ی
- [ ] **مرحله 5: اتصال ربات تلگرام به بخش 'تمرین با استاد هوش مصنوعی'** — ایجاد یک endpoint یا سرویس داخلی که ربات تلگرام را به API چت هوش مصنوعی موجود در پروژه متصل کند. شامل ارسال پیام‌های کاربر از تلگرام به هوش مصنوعی و دریافت پاسخ. خارج از این مرحله: مدیریت موضوعات، انتخاب موضوع، یا پیام صوتی. نکته حیاتی: باید از API موجود پروژه استفاده شود و session مدیریت شود.
- [ ] **مرحله 6: مدیریت و انتخاب موضوعات تمرین از طریق تلگرام** — طراحی و پیاده‌سازی منوی انتخاب موضوع برای تمرین با هوش مصنوعی از طریق دکمه‌های شیشه‌ای تلگرام. شامل دریافت لیست موضوعات از API، نمایش آن‌ها، و ذخیره انتخاب کاربر. خارج از این مرحله: خود مکالمه با هوش مصنوعی. نکته حیاتی: باید از InlineKeyboard با callback_data استفاده شود.
- [ ] **مرحله 7: پیاده‌سازی ارسال پیام صوتی از تلگرام به هوش مصنوعی** — دریافت فایل صوتی از تلگرام (voice message)، ارسال آن به سرویس تحلیل و استخراج متن موجود در پروژه، و سپس ارسال متن استخراج‌شده به هوش مصنوعی. شامل مدیریت فرمت صوتی و خطاهای تبدیل. خارج از این مرحله: دریافت پاسخ صوتی از هوش مصنوعی. نکته حیاتی: باید از سرویس ASR (تشخیص گفتار) موجود در پروژه استفاده شود
- [ ] **مرحله 8: پیاده‌سازی دریافت پاسخ صوتی از هوش مصنوعی در تلگرام** — دریافت پاسخ متنی از هوش مصنوعی، تبدیل آن به صوت (TTS) با استفاده از سرویس موجود در پروژه، و ارسال فایل صوتی به تلگرام. شامل مدیریت کیفیت صوت و خطاهای تبدیل. خارج از این مرحله: ارسال متن پاسخ. نکته حیاتی: باید از سرویس TTS موجود در پروژه استفاده شود و فایل صوتی با فرمت OGG یا MP3 ارسال شود.
- [ ] **مرحله 9: یکپارچه‌سازی و هماهنگ‌سازی کامل چت تلگرام با چت وب** — اطمینان از اینکه تمام پیام‌ها و پاسخ‌ها (متن، صوت، فایل) بین تلگرام و چت وب هوش مصنوعی همگام‌سازی می‌شوند. شامل ذخیره تاریخچه چت در دیتابیس مشترک، نمایش پیام‌های تلگرام در وب و بالعکس. خارج از این مرحله: قابلیت‌های جدید. نکته حیاتی: باید از یک دیتابیس مشترک برای تاریخچه استفاده شود و timestampها هما

---

# 🔹 مرحله 1: راه‌اندازی ربات تلگرام و اتصال اولیه به سیستم

**Scope:** ایجاد یک ربات تلگرام جدید از طریق BotFather، دریافت توکن API، و راه‌اندازی یک webhook یا polling برای دریافت پیام‌ها. این مرحله شامل اتصال پایه‌ای ربات به سرور پروژه است. خارج از این مرحله: پیاده‌سازی منطق دوطرفه، مدیریت محتوا، یا اتصال به هوش مصنوعی. نکته حیاتی: باید از کتابخانه python-telegram-bot یا aiogram استفاده شود و webhook روی آدرس HTTPS معتبر تنظیم شود.
**Key terms:** BotFather, توکن API, webhook, python-telegram-bot, aiogram, HTTPS

**بخش مربوط از متن کاربر:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

## 🎯 هدف (خلاصه ساختاریافته)
راه‌اندازی ربات تلگرام و اتصال اولیه به سیستم

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:1-53` — `imports and configuration` — این فایل نقطه ورود backend است. باید endpoint جدید برای webhook تلگرام و تابع ارسال نوتیفیکیشن به آن اضافه شود. همچنین باید TELEGRAM_BOT_TOKEN از env خوانده شود.
  ```jsx
  import express from 'express';
  import cors from 'cors';
  import { fileURLToPath } from 'url';
  import { dirname, join } from 'path';
  import dotenv from 'dotenv';
  import { createServer } from 'http';
  import { WebSocketServer, WebSocket } from 'ws';
  ```
- `backend/.env.example:1-8` — `environment variables` — باید خط TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here به این فایل اضافه شود.
  ```
  # Gemini API Key - Get from https://aistudio.google.com/apikey
  GEMINI_API_KEY=your_gemini_api_key_here
  
  # Port (optional, default 3001)
  PORT=3001
  
  # Firebase config (optional - for data persistence)
  # VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}
  ```
- `render.yaml:1-10` — `deployment configuration` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. باید سرویس پایتونی جدید به render.yaml اضافه شود.
  ```
  فایل render.yaml در ساختار پروژه موجود است اما محتوای آن در deep context داده نشده است.
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/package.json` (سطر 1) — این فایل dependencies فعلی backend را مشخص می‌کند. برای سرویس پایتونی جدید نیاز به فایل requirements.txt جداگانه است.
- `frontend/index.html` (سطر 11) — این فایل شامل Firebase config است. در آینده ممکن است نوتیفیکیشن‌های تلگرام از طریق Firebase Cloud Messaging نیز مدیریت شوند.
- `.github/workflows/claude-auto-task.yml` — این workflow خودکار تسک‌ها را اجرا می‌کند. اگر سرویس پایتونی جدید اضافه

## 🔍 Context و وضعیت فعلی
کاربر درخواست کرده است که برای این پروژه (اپلیکیشن آموزش لهجه لبنانی با هوش مصنوعی) نوتیفیکیشن از طریق تلگرام فراهم شود و تلگرام دو طرفه کار کند. یعنی علاوه بر اینکه اطلاعات از سیستم گرفته می‌شود و از طریق تلگرام ارسال می‌شود، کاربر بتواند از طریق تلگرام هم برخی کارها روی سیستم انجام دهد. در این تسک خاص، کاربر صرفاً راه‌اندازی اولیه ربات تلگرام را می‌خواهد: ایجاد یک ربات تلگرام جدید از طریق BotFather، دریافت توکن API، و راه‌اندازی یک webhook یا polling برای دریافت پیام‌ها. این مرحله شامل اتصال پایه‌ای ربات به سرور پروژه است. خارج از این مرحله: پیاده‌سازی منطق دوطرفه، مدیریت محتوا، یا اتصال به هوش مصنوعی. نکته حیاتی: باید از کتابخانه python-telegram-bot یا aiogram استفاده شود و webhook روی آدرس HTTPS معتبر تنظیم شود. پروژه فعلی یک اپلیکیشن Node.js/Express با frontend React است (backend/server.js خط ۱-۱۴۰۳). backend/server.js شامل WebSocket (خط ۴۵: new WebSocketServer({ server, path: '/ws/live' }))، APIهای Gemini (خطوط ۵۶-۱۶۴)، و تحلیل فایل (خطوط ۶۹۸-۱۴۰۳) است. backend/package.json شامل dependencies: express, cors, dotenv, ws, multer, fluent-ffmpeg, pdf-parse است. frontend/index.html شامل Firebase config (خطوط ۱۱-۱۸) و Inspector Bridge Script (خطوط ۳۲-۲۰۱) است. backend/.env.example شامل GEMINI_API_KEY و PORT است. کاربر تأکید کرده که باید از python-telegram-bot یا aiogram استفاده شود — این یعنی یک سرویس پایتونی جداگانه باید به پروژه اضافه شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ۱. ایجاد یک سرویس پایتونی جدید در مسیر backend/telegram_bot/ با فایل‌های اصلی bot.py و requirements.txt. ۲. در bot.py، با استفاده از کتابخانه python-telegram-bot (نسخه ۲۰.x) یا aiogram، یک ربات تلگرام با polling (برای توسعه) و webhook (برای production) راه‌اندازی کن. ۳. از BotFather یک ربات جدید بساز و توکن API را در فایل .env پروژه (backend/.env.example) با کلید TELEGRAM_BOT_TOKEN اضافه کن. ۴. webhook را روی آدرس HTTPS معتبر تنظیم کن — برای این کار باید از Render.com (که render.yaml در پروژه موجود است) یا سرویس مشابه استفاده شود. ۵. یک endpoint ساده در backend/server.js (مثلاً POST /api/telegram/webhook) ایجاد کن تا پیام‌های دریافتی از تلگرام را دریافت کند و به سرویس پایتونی فوروارد کند. ۶. در backend/server.js، یک تابع کمکی برای ارسال نوتیفیکیشن به تلگرام (sendTelegramNotification) اضافه کن که از API تلگرام (https://api.telegram.org/bot{token}/sendMessage) استفاده کند. ۷. فایل backend/requirements.txt ایجاد کن شامل python-telegram-bot==20.7 یا aiogram==3.3.0. ۸. فایل render.yaml را به‌روزرسانی کن تا سرویس پایتونی جدید را شامل شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 2: پیاده‌سازی ارسال نوتیفیکیشن از سیستم به تلگرام

**Scope:** ایجاد یک سرویس داخلی که رویدادهای مشخص سیستم (مثلاً ثبت سفارش، خطا، به‌روزرسانی) را به پیام تلگرام تبدیل کرده و به کاربر ارسال کند. شامل تعریف فرمت پیام‌ها، مدیریت خطا در ارسال، و صف‌بندی پیام‌ها. خارج از این مرحله: دریافت پیام از تلگرام یا کنترل سیستم. نکته حیاتی: باید از توکن ربات مرحله قبل استفاده شود و پیام‌ها به چت‌آیدی مشخصی ارسال شوند.
**Key terms:** سرویس نوتیفیکیشن, chat_id, send_message, صف پیام, رویداد سیستم

**بخش مربوط از متن کاربر:**
```
علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد سرویس نوتیفیکیشن تلگرام برای رویدادهای سیستم

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی ارسال نوتیفیکیشن از سیستم به تلگرام را دارد. این شامل ایجاد یک سرویس داخلی است که رویدادهای مشخص سیستم (مثلاً ثبت سفارش، خطا، به‌روزرسانی) را به پیام تلگرام تبدیل کرده و به کاربر ارسال کند. همچنین باید شامل تعریف فرمت پیام‌ها، مدیریت خطا در ارسال، و صف‌بندی پیام‌ها باشد. کاربر تأکید کرده که خارج از این مرحله: دریافت پیام از تلگرام یا کنترل سیستم است. نکته حیاتی: باید از توکن ربات مرحله قبل استفاده شود و پیام‌ها به چت‌آیدی مشخصی ارسال شوند. کلیدواژه‌های کاربر: سرویس نوتیفیکیشن, chat_id, send_message, صف پیام, رویداد سیستم. در کد فعلی پروژه (backend/server.js) هیچ سرویس تلگرامی وجود ندارد. پروژه از Express.js با WebSocket (ws) و Gemini API استفاده می‌کند. فایل backend/.env.example فقط شامل GEMINI_API_KEY و PORT و Firebase config است. backend/package.json شامل وابستگی‌های express, cors, dotenv, ws, multer, fluent-ffmpeg, ffmpeg-static, pdf-parse است. frontend/index.html شامل Firebase config و inspector bridge script است. backend/server.js خط 45: WebSocketServer روی path '/ws/live' ایجاد شده. backend/server.js خط 56-114: endpoint /api/gemini/chat برای چت با Gemini. backend/server.js خط 117-164: endpoint /api/gemini/tts برای تبدیل متن به گفتار. backend/server.js خط 698-800: endpoint /api/analyze-files برای تحلیل فایل‌ها. backend/server.js خط 167-169: endpoint /api/health برای health check. backend/server.js خط 172-192: endpoint /api/list-models برای لیست مدل‌ها. backend/server.js خط 195-240: endpoint /api/test-gemini برای تست Gemini API. backend/server.js خط 582-614: LEBANESE_CORRECTION_PROMPT برای تصحیح عربی لبنانی. backend/server.js خط 617-683: ANALYSIS_SYSTEM_PROMPT برای تحلیل محتوا. backend/server.js خط 686-695: handleMulterError برای مدیریت خطای multer. backend/server.js خط 247-270: تابع splitIntoChunks برای تقسیم محتوای بزرگ. backend/server.js خط 273-294: تابع analyzeWithGemini برای تحلیل با Gemini. backend/server.js خط 297-392: تابع uploadToGeminiFileAPI برای آپلود فایل به Gemini. backend/server.js خط 395-429: توابع analyzeWithGeminiFileAPI و analyzeWithGeminiFileAPIWithModel برای تحلیل فایل با Gemini. backend/server.js خط 432-442: تابع deleteGeminiFile برای حذف فایل از Gemini. backend/server.js خط 445-501: توابع getVideoDuration و splitVideoIntoSegments برای پردازش ویدیو. backend/server.js خط 504-528: تابع extractAudioFromVideo برای استخراج صدا از ویدیو. backend/server.js خط 531-562: تابع extractKeyFrames برای استخراج فریم‌های کلیدی. backend/server.js خط 565-579: تابع extractPdfText برای استخراج متن از PDF. backend/server.js خط 698-800: endpoint /api/analyze-files با multer برای آپلود فایل. backend/server.js خط 800 به بعد: ادامه پردازش فایل‌های صوتی و تصویری. frontend/index.html خط 9-20: Firebase config با apiKey, authDomain, projectId, storageBucket, messagingSenderId, appId. frontend/index.html خط 32-201: inspector bridge script برای ارتباط با پنل مدیریت. frontend/index.html خط 204-207: root div و main.jsx. frontend/src/App.jsx: کامپوننت اصلی React. frontend/src/main.jsx: نقطه ورود React. frontend/vite.config.js: proxy برای /api به localhost:3001. backend/package.json: وابستگی‌های express, cors, dotenv, ws, multer, fluent-ffmpeg, ffmpeg-static, pdf-parse. frontend/package.json: وابستگی‌های firebase, lucide-react, react, react-dom. .github/workflows/claude-auto-task.yml: workflow برای اجرای خودکار Claude Code. backend/.env.example: نمونه فایل env با GEMINI_API_KEY و PORT. package.json: اسکریپت‌های install:all, dev, build, start. render.yaml: فایل کانفیگ برای Render. prompt/archive/task-141249ee-0d23-44e0-aeec-21f989820b60.md: تسک‌های قبلی مرتبط با نوتیفیکیشن. prompt/archive/task-2a0673ad-13dd-4131-9c72-18a093d60686.md: تسک‌های قبلی مرتبط با نوتیفیکیشن. prompt/archive/task-2d1f95e8-b5ea-413f-b1ec-11b7bed6eeb7.md: تسک‌های قبلی مرتبط با نوتیفیکیشن. prompt/archive/task-3336cbc2-34e9-4f8f-8216-78d40b813199.md: تسک‌های قبلی مرتبط با نوتیفیکیشن. prompt/archive/task-38433cce-3e63-4851-89bc-4320f55f4d19.md: تسک‌های قبلی مرتبط با نوتیفیکیشن. prompt/archive/task

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
_(مجری بر اساس Context و معیارهای پذیرش، مراحل را تعیین کند)_

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 3: پیاده‌سازی دریافت دستورات از تلگرام و اجرای عملیات روی سیستم

**Scope:** تحلیل پیام‌های دریافتی از تلگرام، شناسایی دستورات (مثلاً /status، /restart، /report)، و اجرای عملیات متناظر روی سیستم. شامل تعریف لیست دستورات، اعتبارسنجی کاربر، و بازگرداندن نتیجه به تلگرام. خارج از این مرحله: مدیریت محتوای دروس یا اتصال به هوش مصنوعی. نکته حیاتی: باید احراز هویت کاربر تلگرام با کاربر سیستم تطبیق داده شود.
**Key terms:** دستورات تلگرام, CommandHandler, احراز هویت, /status, /restart

**بخش مربوط از متن کاربر:**
```
بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی دریافت دستورات تلگرام و اجرای عملیات روی سیستم

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:38-45` — `server creation and WebSocket setup` — محل فعلی ایجاد سرور HTTP و WebSocket. بعد از WebSocket، Telegram Bot باید راه‌اندازی شود.
  ```jsx
  const app = express();
  const PORT = process.env.PORT || 3001;
  
  // Create HTTP server
  const server = createServer(app);
  
  // Create WebSocket server
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/server.js:167-169` — `Health check endpoint` — این endpoint برای دستور /status استفاده می‌شود تا وضعیت سرور را به تلگرام برگرداند.
  ```jsx
  // Health check
  app.get('/api/health', (req, res) => {
    res.json({ status: 'ok', timestamp: new Date().toISOString() });
  });
  ```
- `backend/.env.example:1-8` — `Environment variables template`
  ```
  # Gemini API Key - Get from https://aistudio.google.com/apikey
  GEMINI_API_KEY=your_gemini_api_key_here
  
  # Port (optional, default 3001)
  PORT=3001
  
  # Firebase config (optional - for data persistence)
  # VITE_FIREBASE_CONFIG={"apiKey":"...","auth
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی دریافت دستورات از تلگرام و اجرای عملیات روی سیستم را دارد. متن خام کاربر: «پیاده‌سازی دریافت دستورات از تلگرام و اجرای عملیات روی سیستم. تحلیل پیام‌های دریافتی از تلگرام، شناسایی دستورات (مثلاً /status، /restart، /report)، و اجرای عملیات متناظر روی سیستم. شامل تعریف لیست دستورات، اعتبارسنجی کاربر، و بازگرداندن نتیجه به تلگرام. خارج از این مرحله: مدیریت محتوای دروس یا اتصال به هوش مصنوعی. نکته حیاتی: باید احراز هویت کاربر تلگرام با کاربر سیستم تطبیق داده شود. --- بخش مربوط از درخواست اصلی کاربر --- بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم. --- کلیدواژه‌ها --- دستورات تلگرام, CommandHandler, احراز هویت, /status, /restart». این پروژه یک اپلیکیشن آموزش لهجه لبنانی با هوش مصنوعی است که از Express.js در backend (فایل backend/server.js) و React در frontend (فایل frontend/src/App.jsx) استفاده می‌کند. backend/server.js شامل WebSocket (خط 45: new WebSocketServer({ server, path: '/ws/live' }))، APIهای Gemini (خطوط 56-114 برای chat، 117-164 برای TTS، 698+ برای تحلیل فایل)، و endpointهای health check (خط 167) و list-models (خط 172) است. backend/package-lock.json نشان می‌دهد وابستگی‌های اصلی شامل express، cors، dotenv، ws هستند. frontend/index.html شامل Firebase config (خطوط 11-18) و Inspector Bridge Script (خطوط 32-201) است. frontend/package-lock.json شامل react، firebase، lucide-react است. frontend/vite.config.js پروکسی /api به localhost:3001 تنظیم کرده. backend/.env.example متغیر GEMINI_API_KEY و PORT و Firebase config را نشان می‌دهد. در حال حاضر هیچ فایل یا سرویسی برای تلگرام در پروژه وجود ندارد. کاربر می‌خواهد یک CommandHandler برای دستورات /status، /restart، /report ایجاد شود که پیام‌های تلگرام را دریافت، احراز هویت کند (تطبیق کاربر تلگرام با کاربر سیستم)، و عملیات متناظر را اجرا کند. خارج از این مرحله: مدیریت محتوای دروس یا اتصال به هوش مصنوعی.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/services/telegramBot.js برای مدیریت Webhook یا polling تلگرام. 2. در backend/server.js، import و راه‌اندازی Telegram Bot در خطوط بعد از WebSocket (حدود خط 45). 3. تعریف لیست دستورات: /status (وضعیت سرور و APIها)، /restart (راه‌اندازی مجدد سرویس‌ها)، /report (گزارش خطاها). 4. پیاده‌سازی احراز هویت: مقایسه chatId یا username تلگرام با لیست کاربران مجاز (می‌توان از متغیر محیطی یا فایل config استفاده کرد). 5. برای /status: فراخوانی endpoint /api/health (backend/server.js خط 167) و بازگرداندن نتیجه. 6. برای /restart: اجرای دستور سیستمی (با احتیاط) یا صرفاً لاگ و اطلاع‌رسانی. 7. برای /report: جمع‌آوری لاگ‌های اخیر از فایل یا console. 8. نصب کتابخانه node-telegram-bot-api یا telegraf در backend/package.json. 9. افزودن متغیر TELEGRAM_BOT_TOKEN به backend/.env.example. 10. تست با ارسال دستورات از تلگرام و بررسی پاسخ.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 4: ایجاد قابلیت مشاهده محتوای دروس از طریق تلگرام

**Scope:** طراحی و پیاده‌سازی دستورات تلگرامی برای مشاهده لیست دروس، مشاهده جزئیات هر درس، و مشاهده فایل‌های ضمیمه (PDF، ویدئو، تصویر). شامل ارسال فایل‌ها از طریق تلگرام و مدیریت لینک‌ها. خارج از این مرحله: آپلود محتوا یا ویرایش. نکته حیاتی: باید از API دیتابیس دروس پروژه استفاده شود و فایل‌ها به صورت مستقیم یا لینک ارسال شوند.
— [merged] پیاده‌سازی دریافت فایل از تلگرام (مستند، تصویر، ویدئو) و ذخیره آن در سیستم مدیریت محتوای دروس. شامل اعتبارسنجی نوع و حجم فایل، انتخاب درس مقصد، و بازگرداندن تأییدیه. خارج از این مرحله: ویرایش یا حذف محتوا. نکته حیاتی: باید از طریق دکمه‌های شیشه‌ای (InlineKeyboard) درس مقصد انتخاب شود.
**Key terms:** لیست دروس, MessageHandler, آپلود فایل, نوع فایل مجاز, ارسال فایل, ذخیره فایل, جزئیات درس, send_document, InlineKeyboard, دیتابیس دروس

**بخش مربوط از متن کاربر:**
```
بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن قابلیت مشاهده و آپلود دروس از طریق تلگرام با InlineKeyboard

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد قابلیت مشاهده محتوای دروس از طریق تلگرام را دارد. این شامل طراحی و پیاده‌سازی دستورات تلگرامی برای مشاهده لیست دروس، مشاهده جزئیات هر درس، و مشاهده فایل‌های ضمیمه (PDF، ویدئو، تصویر) است. همچنین باید ارسال فایل‌ها از طریق تلگرام و مدیریت لینک‌ها انجام شود. خارج از این مرحله: آپلود محتوا یا ویرایش. نکته حیاتی: باید از API دیتابیس دروس پروژه استفاده شود و فایل‌ها به صورت مستقیم یا لینک ارسال شوند. بخش دوم درخواست: پیاده‌سازی دریافت فایل از تلگرام (مستند، تصویر، ویدئو) و ذخیره آن در سیستم مدیریت محتوای دروس. شامل اعتبارسنجی نوع و حجم فایل، انتخاب درس مقصد، و بازگرداندن تأییدیه. خارج از این مرحله: ویرایش یا حذف محتوا. نکته حیاتی: باید از طریق دکمه‌های شیشه‌ای (InlineKeyboard) درس مقصد انتخاب شود. کاربر در بخش مربوط از درخواست اصلی گفته: 'بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم'. کلیدواژه‌ها: لیست دروس, MessageHandler, آپلود فایل, نوع فایل مجاز, ارسال فایل, ذخیره فایل, جزئیات درس, send_document, InlineKeyboard, دیتابیس دروس. در کد فعلی پروژه (backend/server.js) هیچ endpoint یا سرویسی برای مدیریت دروس یا تلگرام وجود ندارد. فایل frontend/index.html شامل Firebase config (apiKey: AIzaSyAMFtV0zIMKKe0bIr68Z1wTORQ1jQpsv9Q, authDomain: labaneseapp.firebaseapp.com, projectId: labaneseapp) است که می‌توان از Firebase Firestore به عنوان دیتابیس دروس استفاده کرد. backend/package.json شامل dependencies: cors, dotenv, express, ws است و نیاز به افزودن کتابخانه node-telegram-bot-api و firebase-admin دارد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/services/telegramBot.js شامل کلاس TelegramBotService با متدهای: startPolling(), handleStartCommand(), handleListLessons(), handleLessonDetails(), handleFileUpload(), setupInlineKeyboard(). 2. ایجاد فایل جدید backend/services/lessonService.js برای ارتباط با Firebase Firestore شامل متدهای: getLessonsList(), getLessonById(), saveFileToLesson(). 3. ایجاد فایل جدید backend/config/firebase.js برای مقداردهی اولیه Firebase Admin SDK با استفاده از Firebase config موجود در frontend/index.html (apiKey, authDomain, projectId, storageBucket, messagingSenderId, appId). 4. در backend/server.js خط 14 (بعد از dotenv.config()) اضافه کردن import و راه‌اندازی TelegramBotService. 5. پیاده‌سازی MessageHandler برای دستور /start که لیست دروس را با InlineKeyboard نمایش دهد. 6. پیاده‌سازی هندلر برای callback_data درس‌ها که جزئیات درس و فایل‌های ضمیمه را با send_document ارسال کند. 7. پیاده‌سازی هندلر برای دریافت فایل (مستند، تصویر، ویدئو) با اعتبارسنجی نوع فایل (PDF, JPG, PNG, MP4) و حجم (حداکثر 50MB) و نمایش InlineKeyboard برای انتخاب درس مقصد. 8. ذخیره فایل در Firebase Storage و ثبت رکورد در Firestore. 9. بازگرداندن تأییدیه به کاربر.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 5: اتصال ربات تلگرام به بخش 'تمرین با استاد هوش مصنوعی'

**Scope:** ایجاد یک endpoint یا سرویس داخلی که ربات تلگرام را به API چت هوش مصنوعی موجود در پروژه متصل کند. شامل ارسال پیام‌های کاربر از تلگرام به هوش مصنوعی و دریافت پاسخ. خارج از این مرحله: مدیریت موضوعات، انتخاب موضوع، یا پیام صوتی. نکته حیاتی: باید از API موجود پروژه استفاده شود و session مدیریت شود.
**Key terms:** تمرین با استاد هوش مصنوعی, API چت, session, ارسال پیام, دریافت پاسخ

**بخش مربوط از متن کاربر:**
```
اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
اتصال ربات تلگرام به API چت هوش مصنوعی موجود

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:56-114` — `app.post('/api/gemini/chat')` — این endpoint اصلی چت هوش مصنوعی است که ربات تلگرام باید از آن استفاده کند. سرویس تلگرام باید payload مشابهی بسازد و این endpoint را به صورت داخلی (fetch) فراخوانی کند.
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
  
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
  
      // Clean payload - preserve audio data if includeAudio flag is set
      if (payload.contents) {
        payload.contents = payload.contents.map(content => ({
          role: content.role,
          parts: content.parts.map(part => {
            // Keep inline_data (audio) if present
            if (part.inline_data && includeAudio) {
              return { inline_data: part.inline_data };
            }
            // Keep text if present
            if (part.text !== undefined) {
              return { text: part.text };
            }
            return null;
          }).filter(p => p !== null)
        }));
      }
  
      const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${GEMINI_API_KEY}`;
  ```
- `backend/server.js:44-45` — `WebSocketServer` — محل مناسب برای import و راه‌اندازی سرویس تلگرام. بعد از این خط، سرویس telegramBot.js باید import و مقداردهی اولیه شود.
  ```jsx
  // Create WebSocket server
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/.env.example:1-8` — `Environment variables`
  ```
  #
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست اتصال ربات تلگرام به بخش 'تمرین با استاد هوش مصنوعی' را دارد. این تسک شامل ایجاد یک endpoint یا سرویس داخلی است که ربات تلگرام را به API چت هوش مصنوعی موجود در پروژه متصل کند. این اتصال باید شامل ارسال پیام‌های کاربر از تلگرام به هوش مصنوعی و دریافت پاسخ باشد. کاربر تأکید کرده که خارج از این مرحله مواردی مانند مدیریت موضوعات، انتخاب موضوع، یا پیام صوتی قرار دارند. نکته حیاتی که کاربر ذکر کرده: باید از API موجود پروژه استفاده شود و session مدیریت شود. بخش مربوط از درخواست اصلی کاربر: 'اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم'. کلیدواژه‌های کاربر: 'تمرین با استاد هوش مصنوعی', 'API چت', 'session', 'ارسال پیام', 'دریافت پاسخ'. در کد فعلی پروژه، endpoint `/api/gemini/chat` در فایل `backend/server.js` خطوط 56-114 موجود است که از مدل `gemini-2.0-flash` استفاده می‌کند و پیام‌های متنی و صوتی را پشتیبانی می‌کند. همچنین WebSocket در خط 45 با مسیر `/ws/live` راه‌اندازی شده است. پروژه از Firebase برای احراز هویت استفاده می‌کند (طبق `frontend/index.html` خطوط 11-18). هیچ سرویس تلگرام یا مدیریت session در کد فعلی وجود ندارد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک فایل جدید `backend/services/telegramBot.js` که با استفاده از کتابخانه `node-telegram-bot-api` (نیاز به نصب) یک ربات تلگرام راه‌اندازی کند. 2. در این سرویس، یک مدیریت session ساده مبتنی بر chatId تلگرام ایجاد شود که state کاربر (موضوع جاری، تاریخچه پیام‌ها) را نگه دارد. 3. هنگام دریافت پیام متنی از کاربر در تلگرام، سرویس باید درخواست را به endpoint موجود `/api/gemini/chat` در `backend/server.js` خط 56 ارسال کند (با استفاده از fetch داخلی) و پاسخ را به کاربر برگرداند. 4. برای مدیریت session، می‌توان از یک Map ساده در حافظه یا Firebase Realtime Database (که قبلاً در پروژه استفاده شده) استفاده کرد. 5. endpoint جدید `/api/telegram/webhook` در `backend/server.js` برای دریافت webhook از تلگرام (در صورت استفاده از webhook به جای polling) اضافه شود. 6. متغیر محیطی `TELEGRAM_BOT_TOKEN` به `backend/.env.example` اضافه شود. 7. فایل `backend/server.js` باید سرویس تلگرام را import و راه‌اندازی کند (مثلاً بعد از خط 45).

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 6: مدیریت و انتخاب موضوعات تمرین از طریق تلگرام

**Scope:** طراحی و پیاده‌سازی منوی انتخاب موضوع برای تمرین با هوش مصنوعی از طریق دکمه‌های شیشه‌ای تلگرام. شامل دریافت لیست موضوعات از API، نمایش آن‌ها، و ذخیره انتخاب کاربر. خارج از این مرحله: خود مکالمه با هوش مصنوعی. نکته حیاتی: باید از InlineKeyboard با callback_data استفاده شود.
**Key terms:** موضوعات تمرین, InlineKeyboard, callback_data, انتخاب موضوع, API موضوعات

**بخش مربوط از متن کاربر:**
```
بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن منوی انتخاب موضوعات تمرین از طریق تلگرام با InlineKeyboard

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:38-48` — `app initialization and middleware` — محل فعلی middlewareها. بعد از خط 48 باید import و استفاده از routes/topics.js اضافه شود. همچنین باید WebSocket server برای ارتباط با ربات تلگرام در نظر گرفته شود.
  ```jsx
  const app = express();
  const PORT = process.env.PORT || 3001;
  
  // Create HTTP server
  const server = createServer(app);
  
  // Create WebSocket server
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  
  app.use(cors());
  app.use(express.json({ limit: '10mb' }));
  ```
- `backend/server.js:166-169` — `Health check endpoint` — الگوی endpoint ساده برای مرجع. endpointهای موضوعات باید با الگوی مشابه پیاده‌سازی شوند.
  ```jsx
  // Health check
  app.get('/api/health', (req, res) => {
    res.json({ status: 'ok', timestamp: new Date().toISOString() });
  });
  ```
- `backend/.env.example:1-8` — `Environment variables template` — محل اضافه کردن متغیرهای محیطی جدید: TELEGRAM_BOT_TOKEN و TOPICS_FILE_PATH
  ```
  # Gemini API Key - Get from https://aistudio.google.com/apikey
  GEMINI_API_KEY=your_gemini_api_key_here
  
  # Port (optional, default 3001)
  PORT=3001
  
  # Firebase config (optional - for data persistence)
  # VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/package.json` (سطر 10) — نیاز به اضافه کردن وابستگی node-telegram-bot-api به dependencies
- `frontend/src`

## 🔍 Context و وضعیت فعلی
کاربر درخواست طراحی و پیاده‌سازی منوی انتخاب موضوع برای تمرین با هوش مصنوعی از طریق دکمه‌های شیشه‌ای تلگرام را دارد. این شامل دریافت لیست موضوعات از API، نمایش آن‌ها در قالب InlineKeyboard با callback_data، و ذخیره انتخاب کاربر است. نکته حیاتی: باید از InlineKeyboard با callback_data استفاده شود. بخش مربوط از درخواست اصلی کاربر: 'بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم'. کلیدواژه‌ها: موضوعات تمرین, InlineKeyboard, callback_data, انتخاب موضوع, API موضوعات. خارج از این مرحله: خود مکالمه با هوش مصنوعی. پروژه فعلی (lebanese-dialect-learning) یک اپلیکیشن آموزش لهجه لبنانی با هوش مصنوعی است که از backend Express.js (backend/server.js) و frontend React (frontend/src/App.jsx) تشکیل شده. در commit‌های اخیر (bf99d20, cc1061c, d34ec7c, 9f58129) کاربر درخواست 'میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار' را داشته که نشان‌دهنده تمایل به یکپارچه‌سازی کامل تلگرام است. backend/server.js شامل WebSocket (خط 45: new WebSocketServer({ server, path: '/ws/live' })) و APIهای مختلف Gemini (خطوط 56-240) است. backend/.env.example شامل متغیرهای محیطی GEMINI_API_KEY و VITE_FIREBASE_CONFIG است. frontend/index.html شامل Firebase config (خطوط 11-18) و Inspector Bridge Script (خطوط 32-201) است. backend/package.json وابستگی‌های اصلی شامل express, cors, dotenv, ws, multer, fluent-ffmpeg, pdf-parse را نشان می‌دهد. frontend/package.json وابستگی‌های firebase, lucide-react, react, react-dom را نشان می‌دهد. برای پیاده‌سازی این قابلیت، نیاز به ایجاد یک سرویس تلگرام جدید (backend/services/telegramService.js) و یک endpoint API برای مدیریت موضوعات (backend/routes/topics.js) است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل backend/services/telegramService.js: پیاده‌سازی سرویس پایه تلگرام با استفاده از کتابخانه node-telegram-bot-api. این سرویس باید متدهایی برای ارسال پیام با InlineKeyboard و پردازش callback_data داشته باشد. 2. ایجاد فایل backend/routes/topics.js: پیاده‌سازی API برای مدیریت موضوعات تمرین. شامل endpoint GET /api/topics برای دریافت لیست موضوعات و POST /api/topics/select برای ذخیره انتخاب کاربر. 3. ایجاد فایل backend/data/topics.json: فایل JSON شامل لیست موضوعات تمرین (مثلاً 'سلام و احوالپرسی', 'خرید و معامله', 'غذا و رستوران', 'سفر و حمل و نقل', 'خانواده و روابط'). 4. به‌روزرسانی backend/server.js: اضافه کردن import و استفاده از routes/topics.js در خطوط بعد از خط 48 (app.use(express.json)). 5. ایجاد فایل backend/telegramBot.js: فایل اصلی ربات تلگرام که از telegramService.js استفاده می‌کند. شامل تابع handleStart برای ارسال منوی اصلی با دکمه 'انتخاب موضوع تمرین' و تابع handleTopicSelection برای نمایش InlineKeyboard با لیست موضوعات. 6. پیاده‌سازی callback_data: هر دکمه InlineKeyboard باید callback_data منحصربه‌فرد داشته باشد (مثلاً 'topic_greetings', 'topic_shopping', 'topic_food'). 7. ذخیره انتخاب کاربر: پس از دریافت callback_data، انتخاب کاربر در یک ساختار داده موقت (مثلاً Map یا Redis) ذخیره شود. 8. نصب وابستگی: npm install node-telegram-bot-api در پوشه backend. 9. به‌روزرسانی backend/.env.example: اضافه کردن TELEGRAM_BOT_TOKEN و TOPICS_FILE_PATH. 10. ایجاد تست‌های پایه برای سرویس تلگرام و API موضوعات.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 7: پیاده‌سازی ارسال پیام صوتی از تلگرام به هوش مصنوعی

**Scope:** دریافت فایل صوتی از تلگرام (voice message)، ارسال آن به سرویس تحلیل و استخراج متن موجود در پروژه، و سپس ارسال متن استخراج‌شده به هوش مصنوعی. شامل مدیریت فرمت صوتی و خطاهای تبدیل. خارج از این مرحله: دریافت پاسخ صوتی از هوش مصنوعی. نکته حیاتی: باید از سرویس ASR (تشخیص گفتار) موجود در پروژه استفاده شود.
**Key terms:** voice message, فایل صوتی, ASR, تشخیص گفتار, استخراج متن, send_voice

**بخش مربوط از متن کاربر:**
```
از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی ارسال پیام صوتی تلگرام به سرویس ASR پروژه

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:790-800` — `audio file processing block` — این بلوک پردازش فایل صوتی موجود است — باید برای voice message تلگرام复用 شود. تابع uploadToGeminiFileAPI (خط 297) و analyzeWithGeminiFileAPI (خط 426) مستقیماً قابل استفاده هستند.
  ```jsx
  } else if (mimeType.startsWith('audio/')) {
            console.log(`Audio file: ${fileName}, size: ${fileSizeMB.toFixed(2)} MB`);
  
            try {
              let result;
  
              if (isLargeFile) {
                // For large audio files, use Gemini File API with streaming
                console.log(`Using File API for large audio: ${fileName}`);
                const uploadedFileName = await uploadToGeminiFileAPI(filePath, mimeType, fileName, fileSize);
  ```
- `backend/server.js:273-294` — `analyzeWithGemini function` — این تابع برای تحلیل متن/صوت با Gemini استفاده می‌شود — بعد از استخراج متن از voice message، می‌توان از این تابع برای ارسال به هوش مصنوعی استفاده کرد.
  ```jsx
  async function analyzeWithGemini(parts, systemPrompt) {
    const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${GEMINI_API_KEY}`;
  
    const payload = {
      contents: [{ role: 'user', parts }],
      systemInstruction: { parts: [{ text: systemPrompt }] }
    };
  
    const response = await fetch(apiUrl, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(payload)
    });
  ```
- `backend/server.js:698-715` — `POST /api/analyze-files endpoint`
  ```jsx
  app.post('/api/analyze-files', upload.array('files', 10), handleMulterError, async (req, res) => {
    console.log('Received file analysis request');
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی ارسال پیام صوتی از تلگرام (voice message) به هوش مصنوعی را دارد. فرآیند شامل: دریافت فایل صوتی از تلگرام (voice message)، ارسال آن به سرویس تحلیل و استخراج متن موجود در پروژه (که در backend/server.js از خط 698 endpoint /api/analyze-files با multer برای آپلود فایل و تحلیل با Gemini پیاده‌سازی شده)، و سپس ارسال متن استخراج‌شده به هوش مصنوعی. کاربر تأکید کرده: 'از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه' — یعنی باید از سرویس ASR (تشخیص گفتار) موجود در پروژه استفاده شود. کلیدواژه‌های کاربر: voice message, فایل صوتی, ASR, تشخیص گفتار, استخراج متن, send_voice. در کد فعلی backend/server.js، تابع analyzeWithGemini (خط 273) و uploadToGeminiFileAPI (خط 297) برای فایل‌های صوتی (mimeType.startsWith('audio/') در خط 790) پیاده‌سازی شده‌اند. همچنین endpoint /api/analyze-files (خط 698) فایل‌های صوتی را با Gemini File API پردازش می‌کند. خارج از این مرحله: دریافت پاسخ صوتی از هوش مصنوعی. نکته حیاتی: باید از سرویس ASR موجود در پروژه استفاده شود — یعنی همان pipeline تحلیل صوتی که در backend/server.js خطوط 790-800 برای فایل‌های صوتی پیاده‌سازی شده.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک endpoint جدید در backend/server.js (مثلاً POST /api/telegram/voice) که: (الف) فایل صوتی دریافتی از تلگرام (voice message) را با multer دریافت کند (همان middleware upload.array در خط 698). (ب) فایل را به تابع uploadToGeminiFileAPI (خط 297) بدهد تا در Gemini File API آپلود شود. (ج) سپس با analyzeWithGeminiFileAPI (خط 426) متن استخراج‌شده را دریافت کند. (د) متن استخراج‌شده را به endpoint /api/gemini/chat (خط 56) بفرستد تا پاسخ هوش مصنوعی دریافت شود. (ه) پاسخ نهایی را به تلگرام برگرداند. 2. اضافه کردن یک تابع کمکی handleTelegramVoice در backend/server.js که: (الف) voice message را از Telegram Bot API دریافت کند (با استفاده از کتابخانه‌ای مانند node-telegram-bot-api). (ب) فایل صوتی را دانلود کند. (ج) به pipeline تحلیل صوتی موجود بدهد. 3. نصب کتابخانه node-telegram-bot-api در backend/package.json. 4. افزودن متغیر محیطی TELEGRAM_BOT_TOKEN به backend/.env.example. 5. پیاده‌سازی Webhook برای Telegram Bot در backend/server.js (مثلاً در خطوط بعد از خط 240).

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 8: پیاده‌سازی دریافت پاسخ صوتی از هوش مصنوعی در تلگرام

**Scope:** دریافت پاسخ متنی از هوش مصنوعی، تبدیل آن به صوت (TTS) با استفاده از سرویس موجود در پروژه، و ارسال فایل صوتی به تلگرام. شامل مدیریت کیفیت صوت و خطاهای تبدیل. خارج از این مرحله: ارسال متن پاسخ. نکته حیاتی: باید از سرویس TTS موجود در پروژه استفاده شود و فایل صوتی با فرمت OGG یا MP3 ارسال شود.
**Key terms:** TTS, تبدیل متن به گفتار, send_audio, پاسخ صوتی, فرمت صوتی

**بخش مربوط از متن کاربر:**
```
پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن پاسخ صوتی TTS به تلگرام

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:117-164` — `app.post('/api/gemini/tts')` — این endpoint TTS موجود است که باید برای تبدیل متن به صوت در تلگرام استفاده شود. خروجی base64 audioData با mimeType است.
  ```jsx
  app.post('/api/gemini/tts', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const { prompt, voice = 'Kore' } = req.body;
      const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-preview-tts:generateContent?key=${GEMINI_API_KEY}`;
      const payload = {
        contents: [{ parts: [{ text: prompt }] }],
        generationConfig: {
          responseModalities: ["AUDIO"],
          speechConfig: { 
            voiceConfig: { 
              prebuiltVoiceConfig: { voiceName: voice } 
            } 
          }
        }
      };
      const response = await fetch(apiUrl, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(payload)
      });
      if (!response.ok) {
        const errorData = await response.text();
        console.error('TTS API error:', errorData);
        return res.status(response.status).json({ error: errorData });
      }
      const result = await response.json();
      const part = result?.candidates?.[0]?.content?.parts?.[0];
      const audioData = part?.inlineData?.data;
      const mimeType = part?.inlineData?.mimeType;
      if (audioData && mimeType?.startsWith("audio/")) {
        res.json({ audioData, mimeType });
      } else {
        res.status(500).json({ error: 'Invalid audio data' });
      }
    } catch (error) {
      console.error('TTS API error:', error);
      res.status(500).json({ error: error.message });
    }
  });
  ```
- `backend/server.js:45-45` — `WebSocketServer` — WebSocket موجود برای ارتباط real-time با فرانت‌اند. پاسخ صوتی باید از طریق همین کانال یا کانال مجزای تلگرام ارسال شود.
  ```jsx
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `frontend/src/App.jsx:1-100` — `App component` — کامپوننت اصلی فرانت‌اند که چت با هوش مصنوعی را مدیریت می‌کند. باید با سیستم تلگرام یکپارچه شود.
  ```jsx
  فایل deep-read نشده — مجری باید مسیر را خود تأیید کند
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/server.js` (سطر 117) — فایل اصلی backend که شامل endpoint TTS (خط 117) و WebSocket (خط 45) و تمام APIهای موجود است. تغییرات اصلی در این فایل اعمال می‌شود.
- `frontend/src/App`

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی دریافت پاسخ صوتی از هوش مصنوعی در تلگرام را دارد. متن خام کاربر: «پیاده‌سازی دریافت پاسخ صوتی از هوش مصنوعی در تلگرام. دریافت پاسخ متنی از هوش مصنوعی، تبدیل آن به صوت (TTS) با استفاده از سرویس موجود در پروژه، و ارسال فایل صوتی به تلگرام. شامل مدیریت کیفیت صوت و خطاهای تبدیل. خارج از این مرحله: ارسال متن پاسخ. نکته حیاتی: باید از سرویس TTS موجود در پروژه استفاده شود و فایل صوتی با فرمت OGG یا MP3 ارسال شود. پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد». کلیدواژه‌ها: TTS, تبدیل متن به گفتار, send_audio, پاسخ صوتی, فرمت صوتی. در کد فعلی پروژه، endpoint TTS در backend/server.js خطوط 117-164 با مسیر /api/gemini/tts وجود دارد که از مدل gemini-2.5-flash-preview-tts استفاده می‌کند و خروجی base64 audioData با mimeType برمی‌گرداند. این سرویس باید برای ارسال پاسخ صوتی به تلگرام استفاده شود. همچنین در commit‌های اخیر (bf99d20, cc1061c, d34ec7c, 9f58129) اشاره به «میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار» شده که نشان‌دهنده وجود زیرساخت اولیه تلگرام است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک تابع جدید در backend/server.js یا فایل مجزای backend/services/telegram-tts.js که: الف) پاسخ متنی هوش مصنوعی را دریافت کند. ب) با استفاده از endpoint موجود /api/gemini/tts (خطوط 117-164) آن را به صوت تبدیل کند. ج) فایل صوتی حاصل را با فرمت OGG یا MP3 ذخیره کند. د) از طریق API تلگرام (sendAudio یا sendVoice) فایل صوتی را ارسال کند. 2. مدیریت خطاهای تبدیل TTS شامل: خطای API key (خط 118-120)، خطای پاسخ نامعتبر (خط 157-159)، خطای شبکه (خط 160-163). 3. مدیریت کیفیت صوت: استفاده از پارامتر voice='Kore' پیش‌فرض (خط 123) و امکان تنظیم voice. 4. اطمینان از اینکه فایل صوتی با فرمت OGG یا MP3 ارسال شود (تبدیل base64 به فایل). 5. یکپارچه‌سازی با سیستم چت موجود در frontend/src/App.jsx و WebSocket در backend/server.js خط 45 (wss در مسیر /ws/live).

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 9: یکپارچه‌سازی و هماهنگ‌سازی کامل چت تلگرام با چت وب

**Scope:** اطمینان از اینکه تمام پیام‌ها و پاسخ‌ها (متن، صوت، فایل) بین تلگرام و چت وب هوش مصنوعی همگام‌سازی می‌شوند. شامل ذخیره تاریخچه چت در دیتابیس مشترک، نمایش پیام‌های تلگرام در وب و بالعکس. خارج از این مرحله: قابلیت‌های جدید. نکته حیاتی: باید از یک دیتابیس مشترک برای تاریخچه استفاده شود و timestampها هماهنگ باشند.
**Key terms:** همگام‌سازی, تاریخچه مشترک, دیتابیس چت, real-time, WebSocket

**بخش مربوط از متن کاربر:**
```
هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد
```

## 🎯 هدف (خلاصه ساختاریافته)
یکپارچه‌سازی چت تلگرام با چت وب و همگام‌سازی تاریخچه

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:45-45` — `WebSocketServer` — WebSocket سرور فعلی که باید برای broadcast پیام‌های همگام‌سازی شده بین تلگرام و وب استفاده شود.
  ```jsx
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/server.js:56-114` — `app.post('/api/gemini/chat')` — Endpoint چت Gemini که باید پس از دریافت پاسخ، آن را در دیتابیس مشترک ذخیره کند و از طریق WebSocket broadcast کند.
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => { ... });
  ```
- `frontend/index.html:11-18` — `__firebase_config` — Firebase config موجود در frontend که می‌توان از آن برای دیتابیس مشترک (Firestore) استفاده کرد.
  ```
  var __firebase_config = JSON.stringify({ apiKey: "AIzaSyAMFtV0zIMKKe0bIr68Z1wTORQ1jQpsv9Q", authDomain: "labaneseapp.firebaseapp.com", projectId: "labaneseapp", storageBucket: "labaneseapp.firebasestorage.app", messagingSenderId: "951874597795", appId: "1:951874597795:web:00745327993adad760a016" });
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/package.json` (سطر 1) — نیاز به اضافه کردن وابستگی‌های node-telegram-bot-api و firebase-admin
- `frontend/src/App.jsx` (سطر 1) — کامپوننت اصلی React که باید WebSocket client برای دریافت پیام‌های همگام‌سازی شده اضافه کند
- `backend/.env.example` (سطر 1) — نیاز به اضافه کردن متغیرهای محیطی TELEGRAM_BOT_TOKEN و FIREBASE_SERVICE_ACCOUNT
- `frontend/package.json` (سطر 1) — نیاز به اضافه کردن وابستگی firebase در frontend (اگر قبلاً نصب نیست)

## 🌐 نقشهٔ وابستگی‌ها
این تسک به فایل‌های backend/server.js (WebSocket و endpoints چت)، frontend/index.html (Firebase config)، backend/package.json (وابستگی‌ها)، frontend/src/App.jsx (UI چت وب) و backend/.env.example (متغیرهای محیطی) وابسته است. فایل‌های جدید backend/services/telegramBot.js و backend/services/chatDatabase.js ایجاد می‌شوند. WebSocket فعلی (wss در خط ۴۵ server.js) برای broadcast استفاده می‌شود. دیتابیس مشترک از Firebase Firestore (با config موجود در frontend/index.html خطوط

## 🔍 Context و وضعیت فعلی
کاربر درخواست یکپارچه‌سازی و هماهنگ‌سازی کامل چت تلگرام با چت وب هوش مصنوعی را دارد. متن خام کاربر: «یکپارچه‌سازی و هماهنگ‌سازی کامل چت تلگرام با چت وب - اطمینان از اینکه تمام پیام‌ها و پاسخ‌ها (متن، صوت، فایل) بین تلگرام و چت وب هوش مصنوعی همگام‌سازی می‌شوند. شامل ذخیره تاریخچه چت در دیتابیس مشترک، نمایش پیام‌های تلگرام در وب و بالعکس. خارج از این مرحله: قابلیت‌های جدید. نکته حیاتی: باید از یک دیتابیس مشترک برای تاریخچه استفاده شود و timestampها هماهنگ باشند. --- بخش مربوط از درخواست اصلی کاربر --- هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد --- کلیدواژه‌ها: همگام‌سازی, تاریخچه مشترک, دیتابیس چت, real-time, WebSocket». در کد فعلی پروژه (backend/server.js) یک WebSocket سرور در خط ۴۵ با مسیر '/ws/live' ایجاد شده است. همچنین یک endpoint چت Gemini در خط ۵۶ (POST /api/gemini/chat) و یک endpoint TTS در خط ۱۱۷ (POST /api/gemini/tts) وجود دارد. frontend/index.html شامل Firebase config (خطوط ۱۱-۱۸) و یک Inspector Bridge برای ارسال رویدادها به parent است. backend/package-lock.json نشان می‌دهد که وابستگی‌های فعلی شامل express, cors, dotenv, ws هستند. frontend/package-lock.json شامل react, firebase, lucide-react است. backend/.env.example شامل GEMINI_API_KEY و Firebase config است. هیچ فایل bot تلگرام یا دیتابیس مشترک در ساختار پروژه وجود ندارد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ۱. ایجاد یک فایل جدید backend/services/telegramBot.js که با استفاده از کتابخانه node-telegram-bot-api یک بات تلگرام راه‌اندازی کند. ۲. ایجاد یک فایل جدید backend/services/chatDatabase.js که با استفاده از Firebase Firestore (که قبلاً در frontend/index.html کانفیگ شده) یک دیتابیس مشترک برای ذخیره تاریخچه چت ایجاد کند. ۳. در backend/server.js، import کردن telegramBot.js و chatDatabase.js و اتصال WebSocket به رویدادهای دیتابیس برای real-time sync. ۴. اصلاح endpoint /api/gemini/chat (خط ۵۶) به طوری که پس از دریافت پاسخ از Gemini، آن را در دیتابیس مشترک ذخیره کند و از طریق WebSocket به کلاینت‌های متصل (وب و تلگرام) broadcast کند. ۵. ایجاد یک endpoint جدید POST /api/telegram/webhook برای دریافت پیام‌های تلگرام و ذخیره در دیتابیس مشترک. ۶. در frontend/src/App.jsx، اضافه کردن یک WebSocket client که به '/ws/live' متصل شود و پیام‌های جدید را از دیتابیس مشترک دریافت کند. ۷. اطمینان از هماهنگی timestampها با استفاده از timestamp سرور (new Date().toISOString()) در تمام عملیات‌های دیتابیس.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: high
- تخمین زمان: large

---

## ✅ معیارهای پذیرش کلی (همهٔ مراحل)
- [ ] همهٔ مراحل بالا با موفقیت پیاده‌سازی شده‌اند
- [ ] تست‌های موجود pass می‌شوند
- [ ] هیچ regression رخ نداده

## Acceptance Criteria

1. فایل backend/services/telegramService.js ایجاد شده و تابع sendVoiceMessage را export کند _(verify: static)_

## Task Steps

### Step 1: راه‌اندازی ربات تلگرام و اتصال اولیه به سیستم
**Status:** `pending` (0%)
**Scope:** ایجاد یک ربات تلگرام جدید از طریق BotFather، دریافت توکن API، و راه‌اندازی یک webhook یا polling برای دریافت پیام‌ها. این مرحله شامل اتصال پایه‌ای ربات به سرور پروژه است. خارج از این مرحله: پیاده‌سازی منطق دوطرفه، مدیریت محتوا، یا اتصال به هوش مصنوعی. نکته حیاتی: باید از کتابخانه python-telegram-bot یا aiogram استفاده شود و webhook روی آدرس HTTPS معتبر تنظیم شود.
**Excerpt:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

### Step 2: پیاده‌سازی ارسال نوتیفیکیشن از سیستم به تلگرام
**Status:** `pending` (0%)
**Scope:** ایجاد یک سرویس داخلی که رویدادهای مشخص سیستم (مثلاً ثبت سفارش، خطا، به‌روزرسانی) را به پیام تلگرام تبدیل کرده و به کاربر ارسال کند. شامل تعریف فرمت پیام‌ها، مدیریت خطا در ارسال، و صف‌بندی پیام‌ها. خارج از این مرحله: دریافت پیام از تلگرام یا کنترل سیستم. نکته حیاتی: باید از توکن ربات مرحله قبل استفاده شود و پیام‌ها به چت‌آیدی مشخصی ارسال شوند.
**Excerpt:**
```
علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته
```

### Step 3: پیاده‌سازی دریافت دستورات از تلگرام و اجرای عملیات روی سیستم
**Status:** `pending` (0%)
**Scope:** تحلیل پیام‌های دریافتی از تلگرام، شناسایی دستورات (مثلاً /status، /restart، /report)، و اجرای عملیات متناظر روی سیستم. شامل تعریف لیست دستورات، اعتبارسنجی کاربر، و بازگرداندن نتیجه به تلگرام. خارج از این مرحله: مدیریت محتوای دروس یا اتصال به هوش مصنوعی. نکته حیاتی: باید احراز هویت کاربر تلگرام با کاربر سیستم تطبیق داده شود.
**Excerpt:**
```
بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

### Step 4: ایجاد قابلیت مشاهده محتوای دروس از طریق تلگرام
**Status:** `pending` (0%)
**Scope:** طراحی و پیاده‌سازی دستورات تلگرامی برای مشاهده لیست دروس، مشاهده جزئیات هر درس، و مشاهده فایل‌های ضمیمه (PDF، ویدئو، تصویر). شامل ارسال فایل‌ها از طریق تلگرام و مدیریت لینک‌ها. خارج از این مرحله: آپلود محتوا یا ویرایش. نکته حیاتی: باید از API دیتابیس دروس پروژه استفاده شود و فایل‌ها به صورت مستقیم یا لینک ارسال شوند.
— [merged] پیاده‌سازی دریافت فایل از تلگرام (مستند، تصویر، ویدئو) و ذخیره آن در سیستم مدیریت محتوای دروس. شامل اعتبارسنجی نوع و حجم فایل، انتخاب درس مقصد، و بازگرداندن تأییدیه. خارج از این مرحله: ویرایش یا حذف محتوا. نکته حیاتی: باید از طریق دکمه‌های شیشه‌ای (InlineKeyboard) درس مقصد انتخاب شود.
**Excerpt:**
```
بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم
```

### Step 5: اتصال ربات تلگرام به بخش 'تمرین با استاد هوش مصنوعی'
**Status:** `pending` (0%)
**Scope:** ایجاد یک endpoint یا سرویس داخلی که ربات تلگرام را به API چت هوش مصنوعی موجود در پروژه متصل کند. شامل ارسال پیام‌های کاربر از تلگرام به هوش مصنوعی و دریافت پاسخ. خارج از این مرحله: مدیریت موضوعات، انتخاب موضوع، یا پیام صوتی. نکته حیاتی: باید از API موجود پروژه استفاده شود و session مدیریت شود.
**Excerpt:**
```
اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم
```

### Step 6: مدیریت و انتخاب موضوعات تمرین از طریق تلگرام
**Status:** `pending` (0%)
**Scope:** طراحی و پیاده‌سازی منوی انتخاب موضوع برای تمرین با هوش مصنوعی از طریق دکمه‌های شیشه‌ای تلگرام. شامل دریافت لیست موضوعات از API، نمایش آن‌ها، و ذخیره انتخاب کاربر. خارج از این مرحله: خود مکالمه با هوش مصنوعی. نکته حیاتی: باید از InlineKeyboard با callback_data استفاده شود.
**Excerpt:**
```
بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم
```

### Step 7: پیاده‌سازی ارسال پیام صوتی از تلگرام به هوش مصنوعی
**Status:** `pending` (0%)
**Scope:** دریافت فایل صوتی از تلگرام (voice message)، ارسال آن به سرویس تحلیل و استخراج متن موجود در پروژه، و سپس ارسال متن استخراج‌شده به هوش مصنوعی. شامل مدیریت فرمت صوتی و خطاهای تبدیل. خارج از این مرحله: دریافت پاسخ صوتی از هوش مصنوعی. نکته حیاتی: باید از سرویس ASR (تشخیص گفتار) موجود در پروژه استفاده شود.
**Excerpt:**
```
از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه
```

### Step 8: پیاده‌سازی دریافت پاسخ صوتی از هوش مصنوعی در تلگرام
**Status:** `pending` (0%)
**Scope:** دریافت پاسخ متنی از هوش مصنوعی، تبدیل آن به صوت (TTS) با استفاده از سرویس موجود در پروژه، و ارسال فایل صوتی به تلگرام. شامل مدیریت کیفیت صوت و خطاهای تبدیل. خارج از این مرحله: ارسال متن پاسخ. نکته حیاتی: باید از سرویس TTS موجود در پروژه استفاده شود و فایل صوتی با فرمت OGG یا MP3 ارسال شود.
**Excerpt:**
```
پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد
```

### Step 9: یکپارچه‌سازی و هماهنگ‌سازی کامل چت تلگرام با چت وب
**Status:** `pending` (0%)
**Scope:** اطمینان از اینکه تمام پیام‌ها و پاسخ‌ها (متن، صوت، فایل) بین تلگرام و چت وب هوش مصنوعی همگام‌سازی می‌شوند. شامل ذخیره تاریخچه چت در دیتابیس مشترک، نمایش پیام‌های تلگرام در وب و بالعکس. خارج از این مرحله: قابلیت‌های جدید. نکته حیاتی: باید از یک دیتابیس مشترک برای تاریخچه استفاده شود و timestampها هماهنگ باشند.
**Excerpt:**
```
هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد
```
