---
task_id: dc721af2-f84f-424d-a491-91e8ab3624c0
title: میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار (6 مرحله)
type: other
priority: medium
execution_priority: 3000
status: pending
external_status: pending
verification_status: pending
watched_id: 6b04f8bd-b562-4fb9-9cd9-0c27458ced59
project: mahdighandi1989/language
created_at: '2026-05-12T11:13:22.960230+00:00'
updated_at: '2026-06-01T21:05:06.160240+00:00'
target_files:
- backend/server.js
- backend/.env.example
- backend/package.json
- frontend/index.html
---

# میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار (6 مرحله)

## Raw Idea

میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم و از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

📖 **خواندن کامل + اجرای مو-به-مو (بسیار مهم):**

این پرامپت — از این یادداشت تا انتها — یک سند واحد است که هر بخشش
حاوی الزام یا context منحصربه‌فرد است. خواندن سطحی یا skim کردن **ممنوع**
است.

- پرامپت را **سطر به سطر** بخوان، نه head/tail/فقط-بخش-اصلی.
- اگر بخشی به‌نظر طولانی یا تکراری آمد، **حتماً** بخوان — تفاوت‌های
  ریز ممکن است در آن جا اساسی باشند.
- هر جمله، URL، نام فایل، نام تابع، یا مقدار عددی که در پرامپت آمده،
  دقیقاً همان است که کاربر می‌خواهد — تغییرش نده، رندش نکن، خلاصه‌اش
  نکن.
- اگر پرامپت چندین درخواست/مرحله/زیرتسک دارد، **همه** را پیاده کن. حتی
  یکی را نه به‌عنوان "خارج از scope" حذف کن.

❌ ممنوعات صریح:
- خلاصه‌سازی متن کاربر در commit message یا response
- "این بخش اصلی نیست، رد می‌کنم"
- "کاربر احتمالاً منظورش این بود..." — منظورش همان است که نوشته
- "این URL/نام به نظر قدیمی است، آپدیتش کردم" — تغییر بدون درخواست ممنوع
- پیاده‌سازی فقط بخشی از پرامپت و تظاهر به کامل بودن
- "همه آیتم‌های لیست A را بررسی کردم، B و C مشابه بودند" — نه؛
  هرکدام را جداگانه

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

🔗 **وابستگی‌ها و همگام‌سازی (بسیار حیاتی — هرگز skip نکن):**

این بخش از همهٔ بخش‌های دیگرِ این یادداشت **مهم‌تر** است. اگر نقض شود،
نتیجهٔ کار ممکن است مشروع به‌نظر برسد ولی در عمل بخش‌های دیگر سیستم را عقب
بیندازد، broken reference تولید کند، یا منجر به data corruption شود.

پیش از و حین تغییر، تمام وابستگی‌ها را در **چهار جهت** به‌طور **کامل و
بدون هیچ خلاصه‌سازی** شناسایی و همگام کن:

**۱. وابستگی‌های upstream (این تسک به چه چیزهایی متکی است):**
- چه فایل‌ها، توابع، کلاس‌ها، API endpoint ها، schema های دیتابیس،
  env vars، یا config هایی که این تسک نیاز دارد؟
- آیا قرار است چیزی را ویرایش/حذف کنی که جای دیگر (signature، رفتار،
  return type، side effect) از آن انتظار خاصی می‌رود؟
- اگر dependency جدیدی اضافه می‌کنی، آیا با dependencyهای موجود تداخل
  دارد (نسخه، compat، lock file)؟

**۲. وابستگی‌های downstream (چه چیزهایی به این تسک متکی‌اند):**
- چه فایل‌ها، توابع، تست‌ها، migrations، docs، یا UI component هایی از
  کدی که داری ویرایش/اضافه/حذف می‌کنی **استفاده می‌کنند**؟
- با grep و reference search **همه‌ی** call sites، importها، subclassها،
  reference های مستقیم و غیرمستقیم را پیدا کن — نه فقط چند مورد اصلی.
- خصوصاً برای حذف یا rename: هیچ broken reference نباید باقی بماند.

**۳. وابستگی‌های cross-tier (بسیار مهم — هرگز فقط یک لایه را نبین):**

تسک شما ممکن است از backend، frontend، database، worker، یا هر tier
دیگری شروع شده باشد. ولی تغییرات تقریباً همیشه روی tier های دیگر هم
اثر می‌گذارند. **مستقل از اینکه تسک از کدام tier است**، این چک‌های دو
طرفه را همیشه انجام بده:

🔁 **اگر backend را تغییر دادی** (API، service، model، route):
  → frontend: کدام component/page/hook این endpoint یا data shape را
    مصرف می‌کند؟ type definition، state shape، error handling، loading
    state، form validation، URL routing همگی باید همگام شوند.
  → mobile/SDK/client library (اگر پروژه دارد): همان داستان frontend.
  → database: آیا migration لازم است؟ آیا rollback امن است؟
  → background workers: آیا event producer/consumer ها تحت تأثیرند؟
  → rate limit، auth، CORS، CSP: آیا رفتار جدید پشتیبانی می‌شود؟

🔁 **اگر frontend را تغییر دادی** (component، form، state، route):
  → backend: آیا endpoint جدید/تغییریافته لازم است؟ آیا data shape ای
    که ارسال می‌شود با schema سرور سازگار است؟
  → backend validation: آیا برای ورودی‌های جدید UI کافی است؟
  → permissions/RBAC: آیا feature جدید نیاز به role check جدید دارد؟
  → analytics/tracking: آیا event های جدید باید در backend log شوند؟
  → SEO/SSR: آیا تغییر route نیاز به sitemap/meta tags جدید دارد؟

🔁 **اگر database/migration را تغییر دادی**:
  → backend models (ORM، Pydantic، dataclasses) همگی به‌روزند؟
  → query های raw SQL یا ORM queries با schema جدید سازگارند؟
  → seed data، fixtures، factory functions تست‌ها به‌روزند؟
  → frontend: آیا data shape جدید در UI به‌درستی render می‌شود؟
  → rollback migration نوشته شده و امن است؟

🔁 **اگر API contract یا event schema را تغییر دادی** (REST، GraphQL،
   WebSocket، gRPC، Kafka، …):
  → OpenAPI/GraphQL schema/proto file آپدیت شد؟
  → همه‌ی consumer ها (client، subscriber، webhook، external API
    user) با version جدید سازگارند؟
  → backward compatibility حفظ شده یا migration path روشن است؟
  → versioning header/path اگر breaking change است؟

🔁 **اگر infrastructure یا config را تغییر دادی** (Dockerfile، CI، Render
   config، env، secrets):
  → README setup/installation section به‌روزه؟
  → `.env.example` با env vars جدید آپدیت شد؟
  → deploy script یا CI workflow هم تغییر کرد؟
  → docs/architecture یا diagram های infrastructure به‌روزند؟

⚠️ **هرگز فقط یک tier را تغییر نده و فرض کنی بقیه خودکار همگام می‌شوند.**
   حتی برای تغییرات به‌ظاهر «کوچک»، چک کن.

**۴. وابستگی‌های جانبی (artifacts که همیشه چک شوند):**

تغییرات کد همیشه روی این artifact ها اثر دارند. **همه را** بررسی و
به‌روز کن — مستندات اولویت **بالا** دارد چون فراموش‌شدنی‌ترین است.

  📝 **مستندات** (همیشه چک کن — حتی برای تغییر کوچک کد):
    - README.md (شرح، setup، نمونه‌های استفاده، badge ها)
    - CHANGELOG.md / RELEASE_NOTES.md
    - docs/ folder (architecture، API reference، user guides، runbooks)
    - inline docstrings/کامنت‌های توابع و کلاس‌های تغییریافته
    - OpenAPI/Swagger annotations، JSDoc/TSDoc
    - architecture diagrams (اگر component اضافه/حذف شد)
    - migration guides (اگر breaking change است)

  🌍 **مستندات کاربر**:
    - i18n files و translation keys
    - UI labels، tooltip ها، help text، error messages
    - in-app onboarding (اگر flow جدید است)

  🧪 **تست‌ها**:
    - unit tests (همه‌ی فایل‌های مرتبط — حتی اگر «بی‌ربط» به‌نظر می‌رسد)
    - integration tests
    - e2e tests (Playwright/Cypress/Selenium)
    - snapshot tests (اگر UI تغییر کرد)
    - contract tests (Pact یا مشابه)
    - performance benchmarks (اگر behavior performance-sensitive تغییر کرد)

  🧬 **type definitions و contracts**:
    - .d.ts files
    - Pydantic models، dataclasses
    - Protobuf/Avro/Thrift schemas
    - GraphQL schema definitions
    - JSON Schemas

  🏗 **infrastructure و config**:
    - Dockerfile، docker-compose.yml
    - Kubernetes manifests
    - Render/Vercel/Netlify config
    - GitHub Actions / GitLab CI workflows
    - environment templates (.env.example، .env.sample)
    - feature flags (LaunchDarkly، GrowthBook، config)

  📊 **monitoring و observability**:
    - logging keys (اگر اضافه/حذف شد، log parser ها هم به‌روز شوند)
    - metric names (Prometheus، Datadog)
    - tracing spans
    - alert rules و dashboards
    - error tracking (Sentry rules، groupings)

  🔐 **security**:
    - auth rules (rate limit، CORS، CSP، HSTS)
    - permissions/RBAC config
    - secrets rotation policies
    - audit log events (اگر action جدید اضافه شد)

  💾 **caches و serialization**:
    - cache keys و TTL (اگر data shape یا lifecycle تغییر کرد)
    - serializer formats (Redis، session storage)
    - browser storage (localStorage، IndexedDB schemas)

**قانون مطلق همگام‌سازی:**
- هر چیزی که در (۱)، (۲)، (۳)، یا (۴) شناسایی شد، در **همان workflow
  این تسک** همگام و به‌روز شود. هرگز برای بعد رها نکن.
- اگر یک فایل/تست/docs نسبت به تغییر شما عقب بماند، در بهترین حالت bug،
  در بدترین حالت مشکل امنیتی یا data corruption تولید می‌کند.
- تغییرات همگام‌سازی می‌توانند در commit جداگانه باشند (در همان task)،
  ولی نباید skip شوند یا به «refactor آینده» سپرده شوند.

**هرگز این جمله‌ها قابل قبول نیست:**
- ❌ «بعداً پیداش می‌کنم»
- ❌ «احتمالاً جای دیگه‌ای استفاده نمی‌شه»
- ❌ «این یه refactor جداگانه‌ست — out of scope»
- ❌ «فقط فایل‌های اصلی رو بررسی کردم»
- ❌ «حدس می‌زنم چیزی بهش وابسته نیست»
- ❌ «دامنه‌ی وابستگی‌ها رو خلاصه کردم» — هرگز خلاصه نکن
- ❌ «این task فقط backend است؛ frontend مشکل خودش» — هرگز
- ❌ «این task فقط frontend است؛ backend از قبل کار می‌کند» — هرگز ثابت نکرده
- ❌ «مستندات بعداً به‌روز می‌شن» — همیشه same-task همگام شوند
- ❌ «testها رو نگاه نکردم چون فقط یه تغییر کوچیک بود»

**در commit message یا PR description**، دامنهٔ وابستگی‌های شناسایی‌شده و
همگام‌شده را به‌طور explicit و **per-tier** بنویس. مثال:
```
Dependencies synced:
- upstream: User model schema, auth middleware
- downstream: 3 API endpoints, 5 frontend components, 12 tests
- cross-tier (backend → frontend): UserProfile.tsx, useUser.ts hook,
  api-types.ts (TS definitions)
- cross-tier (backend → infra): .env.example added NEW_AUTH_SCOPES
- side artifacts: OpenAPI spec, README API section, i18n keys for
  new errors, Sentry alert rule for new error code
```
اگر هیچ وابستگی پیدا نکردی در هر کدام از چهار جهت، صریحاً بنویس:
«بررسی شد — هیچ وابستگی upstream / downstream / cross-tier (backend↔
frontend↔db↔infra) / side شناسایی نشد» تا مشخص باشد بررسی **انجام شده**
نه اینکه فراموش شده.

📋 **مدیریت TO-DO برای اقدامات دستی کاربر (همیشه چک کن):**

⚠️ **هشدار بحرانی — قاعدهٔ ضد-فرار:** TO-DO فقط برای کارهایی است که
**واقعاً غیرممکن** برای agent است (نیاز به انسان مطلق)، نه برای کارهایی
که «بزرگ‌اند»، «وقت می‌برند»، یا «نیازمند fixture/setup» هستند. اگر یک
agent در یک سشن بیش از **۲۰٪ از تسک‌ها** را با TO-DO ببندد، یعنی از کار
فرار می‌کند — این الگو در سشن‌های قبلی **مشاهده** شده و الان ممنوع است.

✅ **فقط برای این موارد TO-DO بساز** (لیست بسته — هرچه خارج این لیست
ممنوع است):

  ۱. **Credential/secret که فقط کاربر دارد**:
     - تنظیم API key واقعی در پنل ادمین خارجی (Render، AWS، Stripe، …)
     - تأیید OAuth client روی console آن سرویس
     - paste کردن webhook secret که فقط بعد از ساخت در dashboard ظاهر می‌شود

  ۲. **Account/billing روی سرویس خارجی که کاربر باید عضو شود**:
     - ساخت account جدید روی Stripe/SendGrid/Twilio/Google Cloud
     - تأیید verification شماره یا ID
     - فعال‌سازی subscription پولی

  ۳. **داده/asset خصوصی که فقط کاربر دارد**:
     - آپلود لوگو/تصویر/فونت برند
     - paste کردن داده‌ای که در محل کار کاربر است
     - import داده‌ای که فقط روی device کاربر است

  ۴. **تصمیم سلیقه‌ای/حقوقی/کسب‌وکار**:
     - انتخاب رنگ‌بندی نهایی یا تم
     - متن دقیق Terms of Service / Privacy Policy
     - تعرفهٔ قیمت‌گذاری
     - نام نهایی برند یا دامنه

⛔ **هرگز TO-DO نکن برای** (لیست سیاه — هر چیزی که در این لیست است
**قابل اجرا** توسط agent است، حتی اگر بزرگ یا چندبخشی باشد):

  ❌ UI component / page / dashboard (هر فریم‌ورک: React, Vue, Angular,
     Svelte، حتی اگر معماری بزرگ دارد) — می‌توانی stub اولیه + state
     management + layout + استایل بسازی
  ❌ "نیازمند Google Drive / Stripe / Twilio API" — می‌توانی **client
     stub** با abstraction layer بسازی که با env var واقعی plug-in شود؛
     کد integration یعنی پیاده‌سازی، نه TO-DO
  ❌ "feature بزرگ، چند روز کار می‌برد" — اندازه دلیل defer نیست؛ کوچک
     شروع کن، iterate کن، در همین سشن کامل کن
  ❌ Celery / background worker / scheduler — یک task ساده + register
     می‌توانی بسازی
  ❌ Migration / model / schema — حتی اگر فیلد جدید نیاز دارد، اضافه کن
  ❌ REST endpoint / GraphQL resolver / WebSocket route — هرگز TO-DO
  ❌ test (unit/integration/e2e) — همیشه قابل نوشتن
  ❌ Documentation / README / API docs — همیشه قابل نوشتن
  ❌ Config file / .env.example / Dockerfile / CI workflow — همیشه قابل
     نوشتن
  ❌ "می‌توانستی .tsx ولی repo .jsx است" — از .jsx استفاده کن، TO-DO نکن
  ❌ "نیازمند فیلد X در مدل دیگر" — اضافه کن فیلد را، TO-DO نکن
  ❌ "تصمیم admin-vs-user-scoped" — پرامپت اولیه scope را معلوم کرده،
     یا با محتاطانه‌ترین تفسیر پیش برو
  ❌ "credential در production هنوز ست نیست" — این TO-DO ساده برای
     تنظیم env var است (مورد ۱ بالا)، نه دلیل برای defer کردن کد
  ❌ "نیازمند verification از کاربر" — اگر اقدام واقعی غیرممکن نیست،
     پیش برو
  ❌ هر چیزی که در یک کامنت `# TODO` معمولی نوشته می‌شد — این فایل
     TO-DO نیست، کامنت inline است

🔬 **قاعدهٔ «حداقل تلاش» قبل از TO-DO**: قبل از TO-DO کردن یک AC، **اثبات
کن** که قابل انجام نیست:

  ۱. آیا می‌توانم یک stub/placeholder بسازم که با env واقعی plug-in شود؟
     → اگر بله، بساز و TO-DO نکن
  ۲. آیا می‌توانم برای این بخش یک test (حتی mock-based) بنویسم؟
     → اگر بله، بنویس و TO-DO نکن
  ۳. آیا می‌توانم abstraction/interface را تعریف کنم، حتی اگر backend
     واقعی نیست؟ → اگر بله، تعریف کن و TO-DO نکن
  ۴. آیا فقط یک حالت سلیقه‌ای/decision کاربر در میان است؟
     → فقط آن یک decision را TO-DO کن، نه کل feature را

اگر یکی از این چهار راه‌حل ممکن بود ولی به TO-DO رفتی، **اعتبار شما از
بین می‌رود**.

📊 **آستانهٔ TO-DO per session**: در یک حلقهٔ اجرای N تسک، اگر بیشتر از
**۲۰٪** تسک‌ها فایل TO-DO ساختی، خودت در گزارش پایانی صریحاً اعلام کن:

  "⚠️ نسبت TO-DO من {K}/{N} = {%} است که از آستانهٔ ۲۰٪ بالاتر است.
   احتمالاً برخی از این TO-DO ها قابل اجرا بودند ولی من فرار کردم.
   لیست TO-DO ها را کاربر باید بازبینی کند که آیا واقعاً Manual-required
   بودند یا agent ضعیف کار کرده."

**یادآوری همیشگی:** اگر در آینده قابلیت‌های شما گسترش پیدا کرد و توانستید
یکی از موارد لیست سفید را خودکار انجام دهید (مثلاً managed credential
injection، یا integration پولی automate شود)، انجام دهید و TO-DO نسازید.
لیست سفید بسته است ولی **بسته از پایین** (می‌تواند کوچک‌تر شود اگر
قابلیت‌ها رشد کنند، ولی هرگز بزرگ‌تر نشود برای فرار).

**اگر هیچ بخش Manual-required نبود (تمام تسک Auto-capable است)**:
  → فایل TO-DO **نساز**. فولدر TO-DO/ باید پاک و معنادار بماند.
  → اگر برای این task از قبل `TO-DO/todo-task-{task_id_first_8}.md` بود
     (یعنی در run قبلی نیاز به دخالت کاربر بود ولی الان نه): فایل قدیمی
     را پاک کن و entry را از `TO-DO/_index.json` حذف کن.

**اگر بخش Manual-required دارد** (همه‌جانبه یا hybrid):
  1. فولدر TO-DO/ را در ریشه ریپو ایجاد کن اگر نیست
  2. فایل `TO-DO/todo-task-{task_id_first_8}.md` بساز با front-matter
     شامل: task_id, task_title, execution_priority, created_at,
     updated_at, status: "pending"
     و در بدنه: «چرا این فایل ساخته شد»، «وضعیت بخش‌های خودکار»
     (commit ها reference)، «کارهایی که باید انجام دهی» با اولویت
     بالا/متوسط/پایین به ترتیب، «وقتی این کارها را تمام کردی»
  3. `TO-DO/_index.json` را با **merge** آپدیت کن (نه overwrite):
     - فایل موجود را بخوان
     - entry های orphan (فایلشان پاک شده) را حذف کن
     - entry این task را اضافه/replace کن
     - بر اساس execution_priority صعودی مرتب کن
     - ساختار: `{"version":1, "generated_at": ISO, "total": N, "items": [...]}`
  4. این تغییرات TO-DO را در **همان commit کد** شامل کن (نه commit جداگانه)

⛔ **ممنوعات مطلق TO-DO**:
  ❌ ساختن TO-DO برای کاری که می‌توانستی خودت انجام دهی (شلوغی فولدر)
  ❌ overwrite کردن `TO-DO/_index.json` بدون merge (data loss)
  ❌ نگه‌داشتن entry هایی که فایل‌شان پاک شده (broken reference)
  ❌ فراموش کردن نوشتن «خروجی مورد انتظار» در هر آیتم TO-DO

این بخش الزامی است. حتی اگر فکر می‌کنی "این تسک کاملاً auto است و نیازی
به TO-DO نیست"، صریحاً در commit message یا report بنویس:
"بررسی شد — این تسک هیچ بخش Manual-required ندارد، TO-DO ساخته نشد."

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

🔁 **Commit + Push فوری per-task (بسیار مهم برای جریان کار صحیح):**

پس از اتمام پیاده‌سازی این تسک، **بلافاصله** commit کن و **همان موقع**
به default branch (main/master) push کن. سپس به تسک بعدی برو.

✓ چرا این قانون حیاتی است:
  - تسک‌های بعدی ممکن است به فایل‌ها/تغییراتی که این تسک ایجاد کرده
    نیاز داشته باشند. اگر push نکنی، `git pull` بعدی آن‌ها را نمی‌بیند.
  - جمع‌کردن تغییرات چند تسک منجر به conflict های بزرگ می‌شود.
  - اگر در میانه fail کنی، task های push شده ضایع نمی‌شوند.

⛔ ممنوع: "همه task ها را تمام می‌کنم بعد یک‌جا push می‌زنم"
⛔ ممنوع: branch جدا برای task — مستقیم به default branch
⛔ ممنوع: task بعدی بدون push کامل task قبلی

---


---

## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند.)_

```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم و از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....
```

## 📋 چک‌لیست مراحل (14 مرحله)

این تسک به مراحل کوچک‌تر تقسیم شده. **در هر verify خودکار، وضعیت هر مرحله به‌صورت `[ ]` (انجام نشده)، `[~]` (ناقص)، یا `[x]` (انجام شده) به‌روز می‌شود.**
وقتی تمام مراحل `[x]` شدند، تسک به‌طور خودکار به «انجام شده» منتقل می‌شود.

- [ ] **مرحله 1: ایجاد ساختار پایه برای اتصال تلگرام دوطرفه با سیستم** — این مرحله شامل ایجاد یک ماژول پایه برای ارتباط با Telegram Bot API است. باید یک کلاس پایه برای ارسال و دریافت پیام‌ها از طریق Webhook یا Polling ایجاد شود. این مرحله فقط شامل ایجاد ساختار پایه و اتصال اولیه است و شامل منطق خاص ارسال اطلاعات دروس یا کنترل سیستم نمی‌شود. نکته حیاتی: باید از کتابخانه p
- [ ] **مرحله 2: پیاده‌سازی ارسال نوتیفیکیشن‌های خودکار از سیستم به تلگرام** — این مرحله شامل ایجاد یک سرویس نوتیفیکیشن است که رویدادهای مشخص سیستم (مثل ثبت‌نام جدید، تغییر وضعیت، اتمام مهلت) را به صورت خودکار به کاربر از طریق تلگرام ارسال کند. باید یک صف پیام یا event-driven mechanism پیاده‌سازی شود. خارج از این مرحله: ارسال محتوای دروس یا کنترل سیستم.
- [ ] **مرحله 3: پیاده‌سازی دستورات کنترلی پایه از طریق تلگرام (Command Handlers)** — این مرحله شامل تعریف و پیاده‌سازی دستورات متنی پایه است که کاربر می‌تواند از طریق تلگرام به سیستم ارسال کند. مثال: /status برای وضعیت سیستم، /courses برای لیست دروس، /help برای راهنما. این دستورات باید به ماژول‌های مربوطه در سیستم متصل شوند. خارج از این مرحله: آپلود فایل، مدیریت محتوای دروس، یا اتصا
- [ ] **مرحله 4: ایجاد ماژول مشاهده محتوای دروس از طریق تلگرام** — این مرحله شامل ایجاد قابلیت مشاهده لیست دروس، جلسات، و محتوای متنی دروس از طریق تلگرام است. باید دستورات /courses, /course_{id}, /session_{id} پیاده‌سازی شوند. محتوا باید به صورت خلاصه و قابل مرور در تلگرام نمایش داده شود. خارج از این مرحله: آپلود محتوا، ویرایش، یا اتصال به هوش مصنوعی.
- [ ] **مرحله 5: پیاده‌سازی آپلود محتوای دروس از طریق تلگرام** — این مرحله شامل قابلیت آپلود فایل‌های درسی (PDF، تصویر، ویدئو) از طریق تلگرام به سیستم است. باید یک دستور /upload ایجاد شود که فایل ارسالی را دریافت کرده و به درس/جلسه مشخص متصل کند. باید validation و ذخیره‌سازی امن فایل‌ها پیاده‌سازی شود. خارج از این مرحله: ویرایش محتوا یا اتصال به هوش مصنوعی.
- [ ] **مرحله 6: ایجاد مدیریت کامل دروس از طریق تلگرام (ویرایش، حذف، سازماندهی)** — این مرحله شامل دستورات پیشرفته‌تر برای مدیریت دروس از طریق تلگرام است: ویرایش توضیحات درس، حذف جلسه، جابجایی محتوا، تغییر ترتیب. باید از inline keyboards و confirmation dialogs استفاده شود. خارج از این مرحله: اتصال به هوش مصنوعی یا تمرین با استاد.
- [ ] **مرحله 7: اتصال تلگرام به بخش 'تمرین با استاد هوش مصنوعی' - احراز هویت و انتخاب موضوع** — این مرحله شامل ایجاد قابلیت اتصال به بخش تمرین با استاد هوش مصنوعی از طریق تلگرام است. باید دستور /practice ایجاد شود که کاربر را به بخش تمرین متصل کند، موضوعات موجود را نمایش دهد، و امکان انتخاب موضوع را فراهم کند. خارج از این مرحله: مکالمه واقعی با هوش مصنوعی یا ارسال پیام صوتی.
- [ ] **مرحله 8: پیاده‌سازی مکالمه متنی با هوش مصنوعی از طریق تلگرام** — این مرحله شامل اتصال چت تلگرام به موتور هوش مصنوعی (مثلاً OpenAI API یا مدل داخلی) برای مکالمه متنی است. پیام‌های متنی کاربر باید به AI ارسال شده و پاسخ به تلگرام برگردد. باید context session حفظ شود. خارج از این مرحله: پیام صوتی، تحلیل صوت، یا پاسخ صوتی.
- [ ] **مرحله 9: پیاده‌سازی ارسال پیام صوتی از تلگرام به هوش مصنوعی برای تحلیل** — این مرحله شامل قابلیت دریافت پیام صوتی از کاربر در تلگرام، تبدیل آن به متن (Speech-to-Text)، ارسال متن به هوش مصنوعی، و دریافت پاسخ است. باید از APIهای تبدیل صوت به متن (مثل Whisper) استفاده شود. خارج از این مرحله: تولید پاسخ صوتی.
- [ ] **مرحله 10: پیاده‌سازی پاسخ صوتی از هوش مصنوعی به تلگرام (Text-to-Speech)** — این مرحله شامل تولید پاسخ صوتی از متن پاسخ هوش مصنوعی و ارسال آن به کاربر در تلگرام است. باید از APIهای Text-to-Speech (مثل Google TTS یا ElevenLabs) استفاده شود. پاسخ صوتی باید به صورت فایل صوتی (OGG/MP3) به تلگرام ارسال شود. خارج از این مرحله: ذخیره‌سازی طولانی‌مدت فایل‌های صوتی.
- [ ] **مرحله 11: همگام‌سازی دوطرفه چت تلگرام با چت وب (Real-time Sync)** — این مرحله شامل همگام‌سازی لحظه‌ای پیام‌ها بین چت تلگرام و چت وب (بخش تمرین با استاد هوش مصنوعی) است. هر پیام ارسالی در تلگرام باید در وب ظاهر شود و بالعکس. باید از WebSocket یا polling برای sync استفاده شود. خارج از این مرحله: مدیریت فایل‌های صوتی یا تاریخچه.
- [ ] **مرحله 12: مدیریت session و تاریخچه تمرینات از طریق تلگرام** — این مرحله شامل قابلیت مشاهده تاریخچه تمرینات قبلی، ادامه session‌های ناقص، و مدیریت session‌ها از طریق تلگرام است. دستورات /history, /continue, /sessions پیاده‌سازی می‌شوند. خارج از این مرحله: تحلیل پیشرفت یا آمار.
- [ ] **مرحله 13: پیاده‌سازی امنیت و احراز هویت برای دستورات تلگرام** — این مرحله شامل احراز هویت کاربران تلگرام و محدود کردن دستورات به کاربران مجاز است. باید از chat ID verification یا OAuth استفاده شود. دستورات حساس (مثل حذف محتوا) نیاز به تأیید اضافی دارند. خارج از این مرحله: مدیریت کاربران یا نقش‌ها.
- [ ] **مرحله 14: تست و دیباگ کامل یکپارچه‌سازی تلگرام با سیستم** — این مرحله شامل تست کامل تمام قابلیت‌های پیاده‌سازی شده: ارسال نوتیفیکیشن، دستورات کنترلی، مشاهده/آپلود محتوا، مکالمه با AI، voice input/output، و sync. باید unit tests و integration tests نوشته شود. خارج از این مرحله: تست performance یا load testing.

---

# 🔹 مرحله 1: ایجاد ساختار پایه برای اتصال تلگرام دوطرفه با سیستم

**Scope:** این مرحله شامل ایجاد یک ماژول پایه برای ارتباط با Telegram Bot API است. باید یک کلاس پایه برای ارسال و دریافت پیام‌ها از طریق Webhook یا Polling ایجاد شود. این مرحله فقط شامل ایجاد ساختار پایه و اتصال اولیه است و شامل منطق خاص ارسال اطلاعات دروس یا کنترل سیستم نمی‌شود. نکته حیاتی: باید از کتابخانه python-telegram-bot یا aiogram استفاده شود و Webhook به صورت امن با token تنظیم شود.
**Key terms:** Telegram Bot API, Webhook, python-telegram-bot, aiogram, token, polling

**بخش مربوط از متن کاربر:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد ماژول پایه اتصال تلگرام دوطرفه با Webhook

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:38-48` — `app و server و wss` — بعد از ایجاد WebSocket server (خط ۴۵)، باید Webhook endpoint تلگرام اضافه شود. همچنین باید مسیر Webhook از پردازش WebSocket جدا باشد.
  ```jsx
  const app = express();
  const PORT = process.env.PORT || 3001;
  const server = createServer(app);
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/server.js:166-169` — `Health check endpoint` — الگوی endpoint‌های API در این فایل مشخص است. Webhook تلگرام باید مشابه همین الگو اضافه شود.
  ```jsx
  app.get('/api/health', (req, res) => {
    res.json({ status: 'ok', timestamp: new Date().toISOString() });
  });
  ```
- `backend/.env.example:1-8` — `Environment variables` — باید متغیر TELEGRAM_BOT_TOKEN به این فایل اضافه شود.
  ```
  GEMINI_API_KEY=your_gemini_api_key_here
  PORT=3001
  # Firebase config (optional - for data persistence)
  # VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/package.json` (سطر 10) — برای اضافه کردن وابستگی telegraf به dependencies نیاز به تغییر دارد.
- `backend/server.js` (سطر 7) — برای اضافه کردن endpoint Webhook و import سرویس تلگرام نیاز به تغییر دارد.
- `backend/.env.example` (سطر 1) — برای اضافه کردن TELEGRAM_BOT_TOKEN به عنوان متغیر محیطی نیاز به تغییر دارد.
- `frontend/src/App.jsx` (سطر 1) — ممکن است در آینده برای نمایش وضعیت اتصال تلگرام در UI نیاز به تغییر داشته باشد.

## 🌐 نقشهٔ وابستگی‌ها
این ماژول جدید (backend/services/telegramBot.js) به کتابخانه telegraf وابسته است که باید به backend/package.json اضافه شود. backend/server

## 🔍 Context و وضعیت فعلی
کاربر درخواست کرده است که برای این پروژه (اپلیکیشن آموزش لهجه لبنانی) نوتیفیکیشن از طریق تلگرام فراهم شود و تلگرام دو طرفه کار کند. یعنی علاوه بر اینکه اطلاعات از سیستم گرفته می‌شود و از طریق تلگرام ارسال می‌شود، کاربر بتواند از طریق تلگرام هم برخی کارها روی سیستم را کنترل کند. این مرحله فقط شامل ایجاد ساختار پایه و اتصال اولیه است و شامل منطق خاص ارسال اطلاعات دروس یا کنترل سیستم نمی‌شود. کلیدواژه‌های اصلی: Telegram Bot API, Webhook, python-telegram-bot, aiogram, token, polling. کاربر تأکید کرده که باید از کتابخانه python-telegram-bot یا aiogram استفاده شود و Webhook به صورت امن با token تنظیم شود. پروژه فعلی یک اپلیکیشن Node.js (Express) با backend در backend/server.js و frontend در frontend/src/App.jsx است. backend/server.js شامل WebSocket (ws) در خط ۴۵ برای ارتباط live، و APIهای مختلف Gemini (Chat در خط ۵۶، TTS در خط ۱۱۷، تحلیل فایل در خط ۶۹۸) است. backend/package.json شامل dependencies: express, cors, dotenv, ws, multer, fluent-ffmpeg, ffmpeg-static, pdf-parse است. frontend با React + Vite + Tailwind ساخته شده. کاربر می‌خواهد یک ماژول پایه تلگرام به پروژه اضافه شود که با معماری فعلی (JavaScript/Node.js) هماهنگ باشد. از آنجایی که پروژه با JavaScript/Node.js نوشته شده، استفاده از کتابخانه‌های پایتونی (python-telegram-bot یا aiogram) نیازمند اضافه کردن یک سرویس مجزا یا تغییر معماری است. بهترین رویکرد: استفاده از کتابخانه Node.js مانند node-telegram-bot-api یا telegraf.js برای حفظ یکپارچگی stack.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ۱. ایجاد فایل جدید backend/services/telegramBot.js با کلاس TelegramBotService که از کتابخانه telegraf.js (یا node-telegram-bot-api) استفاده کند. ۲. پیاده‌سازی متد init(token) برای راه‌اندازی bot با Webhook. ۳. پیاده‌سازی متد setWebhook(url) برای تنظیم Webhook امن با token. ۴. پیاده‌سازی متد sendMessage(chatId, text) برای ارسال پیام. ۵. پیاده‌سازی متد onMessage(callback) برای دریافت پیام‌های کاربر (دو طرفه). ۶. اضافه کردن endpoint جدید در backend/server.js برای دریافت Webhook از تلگرام (POST /api/telegram/webhook). ۷. اضافه کردن متغیر محیطی TELEGRAM_BOT_TOKEN در backend/.env.example. ۸. نصب کتابخانه telegraf با npm install telegraf. ۹. ایجاد فایل تست backend/tests/telegramBot.test.js برای تست اتصال پایه.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 2: پیاده‌سازی ارسال نوتیفیکیشن‌های خودکار از سیستم به تلگرام

**Scope:** این مرحله شامل ایجاد یک سرویس نوتیفیکیشن است که رویدادهای مشخص سیستم (مثل ثبت‌نام جدید، تغییر وضعیت، اتمام مهلت) را به صورت خودکار به کاربر از طریق تلگرام ارسال کند. باید یک صف پیام یا event-driven mechanism پیاده‌سازی شود. خارج از این مرحله: ارسال محتوای دروس یا کنترل سیستم.
**Key terms:** NotificationService, event queue, TelegramBot.send_message, notification templates, user preferences

**بخش مربوط از متن کاربر:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی سرویس نوتیفیکیشن تلگرام دوطرفه

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:44-46` — `WebSocketServer` — اینجا محل ایجاد WebSocket است. بعد از این خط، سرویس تلگرام باید مقداردهی اولیه شود و event listeners به رویدادهای سیستم متصل شوند. همچنین می‌توان از همین WebSocket برای ارسال نوتیفیکیشن به فرانت‌اند استفاده کرد.
  ```jsx
  // Create WebSocket server
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/server.js:53-54` — `GEMINI_API_KEY` — در این خط GEMINI_API_KEY از env خوانده می‌شود. به همین ترتیب، TELEGRAM_BOT_TOKEN باید از env خوانده شود. این الگو نشان می‌دهد که پروژه از dotenv استفاده می‌کند (خط 14: dotenv.config()).

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست کرده است: «میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته». این درخواست در commit 7bddd3c ثبت شده و در task dc721af2-f84f-424d-a491-91e8ab3624c0 پیگیری می‌شود. متن کامل درخواست: «پیاده‌سازی ارسال نوتیفیکیشن‌های خودکار از سیستم به تلگرام. این مرحله شامل ایجاد یک سرویس نوتیفیکیشن است که رویدادهای مشخص سیستم (مثل ثبت‌نام جدید، تغییر وضعیت، اتمام مهلت) را به صورت خودکار به کاربر از طریق تلگرام ارسال کند. باید یک صف پیام یا event-driven mechanism پیاده‌سازی شود. خارج از این مرحله: ارسال محتوای دروس یا کنترل سیستم. — بخش مربوط از درخواست اصلی کاربر — میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته — کلیدواژه‌ها — NotificationService, event queue, TelegramBot.send_message, notification templates, user preferences». پروژه فعلی یک اپلیکیشن آموزش لهجه لبنانی با React (frontend) و Express.js (backend) است که از WebSocket (ws) در backend/server.js خط 45 برای ارتباط بلادرنگ استفاده می‌کند. backend/server.js شامل APIهای مختلفی مانند /api/gemini/chat (خط 56)، /api/gemini/tts (خط 117)، /api/analyze-files (خط 698) است. frontend/index.html شامل Firebase config (خط 11-18) و یک Inspector Bridge (خط 32-201) است. backend/.env.example نشان می‌دهد که GEMINI_API_KEY و Firebase config موجود است. backend/package.json شامل dependencies: express, cors, dotenv, ws, multer, fluent-ffmpeg, ffmpeg-static است. frontend/package.json شامل react, react-dom, firebase, lucide-react است. برای پیاده‌سازی نوتیفیکیشن تلگرام دوطرفه، نیاز به ایجاد یک سرویس جدید در backend (مثلاً backend/services/telegramService.js) داریم که از کتابخانه‌ای مانند node-telegram-bot-api استفاده کند. این سرویس باید رویدادهای سیستم را از طریق یک event queue (می‌توان از EventEmitter داخلی Node.js یا یک صف ساده مبتنی بر آرایه استفاده کرد) دریافت کرده و به کاربران از طریق تلگرام ارسال کند. همچنین باید قابلیت دریافت پیام از تلگرام (دوطرفه) را داشته باشد تا کاربر بتواند دستوراتی مثل وضعیت سیستم یا تنظیمات را ارسال کند. WebSocket موجود در backend/server.js خط 45 (wss = new WebSocketServer({ server, path: '/ws/live' })) می‌تواند برای ارسال نوتیفیکیشن‌های بلادرنگ به فرانت‌اند نیز استفاده شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/services/telegramService.js با کلاس TelegramNotificationService که از کتابخانه node-telegram-bot-api استفاده می‌کند. 2. در این سرویس، یک EventEmitter برای مدیریت رویدادهای سیستم (مانند 'new_registration', 'status_change', 'deadline_expired') ایجاد شود. 3. پیاده‌سازی متد sendNotification(userTelegramId, message, templateName) که پیام را با استفاده از قالب‌های از پیش تعریف شده (notification templates) ارسال کند. 4. پیاده‌سازی متد handleIncomingMessage(msg) برای دریافت پیام‌های دوطرفه از تلگرام و پاسخ به دستوراتی مانند /status, /settings, /help. 5. ایجاد یک event queue ساده (می‌توان از آرایه یا کتابخانه bull/bullmq استفاده کرد) برای مدیریت صف پیام‌ها. 6. اتصال سرویس به backend/server.js: در خطوط 45-46 (بعد از ایجاد WebSocket)، یک نمونه از TelegramNotificationService ایجاد شود و event listeners به رویدادهای سیستم متصل شوند. 7. ایجاد فایل backend/config/telegram.js برای ذخیره توکن ربات تلگرام و تنظیمات. 8. به‌روزرسانی backend/.env.example برای اضافه کردن TELEGRAM_BOT_TOKEN و TELEGRAM_CHAT_ID. 9. نصب وابستگی: npm install node-telegram-bot-api در backend. 10. ایجاد notification templates در فایل backend/services/notificationTemplates.js با قالب‌های پیام برای هر رویداد.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 3: پیاده‌سازی دستورات کنترلی پایه از طریق تلگرام (Command Handlers)

**Scope:** این مرحله شامل تعریف و پیاده‌سازی دستورات متنی پایه است که کاربر می‌تواند از طریق تلگرام به سیستم ارسال کند. مثال: /status برای وضعیت سیستم، /courses برای لیست دروس، /help برای راهنما. این دستورات باید به ماژول‌های مربوطه در سیستم متصل شوند. خارج از این مرحله: آپلود فایل، مدیریت محتوای دروس، یا اتصال به هوش مصنوعی.
**Key terms:** CommandHandler, /status, /courses, /help, telegram message routing, system control commands

**بخش مربوط از متن کاربر:**
```
بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی دستورات کنترلی پایه تلگرام (Command Handlers)

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:38-42` — `app creation and server setup` — بعد از خط 42 (ایجاد WebSocket server)، باید TelegramBotHandler را import و راه‌اندازی کنیم. همچنین نیاز به افزودن endpoint /api/telegram/status و /api/courses داریم.
  ```jsx
  const app = express();
  const PORT = process.env.PORT || 3001;
  
  // Create HTTP server
  const server = createServer(app);
  
  // Create WebSocket server
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/server.js:167-169` — `Health check endpoint` — این endpoint توسط handleStatus() برای گرفتن وضعیت سیستم استفاده می‌شود. باید اطمینان حاصل کنیم که پاسخ health check برای نمایش در تلگرام مناسب است.
  ```jsx
  // Health check
  app.get('/api/health', (req, res) => {
    res.json({ status: 'ok', timestamp: new Date().toISOString() });
  });
  ```
- `backend/.env.example:1-8` — `Environment variables template` — باید خط TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here به این فایل اضافه شود.
  ```
  # Gemini API Key - Get from https://aistudio.google.com/apikey
  GEMINI_API_KEY=your_gemini_api_key_here
  
  # Port (optional, default 3001)
  PORT=3001
  
  # Firebase config (optional - for data persistence)
  # VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/package.json` (سطر 1) — نیاز به افزودن وابستگی node-telegram-bot-api به dependencies دارد
- `backend/server.js` (سطر 38) — فایل اصلی سرور که باید TelegramBotHandler را import و راه‌اندازی کند. همچنین endpoint /api/telegram/status و /api/courses باید به این فایل اضافه شوند.
- `backend/.env.example` (سطر 1) — نیاز به افزودن متغیر TELEGRAM_BOT_TOKEN برای پیکربندی بات تلگرام دارد
- `frontend/src/App.jsx` (سطر 1) — ممکن است نیاز به نمایش وضعیت اتصال تلگرام در UI داشته باشد (اختیاری)
- `prompt/task-dc721af2-f84f-424d-a491-91e8ab3624c0.md` (سطر 1) — تسک قبلی مرتبط با نوتیفیکیشن تلگرام که نشان‌دهنده وجود زیرساخت اولیه برای تلگرام است

## 🌐 نقشهٔ وابستگی‌ها
این تسک یک ماژول جدید backend/services/telegramBot.js ایجاد می‌کند که به backend/server.js import می‌شود. وابستگی node-telegram-bot-api به backend/package.json اضافه می‌شود. متغیر TELEGRAM_BOT_TOKEN به backend

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی دستورات متنی پایه از طریق تلگرام (Command Handlers) را دارد. این دستورات شامل /status برای وضعیت سیستم، /courses برای لیست دروس، و /help برای راهنما هستند. کاربر می‌خواهد از طریق تلگرام کنترل و انجام برخی کارها روی سیستم را داشته باشد. کلیدواژه‌های اصلی: CommandHandler, /status, /courses, /help, telegram message routing, system control commands. خارج از این مرحله: آپلود فایل، مدیریت محتوای دروس، یا اتصال به هوش مصنوعی.

تحلیل کد واقعی پروژه: پروژه یک اپلیکیشن آموزش لهجه لبنانی با هوش مصنوعی است. backend/server.js (خطوط 1-800) شامل APIهای Express شامل /api/gemini/chat (خط 56)، /api/gemini/tts (خط 117)، /api/analyze-files (خط 698) و WebSocket در /ws/live (خط 45) است. frontend/index.html (خط 1-208) شامل Firebase config و Inspector Bridge Script است. backend/package.json وابستگی‌های express, cors, dotenv, ws, multer, fluent-ffmpeg را نشان می‌دهد. frontend/package.json وابستگی‌های react, firebase, lucide-react را نشان می‌دهد. هیچ ماژول تلگرام (node-telegram-bot-api یا telegraf) در وابستگی‌ها وجود ندارد. backend/.env.example (خط 1-8) شامل GEMINI_API_KEY و Firebase config است اما هیچ متغیر تلگرامی (TELEGRAM_BOT_TOKEN) ندارد. این تسک نیازمند ایجاد یک ماژول جدید backend/services/telegramBot.js است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. افزودن وابستگی node-telegram-bot-api به backend/package.json
2. ایجاد فایل جدید backend/services/telegramBot.js شامل:
   - کلاس TelegramBotHandler با متدهای setupCommands(), handleStatus(), handleCourses(), handleHelp()
   - متد setupCommands() برای ثبت دستورات /status, /courses, /help با استفاده از bot.onText(/\/status/, ...)
   - متد handleStatus() که وضعیت سیستم را از endpoint /api/health (backend/server.js خط 167) می‌گیرد
   - متد handleCourses() که لیست دروس را از یک منبع داده (فعلاً stub) برمی‌گرداند
   - متد handleHelp() که راهنمای دستورات را نمایش می‌دهد
3. افزودن متغیر TELEGRAM_BOT_TOKEN به backend/.env.example
4. اصلاح backend/server.js برای import و راه‌اندازی TelegramBotHandler در خطوط 38-42 (بعد از ایجاد app)
5. افزودن endpoint GET /api/telegram/status به backend/server.js برای بررسی وضعیت بات تلگرام
6. افزودن route جدید /api/courses به backend/server.js (فعلاً stub با داده‌های نمونه)
7. افزودن تست‌های واحد برای CommandHandler در backend/tests/telegramBot.test.js

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 4: ایجاد ماژول مشاهده محتوای دروس از طریق تلگرام

**Scope:** این مرحله شامل ایجاد قابلیت مشاهده لیست دروس، جلسات، و محتوای متنی دروس از طریق تلگرام است. باید دستورات /courses, /course_{id}, /session_{id} پیاده‌سازی شوند. محتوا باید به صورت خلاصه و قابل مرور در تلگرام نمایش داده شود. خارج از این مرحله: آپلود محتوا، ویرایش، یا اتصال به هوش مصنوعی.
**Key terms:** /courses, /course_{id}, /session_{id}, course content viewer, telegram inline keyboard, pagination

**بخش مربوط از متن کاربر:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد ماژول مشاهده محتوای دروس از طریق تلگرام

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:38-45` — `server creation and WebSocket setup` — بعد از WebSocket server (خط 45)، باید تلگرام bot را راه‌اندازی کنیم. اینجا نقطه ورود مناسب است.
  ```jsx
  const app = express();
  const PORT = process.env.PORT || 3001;
  
  // Create HTTP server
  const server = createServer(app);
  
  // Create WebSocket server
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/server.js:53` — `GEMINI_API_KEY constant` — الگوی مشابه برای TELEGRAM_BOT_TOKEN: const TELEGRAM_BOT_TOKEN = process.env.TELEGRAM_BOT_TOKEN; در همین محدوده اضافه شود.
  ```jsx
  const GEMINI_API_KEY = process.env.GEMINI_API_KEY;
  ```
- `backend/.env.example:1-8` — `environment variables template` — باید خط TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here به این فایل اضافه شود.
  ```
  # Gemini API Key - Get from https://aistudio.google.com/apikey
  GEMINI_API_KEY=your_gemini_api_key_here
  
  # Port (optional, default 3001)
  PORT=3001
  
  # Firebase config (optional - for data persistence)
  # VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}
  ```
- `backend/package.json:1-15` — `dependencies` — باید node-telegram-bot-api یا telegraf به dependencies اضافه شود.
  ```json
  {
    "name": "lebanese-dialect-backend",
    "version": "1.0.0",
    "dependencies": {
      "cors": "^2.8.5",
      "dotenv": "^16.3.1",
      "express": "^4.18.2",
      "ws": "^8.19.0"
    }
  }
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/server.js` (سطر 45) — فایل اصلی سرور که باید تلگرام bot را import و راه‌اندازی کند. خط 45 نقطه مناسب برای افزودن کد راه‌اندازی است.
- `backend/.env.example` (سطر 1) — تمپلیت env که باید TELEGRAM_BOT_TOKEN را شامل شود تا توسعه‌دهندگان بدانند چه متغیری نیاز است.
- `backend/package.json` (سطر 10) — فایل dependencies که باید کتابخانه تلگرام به آن اضافه شود.
- `prompt/task-dc721af2-f84f-424d-a491-91e8ab3624c0.md` (سطر 1) — تسک قبلی مرتبط با تلگرام که نشان‌دهنده ادامه مسیر توسعه قابلیت‌های تلگرامی است.

## 🌐 نقشهٔ وابستگی‌ها
این تسک وابسته به فایل‌های backend/server.js (برای راه‌اندازی bot)، backend/package.json (برای اضافه کردن dependency)، backend/.env.example (برای مستندسازی متغیر محیطی) است. فایل جدید backend/services/telegramBot.js به عنوان ماژول اصلی

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد قابلیت مشاهده لیست دروس، جلسات، و محتوای متنی دروس از طریق تلگرام را دارد. باید دستورات /courses, /course_{id}, /session_{id} پیاده‌سازی شوند. محتوا باید به صورت خلاصه و قابل مرور در تلگرام نمایش داده شود. خارج از این مرحله: آپلود محتوا، ویرایش، یا اتصال به هوش مصنوعی. این درخواست برگرفته از بخشی از درخواست اصلی کاربر است: 'و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...'. کلیدواژه‌های اصلی: /courses, /course_{id}, /session_{id}, course content viewer, telegram inline keyboard, pagination. با توجه به commit قبلی (7bddd3c) که مربوط به 'میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار' است، این تسک ادامه همان مسیر است. در کد فعلی backend/server.js هیچ endpoint یا ماژول تلگرامی وجود ندارد. frontend/index.html شامل اسکریپت inspector bridge است اما ربطی به تلگرام ندارد. backend/package.json فقط dependencies اصلی (express, cors, ws, dotenv) را دارد و هیچ کتابخانه تلگرامی (مثل node-telegram-bot-api یا telegraf) نصب نیست. backend/.env.example فقط GEMINI_API_KEY و PORT و Firebase config را دارد. بنابراین این تسک نیازمند ایجاد یک ماژول تلگرام جدید از صفر است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. نصب کتابخانه node-telegram-bot-api یا telegraf در backend (اضافه به backend/package.json). 2. ایجاد فایل جدید backend/services/telegramBot.js شامل: a) راه‌اندازی bot با توکن از env (TELEGRAM_BOT_TOKEN). b) هندلر دستور /start با خوش‌آمدگویی. c) هندلر دستور /courses که لیست دروس را با inline keyboard نمایش دهد. d) هندلر callback_query برای /course_{id} که جلسات آن درس را نمایش دهد. e) هندلر callback_query برای /session_{id} که محتوای متنی جلسه را نمایش دهد. f) پیاده‌سازی pagination برای لیست‌های طولانی. 3. ایجاد فایل جدید backend/data/courses.json (یا استفاده از Firebase) برای ذخیره ساختار دروس و جلسات. 4. افزودن TELEGRAM_BOT_TOKEN به backend/.env.example. 5. افزودن خط راه‌اندازی bot در backend/server.js بعد از خط 45 (بعد از ایجاد WebSocket server). 6. پیاده‌سازی نمایش محتوای متنی به صورت خلاصه و قابل مرور با استفاده از Markdown یا HTML در تلگرام.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 5: پیاده‌سازی آپلود محتوای دروس از طریق تلگرام

**Scope:** این مرحله شامل قابلیت آپلود فایل‌های درسی (PDF، تصویر، ویدئو) از طریق تلگرام به سیستم است. باید یک دستور /upload ایجاد شود که فایل ارسالی را دریافت کرده و به درس/جلسه مشخص متصل کند. باید validation و ذخیره‌سازی امن فایل‌ها پیاده‌سازی شود. خارج از این مرحله: ویرایش محتوا یا اتصال به هوش مصنوعی.
**Key terms:** /upload, file handler, telegram document, file storage, course attachment, validation

**بخش مربوط از متن کاربر:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی آپلود محتوای دروس از طریق تلگرام با دستور /upload

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:19-33` — `multer configuration` — این بخش زیرساخت آپلود فایل را فراهم می‌کند. برای آپلود تلگرام باید از همین مکانیزم استفاده کرد اما با یک مسیر مجزا برای فایل‌های درسی.
  ```jsx
  const uploadDir = join(os.tmpdir(), 'lebanese-uploads');
  if (!fs.existsSync(uploadDir)) {
    fs.mkdirSync(uploadDir, { recursive: true });
  }
  
  const storage = multer.diskStorage({
    destination: (req, file, cb) => cb(null, uploadDir),
    filename: (req, file, cb) => cb(null, `${Date.now()}-${file.originalname}`)
  });
  
  const upload = multer({
    storage: storage,
    limits: { fileSize: 500 * 1024 * 1024 }
  });
  ```
- `backend/server.js:698-715` — `app.post('/api/analyze-files')` — این endpoint الگوی خوبی برای دریافت فایل‌هاست. برای آپلود تلگرام باید endpoint مشابهی با مسیر /api/telegram/upload ایجاد شود.
  ```jsx
  app.post('/api/analyze-files', upload.array('files', 10), handleMulterError, async (req, res) => {
    console.log('Received file analysis request');
    if (!GEMINI_API_KEY) {
      console.error('API key not configured');
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const files = req.files || [];
      const textContent = req.body.textContent || '';
      const userInstructions = req.body.userInstructions || '';
  ```
- `backend/server.js:44-45` — `WebSocketServer` — WebSocket موجود می‌تواند برای ارسال نوتیفیکیشن وضعیت آپلود به فرانت‌اند استفاده شود.
  ```jsx
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `frontend/index.html` (سطر 11) — حاوی Firebase config (خطوط 11-18) برای ذخیره‌سازی داده‌های دروس و mapping فایل‌ها
- `frontend/src/App.jsx` (سطر 1) — کامپوننت اصلی فرانت‌اند که باید بخش نمایش و آپلود محتوای دروس را اضافه کند
- `backend/package.json` (سطر 1) — نیازمند اضافه کردن وابستگی node-telegram-bot-api یا کتابخانه مشابه برای ارتباط با Telegram Bot API
- `backend/.env.example` (سطر 1) — باید فیلد TELEGRAM_BOT_TOKEN به env اضافه شود
- `prompt/task-dc721af2-f84f-424d-a491-91e8ab3624c0.md` (سطر 1) — تسک قبلی مرتبط با یکپارچه‌سازی تلگرام که باید ادامه داده شود

## 🌐 نقشهٔ وابستگی‌ها
این قابلیت به فایل‌های backend/server.js (برای multer و WebSocket)، backend/services/telegramUpload.js (جدید)، frontend/src/App.jsx (برای

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی قابلیت آپلود فایل‌های درسی (PDF، تصویر، ویدئو) از طریق تلگرام به سیستم را دارد. باید یک دستور /upload ایجاد شود که فایل ارسالی را دریافت کرده و به درس/جلسه مشخص متصل کند. باید validation و ذخیره‌سازی امن فایل‌ها پیاده‌سازی شود. خارج از این مرحله: ویرایش محتوا یا اتصال به هوش مصنوعی. بخش مربوط از درخواست اصلی کاربر: 'و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...'. کلیدواژه‌ها: /upload, file handler, telegram document, file storage, course attachment, validation. در کد فعلی backend/server.js از خط 19 تا 33 یک سیستم آپلود با multer برای فایل‌های حجیم (تا 500MB) پیاده‌سازی شده که در مسیر uploadDir = join(os.tmpdir(), 'lebanese-uploads') ذخیره می‌کند. همچنین endpoint /api/analyze-files در خط 698 با upload.array('files', 10) فایل‌ها را دریافت می‌کند. پروژه از WebSocket در خط 45 (wss = new WebSocketServer({ server, path: '/ws/live' })) پشتیبانی می‌کند. در commit 7bddd3c کاربر درخواست 'میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار' را داده که نشان‌دهنده تمایل به یکپارچه‌سازی کامل تلگرام است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک فایل جدید backend/services/telegramUpload.js برای مدیریت دستور /upload تلگرام. 2. در این فایل، یک تابع handleUploadCommand ایجاد کن که: الف) فایل ارسالی (document, photo, video) را از طریق Telegram Bot API دریافت کند. ب) فایل را با استفاده از multer (مشابه خطوط 25-33 backend/server.js) در مسیر uploadDir ذخیره کند. ج) validation اولیه روی نوع فایل (PDF, image/*, video/*) و حجم (حداکثر 500MB) انجام دهد. د) فایل را به یک درس/جلسه خاص متصل کند (نیازمند یک ساختار داده برای دروس). 3. ایجاد endpoint جدید در backend/server.js مثل POST /api/telegram/upload که داده‌های دریافتی از تلگرام را پردازش کند. 4. استفاده از WebSocket موجود (خط 45) برای اطلاع‌رسانی وضعیت آپلود به فرانت‌اند. 5. ایجاد یک جدول/collection در دیتابیس (با توجه به Firebase config در frontend/index.html خطوط 11-18) برای ذخیره mapping بین فایل‌های آپلود شده و دروس.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 6: ایجاد مدیریت کامل دروس از طریق تلگرام (ویرایش، حذف، سازماندهی)

**Scope:** این مرحله شامل دستورات پیشرفته‌تر برای مدیریت دروس از طریق تلگرام است: ویرایش توضیحات درس، حذف جلسه، جابجایی محتوا، تغییر ترتیب. باید از inline keyboards و confirmation dialogs استفاده شود. خارج از این مرحله: اتصال به هوش مصنوعی یا تمرین با استاد.
**Key terms:** /edit_course, /delete_session, /reorder, inline keyboard, confirmation dialog, course management

**بخش مربوط از متن کاربر:**
```
و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

## 🎯 هدف (خلاصه ساختاریافته)
مدیریت کامل دروس از طریق تلگرام (ویرایش، حذف، سازماندهی)

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:45-45` — `WebSocketServer` — WebSocket موجود می‌تواند برای ارسال نوتیفیکیشن‌های لحظه‌ای به فرانت‌اند هنگام تغییرات دروس از طریق تلگرام استفاده شود.
  ```jsx
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `frontend/index.html:11-18` — `__firebase_config` — Firebase کانفیگ شده در فرانت‌اند — می‌توان از Firebase Admin SDK در بک‌اند برای ذخیره‌سازی داده‌های دروس و جلسات استفاده کرد.
  ```
  var __firebase_config = JSON.stringify({
          apiKey: "AIzaSyAMFtV0zIMKKe0bIr68Z1wTORQ1jQpsv9Q",
          authDomain: "labaneseapp.firebaseapp.com",
          projectId: "labaneseapp",
          storageBucket: "labaneseapp.firebasestorage.app",
          messagingSenderId: "951874597795",
          appId: "1:951874597795:web:00745327993adad760a016"
        });
  ```
- `backend/server.js:56-114` — `app.post('/api/gemini/chat')` — این endpoint می‌تواند برای پردازش پیام‌های متنی تلگرام و پاسخ هوشمند استفاده شود (البته خارج از scope این مرحله).
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: JavaScript (Node.js backend با Express.js، React/Vite frontend). کتابخانه‌های موجود: express, cors, ws, multer, fluent-ffmpeg, dotenv. کتابخانه

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/package.json` (سطر 1) — نیاز به افزودن وابستگی node-telegram-bot-api و firebase-admin
- `backend/.env.example` (سطر 1) — نیاز به افزودن متغیرهای محیطی TELEGRAM_BOT_TOKEN و FIREBASE_SERVICE_ACCOUNT
- `frontend/src/App.jsx` (سطر 1) — کامپوننت اصلی فرانت‌اند که باید تغییرات دروس را از طریق WebSocket دریافت و نمایش دهد
- `prompt/archive/task-7bddd3c` (سطر 1) — تسک قبلی مرتبط با تلگرام که commit message آن اشاره به 'نوتیفیکشن از طریق تلگرام' دارد
- `render.yaml` (سطر 1) — فایل کانفیگ deployment که باید webhook URL تلگرام را پشتیبانی کند

## 🌐 نقشهٔ وابستگی‌ها
این تسک وابسته به ایجاد فایل‌های جدید backend/services/telegramBot.js (هندلر اصلی ربات تلگرام)، backend/models/course.js (مدل داده دروس و جلسات)، backend/routes/telegram.js (routes مربوط به webhook تلگرام) است. فایل backend/server.js باید فایل routes جدید را import کند. فایل backend/package.json نیاز به وابستگی‌های node-telegram-bot-api و firebase-admin دارد. فایل backend/.env.example نیاز به متغیرهای TELEGRAM_BOT_TOKEN و FIREBASE_SERVICE_ACCOUNT دارد. فایل frontend/src/App.jsx باید از طریق WebSocket (که در backend/server.js خط 45 تعریف شده) تغییرات را دریافت کند.

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد مدیریت کامل دروس از طریق تلگرام را دارد. این مرحله شامل دستورات پیشرفته‌تر برای مدیریت دروس از طریق تلگرام است: ویرایش توضیحات درس، حذف جلسه، جابجایی محتوا، تغییر ترتیب. باید از inline keyboards و confirmation dialogs استفاده شود. خارج از این مرحله: اتصال به هوش مصنوعی یا تمرین با استاد. کلیدواژه‌های مشخص شده: /edit_course, /delete_session, /reorder, inline keyboard, confirmation dialog, course management. کاربر اشاره کرده 'تا حد زیادی کار رو از اینجا پیش ببرم ...' که نشان‌دهنده ادامه کار از یک تسک قبلی است. با بررسی کدبیس، فایل backend/server.js (خطوط 1-1403) شامل WebSocket server در خط 45 (const wss = new WebSocketServer({ server, path: '/ws/live' });) و APIهای مختلف است اما هیچ endpoint یا handler مربوط به تلگرام در آن وجود ندارد. فایل frontend/index.html شامل Firebase config و inspector bridge است. فایل .github/workflows/claude-auto-task.yml شامل workflow خودکار Claude است. فایل‌های prompt/archive متعددی وجود دارند که برخی به تلگرام اشاره دارند (مثلاً task-7bddd3c با commit message 'میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار'). ساختار پروژه شامل backend با Express.js و frontend با React/Vite است. هیچ فایل تلگرام بات یا handler مخصوصی در ساختار فعلی دیده نمی‌شود — این تسک نیاز به ایجاد زیرساخت جدید دارد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک فایل جدید backend/services/telegramBot.js برای مدیریت ربات تلگرام با کتابخانه node-telegram-bot-api. 2. پیاده‌سازی دستور /edit_course با inline keyboard برای انتخاب درس و ویرایش توضیحات. 3. پیاده‌سازی دستور /delete_session با confirmation dialog (دکمه‌های تأیید/لغو). 4. پیاده‌سازی دستور /reorder برای جابجایی محتوا و تغییر ترتیب جلسات با drag-and-drop در inline keyboard. 5. ذخیره‌سازی داده‌ها در Firebase (که قبلاً در frontend/index.html خط 11-18 کانفیگ شده). 6. اتصال به WebSocket موجود در backend/server.js خط 45 برای اطلاع‌رسانی لحظه‌ای. 7. افزودن route جدید در backend/server.js برای webhook تلگرام. 8. ایجاد مدل داده برای دروس و جلسات در فایل جدید backend/models/course.js.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 7: اتصال تلگرام به بخش 'تمرین با استاد هوش مصنوعی' - احراز هویت و انتخاب موضوع

**Scope:** این مرحله شامل ایجاد قابلیت اتصال به بخش تمرین با استاد هوش مصنوعی از طریق تلگرام است. باید دستور /practice ایجاد شود که کاربر را به بخش تمرین متصل کند، موضوعات موجود را نمایش دهد، و امکان انتخاب موضوع را فراهم کند. خارج از این مرحله: مکالمه واقعی با هوش مصنوعی یا ارسال پیام صوتی.
**Key terms:** /practice, topic selection, AI tutor session, authentication, session management, topic list

**بخش مربوط از متن کاربر:**
```
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
اتصال تلگرام به بخش تمرین با استاد هوش مصنوعی

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:1-15` — `imports` — محل افزودن import برای telegramBot و sessionManager
  ```jsx
  import express from 'express';
  import cors from 'cors';
  import { fileURLToPath } from 'url';
  import { dirname, join } from 'path';
  import dotenv from 'dotenv';
  import { createServer } from 'http';
  import { WebSocketServer, WebSocket } from 'ws';
  import multer from 'multer';
  import fs from 'fs';
  import os from 'os';
  import ffmpeg from 'fluent-ffmpeg';
  import ffmpegStatic from 'ffmpeg-static';
  ```
- `backend/server.js:38-45` — `server initialization` — بعد از این بخش، باید webhook route تلگرام اضافه شود
  ```jsx
  const app = express();
  const PORT = process.env.PORT || 3001;
  
  // Create HTTP server
  const server = createServer(app);
  
  // Create WebSocket server
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/server.js:166-169` — `health check endpoint` — الگوی مناسب برای افزودن endpoint تلگرام
  ```jsx
  // Health check
  app.get('/api/health', (req, res) => {
    res.json({ status: 'ok', timestamp: new Date().toISOString() });
  });
  ```
- `backend/.env.example:1-8` — `environment variables` — افزودن TELEGRAM_BOT_TOKEN و TELEGRAM_WEBHOOK_URL به این فایل
  ```
  # Gemini API Key - Get from https://aistudio.google.com/apikey
  GEMINI_API_KEY=your_gemini_api_key_here
  
  # Port (optional, default 3001)
  PORT=3001
  
  # Firebase config (optional - for data persistence)
  # VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/package.json` (سطر 1) — نیاز به افزودن وابستگی node-telegram-bot-api
- `frontend/index.html` (سطر 11) — شامل Firebase config که برای احراز هویت تلگرام استفاده می‌شود
- `frontend/src/App.jsx` (سطر 1) — کامپوننت اصلی فرانت‌اند که بخش تمرین با استاد هوش مصنوعی در آن قرار دارد
- `backend/server.js` (سطر 698) — فایل اصلی بک‌اند که webhook تلگرام به آن اضافه می‌شود

## 🌐 نقشهٔ وابستگی‌ها
این قابلیت به فایل‌های جدیدی نیاز دارد: backend/services/telegramBot.js (هندلرهای تلگرام)، backend/services/topicService.js (مدیریت موضوعات)، backend/services/s

## 🔍 Context و وضعیت فعلی
کاربر درخواست کرده است که قابلیت اتصال به بخش 'تمرین با استاد هوش مصنوعی' از طریق تلگرام فراهم شود. این درخواست بخشی از یک نیاز بزرگ‌تر برای 'نوتیفیکیشن از طریق تلگرام و تلگرام دو طرفه' است که در کامیت 7bddd3c ثبت شده است. کاربر می‌خواهد دستور /practice در تلگرام ایجاد شود که کاربر را به بخش تمرین متصل کند، موضوعات موجود را نمایش دهد، و امکان انتخاب موضوع را فراهم کند. کاربر تصریح کرده است که 'مکالمه واقعی با هوش مصنوعی یا ارسال پیام صوتی' خارج از این مرحله است. کلیدواژه‌های اصلی: /practice, topic selection, AI tutor session, authentication, session management, topic list. در کد فعلی پروژه (backend/server.js) هیچ endpoint یا WebSocket handler برای تلگرام وجود ندارد. WebSocket در خط 45 با مسیر '/ws/live' ایجاد شده است. frontend/index.html شامل Firebase config (خطوط 11-18) و یک Inspector Bridge Script (خطوط 32-201) است. backend/.env.example شامل فیلدهای GEMINI_API_KEY و VITE_FIREBASE_CONFIG است. backend/package.json شامل وابستگی‌های express, cors, dotenv, ws, multer, fluent-ffmpeg, ffmpeg-static, pdf-parse است. frontend/package.json شامل react, react-dom, firebase, lucide-react است. هیچ فایل یا سرویس تلگرام (مانند node-telegram-bot-api یا telegraf) در وابستگی‌ها وجود ندارد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. افزودن کتابخانه node-telegram-bot-api به backend/package.json. 2. ایجاد فایل جدید backend/services/telegramBot.js که شامل: a) راه‌اندازی bot با توکن از env (TELEGRAM_BOT_TOKEN). b) هندلر دستور /start برای خوش‌آمدگویی. c) هندلر دستور /practice که: - کاربر را احراز هویت می‌کند (با استفاده از Firebase Auth یا یک token موقت). - لیست موضوعات موجود را از یک سرویس topic service دریافت می‌کند. - موضوعات را به صورت دکمه‌های شیشه‌ای (Inline Keyboard) نمایش می‌دهد. - با انتخاب موضوع، یک session ID ایجاد کرده و به کاربر اعلام می‌کند. d) هندلر callback_query برای پردازش انتخاب موضوع. 3. ایجاد فایل backend/services/topicService.js برای مدیریت موضوعات (فعلاً با داده‌های استاتیک یا از Firebase). 4. ایجاد فایل backend/services/sessionManager.js برای مدیریت session های کاربران (ذخیره chatId, topicId, status). 5. افزودن route جدید در backend/server.js برای Webhook تلگرام (POST /api/telegram/webhook). 6. به‌روزرسانی backend/.env.example با فیلدهای TELEGRAM_BOT_TOKEN و TELEGRAM_WEBHOOK_URL. 7. خارج از این مرحله: مکالمه واقعی با هوش مصنوعی و ارسال پیام صوتی.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 8: پیاده‌سازی مکالمه متنی با هوش مصنوعی از طریق تلگرام

**Scope:** این مرحله شامل اتصال چت تلگرام به موتور هوش مصنوعی (مثلاً OpenAI API یا مدل داخلی) برای مکالمه متنی است. پیام‌های متنی کاربر باید به AI ارسال شده و پاسخ به تلگرام برگردد. باید context session حفظ شود. خارج از این مرحله: پیام صوتی، تحلیل صوت، یا پاسخ صوتی.
**Key terms:** AI chat, OpenAI API, conversation context, message streaming, session history, text-to-text

**بخش مربوط از متن کاربر:**
```
و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
اتصال چت تلگرام به موتور هوش مصنوعی برای مکالمه متنی

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:56-114` — `app.post('/api/gemini/chat')` — این endpoint اصلی چت با Gemini است. ربات تلگرام باید از همین endpoint برای ارسال پیام‌های کاربر و دریافت پاسخ استفاده کند. نیاز به بازنویسی یا افزودن قابلیت session history دارد.
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
      if (payload.contents) {
        payload.contents = payload.contents.map(content => ({
          role: content.role,
          parts: content.parts.map(part => {
            if (part.inline_data && includeAudio) {
              return { inline_data: part.inline_data };
            }
            if (part.text !== undefined) {
              return { text: part.text };
            }
            return null;
          }).filter(p => p !== null)
        }));
      }
      const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${GEMINI_API_KEY}`;
      const response = await fetch(apiUrl, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(payload)
      });
      if (!response.ok) {
        const errorData = await response.text();
        console.error('Gemini API error:', errorData);
        return res.status(response.status).json({ error: errorData });
      }
      const result = await response.json();
      if (includeAudio) {
        res.json(result);
      } else {
        const text = result.candidates?.[0]?.content?.parts?.[0]?.text;
        if (!text) {
          return res.status(500).json({ error: 'No text in response' });
        }
        res.json({ text });
      }
    } catch (error) {
      console.error('Chat API error:', error);
      res.status(500).json({ error: error.message });
    }
  });
  ```
- `backend/server.js:44-45` — `WebSocketServer` — WebSocket موجود در پروژه. ربات تلگرام می‌تواند از این WebSocket برای ارسال نوتیفیکیشن‌های بلادرنگ به فرانت‌اند استفاده کند.
  ```jsx
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/.env.example:1-8` — `env_example` — فایل env.example باید با متغیر TELEGRAM_BOT_TOKEN به‌روز شود.
  ```
  # Gemini API Key - Get from https://aistudio.google.com/apikey
  GEMINI_API_KEY=your_gemini_api_key_here
  
  # Port (optional, default 3001)
  PORT=3001
  
  # Firebase config (optional - for data persistence)
  # VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/package.json` (سطر 1) — نیاز به افزودن وابستگی node-telegram-bot-api یا grammy به dependencies
- `frontend/index.html` (سطر 11) — شامل Firebase config (خطوط 11-18) که می‌تواند برای ذخیره session history استفاده شود
- `frontend`

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی مکالمه متنی با هوش مصنوعی از طریق تلگرام را دارد. این مرحله شامل اتصال چت تلگرام به موتور هوش مصنوعی (مثلاً OpenAI API یا مدل داخلی) برای مکالمه متنی است. پیام‌های متنی کاربر باید به AI ارسال شده و پاسخ به تلگرام برگردد. باید context session حفظ شود. خارج از این مرحله: پیام صوتی، تحلیل صوت، یا پاسخ صوتی. کاربر می‌خواهد 'از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم'. کلیدواژه‌ها: AI chat, OpenAI API, conversation context, message streaming, session history, text-to-text. پروژه فعلی یک اپلیکیشن آموزش لهجه لبنانی با React (frontend) و Express.js (backend) است که از Gemini API استفاده می‌کند. در backend/server.js خطوط 56-114 endpoint /api/gemini/chat برای چت با Gemini وجود دارد. همچنین در backend/server.js خط 45 یک WebSocket server در path /ws/live وجود دارد. backend/server.js از express, cors, ws, dotenv, multer, fluent-ffmpeg استفاده می‌کند. frontend از React, Vite, Tailwind, Firebase, lucide-react استفاده می‌کند. در حال حاضر هیچ اتصالی به تلگرام در پروژه وجود ندارد. تسک‌های قبلی (کامیت 7bddd3c و task dc721af2) به موضوع 'نوتیفیکیشن از طریق تلگرام و تلگرام دو طرفه' اشاره دارند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک فایل جدید backend/services/telegramBot.js که یک ربات تلگرام با استفاده از کتابخانه node-telegram-bot-api (یا grammy) راه‌اندازی کند. 2. در این فایل، Webhook یا polling برای دریافت پیام‌های متنی از تلگرام تنظیم شود. 3. برای هر پیام متنی دریافتی، session ID بر اساس chatId کاربر ایجاد شود. 4. پیام به همراه history session به endpoint /api/gemini/chat (که در backend/server.js خط 56-114 تعریف شده) ارسال شود. 5. پاسخ دریافتی از Gemini API به همان chatId در تلگرام ارسال شود. 6. context session با استفاده از یک ساختار ساده درون‌حافظه‌ای (Map) یا دیتابیس (Firebase که در frontend/index.html خط 11-18 کانفیگ شده) حفظ شود. 7. یک endpoint جدید در backend/server.js برای health check ربات تلگرام اضافه شود. 8. متغیر محیطی TELEGRAM_BOT_TOKEN به backend/.env.example اضافه شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 9: پیاده‌سازی ارسال پیام صوتی از تلگرام به هوش مصنوعی برای تحلیل

**Scope:** این مرحله شامل قابلیت دریافت پیام صوتی از کاربر در تلگرام، تبدیل آن به متن (Speech-to-Text)، ارسال متن به هوش مصنوعی، و دریافت پاسخ است. باید از APIهای تبدیل صوت به متن (مثل Whisper) استفاده شود. خارج از این مرحله: تولید پاسخ صوتی.
**Key terms:** voice message, Speech-to-Text, Whisper API, audio processing, voice transcription, telegram voice handler

**بخش مربوط از متن کاربر:**
```
و از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی دریافت پیام صوتی تلگرام و ارسال به Whisper برای تحلیل

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:790-800` — `audio processing block` — این بخش کد فعلی برای پردازش فایل‌های صوتی از طریق API آپلود شده به Gemini است. می‌توان از همین منطق برای پردازش پیام‌های صوتی تلگرام استفاده کرد.
  ```jsx
  } else if (mimeType.startsWith('audio/')) {
            console.log(`Audio file: ${fileName}, size: ${fileSizeMB.toFixed(2)} MB`);
  
            try {
              let result;
  
              if (isLargeFile) {
                // For large audio files, use Gemini File API with streaming
                console.log(`Using File API for large audio: ${fileName}`);
                const uploadedFileName = await uploadToGeminiFileAPI(filePath, mimeType, fileName, fileSize);
  ```
- `backend/server.js:297-392` — `uploadToGeminiFileAPI` — این تابع برای آپلود فایل‌های صوتی به Gemini File API استفاده می‌شود. برای پیام‌های صوتی تلگرام، باید فایل را از Telegram دریافت کرده و به این تابع ارسال کرد.
  ```jsx
  async function uploadToGeminiFileAPI(filePathOrBuffer, mimeType, displayName, fileSize = null) {
    const isFilePath = typeof filePathOrBuffer === 'string';
    const actualSize = fileSize || (isFilePath ? fs.statSync(filePathOrBuffer).size : filePathOrBuffer.length);
  
    console.log(`Uploading file to Gemini File API: ${displayName}, size: ${(actualSize / 1024 / 1024).toFixed(2)} MB, from: ${isFilePath ? 'disk' : 'memory'}`);
  ```
- `backend/server.js:45` — `WebSocketServer` — WebSocket موجود برای ارتباطات زنده. می‌توان از آن برای ارسال پاسخ تحلیل به تلگرام استفاده کرد.
  ```jsx
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
JavaScript (Node.js backend با Express)، WebSocket (ws)، multer برای آپلود فایل، Gemini API برای تحلیل، Firebase برای ذخیره‌سازی، OpenAI Whisper API برای Speech-to-Text

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `frontend/index.html` (سطر 11) — حاوی Firebase config (خطوط 11-18) که می‌تواند برای ذخیره‌سازی فایل‌های صوتی استفاده شود
- `backend/package.json` (سطر 1) — نیاز به اضافه کردن وابستگی‌های جدید مانند node-fetch یا axios برای Telegram API
- `backend/.env.example` (سطر 1) — نیاز به اضافه کردن متغیرهای محیطی جدید مانند TELEGRAM_BOT_TOKEN و OPENAI_API_KEY
- `frontend/src/App.jsx` (سطر 1) — کامپوننت اصلی فرانت‌اند که ممکن است نیاز به نمایش وضعیت پیام صوتی داشته باشد
- `prompt/task-dc721af2-f84f-424d-a491-91e8ab3624c0.md` (سطر 1) — تسک مرتبط با پیاده‌سازی نوتیفیکیشن تلگرام دو طرفه که این قابلیت مکمل آن است

## 🌐 نقشهٔ وابستگی‌ها
این قابلیت به فایل‌های backend/server.js (برای endpoint و WebSocket)، backend/package.json (برای وابستگی‌های جدید)، backend/.env.example (برای متغیرهای محیطی)، frontend/index.html (برای Firebase)، و frontend/src/App.jsx (برای نمایش وضعیت) وابسته است. تابع uploadToGeminiFileAPI در خطوط 297-392 و analyzeWithGemini در خطوط 273-294 از backend/server.js برای پردازش صوت استفاده می‌شوند. WebSocket در خط 45 برای ارتباطات زنده استفاده می‌شود.

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی ارسال پیام صوتی از تلگرام به هوش مصنوعی برای تحلیل را دارد. این مرحله شامل قابلیت دریافت پیام صوتی از کاربر در تلگرام، تبدیل آن به متن (Speech-to-Text) با استفاده از APIهای تبدیل صوت به متن (مثل Whisper)، ارسال متن به هوش مصنوعی، و دریافت پاسخ است. خارج از این مرحله: تولید پاسخ صوتی. بخش مربوط از درخواست اصلی کاربر: 'و از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه'. کلیدواژه‌ها: voice message, Speech-to-Text, Whisper API, audio processing, voice transcription, telegram voice handler. در کد فعلی پروژه (backend/server.js)، قابلیت پردازش فایل‌های صوتی از طریق APIهای Gemini وجود دارد: خطوط 790-800 مربوط به پردازش فایل‌های صوتی (audio/)، خطوط 297-392 مربوط به آپلود فایل به Gemini File API، و خطوط 273-294 مربوط به تحلیل محتوا با Gemini. همچنین WebSocket در خط 45 برای ارتباطات زنده راه‌اندازی شده است. پروژه از Firebase (frontend/index.html خطوط 11-18) استفاده می‌کند که می‌تواند برای ذخیره‌سازی فایل‌های صوتی استفاده شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک endpoint جدید در backend/server.js برای دریافت پیام صوتی از تلگرام (POST /api/telegram/voice) که فایل صوتی را دریافت کرده و با استفاده از Whisper API (OpenAI) یا Gemini File API (موجود در خطوط 297-392) متن را استخراج کند. 2. استفاده از تابع uploadToGeminiFileAPI (خط 297) برای آپلود فایل صوتی و سپس analyzeWithGemini (خط 273) برای تحلیل متن استخراج‌شده. 3. اضافه کردن یک WebSocket handler جدید در خط 45 برای ارسال پاسخ تحلیل به تلگرام. 4. ایجاد یک سرویس جدید backend/services/telegramService.js برای مدیریت ارتباط با Telegram Bot API. 5. استفاده از multer (خطوط 25-33) برای دریافت فایل صوتی از تلگرام. 6. ذخیره‌سازی موقت فایل‌های صوتی در uploadDir (خط 20). 7. اضافه کردن کتابخانه node-fetch یا axios برای ارتباط با Telegram API و Whisper API.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 10: پیاده‌سازی پاسخ صوتی از هوش مصنوعی به تلگرام (Text-to-Speech)

**Scope:** این مرحله شامل تولید پاسخ صوتی از متن پاسخ هوش مصنوعی و ارسال آن به کاربر در تلگرام است. باید از APIهای Text-to-Speech (مثل Google TTS یا ElevenLabs) استفاده شود. پاسخ صوتی باید به صورت فایل صوتی (OGG/MP3) به تلگرام ارسال شود. خارج از این مرحله: ذخیره‌سازی طولانی‌مدت فایل‌های صوتی.
**Key terms:** Text-to-Speech, voice response, audio file, Google TTS, ElevenLabs, telegram audio message

**بخش مربوط از متن کاربر:**
```
و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی پاسخ صوتی AI به تلگرام (TTS)

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:117-164` — `app.post('/api/gemini/tts')` — Endpoint TTS موجود که می‌توان از آن برای تولید پاسخ صوتی استفاده کرد. خروجی base64 audioData و mimeType است.
  ```jsx
  app.post('/api/gemini/tts', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const { prompt, voice = 'Kore' } = req.body;
      const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-preview-tts:generateContent?key=${GEMINI_API_KEY}`;
  ```
- `backend/server.js:45` — `WebSocketServer` — WebSocket موجود برای ارتباط real-time با فرانت‌اند. می‌توان از آن برای ارسال همزمان متن و صدا استفاده کرد.
  ```jsx
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/server.js:56-114` — `app.post('/api/gemini/chat')` — Endpoint چت اصلی که پس از دریافت پاسخ، باید متن آن به TTS ارسال شود.
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
JavaScript (Node.js), Express, WebSocket, Gemini API, Telegram Bot API, Firebase

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/.env.example` (سطر 1) — برای اضافه کردن متغیر محیطی TELEGRAM_BOT_TOKEN
- `frontend/index.html` (سطر 10) — حاوی Firebase config که برای احراز هویت کاربران تلگرام استفاده می‌شود
- `backend/package.json` (سطر 1) — برای اضافه کردن وابستگی node-telegram-bot-api یا axios برای ارتباط با Telegram API

## 🌐 نقشهٔ وابستگی‌ها
این قابلیت به backend/server.js (endpoint TTS و Chat) وابسته است. یک فایل جدید backend/services/telegramService.js ایجاد می‌شود که از Telegram Bot API برای ارسال فایل صوتی استفاده می‌کند. backend/server.js باید این سرویس را import کرده و پس از دریافت پاسخ از Gemini Chat، به صورت خودکار TTS را فراخوانی کرده و نتیجه را به تلگرام ارسال کند. متغیر محیطی TELEGRAM_BOT_TOKEN به backend/.env.example اضافه می‌شود.

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی پاسخ صوتی از هوش مصنوعی به تلگرام (Text-to-Speech) را داده است. این مرحله شامل تولید پاسخ صوتی از متن پاسخ هوش مصنوعی و ارسال آن به کاربر در تلگرام است. باید از APIهای Text-to-Speech (مثل Google TTS یا ElevenLabs) استفاده شود. پاسخ صوتی باید به صورت فایل صوتی (OGG/MP3) به تلگرام ارسال شود. خارج از این مرحله: ذخیره‌سازی طولانی‌مدت فایل‌های صوتی. بخش مربوط از درخواست اصلی کاربر: 'و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....' کلیدواژه‌ها: Text-to-Speech, voice response, audio file, Google TTS, ElevenLabs, telegram audio message. در کد فعلی پروژه (backend/server.js) یک endpoint TTS در خطوط 117-164 با مسیر /api/gemini/tts وجود دارد که از مدل gemini-2.5-flash-preview-tts استفاده می‌کند و خروجی base64 audioData و mimeType برمی‌گرداند. همچنین یک WebSocket سرور در خط 45 با مسیر /ws/live وجود دارد. پروژه از Firebase برای احراز هویت استفاده می‌کند (frontend/index.html خطوط 10-19).

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] فایل backend/services/telegramService.js ایجاد شده و تابع sendVoiceMessage را export کند
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک سرویس جدید در backend برای مدیریت ارسال پیام‌های تلگرام (telegramService.js). 2. در این سرویس، تابعی برای ارسال فایل صوتی به تلگرام با استفاده از Bot API (sendAudio) پیاده‌سازی شود. 3. پس از دریافت پاسخ از Gemini Chat API (endpoint /api/gemini/chat در خطوط 56-114 backend/server.js)، متن پاسخ را به endpoint TTS موجود (/api/gemini/tts در خطوط 117-164) ارسال کرده و خروجی base64 audioData را دریافت کند. 4. فایل صوتی حاصل (با فرمت OGG/MP3) از طریق Telegram Bot API به کاربر ارسال شود. 5. WebSocket فعلی (خط 45) برای ارسال همزمان متن و صدا به فرانت‌اند نیز استفاده شود. 6. متغیر محیطی TELEGRAM_BOT_TOKEN به backend/.env.example اضافه شود.

## 💡 نمونه‌های قبل/بعد
**افزودن تابع ارسال صدا به تلگرام در telegramService.js**

_قبل:_
```
// فایل جدید - وجود ندارد
```

_بعد:_
```
import TelegramBot from 'node-telegram-bot-api';

const TELEGRAM_BOT_TOKEN = process.env.TELEGRAM_BOT_TOKEN;
const bot = new TelegramBot(TELEGRAM_BOT_TOKEN, { polling: false });

export async function sendVoiceMessage(chatId, audioBase64, mimeType) {
  const audioBuffer = Buffer.from(audioBase64, 'base64');
  await bot.sendAudio(chatId, audioBuffer, {
    contentType: mimeType || 'audio/ogg',
  });
}
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 11: همگام‌سازی دوطرفه چت تلگرام با چت وب (Real-time Sync)

**Scope:** این مرحله شامل همگام‌سازی لحظه‌ای پیام‌ها بین چت تلگرام و چت وب (بخش تمرین با استاد هوش مصنوعی) است. هر پیام ارسالی در تلگرام باید در وب ظاهر شود و بالعکس. باید از WebSocket یا polling برای sync استفاده شود. خارج از این مرحله: مدیریت فایل‌های صوتی یا تاریخچه.
**Key terms:** WebSocket, real-time sync, message queue, chat synchronization, telegram-web bridge, event-driven

**بخش مربوط از متن کاربر:**
```
و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....
```

## 🎯 هدف (خلاصه ساختاریافته)
همگام‌سازی دوطرفه چت تلگرام با چت وب (Real-time Sync)

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست همگام‌سازی لحظه‌ای پیام‌ها بین چت تلگرام و چت وب (بخش تمرین با استاد هوش مصنوعی) را دارد. هر پیام ارسالی در تلگرام باید در وب ظاهر شود و بالعکس. باید از WebSocket یا polling برای sync استفاده شود. خارج از این مرحله: مدیریت فایل‌های صوتی یا تاریخچه. کاربر اشاره کرده: 'و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد'. کلیدواژه‌ها: WebSocket, real-time sync, message queue, chat synchronization, telegram-web bridge, event-driven. در کد فعلی، backend/server.js خط ۴۵ یک WebSocketServer روی path '/ws/live' ایجاد کرده (const wss = new WebSocketServer({ server, path: '/ws/live' });) که می‌تواند برای sync استفاده شود. frontend/index.html شامل یک Inspector Bridge Script است که از postMessage برای ارتباط با parent استفاده می‌کند. backend/server.js خط ۵۶-۱۱۳ endpoint /api/gemini/chat برای چت با Gemini دارد. backend/server.js خط ۱۱۷-۱۶۴ endpoint /api/gemini/tts برای تبدیل متن به گفتار دارد. backend/server.js خط ۶۹۸-۸۰۰ endpoint /api/analyze-files برای تحلیل فایل دارد. backend/package.json شامل وابستگی‌های express, cors, dotenv, ws, multer, fluent-ffmpeg, ffmpeg-static است. frontend/package.json شامل react, firebase, lucide-react است. backend/.env.example شامل GEMINI_API_KEY و Firebase config است. frontend/vite.config.js پروکسی /api به localhost:3001 دارد. backend/server.js خط ۳۸-۴۲ HTTP server و Express app ایجاد شده. backend/server.js خط ۴۵ WebSocketServer روی path '/ws/live' ایجاد شده. backend/server.js خط ۵۳ GEMINI_API_KEY از env خوانده شده. backend/server.js خط ۵۶-۱۱۳ endpoint /api/gemini/chat. backend/server.js خط ۱۱۷-۱۶۴ endpoint /api/gemini/tts. backend/server.js خط ۱۶۷-۱۶۹ endpoint /api/health. backend/server.js خط ۱۷۲-۱۹۲ endpoint /api/list-models. backend/server.js خط ۱۹۵-۲۴۰ endpoint /api/test-gemini. backend/server.js خط ۶۹۸-۸۰۰ endpoint /api/analyze-files. frontend/index.html خط ۹-۲۰ Firebase config و app id. frontend/index.html خط ۳۲-۲۰۱ Inspector Bridge Script. frontend/src/App.jsx (deep-read نشده). prompt/archive/task-141249ee-0d23-44e0-aeec-21f989820b60.md (deep-read شده). prompt/archive/task-2a0673ad-13dd-4131-9c72-18a093d60686.md (deep-read شده). prompt/archive/task-2d1f95e8-b5ea-413f-b1ec-11b7bed6eeb7.md (deep-read شده). prompt/archive/task-3336cbc2-34e9-4f8f-8216-78d40b813199.md (deep-read شده). prompt/archive/task-38433cce-3e63-4851-89bc-4320f55f4d19.md (deep-read شده). prompt/archive/task-47bfef80-d77a-4386-9dca-fb1a533ca20b.md (deep-read شده). prompt/archive/task-59cbc244-8dbe-4b9d-9a39-a3633ffd0464.md (deep-read شده). prompt/archive/task-5bd04995-7545-4582-acb9-20c453d383a9.md (deep-read شده). prompt/archive/task-6225900f-6226-487c-9ae1-55dbd114e165.md (deep-read شده). prompt/archive/task-645bbdc2-f205-479f-b01f-f8b8ad4ef2e4.md (deep-read شده). prompt/archive/task-6d989ea3-b525-4566-994a-ce401ae7373a.md (deep-read شده). prompt/archive/task-7599ae4a-1d12-4c87-80c2-b16e5876d9d4.md (deep-read شده). prompt/archive/task-8ac8249d-e1de-40f8-ad4b-36e271a1b237.md (deep-read شده). prompt/archive/task-9d4c545b-6683-46f1-8121-8ae6705d2122.md (deep-read شده). prompt/archive/task-adbe7499-a96f-4c38-8c32-e547a3b7e2c2.md (deep-read شده). prompt/archive/task-b3d7759b-2054-457f-a5ce-54dc346fb960.md (deep-read شده). prompt/archive/task-ba60df37-44e8-426e-8bf8-4a48cbedb11b.md (deep-read شده). prompt/archive/task-c7474d4f-8730-4531-b8ec-50de5a444b82.md (deep-read شده). prompt/archive/task-d13296fb-53a8-45a4-919b-de4f984a5a64.md (deep-read شده).

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ۱. ایجاد یک سرویس جدید در backend به نام 'backend/services/telegramBot.js' که با استفاده از کتابخانه 'node-telegram-bot-api' (نیاز به نصب) یک بات تلگرام ایجاد کند. ۲. در backend/server.js، WebSocket موجود (خط ۴۵) را گسترش دهیم تا پیام‌های چت وب را دریافت و به تلگرام ارسال کند. ۳. ایجاد یک endpoint جدید در backend/server.js مانند 'POST /api/telegram/send-message' که پیام را از وب دریافت و به تلگرام ارسال کند. ۴. ایجاد یک endpoint جدید 'POST /api/telegram/webhook' که پیام‌های دریافتی از تلگرام را دریافت و از طریق WebSocket به کلاینت‌های وب broadcast کند. ۵. در frontend/src/App.jsx (نیاز به deep-read)، یک WebSocket client اضافه کنیم که به 'ws://localhost:3001/ws/live' متصل شود و پیام‌های دریافتی از تلگرام را در چت وب نمایش دهد. ۶. در frontend/src/App.jsx، هنگام ارسال پیام در چت وب، پیام را هم به endpoint /api/telegram/send-message و هم به WebSocket ارسال کنیم. ۷. در backend/server.js، تابع broadcastToTelegram ایجاد کنیم که پیام را از طریق بات تلگرام به کاربر ارسال کند. ۸. در backend/server.js، تابع broadcastToWeb ایجاد کنیم که پیام را از طریق WebSocket به همه کلاینت‌های متصل ارسال کند.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 12: مدیریت session و تاریخچه تمرینات از طریق تلگرام

**Scope:** این مرحله شامل قابلیت مشاهده تاریخچه تمرینات قبلی، ادامه session‌های ناقص، و مدیریت session‌ها از طریق تلگرام است. دستورات /history, /continue, /sessions پیاده‌سازی می‌شوند. خارج از این مرحله: تحلیل پیشرفت یا آمار.
**Key terms:** /history, /continue, /sessions, session management, practice history, resume session

**بخش مربوط از متن کاربر:**
```
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن مدیریت session و تاریخچه تمرینات از طریق تلگرام با دستورات /history, /continue, /sessions

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:38-48` — `app initialization and WebSocket server` — در اینجا باید endpointهای جدید برای session management اضافه شوند و همچنین WebSocket برای ارتباط بلادرنگ با تلگرام (اختیاری).
  ```jsx
  const app = express();
  const PORT = process.env.PORT || 3001;
  const server = createServer(app);
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  app.use(cors());
  app.use(express.json({ limit: '10mb' }));
  ```
- `backend/server.js:56-114` — `app.post('/api/gemini/chat')` — این endpoint برای چت با Gemini است
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست کرده است که قابلیت مدیریت session و تاریخچه تمرینات از طریق تلگرام به پروژه اضافه شود. این شامل مشاهده تاریخچه تمرینات قبلی، ادامه session‌های ناقص، و مدیریت session‌ها از طریق تلگرام است. دستورات /history, /continue, /sessions باید پیاده‌سازی شوند. کاربر همچنین اشاره کرده: 'و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم'. خارج از این مرحله: تحلیل پیشرفت یا آمار. کلیدواژه‌های اصلی: /history, /continue, /sessions, session management, practice history, resume session. در کد فعلی پروژه، backend/server.js شامل WebSocket server در خط 45 (const wss = new WebSocketServer({ server, path: '/ws/live' })) و endpointهای مختلف Gemini API است. frontend/index.html شامل Firebase config و inspector bridge است. هیچ endpoint یا handler تلگرام در کد فعلی وجود ندارد. فایل‌های prompt/archive/task-141249ee-0d23-44e0-aeec-21f989820b60.md و task-2a0673ad-13dd-4131-9c72-18a093d60686.md و task-3336cbc2-34e9-4f8f-8216-78d40b813199.md و task-38433cce-3e63-4851-89bc-4320f55f4d19.md و task-47bfef80-d77a-4386-9dca-fb1a533ca20b.md و task-59cbc244-8dbe-4b9d-9a39-a3633ffd0464.md و task-5bd04995-7545-4582-acb9-20c453d383a9.md و task-6225900f-6226-487c-9ae1-55dbd114e165.md و task-645bbdc2-f205-479f-b01f-f8b8ad4ef2e4.md و task-6d989ea3-b525-4566-994a-ce401ae7373a.md و task-7599ae4a-1d12-4c87-80c2-b16e5876d9d4.md و task-8ac8249d-e1de-40f8-ad4b-36e271a1b237.md و task-9d4c545b-6683-46f1-8121-8ae6705d2122.md و task-adbe7499-a96f-4c38-8c32-e547a3b7e2c2.md و task-b3d7759b-2054-457f-a5ce-54dc346fb960.md و task-ba60df37-44e8-426e-8bf8-4a48cbedb11b.md و task-c7474d4f-8730-4531-b8ec-50de5a444b82.md و task-d13296fb-53a8-45a4-919b-de4f984a5a64.md در پوشه prompt/archive موجود هستند اما محتوای آن‌ها در deep context ارائه نشده است. backend/package.json شامل dependencies: express, cors, dotenv, ws, multer, fluent-ffmpeg, ffmpeg-static است. frontend/package.json شامل react, firebase, lucide-react است. برای پیاده‌سازی این قابلیت، نیاز به ایجاد یک سرویس تلگرام (احتمالاً با کتابخانه node-telegram-bot-api یا grammy) و endpointهای REST برای مدیریت session‌ها است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/services/telegramBot.js برای مدیریت ربات تلگرام با دستورات /history, /continue, /sessions. 2. ایجاد فایل جدید backend/services/sessionManager.js برای ذخیره و بازیابی session‌های تمرین (با Firebase یا فایل JSON). 3. ایجاد endpointهای REST در backend/server.js: GET /api/sessions (لیست session‌های کاربر)، GET /api/sessions/:id (جزئیات session)، POST /api/sessions/:id/continue (ادامه session ناقص). 4. در frontend/src/App.jsx، افزودن دکمه یا لینک برای اتصال به ربات تلگرام (با deep link). 5. ذخیره chatId تلگرام در پروفایل کاربر (Firebase). 6. پیاده‌سازی دستور /history برای نمایش ۵ تمرین آخر. 7. پیاده‌سازی دستور /continue برای ادامه آخرین session ناقص. 8. پیاده‌سازی دستور /sessions برای نمایش لیست session‌های فعال.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 13: پیاده‌سازی امنیت و احراز هویت برای دستورات تلگرام

**Scope:** این مرحله شامل احراز هویت کاربران تلگرام و محدود کردن دستورات به کاربران مجاز است. باید از chat ID verification یا OAuth استفاده شود. دستورات حساس (مثل حذف محتوا) نیاز به تأیید اضافی دارند. خارج از این مرحله: مدیریت کاربران یا نقش‌ها.
**Key terms:** authentication, authorization, chat ID whitelist, two-factor, secure commands, access control

**بخش مربوط از متن کاربر:**
```
بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی امنیت و احراز هویت دستورات تلگرام

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:1-14` — `import section` — محل افزودن import برای ماژول احراز هویت تلگرام و کتابخانه Telegram Bot API
  ```jsx
  import express from 'express';
  import cors from 'cors';
  import { fileURLToPath } from 'url';
  import { dirname, join } from 'path';
  import dotenv from 'dotenv';
  import { createServer } from 'http';
  import { WebSocketServer, WebSocket } from 'ws';
  import multer from 'multer';
  import fs from 'fs';
  import os from 'os';
  import ffmpeg from 'fluent-ffmpeg';
  import ffmpegStatic from 'ffmpeg-static';
  ```
- `backend/server.js:38-48` — `app initialization and middleware` — محل افزودن middleware احراز هویت برای Webhook تلگرام و مسیردهی به ماژول telegramAuth
  ```jsx
  const app = express();
  const PORT = process.env.PORT || 3001;
  
  // Create HTTP server
  const server = createServer(app);
  
  // Create WebSocket server
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  
  app.use(cors());
  app.use(express.json({ limit: '10mb' }));
  ```
- `backend/.env.example:1-8` — `environment variables` — محل افزودن متغیرهای TELEGRAM_BOT_TOKEN و AUTHORIZED_CHAT_IDS برای پیکربندی ربات تلگرام
  ```
  # Gemini API Key - Get from https://aistudio.google.com/apikey
  GEMINI_API_KEY=your_gemini_api_key_here
  
  # Port (optional, default 3001)
  PORT=3001
  
  # Firebase config (optional - for data persistence)
  # VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `frontend/index.html` (سطر 11) — شامل Firebase config (apiKey, authDomain, projectId, storageBucket, messagingSenderId, appId) که می‌توان برای OAuth احراز هویت تلگرام استفاده کرد
- `.github/workflows/claude-auto-task.yml` (سطر 79) — نمونه‌ای از ارتباط با backend خارجی از طریق OVERSIGHT_BACKEND_URL و OVERSIGHT_EXTERNAL_TOKEN که الگوی احراز هویت مشابهی دارد
- `backend/package.json` (سطر 1) — نیاز به افزودن وابستگی node-telegram-bot-api یا axios برای ارتباط با Telegram Bot API
- `frontend/package.json` (سطر 1) — شامل firebase dependency که می‌توان برای OAuth و مدیریت کاربران استفاده کرد

## 🌐 نقشهٔ وابستگی‌ها
این قابلیت به فایل‌های backend/server.js (برای افزودن endpoint Webhook و middleware)، backend/.env.example (برای متغیرهای محیطی جدید)، backend/package.json (برای وابستگی جدید Telegram Bot API)، frontend/index.html (برای Firebase config جهت OAuth) وابسته است. همچنین نیاز به ایجاد فایل جدید backend/services/telegramAuth.js دارد. در backend/server.js، middleware احراز هویت باید قبل از endpointهای حساس (مثل POST /api/analyze-files در خط 698) قرار گیرد. متغیرهای محیطی TELEGRAM_BOT_TOKEN و AUTHORIZED_CHAT

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی امنیت و احراز هویت برای دستورات تلگرام را دارد. این مرحله شامل احراز هویت کاربران تلگرام و محدود کردن دستورات به کاربران مجاز است. باید از chat ID verification یا OAuth استفاده شود. دستورات حساس (مثل حذف محتوا) نیاز به تأیید اضافی دارند. خارج از این مرحله: مدیریت کاربران یا نقش‌ها. بخش مربوط از درخواست اصلی کاربر: 'بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم'. کلیدواژه‌ها: authentication, authorization, chat ID whitelist, two-factor, secure commands, access control. در کد فعلی پروژه (backend/server.js) هیچ پیاده‌سازی برای Webhook یا Bot تلگرام وجود ندارد. تنها WebSocket در خط 45 با path '/ws/live' تعریف شده. فایل backend/.env.example فقط شامل GEMINI_API_KEY و PORT و Firebase config است. فایل .github/workflows/claude-auto-task.yml از OVERSIGHT_BACKEND_URL و OVERSIGHT_EXTERNAL_TOKEN برای ارتباط با backend استفاده می‌کند. فایل frontend/index.html حاوی Firebase config (apiKey, authDomain, projectId, storageBucket, messagingSenderId, appId) است که می‌توان از Firebase Authentication برای OAuth استفاده کرد. backend/package.json شامل dependencies: express, cors, dotenv, ws, multer, fluent-ffmpeg, ffmpeg-static, pdf-parse است. frontend/package.json شامل firebase, lucide-react, react, react-dom است. هیچ فایل یا تابعی برای مدیریت تلگرام در پروژه وجود ندارد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/services/telegramAuth.js برای مدیریت احراز هویت تلگرام. 2. پیاده‌سازی middleware برای اعتبارسنجی chat ID بر اساس لیست سفید (whitelist) در فایل .env یا دیتابیس Firebase. 3. ایجاد endpoint جدید در backend/server.js برای Webhook تلگرام (POST /api/telegram/webhook) که پیام‌های دریافتی را پردازش کند. 4. پیاده‌سازی تابع isAuthorizedChat(chatId) که chat ID را با لیست مجاز مقایسه کند. 5. برای دستورات حساس (مثل حذف محتوا)، پیاده‌سازی تأیید دو مرحله‌ای با ارسال کد تأیید به همان چت و انتظار برای پاسخ. 6. افزودن متغیر محیطی TELEGRAM_BOT_TOKEN و AUTHORIZED_CHAT_IDS به backend/.env.example. 7. نصب کتابخانه node-telegram-bot-api یا axios برای ارتباط با Telegram Bot API. 8. استفاده از Firebase Authentication (که در frontend/index.html پیکربندی شده) برای OAuth به عنوان روش جایگزین احراز هویت.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 14: تست و دیباگ کامل یکپارچه‌سازی تلگرام با سیستم

**Scope:** این مرحله شامل تست کامل تمام قابلیت‌های پیاده‌سازی شده: ارسال نوتیفیکیشن، دستورات کنترلی، مشاهده/آپلود محتوا، مکالمه با AI، voice input/output، و sync. باید unit tests و integration tests نوشته شود. خارج از این مرحله: تست performance یا load testing.
**Key terms:** unit tests, integration tests, telegram bot testing, mock API, end-to-end test, error handling

**بخش مربوط از متن کاربر:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه ... و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
تست و دیباگ یکپارچه‌سازی تلگرام با سیستم

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:45-46` — `WebSocketServer` — WebSocket موجود در پروژه — می‌توان از آن برای real-time notification به تلگرام استفاده کرد
  ```jsx
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/server.js:56-114` — `app.post('/api/gemini/chat')` — Endpoint مکالمه با AI — باید از تلگرام هم قابل دسترسی باشد
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
  ```
- `backend/server.js:117-164` — `app.post('/api/gemini/tts')` — Endpoint Text-to-Speech — برای voice output در تلگرام
  ```jsx
  app.post('/api/gemini/tts', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
  ```
- `backend/server.js:698-800` — `app.post('/api/analyze-files')` — Endpoint تحلیل فایل — برای آپلود و تحلیل محتوا از تلگرام
  ```jsx
  app.post('/api/analyze-files', upload.array('files', 10), handleMulterError, async (req, res) => {
    console.log('Received file analysis request');
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/package.json` (سطر 1) — برای اضافه کردن وابستگی‌های جدید (node-telegram-bot-api, jest, supertest) نیاز به بروزرسانی دارد
- `backend/.env.example` (سطر 1) — برای اضافه کردن TELEGRAM_BOT_TOKEN و TELEGRAM_CHAT_ID به متغیرهای محیطی نیاز به بروزرسانی دارد
- `frontend/src/App.jsx` (سطر 1) — کامپوننت اصلی فرانت‌اند — ممکن است نیاز به نمایش وضعیت اتصال تلگرام داشته باشد
- `.github/workflows/claude-auto-task.yml` (سطر 1) — برای اجرای خودکار تست‌ها در CI/CD نیاز به بروزرسانی دارد

## 🌐 نقشهٔ وابستگی‌ها
این تسک وابسته به ایجاد فایل‌های جدید است: backend/services/telegramBot.js (سرویس اصلی تلگرام), backend/services/notificationService.js (مدیریت نوتیفیکیشن), backend/tests/telegramBot.test.js (unit tests), backend/tests/integration/telegramIntegration.test.js (integration tests), backend/tests/mocks/telegramMock.js (mock Telegram API), backend/tests/mocks/geminiMock.js (mock Gemini API). فایل‌های موجود backend/server.js (خطوط 56, 117, 698) باید از طریق سرویس تلگرام قابل دسترسی باشند. فایل backend/package.json نیاز به اضافه کردن وابستگی‌های: node-telegram-bot-api, jest, supertest, nock دارد. فایل backend/.env.example نیاز به اضافه کردن TELEGRAM_BOT_TOKEN و TELEGRAM_CH

## 🔍 Context و وضعیت فعلی
کاربر درخواست تست و دیباگ کامل یکپارچه‌سازی تلگرام با سیستم را دارد. این مرحله شامل تست کامل تمام قابلیت‌های پیاده‌سازی شده است: ارسال نوتیفیکیشن، دستورات کنترلی، مشاهده/آپلود محتوا، مکالمه با AI، voice input/output، و sync. کاربر مشخص کرده که باید unit tests و integration tests نوشته شود و خارج از این مرحله: تست performance یا load testing. بر اساس commit 7bddd3c با پیام 'میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه' و همچنین متن اصلی کاربر: 'میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه ... و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم'. کلیدواژه‌های ذکر شده: unit tests, integration tests, telegram bot testing, mock API, end-to-end test, error handling. در کد فعلی پروژه (backend/server.js) هیچ پیاده‌سازی مرتبط با تلگرام وجود ندارد — فایل‌های backend/server.js (خطوط 1-800) شامل APIهای مربوط به Gemini Chat (خط 56), TTS (خط 117), File Analysis (خط 698) و WebSocket (خط 45) است. فایل frontend/index.html شامل Firebase config و Inspector Bridge است. فایل .github/workflows/claude-auto-task.yml شامل workflow خودکار Claude Code است. هیچ فایل تلگرام‌بات یا سرویس نوتیفیکیشنی در ساختار پروژه دیده نمی‌شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. با توجه به عدم وجود فایل‌های مرتبط با تلگرام در کدبیس فعلی، ابتدا باید فایل‌های سرویس تلگرام ایجاد شوند. مراحل پیشنهادی: 1) ایجاد فایل backend/services/telegramBot.js شامل کلاس TelegramBot با متدهای: sendNotification, handleCommand, handleMessage, handleVoice, handleMedia. 2) ایجاد فایل backend/services/notificationService.js برای مدیریت نوتیفیکیشن‌ها. 3) ایجاد فایل backend/tests/telegramBot.test.js شامل unit tests برای هر متد. 4) ایجاد فایل backend/tests/integration/telegramIntegration.test.js شامل integration tests. 5) ایجاد فایل backend/tests/mocks/telegramMock.js برای mock کردن Telegram API. 6) ایجاد فایل backend/tests/mocks/geminiMock.js برای mock کردن Gemini API. 7) تست‌های end-to-end برای سناریوهای: ارسال نوتیفیکیشن، دستورات کنترلی (/start, /help, /chat), مشاهده/آپلود محتوا, مکالمه با AI, voice input/output, sync. 8) تست error handling برای خطاهای: API key نامعتبر, timeout, invalid message format, network failure.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

## ✅ معیارهای پذیرش کلی (همهٔ مراحل)
- [ ] {'text': 'فایل backend/services/telegramService.js ایجاد شده و تابع sendVoiceMessage را export کند', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['sendVoiceMessage', 'export async function sendVoiceMessage'], 'files_hint': ['backend/services/telegramService']}}

## Acceptance Criteria

1. فایل backend/services/telegramService.js ایجاد شده و تابع sendVoiceMessage را export کند _(verify: static)_

## Task Steps

### Step 1: ایجاد ساختار پایه برای اتصال تلگرام دوطرفه با سیستم
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک ماژول پایه برای ارتباط با Telegram Bot API است. باید یک کلاس پایه برای ارسال و دریافت پیام‌ها از طریق Webhook یا Polling ایجاد شود. این مرحله فقط شامل ایجاد ساختار پایه و اتصال اولیه است و شامل منطق خاص ارسال اطلاعات دروس یا کنترل سیستم نمی‌شود. نکته حیاتی: باید از کتابخانه python-telegram-bot یا aiogram استفاده شود و Webhook به صورت امن با token تنظیم شود.
**Excerpt:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

### Step 2: پیاده‌سازی ارسال نوتیفیکیشن‌های خودکار از سیستم به تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک سرویس نوتیفیکیشن است که رویدادهای مشخص سیستم (مثل ثبت‌نام جدید، تغییر وضعیت، اتمام مهلت) را به صورت خودکار به کاربر از طریق تلگرام ارسال کند. باید یک صف پیام یا event-driven mechanism پیاده‌سازی شود. خارج از این مرحله: ارسال محتوای دروس یا کنترل سیستم.
**Excerpt:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته
```

### Step 3: پیاده‌سازی دستورات کنترلی پایه از طریق تلگرام (Command Handlers)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل تعریف و پیاده‌سازی دستورات متنی پایه است که کاربر می‌تواند از طریق تلگرام به سیستم ارسال کند. مثال: /status برای وضعیت سیستم، /courses برای لیست دروس، /help برای راهنما. این دستورات باید به ماژول‌های مربوطه در سیستم متصل شوند. خارج از این مرحله: آپلود فایل، مدیریت محتوای دروس، یا اتصال به هوش مصنوعی.
**Excerpt:**
```
بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

### Step 4: ایجاد ماژول مشاهده محتوای دروس از طریق تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد قابلیت مشاهده لیست دروس، جلسات، و محتوای متنی دروس از طریق تلگرام است. باید دستورات /courses, /course_{id}, /session_{id} پیاده‌سازی شوند. محتوا باید به صورت خلاصه و قابل مرور در تلگرام نمایش داده شود. خارج از این مرحله: آپلود محتوا، ویرایش، یا اتصال به هوش مصنوعی.
**Excerpt:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

### Step 5: پیاده‌سازی آپلود محتوای دروس از طریق تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل قابلیت آپلود فایل‌های درسی (PDF، تصویر، ویدئو) از طریق تلگرام به سیستم است. باید یک دستور /upload ایجاد شود که فایل ارسالی را دریافت کرده و به درس/جلسه مشخص متصل کند. باید validation و ذخیره‌سازی امن فایل‌ها پیاده‌سازی شود. خارج از این مرحله: ویرایش محتوا یا اتصال به هوش مصنوعی.
**Excerpt:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

### Step 6: ایجاد مدیریت کامل دروس از طریق تلگرام (ویرایش، حذف، سازماندهی)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل دستورات پیشرفته‌تر برای مدیریت دروس از طریق تلگرام است: ویرایش توضیحات درس، حذف جلسه، جابجایی محتوا، تغییر ترتیب. باید از inline keyboards و confirmation dialogs استفاده شود. خارج از این مرحله: اتصال به هوش مصنوعی یا تمرین با استاد.
**Excerpt:**
```
و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

### Step 7: اتصال تلگرام به بخش 'تمرین با استاد هوش مصنوعی' - احراز هویت و انتخاب موضوع
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد قابلیت اتصال به بخش تمرین با استاد هوش مصنوعی از طریق تلگرام است. باید دستور /practice ایجاد شود که کاربر را به بخش تمرین متصل کند، موضوعات موجود را نمایش دهد، و امکان انتخاب موضوع را فراهم کند. خارج از این مرحله: مکالمه واقعی با هوش مصنوعی یا ارسال پیام صوتی.
**Excerpt:**
```
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم
```

### Step 8: پیاده‌سازی مکالمه متنی با هوش مصنوعی از طریق تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل اتصال چت تلگرام به موتور هوش مصنوعی (مثلاً OpenAI API یا مدل داخلی) برای مکالمه متنی است. پیام‌های متنی کاربر باید به AI ارسال شده و پاسخ به تلگرام برگردد. باید context session حفظ شود. خارج از این مرحله: پیام صوتی، تحلیل صوت، یا پاسخ صوتی.
**Excerpt:**
```
و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم
```

### Step 9: پیاده‌سازی ارسال پیام صوتی از تلگرام به هوش مصنوعی برای تحلیل
**Status:** `pending` (0%)
**Scope:** این مرحله شامل قابلیت دریافت پیام صوتی از کاربر در تلگرام، تبدیل آن به متن (Speech-to-Text)، ارسال متن به هوش مصنوعی، و دریافت پاسخ است. باید از APIهای تبدیل صوت به متن (مثل Whisper) استفاده شود. خارج از این مرحله: تولید پاسخ صوتی.
**Excerpt:**
```
و از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه
```

### Step 10: پیاده‌سازی پاسخ صوتی از هوش مصنوعی به تلگرام (Text-to-Speech)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل تولید پاسخ صوتی از متن پاسخ هوش مصنوعی و ارسال آن به کاربر در تلگرام است. باید از APIهای Text-to-Speech (مثل Google TTS یا ElevenLabs) استفاده شود. پاسخ صوتی باید به صورت فایل صوتی (OGG/MP3) به تلگرام ارسال شود. خارج از این مرحله: ذخیره‌سازی طولانی‌مدت فایل‌های صوتی.
**Excerpt:**
```
و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....
```

### Step 11: همگام‌سازی دوطرفه چت تلگرام با چت وب (Real-time Sync)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل همگام‌سازی لحظه‌ای پیام‌ها بین چت تلگرام و چت وب (بخش تمرین با استاد هوش مصنوعی) است. هر پیام ارسالی در تلگرام باید در وب ظاهر شود و بالعکس. باید از WebSocket یا polling برای sync استفاده شود. خارج از این مرحله: مدیریت فایل‌های صوتی یا تاریخچه.
**Excerpt:**
```
و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....
```

### Step 12: مدیریت session و تاریخچه تمرینات از طریق تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل قابلیت مشاهده تاریخچه تمرینات قبلی، ادامه session‌های ناقص، و مدیریت session‌ها از طریق تلگرام است. دستورات /history, /continue, /sessions پیاده‌سازی می‌شوند. خارج از این مرحله: تحلیل پیشرفت یا آمار.
**Excerpt:**
```
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم
```

### Step 13: پیاده‌سازی امنیت و احراز هویت برای دستورات تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل احراز هویت کاربران تلگرام و محدود کردن دستورات به کاربران مجاز است. باید از chat ID verification یا OAuth استفاده شود. دستورات حساس (مثل حذف محتوا) نیاز به تأیید اضافی دارند. خارج از این مرحله: مدیریت کاربران یا نقش‌ها.
**Excerpt:**
```
بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

### Step 14: تست و دیباگ کامل یکپارچه‌سازی تلگرام با سیستم
**Status:** `pending` (0%)
**Scope:** این مرحله شامل تست کامل تمام قابلیت‌های پیاده‌سازی شده: ارسال نوتیفیکیشن، دستورات کنترلی، مشاهده/آپلود محتوا، مکالمه با AI، voice input/output، و sync. باید unit tests و integration tests نوشته شود. خارج از این مرحله: تست performance یا load testing.
**Excerpt:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه ... و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم
```
