---
task_id: dc721af2-f84f-424d-a491-91e8ab3624c0
title: میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار (6 مرحله)
type: other
priority: medium
execution_priority: 3000
status: pending
external_status: pending
verification_status: pending
watched_id: 6b04f8bd-b562-4fb9-9cd9-0c27458ced59
project: mahdighandi1989/language
created_at: '2026-05-12T11:13:22.960230+00:00'
updated_at: '2026-05-29T16:04:41.298792+00:00'
target_files:
- backend/server.js
- backend/.env.example
- frontend/src/App.jsx
- frontend/index.html
---

# میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار (6 مرحله)

## Raw Idea

میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم و از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


---

## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند.)_

```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم و از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....
```

## 📋 چک‌لیست مراحل (6 مرحله)

این تسک به مراحل کوچک‌تر تقسیم شده. **در هر verify خودکار، وضعیت هر مرحله به‌صورت `[ ]` (انجام نشده)، `[~]` (ناقص)، یا `[x]` (انجام شده) به‌روز می‌شود.**
وقتی تمام مراحل `[x]` شدند، تسک به‌طور خودکار به «انجام شده» منتقل می‌شود.

- [ ] **مرحله 1: پیاده‌سازی نوتیفیکیشن یک‌طرفه تلگرام برای ارسال اطلاعات از سیستم** — ایجاد یک بات تلگرام و اتصال آن به سیستم برای ارسال خودکار نوتیفیکیشن‌ها و اطلاعات تعیین‌شده (مثل وضعیت سیستم، هشدارها و ...). این مرحله شامل دریافت پیام از کاربر نیست.
- [ ] **مرحله 2: پیاده‌سازی قابلیت دریافت و پردازش دستورات از تلگرام (کنترل دوطرفه)** — اضافه کردن قابلیت دریافت پیام‌ها و دستورات از کاربر تلگرام و اجرای عملیات مشخص روی سیستم (مثل اجرای دستورات کنترلی). این مرحله شامل مدیریت محتوای دروس نیست.
- [ ] **مرحله 3: اضافه کردن قابلیت مشاهده و آپلود محتوای دروس از طریق تلگرام** — ایجاد قابلیت مشاهده لیست دروس، مشاهده محتوای هر درس و آپلود فایل‌های جدید از طریق تلگرام. این مرحله شامل مدیریت تمرین با استاد هوش مصنوعی نیست.
- [ ] **مرحله 4: اتصال بخش تمرین با استاد هوش مصنوعی به تلگرام برای مدیریت موضوعات و انتخاب** — اضافه کردن قابلیت مشاهده و انتخاب موضوعات تمرین با استاد هوش مصنوعی از طریق تلگرام. این مرحله شامل چت مستقیم با هوش مصنوعی نیست.
- [ ] **مرحله 5: یکپارچه‌سازی چت صوتی و متنی با هوش مصنوعی از طریق تلگرام** — ایجاد قابلیت ارسال پیام متنی و صوتی از تلگرام به چت هوش مصنوعی، دریافت پاسخ (متن و صوت) و نمایش آن در تلگرام. شامل استفاده از میکروفون برای ضبط و ارسال صوت به سیستم آنالیز.
- [ ] **مرحله 6: نوشتن تست‌های یکپارچه‌سازی و نهایی برای کل قابلیت‌های تلگرام** — نوشتن تست‌های خودکار برای تمام مراحل قبلی شامل ارسال نوتیفیکیشن، دریافت دستورات، مدیریت محتوا، و چت با هوش مصنوعی از طریق تلگرام.

---

# 🔹 مرحله 1: پیاده‌سازی نوتیفیکیشن یک‌طرفه تلگرام برای ارسال اطلاعات از سیستم

**Scope:** ایجاد یک بات تلگرام و اتصال آن به سیستم برای ارسال خودکار نوتیفیکیشن‌ها و اطلاعات تعیین‌شده (مثل وضعیت سیستم، هشدارها و ...). این مرحله شامل دریافت پیام از کاربر نیست.
**Key terms:** Telegram Bot API, نوتیفیکیشن, ارسال اطلاعات

**بخش مربوط از متن کاربر:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی نوتیفیکیشن یک‌طرفه تلگرام برای ارسال اطلاعات از سیستم

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:1-12` — `import section` — محل افزودن import سرویس تلگرام و کتابخانه node-telegram-bot-api
  ```jsx
  import express from 'express';
  import cors from 'cors';
  import { fileURLToPath } from 'url';
  import { dirname, join } from 'path';
  import dotenv from 'dotenv';
  import { createServer } from 'http';
  import { WebSocketServer, WebSocket } from 'ws';
  import multer from 'multer';
  import fs from 'fs';
  import os from 'os';
  import ffmpeg from 'fluent-ffmpeg';
  import ffmpegStatic from 'ffmpeg-static';
  ```
- `backend/server.js:53-54` — `GEMINI_API_KEY constant` — الگوی مشابه برای خواندن TELEGRAM_BOT_TOKEN و TELEGRAM_CHAT_ID از environment variables
  ```jsx
  const GEMINI_API_KEY = process.env.GEMINI_API_KEY;
  ```
- `backend/server.js:166-169` — `Health check endpoint` — محل مناسب برای افزودن endpoint جدید POST /api/telegram/notify در کنار سایر endpointهای API
  ```jsx
  // Health check
  app.get('/api/health', (req, res) => {
    res.json({ status: 'ok', timestamp: new Date().toISOString() });
  });
  ```
- `backend/.env.example:1-8` — `Environment variables template` — افزودن متغیرهای TELEGRAM_BOT_TOKEN و TELEGRAM_CHAT_ID به این فایل
  ```
  # Gemini API Key - Get from https://aistudio.google.com/apikey
  GEMINI_API_KEY=your_gemini_api_key_here
  
  # Port (optional, default 3001)
  PORT=3001
  
  # Firebase config (optional - for data persistence)
  # VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: Express.js, WebSocket (ws), multer, ffmpeg, Gemini API, Firebase. کتابخانه‌های مرتبط: node-telegram-bot-api (برای Telegram Bot API).

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/package.json` (سطر 10) — نیاز به افزودن dependency node-telegram-bot-api برای ارتباط با Telegram Bot API
- `frontend/src/App.jsx` (سطر 8) — می‌تواند از نوتیفیکیشن‌های تلگرام برای نمایش وضعیت به کاربر استفاده کند (مثلاً از طریق WebSocket)
- `frontend/index.html` (سطر 32) — Inspector Bridge Script موجود در این فایل می‌تواند برای ارسال خطاهای فرانت‌اند به backend و سپس به تلگرام استفاده شود
- `render.yaml` (سطر 1) — نیاز به افزودن متغیرهای محیطی TELEGRAM_BOT_TOKEN و TELEGRAM_CHAT_ID برای استقرار در Render

## 🌐 نقشهٔ وابستگی‌ها
این قابلیت به فایل‌های backend/server.js (برای endpoint جدید و import سرویس)، backend/.env.example (برای متغیرهای محیطی)، backend/package.json (برای dependency جدید node-telegram-bot-api)، و render.yaml (برای متغیرهای محیطی در استقرار) وابسته است. سرویس telegramNotifier.js باید در backend ایجاد شود. endpoint جدید POST /api/telegram/notify در server.js به روتر express اضافه می‌شود. همچنین می‌توان از WebSocket موجود (خط 45) برای ارسال خودکار نوتیفیکیشن‌ها در زمان بروز خطا استفاده کرد.

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی نوتیفیکیشن یک‌طرفه تلگرام برای ارسال اطلاعات از سیستم را دارد. این شامل ایجاد یک بات تلگرام و اتصال آن به سیستم برای ارسال خودکار نوتیفیکیشن‌ها و اطلاعات تعیین‌شده (مثل وضعیت سیستم، هشدارها و ...) است. کاربر تاکید کرده که این مرحله شامل دریافت پیام از کاربر نیست و صرفاً ارسال یک‌طرفه است. کلیدواژه‌های اصلی: Telegram Bot API, نوتیفیکیشن, ارسال اطلاعات. در کد فعلی پروژه (backend/server.js) هیچ اشاره‌ای به Telegram Bot API یا سرویس نوتیفیکیشن نشده است. پروژه از Express.js (خط 1)، WebSocket (خط 7)، multer (خط 8)، ffmpeg (خط 11) و Gemini API (خطوط 56-114, 117-164, 273-294) استفاده می‌کند. فایل frontend/src/App.jsx شامل Inspector Bridge Script (خطوط 1-126) و Firebase integration (خطوط 137-139) است. backend/.env.example شامل متغیرهای GEMINI_API_KEY, PORT, VITE_FIREBASE_CONFIG است. backend/package.json شامل dependencies: cors, dotenv, express, ws است. برای پیاده‌سازی این قابلیت، نیاز به افزودن یک سرویس جدید در backend و یک endpoint جدید برای ارسال نوتیفیکیشن‌ها به تلگرام است.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] بات تلگرام با موفقیت ایجاد شده و توکن آن در متغیر محیطی TELEGRAM_BOT_TOKEN ذخیره شود
- [ ] endpoint POST /api/telegram/notify در backend/server.js پیاده‌سازی شود و پیام را به تلگرام ارسال کند
- [ ] نوتیفیکیشن‌های خودکار برای خطاهای سیستم (مثلاً خطای Gemini API در خط 111) ارسال شوند
- [ ] نوتیفیکیشن‌های وضعیت سیستم (مثلاً وضعیت WebSocket در خط 45) قابل ارسال باشند
- [ ] متن پیام‌ها به صورت پویا و قابل تنظیم باشد
- [ ] خطاهای ارسال نوتیفیکیشن به درستی لاگ شوند و باعث کرش سرور نشوند
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/services/telegramNotifier.js با یک کلاس/ماژول برای ارسال پیام به تلگرام با استفاده از Telegram Bot API. 2. افزودن متغیر محیطی TELEGRAM_BOT_TOKEN و TELEGRAM_CHAT_ID به backend/.env.example. 3. ایجاد endpoint جدید POST /api/telegram/notify در backend/server.js که از سرویس telegramNotifier استفاده کند. 4. افزودن dependency node-telegram-bot-api به backend/package.json. 5. پیاده‌سازی تابع sendNotification(message, type) که type می‌تواند 'info', 'warning', 'error', 'system_status' باشد. 6. اتصال به سیستم‌های موجود مثل WebSocket (خط 45) و Gemini API (خط 56) برای ارسال خودکار نوتیفیکیشن در صورت خطا یا رویدادهای خاص. 7. افزودن middleware برای لاگ کردن خطاها و ارسال خودکار به تلگرام.

## 💡 نمونه‌های قبل/بعد
**افزودن متغیر محیطی تلگرام به .env.example**

_قبل:_
```
# Firebase config (optional - for data persistence)
# VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}
```

_بعد:_
```
# Firebase config (optional - for data persistence)
# VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}

# Telegram Bot Configuration
# Get bot token from https://t.me/BotFather
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here
```

**افزودن endpoint نوتیفیکیشن تلگرام به server.js**

_قبل:_
```
// Health check
app.get('/api/health', (req, res) => {
  res.json({ status: 'ok', timestamp: new Date().toISOString() });
});
```

_بعد:_
```
// Health check
app.get('/api/health', (req, res) => {
  res.json({ status: 'ok', timestamp: new Date().toISOString() });
});

// Telegram Notification API
app.post('/api/telegram/notify', async (req, res) => {
  const { message, type = 'info' } = req.body;
  if (!message) {
    return res.status(400).json({ error: 'Message is required' });
  }
  try {
    const result = await telegramNotifier.sendNotification(message, type);
    res.json({ success: true, result });
  } catch (error) {
    console.error('Telegram notification error:', error);
    res.status(500).json({ error: error.message });
  }
});
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `curl -X POST http://localhost:3001/api/telegram/notify -H 'Content-Type: application/json' -d '{"message":"Test notification","type":"info"}'`
- `node -e "const tn = require('./backend/services/telegramNotifier'); tn.sendNotification('Test', 'info').then(console.log).catch(console.error)"`
- `npm test -- --grep 'telegram' (در صورت وجود تست)`
- `بررسی لاگ‌های سرور برای تأیید ارسال موفق پیام`

## ⚠️ ریسک‌ها و موارد احتیاط
افزودن dependency جدید (node-telegram-bot-api) به backend/package.json ممکن است با نسخه‌های موجود Node.js (>=18) ناسازگار باشد. endpoint جدید POST /api/telegram/notify باید rate-limited شود تا از spam جلوگیری کند. توکن بات تلگرام در environment variables باید به صورت امن مدیریت شود و در logها نمایش داده نشود. خطاهای Telegram API (مثل invalid token یا chat not found) باید به درستی هندل شوند تا باعث 500 Internal Server Error نشوند. WebSocket موجود در خط 45 ممکن است نیاز به تغییر داشته باشد تا رویدادهای جدید را پشتیبانی کند.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 2: پیاده‌سازی قابلیت دریافت و پردازش دستورات از تلگرام (کنترل دوطرفه)

**Scope:** اضافه کردن قابلیت دریافت پیام‌ها و دستورات از کاربر تلگرام و اجرای عملیات مشخص روی سیستم (مثل اجرای دستورات کنترلی). این مرحله شامل مدیریت محتوای دروس نیست.
**Key terms:** Telegram Bot API, دستورات, کنترل سیستم, Webhook

**بخش مربوط از متن کاربر:**
```
بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی قابلیت دریافت و پردازش دستورات از تلگرام (کنترل دوطرفه سیستم)

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:38-48` — `app و server و wss` — محل فعلی ایجاد اپلیکیشن Express و سرور HTTP. باید endpoint جدید برای Webhook تلگرام به اینجا اضافه شود.
  ```jsx
  const app = express();
  const PORT = process.env.PORT || 3001;
  
  // Create HTTP server
  const server = createServer(app);
  
  // Create WebSocket server
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  
  app.use(cors());
  app.use(express.json({ limit: '10mb' }));
  ```
- `backend/server.js:166-169` — `GET /api/health` — این endpoint می‌تواند به عنوان یکی از دستورات کنترلی تلگرام (مثل /status) فراخوانی شود تا وضعیت سیستم را برگرداند.
  ```jsx
  // Health check
  app.get('/api/health', (req, res) => {
    res.json({ status: 'ok', timestamp: new Date().toISOString() });
  });
  ```
- `backend/server.js:171-192` — `GET /api/list-models` — این endpoint می‌تواند به عنوان دستور /list-models در تلگرام فراخوانی شود تا لیست مدل‌های موجود Gemini را نمایش دهد.
  ```jsx
  // List available models
  app.get('/api/list-models', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
  
    try {
      const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models?key=${GEMINI_API_KEY}`;
      const response = await fetch(apiUrl);
      const data = await response.json();
  
      if (!response.ok) {
        return res.status(response.status).json({ error: data, keyPrefix: GEMINI_API_KEY.substring(0, 10) + '...' });
      }
  
      // Return just model names for easier reading
      const modelNames = data.models?.map(m => m.name) || [];
      res.json({ models: modelNames, count: modelNames.length, keyPrefix: GEMINI_API_KEY.substring(0, 10) + '...' });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  });
  ```
- `backend/.env.example:1-8` — `فایل نمونه متغیرهای محیطی` — باید متغیر محیطی TELEGRAM_BOT_TOKEN به این فایل اضافه شود تا توکن ربات تلگرام در آن ذخیره شود.
  ```
  # Gemini API Key - Get from https://aistudio.google.com/apikey
  GEMINI_API_KEY=your_gemini_api_key_here
  
  # Port (optional, default 3001)
  PORT=3001
  
  # Firebase config (optional - for data persistence)
  # VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack تشخیص داده شده در بالا = JavaScript (Node.js برای بک‌اند با Express.js، React برای فرانت‌اند با Vite، Firebase برای احراز هویت و دیتابیس، WebSocket برای ارتباط زنده). برای تلگرام از کتابخانه `node-telegram-bot-api` یا `telegraf` استفاده خواهد شد.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/package.json` (سطر 10) — برای اضافه کردن وابستگی‌های جدید مانند `node-telegram-bot-api` یا `telegraf` به این فایل نیاز است.
- `frontend/src/App.jsx` (سطر 4) — این فایل شامل Inspector Bridge Script است که از WebSocket استفاده می‌کند. ممکن است نیاز به هماهنگی با Webhook تلگرام برای نمایش وضعیت در فرانت‌اند باشد.
- `frontend/index.html` (سطر 32) — این فایل نیز شامل Inspector Bridge Script است. ممکن است برای نمایش اعلان‌های تلگرام در فرانت‌اند استفاده شود.
- `render.yaml` (سطر 1) — اگر پروژه روی Render مستقر است، ممکن است نیاز به تنظیمات اضافی برای Webhook تلگرام (مثل health check endpoint) در این فایل باشد.

## 🌐 نقشهٔ وابستگی‌ها
این قابلیت به فایل‌های `backend/server.js` (برای اضافه کردن endpoint جدید)، `backend/.env.example` (برای اضافه کردن متغیر محیطی)، `backend/package.json` (برای اضافه کردن وابستگی‌ها)، و یک فایل جدید `backend/services/telegramBot.js` وابسته است. همچنین ممکن است با Inspector Bridge Script در `frontend/src/App.jsx` و `frontend/index.html` برای نمایش وضعیت در فرانت‌اند تعامل داشته باشد. APIهای موجود در `backend/server.js` مانند `/api/health` و `/api/list-models` به عنوان دستورات کنترلی قابل فراخوانی هستند.

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی قابلیت دریافت پیام‌ها و دستورات از کاربر تلگرام و اجرای عملیات مشخص روی سیستم (مثل اجرای دستورات کنترلی) را دارد. این مرحله شامل مدیریت محتوای دروس نیست. بخش مربوط از درخواست اصلی کاربر: 'بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم'. کلیدواژه‌ها: Telegram Bot API, دستورات, کنترل سیستم, Webhook. پروژه فعلی یک اپلیکیشن آموزش لهجه لبنانی با هوش مصنوعی است که از React (فرانت‌اند) و Express.js (بک‌اند) استفاده می‌کند. بک‌اند در `backend/server.js` شامل APIهای مختلفی مانند `/api/gemini/chat` (خط 56)، `/api/gemini/tts` (خط 117)، `/api/health` (خط 167)، `/api/list-models` (خط 172)، `/api/test-gemini` (خط 195) و همچنین WebSocket در مسیر `/ws/live` (خط 45) است. فرانت‌اند در `frontend/src/App.jsx` از Firebase برای احراز هویت و ذخیره‌سازی استفاده می‌کند (خطوط 137-139). همچنین یک Inspector Bridge Script در `frontend/index.html` (خطوط 32-201) و `frontend/src/App.jsx` (خطوط 4-126) وجود دارد که از WebSocket برای ارتباط با یک Inspector استفاده می‌کند. برای پیاده‌سازی این قابلیت، نیاز به ایجاد یک endpoint جدید در بک‌اند برای دریافت Webhook از تلگرام، و همچنین یک سرویس برای پردازش دستورات و اجرای عملیات روی سیستم است.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] یک endpoint جدید `POST /api/telegram/webhook` در `backend/server.js` ایجاد شود که قادر به دریافت Webhook از تلگرام باشد.
- [ ] یک فایل جدید `backend/services/telegramBot.js` ایجاد شود که شامل منطق پردازش دستورات `/start`, `/status`, `/list-models` باشد.
- [ ] دستور `/status` باید وضعیت سیستم را با فراخوانی `GET /api/health` برگرداند.
- [ ] دستور `/list-models` باید لیست مدل‌های Gemini را با فراخوانی `GET /api/list-models` برگرداند.
- [ ] متغیر محیطی `TELEGRAM_BOT_TOKEN` به `backend/.env.example` اضافه شود.
- [ ] ربات تلگرام بتواند پیام تأیید دریافت دستور و نتیجه عملیات را به کاربر ارسال کند.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک فایل جدید `backend/services/telegramBot.js` برای مدیریت Webhook و پردازش دستورات تلگرام. 2. اضافه کردن یک endpoint جدید در `backend/server.js` برای دریافت Webhook از تلگرام (مثلاً `POST /api/telegram/webhook`). 3. پیاده‌سازی منطق پردازش دستورات در فایل جدید (مثل `/start`, `/status`, `/restart`, `/list-models`). 4. استفاده از کتابخانه `node-telegram-bot-api` یا `telegraf` برای تعامل با Telegram Bot API. 5. اضافه کردن متغیر محیطی `TELEGRAM_BOT_TOKEN` به `backend/.env.example`. 6. پیاده‌سازی قابلیت ارسال پیام به کاربر تلگرام برای تأیید دریافت دستور و نتیجه عملیات. 7. اتصال به APIهای موجود در `backend/server.js` برای اجرای دستورات (مثل فراخوانی `/api/list-models` یا `/api/health`).

## 💡 نمونه‌های قبل/بعد
**اضافه کردن endpoint Webhook تلگرام به backend/server.js**

_قبل:_
```
// Health check
app.get('/api/health', (req, res) => {
  res.json({ status: 'ok', timestamp: new Date().toISOString() });
});
```

_بعد:_
```
// Health check
app.get('/api/health', (req, res) => {
  res.json({ status: 'ok', timestamp: new Date().toISOString() });
});

// Telegram Webhook endpoint
app.post('/api/telegram/webhook', async (req, res) => {
  try {
    const update = req.body;
    await telegramBot.handleUpdate(update);
    res.sendStatus(200);
  } catch (error) {
    console.error('Telegram webhook error:', error);
    res.sendStatus(500);
  }
});
```

**اضافه کردن متغیر محیطی به backend/.env.example**

_قبل:_
```
# Firebase config (optional - for data persistence)
# VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}
```

_بعد:_
```
# Firebase config (optional - for data persistence)
# VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}

# Telegram Bot Token - Get from https://t.me/BotFather
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `curl -X POST http://localhost:3001/api/telegram/webhook -H "Content-Type: application/json" -d '{"update_id":123,"message":{"message_id":1,"from":{"id":123456,"is_bot":false,"first_name":"Test"},"chat":{"id":123456,"first_name":"Test"},"text":"/status"}}'`
- `node -e "const bot = require('./backend/services/telegramBot'); console.log('Module loaded successfully');"`
- `npm test -- --testPathPattern=telegram`

## ⚠️ ریسک‌ها و موارد احتیاط
اضافه کردن Webhook تلگرام نیازمند یک endpoint عمومی در اینترنت است که ممکن است امنیت سیستم را به خطر بیندازد. باید از اعتبارسنجی درخواست‌های Webhook با استفاده از توکن تلگرام اطمینان حاصل شود. همچنین، اجرای دستورات کنترلی مانند `/restart` ممکن است باعث قطع سرویس شود و باید با احتیاط پیاده‌سازی شود. فایل `backend/server.js` شامل APIهای متعددی است که ممکن است تحت تأثیر تغییرات قرار گیرند.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 3: اضافه کردن قابلیت مشاهده و آپلود محتوای دروس از طریق تلگرام

**Scope:** ایجاد قابلیت مشاهده لیست دروس، مشاهده محتوای هر درس و آپلود فایل‌های جدید از طریق تلگرام. این مرحله شامل مدیریت تمرین با استاد هوش مصنوعی نیست.
**Key terms:** مشاهده محتوا, آپلود فایل, مدیریت دروس, Telegram

**بخش مربوط از متن کاربر:**
```
بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم
```

## 🎯 هدف (خلاصه ساختاریافته)
اضافه کردن قابلیت مشاهده و آپلود محتوای دروس از طریق تلگرام

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:1-53` — `imports and app setup` — این فایل نقطه شروع backend است. باید endpointهای جدید برای ارتباط با ربات تلگرام و Firebase به این فایل اضافه شود. همچنین نیاز به import کتابخانه node-telegram-bot-api و firebase-admin در اینجا وجود دارد.
  ```jsx
  import express from 'express';
  import cors from 'cors';
  import { fileURLToPath } from 'url';
  import { dirname, join } from 'path';
  import dotenv from 'dotenv';
  import { createServer } from 'http';
  import { WebSocketServer, WebSocket } from 'ws';
  ```
- `frontend/src/App.jsx:136-139` — `Firebase imports` — این فایل شامل تنظیمات Firebase در فرانت‌اند است. برای backend باید از firebase-admin استفاده شود که نیاز به service account دارد. کانفیگ Firebase در frontend/index.html (خطوط 11-18) موجود است.
  ```jsx
  import { initializeApp } from 'firebase/app';
  import { getAuth, signInAnonymously, signInWithCustomToken, onAuthStateChanged } from 'firebase/auth';
  import { getFirestore, doc, onSnapshot, setDoc, setLogLevel } from 'firebase/firestore';
  ```
- `frontend/index.html:11-18` — `Firebase configuration` — این کانفیگ Firebase برای فرانت‌اند است. برای backend باید از firebase-admin با service account استفاده شود. projectId = labaneseapp است.
  ```
  var __firebase_config = JSON.stringify({
          apiKey: "AIzaSyAMFtV0zIMKKe0bIr68Z1wTORQ1jQpsv9Q",
          authDomain: "labaneseapp.firebaseapp.com",
          projectId: "labaneseapp",
          storageBucket: "labaneseapp.firebasestorage.app",
          messagingSenderId: "951874597795",
          appId: "1:951874597795:web:00745327993adad760a016"
        });
  ```
- `backend/.env.example:1-8` — `Environment variables` — این فایل نشان می‌دهد که Firebase قبلاً در نظر گرفته شده اما فعال نیست. باید متغیرهای محیطی برای TELEGRAM_BOT_TOKEN و FIREBASE_SERVICE_ACCOUNT_PATH اضافه شود.
  ```
  # Gemini API Key - Get from https://aistudio.google.com/apikey
  GEMINI_API_KEY=your_gemini_api_key_here
  
  # Port (optional, default 3001)
  PORT=3001
  
  # Firebase config (optional - for data persistence)
  # VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack تشخیص داده شده: JavaScript (Node.js backend با Express، React frontend با Vite). کتابخانه‌های مرتبط: express, cors, ws, multer, fluent-ffmpeg, ffmpeg-static, firebase (فرانت‌اند), firebase-admin (backend), node-telegram-bot-api (نیاز به نصب). دیتابیس: Firebase Firestore. ذخیره‌سازی فایل: Firebase Storage. احراز هویت: Firebase Auth (anonymous).

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/package.json` (سطر 1) — برای اضافه کردن وابستگی‌های جدید مانند node-telegram-bot-api و firebase-admin باید این فایل به‌روزرسانی شود.
- `frontend/src/App.jsx` (سطر 132) — این فایل شامل منطق اصلی فرانت‌اند است. ممکن است نیاز به نمایش اعلان‌های آپلود جدید از طریق WebSocket داشته باشد.
- `backend/server.js` (سطر 45) — WebSocket موجود در خط 45 (const wss = new WebSocketServer({ server, path: '/ws/live' });) می‌تواند برای اطلاع‌رسانی实时 آپلودهای جدید به فرانت‌اند استفاده شود.
- `render.yaml` (سطر 1) — برای استقرار ربات تلگرام در کنار backend فعلی، ممکن است نیاز به تنظیمات جدید در این فایل باشد.

## 🌐 نقشهٔ وابستگی‌ها
این قابلیت به شدت به Firebase وابسته است. در frontend/src/App.jsx از firebase/app، firebase/auth و firebase/firestore استفاده شده است. backend/server.js از express، cors، ws، multer، fluent-ffmpeg و ffmpeg-static استفاده می‌کند. برای پیاده‌سازی ربات تلگرام، نیاز به نصب کتابخانه node-telegram-bot-api (v0.64.0 یا بالاتر) و firebase-admin (v12.0.0 یا بالاتر) است. فایل frontend/index.html شامل کانفیگ Firebase (projectId: labaneseapp) است که باید در backend نیز استفاده شود. WebSocket موجود در backend/server.js (خط 45) می‌تواند برای ارتباط实时 با فرانت‌اند استفاده شود. همچنین multer در backend/server.js (خطوط 25-33) برای آپلود فایل پیکربندی شده که می‌تواند برای آپلود فایل‌های دریافتی از تلگرام نیز استفاده شود.

## 🔍 Context و وضعیت فعلی
کاربر درخواست اضافه کردن قابلیت مشاهده لیست دروس، مشاهده محتوای هر درس و آپلود فایل‌های جدید از طریق تلگرام را دارد. این درخواست بر اساس متن کاربر است: 'بتونم محتوای دروس رو هم از اینجا ببینم یا آپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم'. کلیدواژه‌های اصلی: مشاهده محتوا, آپلود فایل, مدیریت دروس, Telegram. این قابلیت باید به کاربر اجازه دهد تا از طریق ربات تلگرام، لیست دروس موجود را مشاهده کند، محتوای هر درس (شامل متن، فایل‌های صوتی، تصویری و غیره) را ببیند و فایل‌های جدید را آپلود کند. این مرحله شامل مدیریت تمرین با استاد هوش مصنوعی نیست. پروژه فعلی یک اپلیکیشن آموزش لهجه لبنانی با هوش مصنوعی است که از Firebase برای ذخیره‌سازی داده‌ها و احراز هویت استفاده می‌کند. در فایل frontend/src/App.jsx (خطوط 136-139) از firebase/app، firebase/auth و firebase/firestore استفاده شده است. backend/server.js شامل APIهای مختلفی مانند /api/gemini/chat (خط 56)، /api/gemini/tts (خط 117) و /api/health (خط 167) است. ساختار پروژه شامل frontend (React + Vite) و backend (Express + WebSocket) می‌شود.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] کاربر می‌تواند با ارسال دستور /start به ربات تلگرام، پیام خوش‌آمدگویی و راهنما دریافت کند.
- [ ] کاربر می‌تواند با ارسال دستور /lessons، لیست تمام دروس موجود در Firebase Firestore (collection: lessons) را مشاهده کند.
- [ ] کاربر می‌تواند با ارسال دستور /lesson <lessonId>، محتوای کامل یک درس خاص شامل title، content و لیست فایل‌های مرتبط را مشاهده کند.
- [ ] کاربر می‌تواند با ارسال دستور /upload و سپس ارسال یک فایل (صوتی، تصویری، متنی)، فایل را در Firebase Storage در مسیر lessons/{lessonId}/{fileName} آپلود کند.
- [ ] پس از آپلود فایل، یک پیام تأیید به کاربر در تلگرام ارسال شود و اطلاعات فایل در Firestore به‌روزرسانی شود.
- [ ] WebSocket موجود در backend/server.js (path: /ws/live) یک رویداد new-file-uploaded را برای اطلاع‌رسانی به فرانت‌اند ارسال کند.
- [ ] تمامی خطاها (مانند عدم وجود درس، خطای آپلود) با پیام مناسب به کاربر در تلگرام نمایش داده شود.
- [ ] ربات تلگرام بتواند همزمان با چندین کاربر کار کند (state management برای هر کاربر).
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. برای پیاده‌سازی این قابلیت، مراحل زیر پیشنهاد می‌شود:
1. ایجاد یک ربات تلگرام جدید یا استفاده از ربات موجود و دریافت توکن از BotFather.
2. ایجاد یک فایل جدید در backend به نام backend/services/telegramBot.js که شامل منطق اصلی ربات تلگرام باشد.
3. در این فایل، از کتابخانه node-telegram-bot-api (نیاز به نصب دارد) برای ارتباط با API تلگرام استفاده شود.
4. پیاده‌سازی دستورات زیر در ربات:
   - /start: خوش‌آمدگویی و نمایش راهنما
   - /lessons: نمایش لیست دروس موجود (با خواندن از Firebase Firestore)
   - /lesson <id>: نمایش محتوای یک درس خاص
   - /upload: شروع فرآیند آپلود فایل (دریافت فایل و ذخیره در Firebase Storage)
5. اتصال به Firebase با استفاده از کانفیگ موجود در frontend/index.html (خطوط 11-18) و backend/.env.example.
6. ایجاد endpoint جدید در backend/server.js برای هماهنگی بین ربات تلگرام و دیتابیس.
7. استفاده از WebSocket موجود (backend/server.js خط 45) برای اطلاع‌رسانی实时 به فرانت‌اند در صورت آپلود جدید.
8. ذخیره اطلاعات دروس در Firebase Firestore با ساختار: lessons/{lessonId} شامل فیلدهای title, content, files[], createdAt, updatedAt.
9. ذخیره فایل‌های آپلود شده در Firebase Storage با مسیر lessons/{lessonId}/{fileName}.

## 💡 نمونه‌های قبل/بعد
**اضافه کردن endpoint جدید در backend/server.js برای دریافت webhook از تلگرام**

_قبل:_
```
// Health check
app.get('/api/health', (req, res) => {
  res.json({ status: 'ok', timestamp: new Date().toISOString() });
});
```

_بعد:_
```
// Health check
app.get('/api/health', (req, res) => {
  res.json({ status: 'ok', timestamp: new Date().toISOString() });
});

// Telegram webhook endpoint
app.post('/api/telegram/webhook', (req, res) => {
  const update = req.body;
  // Process update from Telegram bot
  telegramBot.processUpdate(update);
  res.sendStatus(200);
});
```

**اضافه کردن متغیر محیطی برای توکن ربات تلگرام در backend/.env.example**

_قبل:_
```
# Firebase config (optional - for data persistence)
# VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}
```

_بعد:_
```
# Firebase config (optional - for data persistence)
# VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}

# Telegram Bot Token - Get from @BotFather
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `npm install node-telegram-bot-api firebase-admin --save`
- `cd backend && node -e "const TelegramBot = require('node-telegram-bot-api'); const bot = new TelegramBot(process.env.TELEGRAM_BOT_TOKEN, {polling: true}); console.log('Bot initialized');"`
- `curl -X POST http://localhost:3001/api/telegram/webhook -H "Content-Type: application/json" -d '{"update_id":123, "message":{"text":"/start", "chat":{"id":12345}}}'`
- `cd frontend && npm run build`

## ⚠️ ریسک‌ها و موارد احتیاط
1. فایل backend/server.js (خط 45) WebSocket را روی path /ws/live ایجاد کرده. اضافه کردن webhook تلگرام ممکن است با مسیرهای موجود تداخل داشته باشد. 2. استفاده از polling برای ربات تلگرام (به جای webhook) ممکن است با محدودیت‌های Render.com (sleep after inactivity) تداخل داشته باشد. 3. Firebase Admin SDK نیاز به service account دارد که نباید در repository عمومی commit شود. 4. فایل frontend/src/App.jsx (خط 8) از WebSocket به آدرس wss://ai-creator-backend-q677.onrender.com استفاده می‌کند که ممکن است در محیط توسعه محلی کار نکند. 5. multer در backend/server.js (خط 32) محدودیت 500MB برای آپلود فایل دارد که ممکن است برای فایل‌های صوتی طولانی کافی نباشد.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 4: اتصال بخش تمرین با استاد هوش مصنوعی به تلگرام برای مدیریت موضوعات و انتخاب

**Scope:** اضافه کردن قابلیت مشاهده و انتخاب موضوعات تمرین با استاد هوش مصنوعی از طریق تلگرام. این مرحله شامل چت مستقیم با هوش مصنوعی نیست.
**Key terms:** تمرین با استاد هوش مصنوعی, مدیریت موضوعات, انتخاب موضوع, Telegram

**بخش مربوط از متن کاربر:**
```
اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
اضافه کردن قابلیت مدیریت و انتخاب موضوعات تمرین با استاد هوش مصنوعی از طریق تلگرام

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:45-46` — `WebSocketServer` — WebSocket endpoint فعلی برای ارتباطات زنده. باید یک endpoint جدید (مثلاً '/ws/topics') برای مدیریت موضوعات تلگرام اضافه شود.
  ```jsx
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `frontend/src/App.jsx:4-125` — `Inspector Bridge Script` — این اسکریپت WebSocket برای Inspector Bridge استفاده می‌شود. می‌توان از الگوی مشابه برای اتصال به WebSocket مدیریت موضوعات تلگرام استفاده کرد.
  ```jsx
  // 🌉 Inspector Bridge Script - Auto-injected
  // ارتباط با Inspector از طریق WebSocket (حل مشکل cross-origin)
  if (typeof window !== 'undefined' && !window.__inspectorBridgeLoaded) {
    window.__inspectorBridgeLoaded = true;
  
    const isInIframe = window !== window.parent;
    const WS_URL = 'wss://ai-creator-backend-q677.onrender.com/api/render/ws/bridge/gh_mahdighandi1989_language';
    let ws = null;
    let wsReady = false;
    let messageQueue = [];
  
    console.log('🌉 Inspector Bridge: Active (WebSocket mode)');
  
    // اتصال WebSocket
    const connectWS = () => {
      if (!WS_URL || WS_URL === 'wss://ai-creator-backend-q677.onrender.com/api/render/ws/bridge/gh_mahdighandi1989_language') return;
      try {
        ws = new WebSocket(WS_URL);
        ws.onopen = () => { ws.send(JSON.stringify({ type: 'register', role: 'bridge' })); };
        ws.onmessage = (event) => {
          try {
            const msg = JSON.parse(event.data);
            if (msg.type === 'registered') {
              wsReady = true;
              console.log('🌉 Inspector Bridge: WebSocket connected');
              messageQueue.forEach(m => ws.send(JSON.stringify(m)));
              messageQueue = [];
              ws.send(JSON.stringify({ type: 'inspector-bridge-ready', pageUrl: window.location.href, isInIframe, timestamp: Date.now() }));
            } else if (msg.type === 'command') {
              handleCommand(msg);
            }
          } catch (e) {}
        };
        ws.onclose = () => { wsReady = false; setTimeout(connectWS, 3000); };
        ws.onerror = () => {};
      } catch (e) {}
    };
  ```
- `frontend/src/App.jsx:137-139` — `Firebase Imports` — Firebase Firestore برای ذخیره‌سازی داده‌ها استفاده می‌شود. می‌توان از آن برای ذخیره موضوعات انتخاب‌شده توسط کاربر از طریق تلگرام استفاده کرد.
  ```jsx
  import { initializeApp } from 'firebase/app';
  import { getAuth, signInAnonymously, signInWithCustomToken, onAuthStateChanged } from 'firebase/auth';
  import { getFirestore, doc, onSnapshot, setDoc, setLogLevel } from 'firebase/firestore';
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack شناسایی‌شده: JavaScript (Node.js backend با Express و ws، React frontend با Vite و Tailwind CSS، Firebase برای احراز هویت و دیتابیس). کتابخانه‌های مرتبط: ws (WebSocket)، firebase (Firestore)، node-telegram-bot-api (برای ربات تلگرام).

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `frontend/src/App.jsx` (سطر 132) — کامپوننت اصلی فرانت‌اند که باید کامپوننت TopicSelector به آن اضافه شود.
- `frontend/index.html` (سطر 1) — فایل HTML اصلی که ممکن است نیاز به اضافه کردن اسکریپت ربات تلگرام داشته باشد.
- `backend/package.json` (سطر 1) — برای اضافه کردن وابستگی 'node-telegram-bot-api' به پروژه.
- `frontend/package.json` (سطر 1) — برای اضافه کردن وابستگی‌های جدید فرانت‌اند (در صورت نیاز).
- `render.yaml` (سطر 1) — فایل کانفیگ Render که ممکن است نیاز به به‌روزرسانی برای WebSocket endpoint جدید داشته باشد.

## 🌐 نقشهٔ وابستگی‌ها
این قابلیت به backend/server.js (برای WebSocket endpoint جدید و مدیریت موضوعات)، frontend/src/App.jsx (برای کامپوننت TopicSelector و اتصال WebSocket)، frontend/index.html (برای اسکریپت‌های اضافی)، و backend/package.json (برای وابستگی node-telegram-bot-api) وابسته است. همچنین از Firebase Firestore (frontend/src/App.jsx خط 139) برای ذخیره‌سازی موضوعات استفاده می‌کند. تغییرات در backend/server.js بر WebSocket endpoint فعلی ('/ws/live') تأثیری نخواهد داشت، اما باید از ایجاد تداخل در مسیرها جلوگیری شود.

## 🔍 Context و وضعیت فعلی
کاربر درخواست کرده است که پس از اتصال به بخش 'تمرین با استاد هوش مصنوعی'، بتواند موضوعات تمرین را از طریق تلگرام مدیریت و انتخاب کند. این قابلیت شامل چت مستقیم با هوش مصنوعی نیست و صرفاً به مدیریت و انتخاب موضوعات (مانند موضوعات درسی یا مکالمه‌ای) از طریق ربات تلگرام اشاره دارد. با بررسی کد پروژه (backend/server.js و frontend/src/App.jsx)، مشخص شد که در حال حاضر هیچ endpoint یا WebSocket route ای برای ارتباط با تلگرام وجود ندارد. پروژه از WebSocket (wss) در backend/server.js خط 45 برای ارتباطات زنده استفاده می‌کند و از Firebase (frontend/src/App.jsx خط 137-139) برای ذخیره‌سازی داده‌ها. همچنین در frontend/src/App.jsx یک Inspector Bridge Script (خط 4-125) وجود دارد که از طریق WebSocket با یک سرور خارجی (wss://ai-creator-backend-q677.onrender.com/api/render/ws/bridge/gh_mahdighandi1989_language) ارتباط برقرار می‌کند. برای پیاده‌سازی این درخواست، نیاز به ایجاد یک WebSocket endpoint جدید در backend/server.js برای مدیریت موضوعات از طریق تلگرام، و احتمالاً یک کامپوننت جدید در frontend برای نمایش و انتخاب موضوعات وجود دارد. همچنین باید یک ربات تلگرام (با استفاده از کتابخانه‌ای مانند node-telegram-bot-api) ایجاد شود که با این WebSocket endpoint ارتباط برقرار کند. کلیدواژه‌های اصلی: تمرین با استاد هوش مصنوعی, مدیریت موضوعات, انتخاب موضوع, Telegram, WebSocket, Firebase.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] یک WebSocket endpoint جدید با مسیر '/ws/topics' در backend/server.js ایجاد شود و به درستی کار کند.
- [ ] یک ربات تلگرام با استفاده از کتابخانه 'node-telegram-bot-api' ایجاد شود و به WebSocket endpoint متصل شود.
- [ ] کاربر بتواند از طریق ربات تلگرام، لیست موضوعات تمرین را مشاهده کند.
- [ ] کاربر بتواند از طریق ربات تلگرام، یک موضوع را برای تمرین انتخاب کند.
- [ ] موضوع انتخاب‌شده در Firebase Firestore ذخیره شود و در فرانت‌اند (کامپوننت TopicSelector) قابل مشاهده باشد.
- [ ] کامپوننت TopicSelector در frontend/src/App.jsx اضافه شود و موضوعات را از WebSocket دریافت و نمایش دهد.
- [ ] تغییرات در backend/server.js بر WebSocket endpoint فعلی ('/ws/live') تأثیری نگذارد.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک WebSocket endpoint جدید در backend/server.js (مثلاً '/ws/topics') برای مدیریت موضوعات تمرین. 2. ایجاد یک ربات تلگرام با استفاده از کتابخانه 'node-telegram-bot-api' که به این endpoint متصل شود. 3. در backend/server.js، یک تابع جدید برای مدیریت درخواست‌های تلگرام (مانند لیست موضوعات، انتخاب موضوع) اضافه شود. 4. در frontend/src/App.jsx، یک کامپوننت جدید (مانند TopicSelector) ایجاد شود که موضوعات را از WebSocket دریافت و نمایش دهد. 5. ذخیره‌سازی موضوعات انتخاب‌شده در Firebase (Firestore) برای همگام‌سازی بین تلگرام و فرانت‌اند. 6. اضافه کردن route های REST API در backend/server.js برای CRUD موضوعات (اختیاری). 7. به‌روزرسانی frontend/index.html برای اضافه کردن اسکریپت ربات تلگرام (در صورت نیاز).

## 💡 نمونه‌های قبل/بعد
**اضافه کردن WebSocket endpoint جدید در backend/server.js**

_قبل:_
```
const wss = new WebSocketServer({ server, path: '/ws/live' });
```

_بعد:_
```
const wss = new WebSocketServer({ server, path: '/ws/live' });
const topicWss = new WebSocketServer({ server, path: '/ws/topics' });

topicWss.on('connection', (ws) => {
  console.log('Topic WebSocket connected');
  ws.on('message', (message) => {
    const data = JSON.parse(message);
    if (data.type === 'get-topics') {
      // ارسال لیست موضوعات از Firebase
      ws.send(JSON.stringify({ type: 'topics-list', topics: ['موضوع ۱', 'موضوع ۲'] }));
    } else if (data.type === 'select-topic') {
      // ذخیره موضوع انتخاب‌شده در Firebase
      ws.send(JSON.stringify({ type: 'topic-selected', topic: data.topic }));
    }
  });
});
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `node backend/server.js (برای تست WebSocket endpoint جدید)`
- `npm run dev (برای تست فرانت‌اند)`
- `تست دستی ربات تلگرام با ارسال دستورات /topics و /select_topic`
- `بررسی Firebase Console برای اطمینان از ذخیره‌سازی موضوعات`

## ⚠️ ریسک‌ها و موارد احتیاط
اضافه کردن WebSocket endpoint جدید در backend/server.js ممکن است با endpoint فعلی ('/ws/live') تداخل ایجاد کند اگر مسیرها به درستی مدیریت نشوند. همچنین، اضافه کردن وابستگی 'node-telegram-bot-api' ممکن است باعث افزایش حجم بسته و زمان بیلد شود. امنیت WebSocket باید در نظر گرفته شود تا از دسترسی غیرمجاز جلوگیری شود. ریسک دیگر این است که ربات تلگرام ممکن است با محدودیت‌های نرخ (rate limiting) API تلگرام مواجه شود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 5: یکپارچه‌سازی چت صوتی و متنی با هوش مصنوعی از طریق تلگرام

**Scope:** ایجاد قابلیت ارسال پیام متنی و صوتی از تلگرام به چت هوش مصنوعی، دریافت پاسخ (متن و صوت) و نمایش آن در تلگرام. شامل استفاده از میکروفون برای ضبط و ارسال صوت به سیستم آنالیز.
**Key terms:** میکروفون, پیام صوتی, آنالیز صوت, پاسخ صوتی, چت هوش مصنوعی, Telegram

**بخش مربوط از متن کاربر:**
```
از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم و از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد
```

## 🎯 هدف (خلاصه ساختاریافته)
یکپارچه‌سازی چت صوتی و متنی با هوش مصنوعی از طریق تلگرام

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:56-114` — `app.post('/api/gemini/chat', ...)` — این endpoint اصلی چت با Gemini است. برای یکپارچه‌سازی با تلگرام، باید از این تابع برای پردازش پیام‌های متنی استفاده شود. همچنین قابلیت includeAudio برای پشتیبانی از صوت وجود دارد.
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
      if (payload.contents) {
        payload.contents = payload.contents.map(content => ({
          role: content.role,
          parts: content.parts.map(part => {
            if (part.inline_data && includeAudio) {
              return { inline_data: part.inline_data };
            }
            if (part.text !== undefined) {
              return { text: part.text };
            }
            return null;
          }).filter(p => p !== null)
        }));
      }
      const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${GEMINI_API_KEY}`;
      const response = await fetch(apiUrl, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(payload)
      });
      if (!response.ok) {
        const errorData = await response.text();
        console.error('Gemini API error:', errorData);
        return res.status(response.status).json({ error: errorData });
      }
      const result = await response.json();
      if (includeAudio) {
        res.json(result);
      } else {
        const text = result.candidates?.[0]?.content?.parts?.[0]?.text;
        if (!text) {
          return res.status(500).json({ error: 'No text in response' });
        }
        res.json({ text });
      }
    } catch (error) {
      console.error('Chat API error:', error);
      res.status(500).json({ error: error.message });
    }
  });
  ```
- `backend/server.js:117-164` — `app.post('/api/gemini/tts', ...)` — این endpoint برای تبدیل متن به صوت (TTS) استفاده می‌شود. برای ارسال پاسخ صوتی به تلگرام، باید از این تابع استفاده کرده و خروجی base64 را به فایل صوتی تبدیل کرده و از طریق Telegram Bot API ارسال کرد.
  ```jsx
  app.post('/api/gemini/tts', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const { prompt, voice = 'Kore' } = req.body;
      const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-preview-tts:generateContent?key=${GEMINI_API_KEY}`;
      const payload = {
        contents: [{ parts: [{ text: prompt }] }],
        generationConfig: {
          responseModalities: ["AUDIO"],
          speechConfig: { 
            voiceConfig: { 
              prebuiltVoiceConfig: { voiceName: voice } 
            } 
          }
        }
      };
      const response = await fetch(apiUrl, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(payload)
      });
      if (!response.ok) {
        const errorData = await response.text();
        console.error('TTS API error:', errorData);
        return res.status(response.status).json({ error: errorData });
      }
      const result = await response.json();
      const part = result?.candidates?.[0]?.content?.parts?.[0];
      const audioData = part?.inlineData?.data;
      const mimeType = part?.inlineData?.mimeType;
      if (audioData && mimeType?.startsWith("audio/")) {
        res.json({ audioData, mimeType });
      } else {
        res.status(500).json({ error: 'Invalid audio data' });
      }
    } catch (error) {
      console.error('TTS API error:', error);
      res.status(500).json({ error: error.message });
    }
  });
  ```
- `backend/server.js:273-294` — `async function analyzeWithGemini(parts, systemPrompt)` — این تابع برای آنالیز محتوا (از جمله صوت) با Gemini استفاده می‌شود. برای پیام‌های صوتی دریافتی از تلگرام، باید فایل صوتی را به این تابع ارسال کرد تا آنالیز و استخراج شود.
  ```jsx
  async function analyzeWithGemini(parts, systemPrompt) {
    const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${GEMINI_API_KEY}`;
    const payload = {
      contents: [{ role: 'user', parts }],
      systemInstruction: { parts: [{ text: systemPrompt }] }
    };
    const response = await fetch(apiUrl, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(payload)
    });
    if (!response.ok) {
      const error = await response.text();
      throw new Error(`Gemini API error: ${error}`);
    }
    const result = await response.json();
    return result.candidates?.[0]?.content?.parts?.[0]?.text || '';
  }
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: JavaScript (Node.js backend با Express, فرانت‌اند React با Vite). کتابخانه‌های مرتبط: express, ws, multer, fluent-ffmpeg, firebase, lucide-react. برای تلگرام: node-telegram-bot-api یا telegraf.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `frontend/src/App.jsx` (سطر 8) — این فایل شامل چت هوش مصنوعی موجود است که کاربر می‌خواهد از طریق تلگرام به آن دسترسی داشته باشد. خط 8: WS_URL = 'wss://ai-creator-backend-q677.onrender.com/api/render/ws/bridge/gh_mahdighandi1989_language' برای WebSocket. همچنین شامل Inspector Bridge Script (خطوط 4-125) برای رهگیری رویدادها.
- `frontend/index.html` (سطر 32) — این فایل شامل Inspector Bridge Script (خطوط 32-201) است که رویدادهای کاربر (کلیک، اسکرول، تایپ) را رهگیری می‌کند. برای یکپارچه‌سازی با تلگرام، ممکن است نیاز به ارسال این رویدادها به تلگرام باشد.
- `backend/package.json` (سطر 1) — برای نصب کتابخانه node-telegram-bot-api باید به این فایل dependency جدید اضافه شود.
- `backend/.env.example` (سطر 1) — برای ذخیره توکن ربات تلگرام (TELEGRAM_BOT_TOKEN) باید به این فایل متغیر محیطی جدید اضافه شود.
- `render.yaml` (سطر 1) — اگر پروژه روی Render مستقر است، ممکن است نیاز به تنظیم Webhook URL در این فایل باشد.

## 🌐 نقشهٔ وابستگی‌ها
این قابلیت به شدت به backend/server.js وابسته است، زیرا تمام APIهای مورد نیاز (Chat, TTS, File Analysis) در آن پیاده‌سازی شده‌اند. همچنین به frontend/src/App.jsx وابسته است زیرا چت موجود در آنجا باید با تلگرام همگام‌سازی شود. کتابخانه node-telegram-bot-api باید به backend/package.json اضافه شود. متغیر محیطی TELEGRAM_BOT_TOKEN باید در backend/.env.example و محیط اجرا تنظیم شود. WebSocket موجود در backend/server.js (خط 45) می‌تواند برای ارسال نوتیفیکیشن به فرانت‌اند استفاده شود.

## 🔍 Context و وضعیت فعلی
کاربر درخواست یکپارچه‌سازی چت هوش مصنوعی موجود در پروژه (که در frontend/src/App.jsx پیاده‌سازی شده) با تلگرام را دارد. هدف این است که کاربر بتواند از طریق تلگرام با هوش مصنوعی چت کند، پیام متنی و صوتی ارسال کند، و پاسخ (متن و صوت) را در تلگرام دریافت کند. کاربر می‌گوید: 'از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم و از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد'. کلیدواژه‌های اصلی: میکروفون, پیام صوتی, آنالیز صوت, پاسخ صوتی, چت هوش مصنوعی, Telegram. در کد فعلی، قابلیت چت صوتی و متنی در frontend/src/App.jsx از طریق WebSocket (WS_URL: 'wss://ai-creator-backend-q677.onrender.com/api/render/ws/bridge/gh_mahdighandi1989_language') و APIهای backend/server.js (مانند /api/gemini/chat در خط 56 و /api/gemini/tts در خط 117) پیاده‌سازی شده است. همچنین در frontend/index.html یک Inspector Bridge Script (خطوط 32-201) برای رهگیری رویدادها وجود دارد. برای یکپارچه‌سازی با تلگرام، نیاز به ایجاد یک ربات تلگرام (با استفاده از Telegram Bot API) و اتصال آن به backend/server.js است تا پیام‌های دریافتی از تلگرام به APIهای موجود هدایت شوند و پاسخ‌ها به تلگرام برگردانده شوند.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] یک ربات تلگرام با استفاده از BotFather ساخته شود و توکن آن در متغیر محیطی TELEGRAM_BOT_TOKEN ذخیره شود.
- [ ] Webhook تلگرام در backend/server.js در مسیر POST /api/telegram/webhook پیاده‌سازی شود و پیام‌های دریافتی را پردازش کند.
- [ ] پیام متنی ارسال شده به ربات تلگرام، به /api/gemini/chat ارسال شده و پاسخ متنی به تلگرام برگردانده شود.
- [ ] پیام صوتی ارسال شده به ربات تلگرام، دانلود شده، با analyzeWithGemini آنالیز شود، و پاسخ صوتی با /api/gemini/tts تولید شده و به تلگرام ارسال شود.
- [ ] پاسخ‌های صوتی و متنی همزمان در تلگرام و چت موجود در frontend/src/App.jsx نمایش داده شوند (با استفاده از WebSocket).
- [ ] کتابخانه node-telegram-bot-api به backend/package.json اضافه و نصب شود.
- [ ] مستندات راه‌اندازی در backend/.env.example به‌روزرسانی شود.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک endpoint جدید در backend/server.js برای Webhook تلگرام (مثلاً POST /api/telegram/webhook) که پیام‌های دریافتی از تلگرام را دریافت کند. 2. در این endpoint، نوع پیام (متن یا صوتی) را تشخیص داده و بر اساس آن: a) برای پیام متنی، به /api/gemini/chat (خط 56) ارسال کند و پاسخ متنی را به تلگرام برگرداند. b) برای پیام صوتی، فایل صوتی را از تلگرام دانلود کرده (با استفاده از Telegram Bot API و file_id)، سپس به سیستم آنالیز موجود (مانند analyzeWithGemini در خط 273 backend/server.js) ارسال کند و پاسخ صوتی (با استفاده از /api/gemini/tts در خط 117) تولید کرده و به تلگرام ارسال کند. 3. نصب کتابخانه node-telegram-bot-api در backend (اضافه به backend/package.json). 4. ایجاد یک سرویس جدید در backend (مثلاً backend/services/telegramService.js) برای مدیریت اتصال و Webhook. 5. اطمینان از اینکه پاسخ‌های صوتی و متنی همزمان در تلگرام و چت موجود (frontend) نمایش داده شوند. 6. استفاده از WebSocket موجود (wss در خط 45 backend/server.js) برای ارسال نوتیفیکیشن به فرانت‌اند هنگام دریافت پیام جدید از تلگرام.

## 💡 نمونه‌های قبل/بعد
**افزودن Webhook تلگرام به backend/server.js**

_قبل:_
```
// backend/server.js - قبل از تغییر
// (بدون هیچ endpoint مربوط به تلگرام)
```

_بعد:_
```
// backend/server.js - بعد از تغییر
import TelegramBot from 'node-telegram-bot-api';

const TELEGRAM_BOT_TOKEN = process.env.TELEGRAM_BOT_TOKEN;
let bot = null;
if (TELEGRAM_BOT_TOKEN) {
  bot = new TelegramBot(TELEGRAM_BOT_TOKEN, { webHook: { port: PORT, host: '0.0.0.0' } });
  bot.setWebHook(`${process.env.WEBHOOK_URL}/api/telegram/webhook`);
  
  bot.on('message', async (msg) => {
    const chatId = msg.chat.id;
    if (msg.text) {
      // ارسال پیام متنی به /api/gemini/chat
      const response = await fetch(`http://localhost:${PORT}/api/gemini/chat`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ contents: [{ role: 'user', parts: [{ text: msg.text }] }] })
      });
      const data = await response.json();
      bot.sendMessage(chatId, data.text);
    } else if (msg.voice) {
      // دانلود فایل صوتی از تلگرام
      const fileLink = await bot.getFileLink(msg.voice.file_id);
      const audioResponse = await fetch(fileLink);
      const audioBuffer = await audioResponse.arrayBuffer();
      // آنالیز صوت با analyzeWithGemini
      const analysisResult = await analyzeWithGemini([
        { inline_data: { mime_type: 'audio/ogg', data: Buffer.from(audioBuffer).toString('base64') } }
      ], 'آنالیز این پیام صوتی را انجام بده.');
      // تولید پاسخ صوتی با TTS
      const ttsResponse = await fetch(`http://localhost:${PORT}/api/gemini/tts`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ prompt: analysisResult })
      });
      const ttsData = await ttsResponse.json();
      // ارسال پاسخ صوتی به تلگرام
      const audioBufferResponse = Buffer.from(ttsData.audioData, 'base64');
      await bot.sendVoice(chatId, audioBufferResponse);
    }
  });
}
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `cd backend && npm install node-telegram-bot-api`
- `cd backend && npm start`
- `ارسال پیام متنی به ربات تلگرام و بررسی دریافت پاسخ`
- `ارسال پیام صوتی به ربات تلگرام و بررسی دریافت پاسخ صوتی`
- `بررسی لاگ‌های backend برای اطمینان از عدم خطا`

## ⚠️ ریسک‌ها و موارد احتیاط
افزودن Webhook تلگرام به backend/server.js ممکن است با WebSocket موجود (خط 45) تداخل داشته باشد، زیرا هر دو از پورت یکسان استفاده می‌کنند. باید اطمینان حاصل شود که Webhook در مسیر جداگانه (مثلاً /api/telegram/webhook) تنظیم شده است. همچنین، دانلود فایل‌های صوتی از تلگرام ممکن است مصرف پهنای باند را افزایش دهد. تابع analyzeWithGemini (خط 273) برای آنالیز صوت استفاده می‌شود، اما ممکن است نیاز به بهینه‌سازی برای فایل‌های بزرگ داشته باشد. ریسک امنیتی: توکن ربات تلگرام نباید در کد منبع ذخیره شود و باید از متغیر محیطی استفاده شود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 6: نوشتن تست‌های یکپارچه‌سازی و نهایی برای کل قابلیت‌های تلگرام

**Scope:** نوشتن تست‌های خودکار برای تمام مراحل قبلی شامل ارسال نوتیفیکیشن، دریافت دستورات، مدیریت محتوا، و چت با هوش مصنوعی از طریق تلگرام.
**Key terms:** تست یکپارچه‌سازی, Telegram Bot, نوتیفیکیشن, چت هوش مصنوعی, مدیریت محتوا

**بخش مربوط از متن کاربر:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه ... بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم ... از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
نوشتن تست‌های یکپارچه‌سازی و نهایی برای قابلیت‌های تلگرام (نوتیفیکیشن، دریافت دستورات، مدیریت محتوا، چت با هوش مصنوعی)

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:56-114` — `app.post('/api/gemini/chat')` — این endpoint برای چت با هوش مصنوعی Gemini استفاده می‌شود. هندلر تلگرام `/chat` باید از این API استفاده کند. تست‌های یکپارچه‌سازی باید این endpoint را از طریق Webhook تلگرام تست کنند.
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
      // Clean payload - preserve audio data if includeAudio flag is set
      if (payload.contents) {
        payload.contents = payload.contents.map(content => ({
          role: content.role,
          parts: content.parts.map(part => {
            if (part.inline_data && includeAudio) {
              return { inline_data: part.inline_data };
            }
            if (part.text !== undefined) {
              return { text: part.text };
            }
            return null;
          }).filter(p => p !== null)
        }));
      }
      const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${GEMINI_API_KEY}`;
      const response = await fetch(apiUrl, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(payload)
      });
      if (!response.ok) {
        const errorData = await response.text();
        console.error('Gemini API error:', errorData);
        return res.status(response.status).json({ error: errorData });
      }
      const result = await response.json();
      if (includeAudio) {
        res.json(result);
      } else {
        const text = result.candidates?.[0]?.content?.parts?.[0]?.text;
        if (!text) {
          return res.status(500).json({ error: 'No text in response' });
        }
        res.json({ text });
      }
    } catch (error) {
      console.error('Chat API error:', error);
      res.status(500).json({ error: error.message });
    }
  });
  ```
- `backend/server.js:242-350` — `FILE ANALYSIS API` — این بخش شامل APIهای تحلیل فایل است. هندلر تلگرام `/upload` باید از این APIها برای آپلود و تحلیل محتوای دروس استفاده کند. تست‌های یکپارچه‌سازی باید آپلود فایل از طریق تلگرام و تحلیل آن را تست کنند.
  ```jsx
  // ============================================
  // FILE ANALYSIS API
  // ============================================
  
  // Helper: Split large content into chunks
  function splitIntoChunks(content, maxChunkSize = 30000) {
    if (content.length <= maxChunkSize) return [content];
    const chunks = [];
    let remaining = content;
    while (remaining.length > 0) {
      let splitPoint = maxChunkSize;
      if (remaining.length > maxChunkSize) {
        const lastNewline = remaining.lastIndexOf('\n', maxChunkSize);
        const lastPeriod = remaining.lastIndexOf('.', maxChunkSize);
        const lastSpace = remaining.lastIndexOf(' ', maxChunkSize);
        splitPoint = Math.max(lastNewline, lastPeriod, lastSpace);
        if (splitPoint < maxChunkSize * 0.5) splitPoint = maxChunkSize;
      }
      chunks.push(remaining.substring(0, splitPoint));
      remaining = remaining.substring(splitPoint).trim();
    }
    return chunks;
  }
  ```
- `backend/server.js:45` — `WebSocketServer` — WebSocket برای قابلیت‌های زنده (Live Voice) استفاده می‌شود. در صورت نیاز به قابلیت چت صوتی زنده از طریق تلگرام، این WebSocket باید در تست‌ها نیز پوشش داده شود.
  ```jsx
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: JavaScript (Node.js backend با Express, React frontend با Vite). کتابخانه‌های موجود: express, cors, dotenv, ws, multer, fluent-ffmpeg, firebase, lucide-react. برای Telegram Bot: پیشنهاد استفاده از `telegraf` (کتابخانه مدرن و محبوب) یا `node-telegram-bot-api`. برای تست: پیشنهاد استفاده از `jest` به عنوان فریمورک تست، `supertest` برای تست HTTP، و `nock` برای mock کردن APIهای خارجی (مانند Gemini API).

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `frontend/src/App.jsx` (سطر 1) — این فایل شامل Inspector Bridge Script (خط 1-126) و کامپوننت‌های React (خط 132+) است. اگر قابلیت‌های تلگرام نیاز به نمایش در فرانت‌اند داشته باشند (مثلاً نمایش نوتیفیکیشن‌ها)، این فایل تحت تأثیر قرار می‌گیرد.
- `frontend/index.html` (سطر 11) — این فایل شامل Inspector Bridge Script (خط 32-201) و Firebase Configuration (خط 11-19) است. اگر قابلیت‌های تلگرام نیاز به تغییر در Firebase یا Inspector Bridge داشته باشند، این فایل تحت تأثیر قرار می‌گیرد.
- `backend/package.json` (سطر 1) — برای اضافه کردن کتابخانه‌های مورد نیاز Telegram Bot (مانند `node-telegram-bot-api` یا `telegraf`) و کتابخانه‌های تست (مانند `jest`, `supertest`, `nock`)، باید این فایل به‌روزرسانی شود.
- `backend/.env.example` (سطر 1) — برای اضافه کردن متغیرهای محیطی مربوط به Telegram Bot (مانند `TELEGRAM_BOT_TOKEN`, `TELEGRAM_WEBHOOK_URL`)، باید این فایل به‌روزرسانی شود.
- `render.yaml` (سطر 1) — اگر Telegram Bot نیاز به Webhook داشته باشد، ممکن است نیاز به تغییر در کانفیگ Render برای اضافه کردن مسیر Webhook باشد.

## 🌐 نقشهٔ وابستگی‌ها
این تسک به شدت به فایل‌های backend وابسته است. فایل `backend/server.js` (خط 56-114 برای Chat API، خط 117-164 برای TTS API، خط 242-350+ برای File Analysis API، خط 45 برای WebSocket) هسته اصلی قابلیت‌هایی است که باید از طریق تلگرام در دسترس قرار گیرند. فایل `backend/package.json` برای اضافه کردن وابستگی‌های جدید (Telegram Bot library, testing libraries) نیاز به تغییر دارد. فایل `backend/.env.example` برای اضافه کردن متغیرهای محیطی جدید نیاز به تغییر دارد. فایل‌های frontend (`frontend/src/App.jsx`, `frontend/index.html`) ممکن است برای نمایش نوتیفیکیشن‌ها یا وضعیت اتصال تلگرام نیاز به تغییر داشته باشند. فایل `render.yaml` ممکن است برای تنظیم Webhook نیاز به تغییر داشته باشد. فایل‌های تست جدید (`backend/tests/telegram.integration.test.js`, `backend/tests/telegram.e2e.test.js`) باید ایجاد شوند.

## 🔍 Context و وضعیت فعلی
کاربر درخواست نوشتن تست‌های خودکار برای تمام مراحل قبلی شامل ارسال نوتیفیکیشن، دریافت دستورات، مدیریت محتوا، و چت با هوش مصنوعی از طریق تلگرام را دارد. این درخواست بر اساس بخش مربوط از درخواست اصلی کاربر است: 'میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه ... بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم ... از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم'. کلیدواژه‌های اصلی: تست یکپارچه‌سازی, Telegram Bot, نوتیفیکیشن, چت هوش مصنوعی, مدیریت محتوا. با بررسی کد واقعی پروژه (backend/server.js, frontend/src/App.jsx, frontend/index.html) مشخص شد که در حال حاضر هیچ پیاده‌سازی برای قابلیت‌های تلگرام وجود ندارد. فایل backend/server.js شامل APIهای مربوط به Gemini Chat (خط 56-114)، TTS (خط 117-164)، File Analysis (خط 242-350+) و WebSocket (خط 45) است. فایل frontend/src/App.jsx شامل Inspector Bridge Script (خط 1-126) و کامپوننت‌های React (خط 132+) است. بنابراین، ابتدا باید قابلیت‌های تلگرام (Bot API) پیاده‌سازی شوند و سپس تست‌های یکپارچه‌سازی و نهایی برای آن‌ها نوشته شود. این تسک شامل: 1) ایجاد یک سرویس Telegram Bot در backend، 2) پیاده‌سازی Webhook برای دریافت دستورات، 3) اتصال به APIهای موجود (Chat، TTS، File Analysis)، 4) مدیریت محتوا (دروس)، 5) نوشتن تست‌های یکپارچه‌سازی برای کل این قابلیت‌ها.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] 1. فایل `backend/services/telegramBot.js` ایجاد شود و شامل کلاس/ماژول TelegramBot با متدهای `handleUpdate`, `sendMessage`, `sendAudio`, `sendDocument` باشد.
- [ ] 2. Webhook endpoint `POST /api/telegram/webhook` در `backend/server.js` اضافه شود و آپدیت‌های تلگرام را به درستی پردازش کند.
- [ ] 3. هندلر دستور `/chat` در Telegram Bot پیاده‌سازی شود و متن پیام را به API `/api/gemini/chat` ارسال کند و پاسخ را به کاربر تلگرام برگرداند.
- [ ] 4. هندلر دستور `/upload` پیاده‌سازی شود و فایل ارسالی از تلگرام را دریافت کرده، به File Analysis API ارسال کند و نتیجه را برگرداند.
- [ ] 5. هندلر دستور `/lessons` پیاده‌سازی شود و لیست دروس را از Firebase (یا منبع داده) دریافت کرده و به کاربر نمایش دهد.
- [ ] 6. ماژول نوتیفیکیشن `backend/services/notificationService.js` ایجاد شود و بتواند پیام‌های نوتیفیکیشن را به کاربران تلگرام ارسال کند.
- [ ] 7. فایل تست `backend/tests/telegram.integration.test.js` ایجاد شود و شامل حداقل 5 تست یکپارچه‌سازی برای Webhook و هندلرهای مختلف باشد.
- [ ] 8. فایل تست `backend/tests/telegram.e2e.test.js` ایجاد شود و شامل حداقل 2 تست نهایی (End-to-End) با mock کردن Telegram API باشد.
- [ ] 9. تمام تست‌ها با دستور `npm run test` (یا معادل آن) اجرا شوند و پاس شوند.
- [ ] 10. متغیرهای محیطی `TELEGRAM_BOT_TOKEN` و `TELEGRAM_WEBHOOK_URL` به `backend/.env.example` اضافه شوند.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید `backend/services/telegramBot.js` برای سرویس Telegram Bot با استفاده از کتابخانه `node-telegram-bot-api` یا `telegraf`. 2. ایجاد Webhook endpoint در `backend/server.js` (مثلاً `POST /api/telegram/webhook`) برای دریافت آپدیت‌های تلگرام. 3. پیاده‌سازی هندلرهای دستورات: `/start`, `/chat`, `/upload`, `/lessons`, `/notifications`. 4. اتصال هندلر `/chat` به API موجود `/api/gemini/chat` (خط 56-114 در backend/server.js). 5. اتصال هندلر `/upload` به File Analysis API موجود (خط 242-350+). 6. ایجاد ماژول مدیریت محتوا (دروس) در `backend/services/lessonManager.js`. 7. ایجاد ماژول نوتیفیکیشن در `backend/services/notificationService.js`. 8. نوشتن تست‌های یکپارچه‌سازی با استفاده از `jest` و `supertest` برای تست Webhook و هندلرها. 9. نوشتن تست‌های نهایی (End-to-End) با استفاده از `telegraf` testing utilities یا mock کردن Telegram API. 10. ایجاد فایل تست `backend/tests/telegram.integration.test.js` و `backend/tests/telegram.e2e.test.js`.

## 💡 نمونه‌های قبل/بعد
**اضافه کردن Webhook endpoint برای Telegram Bot در backend/server.js**

_قبل:_
```
// (در حال حاضر هیچ Webhook endpointی برای Telegram وجود ندارد)
```

_بعد:_
```
// Telegram Bot Webhook
app.post('/api/telegram/webhook', async (req, res) => {
  try {
    const update = req.body;
    await telegramService.handleUpdate(update);
    res.sendStatus(200);
  } catch (error) {
    console.error('Telegram webhook error:', error);
    res.sendStatus(500);
  }
});
```

**ایجاد فایل تست یکپارچه‌سازی backend/tests/telegram.integration.test.js**

_قبل:_
```
// (فایل وجود ندارد)
```

_بعد:_
```
const request = require('supertest');
const app = require('../server');

describe('Telegram Bot Integration Tests', () => {
  describe('POST /api/telegram/webhook', () => {
    it('should handle /start command', async () => {
      const update = {
        update_id: 1,
        message: {
          message_id: 1,
          from: { id: 123, first_name: 'Test' },
          chat: { id: 456 },
          text: '/start'
        }
      };
      const response = await request(app)
        .post('/api/telegram/webhook')
        .send(update)
        .expect(200);
    });

    it('should handle /chat command and call Gemini API', async () => {
      const update = {
        update_id: 2,
        message: {
          message_id: 2,
          from: { id: 123, first_name: 'Test' },
          chat: { id: 456 },
          text: '/chat سلام'
        }
      };
      const response = await request(app)
        .post('/api/telegram/webhook')
        .send(update)
        .expect(200);
    });
  });
});
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `npm install --save telegraf dotenv (برای نصب کتابخانه Telegram Bot)`
- `npm install --save-dev jest supertest nock (برای نصب کتابخانه‌های تست)`
- `npx jest backend/tests/telegram.integration.test.js (برای اجرای تست‌های یکپارچه‌سازی)`
- `npx jest backend/tests/telegram.e2e.test.js (برای اجرای تست‌های نهایی)`
- `npm run test (برای اجرای تمام تست‌ها)`

## ⚠️ ریسک‌ها و موارد احتیاط
1. **وابستگی به API خارجی**: Telegram Bot API و Gemini API هر دو سرویس‌های خارجی هستند. در تست‌ها باید با `nock` یا ابزار مشابه mock شوند. 2. **تغییر در backend/server.js**: اضافه کردن Webhook endpoint و سرویس Telegram Bot به فایل `backend/server.js` ممکن است با middlewareهای موجود (cors, express.json) تداخل داشته باشد. 3. **مدیریت state**: Telegram Bot نیاز به مدیریت state برای هر کاربر دارد (مثلاً وضعیت مکالمه). این state ممکن است با state موجود در Firebase (که در frontend/src/App.jsx استفاده شده) تداخل داشته باشد. 4. **امنیت Webhook**: Webhook باید با استفاده از secret token یا IP filtering محافظت شود تا از حملات جعلی جلوگیری شود. 5. **محدودیت نرخ (Rate Limiting)**: Telegram API و Gemini API هر دو محدودیت نرخ دارند. تست‌ها باید این محدودیت‌ها را در نظر بگیرند.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: high
- تخمین زمان: large

---

## ✅ معیارهای پذیرش کلی (همهٔ مراحل)
- [ ] بات تلگرام با موفقیت ایجاد شده و توکن آن در متغیر محیطی TELEGRAM_BOT_TOKEN ذخیره شود
- [ ] endpoint POST /api/telegram/notify در backend/server.js پیاده‌سازی شود و پیام را به تلگرام ارسال کند
- [ ] نوتیفیکیشن‌های خودکار برای خطاهای سیستم (مثلاً خطای Gemini API در خط 111) ارسال شوند
- [ ] نوتیفیکیشن‌های وضعیت سیستم (مثلاً وضعیت WebSocket در خط 45) قابل ارسال باشند
- [ ] متن پیام‌ها به صورت پویا و قابل تنظیم باشد
- [ ] خطاهای ارسال نوتیفیکیشن به درستی لاگ شوند و باعث کرش سرور نشوند
- [ ] یک endpoint جدید `POST /api/telegram/webhook` در `backend/server.js` ایجاد شود که قادر به دریافت Webhook از تلگرام باشد.
- [ ] یک فایل جدید `backend/services/telegramBot.js` ایجاد شود که شامل منطق پردازش دستورات `/start`, `/status`, `/list-models` باشد.
- [ ] دستور `/status` باید وضعیت سیستم را با فراخوانی `GET /api/health` برگرداند.
- [ ] دستور `/list-models` باید لیست مدل‌های Gemini را با فراخوانی `GET /api/list-models` برگرداند.
- [ ] متغیر محیطی `TELEGRAM_BOT_TOKEN` به `backend/.env.example` اضافه شود.
- [ ] ربات تلگرام بتواند پیام تأیید دریافت دستور و نتیجه عملیات را به کاربر ارسال کند.
- [ ] کاربر می‌تواند با ارسال دستور /start به ربات تلگرام، پیام خوش‌آمدگویی و راهنما دریافت کند.
- [ ] کاربر می‌تواند با ارسال دستور /lessons، لیست تمام دروس موجود در Firebase Firestore (collection: lessons) را مشاهده کند.
- [ ] کاربر می‌تواند با ارسال دستور /lesson <lessonId>، محتوای کامل یک درس خاص شامل title، content و لیست فایل‌های مرتبط را مشاهده کند.
- [ ] کاربر می‌تواند با ارسال دستور /upload و سپس ارسال یک فایل (صوتی، تصویری، متنی)، فایل را در Firebase Storage در مسیر lessons/{lessonId}/{fileName} آپلود کند.
- [ ] پس از آپلود فایل، یک پیام تأیید به کاربر در تلگرام ارسال شود و اطلاعات فایل در Firestore به‌روزرسانی شود.
- [ ] WebSocket موجود در backend/server.js (path: /ws/live) یک رویداد new-file-uploaded را برای اطلاع‌رسانی به فرانت‌اند ارسال کند.
- [ ] تمامی خطاها (مانند عدم وجود درس، خطای آپلود) با پیام مناسب به کاربر در تلگرام نمایش داده شود.
- [ ] ربات تلگرام بتواند همزمان با چندین کاربر کار کند (state management برای هر کاربر).

## Acceptance Criteria

1. بات تلگرام با موفقیت ایجاد شده و توکن آن در متغیر محیطی TELEGRAM_BOT_TOKEN ذخیره شود _(verify: static)_
2. endpoint POST /api/telegram/notify در backend/server.js پیاده‌سازی شود و پیام را به تلگرام ارسال کند _(verify: api_response)_
3. نوتیفیکیشن‌های خودکار برای خطاهای سیستم (مثلاً خطای Gemini API در خط 111) ارسال شوند _(verify: static)_
4. نوتیفیکیشن‌های وضعیت سیستم (مثلاً وضعیت WebSocket در خط 45) قابل ارسال باشند _(verify: static)_
5. متن پیام‌ها به صورت پویا و قابل تنظیم باشد _(verify: static)_
6. خطاهای ارسال نوتیفیکیشن به درستی لاگ شوند و باعث کرش سرور نشوند _(verify: static)_
7. یک endpoint جدید `POST /api/telegram/webhook` در `backend/server.js` ایجاد شود که قادر به دریافت Webhook از تلگرام باشد. _(verify: api_response)_
8. یک فایل جدید `backend/services/telegramBot.js` ایجاد شود که شامل منطق پردازش دستورات `/start`, `/status`, `/list-models` باشد. _(verify: static)_
9. دستور `/status` باید وضعیت سیستم را با فراخوانی `GET /api/health` برگرداند. _(verify: static)_
10. دستور `/list-models` باید لیست مدل‌های Gemini را با فراخوانی `GET /api/list-models` برگرداند. _(verify: static)_
11. متغیر محیطی `TELEGRAM_BOT_TOKEN` به `backend/.env.example` اضافه شود. _(verify: static)_
12. ربات تلگرام بتواند پیام تأیید دریافت دستور و نتیجه عملیات را به کاربر ارسال کند. _(verify: static)_
13. کاربر می‌تواند با ارسال دستور /start به ربات تلگرام، پیام خوش‌آمدگویی و راهنما دریافت کند. _(verify: static)_
14. کاربر می‌تواند با ارسال دستور /lessons، لیست تمام دروس موجود در Firebase Firestore (collection: lessons) را مشاهده کند. _(verify: static)_
15. کاربر می‌تواند با ارسال دستور /lesson <lessonId>، محتوای کامل یک درس خاص شامل title، content و لیست فایل‌های مرتبط را مشاهده کند. _(verify: static)_
16. کاربر می‌تواند با ارسال دستور /upload و سپس ارسال یک فایل (صوتی، تصویری، متنی)، فایل را در Firebase Storage در مسیر lessons/{lessonId}/{fileName} آپلود کند. _(verify: static)_
17. پس از آپلود فایل، یک پیام تأیید به کاربر در تلگرام ارسال شود و اطلاعات فایل در Firestore به‌روزرسانی شود. _(verify: static)_
18. WebSocket موجود در backend/server.js (path: /ws/live) یک رویداد new-file-uploaded را برای اطلاع‌رسانی به فرانت‌اند ارسال کند. _(verify: static)_
19. تمامی خطاها (مانند عدم وجود درس، خطای آپلود) با پیام مناسب به کاربر در تلگرام نمایش داده شود. _(verify: static)_
20. ربات تلگرام بتواند همزمان با چندین کاربر کار کند (state management برای هر کاربر). _(verify: static)_
21. یک WebSocket endpoint جدید با مسیر '/ws/topics' در backend/server.js ایجاد شود و به درستی کار کند. _(verify: api_response)_
22. یک ربات تلگرام با استفاده از کتابخانه 'node-telegram-bot-api' ایجاد شود و به WebSocket endpoint متصل شود. _(verify: static)_
23. کاربر بتواند از طریق ربات تلگرام، لیست موضوعات تمرین را مشاهده کند. _(verify: ui_interaction)_
24. کاربر بتواند از طریق ربات تلگرام، یک موضوع را برای تمرین انتخاب کند. _(verify: ui_interaction)_
25. موضوع انتخاب‌شده در Firebase Firestore ذخیره شود و در فرانت‌اند (کامپوننت TopicSelector) قابل مشاهده باشد. _(verify: api_response)_
26. کامپوننت TopicSelector در frontend/src/App.jsx اضافه شود و موضوعات را از WebSocket دریافت و نمایش دهد. _(verify: static)_
27. تغییرات در backend/server.js بر WebSocket endpoint فعلی ('/ws/live') تأثیری نگذارد. _(verify: static)_
28. یک ربات تلگرام با استفاده از BotFather ساخته شود و توکن آن در متغیر محیطی TELEGRAM_BOT_TOKEN ذخیره شود. _(verify: static)_
29. Webhook تلگرام در backend/server.js در مسیر POST /api/telegram/webhook پیاده‌سازی شود و پیام‌های دریافتی را پردازش کند. _(verify: api_response)_
30. پیام متنی ارسال شده به ربات تلگرام، به /api/gemini/chat ارسال شده و پاسخ متنی به تلگرام برگردانده شود. _(verify: api_response)_
31. پیام صوتی ارسال شده به ربات تلگرام، دانلود شده، با analyzeWithGemini آنالیز شود، و پاسخ صوتی با /api/gemini/tts تولید شده و به تلگرام ارسال شود. _(verify: api_response)_
32. پاسخ‌های صوتی و متنی همزمان در تلگرام و چت موجود در frontend/src/App.jsx نمایش داده شوند (با استفاده از WebSocket). _(verify: ui_interaction)_
33. کتابخانه node-telegram-bot-api به backend/package.json اضافه و نصب شود. _(verify: static)_
34. مستندات راه‌اندازی در backend/.env.example به‌روزرسانی شود. _(verify: static)_
35. 1. فایل `backend/services/telegramBot.js` ایجاد شود و شامل کلاس/ماژول TelegramBot با متدهای `handleUpdate`, `sendMessage`, `sendAudio`, `sendDocument` باشد. _(verify: static)_
36. 2. Webhook endpoint `POST /api/telegram/webhook` در `backend/server.js` اضافه شود و آپدیت‌های تلگرام را به درستی پردازش کند. _(verify: api_response)_
37. 3. هندلر دستور `/chat` در Telegram Bot پیاده‌سازی شود و متن پیام را به API `/api/gemini/chat` ارسال کند و پاسخ را به کاربر تلگرام برگرداند. _(verify: api_response)_
38. 4. هندلر دستور `/upload` پیاده‌سازی شود و فایل ارسالی از تلگرام را دریافت کرده، به File Analysis API ارسال کند و نتیجه را برگرداند. _(verify: api_response)_
39. 5. هندلر دستور `/lessons` پیاده‌سازی شود و لیست دروس را از Firebase (یا منبع داده) دریافت کرده و به کاربر نمایش دهد. _(verify: api_response)_
40. 6. ماژول نوتیفیکیشن `backend/services/notificationService.js` ایجاد شود و بتواند پیام‌های نوتیفیکیشن را به کاربران تلگرام ارسال کند. _(verify: static)_
41. 7. فایل تست `backend/tests/telegram.integration.test.js` ایجاد شود و شامل حداقل 5 تست یکپارچه‌سازی برای Webhook و هندلرهای مختلف باشد. _(verify: backend_test)_
42. 8. فایل تست `backend/tests/telegram.e2e.test.js` ایجاد شود و شامل حداقل 2 تست نهایی (End-to-End) با mock کردن Telegram API باشد. _(verify: backend_test)_
43. 9. تمام تست‌ها با دستور `npm run test` (یا معادل آن) اجرا شوند و پاس شوند. _(verify: backend_test)_
44. 10. متغیرهای محیطی `TELEGRAM_BOT_TOKEN` و `TELEGRAM_WEBHOOK_URL` به `backend/.env.example` اضافه شوند. _(verify: static)_

## Task Steps

### Step 1: پیاده‌سازی نوتیفیکیشن یک‌طرفه تلگرام برای ارسال اطلاعات از سیستم
**Status:** `pending` (0%)
**Scope:** ایجاد یک بات تلگرام و اتصال آن به سیستم برای ارسال خودکار نوتیفیکیشن‌ها و اطلاعات تعیین‌شده (مثل وضعیت سیستم، هشدارها و ...). این مرحله شامل دریافت پیام از کاربر نیست.
**Excerpt:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته
```

### Step 2: پیاده‌سازی قابلیت دریافت و پردازش دستورات از تلگرام (کنترل دوطرفه)
**Status:** `pending` (0%)
**Scope:** اضافه کردن قابلیت دریافت پیام‌ها و دستورات از کاربر تلگرام و اجرای عملیات مشخص روی سیستم (مثل اجرای دستورات کنترلی). این مرحله شامل مدیریت محتوای دروس نیست.
**Excerpt:**
```
بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

### Step 3: اضافه کردن قابلیت مشاهده و آپلود محتوای دروس از طریق تلگرام
**Status:** `pending` (0%)
**Scope:** ایجاد قابلیت مشاهده لیست دروس، مشاهده محتوای هر درس و آپلود فایل‌های جدید از طریق تلگرام. این مرحله شامل مدیریت تمرین با استاد هوش مصنوعی نیست.
**Excerpt:**
```
بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم
```

### Step 4: اتصال بخش تمرین با استاد هوش مصنوعی به تلگرام برای مدیریت موضوعات و انتخاب
**Status:** `pending` (0%)
**Scope:** اضافه کردن قابلیت مشاهده و انتخاب موضوعات تمرین با استاد هوش مصنوعی از طریق تلگرام. این مرحله شامل چت مستقیم با هوش مصنوعی نیست.
**Excerpt:**
```
اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم
```

### Step 5: یکپارچه‌سازی چت صوتی و متنی با هوش مصنوعی از طریق تلگرام
**Status:** `pending` (0%)
**Scope:** ایجاد قابلیت ارسال پیام متنی و صوتی از تلگرام به چت هوش مصنوعی، دریافت پاسخ (متن و صوت) و نمایش آن در تلگرام. شامل استفاده از میکروفون برای ضبط و ارسال صوت به سیستم آنالیز.
**Excerpt:**
```
از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم و از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد
```

### Step 6: نوشتن تست‌های یکپارچه‌سازی و نهایی برای کل قابلیت‌های تلگرام
**Status:** `pending` (0%)
**Scope:** نوشتن تست‌های خودکار برای تمام مراحل قبلی شامل ارسال نوتیفیکیشن، دریافت دستورات، مدیریت محتوا، و چت با هوش مصنوعی از طریق تلگرام.
**Excerpt:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه ... بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم ... از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم
```
