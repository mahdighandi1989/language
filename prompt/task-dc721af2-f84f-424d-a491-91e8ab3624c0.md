---
task_id: dc721af2-f84f-424d-a491-91e8ab3624c0
title: میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار (6 مرحله)
type: other
priority: medium
execution_priority: 3000
status: pending
external_status: pending
verification_status: pending
watched_id: 6b04f8bd-b562-4fb9-9cd9-0c27458ced59
project: mahdighandi1989/language
created_at: '2026-05-12T11:13:22.960230+00:00'
updated_at: '2026-06-02T08:02:14.505831+00:00'
target_files:
- backend/server.js
- backend/.env.example
- frontend/index.html
---

# میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار (6 مرحله)

## Raw Idea

میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم و از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

📖 **خواندن کامل + اجرای مو-به-مو (بسیار مهم):**

این پرامپت — از این یادداشت تا انتها — یک سند واحد است که هر بخشش
حاوی الزام یا context منحصربه‌فرد است. خواندن سطحی یا skim کردن **ممنوع**
است.

- پرامپت را **سطر به سطر** بخوان، نه head/tail/فقط-بخش-اصلی.
- اگر بخشی به‌نظر طولانی یا تکراری آمد، **حتماً** بخوان — تفاوت‌های
  ریز ممکن است در آن جا اساسی باشند.
- هر جمله، URL، نام فایل، نام تابع، یا مقدار عددی که در پرامپت آمده،
  دقیقاً همان است که کاربر می‌خواهد — تغییرش نده، رندش نکن، خلاصه‌اش
  نکن.
- اگر پرامپت چندین درخواست/مرحله/زیرتسک دارد، **همه** را پیاده کن. حتی
  یکی را نه به‌عنوان "خارج از scope" حذف کن.

❌ ممنوعات صریح:
- خلاصه‌سازی متن کاربر در commit message یا response
- "این بخش اصلی نیست، رد می‌کنم"
- "کاربر احتمالاً منظورش این بود..." — منظورش همان است که نوشته
- "این URL/نام به نظر قدیمی است، آپدیتش کردم" — تغییر بدون درخواست ممنوع
- پیاده‌سازی فقط بخشی از پرامپت و تظاهر به کامل بودن
- "همه آیتم‌های لیست A را بررسی کردم، B و C مشابه بودند" — نه؛
  هرکدام را جداگانه

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

🔗 **وابستگی‌ها و همگام‌سازی (بسیار حیاتی — هرگز skip نکن):**

این بخش از همهٔ بخش‌های دیگرِ این یادداشت **مهم‌تر** است. اگر نقض شود،
نتیجهٔ کار ممکن است مشروع به‌نظر برسد ولی در عمل بخش‌های دیگر سیستم را عقب
بیندازد، broken reference تولید کند، یا منجر به data corruption شود.

پیش از و حین تغییر، تمام وابستگی‌ها را در **چهار جهت** به‌طور **کامل و
بدون هیچ خلاصه‌سازی** شناسایی و همگام کن:

**۱. وابستگی‌های upstream (این تسک به چه چیزهایی متکی است):**
- چه فایل‌ها، توابع، کلاس‌ها، API endpoint ها، schema های دیتابیس،
  env vars، یا config هایی که این تسک نیاز دارد؟
- آیا قرار است چیزی را ویرایش/حذف کنی که جای دیگر (signature، رفتار،
  return type، side effect) از آن انتظار خاصی می‌رود؟
- اگر dependency جدیدی اضافه می‌کنی، آیا با dependencyهای موجود تداخل
  دارد (نسخه، compat، lock file)؟

**۲. وابستگی‌های downstream (چه چیزهایی به این تسک متکی‌اند):**
- چه فایل‌ها، توابع، تست‌ها، migrations، docs، یا UI component هایی از
  کدی که داری ویرایش/اضافه/حذف می‌کنی **استفاده می‌کنند**؟
- با grep و reference search **همه‌ی** call sites، importها، subclassها،
  reference های مستقیم و غیرمستقیم را پیدا کن — نه فقط چند مورد اصلی.
- خصوصاً برای حذف یا rename: هیچ broken reference نباید باقی بماند.

**۳. وابستگی‌های cross-tier (بسیار مهم — هرگز فقط یک لایه را نبین):**

تسک شما ممکن است از backend، frontend، database، worker، یا هر tier
دیگری شروع شده باشد. ولی تغییرات تقریباً همیشه روی tier های دیگر هم
اثر می‌گذارند. **مستقل از اینکه تسک از کدام tier است**، این چک‌های دو
طرفه را همیشه انجام بده:

🔁 **اگر backend را تغییر دادی** (API، service، model، route):
  → frontend: کدام component/page/hook این endpoint یا data shape را
    مصرف می‌کند؟ type definition، state shape، error handling، loading
    state، form validation، URL routing همگی باید همگام شوند.
  → mobile/SDK/client library (اگر پروژه دارد): همان داستان frontend.
  → database: آیا migration لازم است؟ آیا rollback امن است؟
  → background workers: آیا event producer/consumer ها تحت تأثیرند؟
  → rate limit، auth، CORS، CSP: آیا رفتار جدید پشتیبانی می‌شود؟

🔁 **اگر frontend را تغییر دادی** (component، form، state، route):
  → backend: آیا endpoint جدید/تغییریافته لازم است؟ آیا data shape ای
    که ارسال می‌شود با schema سرور سازگار است؟
  → backend validation: آیا برای ورودی‌های جدید UI کافی است؟
  → permissions/RBAC: آیا feature جدید نیاز به role check جدید دارد؟
  → analytics/tracking: آیا event های جدید باید در backend log شوند؟
  → SEO/SSR: آیا تغییر route نیاز به sitemap/meta tags جدید دارد؟

🔁 **اگر database/migration را تغییر دادی**:
  → backend models (ORM، Pydantic، dataclasses) همگی به‌روزند؟
  → query های raw SQL یا ORM queries با schema جدید سازگارند؟
  → seed data، fixtures، factory functions تست‌ها به‌روزند؟
  → frontend: آیا data shape جدید در UI به‌درستی render می‌شود؟
  → rollback migration نوشته شده و امن است؟

🔁 **اگر API contract یا event schema را تغییر دادی** (REST، GraphQL،
   WebSocket، gRPC، Kafka، …):
  → OpenAPI/GraphQL schema/proto file آپدیت شد؟
  → همه‌ی consumer ها (client، subscriber، webhook، external API
    user) با version جدید سازگارند؟
  → backward compatibility حفظ شده یا migration path روشن است؟
  → versioning header/path اگر breaking change است؟

🔁 **اگر infrastructure یا config را تغییر دادی** (Dockerfile، CI، Render
   config، env، secrets):
  → README setup/installation section به‌روزه؟
  → `.env.example` با env vars جدید آپدیت شد؟
  → deploy script یا CI workflow هم تغییر کرد؟
  → docs/architecture یا diagram های infrastructure به‌روزند؟

⚠️ **هرگز فقط یک tier را تغییر نده و فرض کنی بقیه خودکار همگام می‌شوند.**
   حتی برای تغییرات به‌ظاهر «کوچک»، چک کن.

**۴. وابستگی‌های جانبی (artifacts که همیشه چک شوند):**

تغییرات کد همیشه روی این artifact ها اثر دارند. **همه را** بررسی و
به‌روز کن — مستندات اولویت **بالا** دارد چون فراموش‌شدنی‌ترین است.

  📝 **مستندات** (همیشه چک کن — حتی برای تغییر کوچک کد):
    - README.md (شرح، setup، نمونه‌های استفاده، badge ها)
    - CHANGELOG.md / RELEASE_NOTES.md
    - docs/ folder (architecture، API reference، user guides، runbooks)
    - inline docstrings/کامنت‌های توابع و کلاس‌های تغییریافته
    - OpenAPI/Swagger annotations، JSDoc/TSDoc
    - architecture diagrams (اگر component اضافه/حذف شد)
    - migration guides (اگر breaking change است)

  🌍 **مستندات کاربر**:
    - i18n files و translation keys
    - UI labels، tooltip ها، help text، error messages
    - in-app onboarding (اگر flow جدید است)

  🧪 **تست‌ها**:
    - unit tests (همه‌ی فایل‌های مرتبط — حتی اگر «بی‌ربط» به‌نظر می‌رسد)
    - integration tests
    - e2e tests (Playwright/Cypress/Selenium)
    - snapshot tests (اگر UI تغییر کرد)
    - contract tests (Pact یا مشابه)
    - performance benchmarks (اگر behavior performance-sensitive تغییر کرد)

  🧬 **type definitions و contracts**:
    - .d.ts files
    - Pydantic models، dataclasses
    - Protobuf/Avro/Thrift schemas
    - GraphQL schema definitions
    - JSON Schemas

  🏗 **infrastructure و config**:
    - Dockerfile، docker-compose.yml
    - Kubernetes manifests
    - Render/Vercel/Netlify config
    - GitHub Actions / GitLab CI workflows
    - environment templates (.env.example، .env.sample)
    - feature flags (LaunchDarkly، GrowthBook، config)

  📊 **monitoring و observability**:
    - logging keys (اگر اضافه/حذف شد، log parser ها هم به‌روز شوند)
    - metric names (Prometheus، Datadog)
    - tracing spans
    - alert rules و dashboards
    - error tracking (Sentry rules، groupings)

  🔐 **security**:
    - auth rules (rate limit، CORS، CSP، HSTS)
    - permissions/RBAC config
    - secrets rotation policies
    - audit log events (اگر action جدید اضافه شد)

  💾 **caches و serialization**:
    - cache keys و TTL (اگر data shape یا lifecycle تغییر کرد)
    - serializer formats (Redis، session storage)
    - browser storage (localStorage، IndexedDB schemas)

**قانون مطلق همگام‌سازی:**
- هر چیزی که در (۱)، (۲)، (۳)، یا (۴) شناسایی شد، در **همان workflow
  این تسک** همگام و به‌روز شود. هرگز برای بعد رها نکن.
- اگر یک فایل/تست/docs نسبت به تغییر شما عقب بماند، در بهترین حالت bug،
  در بدترین حالت مشکل امنیتی یا data corruption تولید می‌کند.
- تغییرات همگام‌سازی می‌توانند در commit جداگانه باشند (در همان task)،
  ولی نباید skip شوند یا به «refactor آینده» سپرده شوند.

**هرگز این جمله‌ها قابل قبول نیست:**
- ❌ «بعداً پیداش می‌کنم»
- ❌ «احتمالاً جای دیگه‌ای استفاده نمی‌شه»
- ❌ «این یه refactor جداگانه‌ست — out of scope»
- ❌ «فقط فایل‌های اصلی رو بررسی کردم»
- ❌ «حدس می‌زنم چیزی بهش وابسته نیست»
- ❌ «دامنه‌ی وابستگی‌ها رو خلاصه کردم» — هرگز خلاصه نکن
- ❌ «این task فقط backend است؛ frontend مشکل خودش» — هرگز
- ❌ «این task فقط frontend است؛ backend از قبل کار می‌کند» — هرگز ثابت نکرده
- ❌ «مستندات بعداً به‌روز می‌شن» — همیشه same-task همگام شوند
- ❌ «testها رو نگاه نکردم چون فقط یه تغییر کوچیک بود»

**در commit message یا PR description**، دامنهٔ وابستگی‌های شناسایی‌شده و
همگام‌شده را به‌طور explicit و **per-tier** بنویس. مثال:
```
Dependencies synced:
- upstream: User model schema, auth middleware
- downstream: 3 API endpoints, 5 frontend components, 12 tests
- cross-tier (backend → frontend): UserProfile.tsx, useUser.ts hook,
  api-types.ts (TS definitions)
- cross-tier (backend → infra): .env.example added NEW_AUTH_SCOPES
- side artifacts: OpenAPI spec, README API section, i18n keys for
  new errors, Sentry alert rule for new error code
```
اگر هیچ وابستگی پیدا نکردی در هر کدام از چهار جهت، صریحاً بنویس:
«بررسی شد — هیچ وابستگی upstream / downstream / cross-tier (backend↔
frontend↔db↔infra) / side شناسایی نشد» تا مشخص باشد بررسی **انجام شده**
نه اینکه فراموش شده.

📋 **مدیریت TO-DO برای اقدامات دستی کاربر (همیشه چک کن):**

⚠️ **هشدار بحرانی — قاعدهٔ ضد-فرار:** TO-DO فقط برای کارهایی است که
**واقعاً غیرممکن** برای agent است (نیاز به انسان مطلق)، نه برای کارهایی
که «بزرگ‌اند»، «وقت می‌برند»، یا «نیازمند fixture/setup» هستند. اگر یک
agent در یک سشن بیش از **۲۰٪ از تسک‌ها** را با TO-DO ببندد، یعنی از کار
فرار می‌کند — این الگو در سشن‌های قبلی **مشاهده** شده و الان ممنوع است.

✅ **فقط برای این موارد TO-DO بساز** (لیست بسته — هرچه خارج این لیست
ممنوع است):

  ۱. **Credential/secret که فقط کاربر دارد**:
     - تنظیم API key واقعی در پنل ادمین خارجی (Render، AWS، Stripe، …)
     - تأیید OAuth client روی console آن سرویس
     - paste کردن webhook secret که فقط بعد از ساخت در dashboard ظاهر می‌شود

  ۲. **Account/billing روی سرویس خارجی که کاربر باید عضو شود**:
     - ساخت account جدید روی Stripe/SendGrid/Twilio/Google Cloud
     - تأیید verification شماره یا ID
     - فعال‌سازی subscription پولی

  ۳. **داده/asset خصوصی که فقط کاربر دارد**:
     - آپلود لوگو/تصویر/فونت برند
     - paste کردن داده‌ای که در محل کار کاربر است
     - import داده‌ای که فقط روی device کاربر است

  ۴. **تصمیم سلیقه‌ای/حقوقی/کسب‌وکار**:
     - انتخاب رنگ‌بندی نهایی یا تم
     - متن دقیق Terms of Service / Privacy Policy
     - تعرفهٔ قیمت‌گذاری
     - نام نهایی برند یا دامنه

⛔ **هرگز TO-DO نکن برای** (لیست سیاه — هر چیزی که در این لیست است
**قابل اجرا** توسط agent است، حتی اگر بزرگ یا چندبخشی باشد):

  ❌ UI component / page / dashboard (هر فریم‌ورک: React, Vue, Angular,
     Svelte، حتی اگر معماری بزرگ دارد) — می‌توانی stub اولیه + state
     management + layout + استایل بسازی
  ❌ "نیازمند Google Drive / Stripe / Twilio API" — می‌توانی **client
     stub** با abstraction layer بسازی که با env var واقعی plug-in شود؛
     کد integration یعنی پیاده‌سازی، نه TO-DO
  ❌ "feature بزرگ، چند روز کار می‌برد" — اندازه دلیل defer نیست؛ کوچک
     شروع کن، iterate کن، در همین سشن کامل کن
  ❌ Celery / background worker / scheduler — یک task ساده + register
     می‌توانی بسازی
  ❌ Migration / model / schema — حتی اگر فیلد جدید نیاز دارد، اضافه کن
  ❌ REST endpoint / GraphQL resolver / WebSocket route — هرگز TO-DO
  ❌ test (unit/integration/e2e) — همیشه قابل نوشتن
  ❌ Documentation / README / API docs — همیشه قابل نوشتن
  ❌ Config file / .env.example / Dockerfile / CI workflow — همیشه قابل
     نوشتن
  ❌ "می‌توانستی .tsx ولی repo .jsx است" — از .jsx استفاده کن، TO-DO نکن
  ❌ "نیازمند فیلد X در مدل دیگر" — اضافه کن فیلد را، TO-DO نکن
  ❌ "تصمیم admin-vs-user-scoped" — پرامپت اولیه scope را معلوم کرده،
     یا با محتاطانه‌ترین تفسیر پیش برو
  ❌ "credential در production هنوز ست نیست" — این TO-DO ساده برای
     تنظیم env var است (مورد ۱ بالا)، نه دلیل برای defer کردن کد
  ❌ "نیازمند verification از کاربر" — اگر اقدام واقعی غیرممکن نیست،
     پیش برو
  ❌ هر چیزی که در یک کامنت `# TODO` معمولی نوشته می‌شد — این فایل
     TO-DO نیست، کامنت inline است

🔬 **قاعدهٔ «حداقل تلاش» قبل از TO-DO**: قبل از TO-DO کردن یک AC، **اثبات
کن** که قابل انجام نیست:

  ۱. آیا می‌توانم یک stub/placeholder بسازم که با env واقعی plug-in شود؟
     → اگر بله، بساز و TO-DO نکن
  ۲. آیا می‌توانم برای این بخش یک test (حتی mock-based) بنویسم؟
     → اگر بله، بنویس و TO-DO نکن
  ۳. آیا می‌توانم abstraction/interface را تعریف کنم، حتی اگر backend
     واقعی نیست؟ → اگر بله، تعریف کن و TO-DO نکن
  ۴. آیا فقط یک حالت سلیقه‌ای/decision کاربر در میان است؟
     → فقط آن یک decision را TO-DO کن، نه کل feature را

اگر یکی از این چهار راه‌حل ممکن بود ولی به TO-DO رفتی، **اعتبار شما از
بین می‌رود**.

📊 **آستانهٔ TO-DO per session**: در یک حلقهٔ اجرای N تسک، اگر بیشتر از
**۲۰٪** تسک‌ها فایل TO-DO ساختی، خودت در گزارش پایانی صریحاً اعلام کن:

  "⚠️ نسبت TO-DO من {K}/{N} = {%} است که از آستانهٔ ۲۰٪ بالاتر است.
   احتمالاً برخی از این TO-DO ها قابل اجرا بودند ولی من فرار کردم.
   لیست TO-DO ها را کاربر باید بازبینی کند که آیا واقعاً Manual-required
   بودند یا agent ضعیف کار کرده."

**یادآوری همیشگی:** اگر در آینده قابلیت‌های شما گسترش پیدا کرد و توانستید
یکی از موارد لیست سفید را خودکار انجام دهید (مثلاً managed credential
injection، یا integration پولی automate شود)، انجام دهید و TO-DO نسازید.
لیست سفید بسته است ولی **بسته از پایین** (می‌تواند کوچک‌تر شود اگر
قابلیت‌ها رشد کنند، ولی هرگز بزرگ‌تر نشود برای فرار).

**اگر هیچ بخش Manual-required نبود (تمام تسک Auto-capable است)**:
  → فایل TO-DO **نساز**. فولدر TO-DO/ باید پاک و معنادار بماند.
  → اگر برای این task از قبل `TO-DO/todo-task-{task_id_first_8}.md` بود
     (یعنی در run قبلی نیاز به دخالت کاربر بود ولی الان نه): فایل قدیمی
     را پاک کن و entry را از `TO-DO/_index.json` حذف کن.

**اگر بخش Manual-required دارد** (همه‌جانبه یا hybrid):
  1. فولدر TO-DO/ را در ریشه ریپو ایجاد کن اگر نیست
  2. فایل `TO-DO/todo-task-{task_id_first_8}.md` بساز با front-matter
     شامل: task_id, task_title, execution_priority, created_at,
     updated_at, status: "pending"
     و در بدنه: «چرا این فایل ساخته شد»، «وضعیت بخش‌های خودکار»
     (commit ها reference)، «کارهایی که باید انجام دهی» با اولویت
     بالا/متوسط/پایین به ترتیب، «وقتی این کارها را تمام کردی»
  3. `TO-DO/_index.json` را با **merge** آپدیت کن (نه overwrite):
     - فایل موجود را بخوان
     - entry های orphan (فایلشان پاک شده) را حذف کن
     - entry این task را اضافه/replace کن
     - بر اساس execution_priority صعودی مرتب کن
     - ساختار: `{"version":1, "generated_at": ISO, "total": N, "items": [...]}`
  4. این تغییرات TO-DO را در **همان commit کد** شامل کن (نه commit جداگانه)

⛔ **ممنوعات مطلق TO-DO**:
  ❌ ساختن TO-DO برای کاری که می‌توانستی خودت انجام دهی (شلوغی فولدر)
  ❌ overwrite کردن `TO-DO/_index.json` بدون merge (data loss)
  ❌ نگه‌داشتن entry هایی که فایل‌شان پاک شده (broken reference)
  ❌ فراموش کردن نوشتن «خروجی مورد انتظار» در هر آیتم TO-DO

این بخش الزامی است. حتی اگر فکر می‌کنی "این تسک کاملاً auto است و نیازی
به TO-DO نیست"، صریحاً در commit message یا report بنویس:
"بررسی شد — این تسک هیچ بخش Manual-required ندارد، TO-DO ساخته نشد."

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

🔁 **Commit + Push فوری per-task (بسیار مهم برای جریان کار صحیح):**

پس از اتمام پیاده‌سازی این تسک، **بلافاصله** commit کن و **همان موقع**
به default branch (main/master) push کن. سپس به تسک بعدی برو.

✓ چرا این قانون حیاتی است:
  - تسک‌های بعدی ممکن است به فایل‌ها/تغییراتی که این تسک ایجاد کرده
    نیاز داشته باشند. اگر push نکنی، `git pull` بعدی آن‌ها را نمی‌بیند.
  - جمع‌کردن تغییرات چند تسک منجر به conflict های بزرگ می‌شود.
  - اگر در میانه fail کنی، task های push شده ضایع نمی‌شوند.

⛔ ممنوع: "همه task ها را تمام می‌کنم بعد یک‌جا push می‌زنم"
⛔ ممنوع: branch جدا برای task — مستقیم به default branch
⛔ ممنوع: task بعدی بدون push کامل task قبلی

---


---

## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند.)_

```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم و از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....
```

## 📋 چک‌لیست مراحل (10 مرحله)

این تسک به مراحل کوچک‌تر تقسیم شده. **در هر verify خودکار، وضعیت هر مرحله به‌صورت `[ ]` (انجام نشده)، `[~]` (ناقص)، یا `[x]` (انجام شده) به‌روز می‌شود.**
وقتی تمام مراحل `[x]` شدند، تسک به‌طور خودکار به «انجام شده» منتقل می‌شود.

- [ ] **مرحله 1: ایجاد ساختار پایه برای اتصال تلگرام دوطرفه (Telegram Bot API + Webhook)** — این مرحله شامل ایجاد یک بات تلگرام جدید (یا استفاده از بات موجود) و راه‌اندازی Webhook برای دریافت پیام‌های کاربر از تلگرام است. باید یک endpoint در بک‌اند (مثلاً FastAPI یا Django) ایجاد شود که پیام‌های دریافتی از تلگرام را پردازش کند. این مرحله فقط ارتباط پایه را فراهم می‌کند و شامل منطق پردازش پی
- [ ] **مرحله 2: پیاده‌سازی ارسال نوتیفیکیشن‌های خودکار از سیستم به تلگرام** — این مرحله شامل ایجاد یک سرویس یا تابع در بک‌اند است که بتواند پیام‌های نوتیفیکیشن (مثلاً یادآوری، هشدار، اطلاع‌رسانی) را به کاربر از طریق تلگرام ارسال کند. باید مشخص شود چه رویدادهایی باعث ارسال نوتیفیکیشن می‌شوند (مثلاً ثبت تمرین جدید، اتمام درس، خطا). این مرحله فقط ارسال یک‌طرفه از سیستم به تلگرام
- [ ] **مرحله 3: پیاده‌سازی دستورات پایه کنترل سیستم از طریق تلگرام (مثلاً /status, /help, /list_courses)** — این مرحله شامل تعریف و پیاده‌سازی چند دستور پایه تلگرامی است که به کاربر اجازه می‌دهد وضعیت سیستم را ببیند، لیست دروس را دریافت کند، و راهنما را مشاهده کند. باید یک handler برای هر دستور در Webhook ایجاد شود. این مرحله فقط دستورات ساده و فقط-متن را پوشش می‌دهد و شامل عملیات پیچیده (مثل آپلود فایل) ن
- [ ] **مرحله 4: ایجاد قابلیت مشاهده لیست دروس و محتوای آنها از طریق تلگرام** — این مرحله شامل پیاده‌سازی دستورات تلگرامی برای مشاهده لیست دروس، مشاهده جزئیات یک درس خاص (مثلاً /course <id>)، و مشاهده لیست جلسات یا فایل‌های یک درس است. باید داده‌ها از دیتابیس پروژه خوانده شده و به صورت فرمت‌شده (متن یا دکمه‌های inline) نمایش داده شوند. این مرحله فقط مشاهده را پوشش می‌دهد و شامل
- [ ] **مرحله 5: پیاده‌سازی قابلیت آپلود فایل (محتوای دروس) از طریق تلگرام** — این مرحله شامل ایجاد قابلیت دریافت فایل‌های ارسالی کاربر از تلگرام (مثلاً PDF، تصویر، صوت) و ذخیره‌سازی آنها در سیستم (مثلاً در پوشه media یا S3) و مرتبط‌سازی با درس مربوطه است. باید از طریق دستور /upload یا دکمه inline انجام شود. این مرحله فقط آپلود را پوشش می‌دهد و شامل پردازش محتوا (مثل OCR یا تح
- [ ] **مرحله 6: ایجاد قابلیت مدیریت موضوعات تمرین با استاد هوش مصنوعی از طریق تلگرام (مشاهده و انتخاب موضوع)** — این مرحله شامل اتصال به بخش 'تمرین با استاد هوش مصنوعی' و پیاده‌سازی دستورات تلگرامی برای مشاهده لیست موضوعات موجود، مشاهده جزئیات یک موضوع، و انتخاب یک موضوع برای شروع تمرین است. باید از API داخلی پروژه برای دریافت موضوعات استفاده شود. این مرحله فقط مدیریت موضوعات را پوشش می‌دهد و شامل خود چت با هو
- [ ] **مرحله 7: پیاده‌سازی چت متنی دوطرفه با هوش مصنوعی از طریق تلگرام** — این مرحله شامل ایجاد یک مکالمه (session) بین کاربر و هوش مصنوعی از طریق تلگرام است. پس از انتخاب موضوع، کاربر می‌تواند پیام متنی ارسال کند و پاسخ از هوش مصنوعی دریافت کند. باید state management برای هر کاربر (مثلاً با user_id) پیاده‌سازی شود تا مکالمه در جریان باشد. این مرحله فقط چت متنی را پوشش می‌
- [ ] **مرحله 8: پیاده‌سازی ارسال پیام صوتی از تلگرام به هوش مصنوعی برای تحلیل و استخراج** — این مرحله شامل دریافت فایل صوتی از کاربر تلگرام، ارسال آن به سرویس تحلیل صوت (که احتمالاً در بخش 'تمرین با استاد هوش مصنوعی' وجود دارد)، دریافت نتیجه تحلیل (متن استخراج‌شده)، و ارسال آن به هوش مصنوعی برای پردازش است. باید از میکروفون تلگرام (voice message) پشتیبانی شود. این مرحله فقط ارسال صوت و دری
- [ ] **مرحله 9: پیاده‌سازی ارسال پاسخ صوتی از هوش مصنوعی به تلگرام (text-to-speech)** — این مرحله شامل گرفتن پاسخ متنی از هوش مصنوعی، تبدیل آن به صوت (با استفاده از یک سرویس TTS مانند Google TTS یا API اختصاصی)، و ارسال فایل صوتی به کاربر در تلگرام است. همچنین باید متن پاسخ نیز همزمان ارسال شود. این مرحله فقط خروجی صوتی را پوشش می‌دهد.
- [ ] **مرحله 10: همگام‌سازی کامل چت تلگرام با چت وب (تمرین با استاد هوش مصنوعی)** — این مرحله شامل اطمینان از همگام‌سازی دوطرفه پیام‌ها بین چت تلگرام و چت وب است. یعنی هر پیامی که در وب ارسال می‌شود در تلگرام نیز نمایش داده شود و بالعکس. باید از یک message queue یا WebSocket برای همگام‌سازی استفاده شود. این مرحله شامل تست یکپارچگی کامل است.

---

# 🔹 مرحله 1: ایجاد ساختار پایه برای اتصال تلگرام دوطرفه (Telegram Bot API + Webhook)

**Scope:** این مرحله شامل ایجاد یک بات تلگرام جدید (یا استفاده از بات موجود) و راه‌اندازی Webhook برای دریافت پیام‌های کاربر از تلگرام است. باید یک endpoint در بک‌اند (مثلاً FastAPI یا Django) ایجاد شود که پیام‌های دریافتی از تلگرام را پردازش کند. این مرحله فقط ارتباط پایه را فراهم می‌کند و شامل منطق پردازش پیام‌ها یا دستورات خاص نیست. نکته حیاتی: باید از کتابخانه python-telegram-bot یا aiogram استفاده شود و Webhook به صورت HTTPS (با SSL) کار کند.
**Key terms:** Telegram Bot API, Webhook, python-telegram-bot, aiogram, HTTPS, endpoint /webhook

**بخش مربوط از متن کاربر:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد ساختار پایه اتصال تلگرام دوطرفه با Webhook

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:38-45` — `app و server و wss` — محل فعلی ایجاد سرور Express و WebSocket. پس از این خطوط باید سرویس تلگرام راه‌اندازی شود.
  ```jsx
  const app = express();
  const PORT = process.env.PORT || 3001;
  
  // Create HTTP server
  const server = createServer(app);
  
  // Create WebSocket server
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/server.js:53-54` — `GEMINI_API_KEY` — الگوی خواندن API key از environment variables. برای TELEGRAM_BOT_TOKEN نیز باید به همین شکل عمل شود.
  ```jsx
  const GEMINI_API_KEY = process.env.GEMINI_API_KEY;
  ```
- `backend/.env.example:1-7` — `فایل env.example` — باید خط TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here به این فایل اضافه شود.
  ```
  # Gemini API Key - Get from https://aistudio.google.com/apikey
  GEMINI_API_KEY=your_gemini_api_key_here
  
  # Port (optional, default 3001)
  PORT=3001
  
  # Firebase config (optional - for data persistence)
  # VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/package.json` — برای افزودن وابستگی telegraf به dependencies نیاز به به‌روزرسانی دارد.
- `backend/package-lock.json` — پس از نصب telegraf به‌روزرسانی می‌شود.
- `frontend/src/App.jsx` — ممکن است در آینده برای نمایش

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد ساختار پایه برای اتصال تلگرام دوطرفه (Telegram Bot API + Webhook) را داده است. این مرحله شامل ایجاد یک بات تلگرام جدید (یا استفاده از بات موجود) و راه‌اندازی Webhook برای دریافت پیام‌های کاربر از تلگرام است. باید یک endpoint در بک‌اند (مثلاً FastAPI یا Django) ایجاد شود که پیام‌های دریافتی از تلگرام را پردازش کند. این مرحله فقط ارتباط پایه را فراهم می‌کند و شامل منطق پردازش پیام‌ها یا دستورات خاص نیست. نکته حیاتی: باید از کتابخانه python-telegram-bot یا aiogram استفاده شود و Webhook به صورت HTTPS (با SSL) کار کند.

بخش مربوط از درخواست اصلی کاربر: «میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم»

کلیدواژه‌ها: Telegram Bot API, Webhook, python-telegram-bot, aiogram, HTTPS, endpoint /webhook

شواهد در کد واقعی پروژه: پروژه فعلی یک اپلیکیشن Node.js/Express با بک‌اند در backend/server.js (خطوط 1-1403) و فرانت‌اند React/Vite است. بک‌اند فعلی از Express (خط 1)، WebSocket (خط 7)، multer (خط 8)، و ffmpeg (خط 11) استفاده می‌کند. هیچ فایل پایتونی در ساختار پروژه وجود ندارد. فایل‌های backend/package.json و backend/package-lock.json نشان می‌دهند که وابستگی‌های فعلی شامل express, cors, dotenv, ws, multer, fluent-ffmpeg, ffmpeg-static, pdf-parse هستند. فایل backend/.env.example (خطوط 1-7) شامل GEMINI_API_KEY و PORT است. فایل frontend/index.html (خطوط 1-208) شامل Firebase Config و Inspector Bridge Script است. فایل .github/workflows/claude-auto-task.yml (خطوط 1-84) شامل workflow خودکار Claude Code است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. با توجه به اینکه پروژه فعلی به زبان JavaScript (Node.js/Express) نوشته شده و هیچ فایل پایتونی در ساختار پروژه وجود ندارد، پیشنهاد می‌شود به جای استفاده از کتابخانه‌های پایتونی (python-telegram-bot یا aiogram) که کاربر اشاره کرده، از کتابخانه Node.js مانند node-telegram-bot-api یا telegraf.js استفاده شود. مراحل پیاده‌سازی:

1. نصب وابستگی: `npm install telegraf` در پوشه backend
2. ایجاد فایل جدید backend/services/telegramBot.js شامل:
   - راه‌اندازی Bot با توکن از متغیر محیطی TELEGRAM_BOT_TOKEN
   - تنظیم Webhook با آدرس HTTPS (مثلاً https://domain.com/api/telegram/webhook)
   - تابع پردازش پیام‌های دریافتی (فعلاً فقط log و reply ساده)
3. افزودن endpoint جدید در backend/server.js:
   - POST /api/telegram/webhook برای دریافت Webhook از تلگرام
   - GET /api/telegram/set-webhook برای تنظیم Webhook (دستی)
4. افزودن متغیر محیطی TELEGRAM_BOT_TOKEN به backend/.env.example
5. به‌روزرسانی backend/server.js برای import و راه‌اندازی سرویس تلگرام در زمان启动 سرور (حدود خط 42-45)
6. اطمینان از HTTPS: در محیط production از reverse proxy (مثلاً Nginx با SSL) استفاده شود. در توسعه از ngrok یا سرویس مشابه برای ایجاد تونل HTTPS استفاده شود.

توجه: کاربر به python-telegram-bot و aiogram اشاره کرده اما پروژه JavaScript است. این تغییر رویکرد باید در توضیحات ذکر شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 2: پیاده‌سازی ارسال نوتیفیکیشن‌های خودکار از سیستم به تلگرام

**Scope:** این مرحله شامل ایجاد یک سرویس یا تابع در بک‌اند است که بتواند پیام‌های نوتیفیکیشن (مثلاً یادآوری، هشدار، اطلاع‌رسانی) را به کاربر از طریق تلگرام ارسال کند. باید مشخص شود چه رویدادهایی باعث ارسال نوتیفیکیشن می‌شوند (مثلاً ثبت تمرین جدید، اتمام درس، خطا). این مرحله فقط ارسال یک‌طرفه از سیستم به تلگرام را پوشش می‌دهد و شامل دریافت دستورات نیست.
**Key terms:** NotificationService, send_telegram_message, event-driven, scheduler, Telegram Bot

**بخش مربوط از متن کاربر:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن سرویس نوتیفیکیشن تلگرام یک‌طرفه به بک‌اند

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:1-14` — `import section` — محل افزودن import سرویس‌های جدید نوتیفیکیشن و تلگرام
  ```jsx
  import express from 'express';
  import cors from 'cors';
  import { fileURLToPath } from 'url';
  import { dirname, join } from 'path';
  import dotenv from 'dotenv';
  import { createServer } from 'http';
  import { WebSocketServer, WebSocket } from 'ws';
  import multer from 'multer';
  import fs from 'fs';
  import os from 'os';
  import ffmpeg from 'fluent-ffmpeg';
  import ffmpegStatic from 'ffmpeg-static';
  ```
- `backend/server.js:38-48` — `app initialization` — محل افزودن middleware و endpoint جدید برای نوتیفیکیشن
  ```jsx
  const app = express();
  const PORT = process.env.PORT || 3001;
  
  // Create HTTP server
  const server = createServer(app);
  
  // Create WebSocket server
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  
  app.use(cors());
  app.use(express.json({ limit: '10mb' }));
  ```
- `backend/.env.example:1-8` — `environment variables` — محل افزودن TELEGRAM_BOT_TOKEN و TELEGRAM_CHAT_ID
  ```
  # Gemini API Key - Get from https://aistudio.google.com/apikey
  GEMINI_API_KEY=your_gemini_api_key_here
  
  # Port (optional, default 3001)
  PORT=3001
  
  # Firebase config (optional - for data persistence)
  # VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
JavaScript (Node.js), Express.js, WebSocket (ws), Gemini API, Firebase, Telegram Bot API

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/package.json` (سطر 1) — برای افزودن وابستگی‌های جدید (node-fetch یا axios) به dependencies نیاز به تغییر دارد
- `package.json` (سطر 1) — برای افزودن اسکریپت نصب وابستگی‌های جدید (install:all) نیاز به تغییر دارد
- `frontend/src/App.jsx` (سطر 1) — ممکن است نیاز به نمایش وضعیت نوتیفیکیشن یا دکمه تست ارسال در UI داشته باشد
- `frontend/index.html` (سطر 1) — ممکن است نیاز به افزودن Firebase Cloud Messaging برای نوتیفیکیشن‌های مرورگر داشته باشد

## 🌐 نقشهٔ وابستگی‌ها
این تغییرات به فایل‌های backend/server.js (به عنوان نقطه ورود اصلی بک‌اند)، backend/.env.example (برای متغیرهای محیطی جدید)، backend/package.json (برای وابستگی‌های جدید)، و package.json ریشه (برای اسکریپت‌های نصب) وابسته است. سرویس جدید telegramService.js توسط notificationService.js فراخوانی می‌شود و notificationService.js توسط رویدادهای مختلف در server.js (مانند ثبت تمرین، اتمام درس، خطا) صدا زده می‌شود. همچنین ممکن است نیاز به تغییر در frontend/src/App.jsx برای نمایش وضعیت نوتیفیکیشن باشد.

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی ارسال نوتیفیکیشن‌های خودکار از سیستم به تلگرام را دارد. این شامل ایجاد یک سرویس یا تابع در بک‌اند است که بتواند پیام‌های نوتیفیکیشن (مثلاً یادآوری، هشدار، اطلاع‌رسانی) را به کاربر از طریق تلگرام ارسال کند. باید مشخص شود چه رویدادهایی باعث ارسال نوتیفیکیشن می‌شوند (مثلاً ثبت تمرین جدید، اتمام درس، خطا). این مرحله فقط ارسال یک‌طرفه از سیستم به تلگرام را پوشش می‌دهد و شامل دریافت دستورات نیست. کاربر همچنین اشاره کرده که در نهایت می‌خواهد تلگرام دو طرفه کار کند یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته. کلیدواژه‌های ذکر شده: NotificationService, send_telegram_message, event-driven, scheduler, Telegram Bot. در کد فعلی پروژه (backend/server.js) هیچ سرویس تلگرام یا نوتیفیکیشنی وجود ندارد. پروژه از Express.js (خط ۱-۴۲)، WebSocket (خط ۴۵)، و Gemini API (خطوط ۵۶-۱۶۴) استفاده می‌کند. فایل backend/.env.example فقط شامل GEMINI_API_KEY و PORT و Firebase config است. فایل package.json در ریشه پروژه اسکریپت‌های dev, build, start را دارد. فایل frontend/index.html شامل Firebase config و Inspector Bridge Script است. فایل .github/workflows/claude-auto-task.yml workflow خودکار Claude Code را مدیریت می‌کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ۱. ایجاد فایل جدید backend/services/telegramService.js با تابع sendTelegramMessage که از Telegram Bot API (https://api.telegram.org/bot<TOKEN>/sendMessage) استفاده کند. ۲. افزودن متغیر محیطی TELEGRAM_BOT_TOKEN و TELEGRAM_CHAT_ID به backend/.env.example. ۳. ایجاد فایل backend/services/notificationService.js که رویدادهای مختلف (ثبت تمرین جدید، اتمام درس، خطا) را مدیریت کرده و با صدا زدن telegramService پیام ارسال کند. ۴. افزودن endpoint جدید POST /api/notifications/send در backend/server.js برای تست دستی ارسال نوتیفیکیشن. ۵. نصب وابستگی node-fetch یا استفاده از fetch داخلی Node.js (موجود در خطوط ۸۶-۸۹ backend/server.js). ۶. افزودن اسکریپت نصب به package.json برای نصب وابستگی‌های جدید.

## 💡 نمونه‌های قبل/بعد
**نمونه 1**

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 3: پیاده‌سازی دستورات پایه کنترل سیستم از طریق تلگرام (مثلاً /status, /help, /list_courses)

**Scope:** این مرحله شامل تعریف و پیاده‌سازی چند دستور پایه تلگرامی است که به کاربر اجازه می‌دهد وضعیت سیستم را ببیند، لیست دروس را دریافت کند، و راهنما را مشاهده کند. باید یک handler برای هر دستور در Webhook ایجاد شود. این مرحله فقط دستورات ساده و فقط-متن را پوشش می‌دهد و شامل عملیات پیچیده (مثل آپلود فایل) نیست.
**Key terms:** CommandHandler, /status, /help, /list_courses, Telegram update

**بخش مربوط از متن کاربر:**
```
بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی دستورات پایه تلگرام /status, /help, /list_courses

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:38-48` — `app (Express application)` — محل فعلی ایجاد Express app و middlewareها. Webhook تلگرام باید به همین app اضافه شود (مثلاً app.post('/api/telegram/webhook', ...)).
  ```jsx
  const app = express();
  const PORT = process.env.PORT || 3001;
  
  // Create HTTP server
  const server = createServer(app);
  
  // Create WebSocket server
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  
  app.use(cors());
  app.use(express.json({ limit: '10mb' }));
  ```
- `backend/server.js:53-54` — `GEMINI_API_KEY` — الگوی خواندن API key از env. برای تلگرام هم باید TELEGRAM_BOT_TOKEN به همین شکل خوانده شود.
  ```jsx
  const GEMINI_API_KEY = process.env.GEMINI_API_KEY;
  ```
- `backend/.env.example:1-6` — `فایل env.example` — باید خط TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here به این فایل اضافه شود.
  ```
  # Gemini API Key - Get from https://aistudio.google.com/apikey
  GEMINI_API_KEY=your_gemini_api_key_here
  
  # Port (optional, default 3001)
  PORT=3001
  
  # Firebase config (optional - for data persistence)
  # VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
JavaScript (Node.js), Express.js, node-telegram-bot-api (پیشنهادی), Webhook, Gemini API (برای /status)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/server.js` (سطر 53) — فایل اصلی backend که Webhook تلگرام باید به آن اضافه شود. همچنین باید TELEGRAM_BOT_TOKEN را از env بخواند.
- `backend/.env.example` (سطر 1) — برای اضافه کردن متغیر محیطی TELEGRAM_BOT_TOKEN به عنوان مثال برای توسعه‌دهندگان.
- `backend/package.json` (سطر 1) — برای اضافه کردن وابستگی node-telegram-bot-api به dependencies.
- `frontend/src/App.jsx` (سطر 1) — احتمالاً نیاز به نمایش وضعیت اتصال تلگرام در UI وجود دارد (اختیاری).
- `prompt/_index.json` (سطر 1) — برای ثبت این تسک در لیست تسک‌های پروژه (اگر از سیستم oversight استفاده می‌شود).

## 🌐 نقشهٔ وابستگی‌ها
این قابلیت به فایل جدید backend/telegramBot.js وابسته است که توسط backend/server.js import می‌شود. کتابخانه node-telegram-bot-api باید به package.json اضافه شود. Webhook endpoint (POST /api/telegram/webhook) در server.js ثبت می‌شود. متغیر محیطی TELEGRAM_BOT_TOKEN باید در .env و .env.example تعریف شود. دستور /status می‌تواند از توابع موجود در server.js (مانند health check در خط 167) استفاده کند. دستور /list_courses ممکن است به یک فایل JSON یا دیتابیس نیاز داشته باشد.

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی دستورات پایه کنترل سیستم از طریق تلگرام را دارد. دستورات مشخص شده عبارتند از: /status, /help, /list_courses. این دستورات باید از طریق Webhook تلگرام پردازش شوند و فقط پاسخ‌های متنی ساده برگردانند. کاربر تأکید کرده که این مرحله فقط دستورات ساده و فقط-متن را پوشش می‌دهد و شامل عملیات پیچیده (مثل آپلود فایل) نیست. بخش مربوط از درخواست اصلی کاربر: 'بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم'. کلیدواژه‌های ذکر شده: CommandHandler, /status, /help, /list_courses, Telegram update. در کد فعلی پروژه (backend/server.js) هیچ Webhook یا هندلری برای تلگرام وجود ندارد. فایل backend/server.js شامل APIهای مربوط به Gemini (Chat در خط 56، TTS در خط 117، File Analysis در خط 698) و WebSocket در خط 45 است. فایل frontend/index.html شامل Firebase config و Inspector Bridge Script است. فایل .github/workflows/claude-auto-task.yml شامل workflow خودکار Claude Code است. هیچ فایل deep-read شده‌ای مستقیماً به تلگرام مرتبط نیست، بنابراین باید یک فایل جدید مانند backend/telegramBot.js یا backend/services/telegramHandler.js ایجاد شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/telegramBot.js شامل: import کتابخانه node-telegram-bot-api (یا استفاده از Webhook با express). 2. تعریف کلاس یا مجموعه توابع برای هندل کردن دستورات /status, /help, /list_courses. 3. پیاده‌سازی /status: بررسی وضعیت سرویس‌های backend (اتصال به Gemini API، uptime، وضعیت WebSocket). 4. پیاده‌سازی /help: نمایش لیست دستورات موجود با توضیح کوتاه. 5. پیاده‌سازی /list_courses: برگرداندن لیست دروس موجود (می‌تواند از یک فایل JSON یا دیتابیس خوانده شود). 6. اضافه کردن Webhook endpoint در backend/server.js (مثلاً POST /api/telegram/webhook) که پیام‌های تلگرام را دریافت کرده و به هندلر مناسب هدایت کند. 7. تنظیم Webhook در تلگرام (با استفاده از BotFather یا API). 8. افزودن متغیر محیطی TELEGRAM_BOT_TOKEN به backend/.env.example. 9. تست دستورات با ارسال پیام به بات تلگرام.

## 💡 نمونه‌های قبل/بعد
**اضافه کردن Webhook endpoint به server.js**

_قبل:_
```
// Health check
app.get('/api/health', (req, res) => {
  res.json({ status: 'ok', timestamp: new Date().toISOString() });
});
```

_بعد:_
```
// Health check
app.get('/api/health', (req,
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 4: ایجاد قابلیت مشاهده لیست دروس و محتوای آنها از طریق تلگرام

**Scope:** این مرحله شامل پیاده‌سازی دستورات تلگرامی برای مشاهده لیست دروس، مشاهده جزئیات یک درس خاص (مثلاً /course <id>)، و مشاهده لیست جلسات یا فایل‌های یک درس است. باید داده‌ها از دیتابیس پروژه خوانده شده و به صورت فرمت‌شده (متن یا دکمه‌های inline) نمایش داده شوند. این مرحله فقط مشاهده را پوشش می‌دهد و شامل آپلود یا ویرایش نیست.
**Key terms:** /courses, /course {id}, InlineKeyboardButton, database query, Course model

**بخش مربوط از متن کاربر:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن دستورات تلگرامی برای مشاهده لیست دروس و محتوای آنها

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:38-48` — `app initialization` — محل فعلی راه‌اندازی Express. باید پس از app.use(express.json(...)) یک middleware برای webhook تلگرام اضافه شود (مثلاً app.post('/api/telegram/webhook', ...)).
  ```jsx
  const app = express();
  const PORT = process.env.PORT || 3001;
  
  // Create HTTP server
  const server = createServer(app);
  
  // Create WebSocket server
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  
  app.use(cors());
  app.use(express.json({ limit: '10mb' }));
  ```
- `backend/server.js:53-54` — `GEMINI_API_KEY constant` — الگوی مشابه برای خواندن TELEGRAM_BOT_TOKEN از environment variables. باید const TELEGRAM_BOT_TOKEN = process.env.TELEGRAM_BOT_TOKEN; اضافه شود.
  ```jsx
  const GEMINI_API_KEY = process.env.GEMINI_API_KEY;
  ```
- `backend/.env.example:1-8` — `environment variables template` — باید خط # Telegram Bot Token - Get from @BotFather
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here اضافه شود.
  ```
  # Gemini API Key - Get from https://aistudio.google.com/apikey
  GEMINI_API_KEY=your_gemini_api_key_here
  
  # Port (optional, default 3001)
  PORT=3001
  
  # Firebase config (optional - for data persistence)
  # VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/package.json` (سطر 1) — برای افزودن وابستگی node-telegram-bot-api به dependencies
- `backend/package-lock.json` (سطر 1) — بعد از نصب وابستگی جدید به‌روزرسانی می‌شود
- `frontend/src/App.jsx` (سطر 1) — ممکن است نیاز به نمایش لیست دروس در UI داشته باشد (اما در scope فعلی نیست)

## 🌐 نقشهٔ وابستگی‌ها
این قابلیت به فایل‌های جدیدی نیاز دارد: backend/services/telegramBot.js (مدیریت ربات تلگرام

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد قابلیت مشاهده لیست دروس و محتوای آنها از طریق تلگرام را دارد. این شامل پیاده‌سازی دستورات تلگرامی برای مشاهده لیست دروس، مشاهده جزئیات یک درس خاص (مثلاً /course <id>)، و مشاهده لیست جلسات یا فایل‌های یک درس است. داده‌ها باید از دیتابیس پروژه خوانده شده و به صورت فرمت‌شده (متن یا دکمه‌های inline) نمایش داده شوند. این مرحله فقط مشاهده را پوشش می‌دهد و شامل آپلود یا ویرایش نیست. کلیدواژه‌های ذکر شده: /courses, /course {id}, InlineKeyboardButton, database query, Course model. کاربر همچنین اشاره کرده: 'و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم'. در کد فعلی پروژه (backend/server.js) هیچ endpoint یا مدل دیتابیسی برای Course وجود ندارد. فایل‌های backend/server.js (خطوط 1-1403) شامل APIهای مربوط به Gemini Chat (خط 56), TTS (خط 117), File Analysis (خط 698) و Health Check (خط 167) است. هیچ اشاره‌ای به Telegram Bot API یا مدل Course در فایل‌های deep-read شده دیده نمی‌شود. پروژه از Express.js (backend/server.js خط 38) و WebSocket (خط 45) استفاده می‌کند. فایل frontend/index.html شامل Firebase Config (خطوط 11-18) و Inspector Bridge Script (خطوط 32-201) است. فایل .github/workflows/claude-auto-task.yml (خطوط 1-84) workflow خودکار Claude Code را تعریف می‌کند. backend/package-lock.json و frontend/package-lock.json وابستگی‌ها را نشان می‌دهند. frontend/src/App.jsx کامپوننت اصلی React است. هیچ فایل مرتبط با Telegram Bot یا مدل Course در ساختار پروژه وجود ندارد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/services/telegramBot.js برای مدیریت ربات تلگرام با استفاده از کتابخانه node-telegram-bot-api. 2. ایجاد مدل Course در backend/models/Course.js با فیلدهای id, title, description, sessions (آرایه‌ای از session objects شامل id, title, files). 3. پیاده‌سازی دستور /courses در تابع handleCourses که لیست دروس را از دیتابیس خوانده و با InlineKeyboardButton نمایش دهد. 4. پیاده‌سازی دستور /course {id} در تابع handleCourseDetail که جزئیات یک درس خاص را نمایش دهد. 5. پیاده‌سازی دستور /course_sessions {courseId} برای نمایش لیست جلسات/فایل‌های یک درس. 6. افزودن route جدید در backend/server.js برای راه‌اندازی ربات تلگرام (مثلاً app.post('/api/telegram/webhook', ...)). 7. نصب وابستگی node-telegram-bot-api در backend/package.json. 8. افزودن متغیر محیطی TELEGRAM_BOT_TOKEN به backend/.env.example.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 5: پیاده‌سازی قابلیت آپلود فایل (محتوای دروس) از طریق تلگرام

**Scope:** این مرحله شامل ایجاد قابلیت دریافت فایل‌های ارسالی کاربر از تلگرام (مثلاً PDF، تصویر، صوت) و ذخیره‌سازی آنها در سیستم (مثلاً در پوشه media یا S3) و مرتبط‌سازی با درس مربوطه است. باید از طریق دستور /upload یا دکمه inline انجام شود. این مرحله فقط آپلود را پوشش می‌دهد و شامل پردازش محتوا (مثل OCR یا تحلیل) نیست.
**Key terms:** MessageHandler (document, photo, audio), file download, storage, CourseFile model, /upload

**بخش مربوط از متن کاربر:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن قابلیت آپلود فایل دروس از طریق تلگرام با دستور /upload

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:19-33` — `multer configuration (uploadDir, storage, upload)` — زیرساخت آپلود فایل با multer از قبل وجود دارد. برای تلگرام باید مسیر ذخیره‌سازی را به media/ تغییر دهیم یا از همین مسیر استفاده کنیم.
  ```jsx
  const uploadDir = join(os.tmpdir(), 'lebanese-uploads');
  if (!fs.existsSync(uploadDir)) {
    fs.mkdirSync(uploadDir, { recursive: true });
  }
  
  const storage = multer.diskStorage({
    destination: (req, file, cb) => cb(null, uploadDir),
    filename: (req, file, cb) => cb(null, `${Date.now()}-${file.originalname}`)
  });
  
  const upload = multer({
    storage: storage,
    limits: { fileSize: 500 * 1024 * 1024 } // 500MB max per file
  });
  ```
- `backend/server.js:698-715` — `app.post('/api/analyze-files', ...)` — این
  ```jsx
  app.post('/api/analyze-files', upload.array('files', 10), handleMulterError, async (req, res) => {
    console.log('Received file analysis request');
  
    if (!GEMINI_API_KEY) {
      console.error('API key not configured');
      return res.status(500).json({ error: 'API key not configured' });
    }
  
    try {
      const files = req.files || [];
      const textContent = req.body.textContent || '';
      const userInstructions = req.body.userInstructions || '';
  
      console.log(`Files received: ${files.length}, Text length: ${textContent.length}`);
  
      if (files.length === 0 && !textContent.trim()) {
        return res.status(400).json({ error: 'هیچ فایل یا متنی برای تحلیل ارسال نشده است' });
      }
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی قابلیت آپلود فایل (محتوای دروس) از طریق تلگرام را داده است. این قابلیت باید بتواند فایل‌های ارسالی کاربر از تلگرام (مثلاً PDF، تصویر، صوت) را دریافت کرده و در سیستم ذخیره‌سازی کند (مثلاً در پوشه media یا S3) و با درس مربوطه مرتبط سازد. فرآیند باید از طریق دستور /upload یا دکمه inline انجام شود. کاربر تأکید کرده که این مرحله فقط آپلود را پوشش می‌دهد و شامل پردازش محتوا (مثل OCR یا تحلیل) نیست. کلیدواژه‌های ذکر شده: MessageHandler (document, photo, audio), file download, storage, CourseFile model, /upload. همچنین کاربر اشاره کرده: «و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا آپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...». در کد فعلی پروژه (backend/server.js) زیرساخت آپلود فایل با multer از خط 19 تا 33 پیاده‌سازی شده است: پوشه آپلود در os.tmpdir() با نام 'lebanese-uploads'، محدودیت حجم 500MB، ذخیره با timestamp. endpoint /api/analyze-files در خط 698 از upload.array('files', 10) برای دریافت چند فایل استفاده می‌کند. اما هیچ endpoint یا handler مخصوص تلگرام وجود ندارد. فایل frontend/index.html شامل Firebase config (خطوط 11-18) و inspector bridge (خطوط 32-201) است. backend/package.json فقط dependencies اصلی (express, cors, dotenv, ws) را دارد و هیچ کتابخانه‌ای برای Telegram Bot API (مثل node-telegram-bot-api یا telegraf) نصب نیست. backend/.env.example فقط GEMINI_API_KEY و PORT و Firebase config را نشان می‌دهد. برای پیاده‌سازی این قابلیت باید یک سرویس تلگرام بوت جدید ایجاد شود که webhook یا polling را مدیریت کند، فایل‌های دریافتی را با multer ذخیره کند، و یک مدل CourseFile برای ذخیره metadata فایل‌ها در دیتابیس (فعلاً Firebase یا فایل JSON) ایجاد شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. نصب کتابخانه node-telegram-bot-api یا telegraf در backend (backend/package.json).
2. ایجاد فایل جدید backend/services/telegramBot.js شامل:
   - راه‌اندازی bot با توکن از env (TELEGRAM_BOT_TOKEN).
   - هندلر برای دستور /upload که از کاربر می‌خواهد فایل (PDF، تصویر، صوت) ارسال کند.
   - هندلر برای message با type document, photo, audio که فایل را دانلود کرده و در پوشه media/ (یا مسیر مشخص) ذخیره کند.
   - ذخیره metadata فایل (chatId, fileId, fileName, mimeType, fileSize, courseId اختیاری) در یک فایل JSON یا Firebase.
3. ایجاد endpoint جدید در backend/server.js (مثلاً POST /api/telegram/upload) برای دریافت مستقیم فایل از تلگرام و ذخیره.
4. ایجاد مدل CourseFile در فایل backend/models/CourseFile.js (فعلاً ساده با fs).
5. افزودن متغیر محیطی TELEGRAM_BOT_TOKEN به backend/.env.example.
6. راه‌اندازی bot در backend/server.js بعد از ایجاد HTTP server (حدود خط 42).
7. ایجاد دکمه inline برای /upload در تلگرام.
8. اطمینان از اینکه فایل‌ها در پوشه media/ (یا S3) ذخیره می‌شوند و مسیرشان در metadata ثبت می‌شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 6: ایجاد قابلیت مدیریت موضوعات تمرین با استاد هوش مصنوعی از طریق تلگرام (مشاهده و انتخاب موضوع)

**Scope:** این مرحله شامل اتصال به بخش 'تمرین با استاد هوش مصنوعی' و پیاده‌سازی دستورات تلگرامی برای مشاهده لیست موضوعات موجود، مشاهده جزئیات یک موضوع، و انتخاب یک موضوع برای شروع تمرین است. باید از API داخلی پروژه برای دریافت موضوعات استفاده شود. این مرحله فقط مدیریت موضوعات را پوشش می‌دهد و شامل خود چت با هوش مصنوعی نیست.
**Key terms:** /topics, /topic {id}, AI Tutor module, topic selection, InlineKeyboardButton

**بخش مربوط از متن کاربر:**
```
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن قابلیت مدیریت موضوعات تمرین با استاد هوش مصنوعی از طریق تلگرام

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:56-114` — `app.post('/api/gemini/chat')` — این endpoint الگوی مناسبی برای ایجاد endpoint‌های جدید /api/topics است. ساختار try-catch و مدیریت خطا مشابه استفاده شود.
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
  ```
- `backend/server.js:698-715` — `app.post('/api/analyze-files')` — این endpoint نشان‌دهنده الگوی middleware و مدیریت خطا در پروژه است. برای endpoint‌های جدید topic از middleware مشابه استفاده شود.
  ```jsx
  app.post('/api/analyze-files', upload.array('files', 10), handleMulterError, async (req, res) => {
    console.log('Received file analysis request');
    if (!GEMINI_API_KEY) {
      console.error('API key not configured');
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const files = req.files || [];
      const textContent = req.body.textContent || '';
      const userInstructions = req.body.userInstructions || '';
  ```
- `backend/.env.example:1-8` — `GEMINI_API_KEY` — این فایل باید با متغیر TELEGRAM_BOT_TOKEN به‌روزرسانی شود تا بات تلگرام بتواند از توکن استفاده کند.
  ```
  # Gemini API Key - Get from https://aistudio.google.com/apikey
  GEMINI_API_KEY=your_gemini_api_key_here
  
  # Port (optional, default 3001)
  PORT=3001
  
  # Firebase config (optional - for data persistence)
  # VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/package.json` (سطر 10) — برای افزودن وابستگی node-telegram-bot-api به dependencies نیاز است.
- `frontend/index.html` (سطر 11) — شامل Firebase config است که ممکن است برای ذخیره‌سازی موضوعات در Firestore استفاده شود.
- `frontend/src/App.jsx` — کامپوننت اصلی فرانت‌اند که ممکن است بخش 'تمرین با استاد هوش مصنوعی' در آن پیاده‌سازی شده باشد و نیاز به

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد قابلیت مدیریت موضوعات تمرین با استاد هوش مصنوعی از طریق تلگرام را دارد. این شامل اتصال به بخش 'تمرین با استاد هوش مصنوعی' و پیاده‌سازی دستورات تلگرامی برای مشاهده لیست موضوعات موجود، مشاهده جزئیات یک موضوع، و انتخاب یک موضوع برای شروع تمرین است. باید از API داخلی پروژه برای دریافت موضوعات استفاده شود. این مرحله فقط مدیریت موضوعات را پوشش می‌دهد و شامل خود چت با هوش مصنوعی نیست. کلیدواژه‌های ذکر شده: /topics, /topic {id}, AI Tutor module, topic selection, InlineKeyboardButton. در کد فعلی پروژه (backend/server.js) endpoint‌های مرتبط با AI شامل /api/gemini/chat (خط 56)، /api/gemini/tts (خط 117)، /api/analyze-files (خط 698) وجود دارد اما هیچ endpoint اختصاصی برای مدیریت موضوعات تمرین (topic management) پیاده‌سازی نشده است. همچنین frontend/index.html شامل Firebase config (خطوط 11-18) و اسکریپت Inspector Bridge (خطوط 32-201) است. backend/package.json وابستگی‌های express, cors, dotenv, ws, multer, fluent-ffmpeg را نشان می‌دهد. frontend/package.json شامل react, firebase, lucide-react است. برای پیاده‌سازی این قابلیت، نیاز به ایجاد یک endpoint جدید در backend (مثلاً /api/topics) و یک ماژول تلگرام بات (که در پروژه وجود ندارد) است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد endpoint GET /api/topics در backend/server.js برای بازگرداندن لیست موضوعات تمرین (مقادیر نمونه یا از دیتابیس). 2. ایجاد endpoint GET /api/topics/:id برای بازگرداندن جزئیات یک موضوع خاص. 3. ایجاد یک ماژول تلگرام بات جدید (مثلاً backend/telegram-bot.js) که از کتابخانه node-telegram-bot-api استفاده کند. 4. پیاده‌سازی دستور /topics در بات تلگرام که لیست موضوعات را با InlineKeyboardButton نمایش دهد. 5. پیاده‌سازی دستور /topic {id} که جزئیات موضوع را نمایش دهد. 6. پیاده‌سازی callback query handler برای انتخاب موضوع (topic selection) که وضعیت کاربر را ذخیره کند. 7. اتصال به endpoint‌های داخلی /api/topics برای دریافت داده‌ها. 8. افزودن وابستگی node-telegram-bot-api به backend/package.json. 9. افزودن متغیر محیطی TELEGRAM_BOT_TOKEN به backend/.env.example.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 7: پیاده‌سازی چت متنی دوطرفه با هوش مصنوعی از طریق تلگرام

**Scope:** این مرحله شامل ایجاد یک مکالمه (session) بین کاربر و هوش مصنوعی از طریق تلگرام است. پس از انتخاب موضوع، کاربر می‌تواند پیام متنی ارسال کند و پاسخ از هوش مصنوعی دریافت کند. باید state management برای هر کاربر (مثلاً با user_id) پیاده‌سازی شود تا مکالمه در جریان باشد. این مرحله فقط چت متنی را پوشش می‌دهد و شامل پیام صوتی نیست.
**Key terms:** ConversationHandler, AI model API, session state, user_id, message history

**بخش مربوط از متن کاربر:**
```
از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی چت متنی دوطرفه با AI از طریق تلگرام

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:56-114` — `POST /api/gemini/chat` — این endpoint اصلی چت با Gemini است. Telegram Bot باید از همین endpoint برای ارسال پیام‌های کاربران استفاده کند. نیاز به بازنویسی یا افزودن یک تابع کمکی (مثلاً callGeminiChat) دارد که از این منطق استفاده کند.
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
      if (payload.contents) {
        payload.contents = payload.contents.map(content => ({
          role: content.role,
          parts: content.parts.map(part => {
            if (part.inline_data && includeAudio) {
              return { inline_data: part.inline_data };
            }
            if (part.text !== undefined) {
              return { text: part.text };
            }
            return null;
          }).filter(p => p !== null)
        }));
      }
      const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${GEMINI_API_KEY}`;
      const response = await fetch(apiUrl, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(payload)
      });
      if (!response.ok) {
        const errorData = await response.text();
        console.error('Gemini API error:', errorData);
        return res.status(response.status).json({ error: errorData });
      }
      const result = await response.json();
      if (includeAudio) {
        res.json(result);
      } else {
        const text = result.candidates?.[0]?.content?.parts?.[0]?.text;
        if (!text) {
          return res.status(500).json({ error: 'No text in response' });
        }
        res.json({ text });
      }
    } catch (error) {
      console.error('Chat API error:', error);
      res.status(500).json({ error: error.message });
    }
  });
  ```
- `backend/server.js:45` — `WebSocketServer` — WebSocket موجود برای ارتباطات زنده است. Telegram Bot از polling یا webhook استفاده می‌کند، نه WebSocket. این خط نشان می‌دهد که پروژه قابلیت real-time دارد اما برای تلگرام باید مکانیزم جداگانه‌ای پیاده‌سازی شود.
  ```jsx
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `frontend/index.html:11-18` — `Firebase Config` — Firebase کانفیگ در frontend وجود دارد. می‌توان از Firebase Realtime Database یا Firestore برای ذخیره session state کاربران تلگرام استفاده کرد. این کانفیگ باید به backend نیز منتقل شود (از طریق env variables).
  ```
  var __firebase_config = JSON.stringify({
    apiKey: "AIzaSyAMFtV0zIMKKe0bIr68Z1wTORQ1jQpsv9Q",
    authDomain: "labaneseapp.firebaseapp.com",
    projectId: "labaneseapp",
    storageBucket: "labaneseapp.firebasestorage.app",
    messagingSenderId: "951874597795",
    appId: "1:951874597795:web:00745327993adad760a016"
  });
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/.env.example` — نیاز به افزودن متغیرهای محیطی جدید: TELE

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی یک مکالمه (session) بین کاربر و هوش مصنوعی از طریق تلگرام را دارد. پس از انتخاب موضوع، کاربر می‌تواند پیام متنی ارسال کند و پاسخ از هوش مصنوعی دریافت کند. باید state management برای هر کاربر (مثلاً با user_id) پیاده‌سازی شود تا مکالمه در جریان باشد. این مرحله فقط چت متنی را پوشش می‌دهد و شامل پیام صوتی نیست. کلیدواژه‌های اصلی: ConversationHandler, AI model API, session state, user_id, message history. بخش مربوط از درخواست اصلی کاربر: 'از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم'. در کد فعلی پروژه (backend/server.js) یک endpoint برای چت با Gemini وجود دارد: POST /api/gemini/chat (خط 56-114) که از مدل gemini-2.0-flash استفاده می‌کند. همچنین یک endpoint TTS در خط 117-164 وجود دارد. اما هیچ Webhook یا Bot API تلگرام در پروژه پیاده‌سازی نشده است. پروژه از WebSocket (خط 45) برای ارتباطات زنده استفاده می‌کند اما این برای تلگرام نیست. فایل frontend/index.html شامل Firebase config است (خط 11-18) که می‌تواند برای احراز هویت کاربران استفاده شود. backend/.env.example نشان می‌دهد که GEMINI_API_KEY و Firebase config قابل تنظیم هستند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک فایل جدید backend/services/telegramBot.js که شامل راه‌اندازی Telegram Bot API با کتابخانه node-telegram-bot-api باشد. 2. پیاده‌سازی ConversationHandler در این فایل که state هر کاربر را بر اساس user_id در یک Map یا Redis ذخیره کند. 3. اتصال به endpoint موجود POST /api/gemini/chat (backend/server.js خط 56-114) برای ارسال پیام کاربر به Gemini و دریافت پاسخ. 4. مدیریت session state: هر کاربر یک state object دارد شامل topic انتخاب‌شده، message history (آخرین N پیام)، و مرحله مکالمه. 5. Webhook تلگرام: تنظیم Webhook برای دریافت پیام‌های کاربران و پردازش آن‌ها. 6. استفاده از Firebase (که در frontend/index.html خط 11-18 کانفیگ شده) برای ذخیره‌سازی پایدار session state در صورت نیاز. 7. پیاده‌سازی fallback برای خطاهای API (مثلاً اگر Gemini API خطا داد، پیام خطای مناسب به کاربر نمایش داده شود). 8. افزودن لاگینگ برای دیباگ session state و خطاها.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 8: پیاده‌سازی ارسال پیام صوتی از تلگرام به هوش مصنوعی برای تحلیل و استخراج

**Scope:** این مرحله شامل دریافت فایل صوتی از کاربر تلگرام، ارسال آن به سرویس تحلیل صوت (که احتمالاً در بخش 'تمرین با استاد هوش مصنوعی' وجود دارد)، دریافت نتیجه تحلیل (متن استخراج‌شده)، و ارسال آن به هوش مصنوعی برای پردازش است. باید از میکروفون تلگرام (voice message) پشتیبانی شود. این مرحله فقط ارسال صوت و دریافت تحلیل را پوشش می‌دهد.
**Key terms:** voice message, speech-to-text, audio analysis service, file download, AI processing

**بخش مربوط از متن کاربر:**
```
از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن Webhook تلگرام برای دریافت و تحلیل پیام صوتی

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:698-800` — `app.post('/api/analyze-files')` — این endpoint اصلی تحلیل فایل است که باید توسط webhook تلگرام فراخوانی شود. بخش audio processing در خط 790 شروع می‌شود.
  ```jsx
  app.post('/api/analyze-files', upload.array('files', 10), handleMulterError, async (req, res) => {
    console.log('Received file analysis request');
    if (!GEMINI_API_KEY) {
      console.error('API key not configured');
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const files = req.files || [];
      const textContent = req.body.textContent || '';
      const userInstructions = req.body.userInstructions || '';
  ```
- `backend/server.js:790-800` — `audio processing branch` — این بخش برای فایل‌های صوتی بزرگ از uploadToGeminiFileAPI استفاده می‌کند. webhook باید این مسیر را replicate کند.
  ```jsx
  } else if (mimeType.startsWith('audio/')) {
    console.log(`Audio file: ${fileName}, size: ${fileSizeMB.toFixed(2)} MB`);
    try {
      let result;
      if (isLargeFile) {
        console.log(`Using File API for large audio: ${fileName}`);
        const uploadedFileName = await uploadToGeminiFileAPI(filePath, mimeType, fileName, fileSize);
  ```
- `backend/server.js:273-294` — `analyzeWithGemini function` — این تابع برای تحلیل مستقیم متن/صوت استفاده می‌شود. webhook می‌تواند از این تابع برای پردازش استفاده کند.
  ```jsx
  async function analyzeWithGemini(parts, systemPrompt) {
    const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${GEMINI_API_KEY}`;
    const payload = {
      contents: [{ role: 'user', parts }],
      systemInstruction: { parts: [{ text: systemPrompt }] }
    };
    const response = await fetch(apiUrl, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(payload)
    });
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/.env.example` (سطر 1) — نیاز به افزودن TELEGRAM_BOT_TOKEN برای دسترسی به Telegram Bot API
- `backend/server.js` (سطر 700) — نیاز به افزودن endpoint جدید POST /api/telegram/webhook و import کتابخانه‌های جدید (axios یا node-fetch برای دانلود فایل)
- `frontend/src/App.jsx` (سطر 1) — احتمالاً نیاز به نمایش وضعیت webhook یا پیام‌های دریافتی از تلگرام در UI دارد (فایل deep-read نشده)
- `backend/package.json` (سطر 1) — نیاز به افزودن وابستگی‌های جدید مانند axios یا node-fetch برای درخواست‌های HTTP به Telegram API

## 🌐 نقشهٔ وابستگی‌ها
این feature به backend/server.js وابسته است (توابع analyzeWithGemini در خط 273، uploadToGeminiFileAPI در خط 297، splitIntoChunks در خط 247). نیاز به ایجاد فایل جدید backend/services/telegramService.js دارد. از multer

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی ارسال پیام صوتی از تلگرام به هوش مصنوعی برای تحلیل و استخراج را دارد. این مرحله شامل دریافت فایل صوتی از کاربر تلگرام (voice message از میکروفون تلگرام)، ارسال آن به سرویس تحلیل صوت که در backend/server.js در endpoint `/api/analyze-files` (خط 698) پیاده‌سازی شده، دریافت نتیجه تحلیل (متن استخراج‌شده)، و ارسال آن به هوش مصنوعی برای پردازش است. کلیدواژه‌های کاربر: voice message, speech-to-text, audio analysis service, file download, AI processing. کاربر اشاره کرده: «از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه». در کد فعلی backend/server.js، تابع `analyzeWithGemini` (خط 273) و `uploadToGeminiFileAPI` (خط 297) برای پردازش فایل‌های صوتی (mimeType.startsWith('audio/') در خط 790) وجود دارد. همچنین WebSocket در خط 45 با path `/ws/live` راه‌اندازی شده. پروژه از Express.js و multer برای آپلود فایل استفاده می‌کند. نیاز به یک endpoint جدید تلگرام webhook (مثلاً POST /api/telegram/webhook) است که voice message را دریافت، فایل را از Telegram servers دانلود کند (با استفاده از bot token و file_id)، به فرمت مناسب تبدیل کند، به endpoint تحلیل صوت ارسال کند، و نتیجه را به کاربر تلگرام برگرداند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/services/telegramService.js برای مدیریت Webhook تلگرام. 2. افزودن endpoint POST /api/telegram/webhook در backend/server.js (حدود خط 700) که: a) پیام JSON تلگرام را دریافت کند. b) اگر message.voice وجود داشت، file_id را استخراج کند. c) با استفاده از bot token (از env) فایل را از https://api.telegram.org/bot{token}/getFile?file_id={fileId} دریافت کند. d) فایل صوتی را با URL https://api.telegram.org/file/bot{token}/{file_path} دانلود کند. e) فایل را در uploadDir (خط 20) ذخیره کند. f) فایل را به endpoint داخلی /api/analyze-files ارسال کند (با استفاده از تابع analyzeWithGemini یا uploadToGeminiFileAPI). g) نتیجه تحلیل را به کاربر تلگرام از طریق sendMessage API برگرداند. 3. افزودن متغیر محیطی TELEGRAM_BOT_TOKEN به backend/.env.example. 4. استفاده از تابع موجود splitIntoChunks (خط 247) برای فایل‌های صوتی طولانی. 5. اضافه کردن validation برای voice message در handleMulterError (خط 686).

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 9: پیاده‌سازی ارسال پاسخ صوتی از هوش مصنوعی به تلگرام (text-to-speech)

**Scope:** این مرحله شامل گرفتن پاسخ متنی از هوش مصنوعی، تبدیل آن به صوت (با استفاده از یک سرویس TTS مانند Google TTS یا API اختصاصی)، و ارسال فایل صوتی به کاربر در تلگرام است. همچنین باید متن پاسخ نیز همزمان ارسال شود. این مرحله فقط خروجی صوتی را پوشش می‌دهد.
**Key terms:** text-to-speech (TTS), voice message reply, audio file generation, Google TTS, AI response

**بخش مربوط از متن کاربر:**
```
پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی ارسال پاسخ صوتی TTS به تلگرام

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:117-164` — `app.post('/api/gemini/tts', ...)` — این endpoint TTS موجود است و خروجی base64 audioData برمی‌گرداند. باید از اینجا برای گرفتن صوت استفاده شود.
  ```jsx
  app.post('/api/gemini/tts', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const { prompt, voice = 'Kore' } = req.body;
      const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-preview-tts:generateContent?key=${GEMINI_API_KEY}`;
      const payload = {
        contents: [{ parts: [{ text: prompt }] }],
        generationConfig: {
          responseModalities: ["AUDIO"],
          speechConfig: { 
            voiceConfig: { 
              prebuiltVoiceConfig: { voiceName: voice } 
            } 
          }
        }
      };
      const response = await fetch(apiUrl, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(payload)
      });
      if (!response.ok) {
        const errorData = await response.text();
        console.error('TTS API error:', errorData);
        return res.status(response.status).json({ error: errorData });
      }
      const result = await response.json();
      const part = result?.candidates?.[0]?.content?.parts?.[0];
      const audioData = part?.inlineData?.data;
      const mimeType = part?.inlineData?.mimeType;
      if (audioData && mimeType?.startsWith("audio/")) {
        res.json({ audioData, mimeType });
      } else {
        res.status(500).json({ error: 'Invalid audio data' });
      }
    } catch (error) {
      console.error('TTS API error:', error);
      res.status(500).json({ error: error.message });
    }
  });
  ```
- `backend/server.js:56-114` — `app.post('/api/gemini/chat', ...)`
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
      if (payload.contents) {
        payload.contents = payload.contents.map(content => ({
          role: content.role,
          parts: content.parts.map(part => {
            if (part.inline_data && includeAudio) {
              return { inline_data: part.inline_data };
            }
            if (part.text !== undefined) {
              return { text: part.text };
            }
            return null;
          }).filter(p => p !== null)
        }));
      }
      const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${G
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی ارسال پاسخ صوتی از هوش مصنوعی به تلگرام (text-to-speech) را داده است. این مرحله شامل گرفتن پاسخ متنی از هوش مصنوعی، تبدیل آن به صوت (با استفاده از یک سرویس TTS مانند Google TTS یا API اختصاصی)، و ارسال فایل صوتی به کاربر در تلگرام است. همچنین باید متن پاسخ نیز همزمان ارسال شود. این مرحله فقط خروجی صوتی را پوشش می‌دهد.

بخش مربوط از درخواست اصلی کاربر: «پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....»

کلیدواژه‌ها: text-to-speech (TTS), voice message reply, audio file generation, Google TTS, AI response

شواهد در کد واقعی پروژه: در backend/server.js از خط 117 تا 164 یک endpoint TTS با مسیر /api/gemini/tts وجود دارد که از مدل gemini-2.5-flash-preview-tts استفاده می‌کند و خروجی audioData و mimeType برمی‌گرداند. همچنین در خط 56 endpoint /api/gemini/chat برای دریافت پاسخ متنی از AI وجود دارد. پروژه از WebSocket در خط 45 (wss = new WebSocketServer({ server, path: '/ws/live' })) برای ارتباط بلادرنگ استفاده می‌کند. در frontend/index.html از خط 9 تا 20 Firebase Configuration (__firebase_config) با پروژه 'labaneseapp' تعریف شده است. در frontend/src/App.jsx (فایل deep-read شده) احتمالاً کامپوننت چت وجود دارد که باید خروجی را به تلگرام نیز ارسال کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک سرویس جدید در backend با نام backend/services/telegramService.js که وظیفه ارسال پیام به تلگرام را دارد.
2. در این سرویس، از API تلگرام (https://api.telegram.org/bot<TOKEN>/sendMessage) برای ارسال متن و از (https://api.telegram.org/bot<TOKEN>/sendAudio) برای ارسال فایل صوتی استفاده شود.
3. پس از دریافت پاسخ از endpoint /api/gemini/chat (خط 56 server.js)، متن پاسخ را گرفته و همزمان:
   - به endpoint /api/gemini/tts (خط 117 server.js) درخواست بدهد تا audioData دریافت شود.
   - فایل صوتی موقتاً در مسیر uploadDir (خط 20 server.js: join(os.tmpdir(), 'lebanese-uploads')) ذخیره شود.
4. از WebSocket موجود (wss در خط 45) برای ارسال همزمان پاسخ صوتی و متنی به کلاینت‌های متصل استفاده شود.
5. یک endpoint جدید مانند POST /api/telegram/send با body شامل {chatId, text, audioData} ایجاد شود.
6. در frontend/src/App.jsx، پس از دریافت پاسخ از WebSocket، دکمه یا trigger خودکاری برای ارسال به تلگرام اضافه شود.
7. از کتابخانه node-fetch یا axios برای درخواست به API تلگرام استفاده شود.
8. توکن ربات تلگرام باید در فایل backend/.env.example (خط 1) به عنوان TELEGRAM_BOT_TOKEN اضافه شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 10: همگام‌سازی کامل چت تلگرام با چت وب (تمرین با استاد هوش مصنوعی)

**Scope:** این مرحله شامل اطمینان از همگام‌سازی دوطرفه پیام‌ها بین چت تلگرام و چت وب است. یعنی هر پیامی که در وب ارسال می‌شود در تلگرام نیز نمایش داده شود و بالعکس. باید از یک message queue یا WebSocket برای همگام‌سازی استفاده شود. این مرحله شامل تست یکپارچگی کامل است.
**Key terms:** message synchronization, WebSocket, message queue, Redis, real-time sync

**بخش مربوط از متن کاربر:**
```
و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....
```

## 🎯 هدف (خلاصه ساختاریافته)
همگام‌سازی دوطرفه چت تلگرام و وب با WebSocket

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:44-45` — `WebSocketServer initialization` — WebSocket سرور فعلی فقط تعریف شده و هیچ handler برای connection/message ندارد. باید event handlerهای 'connection' و 'message' به آن اضافه شود تا پیام‌ها را بین کلاینت‌ها (وب و تلگرام) relay کند.
  ```jsx
  // Create WebSocket server
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/server.js:56-113` — `app.post('/api/gemini/chat')` — این endpoint پس از دریافت پاسخ از Gemini API باید پیام را از طریق WebSocket به همه کلاینت‌های متصل broadcast کند تا در تلگرام نیز نمایش داده شود. همچنین باید پیام کاربر را نیز broadcast کند.
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
  ```
- `backend/server.js:166-169` — `app.get('/api/health')` — می‌توان از این endpoint برای health check سرویس تلگرام نیز استفاده کرد یا یک endpoint مجزا برای وضعیت اتصال تلگرام ایجاد کرد.
  ```jsx
  // Health check
  app.get('/api/health', (req, res) => {
    res.json({ status: 'ok', timestamp: new Date().toISOString() });
  });
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
JavaScript (Node.js) — Express.js برای HTTP server، ws کتابخانه Web

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/package.json` (سطر 1) — نیاز به اضافه کردن وابستگی‌های جدید مانند node-telegram-bot-api یا axios برای ارتباط با Telegram Bot API
- `backend/.env.example` (سطر 1) — نیاز به اضافه کردن متغیرهای محیطی TELEGRAM_BOT_TOKEN و TELEGRAM_CHAT_ID
- `frontend/src/App.jsx` (سطر 1) — کامپوننت اصلی فرانت‌اند که باید WebSocket client را برای دریافت پیام‌های real-time از تلگرام پیاده‌سازی کند
- `frontend/index.html` (سطر 32) — فایل HTML اصلی که اسکریپت Inspector Bridge در آن است (خط ۳۲) — ممکن است نیاز به اصلاح برای پشتیبانی از WebSocket باشد
- `backend/server.js` (سطر 45) — فایل اصلی بک‌اند که تمام endpointها و WebSocket سرور در آن تعریف شده — تغییرات اصلی در این فایل اعمال می‌شود

## 🌐 نقشهٔ وابستگی‌ها
این تسک به فایل backend/server.js به‌عنوان هسته اصلی وابسته است (WebSocket سرور در خط ۴۵ و endpoint چت در خط ۵۶). یک سرویس جدید backend/services/telegramService.js باید ایجاد شود که با Telegram Bot API ارتباط برقرار کند. همچنین backend/services/messageQueue.js برای مدیریت صف پیام‌ها. در فرانت‌اند، frontend/src/App.jsx باید WebSocket client را پیاده‌سازی کند. متغیرهای محیطی جدید در backend/.env.example اضافه می‌شوند. وابستگی npm جدید node-telegram-bot-api یا telegraf به backend/package.json اضافه می‌شود.

## 🔍 Context و وضعیت فعلی
کاربر درخواست همگام‌سازی کامل چت تلگرام با چت وب (تمرین با استاد هوش مصنوعی) را داده است. این مرحله شامل اطمینان از همگام‌سازی دوطرفه پیام‌ها بین چت تلگرام و چت وب است. یعنی هر پیامی که در وب ارسال می‌شود در تلگرام نیز نمایش داده شود و بالعکس. باید از یک message queue یا WebSocket برای همگام‌سازی استفاده شود. این مرحله شامل تست یکپارچگی کامل است. کاربر تأکید کرده: «و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد». کلیدواژه‌های اصلی: message synchronization, WebSocket, message queue, Redis, real-time sync.

در کد فعلی پروژه (backend/server.js خط ۴۵) یک WebSocket سرور در مسیر `/ws/live` ایجاد شده است: `const wss = new WebSocketServer({ server, path: '/ws/live' });` اما این WebSocket فقط تعریف شده و هیچ منطق همگام‌سازی پیام یا مدیریت رویدادهای چت در آن پیاده‌سازی نشده است. همچنین endpoint چت `/api/gemini/chat` (خط ۵۶) فقط درخواست را به Gemini API فوروارد می‌کند و هیچ مکانیسمی برای انتشار پیام‌ها به کلاینت‌های دیگر (تلگرام یا وب) ندارد. frontend/index.html شامل اسکریپت Inspector Bridge (خط ۳۲) است که از postMessage برای ارتباط با parent استفاده می‌کند اما این نیز برای همگام‌سازی چت طراحی نشده است. در حال حاضر هیچ فایل یا سرویس تلگرام در پروژه وجود ندارد و نیاز به ایجاد یک سرویس جدید برای اتصال به Telegram Bot API است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ۱. ایجاد یک سرویس تلگرام جدید در backend/services/telegramService.js که با Telegram Bot API از طریق polling یا webhook متصل شود.
۲. پیاده‌سازی WebSocket handler در backend/server.js (خط ۴۵) برای مدیریت رویدادهای real-time: هنگام دریافت پیام جدید از WebSocket، پیام را به سرویس تلگرام ارسال کند و بالعکس.
۳. ایجاد یک message queue ساده (یا استفاده از Redis در صورت نیاز) در backend/services/messageQueue.js برای بافر کردن پیام‌ها و اطمینان از تحویل.
۴. اصلاح endpoint /api/gemini/chat (خط ۵۶-۱۱۳) به‌گونه‌ای که پس از دریافت پاسخ از Gemini، پیام را از طریق WebSocket به همه کلاینت‌های متصل (از جمله تلگرام) broadcast کند.
۵. ایجاد یک endpoint جدید در backend/server.js برای webhook تلگرام: POST /api/telegram/webhook که پیام‌های دریافتی از تلگرام را پردازش کرده و از طریق WebSocket به کلاینت‌های وب ارسال کند.
۶. تست یکپارچگی: ارسال پیام از وب → مشاهده در تلگرام و ارسال پیام از تلگرام → مشاهده در وب.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: high
- تخمین زمان: large

---

## ✅ معیارهای پذیرش کلی (همهٔ مراحل)
- [ ] همهٔ مراحل بالا با موفقیت پیاده‌سازی شده‌اند
- [ ] تست‌های موجود pass می‌شوند
- [ ] هیچ regression رخ نداده

## Acceptance Criteria

1. فایل backend/services/telegramService.js ایجاد شده و تابع sendVoiceMessage را export کند _(verify: static)_

## Task Steps

### Step 1: ایجاد ساختار پایه برای اتصال تلگرام دوطرفه (Telegram Bot API + Webhook)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک بات تلگرام جدید (یا استفاده از بات موجود) و راه‌اندازی Webhook برای دریافت پیام‌های کاربر از تلگرام است. باید یک endpoint در بک‌اند (مثلاً FastAPI یا Django) ایجاد شود که پیام‌های دریافتی از تلگرام را پردازش کند. این مرحله فقط ارتباط پایه را فراهم می‌کند و شامل منطق پردازش پیام‌ها یا دستورات خاص نیست. نکته حیاتی: باید از کتابخانه python-telegram-bot یا aiogram استفاده شود و Webhook به صورت HTTPS (با SSL) کار کند.
**Excerpt:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

### Step 2: پیاده‌سازی ارسال نوتیفیکیشن‌های خودکار از سیستم به تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک سرویس یا تابع در بک‌اند است که بتواند پیام‌های نوتیفیکیشن (مثلاً یادآوری، هشدار، اطلاع‌رسانی) را به کاربر از طریق تلگرام ارسال کند. باید مشخص شود چه رویدادهایی باعث ارسال نوتیفیکیشن می‌شوند (مثلاً ثبت تمرین جدید، اتمام درس، خطا). این مرحله فقط ارسال یک‌طرفه از سیستم به تلگرام را پوشش می‌دهد و شامل دریافت دستورات نیست.
**Excerpt:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته
```

### Step 3: پیاده‌سازی دستورات پایه کنترل سیستم از طریق تلگرام (مثلاً /status, /help, /list_courses)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل تعریف و پیاده‌سازی چند دستور پایه تلگرامی است که به کاربر اجازه می‌دهد وضعیت سیستم را ببیند، لیست دروس را دریافت کند، و راهنما را مشاهده کند. باید یک handler برای هر دستور در Webhook ایجاد شود. این مرحله فقط دستورات ساده و فقط-متن را پوشش می‌دهد و شامل عملیات پیچیده (مثل آپلود فایل) نیست.
**Excerpt:**
```
بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

### Step 4: ایجاد قابلیت مشاهده لیست دروس و محتوای آنها از طریق تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی دستورات تلگرامی برای مشاهده لیست دروس، مشاهده جزئیات یک درس خاص (مثلاً /course <id>)، و مشاهده لیست جلسات یا فایل‌های یک درس است. باید داده‌ها از دیتابیس پروژه خوانده شده و به صورت فرمت‌شده (متن یا دکمه‌های inline) نمایش داده شوند. این مرحله فقط مشاهده را پوشش می‌دهد و شامل آپلود یا ویرایش نیست.
**Excerpt:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

### Step 5: پیاده‌سازی قابلیت آپلود فایل (محتوای دروس) از طریق تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد قابلیت دریافت فایل‌های ارسالی کاربر از تلگرام (مثلاً PDF، تصویر، صوت) و ذخیره‌سازی آنها در سیستم (مثلاً در پوشه media یا S3) و مرتبط‌سازی با درس مربوطه است. باید از طریق دستور /upload یا دکمه inline انجام شود. این مرحله فقط آپلود را پوشش می‌دهد و شامل پردازش محتوا (مثل OCR یا تحلیل) نیست.
**Excerpt:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

### Step 6: ایجاد قابلیت مدیریت موضوعات تمرین با استاد هوش مصنوعی از طریق تلگرام (مشاهده و انتخاب موضوع)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل اتصال به بخش 'تمرین با استاد هوش مصنوعی' و پیاده‌سازی دستورات تلگرامی برای مشاهده لیست موضوعات موجود، مشاهده جزئیات یک موضوع، و انتخاب یک موضوع برای شروع تمرین است. باید از API داخلی پروژه برای دریافت موضوعات استفاده شود. این مرحله فقط مدیریت موضوعات را پوشش می‌دهد و شامل خود چت با هوش مصنوعی نیست.
**Excerpt:**
```
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم
```

### Step 7: پیاده‌سازی چت متنی دوطرفه با هوش مصنوعی از طریق تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک مکالمه (session) بین کاربر و هوش مصنوعی از طریق تلگرام است. پس از انتخاب موضوع، کاربر می‌تواند پیام متنی ارسال کند و پاسخ از هوش مصنوعی دریافت کند. باید state management برای هر کاربر (مثلاً با user_id) پیاده‌سازی شود تا مکالمه در جریان باشد. این مرحله فقط چت متنی را پوشش می‌دهد و شامل پیام صوتی نیست.
**Excerpt:**
```
از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم
```

### Step 8: پیاده‌سازی ارسال پیام صوتی از تلگرام به هوش مصنوعی برای تحلیل و استخراج
**Status:** `pending` (0%)
**Scope:** این مرحله شامل دریافت فایل صوتی از کاربر تلگرام، ارسال آن به سرویس تحلیل صوت (که احتمالاً در بخش 'تمرین با استاد هوش مصنوعی' وجود دارد)، دریافت نتیجه تحلیل (متن استخراج‌شده)، و ارسال آن به هوش مصنوعی برای پردازش است. باید از میکروفون تلگرام (voice message) پشتیبانی شود. این مرحله فقط ارسال صوت و دریافت تحلیل را پوشش می‌دهد.
**Excerpt:**
```
از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه
```

### Step 9: پیاده‌سازی ارسال پاسخ صوتی از هوش مصنوعی به تلگرام (text-to-speech)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل گرفتن پاسخ متنی از هوش مصنوعی، تبدیل آن به صوت (با استفاده از یک سرویس TTS مانند Google TTS یا API اختصاصی)، و ارسال فایل صوتی به کاربر در تلگرام است. همچنین باید متن پاسخ نیز همزمان ارسال شود. این مرحله فقط خروجی صوتی را پوشش می‌دهد.
**Excerpt:**
```
پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....
```

### Step 10: همگام‌سازی کامل چت تلگرام با چت وب (تمرین با استاد هوش مصنوعی)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل اطمینان از همگام‌سازی دوطرفه پیام‌ها بین چت تلگرام و چت وب است. یعنی هر پیامی که در وب ارسال می‌شود در تلگرام نیز نمایش داده شود و بالعکس. باید از یک message queue یا WebSocket برای همگام‌سازی استفاده شود. این مرحله شامل تست یکپارچگی کامل است.
**Excerpt:**
```
و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....
```
