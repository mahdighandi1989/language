---
task_id: dc721af2-f84f-424d-a491-91e8ab3624c0
title: میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار (6 مرحله)
type: other
priority: medium
execution_priority: 3000
status: pending
external_status: pending
verification_status: pending
watched_id: 6b04f8bd-b562-4fb9-9cd9-0c27458ced59
project: mahdighandi1989/language
created_at: '2026-05-12T11:13:22.960230+00:00'
updated_at: '2026-06-01T20:18:10.718845+00:00'
target_files:
- backend/server.js
- backend/.env.example
- backend/package.json
- frontend/index.html
---

# میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار (6 مرحله)

## Raw Idea

میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم و از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

📖 **خواندن کامل + اجرای مو-به-مو (بسیار مهم):**

این پرامپت — از این یادداشت تا انتها — یک سند واحد است که هر بخشش
حاوی الزام یا context منحصربه‌فرد است. خواندن سطحی یا skim کردن **ممنوع**
است.

- پرامپت را **سطر به سطر** بخوان، نه head/tail/فقط-بخش-اصلی.
- اگر بخشی به‌نظر طولانی یا تکراری آمد، **حتماً** بخوان — تفاوت‌های
  ریز ممکن است در آن جا اساسی باشند.
- هر جمله، URL، نام فایل، نام تابع، یا مقدار عددی که در پرامپت آمده،
  دقیقاً همان است که کاربر می‌خواهد — تغییرش نده، رندش نکن، خلاصه‌اش
  نکن.
- اگر پرامپت چندین درخواست/مرحله/زیرتسک دارد، **همه** را پیاده کن. حتی
  یکی را نه به‌عنوان "خارج از scope" حذف کن.

❌ ممنوعات صریح:
- خلاصه‌سازی متن کاربر در commit message یا response
- "این بخش اصلی نیست، رد می‌کنم"
- "کاربر احتمالاً منظورش این بود..." — منظورش همان است که نوشته
- "این URL/نام به نظر قدیمی است، آپدیتش کردم" — تغییر بدون درخواست ممنوع
- پیاده‌سازی فقط بخشی از پرامپت و تظاهر به کامل بودن
- "همه آیتم‌های لیست A را بررسی کردم، B و C مشابه بودند" — نه؛
  هرکدام را جداگانه

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

🔗 **وابستگی‌ها و همگام‌سازی (بسیار حیاتی — هرگز skip نکن):**

این بخش از همهٔ بخش‌های دیگرِ این یادداشت **مهم‌تر** است. اگر نقض شود،
نتیجهٔ کار ممکن است مشروع به‌نظر برسد ولی در عمل بخش‌های دیگر سیستم را عقب
بیندازد، broken reference تولید کند، یا منجر به data corruption شود.

پیش از و حین تغییر، تمام وابستگی‌ها را در **چهار جهت** به‌طور **کامل و
بدون هیچ خلاصه‌سازی** شناسایی و همگام کن:

**۱. وابستگی‌های upstream (این تسک به چه چیزهایی متکی است):**
- چه فایل‌ها، توابع، کلاس‌ها، API endpoint ها، schema های دیتابیس،
  env vars، یا config هایی که این تسک نیاز دارد؟
- آیا قرار است چیزی را ویرایش/حذف کنی که جای دیگر (signature، رفتار،
  return type، side effect) از آن انتظار خاصی می‌رود؟
- اگر dependency جدیدی اضافه می‌کنی، آیا با dependencyهای موجود تداخل
  دارد (نسخه، compat، lock file)؟

**۲. وابستگی‌های downstream (چه چیزهایی به این تسک متکی‌اند):**
- چه فایل‌ها، توابع، تست‌ها، migrations، docs، یا UI component هایی از
  کدی که داری ویرایش/اضافه/حذف می‌کنی **استفاده می‌کنند**؟
- با grep و reference search **همه‌ی** call sites، importها، subclassها،
  reference های مستقیم و غیرمستقیم را پیدا کن — نه فقط چند مورد اصلی.
- خصوصاً برای حذف یا rename: هیچ broken reference نباید باقی بماند.

**۳. وابستگی‌های cross-tier (بسیار مهم — هرگز فقط یک لایه را نبین):**

تسک شما ممکن است از backend، frontend، database، worker، یا هر tier
دیگری شروع شده باشد. ولی تغییرات تقریباً همیشه روی tier های دیگر هم
اثر می‌گذارند. **مستقل از اینکه تسک از کدام tier است**، این چک‌های دو
طرفه را همیشه انجام بده:

🔁 **اگر backend را تغییر دادی** (API، service، model، route):
  → frontend: کدام component/page/hook این endpoint یا data shape را
    مصرف می‌کند؟ type definition، state shape، error handling، loading
    state، form validation، URL routing همگی باید همگام شوند.
  → mobile/SDK/client library (اگر پروژه دارد): همان داستان frontend.
  → database: آیا migration لازم است؟ آیا rollback امن است؟
  → background workers: آیا event producer/consumer ها تحت تأثیرند؟
  → rate limit، auth، CORS، CSP: آیا رفتار جدید پشتیبانی می‌شود؟

🔁 **اگر frontend را تغییر دادی** (component، form، state، route):
  → backend: آیا endpoint جدید/تغییریافته لازم است؟ آیا data shape ای
    که ارسال می‌شود با schema سرور سازگار است؟
  → backend validation: آیا برای ورودی‌های جدید UI کافی است؟
  → permissions/RBAC: آیا feature جدید نیاز به role check جدید دارد؟
  → analytics/tracking: آیا event های جدید باید در backend log شوند؟
  → SEO/SSR: آیا تغییر route نیاز به sitemap/meta tags جدید دارد؟

🔁 **اگر database/migration را تغییر دادی**:
  → backend models (ORM، Pydantic، dataclasses) همگی به‌روزند؟
  → query های raw SQL یا ORM queries با schema جدید سازگارند؟
  → seed data، fixtures، factory functions تست‌ها به‌روزند؟
  → frontend: آیا data shape جدید در UI به‌درستی render می‌شود؟
  → rollback migration نوشته شده و امن است؟

🔁 **اگر API contract یا event schema را تغییر دادی** (REST، GraphQL،
   WebSocket، gRPC، Kafka، …):
  → OpenAPI/GraphQL schema/proto file آپدیت شد؟
  → همه‌ی consumer ها (client، subscriber، webhook، external API
    user) با version جدید سازگارند؟
  → backward compatibility حفظ شده یا migration path روشن است؟
  → versioning header/path اگر breaking change است؟

🔁 **اگر infrastructure یا config را تغییر دادی** (Dockerfile، CI، Render
   config، env، secrets):
  → README setup/installation section به‌روزه؟
  → `.env.example` با env vars جدید آپدیت شد؟
  → deploy script یا CI workflow هم تغییر کرد؟
  → docs/architecture یا diagram های infrastructure به‌روزند؟

⚠️ **هرگز فقط یک tier را تغییر نده و فرض کنی بقیه خودکار همگام می‌شوند.**
   حتی برای تغییرات به‌ظاهر «کوچک»، چک کن.

**۴. وابستگی‌های جانبی (artifacts که همیشه چک شوند):**

تغییرات کد همیشه روی این artifact ها اثر دارند. **همه را** بررسی و
به‌روز کن — مستندات اولویت **بالا** دارد چون فراموش‌شدنی‌ترین است.

  📝 **مستندات** (همیشه چک کن — حتی برای تغییر کوچک کد):
    - README.md (شرح، setup، نمونه‌های استفاده، badge ها)
    - CHANGELOG.md / RELEASE_NOTES.md
    - docs/ folder (architecture، API reference، user guides، runbooks)
    - inline docstrings/کامنت‌های توابع و کلاس‌های تغییریافته
    - OpenAPI/Swagger annotations، JSDoc/TSDoc
    - architecture diagrams (اگر component اضافه/حذف شد)
    - migration guides (اگر breaking change است)

  🌍 **مستندات کاربر**:
    - i18n files و translation keys
    - UI labels، tooltip ها، help text، error messages
    - in-app onboarding (اگر flow جدید است)

  🧪 **تست‌ها**:
    - unit tests (همه‌ی فایل‌های مرتبط — حتی اگر «بی‌ربط» به‌نظر می‌رسد)
    - integration tests
    - e2e tests (Playwright/Cypress/Selenium)
    - snapshot tests (اگر UI تغییر کرد)
    - contract tests (Pact یا مشابه)
    - performance benchmarks (اگر behavior performance-sensitive تغییر کرد)

  🧬 **type definitions و contracts**:
    - .d.ts files
    - Pydantic models، dataclasses
    - Protobuf/Avro/Thrift schemas
    - GraphQL schema definitions
    - JSON Schemas

  🏗 **infrastructure و config**:
    - Dockerfile، docker-compose.yml
    - Kubernetes manifests
    - Render/Vercel/Netlify config
    - GitHub Actions / GitLab CI workflows
    - environment templates (.env.example، .env.sample)
    - feature flags (LaunchDarkly، GrowthBook، config)

  📊 **monitoring و observability**:
    - logging keys (اگر اضافه/حذف شد، log parser ها هم به‌روز شوند)
    - metric names (Prometheus، Datadog)
    - tracing spans
    - alert rules و dashboards
    - error tracking (Sentry rules، groupings)

  🔐 **security**:
    - auth rules (rate limit، CORS، CSP، HSTS)
    - permissions/RBAC config
    - secrets rotation policies
    - audit log events (اگر action جدید اضافه شد)

  💾 **caches و serialization**:
    - cache keys و TTL (اگر data shape یا lifecycle تغییر کرد)
    - serializer formats (Redis، session storage)
    - browser storage (localStorage، IndexedDB schemas)

**قانون مطلق همگام‌سازی:**
- هر چیزی که در (۱)، (۲)، (۳)، یا (۴) شناسایی شد، در **همان workflow
  این تسک** همگام و به‌روز شود. هرگز برای بعد رها نکن.
- اگر یک فایل/تست/docs نسبت به تغییر شما عقب بماند، در بهترین حالت bug،
  در بدترین حالت مشکل امنیتی یا data corruption تولید می‌کند.
- تغییرات همگام‌سازی می‌توانند در commit جداگانه باشند (در همان task)،
  ولی نباید skip شوند یا به «refactor آینده» سپرده شوند.

**هرگز این جمله‌ها قابل قبول نیست:**
- ❌ «بعداً پیداش می‌کنم»
- ❌ «احتمالاً جای دیگه‌ای استفاده نمی‌شه»
- ❌ «این یه refactor جداگانه‌ست — out of scope»
- ❌ «فقط فایل‌های اصلی رو بررسی کردم»
- ❌ «حدس می‌زنم چیزی بهش وابسته نیست»
- ❌ «دامنه‌ی وابستگی‌ها رو خلاصه کردم» — هرگز خلاصه نکن
- ❌ «این task فقط backend است؛ frontend مشکل خودش» — هرگز
- ❌ «این task فقط frontend است؛ backend از قبل کار می‌کند» — هرگز ثابت نکرده
- ❌ «مستندات بعداً به‌روز می‌شن» — همیشه same-task همگام شوند
- ❌ «testها رو نگاه نکردم چون فقط یه تغییر کوچیک بود»

**در commit message یا PR description**، دامنهٔ وابستگی‌های شناسایی‌شده و
همگام‌شده را به‌طور explicit و **per-tier** بنویس. مثال:
```
Dependencies synced:
- upstream: User model schema, auth middleware
- downstream: 3 API endpoints, 5 frontend components, 12 tests
- cross-tier (backend → frontend): UserProfile.tsx, useUser.ts hook,
  api-types.ts (TS definitions)
- cross-tier (backend → infra): .env.example added NEW_AUTH_SCOPES
- side artifacts: OpenAPI spec, README API section, i18n keys for
  new errors, Sentry alert rule for new error code
```
اگر هیچ وابستگی پیدا نکردی در هر کدام از چهار جهت، صریحاً بنویس:
«بررسی شد — هیچ وابستگی upstream / downstream / cross-tier (backend↔
frontend↔db↔infra) / side شناسایی نشد» تا مشخص باشد بررسی **انجام شده**
نه اینکه فراموش شده.

📋 **مدیریت TO-DO برای اقدامات دستی کاربر (همیشه چک کن):**

⚠️ **هشدار بحرانی — قاعدهٔ ضد-فرار:** TO-DO فقط برای کارهایی است که
**واقعاً غیرممکن** برای agent است (نیاز به انسان مطلق)، نه برای کارهایی
که «بزرگ‌اند»، «وقت می‌برند»، یا «نیازمند fixture/setup» هستند. اگر یک
agent در یک سشن بیش از **۲۰٪ از تسک‌ها** را با TO-DO ببندد، یعنی از کار
فرار می‌کند — این الگو در سشن‌های قبلی **مشاهده** شده و الان ممنوع است.

✅ **فقط برای این موارد TO-DO بساز** (لیست بسته — هرچه خارج این لیست
ممنوع است):

  ۱. **Credential/secret که فقط کاربر دارد**:
     - تنظیم API key واقعی در پنل ادمین خارجی (Render، AWS، Stripe، …)
     - تأیید OAuth client روی console آن سرویس
     - paste کردن webhook secret که فقط بعد از ساخت در dashboard ظاهر می‌شود

  ۲. **Account/billing روی سرویس خارجی که کاربر باید عضو شود**:
     - ساخت account جدید روی Stripe/SendGrid/Twilio/Google Cloud
     - تأیید verification شماره یا ID
     - فعال‌سازی subscription پولی

  ۳. **داده/asset خصوصی که فقط کاربر دارد**:
     - آپلود لوگو/تصویر/فونت برند
     - paste کردن داده‌ای که در محل کار کاربر است
     - import داده‌ای که فقط روی device کاربر است

  ۴. **تصمیم سلیقه‌ای/حقوقی/کسب‌وکار**:
     - انتخاب رنگ‌بندی نهایی یا تم
     - متن دقیق Terms of Service / Privacy Policy
     - تعرفهٔ قیمت‌گذاری
     - نام نهایی برند یا دامنه

⛔ **هرگز TO-DO نکن برای** (لیست سیاه — هر چیزی که در این لیست است
**قابل اجرا** توسط agent است، حتی اگر بزرگ یا چندبخشی باشد):

  ❌ UI component / page / dashboard (هر فریم‌ورک: React, Vue, Angular,
     Svelte، حتی اگر معماری بزرگ دارد) — می‌توانی stub اولیه + state
     management + layout + استایل بسازی
  ❌ "نیازمند Google Drive / Stripe / Twilio API" — می‌توانی **client
     stub** با abstraction layer بسازی که با env var واقعی plug-in شود؛
     کد integration یعنی پیاده‌سازی، نه TO-DO
  ❌ "feature بزرگ، چند روز کار می‌برد" — اندازه دلیل defer نیست؛ کوچک
     شروع کن، iterate کن، در همین سشن کامل کن
  ❌ Celery / background worker / scheduler — یک task ساده + register
     می‌توانی بسازی
  ❌ Migration / model / schema — حتی اگر فیلد جدید نیاز دارد، اضافه کن
  ❌ REST endpoint / GraphQL resolver / WebSocket route — هرگز TO-DO
  ❌ test (unit/integration/e2e) — همیشه قابل نوشتن
  ❌ Documentation / README / API docs — همیشه قابل نوشتن
  ❌ Config file / .env.example / Dockerfile / CI workflow — همیشه قابل
     نوشتن
  ❌ "می‌توانستی .tsx ولی repo .jsx است" — از .jsx استفاده کن، TO-DO نکن
  ❌ "نیازمند فیلد X در مدل دیگر" — اضافه کن فیلد را، TO-DO نکن
  ❌ "تصمیم admin-vs-user-scoped" — پرامپت اولیه scope را معلوم کرده،
     یا با محتاطانه‌ترین تفسیر پیش برو
  ❌ "credential در production هنوز ست نیست" — این TO-DO ساده برای
     تنظیم env var است (مورد ۱ بالا)، نه دلیل برای defer کردن کد
  ❌ "نیازمند verification از کاربر" — اگر اقدام واقعی غیرممکن نیست،
     پیش برو
  ❌ هر چیزی که در یک کامنت `# TODO` معمولی نوشته می‌شد — این فایل
     TO-DO نیست، کامنت inline است

🔬 **قاعدهٔ «حداقل تلاش» قبل از TO-DO**: قبل از TO-DO کردن یک AC، **اثبات
کن** که قابل انجام نیست:

  ۱. آیا می‌توانم یک stub/placeholder بسازم که با env واقعی plug-in شود؟
     → اگر بله، بساز و TO-DO نکن
  ۲. آیا می‌توانم برای این بخش یک test (حتی mock-based) بنویسم؟
     → اگر بله، بنویس و TO-DO نکن
  ۳. آیا می‌توانم abstraction/interface را تعریف کنم، حتی اگر backend
     واقعی نیست؟ → اگر بله، تعریف کن و TO-DO نکن
  ۴. آیا فقط یک حالت سلیقه‌ای/decision کاربر در میان است؟
     → فقط آن یک decision را TO-DO کن، نه کل feature را

اگر یکی از این چهار راه‌حل ممکن بود ولی به TO-DO رفتی، **اعتبار شما از
بین می‌رود**.

📊 **آستانهٔ TO-DO per session**: در یک حلقهٔ اجرای N تسک، اگر بیشتر از
**۲۰٪** تسک‌ها فایل TO-DO ساختی، خودت در گزارش پایانی صریحاً اعلام کن:

  "⚠️ نسبت TO-DO من {K}/{N} = {%} است که از آستانهٔ ۲۰٪ بالاتر است.
   احتمالاً برخی از این TO-DO ها قابل اجرا بودند ولی من فرار کردم.
   لیست TO-DO ها را کاربر باید بازبینی کند که آیا واقعاً Manual-required
   بودند یا agent ضعیف کار کرده."

**یادآوری همیشگی:** اگر در آینده قابلیت‌های شما گسترش پیدا کرد و توانستید
یکی از موارد لیست سفید را خودکار انجام دهید (مثلاً managed credential
injection، یا integration پولی automate شود)، انجام دهید و TO-DO نسازید.
لیست سفید بسته است ولی **بسته از پایین** (می‌تواند کوچک‌تر شود اگر
قابلیت‌ها رشد کنند، ولی هرگز بزرگ‌تر نشود برای فرار).

**اگر هیچ بخش Manual-required نبود (تمام تسک Auto-capable است)**:
  → فایل TO-DO **نساز**. فولدر TO-DO/ باید پاک و معنادار بماند.
  → اگر برای این task از قبل `TO-DO/todo-task-{task_id_first_8}.md` بود
     (یعنی در run قبلی نیاز به دخالت کاربر بود ولی الان نه): فایل قدیمی
     را پاک کن و entry را از `TO-DO/_index.json` حذف کن.

**اگر بخش Manual-required دارد** (همه‌جانبه یا hybrid):
  1. فولدر TO-DO/ را در ریشه ریپو ایجاد کن اگر نیست
  2. فایل `TO-DO/todo-task-{task_id_first_8}.md` بساز با front-matter
     شامل: task_id, task_title, execution_priority, created_at,
     updated_at, status: "pending"
     و در بدنه: «چرا این فایل ساخته شد»، «وضعیت بخش‌های خودکار»
     (commit ها reference)، «کارهایی که باید انجام دهی» با اولویت
     بالا/متوسط/پایین به ترتیب، «وقتی این کارها را تمام کردی»
  3. `TO-DO/_index.json` را با **merge** آپدیت کن (نه overwrite):
     - فایل موجود را بخوان
     - entry های orphan (فایلشان پاک شده) را حذف کن
     - entry این task را اضافه/replace کن
     - بر اساس execution_priority صعودی مرتب کن
     - ساختار: `{"version":1, "generated_at": ISO, "total": N, "items": [...]}`
  4. این تغییرات TO-DO را در **همان commit کد** شامل کن (نه commit جداگانه)

⛔ **ممنوعات مطلق TO-DO**:
  ❌ ساختن TO-DO برای کاری که می‌توانستی خودت انجام دهی (شلوغی فولدر)
  ❌ overwrite کردن `TO-DO/_index.json` بدون merge (data loss)
  ❌ نگه‌داشتن entry هایی که فایل‌شان پاک شده (broken reference)
  ❌ فراموش کردن نوشتن «خروجی مورد انتظار» در هر آیتم TO-DO

این بخش الزامی است. حتی اگر فکر می‌کنی "این تسک کاملاً auto است و نیازی
به TO-DO نیست"، صریحاً در commit message یا report بنویس:
"بررسی شد — این تسک هیچ بخش Manual-required ندارد، TO-DO ساخته نشد."

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

🔁 **Commit + Push فوری per-task (بسیار مهم برای جریان کار صحیح):**

پس از اتمام پیاده‌سازی این تسک، **بلافاصله** commit کن و **همان موقع**
به default branch (main/master) push کن. سپس به تسک بعدی برو.

✓ چرا این قانون حیاتی است:
  - تسک‌های بعدی ممکن است به فایل‌ها/تغییراتی که این تسک ایجاد کرده
    نیاز داشته باشند. اگر push نکنی، `git pull` بعدی آن‌ها را نمی‌بیند.
  - جمع‌کردن تغییرات چند تسک منجر به conflict های بزرگ می‌شود.
  - اگر در میانه fail کنی، task های push شده ضایع نمی‌شوند.

⛔ ممنوع: "همه task ها را تمام می‌کنم بعد یک‌جا push می‌زنم"
⛔ ممنوع: branch جدا برای task — مستقیم به default branch
⛔ ممنوع: task بعدی بدون push کامل task قبلی

---


---

## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند.)_

```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم و از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....
```

## 📋 چک‌لیست مراحل (20 مرحله)

این تسک به مراحل کوچک‌تر تقسیم شده. **در هر verify خودکار، وضعیت هر مرحله به‌صورت `[ ]` (انجام نشده)، `[~]` (ناقص)، یا `[x]` (انجام شده) به‌روز می‌شود.**
وقتی تمام مراحل `[x]` شدند، تسک به‌طور خودکار به «انجام شده» منتقل می‌شود.

- [ ] **مرحله 1: ایجاد ساختار پایه برای اتصال تلگرام دوطرفه (Bot API + Webhook)** — این مرحله شامل راه‌اندازی یک ربات تلگرام با استفاده از python-telegram-bot یا aiogram است. باید webhook یا polling فعال شود تا ربات بتواند پیام‌های کاربر را دریافت کند و پاسخ دهد. در این مرحله فقط اتصال اولیه برقرار می‌شود و هیچ قابلیت خاصی اضافه نمی‌شود. خارج از این مرحله: ارسال نوتیفیکیشن، کنترل س
- [ ] **مرحله 2: اضافه کردن قابلیت ارسال نوتیفیکیشن از سیستم به تلگرام** — این مرحله شامل ایجاد یک سرویس یا تابع در بک‌اند است که بتواند پیام‌های نوتیفیکیشن را به کاربر از طریق ربات تلگرام ارسال کند. باید مشخص شود چه رویدادهایی باعث ارسال نوتیفیکیشن می‌شوند (مثلاً ثبت‌نام، تغییر وضعیت، اتمام فرآیند). خارج از این مرحله: دریافت پیام از کاربر و کنترل سیستم. نکته حیاتی: باید ا
- [ ] **مرحله 3: پیاده‌سازی دستورات کنترلی پایه از طریق تلگرام (مثلاً /status, /help)** — این مرحله شامل تعریف و پیاده‌سازی چند دستور پایه در ربات تلگرام است که کاربر بتواند از طریق آنها اطلاعاتی از سیستم دریافت کند. مثلاً دستور /status برای نمایش وضعیت کلی سیستم، /help برای راهنما. خارج از این مرحله: دستورات پیچیده مانند مدیریت دروس یا اتصال به AI. نکته حیاتی: دستورات باید با استفاده از
- [ ] **مرحله 4: اضافه کردن قابلیت مشاهده لیست دروس از طریق تلگرام** — این مرحله شامل ایجاد یک دستور تلگرامی (مثلاً /courses) است که لیست دروس موجود در سیستم را به صورت خلاصه نمایش دهد. اطلاعات باید از دیتابیس یا API مربوط به دروس خوانده شود. خارج از این مرحله: آپلود محتوا، مشاهده جزئیات درس، مدیریت تمرین با AI. نکته حیاتی: پاسخ باید به صورت فرمت‌شده (مثلاً با Markdown
- [ ] **مرحله 5: اضافه کردن قابلیت مشاهده جزئیات یک درس خاص از طریق تلگرام** — این مرحله شامل ایجاد دستوری مانند /course <id> است که جزئیات کامل یک درس (شامل توضیحات، لیست جلسات، فایل‌های مرتبط) را نمایش دهد. اطلاعات باید از API مربوطه گرفته شود. خارج از این مرحله: آپلود فایل، دانلود فایل، مدیریت تمرین با AI. نکته حیاتی: اگر درس فایل دارد، باید لینک دانلود یا پیش‌نمایش ارائه ش
- [ ] **مرحله 6: اضافه کردن قابلیت آپلود فایل (محتوای دروس) از طریق تلگرام** — این مرحله شامل پیاده‌سازی قابلیت دریافت فایل (PDF، تصویر، ویدئو) از طریق تلگرام و ذخیره آن در سیستم (مثلاً در پوشه مخصوص درس) است. کاربر باید بتواند فایل را برای ربات بفرستد و ربات آن را در درس مشخص‌شده ذخیره کند. خارج از این مرحله: مدیریت تمرین با AI، ویرایش فایل. نکته حیاتی: باید از chat_id و stat
- [ ] **مرحله 7: ایجاد اتصال اولیه به بخش 'تمرین با استاد هوش مصنوعی' از طریق تلگرام** — این مرحله شامل برقراری ارتباط بین ربات تلگرام و API یا سرویس داخلی 'تمرین با استاد هوش مصنوعی' است. باید بتوان یک session جدید شروع کرد و موضوعات موجود را دریافت کرد. خارج از این مرحله: ارسال پیام صوتی، دریافت پاسخ صوتی، مدیریت کامل. نکته حیاتی: باید از API داخلی پروژه استفاده شود و احراز هویت کاربر
- [ ] **مرحله 8: پیاده‌سازی تبادل پیام متنی با استاد هوش مصنوعی از طریق تلگرام** — این مرحله شامل ارسال پیام متنی کاربر از تلگرام به AI و دریافت پاسخ متنی از AI و نمایش آن در تلگرام است. باید از API داخلی AI برای ارسال و دریافت استفاده شود. خارج از این مرحله: پیام صوتی، مدیریت session. نکته حیاتی: باید history مکالمه حفظ شود تا AI بتواند context را بفهمد.
- [ ] **مرحله 9: اضافه کردن قابلیت ارسال پیام صوتی به استاد هوش مصنوعی از طریق تلگرام** — این مرحله شامل دریافت فایل صوتی از کاربر در تلگرام، ارسال آن به سرویس تشخیص گفتار (ASR) داخلی پروژه، ارسال متن استخراج‌شده به AI، و برگرداندن پاسخ به کاربر است. خارج از این مرحله: دریافت پاسخ صوتی از AI. نکته حیاتی: باید از API تشخیص گفتار موجود در پروژه استفاده شود و فایل صوتی قبل از ارسال به API ذ
- [ ] **مرحله 10: اضافه کردن قابلیت دریافت پاسخ صوتی از استاد هوش مصنوعی در تلگرام** — این مرحله شامل تبدیل پاسخ متنی AI به گفتار (TTS) با استفاده از سرویس موجود در پروژه و ارسال فایل صوتی حاصل به کاربر در تلگرام است. خارج از این مرحله: مدیریت session، ارسال همزمان متن و صوت. نکته حیاتی: باید از همان سرویس TTS که در وب استفاده می‌شود استفاده شود تا کیفیت یکسان باشد.
- [ ] **مرحله 11: همگام‌سازی کامل چت تلگرام با چت وب (تاریخچه و وضعیت)** — این مرحله شامل اطمینان از این است که پیام‌های ردوبدل‌شده در تلگرام در تاریخچه چت وب نیز ذخیره شوند و بالعکس. همچنین وضعیت session (فعال، بسته) در هر دو پلتفرم یکسان باشد. خارج از این مرحله: قابلیت‌های جدید. نکته حیاتی: باید از یک دیتابیس مشترک برای ذخیره پیام‌ها استفاده شود و timestampها هماهنگ باشن
- [ ] **مرحله 12: اضافه کردن قابلیت مدیریت موضوعات تمرین AI از طریق تلگرام (ایجاد، ویرایش، حذف)** — این مرحله شامل ایجاد دستورات تلگرامی برای مدیریت موضوعات تمرین AI است. کاربر بتواند موضوع جدید ایجاد کند (/topic_new)، موضوع موجود را ویرایش کند (/topic_edit <id> <text>)، و موضوع را حذف کند (/topic_delete <id>). خارج از این مرحله: خود تمرین با AI. نکته حیاتی: باید احراز هویت و سطح دسترسی کاربر بررس
- [ ] **مرحله 13: اضافه کردن قابلیت انتخاب موضوع تمرین AI از طریق تلگرام** — این مرحله شامل ایجاد یک دستور یا منوی تعاملی (Inline Keyboard) در تلگرام است که کاربر بتواند از بین موضوعات موجود یکی را برای تمرین انتخاب کند. پس از انتخاب، session AI با آن موضوع شروع شود. خارج از این مرحله: مدیریت موضوعات. نکته حیاتی: باید از CallbackQueryHandler برای مدیریت انتخاب‌ها استفاده شود
- [ ] **مرحله 14: نوشتن تست‌های واحد برای سرویس نوتیفیکیشن تلگرام** — این مرحله شامل نوشتن تست‌های واحد برای توابع ارسال پیام به تلگرام است. باید تست شود که پیام به درستی ساخته می‌شود، خطاهای API مدیریت می‌شوند، و chat_id معتبر بررسی می‌شود. خارج از این مرحله: تست‌های integration با خود تلگرام. نکته حیاتی: باید از mock برای API تلگرام استفاده شود.
- [ ] **مرحله 15: نوشتن تست‌های واحد برای دستورات کنترلی تلگرام** — این مرحله شامل نوشتن تست‌های واحد برای هندلرهای دستورات تلگرام (مانند /status, /help, /courses) است. باید تست شود که دستورات به درستی پردازش می‌شوند و پاسخ مناسب تولید می‌شود. خارج از این مرحله: تست‌های مربوط به AI. نکته حیاتی: باید از Mock برای دیتابیس و API استفاده شود.
- [ ] **مرحله 16: نوشتن تست‌های واحد برای قابلیت آپلود فایل از طریق تلگرام** — این مرحله شامل نوشتن تست‌های واحد برای فرآیند دریافت فایل از تلگرام، ذخیره‌سازی و ارتباط با درس مربوطه است. باید سناریوهای مختلف مانند فایل نامعتبر، درس ناموجود، و آپلود موفق تست شود. خارج از این مرحله: تست‌های مربوط به AI. نکته حیاتی: باید از mock برای file download و دیتابیس استفاده شود.
- [ ] **مرحله 17: نوشتن تست‌های واحد برای اتصال و تبادل پیام با استاد هوش مصنوعی** — این مرحله شامل نوشتن تست‌های واحد برای توابعی است که با API استاد هوش مصنوعی ارتباط برقرار می‌کنند. باید تست شود که پیام به درستی ارسال می‌شود، پاسخ دریافت می‌شود، و خطاهای API مدیریت می‌شوند. خارج از این مرحله: تست‌های مربوط به صوت. نکته حیاتی: باید از mock برای API خارجی استفاده شود.
- [ ] **مرحله 18: نوشتن تست‌های واحد برای قابلیت‌های صوتی (ASR و TTS)** — این مرحله شامل نوشتن تست‌های واحد برای توابع تبدیل گفتار به متن (ASR) و متن به گفتار (TTS) است. باید تست شود که فایل صوتی به درستی پردازش می‌شود و خروجی مناسب تولید می‌شود. خارج از این مرحله: تست‌های integration با سرویس‌های خارجی. نکته حیاتی: باید از mock برای سرویس‌های ASR و TTS استفاده شود.
- [ ] **مرحله 19: انجام تست‌های یکپارچه‌سازی (Integration) برای سناریوی کامل تلگرام** — این مرحله شامل نوشتن و اجرای تست‌های یکپارچه‌سازی است که کل جریان کار را از دریافت پیام در تلگرام تا پردازش و پاسخ را پوشش می‌دهد. باید از یک ربات تست یا شبیه‌ساز استفاده شود. خارج از این مرحله: تست‌های واحد. نکته حیاتی: این تست‌ها باید در محیط staging اجرا شوند و با دیتابیس واقعی کار کنند.
- [ ] **مرحله 20: مستندسازی و راهنمای کاربر برای قابلیت‌های تلگرام** — این مرحله شامل نوشتن مستندات کامل برای کاربر نهایی است که نحوه استفاده از تمام قابلیت‌های تلگرام (نوتیفیکیشن، کنترل، دروس، AI) را توضیح می‌دهد. همچنین شامل مستندات فنی برای توسعه‌دهندگان (نحوه اضافه کردن دستور جدید، معماری). خارج از این مرحله: کدنویسی. نکته حیاتی: مستندات باید به دو زبان فارسی (برای

---

# 🔹 مرحله 1: ایجاد ساختار پایه برای اتصال تلگرام دوطرفه (Bot API + Webhook)

**Scope:** این مرحله شامل راه‌اندازی یک ربات تلگرام با استفاده از python-telegram-bot یا aiogram است. باید webhook یا polling فعال شود تا ربات بتواند پیام‌های کاربر را دریافت کند و پاسخ دهد. در این مرحله فقط اتصال اولیه برقرار می‌شود و هیچ قابلیت خاصی اضافه نمی‌شود. خارج از این مرحله: ارسال نوتیفیکیشن، کنترل سیستم، مدیریت دروس، اتصال به AI. نکته حیاتی: باید از کتابخانه‌ای استفاده شود که از async پشتیبانی کند چون پروژه احتمالاً async است.
**Key terms:** python-telegram-bot, aiogram, webhook, polling, BotFather, API_TOKEN

**بخش مربوط از متن کاربر:**
```
میخوام برای این پروژه هم نوتیفیکیشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد ساختار پایه ربات تلگرام دوطرفه با Webhook

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:38-48` — `app و server و wss` — اینجا نقطه اصلی backend است. یک endpoint جدید برای webhook تلگرام باید اضافه شود. همچنین WebSocket موجود (wss) می‌تواند برای ارسال نوتیفیکیشن‌های real-time به فرانت‌اند استفاده شود.
  ```jsx
  const app = express();
  const PORT = process.env.PORT || 3001;
  
  // Create HTTP server
  const server = createServer(app);
  
  // Create WebSocket server
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  
  app.use(cors());
  app.use(express.json({ limit: '10mb' }));
  ```
- `backend/.env.example:1-8` — `متغیرهای محیطی`
  ```
  # Gemini API Key - Get from https://aistudio.google.com/apikey
  GEMINI_API_KEY=your_gemini_api_key_here
  
  # Port (optional, default 3001)
  PORT=3001
  
  # Firebase config (optional - for data persistence)
  # VITE_FIREBASE_CONFIG={"apiKey":"
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست کرده است که برای این پروژه (اپلیکیشن آموزش لهجه لبنانی با هوش مصنوعی) نوتیفیکیشن از طریق تلگرام فراهم شود و تلگرام دوطرفه کار کند. یعنی علاوه بر ارسال اطلاعات از سیستم به تلگرام، بتوان از طریق تلگرام نیز سیستم را کنترل کرد و کارهایی روی سیستم انجام داد. این مرحله فقط شامل راه‌اندازی یک ربات تلگرام با استفاده از python-telegram-bot یا aiogram است. باید webhook یا polling فعال شود تا ربات بتواند پیام‌های کاربر را دریافت کند و پاسخ دهد. در این مرحله فقط اتصال اولیه برقرار می‌شود و هیچ قابلیت خاصی اضافه نمی‌شود. خارج از این مرحله: ارسال نوتیفیکیشن، کنترل سیستم، مدیریت دروس، اتصال به AI. نکته حیاتی: باید از کتابخانه‌ای استفاده شود که از async پشتیبانی کند چون پروژه احتمالاً async است. کلیدواژه‌ها: python-telegram-bot, aiogram, webhook, polling, BotFather, API_TOKEN. پروژه فعلی یک اپلیکیشن Node.js/Express با backend در backend/server.js و frontend React است. backend/server.js شامل WebSocket (ws) در خط 45 برای ارتباطات زنده، و APIهای مختلف Gemini (Chat در خط 56، TTS در خط 117، تحلیل فایل در خط 698) است. backend/server.js از import ماژولار ES (خط 1-12) و async/await استفاده می‌کند. backend/.env.example نشان می‌دهد که متغیرهای محیطی از طریق dotenv مدیریت می‌شوند (خط 5). backend/package.json شامل وابستگی‌های express, cors, dotenv, ws, multer, fluent-ffmpeg, pdf-parse است. frontend/index.html شامل Firebase config (خط 11-18) و یک Inspector Bridge Script (خط 32-201) است. frontend/src/App.jsx کامپوننت اصلی React است. پروژه فاقد هرگونه کد پایتون یا ربات تلگرام است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. با توجه به اینکه پروژه فعلی کاملاً Node.js/Express است و هیچ کد پایتونی ندارد، دو رویکرد وجود دارد:

1. **ایجاد یک سرویس پایتون مجزا** (رویکرد پیشنهادی): یک پکیج پایتون جدید در مسیر `backend/telegram_bot/` ایجاد شود که شامل:
   - `backend/telegram_bot/__init__.py`
   - `backend/telegram_bot/bot.py` (شامل راه‌اندازی ربات با python-telegram-bot و webhook)
   - `backend/telegram_bot/config.py` (خواندن API_TOKEN از متغیر محیطی)
   - `backend/telegram_bot/handlers.py` (هندلرهای پایه)
   - `backend/telegram_bot/requirements.txt` (python-telegram-bot, aiohttp)
   - `backend/telegram_bot/webhook_server.py` (سرور webhook با aiohttp)
   - یک endpoint جدید در `backend/server.js` (مثلاً `/api/telegram/webhook`) برای دریافت webhook از تلگرام و ارسال به سرویس پایتون.
   - متغیر محیطی `TELEGRAM_BOT_TOKEN` به `backend/.env.example` اضافه شود.

2. **استفاده از کتابخانه Node.js** (رویکرد جایگزین): استفاده از `node-telegram-bot-api` که از polling پشتیبانی می‌کند و با معماری async پروژه سازگار است. این رویکرد ساده‌تر است اما ممکن است برای مقیاس بزرگ مناسب نباشد.

مراحل پیاده‌سازی:
1. ایجاد دایرکتوری `backend/telegram_bot/` و فایل‌های مورد نیاز.
2. پیاده‌سازی `config.py` برای خواندن `TELEGRAM_BOT_TOKEN` از متغیر محیطی.
3. پیاده‌سازی `bot.py` با کلاس `TelegramBot` که از `python-telegram-bot` استفاده می‌کند و webhook را تنظیم می‌کند.
4. پیاده‌سازی `handlers.py` با هندلرهای پایه: `/start` (خوش‌آمدگویی)، `/help` (راهنما)، و یک fallback برای پیام‌های متنی.
5. پیاده‌سازی `webhook_server.py` با aiohttp برای دریافت webhook از تلگرام.
6. اضافه کردن endpoint `/api/telegram/webhook` در `backend/server.js` که درخواست‌های webhook را به سرویس پایتون فوروارد کند.
7. اضافه کردن `TELEGRAM_BOT_TOKEN` به `backend/.env.example`.
8. به‌روزرسانی `backend/package.json` برای اضافه کردن اسکریپت راه‌اندازی ربات تلگرام.
9. ایجاد `backend/telegram_bot/requirements.txt` با وابستگی‌ها.
10. تست اتصال با BotFather و تنظیم webhook URL.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 2: اضافه کردن قابلیت ارسال نوتیفیکیشن از سیستم به تلگرام

**Scope:** این مرحله شامل ایجاد یک سرویس یا تابع در بک‌اند است که بتواند پیام‌های نوتیفیکیشن را به کاربر از طریق ربات تلگرام ارسال کند. باید مشخص شود چه رویدادهایی باعث ارسال نوتیفیکیشن می‌شوند (مثلاً ثبت‌نام، تغییر وضعیت، اتمام فرآیند). خارج از این مرحله: دریافت پیام از کاربر و کنترل سیستم. نکته حیاتی: باید از chat_id کاربر استفاده شود که یا از دیتابیس خوانده شود یا در زمان راه‌اندازی ذخیره شود.
**Key terms:** send_message, chat_id, notification_service, event_handler, database

**بخش مربوط از متن کاربر:**
```
میخوام برای این پروژه هم نوتیفیکیشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن سرویس نوتیفیکیشن تلگرام به بک‌اند

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:38-48` — `app و server و wss` — محل اصلی افزودن endpoint جدید برای webhook تلگرام و import سرویس جدید
  ```jsx
  const app = express();
  const PORT = process.env.PORT || 3001;
  const server = createServer(app);
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  app.use(cors());
  app.use(express.json({ limit: '10mb' }));
  ```
- `backend/server.js:53-54` — `GEMINI_API_KEY` — الگوی خواندن API key از env — برای TELEGRAM_BOT_TOKEN نیز به همین شکل عمل شود
  ```jsx
  const GEMINI_API_KEY = process.env.GEMINI_API_KEY;
  ```
- `backend/.env.example:1-8` — `فایل env.example` — افزودن خط TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here به این فایل
  ```
  # Gemini API Key - Get from https://aistudio.google.com/apikey
  GEMINI_API_KEY=your_gemini_api_key_here
  
  # Port (optional, default 3001)
  PORT=3001
  
  # Firebase config (optional - for data persistence)
  # VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}
  ```
- `backend/package.json:1-15` — `dependencies` — افزودن وابستگی axios یا node-telegram-bot-api به این فایل
  ```json
  {
    "name": "lebanese-dialect-backend",
    "version": "1.0.0",
    "dependencies": {
      "cors": "^2.8.5",
      "dotenv": "^16.3.1",
      "express": "^4.18.2",
      "ws": "^8.19.0"
    }
  }
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `frontend/src/App.jsx` (سطر 1) — کامپوننت اصلی فرانت‌اند که ممکن است نیاز به نمایش وضعیت نوتیفیکیشن یا فرم تنظیمات تلگرام داشته باشد
- `frontend/index.html` (سطر 11) — حاوی Firebase config که برای ذخیره chat_id کاربران در دیتابیس استفاده می‌شود
- `backend/package-lock.json` (سطر 1) — بعد از افزودن وابستگی axios/node-telegram-bot-api باید به‌روزرسانی شود
- `frontend/package-lock.json` — اگر در فرانت‌اند نیز تغییری ایجاد شود،

## 🔍 Context و وضعیت فعلی
کاربر درخواست کرده است که قابلیت ارسال نوتیفیکیشن از سیستم به تلگرام به پروژه اضافه شود. این درخواست برگرفته از کامیت 'dad79e7 chore(prompt): sync task dc721af2-f84f-424d-a491-91e8ab3624c0 — میخوام برای این پروژه هم نوتیفیکیشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار' است. کاربر مشخص کرده که این مرحله شامل ایجاد یک سرویس یا تابع در بک‌اند است که بتواند پیام‌های نوتیفیکیشن را به کاربر از طریق ربات تلگرام ارسال کند. باید مشخص شود چه رویدادهایی باعث ارسال نوتیفیکیشن می‌شوند (مثلاً ثبت‌نام، تغییر وضعیت، اتمام فرآیند). خارج از این مرحله: دریافت پیام از کاربر و کنترل سیستم. نکته حیاتی: باید از chat_id کاربر استفاده شود که یا از دیتابیس خوانده شود یا در زمان راه‌اندازی ذخیره شود. کلیدواژه‌های ذکر شده: send_message, chat_id, notification_service, event_handler, database. پروژه فعلی یک اپلیکیشن آموزش لهجه لبنانی با هوش مصنوعی است که از Express.js در بک‌اند (backend/server.js) و React در فرانت‌اند (frontend/src/App.jsx) استفاده می‌کند. بک‌اند در حال حاضر از WebSocket (ws) برای ارتباطات زنده استفاده می‌کند (خط 45 backend/server.js: const wss = new WebSocketServer({ server, path: '/ws/live' });). همچنین از Firebase برای احراز هویت استفاده می‌کند (frontend/index.html خطوط 11-18). کاربر تأکید کرده که تلگرام باید دو طرفه کار کند یعنی علاوه بر ارسال اطلاعات از سیستم به تلگرام، قابلیت دریافت پیام از کاربر و کنترل سیستم نیز باید در مراحل بعدی اضافه شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/services/telegramService.js با توابع اصلی: sendMessage(chatId, text) برای ارسال پیام به تلگرام، setupWebhook() برای تنظیم webhook تلگرام، handleIncomingMessage(message) برای پردازش پیام‌های دریافتی. 2. ایجاد فایل جدید backend/config/telegram.js برای نگهداری تنظیمات ربات تلگرام (BOT_TOKEN, WEBHOOK_URL). 3. افزودن متغیر محیطی TELEGRAM_BOT_TOKEN به backend/.env.example. 4. ایجاد endpoint جدید در backend/server.js برای دریافت webhook از تلگرام: POST /api/telegram/webhook. 5. ایجاد یک event emitter یا middleware برای رویدادهای نوتیفیکیشن (ثبت‌نام، تغییر وضعیت، اتمام فرآیند). 6. ذخیره chat_id کاربران در دیتابیس (Firebase) یا یک فایل JSON ساده. 7. افزودن وابستگی node-telegram-bot-api یا axios به backend/package.json برای ارتباط با Telegram Bot API. 8. ایجاد endpoint GET /api/telegram/set-webhook برای تنظیم دستی webhook. 9. پیاده‌سازی مدیریت خطا و retry mechanism برای ارسال پیام.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 3: پیاده‌سازی دستورات کنترلی پایه از طریق تلگرام (مثلاً /status, /help)

**Scope:** این مرحله شامل تعریف و پیاده‌سازی چند دستور پایه در ربات تلگرام است که کاربر بتواند از طریق آنها اطلاعاتی از سیستم دریافت کند. مثلاً دستور /status برای نمایش وضعیت کلی سیستم، /help برای راهنما. خارج از این مرحله: دستورات پیچیده مانند مدیریت دروس یا اتصال به AI. نکته حیاتی: دستورات باید با استفاده از decoratorهای کتابخانه تلگرام تعریف شوند و پاسخ‌ها از دیتابیس یا API داخلی گرفته شود.
**Key terms:** CommandHandler, /status, /help, message_handler, reply_text

**بخش مربوط از متن کاربر:**
```
بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی دستورات کنترلی پایه تلگرام (/status, /help) با decorator

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:38-42` — `app creation and server start` — محل فعلی ایجاد سرور Express و WebSocket. پس از این خطوط باید BotController راه‌اندازی شود.
  ```jsx
  const app = express();
  const PORT = process.env.PORT || 3001;
  
  // Create HTTP server
  const server = createServer(app);
  
  // Create WebSocket server
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/server.js:167-169` — `GET /api/health` — این endpoint برای دستور /status استفاده می‌شود تا وضعیت سیستم را برگرداند.
  ```jsx
  app.get('/api/health', (req, res) => {
    res.json({ status: 'ok', timestamp: new Date().toISOString() });
  });
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: JavaScript (Node.js), Express.js, WebSocket (ws), Gemini API. کتابخانه‌های مرتبط: node-telegram-bot-api (جدید), dotenv, cors.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/.env.example` (سطر 1) — باید متغیر TELEGRAM_BOT_TOKEN به این فایل اضافه شود
- `backend/package.json` (سطر 1) — باید dependency 'node-telegram-bot-api' به این فایل اضافه شود
- `backend/server.js` (سطر 38) — فایل اصلی سرور که BotController باید در آن import و راه‌اندازی شود

## 🌐 نقشهٔ وابستگی‌ها
این تسک یک سرویس جدید (telegramBot.js) به پروژه اضافه می‌کند که وابسته به کتابخانه node-telegram-bot-api است. BotController از endpoint داخلی /api/health (backend/server.js خط 167) برای پاسخ به /status استفاده می‌کند. فایل backend/server.js باید اصلاح شود تا BotController را import کرده و در زمان start (بعد از خط 42) راه‌اندازی کند. فایل backend/.env.example باید متغیر TELEGRAM_BOT_TOKEN را اضافه کند. فایل backend/package.json باید dependency جدید را شامل شود. هیچ فایل frontend تحت تأثیر قرار نمی‌گیرد.

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی دستورات کنترلی پایه از طریق تلگرام (مثلاً /status, /help) را دارد. این مرحله شامل تعریف و پیاده‌سازی چند دستور پایه در ربات تلگرام است که کاربر بتواند از طریق آنها اطلاعاتی از سیستم دریافت کند. مثلاً دستور /status برای نمایش وضعیت کلی سیستم، /help برای راهنما. خارج از این مرحله: دستورات پیچیده مانند مدیریت دروس یا اتصال به AI. نکته حیاتی: دستورات باید با استفاده از decoratorهای کتابخانه تلگرام تعریف شوند و پاسخ‌ها از دیتابیس یا API داخلی گرفته شود. بخش مربوط از درخواست اصلی کاربر: 'بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم'. کلیدواژه‌ها: CommandHandler, /status, /help, message_handler, reply_text. در کد فعلی پروژه (backend/server.js) هیچ ربات تلگرامی وجود ندارد. پروژه یک backend Express.js با WebSocket (ws) و APIهای Gemini (Chat, TTS, File Analysis) دارد. فایل backend/server.js شامل endpointهای /api/gemini/chat (خط 56), /api/gemini/tts (خط 117), /api/health (خط 167), /api/list-models (خط 172), /api/test-gemini (خط 195), /api/analyze-files (خط 698) است. هیچ فایل یا ماژول تلگرام در ساختار پروژه (98 فایل) وجود ندارد. backend/package.json شامل dependencies: cors, dotenv, express, ws, multer, fluent-ffmpeg, ffmpeg-static, pdf-parse است. frontend/package.json شامل react, firebase, lucide-react است. برای پیاده‌سازی این درخواست، باید یک فایل جدید backend/services/telegramBot.js ایجاد شود که از کتابخانه node-telegram-bot-api استفاده کند و دستورات /status و /help را با decorator (یا pattern مشابه) پیاده‌سازی کند. پاسخ‌ها باید از API داخلی (مثلاً /api/health) گرفته شوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/services/telegramBot.js با ساختار زیر: import TelegramBot از 'node-telegram-bot-api'. تعریف کلاس BotController با decoratorهای سفارشی برای ثبت دستورات. 2. پیاده‌سازی decorator @command('/status') که تابع handleStatus را ثبت کند. handleStatus باید از endpoint داخلی GET /api/health (backend/server.js خط 167) وضعیت را بگیرد و reply_text کند. 3. پیاده‌سازی decorator @command('/help') که تابع handleHelp را ثبت کند. handleHelp باید لیست دستورات موجود را reply_text کند. 4. ایجاد فایل backend/config/telegram.js برای ذخیره TOKEN تلگرام از env (TELEGRAM_BOT_TOKEN). 5. اصلاح backend/server.js (خط 38-42) برای import و راه‌اندازی BotController در زمان start سرور. 6. افزودن dependency 'node-telegram-bot-api' به backend/package.json. 7. افزودن متغیر محیطی TELEGRAM_BOT_TOKEN به backend/.env.example.

## 💡 نمونه‌های قبل/بعد
**افزودن متغیر محیطی به backend/.env.example**

_قبل:_
```
# Firebase config (optional - for data persistence)
# VITE_FIREBASE_CONFIG={"
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 4: اضافه کردن قابلیت مشاهده لیست دروس از طریق تلگرام

**Scope:** این مرحله شامل ایجاد یک دستور تلگرامی (مثلاً /courses) است که لیست دروس موجود در سیستم را به صورت خلاصه نمایش دهد. اطلاعات باید از دیتابیس یا API مربوط به دروس خوانده شود. خارج از این مرحله: آپلود محتوا، مشاهده جزئیات درس، مدیریت تمرین با AI. نکته حیاتی: پاسخ باید به صورت فرمت‌شده (مثلاً با Markdown) باشد تا خوانایی داشته باشد.
**Key terms:** /courses, course_list, database_query, Markdown, reply_markdown

**بخش مربوط از متن کاربر:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن دستور /courses تلگرام برای نمایش لیست دروس

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست اضافه کردن قابلیت مشاهده لیست دروس از طریق تلگرام را دارد. این مرحله شامل ایجاد یک دستور تلگرامی (مثلاً /courses) است که لیست دروس موجود در سیستم را به صورت خلاصه نمایش دهد. اطلاعات باید از دیتابیس یا API مربوط به دروس خوانده شود. خارج از این مرحله: آپلود محتوا، مشاهده جزئیات درس، مدیریت تمرین با AI. نکته حیاتی: پاسخ باید به صورت فرمت‌شده (مثلاً با Markdown) باشد تا خوانایی داشته باشد. بخش مربوط از درخواست اصلی کاربر: 'و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...'. کلیدواژه‌ها: /courses, course_list, database_query, Markdown, reply_markdown.

بر اساس کد واقعی پروژه: در backend/server.js (خطوط 1-53) یک سرور Express با WebSocket و multer برای آپلود فایل وجود دارد. endpointهای موجود شامل /api/gemini/chat (خط 56)، /api/gemini/tts (خط 117)، /api/health (خط 167)، /api/list-models (خط 172)، /api/test-gemini (خط 195)، و /api/analyze-files (خط 698) هستند. هیچ endpoint یا سرویس تلگرام یا دیتابیس در کد فعلی وجود ندارد. فایل backend/.env.example فقط شامل GEMINI_API_KEY و PORT و Firebase config است. فایل frontend/src/App.jsx (که deep-read نشده) احتمالاً کامپوننت اصلی React است. فایل‌های prompt/archive/task-141249ee-0d23-44e0-aeec-21f989820b60.md و task-2a0673ad-13dd-4131-9c72-18a093d60686.md و task-2d1f95e8-b5ea-413f-b1ec-11b7bed6eeb7.md و task-3336cbc2-34e9-4f8f-8216-78d40b813199.md و task-38433cce-3e63-4851-89bc-4320f55f4d19.md و task-47bfef80-d77a-4386-9dca-fb1a533ca20b.md و task-59cbc244-8dbe-4b9d-9a39-a3633ffd0464.md و task-5bd04995-7545-4582-acb9-20c453d383a9.md و task-6225900f-6226-487c-9ae1-55dbd114e165.md و task-645bbdc2-f205-479f-b01f-f8b8ad4ef2e4.md و task-6d989ea3-b525-4566-994a-ce401ae7373a.md و task-7599ae4a-1d12-4c87-80c2-b16e5876d9d4.md و task-8ac8249d-e1de-40f8-ad4b-36e271a1b237.md و task-9d4c545b-6683-46f1-8121-8ae6705d2122.md و task-adbe7499-a96f-4c38-8c32-e547a3b7e2c2.md و task-b3d7759b-2054-457f-a5ce-54dc346fb960.md و task-ba60df37-44e8-426e-8bf8-4a48cbedb11b.md و task-c7474d4f-8730-4531-b8ec-50de5a444b82.md و task-d13296fb-53a8-45a4-919b-de4f984a5a64.md (که deep-read شده‌اند) احتمالاً شامل تسک‌های قبلی هستند اما محتوای آن‌ها در context موجود نیست.

نتیجه: پروژه فاقد هرگونه سرویس تلگرام، دیتابیس، و مدل داده برای دروس است. برای پیاده‌سازی این درخواست، باید از صفر شروع کرد: یک سرویس تلگرام (با کتابخانه‌ای مثل node-telegram-bot-api یا telegraf)، یک دیتابیس (مثلاً SQLite با Sequelize یا Firebase Firestore)، یک مدل Course، یک endpoint API برای دریافت لیست دروس، و یک هندلر برای دستور /courses که پاسخ Markdown بدهد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. نصب کتابخانه node-telegram-bot-api در backend (اضافه به backend/package.json).
2. ایجاد فایل جدید backend/services/telegramBot.js که:
   - با توکن تلگرام (از env) اتصال برقرار کند.
   - هندلر دستور /courses را ثبت کند.
   - در هندلر، از یک endpoint داخلی (مثلاً GET /api/courses) لیست دروس را بگیرد.
   - پاسخ را به صورت Markdown فرمت کند و با reply_markdown ارسال کند.
3. ایجاد فایل جدید backend/models/Course.js (یا استفاده از Firebase اگر قبلاً ست شده) با فیلدهای: id, title, description, createdAt.
4. ایجاد endpoint جدید GET /api/courses در backend/server.js (حدود خط 700) که:
   - از دیتابیس (یا یک فایل JSON موقت) لیست دروس را بخواند.
   - به صورت JSON با آرایه‌ای از اشیاء course برگرداند.
5. ایجاد یک seed ساده (backend/seed.js) برای پر کردن دیتابیس با چند درس نمونه.
6. در backend/server.js، در خطوط 40-50 (بعد از ایجاد app)، سرویس تلگرام را راه‌اندازی کند.
7. اضافه کردن متغیر محیطی TELEGRAM_BOT_TOKEN به backend/.env.example.
8. پاسخ Markdown برای /courses باید شامل: عنوان هر درس به صورت bold، توضیح کوتاه، و شماره/آیدی درس باشد.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 5: اضافه کردن قابلیت مشاهده جزئیات یک درس خاص از طریق تلگرام

**Scope:** این مرحله شامل ایجاد دستوری مانند /course <id> است که جزئیات کامل یک درس (شامل توضیحات، لیست جلسات، فایل‌های مرتبط) را نمایش دهد. اطلاعات باید از API مربوطه گرفته شود. خارج از این مرحله: آپلود فایل، دانلود فایل، مدیریت تمرین با AI. نکته حیاتی: اگر درس فایل دارد، باید لینک دانلود یا پیش‌نمایش ارائه شود.
**Key terms:** /course, course_detail, file_link, session_list, inline_keyboard

**بخش مربوط از متن کاربر:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن دستور /course برای نمایش جزئیات درس در تلگرام

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:38-48` — `app initialization and WebSocket setup` — محل فعلی راه‌اندازی سرور Express و WebSocket. باید ربات تلگرام را به همین سرور HTTP متصل کنیم تا از WebSocket موجود استفاده کند.
  ```jsx
  const app = express();
  const PORT = process.env.PORT || 3001;
  
  // Create HTTP server
  const server = createServer(app);
  
  // Create WebSocket server
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  
  app.use(cors());
  app.use(express.json({ limit: '10mb' }));
  ```
- `backend/server.js:166-169` — `GET /api/health endpoint` — الگوی endpoint ساده در Express. از همین الگو برای ایجاد GET /api/courses/:id استفاده می‌شود.
  ```jsx
  // Health check
  app.get('/api/health', (req, res) => {
    res.json({ status: 'ok', timestamp: new Date().toISOString() });
  });
  ```
- `frontend/index.html:10-19` — `Firebase configuration` — پیکربندی Firebase موجود در frontend. برای احراز هویت کاربران تلگرام می‌توان از همین Firebase استفاده کرد.
  ```
  var __firebase_config = JSON.stringify({
    apiKey: "AIzaSyAMFtV0zIMKKe0bIr68Z1wTORQ1jQpsv9Q",
    authDomain: "labaneseapp.firebaseapp.com",
    projectId: "labaneseapp",
    storageBucket: "labaneseapp.firebasestorage.app",
    messagingSenderId: "951874597795",
    appId: "1:951874597795:web:00745327993adad760a016"
  });
  var __app_id = "labaneseapp";
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/package.json` (سطر 1) — برای افزودن وابستگی node-telegram-bot-api به پروژه نیاز است.
- `backend/.env.example` (سطر 1) — برای افزودن متغیر محیطی TELEGRAM_BOT_TOKEN به فایل env نیاز است.
- `frontend/src/App.jsx` (سطر 1) — ممکن است برای نمایش WebApp تلگرام یا لینک‌های مربوط به دروس نیاز به تغییر داشته باشد.
- `prompt/task-dc721af2-f84f-424d-a491-91e8ab3624c0.md` (سطر 1) — این تسک قبلی مربوط به راه‌اندازی اولیه تلگرام است و باید با آن هماهنگ شود.

## 🌐 نقشهٔ وابستگی‌ها
این تسک به فایل‌های backend/server.js (برای افزودن API جدید و اتصال WebSocket)، backend/package.json (برای افزودن وابستگی node-telegram-bot-api)، backend/.env.example (برای افزود

## 🔍 Context و وضعیت فعلی
کاربر درخواست اضافه کردن قابلیت مشاهده جزئیات یک درس خاص از طریق تلگرام را دارد. این مرحله شامل ایجاد دستوری مانند /course <id> است که جزئیات کامل یک درس (شامل توضیحات، لیست جلسات، فایل‌های مرتبط) را نمایش دهد. اطلاعات باید از API مربوطه گرفته شود. خارج از این مرحله: آپلود فایل، دانلود فایل، مدیریت تمرین با AI. نکته حیاتی: اگر درس فایل دارد، باید لینک دانلود یا پیش‌نمایش ارائه شود. کاربر در بخش مربوط از درخواست اصلی خود گفته: 'و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...'. کلیدواژه‌های مشخص شده: /course, course_detail, file_link, session_list, inline_keyboard. با بررسی کد موجود در backend/server.js، مشخص است که پروژه از Express.js با WebSocket (ws) استفاده می‌کند و APIهای مختلفی مانند /api/gemini/chat، /api/gemini/tts، /api/analyze-files و ... دارد. همچنین در frontend/index.html از Firebase برای احراز هویت استفاده شده است. با توجه به commit قبلی (dad79e7) که اشاره به 'میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار' دارد، به نظر می‌رسد زیرساخت اولیه تلگرام در حال پیاده‌سازی است. فایل‌های مرتبط با این تسک شامل backend/server.js (برای افزودن API جدید)، یک فایل جدید برای مدیریت ربات تلگرام (مانند backend/telegramBot.js)، و احتمالاً فایل‌های frontend برای نمایش اطلاعات در WebApp تلگرام هستند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/telegramBot.js برای مدیریت ربات تلگرام با استفاده از کتابخانه node-telegram-bot-api. 2. افزودن هندلر برای دستور /course <id> که: a) شناسه درس را از پیام استخراج کند. b) یک API جدید در backend/server.js ایجاد کند (مانند GET /api/courses/:id) که جزئیات درس را از دیتابیس یا منبع داده برگرداند. c) پاسخ را به صورت یک پیام فرمت‌شده با inline keyboard شامل دکمه‌های لینک دانلود فایل‌ها (در صورت وجود) و لیست جلسات نمایش دهد. 3. افزودن endpoint GET /api/courses/:id در backend/server.js که اطلاعات درس شامل: title, description, sessions (لیست جلسات با تاریخ و موضوع), files (لیست فایل‌های مرتبط با لینک دانلود) را برگرداند. 4. پیاده‌سازی inline keyboard با دکمه‌های: 'مشاهده جلسات', 'دانلود فایل‌ها', 'بازگشت به لیست دروس'. 5. اتصال ربات تلگرام به WebSocket موجود در خط 45 backend/server.js (const wss = new WebSocketServer({ server, path: '/ws/live' })) برای دریافت نوتیفیکیشن‌های لحظه‌ای. 6. افزودن middleware برای احراز هویت کاربران تلگرام با استفاده از Firebase (که در frontend/index.html خط 11-18 پیکربندی شده).

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 6: اضافه کردن قابلیت آپلود فایل (محتوای دروس) از طریق تلگرام

**Scope:** این مرحله شامل پیاده‌سازی قابلیت دریافت فایل (PDF، تصویر، ویدئو) از طریق تلگرام و ذخیره آن در سیستم (مثلاً در پوشه مخصوص درس) است. کاربر باید بتواند فایل را برای ربات بفرستد و ربات آن را در درس مشخص‌شده ذخیره کند. خارج از این مرحله: مدیریت تمرین با AI، ویرایش فایل. نکته حیاتی: باید از chat_id و state machine استفاده شود تا کاربر مشخص کند فایل مربوط به کدام درس است.
**Key terms:** MessageHandler, document, photo, file_download, upload_service, state_machine

**بخش مربوط از متن کاربر:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن قابلیت آپلود فایل دروس از طریق تلگرام با state machine

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:30-33` — `multer configuration` — این multer configuration موجود برای آپلود فایل‌های دروس از طریق تلگرام قابل استفاده مجدد است. نیاز به افزودن یک instance جدید با storage متفاوت (پوشه lessons) دارد.
  ```jsx
  const upload = multer({
    storage: storage,
    limits: { fileSize: 500 * 1024 * 1024 } // 500MB max per file
  });
  ```
- `backend/server.js:698-700` — `app.post('/api/analyze-files')` — این endpoint الگوی مناسبی برای ایجاد endpoint جدید آپلود فایل دروس است. می‌توان از ساختار مشابه با middleware multer و error handling استفاده کرد.
  ```jsx
  app.post('/api/analyze-files', upload.array('files', 10), handleMulterError, async (req, res) => {
    console.log('Received file analysis request');
  ```
- `backend/server.js:44-45` — `WebSocketServer initialization` — WebSocket موجود می‌تواند برای ارسال نوتیفیکیشن به فرانت‌اند هنگام آپلود فایل جدید از طریق تلگرام استفاده شود.
  ```jsx
  // Create WebSocket server
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/.env.example` (سطر 8) — نیاز به افزودن متغیر TELEGRAM_BOT_TOKEN برای پیکربندی ربات تلگرام
- `frontend/src/App.jsx` — نیاز به به‌روزرسانی برای نمایش لیست فایل‌های آپلود شده هر درس و امکان مشاهده/دانلود آن‌ها
- `frontend/index.html` — Firebase configuration موجود در خطوط ۱۰-۱۸ می‌تواند برای ذخیره فایل‌ها در

## 🔍 Context و وضعیت فعلی
کاربر درخواست اضافه کردن قابلیت آپلود فایل (محتوای دروس) از طریق تلگرام را دارد. این قابلیت باید به کاربر اجازه دهد فایل‌هایی از نوع PDF، تصویر، و ویدئو را برای ربات تلگرام بفرستد و ربات آن را در درس مشخص‌شده ذخیره کند. نکته حیاتی که کاربر تأکید کرده: باید از chat_id و state machine استفاده شود تا کاربر مشخص کند فایل مربوط به کدام درس است. کاربر همچنین اشاره کرده: «و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا آپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم». کلیدواژه‌های ذکر شده: MessageHandler, document, photo, file_download, upload_service, state_machine. خارج از این مرحله: مدیریت تمرین با AI، ویرایش فایل. در کد فعلی پروژه (backend/server.js) زیرساخت WebSocket در خط ۴۵ (const wss = new WebSocketServer({ server, path: '/ws/live' })) و آپلود فایل با multer در خطوط ۳۰-۳۳ (const upload = multer({ storage: storage, limits: { fileSize: 500 * 1024 * 1024 } })) وجود دارد. endpoint اصلی آپلود فایل در خط ۶۹۸ (app.post('/api/analyze-files', upload.array('files', 10), handleMulterError, async (req, res) => {...})) پیاده‌سازی شده که از multer برای دریافت فایل‌ها و تحلیل آن‌ها با Gemini API استفاده می‌کند. همچنین در frontend/index.html خطوط ۱۰-۱۸ Firebase configuration (var __firebase_config = JSON.stringify({...})) و app id (var __app_id = "labaneseapp") تعریف شده که می‌توان از Firebase Storage برای ذخیره فایل‌های آپلود شده استفاده کرد. backend/.env.example خط ۸ نیز Firebase config اختیاری را نشان می‌دهد. برای پیاده‌سازی این قابلیت، نیاز به ایجاد یک ربات تلگرام جدید (یا توسعه ربات موجود)، پیاده‌سازی state machine برای مدیریت مکالمه (انتخاب درس → آپلود فایل → تأیید)، و ایجاد endpoint جدید برای ذخیره فایل در پوشه مخصوص درس است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ۱. ایجاد فایل جدید backend/services/telegramUploadService.js با پیاده‌سازی state machine شامل state‌های: IDLE, WAITING_LESSON_SELECTION, WAITING_FILE_UPLOAD, CONFIRM_UPLOAD. ۲. ایجاد فایل جدید backend/services/telegramBot.js که با استفاده از کتابخانه node-telegram-bot-api (نیاز به نصب) ربات تلگرام را راه‌اندازی کند و MessageHandler برای document, photo, video را پیاده‌سازی کند. ۳. ایجاد endpoint جدید POST /api/lessons/upload در backend/server.js (پس از خط ۶۹۸) که فایل را در مسیر backend/uploads/lessons/{lessonId}/ ذخیره کند و metadata آن را در یک فایل JSON (مانند backend/data/lessonFiles.json) ثبت کند. ۴. استفاده از multer موجود (خطوط ۳۰-۳۳) برای مدیریت آپلود فایل. ۵. ایجاد دایرکتوری backend/uploads/lessons/ و backend/data/. ۶. افزودن متغیر محیطی TELEGRAM_BOT_TOKEN به backend/.env.example. ۷. به‌روزرسانی frontend/src/App.jsx برای نمایش لیست فایل‌های آپلود شده هر درس.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 7: ایجاد اتصال اولیه به بخش 'تمرین با استاد هوش مصنوعی' از طریق تلگرام

**Scope:** این مرحله شامل برقراری ارتباط بین ربات تلگرام و API یا سرویس داخلی 'تمرین با استاد هوش مصنوعی' است. باید بتوان یک session جدید شروع کرد و موضوعات موجود را دریافت کرد. خارج از این مرحله: ارسال پیام صوتی، دریافت پاسخ صوتی، مدیریت کامل. نکته حیاتی: باید از API داخلی پروژه استفاده شود و احراز هویت کاربر در تلگرام با حساب کاربری در سیستم تطبیق داده شود.
**Key terms:** ai_tutor_api, session_start, topic_list, authentication, user_mapping

**بخش مربوط از متن کاربر:**
```
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
اتصال ربات تلگرام به بخش تمرین با استاد هوش مصنوعی

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:56-114` — `app.post('/api/gemini/chat')` — این endpoint فعلی برای چت با Gemini است. باید یک endpoint جدید مشابه برای AI Tutor ایجاد شود که session management و topic list را پشتیبانی کند.
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
      // Clean payload - preserve audio data if includeAudio flag is set
      if (payload.contents) {
        payload.contents = payload.contents.map(content => ({
          role: content.role,
          parts: content.parts.map(part => {
            if (part.inline_data && includeAudio) {
              return { inline_data: part.inline_data };
            }
            if (part.text !== undefined) {
              return { text: part.text };
            }
            return null;
          }).filter(p => p !== null)
        }));
      }
      const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${GEMINI_API_KEY}`;
      const response = await fetch(apiUrl, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(payload)
      });
      if (!response.ok) {
        const errorData = await response.text();
        console.error('Gemini API error:', errorData);
        return res.status(response.status).json({ error: errorData });
      }
      const result = await response.json();
      if (includeAudio) {
        res.json(result);
      } else {
        const text = result.candidates?.[0]?.content?.parts?.[0]?.text;
        if (!text) {
          return res.status(500).json({ error: 'No text in response' });
        }
        res.json({ text });
      }
    } catch (error) {
      console.error('Chat API error:', error);
      res.status(500).json({ error: error.message });
    }
  });
  ```
- `backend/server.js:44-45` — `WebSocketServer` — WebSocket موجود در خط 45 برای ارتباط live استفاده می‌شود. می‌توان از همین مکانیزم برای ارتباط دوطرفه تلگرام با AI Tutor استفاده کرد.
  ```jsx
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `frontend/index.html:11-18` — `Firebase Configuration` — Firebase Config موجود در frontend/index.html برای احراز هویت استفاده می‌شود. باید یک سرویس backend برای تطبیق هویت کاربر تلگرام با Firebase User ID ایجاد شود.
  ```
  var __firebase_config = JSON.stringify({
    apiKey: "AIzaSyAMFtV0zIMKKe0bIr68Z1wTORQ1jQpsv9Q",
    authDomain: "labaneseapp.firebaseapp.com",
    projectId: "labaneseapp",
    storageBucket: "labaneseapp.firebasestorage.app",
    messagingSenderId: "951874597795",
    appId: "1:951874597795:web:00745327993adad760a016"
  });
  var __app_id = "labaneseapp";
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد اتصال اولیه بین ربات تلگرام و بخش 'تمرین با استاد هوش مصنوعی' (AI Tutor) را دارد. این مرحله شامل برقراری ارتباط بین ربات تلگرام و API داخلی پروژه است. باید بتوان یک session جدید (session_start) شروع کرد و موضوعات موجود (topic_list) را دریافت کرد. خارج از این مرحله: ارسال پیام صوتی، دریافت پاسخ صوتی، مدیریت کامل. نکته حیاتی: باید از API داخلی پروژه استفاده شود و احراز هویت کاربر در تلگرام با حساب کاربری در سیستم تطبیق داده شود (authentication, user_mapping). کلیدواژه‌های ذکر شده: ai_tutor_api, session_start, topic_list, authentication, user_mapping. در پروژه فعلی، backend/server.js شامل APIهای مربوط به Gemini (Chat در خط 56، TTS در خط 117، File Analysis در خط 698) است اما هیچ endpoint یا سرویسی برای 'تمرین با استاد هوش مصنوعی' (AI Tutor) وجود ندارد. همچنین frontend/index.html شامل Firebase Config (خط 11-18) است که می‌تواند برای احراز هویت استفاده شود. فایل backend/.env.example نیز اشاره به Firebase Config دارد. در حال حاضر هیچ فایل یا سرویس تلگرامی در پروژه وجود ندارد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک سرویس جدید در backend به نام 'backend/services/aiTutorService.js' که شامل توابع startSession(userId) برای شروع session جدید و getTopics() برای دریافت لیست موضوعات باشد. 2. ایجاد یک endpoint جدید در backend/server.js مانند POST /api/ai-tutor/start-session که session_start را مدیریت کند و GET /api/ai-tutor/topics که topic_list را برگرداند. 3. ایجاد یک سرویس احراز هویت تلگرام در 'backend/services/telegramAuthService.js' که user_mapping بین Telegram User ID و Firebase User ID را انجام دهد. 4. ایجاد یک WebSocket endpoint یا polling mechanism برای ارتباط دوطرفه تلگرام با AI Tutor. 5. استفاده از Firebase Authentication (که در frontend/index.html خط 11-18 پیکربندی شده) برای تطبیق هویت کاربر تلگرام با حساب کاربری در سیستم.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 8: پیاده‌سازی تبادل پیام متنی با استاد هوش مصنوعی از طریق تلگرام

**Scope:** این مرحله شامل ارسال پیام متنی کاربر از تلگرام به AI و دریافت پاسخ متنی از AI و نمایش آن در تلگرام است. باید از API داخلی AI برای ارسال و دریافت استفاده شود. خارج از این مرحله: پیام صوتی، مدیریت session. نکته حیاتی: باید history مکالمه حفظ شود تا AI بتواند context را بفهمد.
**Key terms:** send_message, ai_response, conversation_history, api_call, reply_text

**بخش مربوط از متن کاربر:**
```
و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی تبادل پیام متنی با استاد هوش مصنوعی از طریق تلگرام

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:56-114` — `app.post('/api/gemini/chat')` — این endpoint اصلی برای ارسال پیام به Gemini API است. برای تلگرام، باید مشابه این endpoint را با webhook تلگرام ترکیب کرد.
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
      if (payload.contents) {
        payload.contents = payload.contents.map(content => ({
          role: content.role,
          parts: content.parts.map(part => {
            if (part.inline_data && includeAudio) {
              return { inline_data: part.inline_data };
            }
            if (part.text !== undefined) {
              return { text: part.text };
            }
            return null;
          }).filter(p => p !== null)
        }));
      }
      const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${GEMINI_API_KEY}`;
      const response = await fetch(apiUrl, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(payload)
      });
      if (!response.ok) {
        const errorData = await response.text();
        console.error('Gemini API error:', errorData);
        return res.status(response.status).json({ error: errorData });
      }
      const result = await response.json();
      if (includeAudio) {
        res.json(result);
      } else {
        const text = result.candidates?.[0]?.content?.parts?.[0]?.text;
        if (!text) {
          return res.status(500).json({ error: 'No text in response' });
        }
        res.json({ text });
      }
    } catch (error) {
      console.error('Chat API error:', error);
      res.status(500).json({ error: error.message });
    }
  });
  ```
- `backend/server.js:45` — `WebSocketServer` — WebSocket موجود برای ارتباط live. برای تلگرام نیازی به WebSocket نیست، اما می‌توان از آن برای اطلاع‌رسانی استفاده کرد.
  ```jsx
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `frontend/index.html:11-18` — `Firebase Config`
  ```
  var __firebase_config = JSON.stringify({
    apiKey: "AIzaSyAMFtV0zIMKKe0bIr68Z1wTORQ1jQpsv9Q",
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی تبادل پیام متنی با استاد هوش مصنوعی از طریق تلگرام را دارد. این مرحله شامل ارسال پیام متنی کاربر از تلگرام به AI و دریافت پاسخ متنی از AI و نمایش آن در تلگرام است. باید از API داخلی AI برای ارسال و دریافت استفاده شود. خارج از این مرحله: پیام صوتی، مدیریت session. نکته حیاتی: باید history مکالمه حفظ شود تا AI بتواند context را بفهمد. بخش مربوط از درخواست اصلی کاربر: 'و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم'. کلیدواژه‌ها: send_message, ai_response, conversation_history, api_call, reply_text. در کد فعلی پروژه، backend/server.js شامل endpointهای /api/gemini/chat (خط 56) و /api/gemini/tts (خط 117) است که از Gemini API استفاده می‌کنند. همچنین WebSocket در خط 45 با path '/ws/live' راه‌اندازی شده. frontend/index.html شامل Firebase config (خطوط 11-18) و inspector bridge (خطوط 32-201) است. backend/.env.example نشان می‌دهد که GEMINI_API_KEY و Firebase config قابل تنظیم هستند. backend/package.json شامل dependencies: express, cors, dotenv, ws, multer, fluent-ffmpeg, ffmpeg-static, pdf-parse است. frontend/package.json شامل react, react-dom, firebase, lucide-react است. پروژه در GitHub با repo name 'mahdighandi1989/language' و watched_id '6b04f8bd-b562-4fb9-9cd9-0c27458ced59' است. برای پیاده‌سازی، نیاز به ایجاد یک endpoint جدید در backend/server.js برای دریافت پیام از تلگرام (webhook) و ارسال به Gemini API، ذخیره conversation history در Firebase یا حافظه، و ارسال پاسخ به تلگرام است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/services/telegramService.js برای مدیریت webhook تلگرام و ارسال/دریافت پیام. 2. افزودن endpoint POST /api/telegram/webhook در backend/server.js برای دریافت پیام‌های تلگرام. 3. استفاده از تابع analyzeWithGemini (خط 273 در backend/server.js) برای ارسال پیام به Gemini API و دریافت پاسخ. 4. پیاده‌سازی ذخیره conversation history در Firebase (با استفاده از firebase config موجود در frontend/index.html خطوط 11-18) یا در حافظه با Map. 5. ارسال پاسخ به تلگرام با استفاده از Telegram Bot API (sendMessage). 6. افزودن متغیر محیطی TELEGRAM_BOT_TOKEN به backend/.env.example. 7. تست با ارسال پیام متنی از تلگرام و بررسی پاسخ.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 9: اضافه کردن قابلیت ارسال پیام صوتی به استاد هوش مصنوعی از طریق تلگرام

**Scope:** این مرحله شامل دریافت فایل صوتی از کاربر در تلگرام، ارسال آن به سرویس تشخیص گفتار (ASR) داخلی پروژه، ارسال متن استخراج‌شده به AI، و برگرداندن پاسخ به کاربر است. خارج از این مرحله: دریافت پاسخ صوتی از AI. نکته حیاتی: باید از API تشخیص گفتار موجود در پروژه استفاده شود و فایل صوتی قبل از ارسال به API ذخیره موقت شود.
**Key terms:** voice_message, ASR, speech_to_text, audio_file, transcription

**بخش مربوط از متن کاربر:**
```
و از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن قابلیت دریافت و پردازش پیام صوتی از تلگرام با ASR داخلی

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:698-800` — `app.post('/api/analyze-files')` — این endpoint اصلی تحلیل فایل‌هاست که می‌توان از آن برای ASR استفاده کرد. منطق پردازش فایل صوتی در اینجا پیاده‌سازی شده.
  ```jsx
  app.post('/api/analyze-files', upload.array('files', 10), handleMulterError, async (req, res) => {
    console.log('Received file analysis request');
    if (!GEMINI_API_KEY) {
      console.error('API key not configured');
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const files = req.files || [];
      const textContent = req.body.textContent || '';
      const userInstructions = req.body.userInstructions || '';
  ```
- `backend/server.js:273-294` — `analyzeWithGemini` — این تابع هسته ASR پروژه است که فایل صوتی را به Gemini می‌فرستد و متن استخراج‌شده را برمی‌گرداند.
  ```jsx
  async function analyzeWithGemini(parts, systemPrompt) {
    const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${GEMINI_API_KEY}`;
    const payload = {
      contents: [{ role: 'user', parts }],
      systemInstruction: { parts: [{ text: systemPrompt }] }
    };
    const response = await fetch(apiUrl, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(payload)
    });
  ```
- `backend/server.js:56-114` — `app.post('/api/gemini/chat')` — این endpoint برای ارسال متن به AI و دریافت پاسخ استفاده می‌شود. پس از ASR، متن استخراج‌شده به اینجا فرستاده می‌شود.
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/server.js` (سطر 1) — فایل اصلی backend که تمام endpointها و توابع ASR در آن قرار دارند. تغییرات اصلی در این فایل اعمال می‌شود.
- `frontend/src/App.jsx` — کامپوننت اصلی فرانت‌اند که ممکن است برای نمایش وضعیت پیام صوتی

## 🔍 Context و وضعیت فعلی
کاربر درخواست اضافه کردن قابلیت ارسال پیام صوتی به استاد هوش مصنوعی از طریق تلگرام را دارد. این مرحله شامل دریافت فایل صوتی از کاربر در تلگرام، ارسال آن به سرویس تشخیص گفتار (ASR) داخلی پروژه، ارسال متن استخراج‌شده به AI، و برگرداندن پاسخ به کاربر است. خارج از این مرحله: دریافت پاسخ صوتی از AI. نکته حیاتی: باید از API تشخیص گفتار موجود در پروژه استفاده شود و فایل صوتی قبل از ارسال به API ذخیره موقت شود. کاربر همچنین اشاره کرده: 'و از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه'. کلیدواژه‌های اصلی: voice_message, ASR, speech_to_text, audio_file, transcription. در کد فعلی پروژه (backend/server.js)، یک endpoint برای تحلیل فایل‌های صوتی از طریق Gemini API وجود دارد: مسیر /api/analyze-files (خط 698) که از multer برای آپلود فایل استفاده می‌کند و فایل‌های صوتی را با تابع analyzeWithGemini (خط 273) یا uploadToGeminiFileAPI (خط 297) پردازش می‌کند. همچنین یک WebSocket در مسیر /ws/live (خط 45) وجود دارد. پروژه از Firebase برای احراز هویت استفاده می‌کند (frontend/index.html خطوط 11-18). برای پیاده‌سازی این قابلیت، باید یک endpoint جدید در backend/server.js ایجاد شود که فایل صوتی را از تلگرام دریافت کند، آن را موقتاً ذخیره کند، با استفاده از ASR موجود (همان analyzeWithGemini یا مسیر /api/analyze-files) متن را استخراج کند، متن را به AI بفرستد و پاسخ را به کاربر برگرداند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک endpoint جدید در backend/server.js با مسیر POST /api/telegram/voice که فایل صوتی را از تلگرام دریافت کند (با multer). 2. ذخیره موقت فایل در uploadDir (خط 20). 3. استفاده از تابع analyzeWithGemini (خط 273) یا فراخوانی داخلی /api/analyze-files برای استخراج متن از صوت. 4. ارسال متن استخراج‌شده به endpoint /api/gemini/chat (خط 56) برای دریافت پاسخ AI. 5. برگرداندن پاسخ متنی به کاربر تلگرام. 6. پاکسازی فایل موقت پس از پردازش. 7. اضافه کردن مسیر WebSocket جدید برای ارتباط دوطرفه تلگرام در صورت نیاز.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 10: اضافه کردن قابلیت دریافت پاسخ صوتی از استاد هوش مصنوعی در تلگرام

**Scope:** این مرحله شامل تبدیل پاسخ متنی AI به گفتار (TTS) با استفاده از سرویس موجود در پروژه و ارسال فایل صوتی حاصل به کاربر در تلگرام است. خارج از این مرحله: مدیریت session، ارسال همزمان متن و صوت. نکته حیاتی: باید از همان سرویس TTS که در وب استفاده می‌شود استفاده شود تا کیفیت یکسان باشد.
**Key terms:** TTS, text_to_speech, voice_reply, audio_file, send_voice

**بخش مربوط از متن کاربر:**
```
و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن پاسخ صوتی TTS به ربات تلگرام

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:117-164` — `app.post('/api/gemini/tts')` — این endpoint سرویس TTS موجود است که باید در تلگرام复用 شود. خروجی audioData و mimeType را برمی‌گرداند.
  ```jsx
  app.post('/api/gemini/tts', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const { prompt, voice = 'Kore' } = req.body;
      const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-preview-tts:generateContent?key=${GEMINI_API_KEY}`;
  ```
- `backend/server.js:44-45` — `WebSocketServer creation` — WebSocket موجود که باید handler جدید برای voice_reply به آن اضافه شود.
  ```jsx
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/server.js:19-23` — `uploadDir` — دایرکتوری موقت برای ذخیره فایل‌های صوتی قبل از ارسال به تلگرام.
  ```jsx
  const uploadDir = join(os.tmpdir(), 'lebanese-uploads');
  if (!fs.existsSync(uploadDir)) {
    fs.mkdirSync(uploadDir, { recursive: true });
  }
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
JavaScript (Node.js با Express), WebSocket (ws کتابخانه), Gemini API (TTS endpoint), multer برای ذخیره فایل

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/server.js` (سطر 1) — فایل اصلی backend که شامل endpoint TTS و WebSocket server است
- `backend/.env.example` (سطر 1) — ممکن است نیاز به افزودن TELEGRAM_BOT_TOKEN به env باشد
- `frontend/src/App.jsx` (سطر 1) — کامپوننت frontend که از TTS در وب استفاده می‌کند — برای اطمینان از یکسان بودن voice پیش‌فرض

## 🌐 نقشهٔ وابستگی‌ها
این قابلیت به backend/server.js وابسته است. تابع sendVoiceReply باید از endpoint /api/gemini/tts (خطوط 117-164) استفاده کند. WebSocket handler در خط 45 باید توسعه یابد. فایل‌های صوتی موقت در uploadDir (خط 20) ذخیره می‌شوند. نیاز به کتابخانه تلگرام (مثلاً node-telegram-bot-api) یا استفاده از WebSocket برای ارتباط با تلگرام است. env ممکن است نیاز به TELEGRAM_BOT_TOKEN داشته باشد.

## 🔍 Context و وضعیت فعلی
کاربر درخواست اضافه کردن قابلیت دریافت پاسخ صوتی از استاد هوش مصنوعی در تلگرام را دارد. این مرحله شامل تبدیل پاسخ متنی AI به گفتار (TTS) با استفاده از سرویس موجود در پروژه و ارسال فایل صوتی حاصل به کاربر در تلگرام است. خارج از این مرحله: مدیریت session، ارسال همزمان متن و صوت. نکته حیاتی: باید از همان سرویس TTS که در وب استفاده می‌شود استفاده شود تا کیفیت یکسان باشد. بخش مربوط از درخواست اصلی کاربر: 'و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....' کلیدواژه‌ها: TTS, text_to_speech, voice_reply, audio_file, send_voice. در کد فعلی backend/server.js خطوط 117-164 endpoint /api/gemini/tts وجود دارد که از مدل gemini-2.5-flash-preview-tts استفاده می‌کند و خروجی audioData با mimeType audio/ برمی‌گرداند. این همان سرویس TTS است که در وب استفاده می‌شود و باید در تلگرام نیز از همین endpoint استفاده شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] تابع sendVoiceReply(chatId, text) در backend/server.js ایجاد شود و endpoint /api/gemini/tts را با voice='Kore' صدا بزند
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک تابع جدید در backend/server.js به نام sendVoiceReply(chatId, text) که: a) متن را به endpoint /api/gemini/tts بفرستد (POST با body شامل {prompt: text, voice: 'Kore'}) b) audioData base64 را دریافت کند c) buffer صوتی را از base64 decode کند d) فایل صوتی موقت در uploadDir ذخیره کند e) از طریق WebSocket یا API تلگرام، فایل صوتی را به chatId مشخص شده ارسال کند f) فایل موقت را پاک کند. 2. در WebSocket handler موجود (خط 45 wss = new WebSocketServer) یک event handler جدید برای 'voice_reply' اضافه کند که chatId و text را دریافت و تابع بالا را صدا بزند. 3. اطمینان از اینکه voice پیش‌فرض 'Kore' (همانند خط 123) استفاده شود تا کیفیت یکسان با وب حفظ شود.

## 💡 نمونه‌های قبل/بعد
**افزودن تابع sendVoiceReply به backend/server.js**

_قبل:_
```
// (فعلاً وجود ندارد)
```

_بعد:_
```
async function sendVoiceReply(chatId, text) {
  try {
    const response = await fetch(`http://localhost:${PORT}/api/gemini/tts`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ prompt: text, voice: 'Kore' })
    });
    const data = await response.json();
    if (data.audioData) {
      const buffer = Buffer.from(data.audioData, 'base64');
      const filePath = join(uploadDir, `voice_${Date.now()}.ogg`);
      fs.writeFileSync(filePath, buffer);
      // ارسال به تلگرام از طریق WebSocket یا API
      // ...
      fs.unlinkSync(filePath);
    }
  } catch (err) {
    console.error('Voice reply error:', err);
  }
}
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 11: همگام‌سازی کامل چت تلگرام با چت وب (تاریخچه و وضعیت)

**Scope:** این مرحله شامل اطمینان از این است که پیام‌های ردوبدل‌شده در تلگرام در تاریخچه چت وب نیز ذخیره شوند و بالعکس. همچنین وضعیت session (فعال، بسته) در هر دو پلتفرم یکسان باشد. خارج از این مرحله: قابلیت‌های جدید. نکته حیاتی: باید از یک دیتابیس مشترک برای ذخیره پیام‌ها استفاده شود و timestampها هماهنگ باشند.
**Key terms:** sync, conversation_history, database, timestamp, web_chat

**بخش مربوط از متن کاربر:**
```
و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....
```

## 🎯 هدف (خلاصه ساختاریافته)
همگام‌سازی کامل چت تلگرام با چت وب (تاریخچه و وضعیت)

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:45` — `wss` — WebSocket سرور فعلی برای چت وب. باید اصلاح شود تا پیام‌ها را به تلگرام نیز فوروارد کند.
  ```jsx
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/server.js:56-114` — `app.post('/api/gemini/chat')` — Endpoint چت با Gemini. پاسخ‌های صوتی و متنی این endpoint باید به تلگرام نیز ارسال شوند.
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => { ... })
  ```
- `frontend/index.html:11-18` — `__firebase_config` — Firebase config موجود در frontend. از Firebase Firestore به عنوان دیتابیس مشترک برای ذخیره تاریخچه چت استفاده شود.
  ```
  var __firebase_config = JSON.stringify({ apiKey: "AIzaSyAMFtV0zIMKKe0bIr68Z1wTORQ1jQpsv9Q", authDomain: "labaneseapp.firebaseapp.com", projectId: "labaneseapp", storageBucket: "labaneseapp.firebasestorage.app", messagingSenderId: "951874597795", appId: "1:951874597795:web:00745327993adad760a016" });
  ```
- `backend/.env.example:1-8` — `GEMINI_API_KEY` — فایل env example. باید TELEGRAM_BOT_TOKEN به آن اضافه شود.
  ```
  GEMINI_API_KEY=your_gemini_api_key_here
  PORT=3001
  # Firebase config (optional - for data persistence)
  # VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `frontend/src/App.jsx` — کامپوننت اصلی چت وب. باید وضعیت همگام‌سازی با تلگرام را نمایش دهد و پیام‌های جدید را از WebSocket دریافت کند.
- `backend/package.json` — باید وابستگی‌های جدید (grammy یا node-telegram-bot-api و firebase-admin) به آن اضافه شود.
- `frontend/package.json` — باید وابستگی‌های جدید (firebase) به آن اضافه شود (اگر قبلاً نصب نیست).
- `prompt/task-dc721af2-f84f-424d-a491-91e8ab3624c0.md` — تسک قبلی مرتبط با افزودن نوتیفیکیشن تلگرام دوطرفه. این تسک ادامه و تکمیل آن است.

## 🌐 نقشهٔ وابستگی‌ها
این تسک به فایل‌های backend/server.js (WebSocket و APIهای Gemini)، frontend/index.html (Firebase config)، backend/.env.example (متغیرهای محیطی)، frontend/src/App.jsx (UI چت وب)، و backend/package.json (وابستگی‌ها) وابسته است. همچنین به یک دیتابیس مشترک (Firebase Firestore) برای ذخیره تاریخچه چت نیاز دارد. WebSocket فعلی (خط 45 server.js) باید اصلاح شود تا پیام‌ها

## 🔍 Context و وضعیت فعلی
کاربر درخواست همگام‌سازی کامل چت تلگرام با چت وب را دارد. این شامل اطمینان از این است که پیام‌های ردوبدل‌شده در تلگرام در تاریخچه چت وب نیز ذخیره شوند و بالعکس. همچنین وضعیت session (فعال، بسته) در هر دو پلتفرم یکسان باشد. خارج از این مرحله: قابلیت‌های جدید. نکته حیاتی: باید از یک دیتابیس مشترک برای ذخیره پیام‌ها استفاده شود و timestampها هماهنگ باشند. بخش مربوط از درخواست اصلی کاربر: 'و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....' کلیدواژه‌ها: sync, conversation_history, database, timestamp, web_chat. در کد فعلی پروژه (backend/server.js) هیچ endpoint یا سرویسی برای تلگرام وجود ندارد. فایل frontend/src/App.jsx نیز deep-read نشده اما احتمالاً چت وب را مدیریت می‌کند. backend/server.js شامل WebSocket (خط 45: const wss = new WebSocketServer({ server, path: '/ws/live' });) و APIهای Gemini (خطوط 56-164) است. backend/.env.example فقط GEMINI_API_KEY و PORT و Firebase config را دارد. backend/package.json شامل dependencies: express, cors, dotenv, ws, multer, fs, os, fluent-ffmpeg, ffmpeg-static, pdf-parse است. frontend/package.json شامل react, react-dom, firebase, lucide-react است. برای پیاده‌سازی این تسک، نیاز به ایجاد یک سرویس تلگرام (با استفاده از node-telegram-bot-api یا grammy)، یک دیتابیس مشترک (احتمالاً Firebase Firestore که در frontend/index.html خط 11-18 کانفیگ شده)، و هماهنگ‌سازی timestampها بین WebSocket و Telegram Bot است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/services/telegramService.js برای مدیریت Webhook و ارسال/دریافت پیام‌های تلگرام با استفاده از کتابخانه grammy. 2. ایجاد فایل جدید backend/services/chatSyncService.js برای همگام‌سازی پیام‌ها بین تلگرام و وب با استفاده از Firebase Firestore (کانفیگ شده در frontend/index.html خط 11-18). 3. افزودن endpoint جدید در backend/server.js برای Webhook تلگرام (POST /api/telegram/webhook). 4. اصلاح WebSocket handler در backend/server.js (خط 45) برای broadcast پیام‌های جدید به کلاینت‌های وب و ذخیره همزمان در Firestore. 5. افزودن متغیر محیطی TELEGRAM_BOT_TOKEN به backend/.env.example. 6. ایجاد فایل frontend/src/services/telegramSync.js برای نمایش وضعیت همگام‌سازی در UI وب. 7. اطمینان از هماهنگی timestampها با استفاده از Date.now() یا Timestamp از Firebase.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: high
- تخمین زمان: large

---

# 🔹 مرحله 12: اضافه کردن قابلیت مدیریت موضوعات تمرین AI از طریق تلگرام (ایجاد، ویرایش، حذف)

**Scope:** این مرحله شامل ایجاد دستورات تلگرامی برای مدیریت موضوعات تمرین AI است. کاربر بتواند موضوع جدید ایجاد کند (/topic_new)، موضوع موجود را ویرایش کند (/topic_edit <id> <text>)، و موضوع را حذف کند (/topic_delete <id>). خارج از این مرحله: خود تمرین با AI. نکته حیاتی: باید احراز هویت و سطح دسترسی کاربر بررسی شود (فقط مدیر یا استاد).
**Key terms:** /topic_new, /topic_edit, /topic_delete, CRUD, authorization

**بخش مربوط از متن کاربر:**
```
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن دستورات تلگرامی مدیریت موضوعات تمرین AI (ایجاد، ویرایش، حذف) با احراز هویت

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست اضافه کردن قابلیت مدیریت موضوعات تمرین AI از طریق تلگرام را دارد. این شامل ایجاد دستورات تلگرامی برای مدیریت موضوعات تمرین AI است. کاربر بتواند موضوع جدید ایجاد کند (/topic_new)، موضوع موجود را ویرایش کند (/topic_edit <id> <text>)، و موضوع را حذف کند (/topic_delete <id>). خارج از این مرحله: خود تمرین با AI. نکته حیاتی: باید احراز هویت و سطح دسترسی کاربر بررسی شود (فقط مدیر یا استاد). بخش مربوط از درخواست اصلی کاربر: 'و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم'. کلیدواژه‌ها: /topic_new, /topic_edit, /topic_delete, CRUD, authorization. در کد فعلی پروژه (backend/server.js) هیچ پیاده‌سازی برای تلگرام یا مدیریت موضوعات وجود ندارد. فایل‌های مرتبط با تلگرام در پروژه دیده نمی‌شود. فایل‌های prompt/archive/task-141249ee-0d23-44e0-aeec-21f989820b60.md و prompt/archive/task-2a0673ad-13dd-4131-9c72-18a093d60686.md و prompt/archive/task-2d1f95e8-b5ea-413f-b1ec-11b7bed6eeb7.md و prompt/archive/task-3336cbc2-34e9-4f8f-8216-78d40b813199.md و prompt/archive/task-38433cce-3e63-4851-89bc-4320f55f4d19.md و prompt/archive/task-47bfef80-d77a-4386-9dca-fb1a533ca20b.md و prompt/archive/task-59cbc244-8dbe-4b9d-9a39-a3633ffd0464.md و prompt/archive/task-5bd04995-7545-4582-acb9-20c453d383a9.md و prompt/archive/task-6225900f-6226-487c-9ae1-55dbd114e165.md و prompt/archive/task-645bbdc2-f205-479f-b01f-f8b8ad4ef2e4.md و prompt/archive/task-6d989ea3-b525-4566-994a-ce401ae7373a.md و prompt/archive/task-7599ae4a-1d12-4c87-80c2-b16e5876d9d4.md و prompt/archive/task-8ac8249d-e1de-40f8-ad4b-36e271a1b237.md و prompt/archive/task-9d4c545b-6683-46f1-8121-8ae6705d2122.md و prompt/archive/task-adbe7499-a96f-4c38-8c32-e547a3b7e2c2.md و prompt/archive/task-b3d7759b-2054-457f-a5ce-54dc346fb960.md و prompt/archive/task-ba60df37-44e8-426e-8bf8-4a48cbedb11b.md و prompt/archive/task-c7474d4f-8730-4531-b8ec-50de5a444b82.md و prompt/archive/task-d13296fb-53a8-45a4-919b-de4f984a5a64.md در deep context موجودند اما محتوای آن‌ها در اینجا نمایش داده نشده است. backend/server.js خطوط 1-800 شامل APIهای مربوط به Gemini، تحلیل فایل، و WebSocket است اما هیچ ربطی به تلگرام ندارد. frontend/index.html شامل Firebase config و Inspector Bridge است. backend/package.json شامل dependencies: express, cors, dotenv, ws, multer, fluent-ffmpeg, ffmpeg-static, pdf-parse است. frontend/package.json شامل react, firebase, lucide-react است. هیچ کتابخانه‌ای برای تلگرام (node-telegram-bot-api یا telegraf) در dependencies دیده نمی‌شود. بنابراین این تسک نیازمند اضافه کردن یک سرویس تلگرام جدید در backend است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/services/telegramBot.js با استفاده از کتابخانه node-telegram-bot-api (نیاز به نصب دارد). 2. پیاده‌سازی دستور /topic_new که از کاربر متن موضوع را دریافت کرده و در یک دیتابیس (فعلاً می‌توان از فایل JSON یا Firebase استفاده کرد) ذخیره کند. 3. پیاده‌سازی دستور /topic_edit <id> <text> که موضوع با شناسه مشخص را ویرایش کند. 4. پیاده‌سازی دستور /topic_delete <id> که موضوع را حذف کند. 5. پیاده‌سازی middleware احراز هویت که بررسی کند کاربر فقط مدیر یا استاد باشد (با استفاده از chatId مجاز یا لیست کاربران در Firebase). 6. اضافه کردن فایل backend/services/topicService.js برای مدیریت CRUD موضوعات. 7. اضافه کردن endpoint REST در backend/server.js برای همگام‌سازی موضوعات با فرانت‌اند (GET /api/topics, POST /api/topics, PUT /api/topics/:id, DELETE /api/topics/:id). 8. اتصال به Firebase (که قبلاً در frontend/index.html خط 11-18 کانفیگ شده) برای ذخیره‌سازی موضوعات. 9. افزودن متغیر محیطی TELEGRAM_BOT_TOKEN به backend/.env.example. 10. به‌روزرسانی backend/server.js برای راه‌اندازی بات تلگرام در زمان استارت سرور.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 13: اضافه کردن قابلیت انتخاب موضوع تمرین AI از طریق تلگرام

**Scope:** این مرحله شامل ایجاد یک دستور یا منوی تعاملی (Inline Keyboard) در تلگرام است که کاربر بتواند از بین موضوعات موجود یکی را برای تمرین انتخاب کند. پس از انتخاب، session AI با آن موضوع شروع شود. خارج از این مرحله: مدیریت موضوعات. نکته حیاتی: باید از CallbackQueryHandler برای مدیریت انتخاب‌ها استفاده شود.
**Key terms:** InlineKeyboard, CallbackQueryHandler, topic_selection, session_start, button

**بخش مربوط از متن کاربر:**
```
و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن انتخاب موضوع تمرین AI از طریق تلگرام با InlineKeyboard

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:53-54` — `const GEMINI_API_KEY` — محل فعلی تعریف API keyها — پس از این خط باید TelegramBotHandler راه‌اندازی شود
  ```jsx
  const GEMINI_API_KEY = process.env.GEMINI_API_KEY;
  ```
- `backend/server.js:56-114` — `app.post('/api/gemini/chat')` — API موجود Gemini Chat — session AI تلگرام باید از همین endpoint استفاده کند با topic به عنوان system instruction
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
      if (payload.contents) {
        payload.contents = payload.contents.map(content => ({
          role: content.role,
          parts: content.parts.map(part => {
            if (part.inline_data && includeAudio) {
              return { inline_data: part.inline_data };
            }
            if (part.text !== undefined) {
              return { text: part.text };
            }
            return null;
          }).filter(p => p !== null)
        }));
      }
      const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${GEMINI_API_KEY}`;
  ```
- `backend/.env.example:1-6` — `GEMINI_API_KEY` — فایل env.example — باید خط TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here به آن اضافه شود
  ```
  # Gemini API Key - Get from https://aistudio.google.com/apikey
  GEMINI_API_KEY=your_gemini_api_key_here
  
  # Port (optional, default 3001)
  PORT=3001
  
  # Firebase config (optional - for data persistence)
  # VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/package.json` (سطر 1) — برای افزودن وابستگی node-telegram-bot-api به dependencies نیاز به تغییر دارد
- `backend/server.js` (سطر 53) — فایل اصلی سرور — باید TelegramBotHandler را import و راه‌اندازی کند (حدود خط 53)
- `backend/.env.example` (سطر 6) — برای افزودن متغیر محیطی TELEGRAM_BOT_TOKEN به عنوان راهنما برای توسعه‌دهندگان
- `frontend/src/App.jsx` (سطر 1) — اگر قرار باشد session AI از طریق WebSocket با frontend هم sync شود، این فایل نیاز به تغییر دارد
- `backend/server.js` (سطر 45) — WebSocket موجود در خط 45 (const wss = new WebSocketServer({ server, path: '/ws/live' })) می‌تواند برای ارسال وضعیت session به frontend استفاده شود

## 🌐 نقشهٔ وابستگی‌ها
این قابلیت به فایل‌های جدید backend/services/telegramBot.js و backend/services/aiSessionManager.js وابسته

## 🔍 Context و وضعیت فعلی
کاربر درخواست اضافه کردن قابلیت انتخاب موضوع تمرین AI از طریق تلگرام را دارد. این مرحله شامل ایجاد یک دستور یا منوی تعاملی (Inline Keyboard) در تلگرام است که کاربر بتواند از بین موضوعات موجود یکی را برای تمرین انتخاب کند. پس از انتخاب، session AI با آن موضوع شروع شود. خارج از این مرحله: مدیریت موضوعات. نکته حیاتی: باید از CallbackQueryHandler برای مدیریت انتخاب‌ها استفاده شود. بخش مربوط از درخواست اصلی کاربر: 'و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم'. کلیدواژه‌ها: InlineKeyboard, CallbackQueryHandler, topic_selection, session_start, button. در کد فعلی پروژه (backend/server.js) هیچ پیاده‌سازی تلگرام وجود ندارد. فایل‌های مرتبط با تلگرام در پروژه موجود نیستند و باید از صفر ساخته شوند. پروژه از Express.js در backend و React در frontend استفاده می‌کند. WebSocket در خط 45 backend/server.js برای ارتباطات زنده پیاده‌سازی شده. APIهای Gemini در خطوط 56-164 backend/server.js موجود هستند. فایل backend/.env.example نشان می‌دهد که متغیرهای محیطی برای GEMINI_API_KEY و PORT تعریف شده‌اند. برای پیاده‌سازی تلگرام، نیاز به افزودن کتابخانه node-telegram-bot-api و ایجاد فایل جدید backend/services/telegramBot.js است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/services/telegramBot.js با ساختار زیر: 
   - import کتابخانه node-telegram-bot-api
   - ایجاد یک کلاس TelegramBotHandler
   - پیاده‌سازی متد start() برای راه‌اندازی بات با توکن از process.env.TELEGRAM_BOT_TOKEN
   - پیاده‌سازی دستور /start با ارسال پیام خوش‌آمدگویی و InlineKeyboard با دکمه‌های موضوعات
   - پیاده‌سازی دستور /practice که InlineKeyboard با موضوعات تمرینی نمایش دهد
   - استفاده از CallbackQueryHandler برای مدیریت کلیک روی دکمه‌ها
   - پس از انتخاب موضوع، session AI با آن موضوع شروع شود و کاربر بتواند از طریق چت با هوش مصنوعی صحبت کند
   - ارسال درخواست به API Gemini (backend/server.js خط 56-114) با context موضوع انتخاب‌شده
2. افزودن متغیر محیطی TELEGRAM_BOT_TOKEN به backend/.env.example
3. افزودن import و راه‌اندازی TelegramBotHandler در backend/server.js (حدود خط 53)
4. نصب کتابخانه node-telegram-bot-api با npm install node-telegram-bot-api
5. ایجاد فایل backend/services/aiSessionManager.js برای مدیریت session‌های AI بر اساس topic
6. اتصال session AI به API موجود /api/gemini/chat (backend/server.js خط 56) با ارسال topic به عنوان system instruction

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 14: نوشتن تست‌های واحد برای سرویس نوتیفیکیشن تلگرام

**Scope:** این مرحله شامل نوشتن تست‌های واحد برای توابع ارسال پیام به تلگرام است. باید تست شود که پیام به درستی ساخته می‌شود، خطاهای API مدیریت می‌شوند، و chat_id معتبر بررسی می‌شود. خارج از این مرحله: تست‌های integration با خود تلگرام. نکته حیاتی: باید از mock برای API تلگرام استفاده شود.
**Key terms:** unittest, mock, send_message, notification_service, pytest

**بخش مربوط از متن کاربر:**
```
میخوام برای این پروژه هم نوتیفیکیشن از طریق تلگرام فراهم بشه
```

## 🎯 هدف (خلاصه ساختاریافته)
نوشتن تست‌های واحد برای سرویس نوتیفیکیشن تلگرام

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:1-14` — `import section` — محل فعلی importها در backend/server.js — سرویس جدید تلگرام باید در فایل جداگانه‌ای ایجاد شود و در اینجا import گردد
  ```jsx
  import express from 'express';
  import cors from 'cors';
  import { fileURLToPath } from 'url';
  import { dirname, join } from 'path';
  import dotenv from 'dotenv';
  import { createServer } from 'http';
  import { WebSocketServer, WebSocket } from 'ws';
  import multer from 'multer';
  import fs from 'fs';
  import os from 'os';
  import ffmpeg from 'fluent-ffmpeg';
  import ffmpegStatic from 'ffmpeg-static';
  ```
- `backend/.env.example:1-7` — `environment variables` — فایل .env.example فعلی — باید متغیر TELEGRAM_BOT_TOKEN به آن اضافه شود
  ```
  # Gemini API Key - Get from https://aistudio.google.com/apikey
  GEMINI_API_KEY=your_gemini_api_key_here
  
  # Port (optional, default 3001)
  PORT=3001
  
  # Firebase config (optional - for data persistence)
  # VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}
  ```
- `backend/package.json:1-15` — `dependencies` — package.json backend — باید وابستگی axios یا node-fetch برای فراخوانی API تلگرام اضافه شود
  ```json
  {
    "name": "lebanese-dialect-backend",
    "version": "1.0.0",
    "dependencies": {
      "cors": "^2.8.5",
      "dotenv": "^16.3.1",
      "express": "^4.18.2",
      "ws": "^8.19.0"
    }
  }
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `prompt/archive/task-dc721af2-f84f-424d-a491-91e8ab3624c0.md` (سطر 1) — فایل تسک اصلی که درخواست کاربر برای نوتیفیکیشن تلگرام در آن ثبت شده است
- `prompt/task-dc721af2-f84f-424d-a491-91e8ab3624c0.md` (سطر 1) — فایل تسک فعال که درخواست کاربر برای نوتیفیکیشن تلگرام در آن ثبت شده است
- `frontend/src/App.jsx` (سطر 1) — کامپوننت اصلی فرانت‌اند که احتمالاً باید نوتیفیکیشن‌های تلگرام را نمایش دهد یا دکمه ارسال نوتیفیکیشن داشته باشد
- `backend/server.js` (سطر 56) — فایل اصلی backend که سرویس تلگرام باید در آن import و استفاده شود

## 🌐 نقشهٔ وابستگی‌ها
این تسک وابسته به ایجاد فایل جدید backend/services/telegram_notification_service.js است که توسط backend/server.js (خطوط 1-14) import خواهد شد. همچنین نیاز به اضافه کردن وابستگی axios یا node-fetch به backend/package.json دارد. متغیر محیطی TELEGRAM_BOT_TOKEN باید به backend/.env.example اض

## 🔍 Context و وضعیت فعلی
کاربر درخواست نوشتن تست‌های واحد برای توابع ارسال پیام به تلگرام را دارد. این درخواست برگرفته از درخواست اصلی کاربر است: «میخوام برای این پروژه هم نوتیفیکیشن از طریق تلگرام فراهم بشه». باید تست شود که پیام به درستی ساخته می‌شود، خطاهای API مدیریت می‌شوند، و chat_id معتبر بررسی می‌شود. خارج از این مرحله: تست‌های integration با خود تلگرام. نکته حیاتی: باید از mock برای API تلگرام استفاده شود. کلیدواژه‌ها: unittest, mock, send_message, notification_service, pytest. در کد فعلی پروژه (backend/server.js) هیچ سرویس نوتیفیکیشن تلگرامی وجود ندارد. فایل‌های مرتبط با این تسک شامل backend/server.js (خطوط 1-1403) است که APIهای مختلفی مانند /api/gemini/chat (خط 56)، /api/gemini/tts (خط 117)، /api/analyze-files (خط 698) را پیاده‌سازی کرده اما خبری از سرویس تلگرام نیست. همچنین فایل backend/.env.example (خطوط 1-7) شامل متغیرهای محیطی GEMINI_API_KEY و PORT است و هیچ متغیری برای BOT_TOKEN تلگرام ندارد. فایل frontend/src/App.jsx نیز کامپوننت اصلی فرانت‌اند است که احتمالاً باید نوتیفیکیشن‌ها را نمایش دهد. فایل‌های prompt/archive/task-dc721af2-f84f-424d-a491-91e8ab3624c0.md و prompt/task-dc721af2-f84f-424d-a491-91e8ab3624c0.md مستقیماً به این تسک مرتبط هستند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. مراحل پیاده‌سازی: 1) ایجاد فایل backend/services/telegram_notification_service.js شامل توابع: sendMessage(chatId, text) برای ارسال پیام به تلگرام با استفاده از API تلگرام (https://api.telegram.org/bot{BOT_TOKEN}/sendMessage)، validateChatId(chatId) برای بررسی معتبر بودن chat_id، و formatMessage(message) برای ساخت پیام. 2) ایجاد فایل backend/tests/test_telegram_notification_service.js با استفاده از کتابخانه Jest (که در package.json موجود است) و mocking کتابخانه axios یا node-fetch برای شبیه‌سازی API تلگرام. 3) تست‌ها باید شامل: تست ارسال موفق پیام (mock کردن پاسخ 200 از API تلگرام)، تست خطای 401 (توکن نامعتبر)، تست خطای 400 (chat_id نامعتبر)، تست timeout شبکه، تست ساخت پیام با متن ساده و با فرمت Markdown. 4) اضافه کردن متغیر محیطی TELEGRAM_BOT_TOKEN به backend/.env.example. 5) اجرای تست‌ها با دستور: npx jest backend/tests/test_telegram_notification_service.js --verbose

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 15: نوشتن تست‌های واحد برای دستورات کنترلی تلگرام

**Scope:** این مرحله شامل نوشتن تست‌های واحد برای هندلرهای دستورات تلگرام (مانند /status, /help, /courses) است. باید تست شود که دستورات به درستی پردازش می‌شوند و پاسخ مناسب تولید می‌شود. خارج از این مرحله: تست‌های مربوط به AI. نکته حیاتی: باید از Mock برای دیتابیس و API استفاده شود.
**Key terms:** CommandHandler, unittest, mock, reply_text, handler_test

**بخش مربوط از متن کاربر:**
```
بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

## 🎯 هدف (خلاصه ساختاریافته)
نوشتن تست‌های واحد برای هندلرهای دستورات تلگرام (CommandHandler)

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:1-14` — `imports` — فایل اصلی backend که APIهای فعلی را تعریف می‌کند. هیچ هندلر تلگرامی در این فایل وجود ندارد. برای اضافه کردن سرویس تلگرام، باید یک فایل جداگانه (telegramService.js) ایجاد شود و در server.js import شود.
  ```jsx
  import express from 'express';
  import cors from 'cors';
  import { fileURLToPath } from 'url';
  import { dirname, join } from 'path';
  import dotenv from 'dotenv';
  import { createServer } from 'http';
  import { WebSocketServer, WebSocket } from 'ws';
  import multer from 'multer';
  import fs from 'fs';
  import os from 'os';
  import ffmpeg from 'fluent-ffmpeg';
  import ffmpegStatic from 'ffmpeg-static';
  ```
- `backend/package.json:1-15` — `dependencies` — فایل package.json backend که وابستگی‌های فعلی را نشان می‌دهد. برای تست‌نویسی باید mocha, chai, sinon به devDependencies اضافه شوند.
  ```json
  {
    "name": "lebanese-dialect-backend",
    "version": "1.0.0",
    "dependencies": {
      "cors": "^2.8.5",
      "dotenv": "^16.3.1",
      "express": "^4.18.2",
      "ws": "^8.19.0"
    }
  }
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack تشخیص داده شده: JavaScript (Node.js) با Express برای backend، React برای frontend. کتابخانه

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/server.js` (سطر 1) — فایل اصلی backend که باید سرویس تلگرام در آن import شود و endpointهای جدید (در صورت نیاز) اضافه شوند.
- `backend/.env.example` (سطر 1) — فایل نمونه env که باید شامل متغیرهای محیطی برای توکن تلگرام (TELEGRAM_BOT_TOKEN) باشد.
- `frontend/src/App.jsx` (سطر 1) — کامپوننت اصلی فرانت‌اند که ممکن است در آینده از وضعیت تلگرام (مثلاً برای نمایش نوتیفیکیشن) استفاده کند.

## 🌐 نقشهٔ وابستگی‌ها
این تسک به فایل‌های backend/server.js (برای import سرویس تلگرام و اضافه کردن مسیرهای API در صورت نیاز)، backend/package.json (برای اضافه کردن وابستگی‌های تست و کتابخانه تلگرام)، backend/.env.example (برای اضافه کردن TELEGRAM_BOT_TOKEN)، و backend/services/telegramService.js (فایل جدید برای هندلرهای دستورات) وابسته است. همچنین فایل‌های تست backend/tests/telegramService.test.js باید ایجاد شوند. وابستگی‌های جدید شامل node-telegram-bot-api (برای ارتباط با تلگرام)، mocha, chai, sinon (برای تست‌نویسی) هستند.

## 🔍 Context و وضعیت فعلی
کاربر درخواست نوشتن تست‌های واحد برای هندلرهای دستورات تلگرام (مانند /status, /help, /courses) را دارد. باید تست شود که دستورات به درستی پردازش می‌شوند و پاسخ مناسب تولید می‌شود. خارج از این مرحله: تست‌های مربوط به AI. نکته حیاتی: باید از Mock برای دیتابیس و API استفاده شود. کلیدواژه‌های ذکر شده: CommandHandler, unittest, mock, reply_text, handler_test. بخش مربوط از درخواست اصلی کاربر: "بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم". با بررسی کد واقعی پروژه در backend/server.js (خطوط 1-800)، مشخص شد که این فایل شامل APIهای مربوط به Gemini (Chat, TTS, File Analysis) و WebSocket است، اما هیچ هندلر تلگرامی در آن وجود ندارد. فایل‌های frontend/index.html و frontend/src/App.jsx نیز فقط مربوط به فرانت‌اند هستند. در ساختار پروژه، فایل backend/package.json و backend/package-lock.json نشان می‌دهند که وابستگی‌های فعلی شامل express, cors, dotenv, ws, multer, fluent-ffmpeg, ffmpeg-static, pdf-parse هستند و هیچ کتابخانه‌ای مانند node-telegram-bot-api یا telegraf وجود ندارد. بنابراین، برای پیاده‌سازی این تسک، ابتدا باید یک سرویس تلگرام (احتمالاً در backend/services/telegramService.js) ایجاد شود و سپس تست‌های واحد برای آن نوشته شود. فایل‌های مرتبط با تست در پروژه وجود ندارند و باید پوشه backend/tests/ ایجاد شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل backend/services/telegramService.js با کلاس TelegramBotService شامل متدهای handleCommand, handleStatus, handleHelp, handleCourses. 2. ایجاد فایل backend/tests/telegramService.test.js با تست‌های واحد برای هر دستور. 3. استفاده از کتابخانه mocha و chai برای تست‌نویسی (اضافه به devDependencies). 4. Mock کردن دیتابیس و APIهای خارجی با sinon یا jest.mock. 5. تست‌های زیر: a) تست /status: بررسی اینکه پاسخ شامل وضعیت سیستم است. b) تست /help: بررسی اینکه پاسخ شامل لیست دستورات است. c) تست /courses: بررسی اینکه پاسخ شامل لیست دوره‌ها است. d) تست reply_text: بررسی اینکه تابع reply_text با متن مناسب فراخوانی می‌شود. 6. اضافه کردن اسکریپت test به backend/package.json. 7. اطمینان از اینکه تست‌ها با npm test در backend اجرا می‌شوند.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 16: نوشتن تست‌های واحد برای قابلیت آپلود فایل از طریق تلگرام

**Scope:** این مرحله شامل نوشتن تست‌های واحد برای فرآیند دریافت فایل از تلگرام، ذخیره‌سازی و ارتباط با درس مربوطه است. باید سناریوهای مختلف مانند فایل نامعتبر، درس ناموجود، و آپلود موفق تست شود. خارج از این مرحله: تست‌های مربوط به AI. نکته حیاتی: باید از mock برای file download و دیتابیس استفاده شود.
**Key terms:** unittest, mock, file_upload, state_machine, MessageHandler

**بخش مربوط از متن کاربر:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
نوشتن تست‌های واحد برای قابلیت آپلود فایل از طریق تلگرام

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:19-33` — `multer storage configuration` — زیرساخت فعلی آپلود فایل با multer — برای Telegram upload باید复用 شود یا سرویس جداگانه‌ای برای ذخیره‌سازی فایل‌های دریافتی از Telegram ایجاد شود.
  ```jsx
  const uploadDir = join(os.tmpdir(), 'lebanese-uploads');
  if (!fs.existsSync(uploadDir)) {
    fs.mkdirSync(uploadDir, { recursive: true });
  }
  
  const storage = multer.diskStorage({
    destination: (req, file, cb) => cb(null, uploadDir),
    filename: (req, file, cb) => cb(null, `${Date.now()}-${file.originalname}`)
  });
  
  const upload = multer({
    storage: storage,
    limits: { fileSize: 500 * 1024 * 1024 }
  });
  ```
- `backend/server.js:698-715` — `app.post('/api/analyze-files')`
  ```jsx
  app.post('/api/analyze-files', upload.array('files',
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست نوشتن تست‌های واحد برای فرآیند دریافت فایل از تلگرام، ذخیره‌سازی و ارتباط با درس مربوطه را دارد. این تسک بر اساس درخواست اصلی کاربر است: «میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار» و بخش «و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم». کلیدواژه‌های مشخص شده: unittest, mock, file_upload, state_machine, MessageHandler. سناریوهای مورد نیاز: فایل نامعتبر، درس ناموجود، آپلود موفق. نکته حیاتی: باید از mock برای file download و دیتابیس استفاده شود. خارج از این مرحله: تست‌های مربوط به AI. در کد فعلی پروژه (backend/server.js) زیرساخت آپلود فایل با multer در خطوط 19-33 پیاده‌سازی شده است: storage در uploadDir (os.tmpdir() + '/lebanese-uploads') با محدودیت 500MB. endpoint /api/analyze-files در خط 698 از upload.array('files', 10) استفاده می‌کند. WebSocket در خط 45 روی /ws/live ایجاد شده. فایل‌های مرتبط با Telegram در ساختار پروژه دیده نمی‌شوند و باید ساخته شوند. فایل‌های prompt/archive/task-141249ee-0d23-44e0-aeec-21f989820b60.md و task-2a0673ad-13dd-4131-9c72-18a093d60686.md و task-3336cbc2-34e9-4f8f-8216-78d40b813199.md و task-47bfef80-d77a-4386-9dca-fb1a533ca20b.md و task-59cbc244-8dbe-4b9d-9a39-a3633ffd0464.md و task-645bbdc2-f205-479f-b01f-f8b8ad4ef2e4.md و task-6d989ea3-b525-4566-994a-ce401ae7373a.md و task-7599ae4a-1d12-4c87-80c2-b16e5876d9d4.md و task-8ac8249d-e1de-40f8-ad4b-36e271a1b237.md و task-9d4c545b-6683-46f1-8121-8ae6705d2122.md و task-adbe7499-a96f-4c38-8c32-e547a3b7e2c2.md و task-b3d7759b-2054-457f-a5ce-54dc346fb960.md و task-ba60df37-44e8-426e-8bf8-4a48cbedb11b.md و task-c7474d4f-8730-4531-b8ec-50de5a444b82.md و task-d13296fb-53a8-45a4-919b-de4f984a5a64.md در آرشیو موجودند اما محتوایشان deep-read نشده است. backend/package.json شامل وابستگی‌های express, cors, dotenv, ws, multer, fluent-ffmpeg, ffmpeg-static, pdf-parse است. frontend/package.json شامل react, firebase, lucide-react است. نیاز به ایجاد فایل‌های جدید backend/services/telegramService.js و backend/tests/test_telegramUpload.js و احتمالاً backend/models/Lesson.js یا استفاده از Firebase برای ذخیره‌سازی.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل backend/services/telegramService.js شامل کلاس TelegramFileHandler با متدهای handleFileUpload, validateFile, saveFileToStorage, associateWithLesson. 2. ایجاد فایل backend/tests/test_telegramUpload.js با تست‌های واحد: test_handleFileUpload_invalidFileType (فایل نامعتبر), test_handleFileUpload_lessonNotFound (درس ناموجود), test_handleFileUpload_success (آپلود موفق), test_handleFileUpload_mockFileDownload (mock برای file download), test_handleFileUpload_mockDatabase (mock برای دیتابیس). 3. استفاده از کتابخانه vitest (موجود در devDependencies فرانت‌اند) یا mocha/chai برای بک‌اند. 4. mock کردن توابع: downloadFile از Telegram API, findLessonById از دیتابیس, saveFile از سیستم فایل. 5. تست state_machine برای وضعیت‌های مختلف آپلود (دریافت, اعتبارسنجی, ذخیره, ارتباط با درس). 6. اطمینان از عدم وابستگی به AI در تست‌ها (خارج از scope). 7. اضافه کردن اسکریپت test به backend/package.json.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 17: نوشتن تست‌های واحد برای اتصال و تبادل پیام با استاد هوش مصنوعی

**Scope:** این مرحله شامل نوشتن تست‌های واحد برای توابعی است که با API استاد هوش مصنوعی ارتباط برقرار می‌کنند. باید تست شود که پیام به درستی ارسال می‌شود، پاسخ دریافت می‌شود، و خطاهای API مدیریت می‌شوند. خارج از این مرحله: تست‌های مربوط به صوت. نکته حیاتی: باید از mock برای API خارجی استفاده شود.
**Key terms:** unittest, mock, ai_api, send_message, response_handler

**بخش مربوط از متن کاربر:**
```
و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
نوشتن تست‌های واحد برای اتصال و تبادل پیام با استاد هوش مصنوعی

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:56-114` — `app.post('/api/gemini/chat', ...)` — Endpoint اصلی چت با هوش مصنوعی که باید تست‌های واحد برای آن نوشته شود. از fetch به API خارجی Gemini استفاده می‌کند.
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
  
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
  
      // Clean payload - preserve audio data if includeAudio flag is set
      if (payload.contents) {
        payload.contents = payload.contents.map(content => ({
          role: content.role,
          parts: content.parts.map(part => {
            // Keep inline_data (audio) if present
            if (part.inline_data && includeAudio) {
              return { inline_data: part.inline_data };
            }
            // Keep text if present
            if (part.text !== undefined) {
              return { text: part.text };
            }
            return null;
          }).filter(p => p !== null)
        }));
      }
  
      const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${GEMINI_API_KEY}`;
  
      const response = await fetch(apiUrl, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(payload)
      });
  
      if (!response.ok) {
        const errorData = await response.text();
        console.error('Gemini API error:', errorData);
        return res.status(response.status).json({ error: errorData });
      }
  
      const result = await response.json();
  
      // Return full result for audio requests, just text otherwise
      if (includeAudio) {
        res.json(result);
      } else {
        const text = result.candidates?.[0]?.content?.parts?.[0]?.text;
        if (!text) {
          return res.status(500).json({ error: 'No text in response' });
        }
        res.json({ text });
      }
    } catch (error) {
      console.error('Chat API error:', error);
      res.status(500).json({ error: error.message });
    }
  });
  ```
- `backend/server.js:273-294` — `async function analyzeWithGemini(parts, systemPrompt)`
  ```jsx
  async function analyzeWithGemini(parts, systemPrompt) {
    const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${GEMINI_API_KEY}`;
  
    const payload = {
      contents: [{ role: 'user', parts }],
      systemInstruction:
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست نوشتن تست‌های واحد برای توابعی که با API استاد هوش مصنوعی ارتباط برقرار می‌کنند را دارد. این تست‌ها باید بررسی کنند که پیام به درستی ارسال می‌شود، پاسخ دریافت می‌شود، و خطاهای API مدیریت می‌شوند. خارج از این مرحله: تست‌های مربوط به صوت. نکته حیاتی: باید از mock برای API خارجی استفاده شود. بخش مربوط از درخواست اصلی کاربر: 'و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم'. کلیدواژه‌ها: unittest, mock, ai_api, send_message, response_handler. در کد واقعی پروژه، endpoint اصلی برای چت با هوش مصنوعی در فایل backend/server.js در خطوط 56-114 (app.post('/api/gemini/chat', ...)) تعریف شده است. این endpoint از API خارجی Gemini (https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent) استفاده می‌کند. همچنین endpoint تحلیل فایل در خطوط 698-800 (app.post('/api/analyze-files', ...)) و توابع کمکی مانند analyzeWithGemini (خط 273) و uploadToGeminiFileAPI (خط 297) وجود دارند. در frontend، فایل frontend/src/App.jsx احتمالاً شامل کامپوننت‌های چت و فراخوانی این APIهاست. تست‌ها باید با استفاده از کتابخانه‌ای مانند vitest یا jest نوشته شوند و mock برای fetch یا ماژول‌های خارجی استفاده کنند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل backend/tests/test_gemini_chat.js با تست‌های واحد برای endpoint /api/gemini/chat. 2. mock کردن تابع fetch برای شبیه‌سازی پاسخ‌های API خارجی Gemini. 3. تست ارسال پیام موفق: بررسی status 200 و بازگشت متن پاسخ. 4. تست ارسال پیام با includeAudio=true: بررسی بازگشت کامل result شامل audio. 5. تست خطای API: شبیه‌سازی خطای 400/500 از Gemini و بررسی مدیریت خطا. 6. تست عدم وجود API key: بررسی status 500 و پیام خطای مناسب. 7. تست payload خالی یا ناقص: بررسی رفتار endpoint. 8. تست تابع analyzeWithGemini (خط 273) با mock کردن fetch. 9. تست تابع splitIntoChunks (خط 247) برای اطمینان از تقسیم‌بندی صحیح متن. 10. اطمینان از استفاده از mock برای تمام درخواست‌های خارجی (به‌ویژه https://generativelanguage.googleapis.com).

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 18: نوشتن تست‌های واحد برای قابلیت‌های صوتی (ASR و TTS)

**Scope:** این مرحله شامل نوشتن تست‌های واحد برای توابع تبدیل گفتار به متن (ASR) و متن به گفتار (TTS) است. باید تست شود که فایل صوتی به درستی پردازش می‌شود و خروجی مناسب تولید می‌شود. خارج از این مرحله: تست‌های integration با سرویس‌های خارجی. نکته حیاتی: باید از mock برای سرویس‌های ASR و TTS استفاده شود.
**Key terms:** unittest, mock, ASR, TTS, audio_processing

**بخش مربوط از متن کاربر:**
```
و از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه و پاسخ صوتی استاد و متن
```

## 🎯 هدف (خلاصه ساختاریافته)
نوشتن تست‌های واحد برای ASR و TTS صوتی

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:117-164` — `app.post('/api/gemini/tts')` — Endpoint TTS که باید با mock تست شود — خروجی audioData و mimeType را بررسی کند
  ```jsx
  app.post('/api/gemini/tts', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const { prompt, voice = 'Kore' } = req.body;
      const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-preview-tts:generateContent?key=${GEMINI_API_KEY}`;
      const payload = {
        contents: [{ parts: [{ text: prompt }] }],
        generationConfig: {
          responseModalities: ["AUDIO"],
          speechConfig: { 
            voiceConfig: { 
              prebuiltVoiceConfig: { voiceName: voice } 
            } 
          }
        }
      };
  ```
- `backend/server.js:273-294` — `analyzeWithGemini` — تابع تحلیل محتوای صوتی که باید با mock برای ASR تست شود
  ```jsx
  async function analyzeWithGemini(parts, systemPrompt) {
    const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${GEMINI_API_KEY}`;
    const payload = {
      contents: [{ role: 'user', parts }],
      systemInstruction: { parts: [{ text: systemPrompt }] }
    };
    const response = await fetch(apiUrl, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(payload)
    });
  ```
- `backend/server.js:297-392` — `uploadToGeminiFileAPI` — تابع آپلود فایل صوتی که باید با mock برای تست ASR شبیه‌سازی شود
  ```jsx
  async function uploadToGeminiFileAPI(filePathOrBuffer, mimeType, displayName, fileSize = null) {
    const isFilePath = typeof filePathOrBuffer === 'string';
    const actualSize = fileSize || (isFilePath ? fs.statSync(filePathOrBuffer).size : filePathOrBuffer.length);
    console.log(`Uploading file to Gemini File API: ${displayName}, size: ${(actualSize / 1024 / 1024).toFixed(2)} MB, from: ${isFilePath ? 'disk' : 'memory'}`);
    const startUploadUrl = `https://generativelanguage.googleapis.com/upload/v1beta/files?key=${GEMINI_API_KEY}`;
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/server.js` (سطر 1) — فایل اصلی شامل تمام توابع ASR/TTS و endpoint‌های مرتبط
- `frontend/src/App.jsx` — احتمالاً شامل کامپوننت‌های ضبط صدا و ارسال به backend — نیاز به mock در تست‌های frontend
- `backend/package.json`

## 🔍 Context و وضعیت فعلی
کاربر درخواست نوشتن تست‌های واحد برای قابلیت‌های صوتی (ASR و TTS) را دارد. متن خام کاربر: «نوشتن تست‌های واحد برای قابلیت‌های صوتی (ASR و TTS). این مرحله شامل نوشتن تست‌های واحد برای توابع تبدیل گفتار به متن (ASR) و متن به گفتار (TTS) است. باید تست شود که فایل صوتی به درستی پردازش می‌شود و خروجی مناسب تولید می‌شود. خارج از این مرحله: تست‌های integration با سرویس‌های خارجی. نکته حیاتی: باید از mock برای سرویس‌های ASR و TTS استفاده شود.» همچنین کاربر اشاره کرده: «و از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه و پاسخ صوتی استاد و متن». کلیدواژه‌ها: unittest, mock, ASR, TTS, audio_processing. در کد واقعی پروژه (backend/server.js)، توابع مرتبط با صدا شامل: 1) endpoint /api/gemini/tts (خطوط 117-164) که TTS را با مدل gemini-2.5-flash-preview-tts انجام می‌دهد و خروجی audioData و mimeType برمی‌گرداند. 2) تابع analyzeWithGemini (خطوط 273-294) که برای تحلیل محتوای صوتی استفاده می‌شود. 3) تابع uploadToGeminiFileAPI (خطوط 297-392) که فایل‌های صوتی بزرگ را به Gemini File API آپلود می‌کند. 4) پردازش فایل‌های صوتی در endpoint /api/analyze-files (خطوط 790-800) که فایل‌های صوتی را با استفاده از Gemini File API تحلیل می‌کند. همچنین در frontend/src/App.jsx (که deep-read نشده) احتمالاً کامپوننت‌های ضبط صدا و پخش پاسخ صوتی وجود دارد. تست‌ها باید با mock برای سرویس‌های خارجی (Gemini API) نوشته شوند و integration tests خارج از scope این تسک هستند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل backend/tests/test_audio.py برای تست‌های واحد ASR و TTS. 2. mock کردن تابع fetch برای شبیه‌سازی پاسخ‌های Gemini API در endpoint /api/gemini/tts (خطوط 117-164 backend/server.js). 3. mock کردن تابع uploadToGeminiFileAPI (خطوط 297-392) برای شبیه‌سازی آپلود فایل صوتی. 4. mock کردن تابع analyzeWithGemini (خطوط 273-294) برای شبیه‌سازی تحلیل صوتی. 5. تست پردازش فایل صوتی در endpoint /api/analyze-files (خطوط 698-800) با mock کردن سرویس‌های خارجی. 6. تست تولید خروجی مناسب (audioData و mimeType) از TTS endpoint. 7. تست خطاهای سرویس‌های خارجی (API key not configured, network error). 8. استفاده از کتابخانه vitest (متناسب با stack Vite) یا jest برای اجرای تست‌ها.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 19: انجام تست‌های یکپارچه‌سازی (Integration) برای سناریوی کامل تلگرام

**Scope:** این مرحله شامل نوشتن و اجرای تست‌های یکپارچه‌سازی است که کل جریان کار را از دریافت پیام در تلگرام تا پردازش و پاسخ را پوشش می‌دهد. باید از یک ربات تست یا شبیه‌ساز استفاده شود. خارج از این مرحله: تست‌های واحد. نکته حیاتی: این تست‌ها باید در محیط staging اجرا شوند و با دیتابیس واقعی کار کنند.
**Key terms:** integration_test, end_to_end, staging, bot_simulator, full_flow

**بخش مربوط از متن کاربر:**
```
تمام درخواست کاربر از نوتیفیکیشن تا AI صوتی
```

## 🎯 هدف (خلاصه ساختاریافته)
تست‌های یکپارچه‌سازی سناریوی کامل تلگرام

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:45-46` — `WebSocketServer` — WebSocket موجود برای live connection — می‌توان از همین infra برای ارتباط دوطرفه تلگرام استفاده کرد یا route مجزا ایجاد کرد
  ```jsx
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/server.js:56-114` — `app.post('/api/gemini/chat')` — این endpoint الگوی مناسبی برای ساختار webhook تلگرام است — دریافت payload، اعتبارسنجی، پردازش با Gemini، بازگشت پاسخ
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
      // ... Gemini API call
    } catch (error) {
      console.error('Chat API error:', error);
      res.status(500).json({ error: error.message });
    }
  });
  ```
- `backend/server.js:1-14` — `imports` — محل افزودن import برای telegram bot library — مثلاً import TelegramBot from 'node-telegram-bot-api'
  ```jsx
  import express from 'express';
  import cors from 'cors';
  import { fileURLToPath } from 'url';
  import { dirname, join } from 'path';
  import dotenv from 'dotenv';
  import { createServer } from 'http';
  import { WebSocketServer, WebSocket } from 'ws';
  import multer from 'multer';
  import fs from 'fs';
  import os from 'os';
  import ffmpeg from 'fluent-ffmpeg';
  import ffmpegStatic from 'ffmpeg-static';
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: JavaScript (Node.js/Express backend, React/Vite frontend). کتابخانه‌های موجود: express, cors, ws, multer, dotenv, fluent-ffmpeg. کتابخانه‌های پیشنهادی برای

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `frontend/src/App.jsx` (سطر 1) — UI اصلی که احتمالاً نیاز به نمایش وضعیت اتصال تلگرام یا نوتیفیکیشن‌ها دارد
- `backend/.env.example` (سطر 1) — نیاز به افزودن متغیرهای محیطی TELEGRAM_BOT_TOKEN و TELEGRAM_WEBHOOK_URL
- `backend/package.json` (سطر 1) — نیاز به افزودن وابستگی node-telegram-bot-api یا axios به dependencies
- `prompt/archive/task-dc721af2-f84f-424d-a491-91e8ab3624c0.md` (سطر 1) — تسک قبلی مرتبط با نوتیفیکیشن تلگرام که context این feature را مشخص می‌کند
- `.github/workflows/claude-auto-task.yml` (سطر 1) — CI/CD pipeline که باید تست‌های یکپارچه‌سازی را در محیط staging اجرا کند

## 🌐 نقشهٔ وابستگی‌ها
این feature به فایل‌های backend/server.js (برای افزودن route webhook و import کتابخانه تلگرام)، backend/.env.example (برای متغیرهای محیطی جدید)، backend/package.json (برای وابستگی‌های جدید)، frontend/src/App.jsx (برای نمایش وضعیت در UI)، و فایل‌های جدید backend/services/telegramService.js و backend/tests/integration/telegramFlow.test.js وابسته است. همچنین به سرویس Gemini API (خط 56-114 server.js) برای پردازش پیام‌ها و به WebSocket موجود (خط 45 server.js) برای ارتباط دوطرفه وابسته است. تست‌ها به دیتابیس واقعی در محیط staging نیاز دارند.

## 🔍 Context و وضعیت فعلی
کاربر درخواست انجام تست‌های یکپارچه‌سازی (Integration) برای سناریوی کامل تلگرام را داده است. این مرحله شامل نوشتن و اجرای تست‌های یکپارچه‌سازی است که کل جریان کار را از دریافت پیام در تلگرام تا پردازش و پاسخ را پوشش می‌دهد. باید از یک ربات تست یا شبیه‌ساز استفاده شود. خارج از این مرحله: تست‌های واحد. نکته حیاتی: این تست‌ها باید در محیط staging اجرا شوند و با دیتابیس واقعی کار کنند. کلیدواژه‌های کاربر: integration_test, end_to_end, staging, bot_simulator, full_flow. با توجه به commit dad79e7 که اشاره دارد 'میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار'، این تسک ادامه همان feature است. در کد فعلی (backend/server.js) هیچ endpoint یا WebSocket handler مرتبط با تلگرام وجود ندارد. فایل‌های مرتبط با تلگرام در deep context موجود نیستند و باید ایجاد شوند. ساختار پروژه شامل backend/server.js با WebSocket در path '/ws/live' (خط 45) و چندین API endpoint برای Gemini است. فایل frontend/src/App.jsx نیز deep-read شده اما محتوای آن نشان‌دهنده UI اصلی اپلیکیشن است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/services/telegramService.js برای مدیریت Webhook و ارسال/دریافت پیام‌های تلگرام با استفاده از کتابخانه node-telegram-bot-api یا axios. 2. ایجاد فایل جدید backend/tests/integration/telegramFlow.test.js برای تست‌های یکپارچه‌سازی. 3. در فایل backend/server.js، یک route جدید POST /api/telegram/webhook اضافه شود (مشابه ساختار route های موجود مثل خط 56-114 برای /api/gemini/chat). 4. ایجاد فایل backend/config/telegram.js برای تنظیمات ربات تلگرام (token, webhook URL). 5. ایجاد فایل backend/tests/staging/setup.js برای پیکربندی محیط staging با دیتابیس واقعی. 6. استفاده از شبیه‌ساز ربات (مثلاً nock یا sinon) برای mock کردن API تلگرام در تست‌ها. 7. تست‌ها باید شامل: دریافت پیام متنی از تلگرام، پردازش با Gemini API (خط 56-114 server.js)، ارسال پاسخ به تلگرام، و مدیریت خطاها باشند.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 20: مستندسازی و راهنمای کاربر برای قابلیت‌های تلگرام

**Scope:** این مرحله شامل نوشتن مستندات کامل برای کاربر نهایی است که نحوه استفاده از تمام قابلیت‌های تلگرام (نوتیفیکیشن، کنترل، دروس، AI) را توضیح می‌دهد. همچنین شامل مستندات فنی برای توسعه‌دهندگان (نحوه اضافه کردن دستور جدید، معماری). خارج از این مرحله: کدنویسی. نکته حیاتی: مستندات باید به دو زبان فارسی (برای کاربر) و انگلیسی (برای توسعه‌دهنده) باشد.
**Key terms:** README, documentation, user_guide, developer_guide, commands_list

**بخش مربوط از متن کاربر:**
```
تمام درخواست کاربر
```

## 🎯 هدف (خلاصه ساختاریافته)
مستندسازی قابلیت‌های تلگرام برای کاربر و توسعه‌دهنده

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:1-53` — `WebSocket and Express setup` — این بخش از کد برای مستندسازی معماری WebSocket و Express که پایه قابلیت‌های تلگرام است، استفاده می‌شود. WebSocket در خط 45 با path '/ws/live' راه‌اندازی شده.
  ```jsx
  import express from 'express';
  import cors from 'cors';
  import { fileURLToPath } from 'url';
  import { dirname, join } from 'path';
  import dotenv from 'dotenv';
  import { createServer } from 'http';
  import { WebSocketServer, WebSocket } from 'ws';
  ```
- `frontend/index.html:1-208` — `Firebase config and Inspector Bridge` — این فایل حاوی Firebase configuration (خطوط 11-18) و Inspector Bridge script (خطوط 32-201) است که برای مستندسازی نحوه اتصال به سرویس‌های خارجی و مانیتورینگ مفید است.
  ```
  <!DOCTYPE html>
  <html lang="fa" dir="rtl">
    <head>
      <meta charset="UTF-8" />
      <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
      <meta name="viewport" content="width=device-width, initial-scale=1.0" />
      <meta name="description" content="اپلیکیشن آموزش لهجه لبنانی با هوش مصنوعی" />
      <title>آموزش لهجه لبنانی</title>
  ```
- `backend/.env.example:1-8` — `Environment variables template`
  ```
  # Gemini API Key - Get from https://aist
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
این تسک شامل نوشتن مستندات کامل برای کاربر نهایی است که نحوه استفاده از تمام قابلیت‌های تلگرام (نوتیفیکیشن، کنترل، دروس، AI) را توضیح می‌دهد. همچنین شامل مستندات فنی برای توسعه‌دهندگان (نحوه اضافه کردن دستور جدید، معماری) می‌باشد. خارج از این مرحله: کدنویسی. نکته حیاتی: مستندات باید به دو زبان فارسی (برای کاربر) و انگلیسی (برای توسعه‌دهنده) باشد. کلیدواژه‌های اصلی: README, documentation, user_guide, developer_guide, commands_list. بر اساس commit اخیر (dad79e7) که اشاره دارد 'میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار'، این تسک مستندسازی همان قابلیت‌های تلگرام است که قبلاً پیاده‌سازی شده یا در حال پیاده‌سازی است. فایل‌های مرتبط در پروژه شامل backend/server.js (خطوط 1-53 برای WebSocket و Express setup)، frontend/index.html (خطوط 1-208 برای Firebase config و Inspector Bridge)، و فایل‌های task در prompt/archive/ می‌باشند. همچنین فایل‌های task-141249ee-0d23-44e0-aeec-21f989820b60.md و task-2a0673ad-13dd-4131-9c72-18a093d60686.md و task-2d1f95e8-b5ea-413f-b1ec-11b7bed6eeb7.md و task-3336cbc2-34e9-4f8f-8216-78d40b813199.md و task-38433cce-3e63-4851-89bc-4320f55f4d19.md و task-47bfef80-d77a-4386-9dca-fb1a533ca20b.md و task-59cbc244-8dbe-4b9d-9a39-a3633ffd0464.md و task-5bd04995-7545-4582-acb9-20c453d383a9.md و task-6225900f-6226-487c-9ae1-55dbd114e165.md و task-645bbdc2-f205-479f-b01f-f8b8ad4ef2e4.md و task-6d989ea3-b525-4566-994a-ce401ae7373a.md و task-7599ae4a-1d12-4c87-80c2-b16e5876d9d4.md و task-8ac8249d-e1de-40f8-ad4b-36e271a1b237.md و task-9d4c545b-6683-46f1-8121-8ae6705d2122.md و task-adbe7499-a96f-4c38-8c32-e547a3b7e2c2.md و task-b3d7759b-2054-457f-a5ce-54dc346fb960.md و task-ba60df37-44e8-426e-8bf8-4a48cbedb11b.md و task-c7474d4f-8730-4531-b8ec-50de5a444b82.md و task-d13296fb-53a8-45a4-919b-de4f984a5a64.md در prompt/archive/ موجود هستند که ممکن است حاوی جزئیات پیاده‌سازی تلگرام باشند. فایل‌های backend/.env.example و frontend/vite.config.js نیز برای مستندسازی نحوه راه‌اندازی محیط توسعه مفید هستند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل README.md در ریشه پروژه با دو بخش اصلی: (الف) راهنمای کاربر به زبان فارسی شامل نحوه اتصال ربات تلگرام، لیست دستورات (commands_list)، نحوه دریافت نوتیفیکیشن، کنترل از طریق تلگرام، دسترسی به دروس و AI. (ب) راهنمای توسعه‌دهنده به زبان انگلیسی شامل معماری سیستم تلگرام (بر اساس WebSocket در backend/server.js خط 45: const wss = new WebSocketServer({ server, path: '/ws/live' }) و Express endpoints در خطوط 56-240)، نحوه اضافه کردن دستور جدید، ساختار فایل‌ها. 2. ایجاد فایل docs/TELEGRAM_USER_GUIDE.md با مستندات کامل کاربری به فارسی. 3. ایجاد فایل docs/TELEGRAM_DEV_GUIDE.md با مستندات فنی به انگلیسی. 4. به‌روزرسانی فایل frontend/index.html (خطوط 1-208) برای اضافه کردن لینک به مستندات در footer یا help section. 5. اطمینان از اینکه تمام کلیدواژه‌های README, documentation, user_guide, developer_guide, commands_list در مستندات پوشش داده شده‌اند.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: docs
- اولویت: medium
- تخمین زمان: small

---

## ✅ معیارهای پذیرش کلی (همهٔ مراحل)
- [ ] {'text': "تابع sendVoiceReply(chatId, text) در backend/server.js ایجاد شود و endpoint /api/gemini/tts را با voice='Kore' صدا بزند", 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['sendVoiceReply', '/api/gemini/tts', "voice: 'Kore'"], 'files_hint': ['backend/server.js']}}
- [ ] {'text': ''}

## Acceptance Criteria

1. تابع sendVoiceReply(chatId, text) در backend/server.js ایجاد شود و endpoint /api/gemini/tts را با voice='Kore' صدا بزند _(verify: static)_

## Task Steps

### Step 1: ایجاد ساختار پایه برای اتصال تلگرام دوطرفه (Bot API + Webhook)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل راه‌اندازی یک ربات تلگرام با استفاده از python-telegram-bot یا aiogram است. باید webhook یا polling فعال شود تا ربات بتواند پیام‌های کاربر را دریافت کند و پاسخ دهد. در این مرحله فقط اتصال اولیه برقرار می‌شود و هیچ قابلیت خاصی اضافه نمی‌شود. خارج از این مرحله: ارسال نوتیفیکیشن، کنترل سیستم، مدیریت دروس، اتصال به AI. نکته حیاتی: باید از کتابخانه‌ای استفاده شود که از async پشتیبانی کند چون پروژه احتمالاً async است.
**Excerpt:**
```
میخوام برای این پروژه هم نوتیفیکیشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

### Step 2: اضافه کردن قابلیت ارسال نوتیفیکیشن از سیستم به تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک سرویس یا تابع در بک‌اند است که بتواند پیام‌های نوتیفیکیشن را به کاربر از طریق ربات تلگرام ارسال کند. باید مشخص شود چه رویدادهایی باعث ارسال نوتیفیکیشن می‌شوند (مثلاً ثبت‌نام، تغییر وضعیت، اتمام فرآیند). خارج از این مرحله: دریافت پیام از کاربر و کنترل سیستم. نکته حیاتی: باید از chat_id کاربر استفاده شود که یا از دیتابیس خوانده شود یا در زمان راه‌اندازی ذخیره شود.
**Excerpt:**
```
میخوام برای این پروژه هم نوتیفیکیشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته
```

### Step 3: پیاده‌سازی دستورات کنترلی پایه از طریق تلگرام (مثلاً /status, /help)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل تعریف و پیاده‌سازی چند دستور پایه در ربات تلگرام است که کاربر بتواند از طریق آنها اطلاعاتی از سیستم دریافت کند. مثلاً دستور /status برای نمایش وضعیت کلی سیستم، /help برای راهنما. خارج از این مرحله: دستورات پیچیده مانند مدیریت دروس یا اتصال به AI. نکته حیاتی: دستورات باید با استفاده از decoratorهای کتابخانه تلگرام تعریف شوند و پاسخ‌ها از دیتابیس یا API داخلی گرفته شود.
**Excerpt:**
```
بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

### Step 4: اضافه کردن قابلیت مشاهده لیست دروس از طریق تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک دستور تلگرامی (مثلاً /courses) است که لیست دروس موجود در سیستم را به صورت خلاصه نمایش دهد. اطلاعات باید از دیتابیس یا API مربوط به دروس خوانده شود. خارج از این مرحله: آپلود محتوا، مشاهده جزئیات درس، مدیریت تمرین با AI. نکته حیاتی: پاسخ باید به صورت فرمت‌شده (مثلاً با Markdown) باشد تا خوانایی داشته باشد.
**Excerpt:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

### Step 5: اضافه کردن قابلیت مشاهده جزئیات یک درس خاص از طریق تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد دستوری مانند /course <id> است که جزئیات کامل یک درس (شامل توضیحات، لیست جلسات، فایل‌های مرتبط) را نمایش دهد. اطلاعات باید از API مربوطه گرفته شود. خارج از این مرحله: آپلود فایل، دانلود فایل، مدیریت تمرین با AI. نکته حیاتی: اگر درس فایل دارد، باید لینک دانلود یا پیش‌نمایش ارائه شود.
**Excerpt:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

### Step 6: اضافه کردن قابلیت آپلود فایل (محتوای دروس) از طریق تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی قابلیت دریافت فایل (PDF، تصویر، ویدئو) از طریق تلگرام و ذخیره آن در سیستم (مثلاً در پوشه مخصوص درس) است. کاربر باید بتواند فایل را برای ربات بفرستد و ربات آن را در درس مشخص‌شده ذخیره کند. خارج از این مرحله: مدیریت تمرین با AI، ویرایش فایل. نکته حیاتی: باید از chat_id و state machine استفاده شود تا کاربر مشخص کند فایل مربوط به کدام درس است.
**Excerpt:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

### Step 7: ایجاد اتصال اولیه به بخش 'تمرین با استاد هوش مصنوعی' از طریق تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل برقراری ارتباط بین ربات تلگرام و API یا سرویس داخلی 'تمرین با استاد هوش مصنوعی' است. باید بتوان یک session جدید شروع کرد و موضوعات موجود را دریافت کرد. خارج از این مرحله: ارسال پیام صوتی، دریافت پاسخ صوتی، مدیریت کامل. نکته حیاتی: باید از API داخلی پروژه استفاده شود و احراز هویت کاربر در تلگرام با حساب کاربری در سیستم تطبیق داده شود.
**Excerpt:**
```
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم
```

### Step 8: پیاده‌سازی تبادل پیام متنی با استاد هوش مصنوعی از طریق تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ارسال پیام متنی کاربر از تلگرام به AI و دریافت پاسخ متنی از AI و نمایش آن در تلگرام است. باید از API داخلی AI برای ارسال و دریافت استفاده شود. خارج از این مرحله: پیام صوتی، مدیریت session. نکته حیاتی: باید history مکالمه حفظ شود تا AI بتواند context را بفهمد.
**Excerpt:**
```
و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم
```

### Step 9: اضافه کردن قابلیت ارسال پیام صوتی به استاد هوش مصنوعی از طریق تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل دریافت فایل صوتی از کاربر در تلگرام، ارسال آن به سرویس تشخیص گفتار (ASR) داخلی پروژه، ارسال متن استخراج‌شده به AI، و برگرداندن پاسخ به کاربر است. خارج از این مرحله: دریافت پاسخ صوتی از AI. نکته حیاتی: باید از API تشخیص گفتار موجود در پروژه استفاده شود و فایل صوتی قبل از ارسال به API ذخیره موقت شود.
**Excerpt:**
```
و از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه
```

### Step 10: اضافه کردن قابلیت دریافت پاسخ صوتی از استاد هوش مصنوعی در تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل تبدیل پاسخ متنی AI به گفتار (TTS) با استفاده از سرویس موجود در پروژه و ارسال فایل صوتی حاصل به کاربر در تلگرام است. خارج از این مرحله: مدیریت session، ارسال همزمان متن و صوت. نکته حیاتی: باید از همان سرویس TTS که در وب استفاده می‌شود استفاده شود تا کیفیت یکسان باشد.
**Excerpt:**
```
و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....
```

### Step 11: همگام‌سازی کامل چت تلگرام با چت وب (تاریخچه و وضعیت)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل اطمینان از این است که پیام‌های ردوبدل‌شده در تلگرام در تاریخچه چت وب نیز ذخیره شوند و بالعکس. همچنین وضعیت session (فعال، بسته) در هر دو پلتفرم یکسان باشد. خارج از این مرحله: قابلیت‌های جدید. نکته حیاتی: باید از یک دیتابیس مشترک برای ذخیره پیام‌ها استفاده شود و timestampها هماهنگ باشند.
**Excerpt:**
```
و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....
```

### Step 12: اضافه کردن قابلیت مدیریت موضوعات تمرین AI از طریق تلگرام (ایجاد، ویرایش، حذف)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد دستورات تلگرامی برای مدیریت موضوعات تمرین AI است. کاربر بتواند موضوع جدید ایجاد کند (/topic_new)، موضوع موجود را ویرایش کند (/topic_edit <id> <text>)، و موضوع را حذف کند (/topic_delete <id>). خارج از این مرحله: خود تمرین با AI. نکته حیاتی: باید احراز هویت و سطح دسترسی کاربر بررسی شود (فقط مدیر یا استاد).
**Excerpt:**
```
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم
```

### Step 13: اضافه کردن قابلیت انتخاب موضوع تمرین AI از طریق تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک دستور یا منوی تعاملی (Inline Keyboard) در تلگرام است که کاربر بتواند از بین موضوعات موجود یکی را برای تمرین انتخاب کند. پس از انتخاب، session AI با آن موضوع شروع شود. خارج از این مرحله: مدیریت موضوعات. نکته حیاتی: باید از CallbackQueryHandler برای مدیریت انتخاب‌ها استفاده شود.
**Excerpt:**
```
و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم
```

### Step 14: نوشتن تست‌های واحد برای سرویس نوتیفیکیشن تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل نوشتن تست‌های واحد برای توابع ارسال پیام به تلگرام است. باید تست شود که پیام به درستی ساخته می‌شود، خطاهای API مدیریت می‌شوند، و chat_id معتبر بررسی می‌شود. خارج از این مرحله: تست‌های integration با خود تلگرام. نکته حیاتی: باید از mock برای API تلگرام استفاده شود.
**Excerpt:**
```
میخوام برای این پروژه هم نوتیفیکیشن از طریق تلگرام فراهم بشه
```

### Step 15: نوشتن تست‌های واحد برای دستورات کنترلی تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل نوشتن تست‌های واحد برای هندلرهای دستورات تلگرام (مانند /status, /help, /courses) است. باید تست شود که دستورات به درستی پردازش می‌شوند و پاسخ مناسب تولید می‌شود. خارج از این مرحله: تست‌های مربوط به AI. نکته حیاتی: باید از Mock برای دیتابیس و API استفاده شود.
**Excerpt:**
```
بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

### Step 16: نوشتن تست‌های واحد برای قابلیت آپلود فایل از طریق تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل نوشتن تست‌های واحد برای فرآیند دریافت فایل از تلگرام، ذخیره‌سازی و ارتباط با درس مربوطه است. باید سناریوهای مختلف مانند فایل نامعتبر، درس ناموجود، و آپلود موفق تست شود. خارج از این مرحله: تست‌های مربوط به AI. نکته حیاتی: باید از mock برای file download و دیتابیس استفاده شود.
**Excerpt:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم
```

### Step 17: نوشتن تست‌های واحد برای اتصال و تبادل پیام با استاد هوش مصنوعی
**Status:** `pending` (0%)
**Scope:** این مرحله شامل نوشتن تست‌های واحد برای توابعی است که با API استاد هوش مصنوعی ارتباط برقرار می‌کنند. باید تست شود که پیام به درستی ارسال می‌شود، پاسخ دریافت می‌شود، و خطاهای API مدیریت می‌شوند. خارج از این مرحله: تست‌های مربوط به صوت. نکته حیاتی: باید از mock برای API خارجی استفاده شود.
**Excerpt:**
```
و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم
```

### Step 18: نوشتن تست‌های واحد برای قابلیت‌های صوتی (ASR و TTS)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل نوشتن تست‌های واحد برای توابع تبدیل گفتار به متن (ASR) و متن به گفتار (TTS) است. باید تست شود که فایل صوتی به درستی پردازش می‌شود و خروجی مناسب تولید می‌شود. خارج از این مرحله: تست‌های integration با سرویس‌های خارجی. نکته حیاتی: باید از mock برای سرویس‌های ASR و TTS استفاده شود.
**Excerpt:**
```
و از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه و پاسخ صوتی استاد و متن
```

### Step 19: انجام تست‌های یکپارچه‌سازی (Integration) برای سناریوی کامل تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل نوشتن و اجرای تست‌های یکپارچه‌سازی است که کل جریان کار را از دریافت پیام در تلگرام تا پردازش و پاسخ را پوشش می‌دهد. باید از یک ربات تست یا شبیه‌ساز استفاده شود. خارج از این مرحله: تست‌های واحد. نکته حیاتی: این تست‌ها باید در محیط staging اجرا شوند و با دیتابیس واقعی کار کنند.
**Excerpt:**
```
تمام درخواست کاربر از نوتیفیکیشن تا AI صوتی
```

### Step 20: مستندسازی و راهنمای کاربر برای قابلیت‌های تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل نوشتن مستندات کامل برای کاربر نهایی است که نحوه استفاده از تمام قابلیت‌های تلگرام (نوتیفیکیشن، کنترل، دروس، AI) را توضیح می‌دهد. همچنین شامل مستندات فنی برای توسعه‌دهندگان (نحوه اضافه کردن دستور جدید، معماری). خارج از این مرحله: کدنویسی. نکته حیاتی: مستندات باید به دو زبان فارسی (برای کاربر) و انگلیسی (برای توسعه‌دهنده) باشد.
**Excerpt:**
```
تمام درخواست کاربر
```
