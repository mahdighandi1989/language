---
task_id: dc721af2-f84f-424d-a491-91e8ab3624c0
title: میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار (6 مرحله)
type: other
priority: medium
execution_priority: 3000
status: pending
external_status: pending
verification_status: pending
watched_id: 6b04f8bd-b562-4fb9-9cd9-0c27458ced59
project: mahdighandi1989/language
created_at: '2026-05-12T11:13:22.960230+00:00'
updated_at: '2026-06-02T06:25:25.592004+00:00'
target_files:
- backend/server.js
- backend/.env.example
- frontend/index.html
---

# میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار (6 مرحله)

## Raw Idea

میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم و از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

📖 **خواندن کامل + اجرای مو-به-مو (بسیار مهم):**

این پرامپت — از این یادداشت تا انتها — یک سند واحد است که هر بخشش
حاوی الزام یا context منحصربه‌فرد است. خواندن سطحی یا skim کردن **ممنوع**
است.

- پرامپت را **سطر به سطر** بخوان، نه head/tail/فقط-بخش-اصلی.
- اگر بخشی به‌نظر طولانی یا تکراری آمد، **حتماً** بخوان — تفاوت‌های
  ریز ممکن است در آن جا اساسی باشند.
- هر جمله، URL، نام فایل، نام تابع، یا مقدار عددی که در پرامپت آمده،
  دقیقاً همان است که کاربر می‌خواهد — تغییرش نده، رندش نکن، خلاصه‌اش
  نکن.
- اگر پرامپت چندین درخواست/مرحله/زیرتسک دارد، **همه** را پیاده کن. حتی
  یکی را نه به‌عنوان "خارج از scope" حذف کن.

❌ ممنوعات صریح:
- خلاصه‌سازی متن کاربر در commit message یا response
- "این بخش اصلی نیست، رد می‌کنم"
- "کاربر احتمالاً منظورش این بود..." — منظورش همان است که نوشته
- "این URL/نام به نظر قدیمی است، آپدیتش کردم" — تغییر بدون درخواست ممنوع
- پیاده‌سازی فقط بخشی از پرامپت و تظاهر به کامل بودن
- "همه آیتم‌های لیست A را بررسی کردم، B و C مشابه بودند" — نه؛
  هرکدام را جداگانه

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

🔗 **وابستگی‌ها و همگام‌سازی (بسیار حیاتی — هرگز skip نکن):**

این بخش از همهٔ بخش‌های دیگرِ این یادداشت **مهم‌تر** است. اگر نقض شود،
نتیجهٔ کار ممکن است مشروع به‌نظر برسد ولی در عمل بخش‌های دیگر سیستم را عقب
بیندازد، broken reference تولید کند، یا منجر به data corruption شود.

پیش از و حین تغییر، تمام وابستگی‌ها را در **چهار جهت** به‌طور **کامل و
بدون هیچ خلاصه‌سازی** شناسایی و همگام کن:

**۱. وابستگی‌های upstream (این تسک به چه چیزهایی متکی است):**
- چه فایل‌ها، توابع، کلاس‌ها، API endpoint ها، schema های دیتابیس،
  env vars، یا config هایی که این تسک نیاز دارد؟
- آیا قرار است چیزی را ویرایش/حذف کنی که جای دیگر (signature، رفتار،
  return type، side effect) از آن انتظار خاصی می‌رود؟
- اگر dependency جدیدی اضافه می‌کنی، آیا با dependencyهای موجود تداخل
  دارد (نسخه، compat، lock file)؟

**۲. وابستگی‌های downstream (چه چیزهایی به این تسک متکی‌اند):**
- چه فایل‌ها، توابع، تست‌ها، migrations، docs، یا UI component هایی از
  کدی که داری ویرایش/اضافه/حذف می‌کنی **استفاده می‌کنند**؟
- با grep و reference search **همه‌ی** call sites، importها، subclassها،
  reference های مستقیم و غیرمستقیم را پیدا کن — نه فقط چند مورد اصلی.
- خصوصاً برای حذف یا rename: هیچ broken reference نباید باقی بماند.

**۳. وابستگی‌های cross-tier (بسیار مهم — هرگز فقط یک لایه را نبین):**

تسک شما ممکن است از backend، frontend، database، worker، یا هر tier
دیگری شروع شده باشد. ولی تغییرات تقریباً همیشه روی tier های دیگر هم
اثر می‌گذارند. **مستقل از اینکه تسک از کدام tier است**، این چک‌های دو
طرفه را همیشه انجام بده:

🔁 **اگر backend را تغییر دادی** (API، service، model، route):
  → frontend: کدام component/page/hook این endpoint یا data shape را
    مصرف می‌کند؟ type definition، state shape، error handling، loading
    state، form validation، URL routing همگی باید همگام شوند.
  → mobile/SDK/client library (اگر پروژه دارد): همان داستان frontend.
  → database: آیا migration لازم است؟ آیا rollback امن است؟
  → background workers: آیا event producer/consumer ها تحت تأثیرند؟
  → rate limit، auth، CORS، CSP: آیا رفتار جدید پشتیبانی می‌شود؟

🔁 **اگر frontend را تغییر دادی** (component، form، state، route):
  → backend: آیا endpoint جدید/تغییریافته لازم است؟ آیا data shape ای
    که ارسال می‌شود با schema سرور سازگار است؟
  → backend validation: آیا برای ورودی‌های جدید UI کافی است؟
  → permissions/RBAC: آیا feature جدید نیاز به role check جدید دارد؟
  → analytics/tracking: آیا event های جدید باید در backend log شوند؟
  → SEO/SSR: آیا تغییر route نیاز به sitemap/meta tags جدید دارد؟

🔁 **اگر database/migration را تغییر دادی**:
  → backend models (ORM، Pydantic، dataclasses) همگی به‌روزند؟
  → query های raw SQL یا ORM queries با schema جدید سازگارند؟
  → seed data، fixtures، factory functions تست‌ها به‌روزند؟
  → frontend: آیا data shape جدید در UI به‌درستی render می‌شود؟
  → rollback migration نوشته شده و امن است؟

🔁 **اگر API contract یا event schema را تغییر دادی** (REST، GraphQL،
   WebSocket، gRPC، Kafka، …):
  → OpenAPI/GraphQL schema/proto file آپدیت شد؟
  → همه‌ی consumer ها (client، subscriber، webhook، external API
    user) با version جدید سازگارند؟
  → backward compatibility حفظ شده یا migration path روشن است؟
  → versioning header/path اگر breaking change است؟

🔁 **اگر infrastructure یا config را تغییر دادی** (Dockerfile، CI، Render
   config، env، secrets):
  → README setup/installation section به‌روزه؟
  → `.env.example` با env vars جدید آپدیت شد؟
  → deploy script یا CI workflow هم تغییر کرد؟
  → docs/architecture یا diagram های infrastructure به‌روزند؟

⚠️ **هرگز فقط یک tier را تغییر نده و فرض کنی بقیه خودکار همگام می‌شوند.**
   حتی برای تغییرات به‌ظاهر «کوچک»، چک کن.

**۴. وابستگی‌های جانبی (artifacts که همیشه چک شوند):**

تغییرات کد همیشه روی این artifact ها اثر دارند. **همه را** بررسی و
به‌روز کن — مستندات اولویت **بالا** دارد چون فراموش‌شدنی‌ترین است.

  📝 **مستندات** (همیشه چک کن — حتی برای تغییر کوچک کد):
    - README.md (شرح، setup، نمونه‌های استفاده، badge ها)
    - CHANGELOG.md / RELEASE_NOTES.md
    - docs/ folder (architecture، API reference، user guides، runbooks)
    - inline docstrings/کامنت‌های توابع و کلاس‌های تغییریافته
    - OpenAPI/Swagger annotations، JSDoc/TSDoc
    - architecture diagrams (اگر component اضافه/حذف شد)
    - migration guides (اگر breaking change است)

  🌍 **مستندات کاربر**:
    - i18n files و translation keys
    - UI labels، tooltip ها، help text، error messages
    - in-app onboarding (اگر flow جدید است)

  🧪 **تست‌ها**:
    - unit tests (همه‌ی فایل‌های مرتبط — حتی اگر «بی‌ربط» به‌نظر می‌رسد)
    - integration tests
    - e2e tests (Playwright/Cypress/Selenium)
    - snapshot tests (اگر UI تغییر کرد)
    - contract tests (Pact یا مشابه)
    - performance benchmarks (اگر behavior performance-sensitive تغییر کرد)

  🧬 **type definitions و contracts**:
    - .d.ts files
    - Pydantic models، dataclasses
    - Protobuf/Avro/Thrift schemas
    - GraphQL schema definitions
    - JSON Schemas

  🏗 **infrastructure و config**:
    - Dockerfile، docker-compose.yml
    - Kubernetes manifests
    - Render/Vercel/Netlify config
    - GitHub Actions / GitLab CI workflows
    - environment templates (.env.example، .env.sample)
    - feature flags (LaunchDarkly، GrowthBook، config)

  📊 **monitoring و observability**:
    - logging keys (اگر اضافه/حذف شد، log parser ها هم به‌روز شوند)
    - metric names (Prometheus، Datadog)
    - tracing spans
    - alert rules و dashboards
    - error tracking (Sentry rules، groupings)

  🔐 **security**:
    - auth rules (rate limit، CORS، CSP، HSTS)
    - permissions/RBAC config
    - secrets rotation policies
    - audit log events (اگر action جدید اضافه شد)

  💾 **caches و serialization**:
    - cache keys و TTL (اگر data shape یا lifecycle تغییر کرد)
    - serializer formats (Redis، session storage)
    - browser storage (localStorage، IndexedDB schemas)

**قانون مطلق همگام‌سازی:**
- هر چیزی که در (۱)، (۲)، (۳)، یا (۴) شناسایی شد، در **همان workflow
  این تسک** همگام و به‌روز شود. هرگز برای بعد رها نکن.
- اگر یک فایل/تست/docs نسبت به تغییر شما عقب بماند، در بهترین حالت bug،
  در بدترین حالت مشکل امنیتی یا data corruption تولید می‌کند.
- تغییرات همگام‌سازی می‌توانند در commit جداگانه باشند (در همان task)،
  ولی نباید skip شوند یا به «refactor آینده» سپرده شوند.

**هرگز این جمله‌ها قابل قبول نیست:**
- ❌ «بعداً پیداش می‌کنم»
- ❌ «احتمالاً جای دیگه‌ای استفاده نمی‌شه»
- ❌ «این یه refactor جداگانه‌ست — out of scope»
- ❌ «فقط فایل‌های اصلی رو بررسی کردم»
- ❌ «حدس می‌زنم چیزی بهش وابسته نیست»
- ❌ «دامنه‌ی وابستگی‌ها رو خلاصه کردم» — هرگز خلاصه نکن
- ❌ «این task فقط backend است؛ frontend مشکل خودش» — هرگز
- ❌ «این task فقط frontend است؛ backend از قبل کار می‌کند» — هرگز ثابت نکرده
- ❌ «مستندات بعداً به‌روز می‌شن» — همیشه same-task همگام شوند
- ❌ «testها رو نگاه نکردم چون فقط یه تغییر کوچیک بود»

**در commit message یا PR description**، دامنهٔ وابستگی‌های شناسایی‌شده و
همگام‌شده را به‌طور explicit و **per-tier** بنویس. مثال:
```
Dependencies synced:
- upstream: User model schema, auth middleware
- downstream: 3 API endpoints, 5 frontend components, 12 tests
- cross-tier (backend → frontend): UserProfile.tsx, useUser.ts hook,
  api-types.ts (TS definitions)
- cross-tier (backend → infra): .env.example added NEW_AUTH_SCOPES
- side artifacts: OpenAPI spec, README API section, i18n keys for
  new errors, Sentry alert rule for new error code
```
اگر هیچ وابستگی پیدا نکردی در هر کدام از چهار جهت، صریحاً بنویس:
«بررسی شد — هیچ وابستگی upstream / downstream / cross-tier (backend↔
frontend↔db↔infra) / side شناسایی نشد» تا مشخص باشد بررسی **انجام شده**
نه اینکه فراموش شده.

📋 **مدیریت TO-DO برای اقدامات دستی کاربر (همیشه چک کن):**

⚠️ **هشدار بحرانی — قاعدهٔ ضد-فرار:** TO-DO فقط برای کارهایی است که
**واقعاً غیرممکن** برای agent است (نیاز به انسان مطلق)، نه برای کارهایی
که «بزرگ‌اند»، «وقت می‌برند»، یا «نیازمند fixture/setup» هستند. اگر یک
agent در یک سشن بیش از **۲۰٪ از تسک‌ها** را با TO-DO ببندد، یعنی از کار
فرار می‌کند — این الگو در سشن‌های قبلی **مشاهده** شده و الان ممنوع است.

✅ **فقط برای این موارد TO-DO بساز** (لیست بسته — هرچه خارج این لیست
ممنوع است):

  ۱. **Credential/secret که فقط کاربر دارد**:
     - تنظیم API key واقعی در پنل ادمین خارجی (Render، AWS، Stripe، …)
     - تأیید OAuth client روی console آن سرویس
     - paste کردن webhook secret که فقط بعد از ساخت در dashboard ظاهر می‌شود

  ۲. **Account/billing روی سرویس خارجی که کاربر باید عضو شود**:
     - ساخت account جدید روی Stripe/SendGrid/Twilio/Google Cloud
     - تأیید verification شماره یا ID
     - فعال‌سازی subscription پولی

  ۳. **داده/asset خصوصی که فقط کاربر دارد**:
     - آپلود لوگو/تصویر/فونت برند
     - paste کردن داده‌ای که در محل کار کاربر است
     - import داده‌ای که فقط روی device کاربر است

  ۴. **تصمیم سلیقه‌ای/حقوقی/کسب‌وکار**:
     - انتخاب رنگ‌بندی نهایی یا تم
     - متن دقیق Terms of Service / Privacy Policy
     - تعرفهٔ قیمت‌گذاری
     - نام نهایی برند یا دامنه

⛔ **هرگز TO-DO نکن برای** (لیست سیاه — هر چیزی که در این لیست است
**قابل اجرا** توسط agent است، حتی اگر بزرگ یا چندبخشی باشد):

  ❌ UI component / page / dashboard (هر فریم‌ورک: React, Vue, Angular,
     Svelte، حتی اگر معماری بزرگ دارد) — می‌توانی stub اولیه + state
     management + layout + استایل بسازی
  ❌ "نیازمند Google Drive / Stripe / Twilio API" — می‌توانی **client
     stub** با abstraction layer بسازی که با env var واقعی plug-in شود؛
     کد integration یعنی پیاده‌سازی، نه TO-DO
  ❌ "feature بزرگ، چند روز کار می‌برد" — اندازه دلیل defer نیست؛ کوچک
     شروع کن، iterate کن، در همین سشن کامل کن
  ❌ Celery / background worker / scheduler — یک task ساده + register
     می‌توانی بسازی
  ❌ Migration / model / schema — حتی اگر فیلد جدید نیاز دارد، اضافه کن
  ❌ REST endpoint / GraphQL resolver / WebSocket route — هرگز TO-DO
  ❌ test (unit/integration/e2e) — همیشه قابل نوشتن
  ❌ Documentation / README / API docs — همیشه قابل نوشتن
  ❌ Config file / .env.example / Dockerfile / CI workflow — همیشه قابل
     نوشتن
  ❌ "می‌توانستی .tsx ولی repo .jsx است" — از .jsx استفاده کن، TO-DO نکن
  ❌ "نیازمند فیلد X در مدل دیگر" — اضافه کن فیلد را، TO-DO نکن
  ❌ "تصمیم admin-vs-user-scoped" — پرامپت اولیه scope را معلوم کرده،
     یا با محتاطانه‌ترین تفسیر پیش برو
  ❌ "credential در production هنوز ست نیست" — این TO-DO ساده برای
     تنظیم env var است (مورد ۱ بالا)، نه دلیل برای defer کردن کد
  ❌ "نیازمند verification از کاربر" — اگر اقدام واقعی غیرممکن نیست،
     پیش برو
  ❌ هر چیزی که در یک کامنت `# TODO` معمولی نوشته می‌شد — این فایل
     TO-DO نیست، کامنت inline است

🔬 **قاعدهٔ «حداقل تلاش» قبل از TO-DO**: قبل از TO-DO کردن یک AC، **اثبات
کن** که قابل انجام نیست:

  ۱. آیا می‌توانم یک stub/placeholder بسازم که با env واقعی plug-in شود؟
     → اگر بله، بساز و TO-DO نکن
  ۲. آیا می‌توانم برای این بخش یک test (حتی mock-based) بنویسم؟
     → اگر بله، بنویس و TO-DO نکن
  ۳. آیا می‌توانم abstraction/interface را تعریف کنم، حتی اگر backend
     واقعی نیست؟ → اگر بله، تعریف کن و TO-DO نکن
  ۴. آیا فقط یک حالت سلیقه‌ای/decision کاربر در میان است؟
     → فقط آن یک decision را TO-DO کن، نه کل feature را

اگر یکی از این چهار راه‌حل ممکن بود ولی به TO-DO رفتی، **اعتبار شما از
بین می‌رود**.

📊 **آستانهٔ TO-DO per session**: در یک حلقهٔ اجرای N تسک، اگر بیشتر از
**۲۰٪** تسک‌ها فایل TO-DO ساختی، خودت در گزارش پایانی صریحاً اعلام کن:

  "⚠️ نسبت TO-DO من {K}/{N} = {%} است که از آستانهٔ ۲۰٪ بالاتر است.
   احتمالاً برخی از این TO-DO ها قابل اجرا بودند ولی من فرار کردم.
   لیست TO-DO ها را کاربر باید بازبینی کند که آیا واقعاً Manual-required
   بودند یا agent ضعیف کار کرده."

**یادآوری همیشگی:** اگر در آینده قابلیت‌های شما گسترش پیدا کرد و توانستید
یکی از موارد لیست سفید را خودکار انجام دهید (مثلاً managed credential
injection، یا integration پولی automate شود)، انجام دهید و TO-DO نسازید.
لیست سفید بسته است ولی **بسته از پایین** (می‌تواند کوچک‌تر شود اگر
قابلیت‌ها رشد کنند، ولی هرگز بزرگ‌تر نشود برای فرار).

**اگر هیچ بخش Manual-required نبود (تمام تسک Auto-capable است)**:
  → فایل TO-DO **نساز**. فولدر TO-DO/ باید پاک و معنادار بماند.
  → اگر برای این task از قبل `TO-DO/todo-task-{task_id_first_8}.md` بود
     (یعنی در run قبلی نیاز به دخالت کاربر بود ولی الان نه): فایل قدیمی
     را پاک کن و entry را از `TO-DO/_index.json` حذف کن.

**اگر بخش Manual-required دارد** (همه‌جانبه یا hybrid):
  1. فولدر TO-DO/ را در ریشه ریپو ایجاد کن اگر نیست
  2. فایل `TO-DO/todo-task-{task_id_first_8}.md` بساز با front-matter
     شامل: task_id, task_title, execution_priority, created_at,
     updated_at, status: "pending"
     و در بدنه: «چرا این فایل ساخته شد»، «وضعیت بخش‌های خودکار»
     (commit ها reference)، «کارهایی که باید انجام دهی» با اولویت
     بالا/متوسط/پایین به ترتیب، «وقتی این کارها را تمام کردی»
  3. `TO-DO/_index.json` را با **merge** آپدیت کن (نه overwrite):
     - فایل موجود را بخوان
     - entry های orphan (فایلشان پاک شده) را حذف کن
     - entry این task را اضافه/replace کن
     - بر اساس execution_priority صعودی مرتب کن
     - ساختار: `{"version":1, "generated_at": ISO, "total": N, "items": [...]}`
  4. این تغییرات TO-DO را در **همان commit کد** شامل کن (نه commit جداگانه)

⛔ **ممنوعات مطلق TO-DO**:
  ❌ ساختن TO-DO برای کاری که می‌توانستی خودت انجام دهی (شلوغی فولدر)
  ❌ overwrite کردن `TO-DO/_index.json` بدون merge (data loss)
  ❌ نگه‌داشتن entry هایی که فایل‌شان پاک شده (broken reference)
  ❌ فراموش کردن نوشتن «خروجی مورد انتظار» در هر آیتم TO-DO

این بخش الزامی است. حتی اگر فکر می‌کنی "این تسک کاملاً auto است و نیازی
به TO-DO نیست"، صریحاً در commit message یا report بنویس:
"بررسی شد — این تسک هیچ بخش Manual-required ندارد، TO-DO ساخته نشد."

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

🔁 **Commit + Push فوری per-task (بسیار مهم برای جریان کار صحیح):**

پس از اتمام پیاده‌سازی این تسک، **بلافاصله** commit کن و **همان موقع**
به default branch (main/master) push کن. سپس به تسک بعدی برو.

✓ چرا این قانون حیاتی است:
  - تسک‌های بعدی ممکن است به فایل‌ها/تغییراتی که این تسک ایجاد کرده
    نیاز داشته باشند. اگر push نکنی، `git pull` بعدی آن‌ها را نمی‌بیند.
  - جمع‌کردن تغییرات چند تسک منجر به conflict های بزرگ می‌شود.
  - اگر در میانه fail کنی، task های push شده ضایع نمی‌شوند.

⛔ ممنوع: "همه task ها را تمام می‌کنم بعد یک‌جا push می‌زنم"
⛔ ممنوع: branch جدا برای task — مستقیم به default branch
⛔ ممنوع: task بعدی بدون push کامل task قبلی

---


---

## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند.)_

```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم و از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....
```

## 📋 چک‌لیست مراحل (10 مرحله)

این تسک به مراحل کوچک‌تر تقسیم شده. **در هر verify خودکار، وضعیت هر مرحله به‌صورت `[ ]` (انجام نشده)، `[~]` (ناقص)، یا `[x]` (انجام شده) به‌روز می‌شود.**
وقتی تمام مراحل `[x]` شدند، تسک به‌طور خودکار به «انجام شده» منتقل می‌شود.

- [ ] **مرحله 1: ایجاد ساختار پایه برای نوتیفیکیشن تلگرام دوطرفه** — این مرحله شامل ایجاد یک ماژول پایه برای ارتباط با تلگرام (Bot API) است. باید یک کلاس یا سرویس اصلی برای ارسال و دریافت پیام‌ها از طریق تلگرام ساخته شود. این مرحله فقط شامل راه‌اندازی Webhook یا Polling برای دریافت پیام‌ها و ارسال پاسخ‌های ساده است. خارج از این مرحله: پردازش محتوای دروس، مدیریت تمرین
- [ ] **مرحله 2: پیاده‌سازی ارسال نوتیفیکیشن‌های سیستمی از طریق تلگرام** — این مرحله شامل اتصال ماژول تلگرام به رویدادهای سیستمی است. باید مشخص شود چه رویدادهایی (مثلاً اتمام یک پردازش، خطا، به‌روزرسانی وضعیت) باید نوتیفیکیشن تلگرامی دریافت کنند. یک سرویس Event Bus یا Observer Pattern برای انتشار رویدادها و ارسال خودکار پیام به تلگرام پیاده‌سازی می‌شود. خارج از این مرحله: 
- [ ] **مرحله 3: پیاده‌سازی دستورات کنترلی ساده از طریق تلگرام** — این مرحله شامل تعریف و پیاده‌سازی دستورات متنی ساده است که کاربر می‌تواند از طریق تلگرام به سیستم بفرستد. مثال: /status برای وضعیت سیستم، /restart برای راه‌اندازی مجدد یک سرویس، /help برای راهنما. این دستورات باید توسط هندلرهای مخصوص پردازش شده و عملیات مربوطه روی سیستم اجرا شود. خارج از این مرحله: 
- [ ] **مرحله 4: ایجاد ماژول مدیریت محتوای دروس از طریق تلگرام** — این مرحله شامل ایجاد یک ماژول جدید برای مدیریت محتوای دروس (مشاهده و آپلود) از طریق تلگرام است. باید قابلیت‌های زیر پیاده‌سازی شود: ۱) دریافت لیست دروس موجود، ۲) مشاهده جزئیات یک درس (متن، فایل‌های ضمیمه)، ۳) آپلود فایل (PDF، تصویر، ویدئو) به عنوان محتوای جدید برای یک درس. این ماژول باید با دیتابیس 
- [ ] **مرحله 5: اتصال ماژول تلگرام به بخش 'تمرین با استاد هوش مصنوعی'** — این مرحله شامل ایجاد یکپارچگی بین ربات تلگرام و بخش 'تمرین با استاد هوش مصنوعی' است. باید یک API endpoint یا WebSocket connection برای تبادل پیام‌ها بین تلگرام و چت هوش مصنوعی ایجاد شود. کاربر باید بتواند از طریق تلگرام موضوعات تمرین را مشاهده، انتخاب و مدیریت کند. خارج از این مرحله: پیاده‌سازی خود 
- [ ] **مرحله 6: پیاده‌سازی ارسال و دریافت پیام صوتی از طریق تلگرام برای استاد هوش مصنوعی** — این مرحله شامل قابلیت ارسال پیام صوتی از طریق تلگرام به استاد هوش مصنوعی است. کاربر باید بتواند یک پیام صوتی ضبط کرده و به ربات بفرستد. ربات باید فایل صوتی را دریافت کرده، به سرویس Speech-to-Text (STT) ارسال کند، متن استخراج شده را به هوش مصنوعی بفرستد، و پاسخ را به صورت متن و/یا صوتی (Text-to-Speec
- [ ] **مرحله 7: همگام‌سازی دوطرفه پیام‌ها بین چت وب و تلگرام** — این مرحله شامل همگام‌سازی پیام‌ها بین چت وب (بخش 'تمرین با استاد هوش مصنوعی') و تلگرام است. هر پیامی که در چت وب ارسال یا دریافت می‌شود باید در تلگرام نیز نمایش داده شود و بالعکس. باید از یک message queue یا WebSocket برای broadcast پیام‌ها استفاده شود. خارج از این مرحله: پردازش محتوای پیام‌ها. نکته
- [ ] **مرحله 8: پیاده‌سازی مدیریت موضوعات تمرین از طریق تلگرام** — این مرحله شامل قابلیت مدیریت کامل موضوعات تمرین (Topics) از طریق تلگرام است. کاربر باید بتواند: ۱) موضوعات جدید ایجاد کند، ۲) موضوعات موجود را ویرایش کند، ۳) موضوعات را حذف کند، ۴) ترتیب موضوعات را تغییر دهد. این عملیات باید از طریق دستورات تلگرام و با تأیید (confirmation) انجام شود. خارج از این مرح
- [ ] **مرحله 9: پیاده‌سازی انتخاب موضوع تمرین از طریق تلگرام** — این مرحله شامل قابلیت انتخاب یک موضوع تمرین توسط کاربر از طریق تلگرام است. کاربر باید بتواند از لیست موضوعات، یکی را انتخاب کرده و session تمرین را شروع کند. انتخاب باید از طریق دکمه‌های Inline Keyboard در تلگرام انجام شود. خارج از این مرحله: خود مکالمه با هوش مصنوعی. نکته حیاتی: باید وضعیت کاربر (s
- [ ] **مرحله 10: تست و دیباگ یکپارچه کل سیستم تلگرام** — این مرحله شامل تست کامل تمام قابلیت‌های پیاده‌سازی شده در مراحل قبل است. باید سناریوهای زیر تست شوند: ۱) ارسال و دریافت نوتیفیکیشن، ۲) اجرای دستورات کنترلی، ۳) آپلود و مشاهده محتوای دروس، ۴) مکالمه متنی با استاد هوش مصنوعی، ۵) ارسال و دریافت پیام صوتی، ۶) همگام‌سازی دوطرفه پیام‌ها، ۷) مدیریت موضوعات

---

# 🔹 مرحله 1: ایجاد ساختار پایه برای نوتیفیکیشن تلگرام دوطرفه

**Scope:** این مرحله شامل ایجاد یک ماژول پایه برای ارتباط با تلگرام (Bot API) است. باید یک کلاس یا سرویس اصلی برای ارسال و دریافت پیام‌ها از طریق تلگرام ساخته شود. این مرحله فقط شامل راه‌اندازی Webhook یا Polling برای دریافت پیام‌ها و ارسال پاسخ‌های ساده است. خارج از این مرحله: پردازش محتوای دروس، مدیریت تمرین با استاد هوش مصنوعی، یا هرگونه منطق تجاری خاص. نکته حیاتی: باید از کتابخانه‌ای مثل python-telegram-bot یا aiogram استفاده شود و Webhook به‌درستی پیکربندی شود.
**Key terms:** Telegram Bot API, Webhook, python-telegram-bot, aiogram, Bot Token, Message Handler

**بخش مربوط از متن کاربر:**
```
میخوام برای این پروژه هم نوتیفیکیشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد ماژول پایه نوتیفیکیشن تلگرام دوطرفه

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:1-14` — `import section` — محل افزودن import سرویس تلگرام جدید. باید import { startTelegramBot } from './services/telegramBot.js' به این بخش اضافه شود.
  ```jsx
  import express from 'express';
  import cors from 'cors';
  import { fileURLToPath } from 'url';
  import { dirname, join } from 'path';
  import dotenv from 'dotenv';
  import { createServer } from 'http';
  import { WebSocketServer, WebSocket } from 'ws';
  import multer from 'multer';
  import fs from 'fs';
  import os from 'os';
  import ffmpeg from 'fluent-ffmpeg';
  import ffmpegStatic from 'ffmpeg-static';
  ```
- `backend/server.js:42-48` — `server and WebSocket initialization` — محل مناسب برای فراخوانی startTelegramBot() بعد از راه‌اندازی WebSocket. باید کد startTelegramBot(server) بعد از خط 45 اضافه شود.
  ```jsx
  const server = createServer(app);
  
  // Create WebSocket server
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  
  app.use(cors());
  app.use(express.json({ limit: '10mb' }));
  ```
- `backend/.env.example:1-8` — `environment variables`
  ```
  # Gemini API Key - Get from https://aistudio.google.com/apikey
  GEMINI_API_KEY=your_gemini_api
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست کرده است که برای این پروژه (اپلیکیشن آموزش لهجه لبنانی) نوتیفیکیشن از طریق تلگرام فراهم شود و تلگرام به صورت دوطرفه کار کند. یعنی علاوه بر ارسال اطلاعات از سیستم به تلگرام، بتوان از طریق تلگرام نیز سیستم را کنترل کرد و برخی کارها را انجام داد. این تسک اولین مرحله از پیاده‌سازی است و شامل ایجاد یک ماژول پایه برای ارتباط با Telegram Bot API می‌شود. باید یک کلاس یا سرویس اصلی برای ارسال و دریافت پیام‌ها از طریق تلگرام ساخته شود. این مرحله فقط شامل راه‌اندازی Webhook یا Polling برای دریافت پیام‌ها و ارسال پاسخ‌های ساده است. خارج از این مرحله: پردازش محتوای دروس، مدیریت تمرین با استاد هوش مصنوعی، یا هرگونه منطق تجاری خاص. نکته حیاتی: باید از کتابخانه‌ای مثل python-telegram-bot یا aiogram استفاده شود و Webhook به‌درستی پیکربندی شود. کاربر کلیدواژه‌های Telegram Bot API, Webhook, python-telegram-bot, aiogram, Bot Token, Message Handler را ذکر کرده است. پروژه فعلی یک اپلیکیشن Node.js با backend مبتنی بر Express (backend/server.js) و frontend React (frontend/src/App.jsx) است. backend/server.js شامل APIهای مختلف از جمله /api/gemini/chat (خط 56), /api/gemini/tts (خط 117), /api/analyze-files (خط 698) و WebSocket در /ws/live (خط 45) است. backend از ماژول‌های express, cors, ws, multer, ffmpeg استفاده می‌کند. فایل backend/.env.example نشان می‌دهد که متغیرهای محیطی GEMINI_API_KEY, PORT و Firebase config استفاده می‌شوند. backend/package.json شامل وابستگی‌های cors, dotenv, express, ws است. frontend از React, Vite, Tailwind CSS, Firebase, lucide-react استفاده می‌کند. از آنجایی که پروژه فعلی JavaScript/Node.js است و کاربر کتابخانه‌های پایتونی (python-telegram-bot یا aiogram) را پیشنهاد کرده، این یک تغییر تکنولوژی استک است. با توجه به اینکه backend فعلی Node.js است، بهتر است از کتابخانه node-telegram-bot-api (که معادل Node.js کتابخانه‌های پیشنهادی کاربر است) استفاده شود تا با استک فعلی پروژه هماهنگ باشد. فایل‌های مرتبط: backend/server.js (خطوط 1-14 برای importها، خط 45 برای WebSocket، خطوط 56-114 برای APIهای Gemini)، backend/.env.example (برای افزودن متغیر محیطی BOT_TOKEN)، frontend/src/App.jsx (برای نمایش وضعیت اتصال تلگرام).

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. نصب کتابخانه node-telegram-bot-api در backend با دستور npm install node-telegram-bot-api در پوشه backend. 2. ایجاد فایل جدید backend/services/telegramBot.js که شامل کلاس TelegramBotService است. این کلاس باید: a) با استفاده از Bot Token (از process.env.TELEGRAM_BOT_TOKEN) به Telegram Bot API متصل شود. b) متد startPolling() برای شروع Polling (یا startWebhook() برای Webhook) داشته باشد. c) یک Message Handler پایه داشته باشد که پیام‌های دریافتی را لاگ کند و پاسخ ساده 'سلام! من ربات تلگرام اپلیکیشن آموزش لهجه لبنانی هستم.' بدهد. d) متد sendMessage(chatId, text) برای ارسال پیام به کاربر. e) متد sendNotification(chatId, message) برای ارسال نوتیفیکیشن. 3. افزودن متغیر TELEGRAM_BOT_TOKEN به backend/.env.example. 4. در backend/server.js، در خطوط بعد از ایجاد WebSocket (حدود خط 45)، سرویس تلگرام را import و راه‌اندازی کند: import { startTelegramBot } from './services/telegramBot.js' و سپس startTelegramBot(). 5. ایجاد یک endpoint جدید در backend/server.js مثل POST /api/telegram/send-notification که از طریق API داخلی بتوان نوتیفیکیشن ارسال کرد. 6. Webhook باید به آدرس https://<domain>/webhook/telegram پیکربندی شود (در صورت استفاده از Webhook به جای Polling). 7. برای پشتیبانی از Webhook، یک route جدید در Express اضافه شود: app.post('/webhook/telegram', (req, res) => { ... }).

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 2: پیاده‌سازی ارسال نوتیفیکیشن‌های سیستمی از طریق تلگرام

**Scope:** این مرحله شامل اتصال ماژول تلگرام به رویدادهای سیستمی است. باید مشخص شود چه رویدادهایی (مثلاً اتمام یک پردازش، خطا، به‌روزرسانی وضعیت) باید نوتیفیکیشن تلگرامی دریافت کنند. یک سرویس Event Bus یا Observer Pattern برای انتشار رویدادها و ارسال خودکار پیام به تلگرام پیاده‌سازی می‌شود. خارج از این مرحله: ارسال محتوای دروس یا فایل‌های آموزشی. نکته حیاتی: باید از queue (مثل Redis یا Celery) برای جلوگیری از blocking استفاده شود.
**Key terms:** Event Bus, Observer Pattern, Notification Service, Redis Queue, Celery Task, System Events

**بخش مربوط از متن کاربر:**
```
اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی نوتیفیکیشن سیستمی تلگرام با Event Bus و Redis Queue

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:1-12` — `import section` — محل افزودن importهای جدید: import NotificationService از './services/notificationService.js' و import { notificationQueue } از './queues/notificationQueue.js' بعد از خط 12
  ```jsx
  import express from 'express';
  import cors from 'cors';
  import { fileURLToPath } from 'url';
  import { dirname, join } from 'path';
  import dotenv from 'dotenv';
  import { createServer } from 'http';
  import { WebSocketServer, WebSocket } from 'ws';
  import multer from 'multer';
  import fs from 'fs';
  import os from 'os';
  import ffmpeg from 'fluent-ffmpeg';
  import ffmpegStatic from 'ffmpeg-static';
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی ارسال نوتیفیکیشن‌های سیستمی از طریق تلگرام را دارد. این مرحله شامل اتصال ماژول تلگرام به رویدادهای سیستمی است. باید مشخص شود چه رویدادهایی (مثلاً اتمام یک پردازش، خطا، به‌روزرسانی وضعیت) باید نوتیفیکیشن تلگرامی دریافت کنند. یک سرویس Event Bus یا Observer Pattern برای انتشار رویدادها و ارسال خودکار پیام به تلگرام پیاده‌سازی می‌شود. خارج از این مرحله: ارسال محتوای دروس یا فایل‌های آموزشی. نکته حیاتی: باید از queue (مثل Redis یا Celery) برای جلوگیری از blocking استفاده شود. کاربر تأکید کرده: 'اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته'. کلیدواژه‌های اصلی: Event Bus, Observer Pattern, Notification Service, Redis Queue, Celery Task, System Events. در کد فعلی پروژه (backend/server.js) هیچ سرویس نوتیفیکیشن یا Event Bus وجود ندارد. فایل backend/server.js شامل APIهای Gemini (Chat در خط 56، TTS در خط 117، File Analysis در خط 698) و WebSocket در خط 45 است. فایل frontend/index.html شامل Firebase Config (خط 11-18) و Inspector Bridge (خط 32-201) است. فایل .github/workflows/claude-auto-task.yml شامل workflow خودکار Claude Code است. backend/package-lock.json و backend/package.json وابستگی‌های فعلی (express, cors, dotenv, ws, multer, fluent-ffmpeg) را نشان می‌دهد. frontend/package-lock.json و package.json وابستگی‌های frontend (react, firebase, lucide-react, vite) را نشان می‌دهد. backend/.env.example فقط GEMINI_API_KEY و PORT و Firebase Config را دارد — هیچ متغیری برای Telegram Bot Token یا Redis وجود ندارد. برای پیاده‌سازی این درخواست، باید یک سرویس جدید backend/services/notificationService.js ایجاد شود که شامل: 1) ماژول Telegram Bot با استفاده از کتابخانه node-telegram-bot-api، 2) Event Bus داخلی با EventEmitter، 3) Redis Queue با کتابخانه bull یا ioredis برای non-blocking processing، 4) اتصال به رویدادهای سیستمی موجود (مانند اتمام پردازش فایل در خط 698-800، خطاهای API در خطوط 94, 111, 146, 162, 190, 238, 289, 322, 354, 417, 440). همچنین باید متغیرهای محیطی جدید TELEGRAM_BOT_TOKEN و TELEGRAM_CHAT_ID و REDIS_URL به backend/.env.example اضافه شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/services/notificationService.js با کلاس NotificationService که از EventEmitter ارث‌بری می‌کند. 2. در این کلاس متدهای: subscribe(eventName, callback), emit(eventName, data), sendTelegramNotification(chatId, message) پیاده‌سازی شود. 3. ایجاد فایل جدید backend/services/telegramBot.js با استفاده از کتابخانه node-telegram-bot-api که Webhook یا Polling را برای دریافت پیام‌های دوطرفه تلگرام راه‌اندازی می‌کند. 4. ایجاد فایل جدید backend/queues/notificationQueue.js با bull که jobهای نوتیفیکیشن را در Redis queue می‌گذارد و worker آن‌ها را پردازش می‌کند. 5. نصب وابستگی‌های جدید: npm install node-telegram-bot-api bull ioredis در backend/. 6. افزودن متغیرهای محیطی TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID, REDIS_URL به backend/.env.example. 7. اصلاح backend/server.js: import NotificationService در خطوط ابتدایی (بعد از خط 12)، نمونه‌سازی NotificationService بعد از خط 45، اتصال به رویدادهای موجود: بعد از اتمام موفق تحلیل فایل در تابع analyzeWithGemini (خط 273-294) emit('file_analysis_complete', {fileName, result})، بعد از خطا در APIها emit('api_error', {endpoint, error})، بعد از WebSocket connection در خط 45 emit('websocket_connected', {clientId}). 8. اصلاح backend/server.js برای استفاده از queue: در endpoint /api/analyze-files (خط 698) به جای await مستقیم analyzeWithGemini، job به notificationQueue اضافه شود. 9. ایجاد فایل backend/queues/notificationWorker.js که worker bull را راه‌اندازی کرده و jobها را با اولویت پردازش می‌کند. 10. افزودن لاگینگ برای تمام رویدادها در فایل backend/services/logger.js (اختیاری). 11. به‌روزرسانی backend/package.json با اسکریپت‌های جدید برای start worker.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 3: پیاده‌سازی دستورات کنترلی ساده از طریق تلگرام

**Scope:** این مرحله شامل تعریف و پیاده‌سازی دستورات متنی ساده است که کاربر می‌تواند از طریق تلگرام به سیستم بفرستد. مثال: /status برای وضعیت سیستم، /restart برای راه‌اندازی مجدد یک سرویس، /help برای راهنما. این دستورات باید توسط هندلرهای مخصوص پردازش شده و عملیات مربوطه روی سیستم اجرا شود. خارج از این مرحله: دستورات مربوط به محتوای دروس یا تمرین با استاد هوش مصنوعی. نکته حیاتی: دستورات باید احراز هویت شوند تا فقط کاربر مجاز بتواند از آنها استفاده کند.
**Key terms:** Command Handler, /status, /restart, /help, Authentication, Authorization

**بخش مربوط از متن کاربر:**
```
بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی دستورات کنترلی تلگرام (Command Handler) با احراز هویت

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:38-48` — `app (Express app) + server (HTTP server)` — اینجا محل افزودن route جدید POST /api/telegram/webhook است. بعد از خط ۴۸ (express.json) باید route جدید اضافه شود. همچنین نیاز به import کتابخانه تلگرام در ابتدای فایل.
  ```jsx
  const app = express();
  const PORT = process.env.PORT || 3001;
  
  // Create HTTP server
  const server = createServer(app);
  
  // Create WebSocket server
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  
  app.use(cors());
  app.use(express.json({ limit: '10mb' }));
  ```
- `backend/.env.example:1-8` — `فایل نمونه متغیرهای محیطی` — باید دو خط جدید اضافه شود: TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here و TELEGRAM_ALLOWED_USERS=chat_id_1,chat_id_2
  ```
  # Gemini API Key - Get from https://aistudio.google.com/apikey
  GEMINI_API_KEY=your_gemini_api_key_here
  
  # Port (optional, default 3001)
  PORT=3001
  
  # Firebase config (optional - for data persistence)
  # VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/package.json` (سطر 10) — برای اضافه کردن وابستگی node-telegram-bot-api یا axios (برای فراخوانی Telegram API) باید package.json به‌روز شود.
- `.github/workflows/claude-auto-task.yml` (سطر 17) — دستور /restart باید بتواند workflow 'Claude Auto Task Runner' را از طریق GitHub API dispatch کند. نیاز به بررسی secrets و endpoint.
- `frontend/index.html` — Firebase config موجود در frontend (apiKey, authDomain, projectId) می‌تواند برای احر

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی دستورات کنترلی ساده از طریق تلگرام را دارد. این مرحله شامل تعریف و پیاده‌سازی دستورات متنی ساده است که کاربر می‌تواند از طریق تلگرام به سیستم بفرستد. مثال: /status برای وضعیت سیستم، /restart برای راه‌اندازی مجدد یک سرویس، /help برای راهنما. این دستورات باید توسط هندلرهای مخصوص پردازش شده و عملیات مربوطه روی سیستم اجرا شود. خارج از این مرحله: دستورات مربوط به محتوای دروس یا تمرین با استاد هوش مصنوعی. نکته حیاتی: دستورات باید احراز هویت شوند تا فقط کاربر مجاز بتواند از آنها استفاده کند. کاربر در بخش مربوط از درخواست اصلی خود گفته: 'بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم'. کلیدواژه‌های مشخص شده: Command Handler, /status, /restart, /help, Authentication, Authorization. در کد فعلی پروژه (backend/server.js) هیچ پیاده‌سازی برای Webhook یا Bot API تلگرام وجود ندارد. تنها WebSocket در خط ۴۵ با مسیر '/ws/live' تعریف شده که برای ارتباطات live است. فایل backend/.env.example فقط شامل GEMINI_API_KEY و PORT و Firebase config است — هیچ متغیری برای توکن ربات تلگرام (مثلاً TELEGRAM_BOT_TOKEN) وجود ندارد. فایل frontend/index.html شامل Firebase config (apiKey: AIzaSyAMFtV0zIMKKe0bIr68Z1wTORQ1jQpsv9Q, authDomain: labaneseapp.firebaseapp.com, projectId: labaneseapp) است که می‌توان از Firebase Authentication برای احراز هویت کاربر تلگرام استفاده کرد. فایل .github/workflows/claude-auto-task.yml نشان می‌دهد که پروژه از GitHub Actions و Claude Code برای اجرای خودکار تسک‌ها استفاده می‌کند — دستور /restart می‌تواند از طریق API گیت‌هاب workflow dispatch را فعال کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ۱. ایجاد فایل جدید backend/services/telegramBot.js: شامل کلاس TelegramBotHandler با متدهای handleCommand, authenticateUser, processCommand. ۲. تعریف هندلرهای دستور: /status (وضعیت سرور، uptime، مصرف RAM)، /restart (راه‌اندازی مجدد سرویس‌ها از طریق API)، /help (راهنمای دستورات). ۳. پیاده‌سازی احراز هویت: مقایسه chatId کاربر تلگرام با لیست مجاز در متغیر محیطی TELEGRAM_ALLOWED_USERS (کاما جدا). ۴. افزودن Webhook endpoint در backend/server.js: POST /api/telegram/webhook برای دریافت آپدیت‌های تلگرام. ۵. افزودن متغیرهای محیطی جدید به backend/.env.example: TELEGRAM_BOT_TOKEN, TELEGRAM_ALLOWED_USERS. ۶. نصب کتابخانه node-telegram-bot-api یا استفاده از fetch خام برای Telegram Bot API (https://api.telegram.org/bot<token>/sendMessage). ۷. پیاده‌سازی دستور /restart با فراخوانی GitHub API برای dispatch workflow (با استفاده از GITHUB_TOKEN یا PAT). ۸. افزودن لاگینگ برای تمام دستورات اجرا شده.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 4: ایجاد ماژول مدیریت محتوای دروس از طریق تلگرام

**Scope:** این مرحله شامل ایجاد یک ماژول جدید برای مدیریت محتوای دروس (مشاهده و آپلود) از طریق تلگرام است. باید قابلیت‌های زیر پیاده‌سازی شود: ۱) دریافت لیست دروس موجود، ۲) مشاهده جزئیات یک درس (متن، فایل‌های ضمیمه)، ۳) آپلود فایل (PDF، تصویر، ویدئو) به عنوان محتوای جدید برای یک درس. این ماژول باید با دیتابیس موجود دروس ارتباط برقرار کند. خارج از این مرحله: مدیریت تمرین با استاد هوش مصنوعی. نکته حیاتی: باید محدودیت حجم فایل در تلگرام (۵۰ مگابایت) در نظر گرفته شود.
**Key terms:** Course Content Module, File Upload, File Download, Course List, Telegram File Size Limit, Database Integration

**بخش مربوط از متن کاربر:**
```
بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد ماژول مدیریت محتوای دروس از طریق تلگرام

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:56-114` — `app.post('/api/gemini/chat', ...)` — الگوی موجود برای API endpointها — مسیرهای جدید /api/courses باید با همین الگو (express async handler + error handling) ساخته شوند.
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
  ```
- `backend/server.js:30-33` — `const upload = multer({...})` — multer قبلاً پیکربندی شده — برای آپلود فایل‌های دروس می‌توان از همین استفاده کرد اما محدودیت باید به 50MB (Telegram limit) کاهش یابد.
  ```jsx
  const upload = multer({
    storage: storage,
    limits: { fileSize: 500 * 1024 * 1024 } // 500MB max per file
  });
  ```
- `frontend/index.html:11-18` — `var __firebase_config = JSON.stringify({...})` — Firebase config موجود — برای ذخیره‌سازی داده‌های دروس (Firestore) و فایل‌ها (Storage) استفاده می‌شود.
  ```
  var __firebase_config = JSON.stringify({
    apiKey: "AIzaSyAMFtV0zIMKKe0bIr68Z1wTORQ1jQpsv9Q",
    authDomain: "labaneseapp.firebaseapp.com",
    projectId: "labaneseapp",
    storageBucket: "labaneseapp.firebasestorage.app",
    messagingSenderId: "951874597795",
    appId: "1:951874597795:web:00745327993adad760a016"
  });
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `frontend/src/App.jsx` (سطر 1) — کامپوننت اصلی React — مسیرهای جدید برای صفحات مدیریت دروس باید به React Router اضافه شوند.
- `backend/package.json` (سطر 1) — نیاز به اضافه کردن dependencyهای جدید: firebase-admin (برای ارتباط با Firestore از سمت سرور) و node-telegram-bot-api (برای Telegram Bot).
- `frontend/package.json` (سطر 1) — dependency firebase قبلاً وجود دارد — ممکن است نیاز به نسخه جدید یا firebase/storage باشد.
- `backend/.env.example` (سطر 1) — نیاز به اضافه کردن متغیرهای محیطی جدید: TELEGRAM_BOT_TOKEN, FIREBASE_SERVICE_ACCOUNT_PATH.
- `prompt/task-dc721af2-f84f-424d-a491-91`

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد یک ماژول جدید برای مدیریت محتوای دروس (مشاهده و آپلود) از طریق تلگرام را دارد. این ماژول باید قابلیت‌های زیر را پیاده‌سازی کند: ۱) دریافت لیست دروس موجود، ۲) مشاهده جزئیات یک درس (متن، فایل‌های ضمیمه)، ۳) آپلود فایل (PDF، تصویر، ویدئو) به عنوان محتوای جدید برای یک درس. این ماژول باید با دیتابیس موجود دروس ارتباط برقرار کند. خارج از این مرحله: مدیریت تمرین با استاد هوش مصنوعی. نکته حیاتی: باید محدودیت حجم فایل در تلگرام (۵۰ مگابایت) در نظر گرفته شود. کاربر در بخش مربوط از درخواست اصلی خود گفته: 'بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم'. کلیدواژه‌های اصلی: Course Content Module, File Upload, File Download, Course List, Telegram File Size Limit, Database Integration. در کد فعلی پروژه (backend/server.js) هیچ endpoint یا ماژولی برای مدیریت دروس (Course) وجود ندارد. تنها APIهای موجود مربوط به Gemini Chat (خط ۵۶), TTS (خط ۱۱۷), Health Check (خط ۱۶۷), List Models (خط ۱۷۲), Test Gemini (خط ۱۹۵), و File Analysis (خط ۶۹۸) هستند. همچنین در frontend/index.html و frontend/src/App.jsx هیچ کامپوننتی برای نمایش لیست دروس یا آپلود محتوا وجود ندارد. بنابراین این یک feature جدید است که نیازمند ایجاد ساختار دیتابیس (احتمالاً Firebase Firestore با توجه به firebase_config در index.html خط ۱۱-۱۸ و package.json که firebase را به عنوان dependency دارد)، API endpoints جدید در backend/server.js، و یک Telegram Bot Interface (با توجه به commit message‌های اخیر که اشاره به 'نوتیفیکیشن از طریق تلگرام' و 'تلگرام دو طرفه کار' دارند) می‌باشد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ۱) ایجاد مدل داده برای Course و CourseContent در Firebase Firestore (با توجه به Firebase config موجود در frontend/index.html خط ۱۱-۱۸ و dependency firebase در package.json). ۲) ایجاد API endpoints جدید در backend/server.js: GET /api/courses (لیست دروس), GET /api/courses/:id (جزئیات درس), POST /api/courses/:id/upload (آپلود فایل برای درس) با استفاده از multer (که قبلاً در خط ۳۰-۳۳ backend/server.js پیکربندی شده). ۳) ایجاد یک ماژول Telegram Bot (فایل جدید backend/services/telegramBot.js) که از کتابخانه node-telegram-bot-api استفاده کند و دستورات /courses, /course <id>, /upload <courseId> را مدیریت کند. ۴) محدودیت حجم فایل ۵۰ مگابایت برای آپلود از طریق تلگرام (Telegram File Size Limit) باید در validation سمت سرور اعمال شود. ۵) ذخیره فایل‌های آپلود شده در Firebase Storage (با توجه به storageBucket: 'labaneseapp.firebasestorage.app' در frontend/index.html خط ۱۵). ۶) ایجاد کامپوننت‌های React جدید در frontend/src/ برای نمایش لیست دروس و فرم آپلود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 5: اتصال ماژول تلگرام به بخش 'تمرین با استاد هوش مصنوعی'

**Scope:** این مرحله شامل ایجاد یکپارچگی بین ربات تلگرام و بخش 'تمرین با استاد هوش مصنوعی' است. باید یک API endpoint یا WebSocket connection برای تبادل پیام‌ها بین تلگرام و چت هوش مصنوعی ایجاد شود. کاربر باید بتواند از طریق تلگرام موضوعات تمرین را مشاهده، انتخاب و مدیریت کند. خارج از این مرحله: پیاده‌سازی خود هوش مصنوعی یا پردازش صوت. نکته حیاتی: باید state management برای هر کاربر در نظر گرفته شود تا مکالمات موازی مدیریت شوند.
**Key terms:** AI Tutor Module, WebSocket, API Integration, State Management, Topic Selection, Session Management

**بخش مربوط از متن کاربر:**
```
اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
اتصال ماژول تلگرام به بخش 'تمرین با استاد هوش مصنوعی' با WebSocket و State Management

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:44-45` — `WebSocketServer` — WebSocket server فعلی با مسیر '/ws/live'. باید یک مسیر جدید '/ws/telegram' اضافه شود یا مسیریابی بر اساس نوع پیام به این WebSocket اضافه شود.
  ```jsx
  // Create WebSocket server
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/server.js:56-114` — `app.post('/api/gemini/chat')`
  ```jsx
  // Gemini Chat API
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
  
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
  
      // Clean payload - preserve audio data if includeAudio flag is set
      if (payload.contents) {
        payload.contents = payload.contents.map(content => ({
          role: content.role,
          parts: content.parts.map(part => {
            // Keep inline_data (audio) if present
            if (part.inline_data && includeAudio) {
              return { inline_data: part.inline_data };
            }
            // Keep text if present
            if (part.text !== undefined) {
              return { text: part.text };
            }
            return null;
          }).filter(p => p !== null)
        }));
      }
  
      const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${GEMINI_API_KEY}`;
  
      const response = await fetch(apiUrl, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست کرده است که ماژول تلگرام به بخش 'تمرین با استاد هوش مصنوعی' متصل شود. این مرحله شامل ایجاد یکپارچگی بین ربات تلگرام و بخش 'تمرین با استاد هوش مصنوعی' است. باید یک API endpoint یا WebSocket connection برای تبادل پیام‌ها بین تلگرام و چت هوش مصنوعی ایجاد شود. کاربر باید بتواند از طریق تلگرام موضوعات تمرین را مشاهده، انتخاب و مدیریت کند. خارج از این مرحله: پیاده‌سازی خود هوش مصنوعی یا پردازش صوت. نکته حیاتی: باید state management برای هر کاربر در نظر گرفته شود تا مکالمات موازی مدیریت شوند. بخش مربوط از درخواست اصلی کاربر: 'اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم'. کلیدواژه‌ها: AI Tutor Module, WebSocket, API Integration, State Management, Topic Selection, Session Management. در کد فعلی پروژه (backend/server.js) یک WebSocket server در خط 45 با مسیر '/ws/live' ایجاد شده است (const wss = new WebSocketServer({ server, path: '/ws/live' })). همچنین یک endpoint برای چت با Gemini در خط 56 (app.post('/api/gemini/chat')) وجود دارد. frontend/index.html شامل یک Inspector Bridge است که از طریق postMessage با والد ارتباط برقرار می‌کند. backend/.env.example نشان می‌دهد که Firebase برای persistence داده‌ها اختیاری است. backend/package.json شامل وابستگی‌های express, cors, dotenv, ws, multer, fluent-ffmpeg است. frontend/package.json شامل react, firebase, lucide-react است. این تسک نیازمند ایجاد یک WebSocket endpoint جدید (مثلاً '/ws/telegram') یا استفاده از همان '/ws/live' با مسیریابی بر اساس نوع پیام، و ایجاد یک API endpoint برای مدیریت موضوعات تمرین (مثلاً '/api/topics') است. State management باید با استفاده از یک Map یا دیکشنری در سمت سرور (backend/server.js) برای هر کاربر (chatId تلگرام) انجام شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک WebSocket endpoint جدید در backend/server.js با مسیر '/ws/telegram' (یا افزودن مسیریابی به wss موجود در خط 45) که پیام‌های تلگرام را دریافت و به بخش AI Tutor هدایت کند. 2. ایجاد یک API endpoint جدید در backend/server.js مانند 'app.post('/api/telegram/topics')' برای دریافت و بازگرداندن لیست موضوعات تمرین. 3. پیاده‌سازی یک کلاس یا ماژول StateManager در backend/server.js (یا فایل جداگانه backend/services/stateManager.js) که با استفاده از Map<chatId, Session> وضعیت هر کاربر را مدیریت کند. 4. ایجاد یک تابع handleTelegramMessage در backend/server.js که پیام دریافتی از WebSocket تلگرام را پردازش کرده و با استفاده از endpoint '/api/gemini/chat' (خط 56) به AI Tutor ارسال کند. 5. افزودن event handler برای WebSocket در backend/server.js برای مدیریت اتصال، دریافت پیام، و قطع اتصال. 6. ایجاد یک endpoint برای مدیریت session مانند 'app.post('/api/telegram/session')' برای شروع/پایان جلسات تمرین. 7. اطمینان از اینکه state management برای هر کاربر (chatId) به صورت مجزا انجام می‌شود تا مکالمات موازی مدیریت شوند.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 6: پیاده‌سازی ارسال و دریافت پیام صوتی از طریق تلگرام برای استاد هوش مصنوعی

**Scope:** این مرحله شامل قابلیت ارسال پیام صوتی از طریق تلگرام به استاد هوش مصنوعی است. کاربر باید بتواند یک پیام صوتی ضبط کرده و به ربات بفرستد. ربات باید فایل صوتی را دریافت کرده، به سرویس Speech-to-Text (STT) ارسال کند، متن استخراج شده را به هوش مصنوعی بفرستد، و پاسخ را به صورت متن و/یا صوتی (Text-to-Speech) به کاربر برگرداند. خارج از این مرحله: پردازش ویدئو یا سایر فرمت‌های چندرسانه‌ای. نکته حیاتی: باید از یک سرویس STT/TTS معتبر (مثل Google Cloud Speech-to-Text یا Whisper) استفاده شود.
**Key terms:** Voice Message, Speech-to-Text (STT), Text-to-Speech (TTS), Whisper API, Google Cloud Speech, Audio Processing

**بخش مربوط از متن کاربر:**
```
از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن قابلیت پیام صوتی تلگرام به استاد هوش مصنوعی

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:56-114` — `app.post('/api/gemini/chat')` — این endpoint برای ارسال متن به Gemini و دریافت پاسخ استفاده می‌شود. در تسک جدید، پس از STT، متن استخراج‌شده به اینجا فرستاده می‌شود.
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
      ...
    }
  ```
- `backend/server.js:117-164` — `app.post('/api/gemini/tts')` — این endpoint برای تبدیل متن به صوت استفاده می‌شود. پاسخ هوش مصنوعی به اینجا فرستاده می‌شود تا فایل صوتی تولید شود.
  ```jsx
  app.post('/api/gemini/tts', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const { prompt, voice = 'Kore' } = req.body;
      const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-preview-tts:generateContent?key=${GEMINI_API_KEY}`;
      ...
    }
  ```
- `backend/server.js:698-800` — `app.post('/api/analyze-files')` — این endpoint نمونه‌ای از پردازش فایل‌های صوتی با multer و ffmpeg است. منطق مشابهی برای دانلود و پردازش فایل صوتی تلگرام استفاده می‌شود.
  ```jsx
  app.post('/api/analyze-files', upload.array('files', 10), handleMulterError, async (req, res) => {
    console.log('Received file analysis request');
    if (!GEMINI_API_KEY) {
      console.error('API key not configured');
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const files = req.files || [];
      ...
    }
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/.env.example` (سطر 1) — برای اضافه کردن متغیر محیطی TELEGRAM_BOT_TOKEN
- `backend/package.json` (سطر 1) — برای اضافه کردن وابستگی node-telegram-bot-api یا telegraf
- `frontend/src/App.jsx` — برای نمایش

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی ارسال و دریافت پیام صوتی از طریق تلگرام برای استاد هوش مصنوعی را دارد. این قابلیت باید به کاربر اجازه دهد یک پیام صوتی ضبط کرده و به ربات تلگرام بفرستد. ربات باید فایل صوتی را دریافت کرده، به سرویس Speech-to-Text (STT) ارسال کند، متن استخراج شده را به هوش مصنوعی (که در حال حاضر از طریق endpoint `/api/gemini/chat` در `backend/server.js` خط ۵۶ با مدل `gemini-2.0-flash` کار می‌کند) بفرستد، و پاسخ را به صورت متن و/یا صوتی (Text-to-Speech) از طریق endpoint `/api/gemini/tts` در `backend/server.js` خط ۱۱۷ به کاربر برگرداند. کاربر تأکید کرده: «از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد». خارج از این مرحله: پردازش ویدئو یا سایر فرمت‌های چندرسانه‌ای. نکته حیاتی: باید از یک سرویس STT/TTS معتبر (مثل Google Cloud Speech-to-Text یا Whisper API) استفاده شود. کلیدواژه‌ها: Voice Message, Speech-to-Text (STT), Text-to-Speech (TTS), Whisper API, Google Cloud Speech, Audio Processing. در کد فعلی، پردازش فایل‌های صوتی از طریق endpoint `/api/analyze-files` در `backend/server.js` خط ۶۹۸ انجام می‌شود که از `multer` برای آپلود و `ffmpeg` برای پردازش استفاده می‌کند. همچنین WebSocket در خط ۴۵ برای ارتباطات زنده راه‌اندازی شده است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ۱. ایجاد یک endpoint جدید در `backend/server.js` برای Webhook تلگرام (مثلاً `POST /api/telegram/webhook`) که پیام‌های صوتی را دریافت کند. ۲. در این endpoint، فایل صوتی را از تلگرام دانلود کرده و با استفاده از سرویس STT (مثلاً Whisper API از طریق OpenAI یا Google Cloud Speech-to-Text) به متن تبدیل کند. ۳. متن استخراج‌شده را به endpoint موجود `/api/gemini/chat` (خط ۵۶) ارسال کند تا پاسخ هوش مصنوعی دریافت شود. ۴. پاسخ متنی را با استفاده از endpoint `/api/gemini/tts` (خط ۱۱۷) به صوت تبدیل کند. ۵. پاسخ نهایی (متن + صوت) را از طریق تلگرام به کاربر ارسال کند. ۶. برای نصب وابستگی‌ها، کتابخانه `node-telegram-bot-api` یا `telegraf` را به `backend/package.json` اضافه کند. ۷. متغیر محیطی `TELEGRAM_BOT_TOKEN` را به `backend/.env.example` اضافه کند. ۸. از WebSocket موجود (خط ۴۵) برای همگام‌سازی وضعیت با فرانت‌اند استفاده کند تا پیام‌ها در چت وب نیز نمایش داده شوند.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 7: همگام‌سازی دوطرفه پیام‌ها بین چت وب و تلگرام

**Scope:** این مرحله شامل همگام‌سازی پیام‌ها بین چت وب (بخش 'تمرین با استاد هوش مصنوعی') و تلگرام است. هر پیامی که در چت وب ارسال یا دریافت می‌شود باید در تلگرام نیز نمایش داده شود و بالعکس. باید از یک message queue یا WebSocket برای broadcast پیام‌ها استفاده شود. خارج از این مرحله: پردازش محتوای پیام‌ها. نکته حیاتی: باید از duplicate messages جلوگیری شود و ترتیب پیام‌ها حفظ شود.
**Key terms:** Message Sync, WebSocket Broadcast, Message Queue, Real-time Sync, Deduplication, Order Preservation

**بخش مربوط از متن کاربر:**
```
هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد
```

## 🎯 هدف (خلاصه ساختاریافته)
همگام‌سازی دوطرفه پیام‌ها بین چت وب و تلگرام با WebSocket

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:44-45` — `WebSocketServer` — WebSocket موجود باید برای broadcast پیام‌های چت به تلگرام و بالعکس توسعه یابد. در حال حاضر فقط برای inspector bridge استفاده می‌شود.
  ```jsx
  // Create WebSocket server
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/server.js:56-114` — `app.post('/api/gemini/chat')` — این endpoint چت هوش مصنوعی است. پس از دریافت پاسخ از Gemini، باید پیام را به message queue اضافه کند تا به تلگرام broadcast شود.
  ```jsx
  // Gemini Chat API
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
  ```
- `frontend/index.html:32-201` — `Inspector Bridge Script` — این اسکریپت از postMessage برای ارتباط با parent استفاده می‌کند. برای همگام‌سازی با تلگرام، باید WebSocket مستقیم به backend اضافه شود.
  ```
  <script>
  (function() {
    console.log('🌉 Inspector Bridge: Script starting...');
    if (window.__inspectorBridgeLoaded) {
      console.log('🌉 Inspector Bridge: Already loaded, skipping');
      return;
    }
    window.__inspectorBridgeLoaded = true;
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/.env.example` (سطر 1) — باید متغیرهای TELEGRAM_BOT_TOKEN و TELEGRAM_CHAT_ID به این فایل اضافه شود.
- `backend/package.json` (سطر 1) — باید وابستگی node-telegram-bot-api به این فایل اضافه شود.
- `frontend/src/App.jsx` — کامپوننت اصلی React که چت وب را مدیریت می

## 🔍 Context و وضعیت فعلی
کاربر درخواست همگام‌سازی دوطرفه پیام‌ها بین چت وب (بخش 'تمرین با استاد هوش مصنوعی') و تلگرام را دارد. هر پیامی که در چت وب ارسال یا دریافت می‌شود باید در تلگرام نیز نمایش داده شود و بالعکس. باید از یک message queue یا WebSocket برای broadcast پیام‌ها استفاده شود. خارج از این مرحله: پردازش محتوای پیام‌ها. نکته حیاتی: باید از duplicate messages جلوگیری شود و ترتیب پیام‌ها حفظ شود. کلیدواژه‌ها: Message Sync, WebSocket Broadcast, Message Queue, Real-time Sync, Deduplication, Order Preservation. در کد فعلی پروژه، backend/server.js از WebSocketServer در خط ۴۵ با مسیر '/ws/live' استفاده می‌کند (const wss = new WebSocketServer({ server, path: '/ws/live' });) اما این WebSocket فقط برای inspector bridge در frontend/index.html استفاده می‌شود و هیچ ارتباطی با چت هوش مصنوعی یا تلگرام ندارد. endpoint چت هوش مصنوعی در '/api/gemini/chat' (خط ۵۶) از HTTP استفاده می‌کند نه WebSocket. frontend/index.html شامل اسکریپت inspector bridge (خطوط ۳۲-۲۰۱) است که رویدادهای کلیک، اسکرول، تایپ و فوکوس را از طریق postMessage به parent ارسال می‌کند. هیچ کد تلگرام یا message queue در پروژه وجود ندارد. برای پیاده‌سازی این درخواست، باید یک سرویس تلگرام (با کتابخانه‌ای مانند node-telegram-bot-api) به backend اضافه شود، WebSocket موجود برای broadcast پیام‌های چت به تلگرام توسعه یابد، و یک message queue (مانند bull یا redis) برای مدیریت ترتیب و جلوگیری از duplicate پیاده‌سازی شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ۱. نصب کتابخانه node-telegram-bot-api در backend (backend/package.json). ۲. ایجاد فایل جدید backend/services/telegramService.js که شامل: - راه‌اندازی bot با توکن تلگرام از env - تابع sendMessageToTelegram(chatId, text) - تابع handleTelegramMessage که پیام‌های دریافتی از تلگرام را به WebSocket broadcast کند. ۳. ایجاد فایل جدید backend/services/messageQueue.js با: - صف پیام‌ها با شناسه یکتا (UUID) برای deduplication - تابع enqueueMessage برای افزودن پیام به صف - تابع processQueue برای پردازش ترتیبی پیام‌ها - مکانیزم broadcast به WebSocket clients. ۴. تغییر backend/server.js: - import سرویس‌های جدید - افزودن endpoint جدید POST /api/telegram/webhook برای دریافت webhook از تلگرام - اتصال WebSocket به message queue برای broadcast پیام‌های چت به تلگرام - افزودن event handler برای WebSocket که پیام‌های دریافتی از کلاینت وب را به message queue اضافه کند. ۵. تغییر frontend/src/App.jsx: - اتصال به WebSocket برای دریافت پیام‌های broadcast شده از تلگرام - نمایش پیام‌های تلگرام در چت وب - ارسال پیام‌های چت وب از طریق WebSocket به backend. ۶. افزودن متغیرهای محیطی TELEGRAM_BOT_TOKEN و TELEGRAM_CHAT_ID به backend/.env.example.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 8: پیاده‌سازی مدیریت موضوعات تمرین از طریق تلگرام

**Scope:** این مرحله شامل قابلیت مدیریت کامل موضوعات تمرین (Topics) از طریق تلگرام است. کاربر باید بتواند: ۱) موضوعات جدید ایجاد کند، ۲) موضوعات موجود را ویرایش کند، ۳) موضوعات را حذف کند، ۴) ترتیب موضوعات را تغییر دهد. این عملیات باید از طریق دستورات تلگرام و با تأیید (confirmation) انجام شود. خارج از این مرحله: مدیریت محتوای دروس. نکته حیاتی: باید دسترسی‌های امنیتی (فقط ادمین) برای این عملیات در نظر گرفته شود.
**Key terms:** Topic CRUD, Create Topic, Edit Topic, Delete Topic, Reorder Topics, Admin Authorization

**بخش مربوط از متن کاربر:**
```
بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
مدیریت موضوعات تمرین از طریق تلگرام با دسترسی ادمین

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:56-114` — `app.post('/api/gemini/chat')` — این endpoint الگوی ساخت endpointهای جدید Topic CRUD است. باید endpointهای مشابه با مسیر /api/topics اضافه شوند.
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
  
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
  ```
- `backend/.env.example:1-8` — `Environment variables` — Firebase برای persistence داده‌های Topic در نظر گرفته شده. باید متغیرهای محیط
  ```
  # Gemini API Key - Get from https://aistudio.google.com/apikey
  GEMINI_API_KEY=your_gemini_api_key_here
  
  # Port (optional, default 3001)
  PORT=3001
  
  # Firebase config (optional - for data persistence)
  # VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی مدیریت کامل موضوعات تمرین (Topics) از طریق تلگرام را دارد. این قابلیت شامل ۴ عملیات CRUD است: ۱) ایجاد موضوع جدید (Create Topic)، ۲) ویرایش موضوع موجود (Edit Topic)، ۳) حذف موضوع (Delete Topic)، ۴) تغییر ترتیب موضوعات (Reorder Topics). تمام این عملیات باید از طریق دستورات تلگرام و با تأیید (confirmation) انجام شود. نکته حیاتی: فقط ادمین‌ها مجاز به انجام این عملیات هستند (Admin Authorization). خارج از این مرحله: مدیریت محتوای دروس. بخش مربوط از درخواست اصلی کاربر: 'بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم'. کلیدواژه‌ها: Topic CRUD, Create Topic, Edit Topic, Delete Topic, Reorder Topics, Admin Authorization. در کد فعلی پروژه (backend/server.js) هیچ endpoint یا سرویسی برای مدیریت Topic وجود ندارد. فایل‌های مرتبط با تلگرام در deep context موجود نیستند اما با توجه به commit history (کامیت 856e24e با پیام 'میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار') مشخص است که زیرساخت تلگرام در حال پیاده‌سازی است. فایل‌های prompt/archive/task-141249ee-0d23-44e0-aeec-21f989820b60.md و prompt/archive/task-2a0673ad-13dd-4131-9c72-18a093d60686.md و prompt/archive/task-3336cbc2-34e9-4f8f-8216-78d40b813199.md و prompt/archive/task-38433cce-3e63-4851-89bc-4320f55f4d19.md و prompt/archive/task-47bfef80-d77a-4386-9dca-fb1a533ca20b.md و prompt/archive/task-59cbc244-8dbe-4b9d-9a39-a3633ffd0464.md و prompt/archive/task-5bd04995-7545-4582-acb9-20c453d383a9.md و prompt/archive/task-6225900f-6226-487c-9ae1-55dbd114e165.md و prompt/archive/task-645bbdc2-f205-479f-b01f-f8b8ad4ef2e4.md و prompt/archive/task-6d989ea3-b525-4566-994a-ce401ae7373a.md و prompt/archive/task-7599ae4a-1d12-4c87-80c2-b16e5876d9d4.md و prompt/archive/task-8ac8249d-e1de-40f8-ad4b-36e271a1b237.md و prompt/archive/task-9d4c545b-6683-46f1-8121-8ae6705d2122.md و prompt/archive/task-adbe7499-a96f-4c38-8c32-e547a3b7e2c2.md و prompt/archive/task-b3d7759b-2054-457f-a5ce-54dc346fb960.md و prompt/archive/task-ba60df37-44e8-426e-8bf8-4a48cbedb11b.md و prompt/archive/task-c7474d4f-8730-4531-b8ec-50de5a444b82.md و prompt/archive/task-d13296fb-53a8-45a4-919b-de4f984a5a64.md حاوی تسک‌های قبلی هستند که می‌توانند برای فهم الگوی پیاده‌سازی تلگرام استفاده شوند. backend/server.js خط ۵۶-۱۱۳ شامل endpoint /api/gemini/chat است که می‌تواند الگویی برای ساخت endpointهای جدید باشد. backend/.env.example نشان می‌دهد که Firebase برای persistence داده در نظر گرفته شده است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ۱. ایجاد فایل جدید backend/services/topicService.js برای مدیریت Topicها با توابع: createTopic(name, description, order), updateTopic(id, data), deleteTopic(id), reorderTopics(orderedIds), getTopics(). ۲. ایجاد فایل جدید backend/services/telegramBot.js (اگر وجود ندارد) با هندلرهای دستورات: /createtopic, /edittopic, /deletetopic, /reordertopics. ۳. پیاده‌سازی middleware احراز هویت ادمین در backend/middleware/adminAuth.js که chatId کاربر تلگرام را با لیست ادمین‌ها چک کند. ۴. ایجاد endpointهای REST در backend/server.js: POST /api/topics (ایجاد), PUT /api/topics/:id (ویرایش), DELETE /api/topics/:id (حذف), PATCH /api/topics/reorder (تغییر ترتیب). ۵. ذخیره‌سازی داده‌ها در Firebase Firestore (با توجه به backend/.env.example که Firebase config دارد) یا در فایل JSON محلی. ۶. پیاده‌سازی مکانیزم تأیید (confirmation) برای عملیات حذف و تغییر ترتیب با استفاده از inline keyboard تلگرام.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 9: پیاده‌سازی انتخاب موضوع تمرین از طریق تلگرام

**Scope:** این مرحله شامل قابلیت انتخاب یک موضوع تمرین توسط کاربر از طریق تلگرام است. کاربر باید بتواند از لیست موضوعات، یکی را انتخاب کرده و session تمرین را شروع کند. انتخاب باید از طریق دکمه‌های Inline Keyboard در تلگرام انجام شود. خارج از این مرحله: خود مکالمه با هوش مصنوعی. نکته حیاتی: باید وضعیت کاربر (state) ذخیره شود تا بدانیم در کدام session قرار دارد.
**Key terms:** Inline Keyboard, Topic Selection, Session Start, User State, Callback Query

**بخش مربوط از متن کاربر:**
```
و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی انتخاب موضوع تمرین از طریق تلگرام با Inline Keyboard و ذخیره وضعیت کاربر

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:45-46` — `WebSocketServer` — WebSocket موجود برای ارتباط بلادرنگ با فرانت‌اند. برای مکالمه با هوش مصنوعی از طریق تلگرام می‌توان از همین WebSocket یا یک API جدید استفاده کرد.
  ```jsx
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/server.js:56-114` — `app.post('/api/gemini/chat')` — API موجود برای چت با Gemini. می‌توان از این endpoint برای مکالمه با هوش مصنوعی در session تلگرام استفاده کرد (با ارسال درخواست از سمت بات تلگرام به این API).
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => { ... });
  ```
- `backend/server.js:698-715` — `app.post('/api/analyze-files')` — API تحلیل فایل. ممکن است در آینده برای ارسال فایل از طریق تلگرام (مثلاً تصویر یا صدا) استفاده شود.
  ```jsx
  app.post('/api/analyze-files', upload.array('files', 10), handleMulterError, async (req, res) => { ... });
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/package.json` (سطر 1) — برای افزودن وابستگی node-telegram-bot-api به پروژه نیاز به تغییر دارد.
- `backend/.env.example` (سطر 1) — برای افزودن متغیر محیطی TELEGRAM_BOT_TOKEN نیاز به تغییر دارد.
- `frontend/src/App.jsx` (سطر 1) — ممکن است نیاز به نمایش وضعیت session تلگرام در فرانت‌اند باشد (اختیاری).
- `prompt/archive/task-dc721af2-f84f-424d-a491-91e8ab3624c0.md` (سطر 1) — این تسک قبلی مربوط به افزودن نوتیفیکیشن تلگرام دوطرفه است که می‌تواند به عنوان مرجع برای ساختار بات تلگرام استفاده شود.

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی قابلیت انتخاب یک موضوع تمرین از طریق تلگرام را دارد. کاربر باید بتواند از لیست موضوعات، یکی را انتخاب کرده و session تمرین را شروع کند. انتخاب باید از طریق دکمه‌های Inline Keyboard در تلگرام انجام شود. خارج از این مرحله: خود مکالمه با هوش مصنوعی. نکته حیاتی: باید وضعیت کاربر (state) ذخیره شود تا بدانیم در کدام session قرار دارد. بخش مربوط از درخواست اصلی کاربر: 'و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم'. کلیدواژه‌ها: Inline Keyboard, Topic Selection, Session Start, User State, Callback Query. با بررسی کد واقعی پروژه (backend/server.js خطوط 1-800، frontend/index.html، frontend/src/App.jsx، و فایل‌های prompt/archive)، مشخص شد که پروژه یک اپلیکیشن آموزش لهجه لبنانی با هوش مصنوعی است که از Express.js در بک‌اند و React در فرانت‌اند استفاده می‌کند. در حال حاضر هیچ پیاده‌سازی برای تلگرام (Telegram Bot API) وجود ندارد. بک‌اند شامل WebSocket (خط 45: new WebSocketServer({ server, path: '/ws/live' })) و APIهای مختلف Gemini (خطوط 56-240) است. برای پیاده‌سازی این قابلیت، نیاز به ایجاد یک Telegram Bot جدید (با استفاده از کتابخانه node-telegram-bot-api یا grammy) در بک‌اند، تعریف لیست موضوعات تمرین، ایجاد Inline Keyboard برای نمایش موضوعات، هندل کردن Callback Query برای دریافت انتخاب کاربر، ذخیره state کاربر (chatId, selectedTopic, sessionStatus) در یک دیتابیس ساده (فایل JSON یا SQLite)، و ایجاد یک endpoint جدید برای ارتباط با هوش مصنوعی موجود (احتمالاً از طریق WebSocket موجود یا API جدید) است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. نصب کتابخانه node-telegram-bot-api در backend (backend/package.json). 2. ایجاد فایل جدید backend/services/telegramBot.js شامل: راه‌اندازی بات با توکن از env (TELEGRAM_BOT_TOKEN)، تعریف لیست موضوعات تمرین (مانند 'سلام و احوالپرسی', 'خرید و معامله', 'غذا و رستوران', 'جهت‌ها و مسیریابی')، تابع sendTopicSelectionKeyboard(chatId) که یک Inline Keyboard با دکمه‌های موضوعات می‌فرستد، هندلر callback_query که انتخاب کاربر را دریافت کرده و state را ذخیره می‌کند. 3. ایجاد فایل جدید backend/services/sessionStore.js برای ذخیره state کاربر (chatId, selectedTopic, sessionStatus, createdAt) با استفاده از یک Map در حافظه یا فایل JSON. 4. ایجاد endpoint جدید در backend/server.js (مثلاً POST /api/telegram/start-session) که توسط بات تلگرام فراخوانی می‌شود و session را شروع می‌کند. 5. اتصال session به WebSocket موجود (backend/server.js خط 45) یا ایجاد یک API جدید برای مکالمه با هوش مصنوعی (با استفاده از endpoint /api/gemini/chat در خط 56). 6. افزودن متغیر محیطی TELEGRAM_BOT_TOKEN به backend/.env.example.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 10: تست و دیباگ یکپارچه کل سیستم تلگرام

**Scope:** این مرحله شامل تست کامل تمام قابلیت‌های پیاده‌سازی شده در مراحل قبل است. باید سناریوهای زیر تست شوند: ۱) ارسال و دریافت نوتیفیکیشن، ۲) اجرای دستورات کنترلی، ۳) آپلود و مشاهده محتوای دروس، ۴) مکالمه متنی با استاد هوش مصنوعی، ۵) ارسال و دریافت پیام صوتی، ۶) همگام‌سازی دوطرفه پیام‌ها، ۷) مدیریت موضوعات. خارج از این مرحله: تست هوش مصنوعی یا سرویس‌های خارجی. نکته حیاتی: باید خطاهای احتمالی (مثل قطعی شبکه، حجم بالای فایل) نیز تست شوند.
**Key terms:** Integration Test, End-to-End Test, Error Handling, Network Failure, File Size Limit, User Acceptance Test

**بخش مربوط از متن کاربر:**
```
و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

## 🎯 هدف (خلاصه ساختاریافته)
تست و دیباگ یکپارچه سیستم تلگرام

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:44-46` — `WebSocketServer` — WebSocket server برای ارتباط دوطرفه با تلگرام - باید تست شود که قطعی شبکه را مدیریت می‌کند
  ```jsx
  // Create WebSocket server
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/server.js:56-114` — `app.post('/api/gemini/chat')` — Endpoint مکالمه با استاد هوش مصنوعی - باید تست شود که پیام‌های متنی را پردازش می‌کند
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
  ```
- `backend/server.js:30-33` — `multer configuration` — تنظیمات آپلود فایل با محدودیت حجم - باید تست شود که فایل‌های بزرگ را مدیریت می‌کند
  ```jsx
  const upload = multer({
    storage: storage,
    limits: { fileSize: 500 * 1024 * 1024 } // 500MB max per file
  });
  ```
- `frontend/index.html:10-19` — `Firebase Configuration` — Firebase config برای نوتیفیکیشن - باید تست شود که FCM messages به درستی ارسال و دریافت می‌شوند
  ```
  var __firebase_config = JSON.stringify({
    apiKey: "AIzaSyAMFtV0zIMKKe0bIr68Z1wTORQ1jQpsv9Q",
    authDomain: "labaneseapp.firebaseapp.com",
    projectId: "labaneseapp",
    storageBucket: "labaneseapp.firebasestorage.app",
    messagingSenderId: "951874597795",
    appId: "1:951874597795:web:00745327993adad760a016"
  });
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/package.json` (سطر 1) — وابستگی‌های backend شامل ws, express, multer برای WebSocket و آپلود فایل
- `frontend/package.json` — وابستگی‌های frontend شامل firebase برای نوتیفیکیشن

## 🔍 Context و وضعیت فعلی
این تسک شامل تست کامل تمام قابلیت‌های پیاده‌سازی شده سیستم تلگرام در پروژه آموزش لهجه لبنانی است. کاربر درخواست کرده که سناریوهای زیر تست شوند: ۱) ارسال و دریافت نوتیفیکیشن، ۲) اجرای دستورات کنترلی، ۳) آپلود و مشاهده محتوای دروس، ۴) مکالمه متنی با استاد هوش مصنوعی، ۵) ارسال و دریافت پیام صوتی، ۶) همگام‌سازی دوطرفه پیام‌ها، ۷) مدیریت موضوعات. همچنین نکته حیاتی این است که خطاهای احتمالی مثل قطعی شبکه و حجم بالای فایل نیز تست شوند. خارج از این مرحله: تست هوش مصنوعی یا سرویس‌های خارجی. با توجه به commit history (کامیت‌های 856e24e و c12b712) که اشاره به 'میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار' دارند، این تسک ادامه همان کار است. در فایل backend/server.js خطوط 45-46 WebSocket server با path '/ws/live' تعریف شده که برای ارتباط دوطرفه استفاده می‌شود. همچنین در frontend/index.html خطوط 10-19 Firebase configuration برای پروژه 'labaneseapp' تنظیم شده که می‌تواند برای نوتیفیکیشن استفاده شود. کلیدواژه‌های کاربر: Integration Test, End-to-End Test, Error Handling, Network Failure, File Size Limit, User Acceptance Test.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. برای تست یکپارچه سیستم تلگرام، مراحل زیر باید انجام شود:
1. ایجاد فایل تست backend/tests/test_telegram_integration.js با سناریوهای تست کامل
2. تست WebSocket connection در backend/server.js خط 45 (wss = new WebSocketServer({ server, path: '/ws/live' })) با شبیه‌سازی قطعی شبکه
3. تست ارسال نوتیفیکیشن از طریق Firebase Cloud Messaging با استفاده از config در frontend/index.html خطوط 11-18
4. تست آپلود فایل با حجم بالا با استفاده از multer در backend/server.js خطوط 30-33 (limits: { fileSize: 500 * 1024 * 1024 })
5. تست مکالمه متنی با endpoint /api/gemini/chat در backend/server.js خطوط 56-114
6. تست ارسال و دریافت پیام صوتی با endpoint /api/gemini/tts در backend/server.js خطوط 117-164
7. تست همگام‌سازی دوطرفه پیام‌ها از طریق WebSocket
8. تست مدیریت موضوعات (topic management) از طریق API endpoints
9. شبیه‌سازی خطاهای شبکه و حجم بالای فایل برای اطمینان از handling مناسب

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: medium
- تخمین زمان: large

---

## ✅ معیارهای پذیرش کلی (همهٔ مراحل)
- [ ] همهٔ مراحل بالا با موفقیت پیاده‌سازی شده‌اند
- [ ] تست‌های موجود pass می‌شوند
- [ ] هیچ regression رخ نداده

## Acceptance Criteria

1. فایل backend/services/telegramService.js ایجاد شده و تابع sendVoiceMessage را export کند _(verify: static)_

## Task Steps

### Step 1: ایجاد ساختار پایه برای نوتیفیکیشن تلگرام دوطرفه
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک ماژول پایه برای ارتباط با تلگرام (Bot API) است. باید یک کلاس یا سرویس اصلی برای ارسال و دریافت پیام‌ها از طریق تلگرام ساخته شود. این مرحله فقط شامل راه‌اندازی Webhook یا Polling برای دریافت پیام‌ها و ارسال پاسخ‌های ساده است. خارج از این مرحله: پردازش محتوای دروس، مدیریت تمرین با استاد هوش مصنوعی، یا هرگونه منطق تجاری خاص. نکته حیاتی: باید از کتابخانه‌ای مثل python-telegram-bot یا aiogram استفاده شود و Webhook به‌درستی پیکربندی شود.
**Excerpt:**
```
میخوام برای این پروژه هم نوتیفیکیشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

### Step 2: پیاده‌سازی ارسال نوتیفیکیشن‌های سیستمی از طریق تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل اتصال ماژول تلگرام به رویدادهای سیستمی است. باید مشخص شود چه رویدادهایی (مثلاً اتمام یک پردازش، خطا، به‌روزرسانی وضعیت) باید نوتیفیکیشن تلگرامی دریافت کنند. یک سرویس Event Bus یا Observer Pattern برای انتشار رویدادها و ارسال خودکار پیام به تلگرام پیاده‌سازی می‌شود. خارج از این مرحله: ارسال محتوای دروس یا فایل‌های آموزشی. نکته حیاتی: باید از queue (مثل Redis یا Celery) برای جلوگیری از blocking استفاده شود.
**Excerpt:**
```
اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته
```

### Step 3: پیاده‌سازی دستورات کنترلی ساده از طریق تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل تعریف و پیاده‌سازی دستورات متنی ساده است که کاربر می‌تواند از طریق تلگرام به سیستم بفرستد. مثال: /status برای وضعیت سیستم، /restart برای راه‌اندازی مجدد یک سرویس، /help برای راهنما. این دستورات باید توسط هندلرهای مخصوص پردازش شده و عملیات مربوطه روی سیستم اجرا شود. خارج از این مرحله: دستورات مربوط به محتوای دروس یا تمرین با استاد هوش مصنوعی. نکته حیاتی: دستورات باید احراز هویت شوند تا فقط کاربر مجاز بتواند از آنها استفاده کند.
**Excerpt:**
```
بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

### Step 4: ایجاد ماژول مدیریت محتوای دروس از طریق تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک ماژول جدید برای مدیریت محتوای دروس (مشاهده و آپلود) از طریق تلگرام است. باید قابلیت‌های زیر پیاده‌سازی شود: ۱) دریافت لیست دروس موجود، ۲) مشاهده جزئیات یک درس (متن، فایل‌های ضمیمه)، ۳) آپلود فایل (PDF، تصویر، ویدئو) به عنوان محتوای جدید برای یک درس. این ماژول باید با دیتابیس موجود دروس ارتباط برقرار کند. خارج از این مرحله: مدیریت تمرین با استاد هوش مصنوعی. نکته حیاتی: باید محدودیت حجم فایل در تلگرام (۵۰ مگابایت) در نظر گرفته شود.
**Excerpt:**
```
بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم
```

### Step 5: اتصال ماژول تلگرام به بخش 'تمرین با استاد هوش مصنوعی'
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یکپارچگی بین ربات تلگرام و بخش 'تمرین با استاد هوش مصنوعی' است. باید یک API endpoint یا WebSocket connection برای تبادل پیام‌ها بین تلگرام و چت هوش مصنوعی ایجاد شود. کاربر باید بتواند از طریق تلگرام موضوعات تمرین را مشاهده، انتخاب و مدیریت کند. خارج از این مرحله: پیاده‌سازی خود هوش مصنوعی یا پردازش صوت. نکته حیاتی: باید state management برای هر کاربر در نظر گرفته شود تا مکالمات موازی مدیریت شوند.
**Excerpt:**
```
اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم
```

### Step 6: پیاده‌سازی ارسال و دریافت پیام صوتی از طریق تلگرام برای استاد هوش مصنوعی
**Status:** `pending` (0%)
**Scope:** این مرحله شامل قابلیت ارسال پیام صوتی از طریق تلگرام به استاد هوش مصنوعی است. کاربر باید بتواند یک پیام صوتی ضبط کرده و به ربات بفرستد. ربات باید فایل صوتی را دریافت کرده، به سرویس Speech-to-Text (STT) ارسال کند، متن استخراج شده را به هوش مصنوعی بفرستد، و پاسخ را به صورت متن و/یا صوتی (Text-to-Speech) به کاربر برگرداند. خارج از این مرحله: پردازش ویدئو یا سایر فرمت‌های چندرسانه‌ای. نکته حیاتی: باید از یک سرویس STT/TTS معتبر (مثل Google Cloud Speech-to-Text یا Whisper) استفاده شود.
**Excerpt:**
```
از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد
```

### Step 7: همگام‌سازی دوطرفه پیام‌ها بین چت وب و تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل همگام‌سازی پیام‌ها بین چت وب (بخش 'تمرین با استاد هوش مصنوعی') و تلگرام است. هر پیامی که در چت وب ارسال یا دریافت می‌شود باید در تلگرام نیز نمایش داده شود و بالعکس. باید از یک message queue یا WebSocket برای broadcast پیام‌ها استفاده شود. خارج از این مرحله: پردازش محتوای پیام‌ها. نکته حیاتی: باید از duplicate messages جلوگیری شود و ترتیب پیام‌ها حفظ شود.
**Excerpt:**
```
هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد
```

### Step 8: پیاده‌سازی مدیریت موضوعات تمرین از طریق تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل قابلیت مدیریت کامل موضوعات تمرین (Topics) از طریق تلگرام است. کاربر باید بتواند: ۱) موضوعات جدید ایجاد کند، ۲) موضوعات موجود را ویرایش کند، ۳) موضوعات را حذف کند، ۴) ترتیب موضوعات را تغییر دهد. این عملیات باید از طریق دستورات تلگرام و با تأیید (confirmation) انجام شود. خارج از این مرحله: مدیریت محتوای دروس. نکته حیاتی: باید دسترسی‌های امنیتی (فقط ادمین) برای این عملیات در نظر گرفته شود.
**Excerpt:**
```
بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم
```

### Step 9: پیاده‌سازی انتخاب موضوع تمرین از طریق تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل قابلیت انتخاب یک موضوع تمرین توسط کاربر از طریق تلگرام است. کاربر باید بتواند از لیست موضوعات، یکی را انتخاب کرده و session تمرین را شروع کند. انتخاب باید از طریق دکمه‌های Inline Keyboard در تلگرام انجام شود. خارج از این مرحله: خود مکالمه با هوش مصنوعی. نکته حیاتی: باید وضعیت کاربر (state) ذخیره شود تا بدانیم در کدام session قرار دارد.
**Excerpt:**
```
و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم
```

### Step 10: تست و دیباگ یکپارچه کل سیستم تلگرام
**Status:** `pending` (0%)
**Scope:** این مرحله شامل تست کامل تمام قابلیت‌های پیاده‌سازی شده در مراحل قبل است. باید سناریوهای زیر تست شوند: ۱) ارسال و دریافت نوتیفیکیشن، ۲) اجرای دستورات کنترلی، ۳) آپلود و مشاهده محتوای دروس، ۴) مکالمه متنی با استاد هوش مصنوعی، ۵) ارسال و دریافت پیام صوتی، ۶) همگام‌سازی دوطرفه پیام‌ها، ۷) مدیریت موضوعات. خارج از این مرحله: تست هوش مصنوعی یا سرویس‌های خارجی. نکته حیاتی: باید خطاهای احتمالی (مثل قطعی شبکه، حجم بالای فایل) نیز تست شوند.
**Excerpt:**
```
و تا حد زیادی کار رو از اینجا پیش ببرم ...
```
