---
task_id: d1d72037-600d-4197-9f99-a4bfcc43d938
title: نوشتن تست واحد برای APIهای Gemini و WebSocket
type: bug
priority: critical
execution_priority: 1000
status: done
external_status: pending
verification_status: done
watched_id: 6b04f8bd-b562-4fb9-9cd9-0c27458ced59
project: mahdighandi1989/language
created_at: '2026-05-16T07:41:06.916006+00:00'
updated_at: '2026-06-01T18:38:27.186774+00:00'
archived: true
archived_at: '2026-06-01T18:38:16.462720+00:00'
target_files:
- backend/server.js
- backend/server.js
- backend/server.js
---

# نوشتن تست واحد برای APIهای Gemini و WebSocket

## Raw Idea

فایل backend/server.js شامل منطق حیاتی پروژه است: APIهای Gemini (Chat، TTS، File Analysis)، WebSocket برای ارتباط زنده، و آپلود فایل. هیچ فایل تستی برای این ماژول وجود ندارد. این یک ریسک امنیتی و عملکردی بزرگ است، زیرا خطا در این فایل می‌تواند کل برنامه را از کار بیندازد. مسیرهای بحرانی مانند احراز هویت API Key، مدیریت خطاهای Gemini، و WebSocket connections بدون پوشش تست هستند.
---
[scan #2 at 2026-05-23T07:36:07.805031+00:00]
فایل backend/server.js در خط 400 قطع شده است (با [TRUNCATED at line 400 of 1403] مشخص شده). این فایل شامل APIهای مهمی مانند تحلیل فایل، آپلود، و پردازش است. با توجه به اینکه فایل 1403 خط دارد و ما فقط 400 خط اول را می‌بینیم، بخش‌های حیاتی مانند endpointهای /api/analyze-file، /api/upload، و WebSocket
---
[scan #3 at 2026-05-30T07:41:19.697678+00:00]
فایل backend/server.js با بیش از 1400 خط کد، تمام منطق اصلی backend (API endpoints, WebSocket, file analysis, Gemini integration, TTS) را در خود دارد. هیچ فایل تستی برای این فایل یا توابع آن وجود ندارد. این موضوع باعث می‌شود هر تغییری در این فایل ریسک بالایی داشته باشد و باگ‌ها در production کشف شون

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
فایل backend/server.js بدون تست واحد برای APIهای Gemini و WebSocket

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:56-114` — `app.post('/api/gemini/chat')` — Endpoint اصلی چت با Gemini - بدون تست
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
      ...
  ```
- `backend/server.js:117-164` — `app.post('/api/gemini/tts')` — Endpoint Text-to-Speech - بدون تست
  ```jsx
  app.post('/api/gemini/tts', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const { prompt, voice = 'Kore' } = req.body;
      ...
  ```
- `backend/server.js:45-45` — `const wss = new WebSocketServer({ server, path: '/ws/live' })` — WebSocket Server - بدون تست برای اتصال و پیام‌ها
  ```jsx
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Express.js + ws (WebSocket) + Gemini API + multer

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `frontend/src/App.jsx` (سطر 132) — فرانت‌اند از این APIها استفاده می‌کند
- `backend/package.json` (سطر 1) — وابستگی‌های پروژه backend

## 🌐 نقشهٔ وابستگی‌ها
این فایل مرکز تمام API calls و WebSocket connections است. تمام کامپوننت‌های فرانت‌اند به آن وابسته هستند.

## 🔍 Context و وضعیت فعلی
فایل backend/server.js شامل منطق حیاتی پروژه است: APIهای Gemini (Chat، TTS، File Analysis)، WebSocket برای ارتباط زنده، و آپلود فایل. هیچ فایل تستی برای این ماژول وجود ندارد. این یک ریسک امنیتی و عملکردی بزرگ است، زیرا خطا در این فایل می‌تواند کل برنامه را از کار بیندازد. مسیرهای بحرانی مانند احراز هویت API Key، مدیریت خطاهای Gemini، و WebSocket connections بدون پوشش تست هستند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] فایل backend/tests/server.test.js ایجاد شود
- [ ] تست برای /api/gemini/chat با و بدون API Key
- [ ] تست برای /api/gemini/tts با و بدون API Key
- [ ] تست برای WebSocket connection و پیام‌ها
- [ ] تست برای /api/health
- [ ] تست برای /api/list-models
- [ ] تست برای /api/test-gemini
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ایجاد فایل تست backend/tests/server.test.js با تست‌های واحد و یکپارچه‌سازی برای تمام API endpoints و WebSocket functionality.

## 💡 نمونه‌های قبل/بعد
**تست برای /api/gemini/chat**

_قبل:_
```
هیچ تستی وجود ندارد
```

_بعد:_
```
describe('POST /api/gemini/chat', () => {
  it('should return 500 if API key not configured', async () => {
    const res = await request(app).post('/api/gemini/chat').send({});
    expect(res.status).toBe(500);
    expect(res.body.error).toBe('API key not configured');
  });
  it('should return text response for valid request', async () => {
    // Mock Gemini API
    const res = await request(app).post('/api/gemini/chat').send({
      contents: [{ role: 'user', parts: [{ text: 'hello' }] }]
    });
    expect(res.status).toBe(200);
    expect(res.body).toHaveProperty('text');
  });
});
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `cd backend && npx jest tests/server.test.js --coverage`
- `cd backend && npx jest tests/server.test.js -t 'POST /api/gemini/chat'`

## ⚠️ ریسک‌ها و موارد احتیاط
نیاز به mocking دقیق Gemini API و WebSocket. ممکن است تست‌های یکپارچه‌سازی زمان‌بر باشند.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: bug
- اولویت: critical
- تخمین زمان: large

## Acceptance Criteria

1. فایل backend/tests/server.test.js ایجاد شود _(verify: static)_
2. تست برای /api/gemini/chat با و بدون API Key _(verify: backend_test)_
3. تست برای /api/gemini/tts با و بدون API Key _(verify: backend_test)_
4. تست برای WebSocket connection و پیام‌ها _(verify: backend_test)_
5. تست برای /api/health _(verify: backend_test)_
6. تست برای /api/list-models _(verify: backend_test)_
7. تست برای /api/test-gemini _(verify: backend_test)_

## Task Steps

### Step 1: نوشتن تست واحد برای endpoint /api/gemini/chat
**Status:** `done` (100%)
**Scope:** این مرحله شامل نوشتن تست‌های واحد برای endpoint POST /api/gemini/chat در فایل backend/server.js است. تست‌ها باید موارد زیر را پوشش دهند: 1) عدم وجود API key (بازگشت 500 با پیام خطای مناسب)، 2) ارسال payload معتبر و دریافت پاسخ موفق، 3) مدیریت خطاهای Gemini API (مثلاً timeout یا invalid response)، 4) حذف صحیح فیلد includeAudio از payload قبل از ارسال به Gemini. این مرحله شامل تست endpoint TTS، WebSocket یا آپلود فایل نمی‌شود. فایل تست باید در backend/tests/ ایجاد شود.
**Excerpt:**
```
backend/server.js:56-114 — app.post('/api/gemini/chat') — Endpoint اصلی چت با Gemini - بدون تست
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
      ...
```

### Step 2: نوشتن تست واحد برای endpoint /api/gemini/tts
**Status:** `done` (100%)
**Scope:** این مرحله شامل نوشتن تست‌های واحد برای endpoint POST /api/gemini/tts در فایل backend/server.js است. تست‌ها باید موارد زیر را پوشش دهند: 1) عدم وجود API key (بازگشت 500 با پیام خطای مناسب)، 2) ارسال prompt و voice معتبر و دریافت پاسخ موفق (فایل صوتی)، 3) مدیریت خطاهای Gemini TTS API، 4) استفاده از voice پیش‌فرض 'Kore' در صورت عدم ارسال voice. این مرحله شامل تست chat endpoint یا WebSocket نمی‌شود.
**Excerpt:**
```
backend/server.js:117-164 — app.post('/api/gemini/tts') — Endpoint Text-to-Speech - بدون تست
  ```jsx
  app.post('/api/gemini/tts', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const { prompt, voice = 'Kore' } = req.body;
      ...
```

### Step 3: نوشتن تست واحد برای WebSocket connections و پیام‌ها
**Status:** `done` (100%)
**Scope:** این مرحله شامل نوشتن تست‌های واحد برای WebSocket Server ایجاد شده در backend/server.js با مسیر /ws/live است. تست‌ها باید موارد زیر را پوشش دهند: 1) اتصال موفق به WebSocket، 2) ارسال و دریافت پیام، 3) مدیریت قطع اتصال، 4) رفتار در صورت عدم وجود API key. این مرحله شامل تست HTTP endpoints یا آپلود فایل نمی‌شود. تست‌ها باید با استفاده از کتابخانه ws یا شبیه‌ساز WebSocket نوشته شوند.
**Excerpt:**
```
backend/server.js:45-45 — const wss = new WebSocketServer({ server, path: '/ws/live' }) — WebSocket Server - بدون تست برای اتصال و پیام‌ها
  ```jsx
  const wss = new WebSocketServer({ server, path: '/ws/live' });
```

### Step 4: نوشتن تست واحد برای آپلود فایل (در صورت وجود)
**Status:** `done` (100%)
**Scope:** این مرحله شامل بررسی وجود endpoint آپلود فایل در backend/server.js و نوشتن تست‌های واحد برای آن است. اگر endpoint آپلود فایل وجود دارد، تست‌ها باید موارد زیر را پوشش دهند: 1) آپلود موفق فایل، 2) مدیریت خطاهای multer (مثلاً حجم زیاد فایل)، 3) عدم وجود فایل در request. اگر endpoint آپلود فایل وجود ندارد، این مرحله باید صرفاً یک تست placeholder ایجاد کند که نشان دهد این قابلیت وجود ندارد. این مرحله شامل تست Gemini endpoints یا WebSocket نمی‌شود.
**Excerpt:**
```
فایل backend/server.js شامل منطق حیاتی پروژه است: APIهای Gemini (Chat، TTS، File Analysis)، WebSocket برای ارتباط زنده، و آپلود فایل.
```

### Step 5: نوشتن تست واحد برای احراز هویت API Key در endpoints
**Status:** `done` (100%)
**Scope:** این مرحله شامل نوشتن تست‌های واحد برای بررسی احراز هویت API Key در تمام endpoints (chat, tts, file upload) است. تست‌ها باید موارد زیر را پوشش دهند: 1) عدم وجود GEMINI_API_KEY در environment variables (بازگشت 500 با پیام خطای مناسب)، 2) وجود API key معتبر (اجرای موفق endpoint)، 3) تغییر API key در runtime (در صورت امکان). این مرحله شامل تست خود Gemini API یا WebSocket نمی‌شود.
**Excerpt:**
```
مسیرهای بحرانی مانند احراز هویت API Key، مدیریت خطاهای Gemini، و WebSocket connections بدون پوشش تست هستند.
```

### Step 6: نوشتن تست واحد برای مدیریت خطاهای Gemini API
**Status:** `done` (100%)
**Scope:** این مرحله شامل نوشتن تست‌های واحد برای مدیریت خطاهای برگشتی از Gemini API در endpoints chat و tts است. تست‌ها باید موارد زیر را پوشش دهند: 1) timeout از سمت Gemini (بازگشت 503 با پیام خطای مناسب)، 2) invalid response از Gemini (مثلاً JSON نامعتبر)، 3) rate limiting از سمت Gemini (بازگشت 429)، 4) خطاهای داخلی Gemini (بازگشت 502). این مرحله شامل تست WebSocket یا آپلود فایل نمی‌شود.
**Excerpt:**
```
مدیریت خطاهای Gemini، و WebSocket connections بدون پوشش تست هستند.
```

### Step 7: نوشتن تست واحد برای WebSocket connections با خطاهای Gemini
**Status:** `done` (100%)
**Scope:** این مرحله شامل نوشتن تست‌های واحد برای مدیریت خطاهای Gemini در WebSocket connections است. تست‌ها باید موارد زیر را پوشش دهند: 1) قطع connection در صورت خطای Gemini، 2) ارسال پیام خطا به client، 3) تلاش مجدد برای اتصال (در صورت پیاده‌سازی). این مرحله شامل تست HTTP endpoints یا آپلود فایل نمی‌شود.
**Excerpt:**
```
WebSocket connections بدون پوشش تست هستند.
```

### Step 8: نوشتن تست واحد برای middleware و مسیریابی
**Status:** `done` (100%)
**Scope:** این مرحله شامل نوشتن تست‌های واحد برای middlewareهای استفاده شده در backend/server.js (مانند multer برای آپلود فایل، JSON body parser) و مسیریابی صحیح endpoints است. تست‌ها باید موارد زیر را پوشش دهند: 1) مسیریابی صحیح به endpoints مختلف، 2) عملکرد middlewareها (مثلاً parse کردن JSON body)، 3) خطاهای middleware (مثلاً invalid JSON). این مرحله شامل تست خود Gemini API یا WebSocket نمی‌شود.
**Excerpt:**
```
فایل backend/server.js شامل منطق حیاتی پروژه است: APIهای Gemini (Chat، TTS، File Analysis)، WebSocket برای ارتباط زنده، و آپلود فایل.
```
