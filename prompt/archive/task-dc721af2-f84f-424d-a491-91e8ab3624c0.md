---
task_id: dc721af2-f84f-424d-a491-91e8ab3624c0
title: پیاده‌سازی اتصال دوطرفه تلگرام
type: other
priority: medium
execution_priority: 3050
status: done
external_status: done
verification_status: done
watched_id: 6b04f8bd-b562-4fb9-9cd9-0c27458ced59
project: mahdighandi1989/language
created_at: '2026-05-12T11:13:22.960230+00:00'
updated_at: '2026-06-03T04:15:28.402229+00:00'
archived: true
archived_at: '2026-06-02T21:41:46.712089+00:00'
target_files:
- backend/server.js
- backend/.env.example
- backend/package.json
- frontend/index.html
- backend/server
- frontend/src/App.jsx
- docs/telegram-user-guide.md
- README.md
---

# پیاده‌سازی اتصال دوطرفه تلگرام

## Raw Idea

میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم و از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

📖 **خواندن کامل + اجرای مو-به-مو (بسیار مهم):**

این پرامپت — از این یادداشت تا انتها — یک سند واحد است که هر بخشش
حاوی الزام یا context منحصربه‌فرد است. خواندن سطحی یا skim کردن **ممنوع**
است.

- پرامپت را **سطر به سطر** بخوان، نه head/tail/فقط-بخش-اصلی.
- اگر بخشی به‌نظر طولانی یا تکراری آمد، **حتماً** بخوان — تفاوت‌های
  ریز ممکن است در آن جا اساسی باشند.
- هر جمله، URL، نام فایل، نام تابع، یا مقدار عددی که در پرامپت آمده،
  دقیقاً همان است که کاربر می‌خواهد — تغییرش نده، رندش نکن، خلاصه‌اش
  نکن.
- اگر پرامپت چندین درخواست/مرحله/زیرتسک دارد، **همه** را پیاده کن. حتی
  یکی را نه به‌عنوان "خارج از scope" حذف کن.

❌ ممنوعات صریح:
- خلاصه‌سازی متن کاربر در commit message یا response
- "این بخش اصلی نیست، رد می‌کنم"
- "کاربر احتمالاً منظورش این بود..." — منظورش همان است که نوشته
- "این URL/نام به نظر قدیمی است، آپدیتش کردم" — تغییر بدون درخواست ممنوع
- پیاده‌سازی فقط بخشی از پرامپت و تظاهر به کامل بودن
- "همه آیتم‌های لیست A را بررسی کردم، B و C مشابه بودند" — نه؛
  هرکدام را جداگانه

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

🔗 **وابستگی‌ها و همگام‌سازی (بسیار حیاتی — هرگز skip نکن):**

این بخش از همهٔ بخش‌های دیگرِ این یادداشت **مهم‌تر** است. اگر نقض شود،
نتیجهٔ کار ممکن است مشروع به‌نظر برسد ولی در عمل بخش‌های دیگر سیستم را عقب
بیندازد، broken reference تولید کند، یا منجر به data corruption شود.

پیش از و حین تغییر، تمام وابستگی‌ها را در **چهار جهت** به‌طور **کامل و
بدون هیچ خلاصه‌سازی** شناسایی و همگام کن:

**۱. وابستگی‌های upstream (این تسک به چه چیزهایی متکی است):**
- چه فایل‌ها، توابع، کلاس‌ها، API endpoint ها، schema های دیتابیس،
  env vars، یا config هایی که این تسک نیاز دارد؟
- آیا قرار است چیزی را ویرایش/حذف کنی که جای دیگر (signature، رفتار،
  return type، side effect) از آن انتظار خاصی می‌رود؟
- اگر dependency جدیدی اضافه می‌کنی، آیا با dependencyهای موجود تداخل
  دارد (نسخه، compat، lock file)؟

**۲. وابستگی‌های downstream (چه چیزهایی به این تسک متکی‌اند):**
- چه فایل‌ها، توابع، تست‌ها، migrations، docs، یا UI component هایی از
  کدی که داری ویرایش/اضافه/حذف می‌کنی **استفاده می‌کنند**؟
- با grep و reference search **همه‌ی** call sites، importها، subclassها،
  reference های مستقیم و غیرمستقیم را پیدا کن — نه فقط چند مورد اصلی.
- خصوصاً برای حذف یا rename: هیچ broken reference نباید باقی بماند.

**۳. وابستگی‌های cross-tier (بسیار مهم — هرگز فقط یک لایه را نبین):**

تسک شما ممکن است از backend، frontend، database، worker، یا هر tier
دیگری شروع شده باشد. ولی تغییرات تقریباً همیشه روی tier های دیگر هم
اثر می‌گذارند. **مستقل از اینکه تسک از کدام tier است**، این چک‌های دو
طرفه را همیشه انجام بده:

🔁 **اگر backend را تغییر دادی** (API، service، model، route):
  → frontend: کدام component/page/hook این endpoint یا data shape را
    مصرف می‌کند؟ type definition، state shape، error handling، loading
    state، form validation، URL routing همگی باید همگام شوند.
  → mobile/SDK/client library (اگر پروژه دارد): همان داستان frontend.
  → database: آیا migration لازم است؟ آیا rollback امن است؟
  → background workers: آیا event producer/consumer ها تحت تأثیرند؟
  → rate limit، auth، CORS، CSP: آیا رفتار جدید پشتیبانی می‌شود؟

🔁 **اگر frontend را تغییر دادی** (component، form، state، route):
  → backend: آیا endpoint جدید/تغییریافته لازم است؟ آیا data shape ای
    که ارسال می‌شود با schema سرور سازگار است؟
  → backend validation: آیا برای ورودی‌های جدید UI کافی است؟
  → permissions/RBAC: آیا feature جدید نیاز به role check جدید دارد؟
  → analytics/tracking: آیا event های جدید باید در backend log شوند؟
  → SEO/SSR: آیا تغییر route نیاز به sitemap/meta tags جدید دارد؟

🔁 **اگر database/migration را تغییر دادی**:
  → backend models (ORM، Pydantic، dataclasses) همگی به‌روزند؟
  → query های raw SQL یا ORM queries با schema جدید سازگارند؟
  → seed data، fixtures، factory functions تست‌ها به‌روزند؟
  → frontend: آیا data shape جدید در UI به‌درستی render می‌شود؟
  → rollback migration نوشته شده و امن است؟

🔁 **اگر API contract یا event schema را تغییر دادی** (REST، GraphQL،
   WebSocket، gRPC، Kafka، …):
  → OpenAPI/GraphQL schema/proto file آپدیت شد؟
  → همه‌ی consumer ها (client، subscriber، webhook، external API
    user) با version جدید سازگارند؟
  → backward compatibility حفظ شده یا migration path روشن است؟
  → versioning header/path اگر breaking change است؟

🔁 **اگر infrastructure یا config را تغییر دادی** (Dockerfile، CI، Render
   config، env، secrets):
  → README setup/installation section به‌روزه؟
  → `.env.example` با env vars جدید آپدیت شد؟
  → deploy script یا CI workflow هم تغییر کرد؟
  → docs/architecture یا diagram های infrastructure به‌روزند؟

⚠️ **هرگز فقط یک tier را تغییر نده و فرض کنی بقیه خودکار همگام می‌شوند.**
   حتی برای تغییرات به‌ظاهر «کوچک»، چک کن.

**۴. وابستگی‌های جانبی (artifacts که همیشه چک شوند):**

تغییرات کد همیشه روی این artifact ها اثر دارند. **همه را** بررسی و
به‌روز کن — مستندات اولویت **بالا** دارد چون فراموش‌شدنی‌ترین است.

  📝 **مستندات** (همیشه چک کن — حتی برای تغییر کوچک کد):
    - README.md (شرح، setup، نمونه‌های استفاده، badge ها)
    - CHANGELOG.md / RELEASE_NOTES.md
    - docs/ folder (architecture، API reference، user guides، runbooks)
    - inline docstrings/کامنت‌های توابع و کلاس‌های تغییریافته
    - OpenAPI/Swagger annotations، JSDoc/TSDoc
    - architecture diagrams (اگر component اضافه/حذف شد)
    - migration guides (اگر breaking change است)

  🌍 **مستندات کاربر**:
    - i18n files و translation keys
    - UI labels، tooltip ها، help text، error messages
    - in-app onboarding (اگر flow جدید است)

  🧪 **تست‌ها**:
    - unit tests (همه‌ی فایل‌های مرتبط — حتی اگر «بی‌ربط» به‌نظر می‌رسد)
    - integration tests
    - e2e tests (Playwright/Cypress/Selenium)
    - snapshot tests (اگر UI تغییر کرد)
    - contract tests (Pact یا مشابه)
    - performance benchmarks (اگر behavior performance-sensitive تغییر کرد)

  🧬 **type definitions و contracts**:
    - .d.ts files
    - Pydantic models، dataclasses
    - Protobuf/Avro/Thrift schemas
    - GraphQL schema definitions
    - JSON Schemas

  🏗 **infrastructure و config**:
    - Dockerfile، docker-compose.yml
    - Kubernetes manifests
    - Render/Vercel/Netlify config
    - GitHub Actions / GitLab CI workflows
    - environment templates (.env.example، .env.sample)
    - feature flags (LaunchDarkly، GrowthBook، config)

  📊 **monitoring و observability**:
    - logging keys (اگر اضافه/حذف شد، log parser ها هم به‌روز شوند)
    - metric names (Prometheus، Datadog)
    - tracing spans
    - alert rules و dashboards
    - error tracking (Sentry rules، groupings)

  🔐 **security**:
    - auth rules (rate limit، CORS، CSP، HSTS)
    - permissions/RBAC config
    - secrets rotation policies
    - audit log events (اگر action جدید اضافه شد)

  💾 **caches و serialization**:
    - cache keys و TTL (اگر data shape یا lifecycle تغییر کرد)
    - serializer formats (Redis، session storage)
    - browser storage (localStorage، IndexedDB schemas)

**قانون مطلق همگام‌سازی:**
- هر چیزی که در (۱)، (۲)، (۳)، یا (۴) شناسایی شد، در **همان workflow
  این تسک** همگام و به‌روز شود. هرگز برای بعد رها نکن.
- اگر یک فایل/تست/docs نسبت به تغییر شما عقب بماند، در بهترین حالت bug،
  در بدترین حالت مشکل امنیتی یا data corruption تولید می‌کند.
- تغییرات همگام‌سازی می‌توانند در commit جداگانه باشند (در همان task)،
  ولی نباید skip شوند یا به «refactor آینده» سپرده شوند.

**هرگز این جمله‌ها قابل قبول نیست:**
- ❌ «بعداً پیداش می‌کنم»
- ❌ «احتمالاً جای دیگه‌ای استفاده نمی‌شه»
- ❌ «این یه refactor جداگانه‌ست — out of scope»
- ❌ «فقط فایل‌های اصلی رو بررسی کردم»
- ❌ «حدس می‌زنم چیزی بهش وابسته نیست»
- ❌ «دامنه‌ی وابستگی‌ها رو خلاصه کردم» — هرگز خلاصه نکن
- ❌ «این task فقط backend است؛ frontend مشکل خودش» — هرگز
- ❌ «این task فقط frontend است؛ backend از قبل کار می‌کند» — هرگز ثابت نکرده
- ❌ «مستندات بعداً به‌روز می‌شن» — همیشه same-task همگام شوند
- ❌ «testها رو نگاه نکردم چون فقط یه تغییر کوچیک بود»

**در commit message یا PR description**، دامنهٔ وابستگی‌های شناسایی‌شده و
همگام‌شده را به‌طور explicit و **per-tier** بنویس. مثال:
```
Dependencies synced:
- upstream: User model schema, auth middleware
- downstream: 3 API endpoints, 5 frontend components, 12 tests
- cross-tier (backend → frontend): UserProfile.tsx, useUser.ts hook,
  api-types.ts (TS definitions)
- cross-tier (backend → infra): .env.example added NEW_AUTH_SCOPES
- side artifacts: OpenAPI spec, README API section, i18n keys for
  new errors, Sentry alert rule for new error code
```
اگر هیچ وابستگی پیدا نکردی در هر کدام از چهار جهت، صریحاً بنویس:
«بررسی شد — هیچ وابستگی upstream / downstream / cross-tier (backend↔
frontend↔db↔infra) / side شناسایی نشد» تا مشخص باشد بررسی **انجام شده**
نه اینکه فراموش شده.

📋 **مدیریت TO-DO برای اقدامات دستی کاربر (همیشه چک کن):**

⚠️ **هشدار بحرانی — قاعدهٔ ضد-فرار:** TO-DO فقط برای کارهایی است که
**واقعاً غیرممکن** برای agent است (نیاز به انسان مطلق)، نه برای کارهایی
که «بزرگ‌اند»، «وقت می‌برند»، یا «نیازمند fixture/setup» هستند. اگر یک
agent در یک سشن بیش از **۲۰٪ از تسک‌ها** را با TO-DO ببندد، یعنی از کار
فرار می‌کند — این الگو در سشن‌های قبلی **مشاهده** شده و الان ممنوع است.

✅ **فقط برای این موارد TO-DO بساز** (لیست بسته — هرچه خارج این لیست
ممنوع است):

  ۱. **Credential/secret که فقط کاربر دارد**:
     - تنظیم API key واقعی در پنل ادمین خارجی (Render، AWS، Stripe، …)
     - تأیید OAuth client روی console آن سرویس
     - paste کردن webhook secret که فقط بعد از ساخت در dashboard ظاهر می‌شود

  ۲. **Account/billing روی سرویس خارجی که کاربر باید عضو شود**:
     - ساخت account جدید روی Stripe/SendGrid/Twilio/Google Cloud
     - تأیید verification شماره یا ID
     - فعال‌سازی subscription پولی

  ۳. **داده/asset خصوصی که فقط کاربر دارد**:
     - آپلود لوگو/تصویر/فونت برند
     - paste کردن داده‌ای که در محل کار کاربر است
     - import داده‌ای که فقط روی device کاربر است

  ۴. **تصمیم سلیقه‌ای/حقوقی/کسب‌وکار**:
     - انتخاب رنگ‌بندی نهایی یا تم
     - متن دقیق Terms of Service / Privacy Policy
     - تعرفهٔ قیمت‌گذاری
     - نام نهایی برند یا دامنه

⛔ **هرگز TO-DO نکن برای** (لیست سیاه — هر چیزی که در این لیست است
**قابل اجرا** توسط agent است، حتی اگر بزرگ یا چندبخشی باشد):

  ❌ UI component / page / dashboard (هر فریم‌ورک: React, Vue, Angular,
     Svelte، حتی اگر معماری بزرگ دارد) — می‌توانی stub اولیه + state
     management + layout + استایل بسازی
  ❌ "نیازمند Google Drive / Stripe / Twilio API" — می‌توانی **client
     stub** با abstraction layer بسازی که با env var واقعی plug-in شود؛
     کد integration یعنی پیاده‌سازی، نه TO-DO
  ❌ "feature بزرگ، چند روز کار می‌برد" — اندازه دلیل defer نیست؛ کوچک
     شروع کن، iterate کن، در همین سشن کامل کن
  ❌ Celery / background worker / scheduler — یک task ساده + register
     می‌توانی بسازی
  ❌ Migration / model / schema — حتی اگر فیلد جدید نیاز دارد، اضافه کن
  ❌ REST endpoint / GraphQL resolver / WebSocket route — هرگز TO-DO
  ❌ test (unit/integration/e2e) — همیشه قابل نوشتن
  ❌ Documentation / README / API docs — همیشه قابل نوشتن
  ❌ Config file / .env.example / Dockerfile / CI workflow — همیشه قابل
     نوشتن
  ❌ "می‌توانستی .tsx ولی repo .jsx است" — از .jsx استفاده کن، TO-DO نکن
  ❌ "نیازمند فیلد X در مدل دیگر" — اضافه کن فیلد را، TO-DO نکن
  ❌ "تصمیم admin-vs-user-scoped" — پرامپت اولیه scope را معلوم کرده،
     یا با محتاطانه‌ترین تفسیر پیش برو
  ❌ "credential در production هنوز ست نیست" — این TO-DO ساده برای
     تنظیم env var است (مورد ۱ بالا)، نه دلیل برای defer کردن کد
  ❌ "نیازمند verification از کاربر" — اگر اقدام واقعی غیرممکن نیست،
     پیش برو
  ❌ هر چیزی که در یک کامنت `# TODO` معمولی نوشته می‌شد — این فایل
     TO-DO نیست، کامنت inline است

🔬 **قاعدهٔ «حداقل تلاش» قبل از TO-DO**: قبل از TO-DO کردن یک AC، **اثبات
کن** که قابل انجام نیست:

  ۱. آیا می‌توانم یک stub/placeholder بسازم که با env واقعی plug-in شود؟
     → اگر بله، بساز و TO-DO نکن
  ۲. آیا می‌توانم برای این بخش یک test (حتی mock-based) بنویسم؟
     → اگر بله، بنویس و TO-DO نکن
  ۳. آیا می‌توانم abstraction/interface را تعریف کنم، حتی اگر backend
     واقعی نیست؟ → اگر بله، تعریف کن و TO-DO نکن
  ۴. آیا فقط یک حالت سلیقه‌ای/decision کاربر در میان است؟
     → فقط آن یک decision را TO-DO کن، نه کل feature را

اگر یکی از این چهار راه‌حل ممکن بود ولی به TO-DO رفتی، **اعتبار شما از
بین می‌رود**.

📊 **آستانهٔ TO-DO per session**: در یک حلقهٔ اجرای N تسک، اگر بیشتر از
**۲۰٪** تسک‌ها فایل TO-DO ساختی، خودت در گزارش پایانی صریحاً اعلام کن:

  "⚠️ نسبت TO-DO من {K}/{N} = {%} است که از آستانهٔ ۲۰٪ بالاتر است.
   احتمالاً برخی از این TO-DO ها قابل اجرا بودند ولی من فرار کردم.
   لیست TO-DO ها را کاربر باید بازبینی کند که آیا واقعاً Manual-required
   بودند یا agent ضعیف کار کرده."

**یادآوری همیشگی:** اگر در آینده قابلیت‌های شما گسترش پیدا کرد و توانستید
یکی از موارد لیست سفید را خودکار انجام دهید (مثلاً managed credential
injection، یا integration پولی automate شود)، انجام دهید و TO-DO نسازید.
لیست سفید بسته است ولی **بسته از پایین** (می‌تواند کوچک‌تر شود اگر
قابلیت‌ها رشد کنند، ولی هرگز بزرگ‌تر نشود برای فرار).

**اگر هیچ بخش Manual-required نبود (تمام تسک Auto-capable است)**:
  → فایل TO-DO **نساز**. فولدر TO-DO/ باید پاک و معنادار بماند.
  → اگر برای این task از قبل `TO-DO/todo-task-{task_id_first_8}.md` بود
     (یعنی در run قبلی نیاز به دخالت کاربر بود ولی الان نه): فایل قدیمی
     را پاک کن و entry را از `TO-DO/_index.json` حذف کن.

**اگر بخش Manual-required دارد** (همه‌جانبه یا hybrid):
  1. فولدر TO-DO/ را در ریشه ریپو ایجاد کن اگر نیست
  2. فایل `TO-DO/todo-task-{task_id_first_8}.md` بساز با front-matter
     شامل: task_id, task_title, execution_priority, created_at,
     updated_at, status: "pending"
     و در بدنه: «چرا این فایل ساخته شد»، «وضعیت بخش‌های خودکار»
     (commit ها reference)، «کارهایی که باید انجام دهی» با اولویت
     بالا/متوسط/پایین به ترتیب، «وقتی این کارها را تمام کردی»
  3. `TO-DO/_index.json` را با **merge** آپدیت کن (نه overwrite):
     - فایل موجود را بخوان
     - entry های orphan (فایلشان پاک شده) را حذف کن
     - entry این task را اضافه/replace کن
     - بر اساس execution_priority صعودی مرتب کن
     - ساختار: `{"version":1, "generated_at": ISO, "total": N, "items": [...]}`
  4. این تغییرات TO-DO را در **همان commit کد** شامل کن (نه commit جداگانه)

⛔ **ممنوعات مطلق TO-DO**:
  ❌ ساختن TO-DO برای کاری که می‌توانستی خودت انجام دهی (شلوغی فولدر)
  ❌ overwrite کردن `TO-DO/_index.json` بدون merge (data loss)
  ❌ نگه‌داشتن entry هایی که فایل‌شان پاک شده (broken reference)
  ❌ فراموش کردن نوشتن «خروجی مورد انتظار» در هر آیتم TO-DO

این بخش الزامی است. حتی اگر فکر می‌کنی "این تسک کاملاً auto است و نیازی
به TO-DO نیست"، صریحاً در commit message یا report بنویس:
"بررسی شد — این تسک هیچ بخش Manual-required ندارد، TO-DO ساخته نشد."

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

🔁 **Commit + Push فوری per-task (بسیار مهم برای جریان کار صحیح):**

پس از اتمام پیاده‌سازی این تسک، **بلافاصله** commit کن و **همان موقع**
به default branch (main/master) push کن. سپس به تسک بعدی برو.

✓ چرا این قانون حیاتی است:
  - تسک‌های بعدی ممکن است به فایل‌ها/تغییراتی که این تسک ایجاد کرده
    نیاز داشته باشند. اگر push نکنی، `git pull` بعدی آن‌ها را نمی‌بیند.
  - جمع‌کردن تغییرات چند تسک منجر به conflict های بزرگ می‌شود.
  - اگر در میانه fail کنی، task های push شده ضایع نمی‌شوند.

⛔ ممنوع: "همه task ها را تمام می‌کنم بعد یک‌جا push می‌زنم"
⛔ ممنوع: branch جدا برای task — مستقیم به default branch
⛔ ممنوع: task بعدی بدون push کامل task قبلی

---


---

## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند.)_

```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم و از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد ....
```

## 📋 چک‌لیست مراحل (25 مرحله)

این تسک به مراحل کوچک‌تر تقسیم شده. **در هر verify خودکار، وضعیت هر مرحله به‌صورت `[ ]` (انجام نشده)، `[~]` (ناقص)، یا `[x]` (انجام شده) به‌روز می‌شود.**
وقتی تمام مراحل `[x]` شدند، تسک به‌طور خودکار به «انجام شده» منتقل می‌شود.

- [x] **مرحله 1: ایجاد ساختار پایه برای اتصال تلگرام دوطرفه (Bot API + Webhook)** — این مرحله شامل ایجاد یک سرویس تلگرام با استفاده از python-telegram-bot یا aiogram است که قابلیت دریافت و ارسال پیام را دارد. باید webhook یا polling برای دریافت پیام‌های کاربر تنظیم شود. این مرحله فقط زیرساخت ارتباطی را فراهم می‌کند و شامل هیچ منطق تجاری یا دستور خاصی نیست. نکته حیاتی: حتماً از asyn
- [x] **مرحله 2: پیاده‌سازی سیستم نوتیفیکیشن تلگرام برای رویدادهای سیستم** — این مرحله شامل ایجاد مکانیزمی است که رویدادهای مهم سیستم (مثلاً ثبت‌نام جدید، تکمیل تمرین، خطا) را به کاربر از طریق تلگرام ارسال کند. باید یک سرویس NotificationService بسازد که از طریق TelegramService پیام ارسال کند. خارج از این مرحله: تعیین اینکه چه رویدادهایی باید نوتیفیکیشن بفرستند (بعداً مشخص می
- [x] **مرحله 3: پیاده‌سازی دستورات پایه کنترل سیستم از طریق تلگرام (Command Handlers)** — این مرحله شامل تعریف و پیاده‌سازی دستورات پایه تلگرام مانند /status (وضعیت سیستم)، /help (راهنما)، /courses (لیست دروس) است. باید یک CommandHandler برای هر دستور ایجاد شود که منطق مربوطه را اجرا کند. خارج از این مرحله: دستورات پیچیده مانند آپلود فایل یا مدیریت تمرین با استاد. نکته حیاتی: دستورات بای
- [x] **مرحله 4: ایجاد سیستم احراز هویت و اتصال کاربر تلگرام به حساب کاربری** — این مرحله شامل ایجاد مکانیزمی است که کاربر تلگرام را به حساب کاربری موجود در سیستم متصل کند. باید یک دستور /login یا /verify ایجاد شود که کاربر یک کد یکتا دریافت کند و در وبسایت وارد کند. همچنین باید mapping بین chat_id تلگرام و user_id سیستم ذخیره شود. خارج از این مرحله: احراز هویت دو مرحله‌ای یا O
- [x] **مرحله 5: ایجاد دستور /courses برای نمایش لیست دروس و وضعیت آنها** — این مرحله شامل توسعه دستور /courses است که لیست تمام دروس موجود در سیستم را با وضعیت هر کدام (فعال/غیرفعال، تعداد جلسات، آخرین به‌روزرسانی) نمایش دهد. باید از دیتابیس خوانده شود و به صورت فرمت‌شده در تلگرام نمایش داده شود. خارج از این مرحله: امکان تغییر وضعیت دروس از تلگرام. نکته حیاتی: پاسخ باید خل
- [x] **مرحله 6: ایجاد دستور /course_content برای مشاهده محتوای یک درس خاص** — این مرحله شامل ایجاد دستور /course_content <course_id> است که محتوای یک درس خاص (لیست جلسات، فایل‌ها، تمرین‌ها) را نمایش دهد. باید از دیتابیس یا storage محتوای درس را بخواند و به صورت دسته‌بندی‌شده نمایش دهد. خارج از این مرحله: آپلود محتوا. نکته حیاتی: محتوا ممکن است حجیم باشد، بنابراین باید صفحه‌بن
- [x] **مرحله 7: ایجاد دستور /upload برای آپلود محتوای دروس از طریق تلگرام** — این مرحله شامل ایجاد دستور /upload <course_id> است که کاربر می‌تواند یک فایل (PDF، تصویر، ویدئو) را به عنوان محتوای درس آپلود کند. باید فایل را از تلگرام دریافت کرده، در storage ذخیره کند و متادیتا را در دیتابیس ثبت کند. خارج از این مرحله: پردازش فایل (OCR، تبدیل). نکته حیاتی: محدودیت حجم فایل تلگرا
- [x] **مرحله 8: ایجاد دستور /delete_content برای حذف محتوای دروس از تلگرام** — این مرحله شامل ایجاد دستور /delete_content <course_id> <content_id> است که کاربر می‌تواند یک محتوای خاص را از درس حذف کند. باید از دیتابیس و storage حذف شود. خارج از این مرحله: حذف دسته‌جمعی. نکته حیاتی: باید تأییدیه (confirmation) از کاربر گرفته شود قبل از حذف.
- [x] **مرحله 9: ایجاد دستور /edit_course برای ویرایش متادیتای دروس از تلگرام** — این مرحله شامل ایجاد دستور /edit_course <course_id> است که کاربر می‌تواند متادیتای درس (نام، توضیحات، وضعیت) را ویرایش کند. باید از طریق مراحل (step-by-step) با کاربر تعامل کند. خارج از این مرحله: ویرایش محتوای دروس. نکته حیاتی: باید validation روی ورودی‌ها انجام شود.
- [x] **مرحله 10: ایجاد دستور /add_course برای افزودن درس جدید از تلگرام** — این مرحله شامل ایجاد دستور /add_course است که کاربر می‌تواند یک درس جدید با نام، توضیحات و سایر فیلدها ایجاد کند. باید از طریق مراحل با کاربر تعامل کند و در دیتابیس ذخیره کند. خارج از این مرحله: افزودن محتوا به درس جدید. نکته حیاتی: باید بررسی شود که درس تکراری نباشد.
- [x] **مرحله 11: ایجاد دستور /delete_course برای حذف درس از تلگرام** — این مرحله شامل ایجاد دستور /delete_course <course_id> است که کاربر می‌تواند یک درس و تمام محتوای آن را حذف کند. باید تأییدیه دو مرحله‌ای بگیرد و سپس درس و محتواهای مرتبط را از دیتابیس و storage حذف کند. خارج از این مرحله: حذف انتخابی محتوا. نکته حیاتی: این عملیات غیرقابل بازگشت است و باید هشدار داده
- [x] **مرحله 12: ایجاد دستور /practice برای اتصال به بخش 'تمرین با استاد هوش مصنوعی'** — این مرحله شامل ایجاد دستور /practice است که کاربر را به بخش تمرین با استاد هوش مصنوعی متصل می‌کند. باید وضعیت فعلی جلسات تمرین را نمایش دهد و گزینه‌هایی برای شروع جلسه جدید یا ادامه جلسه قبلی ارائه دهد. خارج از این مرحله: خود مکالمه با هوش مصنوعی. نکته حیاتی: باید از API موجود در بخش 'تمرین با استاد
- [x] **مرحله 13: ایجاد دستور /practice_topic برای انتخاب موضوع تمرین از تلگرام** — این مرحله شامل ایجاد دستور /practice_topic <session_id> است که کاربر می‌تواند موضوع تمرین را برای یک جلسه انتخاب کند. باید لیست موضوعات موجود را از بخش AI دریافت کرده و به کاربر نمایش دهد. کاربر با انتخاب یک موضوع، آن را برای جلسه تنظیم می‌کند. خارج از این مرحله: ایجاد موضوع جدید. نکته حیاتی: موضوعا
- [x] **مرحله 14: ایجاد دستور /practice_chat برای شروع مکالمه با هوش مصنوعی از طریق تلگرام** — این مرحله شامل ایجاد دستور /practice_chat <session_id> است که کاربر را وارد حالت مکالمه با هوش مصنوعی می‌کند. در این حالت، تمام پیام‌های کاربر به API هوش مصنوعی ارسال شده و پاسخ دریافت می‌شود. خارج از این مرحله: پردازش پیام صوتی. نکته حیاتی: باید state management داشته باشد تا بداند کاربر در کدام جل
- [x] **مرحله 15: پیاده‌سازی قابلیت ارسال پیام صوتی به هوش مصنوعی از تلگرام** — این مرحله شامل اضافه کردن قابلیت دریافت پیام صوتی از کاربر در حالت مکالمه با هوش مصنوعی است. باید فایل صوتی را از تلگرام دریافت کرده، به سرویس Speech-to-Text ارسال کند، متن استخراج‌شده را به API هوش مصنوعی بفرستد و پاسخ را دریافت کند. خارج از این مرحله: پاسخ صوتی. نکته حیاتی: باید از میکروفون تلگرام
- [x] **مرحله 16: پیاده‌سازی قابلیت دریافت پاسخ صوتی از هوش مصنوعی در تلگرام** — این مرحله شامل اضافه کردن قابلیت ارسال پاسخ صوتی از هوش مصنوعی به کاربر است. باید متن پاسخ هوش مصنوعی را به سرویس Text-to-Speech بفرستد، فایل صوتی حاصل را در تلگرام آپلود کند و برای کاربر ارسال کند. خارج از این مرحله: انتخاب صدای مختلف. نکته حیاتی: باید هم پاسخ صوتی و هم متن پاسخ به صورت همزمان ارسا
- [x] **مرحله 17: ایجاد دستور /practice_end برای پایان جلسه تمرین از تلگرام** — این مرحله شامل ایجاد دستور /practice_end <session_id> است که کاربر می‌تواند جلسه تمرین را پایان دهد. باید خلاصه جلسه (تعداد پیام‌ها، موضوع، مدت زمان) را از API هوش مصنوعی دریافت کرده و برای کاربر ارسال کند. خارج از این مرحله: ذخیره تاریخچه. نکته حیاتی: باید state کاربر را به حالت عادی برگرداند.
- [x] **مرحله 18: ایجاد دستور /practice_history برای مشاهده تاریخچه جلسات تمرین از تلگرام** — این مرحله شامل ایجاد دستور /practice_history است که کاربر می‌تواند تاریخچه جلسات تمرین خود را مشاهده کند. باید از دیتابیس یا API هوش مصنوعی تاریخچه را دریافت کرده و به صورت لیست نمایش دهد. خارج از این مرحله: مشاهده جزئیات هر جلسه. نکته حیاتی: باید صفحه‌بندی شود.
- [x] **مرحله 19: ایجاد دستور /practice_details برای مشاهده جزئیات یک جلسه تمرین خاص** — این مرحله شامل ایجاد دستور /practice_details <session_id> است که کاربر می‌تواند جزئیات یک جلسه خاص (موضوع، تعداد پیام‌ها، خلاصه) را مشاهده کند. باید از API هوش مصنوعی یا دیتابیس اطلاعات را دریافت کند. خارج از این مرحله: مشاهده متن کامل مکالمه. نکته حیاتی: باید خلاصه‌ای از مکالمه ارائه دهد.
- [x] **مرحله 20: ایجاد دستور /settings برای مدیریت تنظیمات نوتیفیکیشن و تلگرام** — این مرحله شامل ایجاد دستور /settings است که کاربر می‌تواند تنظیمات نوتیفیکیشن (فعال/غیرفعال کردن انواع نوتیفیکیشن‌ها) و تنظیمات عمومی تلگرام را مدیریت کند. باید از طریق دکمه‌های inline keyboard تعامل انجام شود. خارج از این مرحله: تنظیمات پیشرفته. نکته حیاتی: تنظیمات باید در دیتابیس ذخیره شود.
- [x] **مرحله 21: ایجاد دستور /help برای نمایش راهنمای کامل دستورات تلگرام** — این مرحله شامل به‌روزرسانی دستور /help است تا تمام دستورات جدید (courses, upload, practice, settings و ...) را به صورت دسته‌بندی‌شده نمایش دهد. باید با inline keyboard یا Markdown فرمت شود. خارج از این مرحله: راهنمای پیشرفته. نکته حیاتی: باید بر اساس سطح دسترسی کاربر، دستورات متفاوت نمایش دهد.
- [x] **مرحله 22: پیاده‌سازی سیستم لاگینگ و مانیتورینگ برای ربات تلگرام** — این مرحله شامل اضافه کردن لاگینگ برای تمام تعاملات ربات تلگرام (دستورات، خطاها، زمان پاسخ) است. باید لاگ‌ها در فایل یا دیتابیس ذخیره شوند و یک دستور /admin_logs برای مدیران ایجاد شود. خارج از این مرحله: آنالیز لاگ. نکته حیاتی: لاگ‌ها نباید شامل اطلاعات حساس (پسورد) باشند.
- [x] **مرحله 23: ایجاد تست‌های واحد برای سرویس تلگرام** — این مرحله شامل نوشتن تست‌های واحد برای TelegramService شامل ارسال پیام، دریافت پیام، مدیریت state و هندل کردن خطاها است. باید با mock کردن API تلگرام تست شود. خارج از این مرحله: تست‌های integration. نکته حیاتی: باید حداقل ۸۰% کد سرویس تلگرام تحت پوشش تست باشد.
- [x] **مرحله 24: ایجاد تست‌های integration برای دستورات تلگرام** — این مرحله شامل نوشتن تست‌های integration برای تمام دستورات تلگرام (courses, upload, practice و ...) است. باید با شبیه‌سازی پیام‌های تلگرام و بررسی پاسخ‌ها انجام شود. خارج از این مرحله: تست با ربات واقعی. نکته حیاتی: باید از دیتابیس تستی استفاده شود.
- [x] **مرحله 25: ایجاد مستندات کاربری برای ربات تلگرام** — این مرحله شامل نوشتن مستندات کامل برای کاربران ربات تلگرام است. شامل توضیح تمام دستورات، نحوه استفاده، مثال‌ها و troubleshooting. باید به صورت فایل Markdown در repository ذخیره شود. خارج از این مرحله: مستندات فنی. نکته حیاتی: باید به زبان فارسی باشد.

---

# 🔹 مرحله 1: ایجاد ساختار پایه برای اتصال تلگرام دوطرفه (Bot API + Webhook)

**Scope:** این مرحله شامل ایجاد یک سرویس تلگرام با استفاده از python-telegram-bot یا aiogram است که قابلیت دریافت و ارسال پیام را دارد. باید webhook یا polling برای دریافت پیام‌های کاربر تنظیم شود. این مرحله فقط زیرساخت ارتباطی را فراهم می‌کند و شامل هیچ منطق تجاری یا دستور خاصی نیست. نکته حیاتی: حتماً از async استفاده شود تا با سایر بخش‌های پروژه تداخل نداشته باشد.
**Key terms:** Telegram Bot API, webhook, python-telegram-bot, aiogram, polling, async handler

**بخش مربوط از متن کاربر:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد سرویس تلگرام دوطرفه با Webhook

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:68-68` — `WebSocketServer` — بک‌اند فعلی از WebSocket برای ارتباط زنده استفاده می‌کند. سرویس تلگرام باید به‌صورت موازی با این WebSocket کار کند و از async/await استفاده کند تا با event loop تداخل نداشته باشد.
  ```jsx
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/.env.example:1-10` — `GEMINI_API_KEY` — فایل env.example باید با متغیرهای TELEGRAM_BOT_TOKEN و TELEGRAM_WEBHOOK_URL به‌روز شود.
  ```
  GEMINI_API_KEY=your_gemini_api_key_here
  PORT=3001
  ```
- `backend/package.json:1-15` — `dependencies` — بک‌اند فعلی از Express.js و ws استفاده می‌کند. سرویس تلگرام به‌صورت یک سرویس مجزا (با python-telegram-bot) پیاده‌سازی می‌شود و نیازی به تغییر package.json نیست.
  ```json
  {
    "name": "lebanese-dialect-backend",
    "version": "1.0.0",
    "dependencies": {
      "cors": "^2.8.5",
      "dotenv": "^16.3.1",
      "express": "^4.18.2",
      "ws": "^8.19.0"
    }
  }
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/server.js` — باید endpoint POST /api/tele

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد ساختار پایه برای اتصال تلگرام دوطرفه (Bot API + Webhook) را داده است. این شامل ایجاد یک سرویس تلگرام با استفاده از python-telegram-bot یا aiogram است که قابلیت دریافت و ارسال پیام را دارد. باید webhook یا polling برای دریافت پیام‌های کاربر تنظیم شود. این مرحله فقط زیرساخت ارتباطی را فراهم می‌کند و شامل هیچ منطق تجاری یا دستور خاصی نیست. نکته حیاتی: حتماً از async استفاده شود تا با سایر بخش‌های پروژه تداخل نداشته باشد. کاربر می‌گوید: 'میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم'. کلیدواژه‌ها: Telegram Bot API, webhook, python-telegram-bot, aiogram, polling, async handler. پروژه فعلی یک اپلیکیشن آموزش لهجه لبنانی با بک‌اند Express.js (backend/server.js) و فرانت‌اند React (frontend/src/App.jsx) است. بک‌اند فعلی از WebSocket (backend/server.js خط 68: new WebSocketServer({ server, path: '/ws/live' })) برای ارتباطات زنده استفاده می‌کند. سرویس تلگرام باید به‌صورت یک سرویس مجزا (احتمالاً در پوشه backend/services/) ایجاد شود تا با معماری فعلی تداخل نداشته باشد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد پوشه backend/services/telegram/ و فایل‌های: __init__.py, bot.py (شامل کلاس TelegramBot با متدهای start_polling, setup_webhook, handle_update, send_message), config.py (شامل تنظیمات TOKEN, WEBHOOK_URL, ALLOWED_USER_IDS). 2. استفاده از کتابخانه python-telegram-bot v20+ (نسخه async) برای پیاده‌سازی. 3. در bot.py: ایجاد Application با Application.builder().token(TOKEN).build()، تنظیم webhook با await bot.initialize() و await bot.updater.start_webhook(listen='0.0.0.0', port=WEBHOOK_PORT, url_path=TOKEN). 4. پیاده‌سازی handler پایه برای دریافت پیام‌های متنی (MessageHandler(filters.TEXT & ~filters.COMMAND, handle_message)). 5. ایجاد endpoint در backend/server.js برای دریافت webhook از تلگرام (POST /api/telegram/webhook) که درخواست را به سرویس تلگرام فوروارد کند. 6. افزودن متغیر محیطی TELEGRAM_BOT_TOKEN و TELEGRAM_WEBHOOK_URL به backend/.env.example. 7. ایجاد فایل backend/services/telegram/handlers.py برای handlerهای پایه (echo, start, help). 8. اطمینان از async بودن تمام توابع (async def) برای سازگاری با event loop.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 2: پیاده‌سازی سیستم نوتیفیکیشن تلگرام برای رویدادهای سیستم

**Scope:** این مرحله شامل ایجاد مکانیزمی است که رویدادهای مهم سیستم (مثلاً ثبت‌نام جدید، تکمیل تمرین، خطا) را به کاربر از طریق تلگرام ارسال کند. باید یک سرویس NotificationService بسازد که از طریق TelegramService پیام ارسال کند. خارج از این مرحله: تعیین اینکه چه رویدادهایی باید نوتیفیکیشن بفرستند (بعداً مشخص می‌شود). نکته حیاتی: باید از queue یا event bus استفاده شود تا ارسال نوتیفیکیشن async باشد.
**Key terms:** NotificationService, TelegramService, event bus, async notification, message queue

**بخش مربوط از متن کاربر:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی سیستم نوتیفیکیشن تلگرام با event bus

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:1-15` — `import section` — محل اضافه کردن importهای جدید برای NotificationService و TelegramService و EventBus
  ```jsx
  import express from 'express';
  import cors from 'cors';
  import { fileURLToPath } from 'url';
  import { dirname, join } from 'path';
  import dotenv from 'dotenv';
  import { createServer } from 'http';
  import { WebSocketServer, WebSocket } from 'ws';
  import multer from 'multer';
  import fs from 'fs';
  import os from 'os';
  import ffmpeg from 'fluent-ffmpeg';
  import ffmpegStatic from 'ffmpeg-static';
  
  dotenv.config();
  ```
- `backend/server.js:68-71` — `WebSocket and middleware setup` — محل اضافه کردن middleware ثبت رویدادها و راه‌اندازی NotificationService بعد از WebSocket setup
  ```jsx
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  
  app.use(cors());
  app.use(express.json({ limit: '10mb' }));
  ```
- `backend/server.js:189-192` — `health check endpoint` — می‌توان endpoint جدید /api/notifications/status برای بررسی وضعیت سرویس نوتیفیکیشن اضافه کرد
  ```jsx
  app.get('/api/health', (req, res) => {
    res.json({ status: 'ok', timestamp: new Date().toISOString() });
  });
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی سیستم نوتیفیکیشن تلگرام برای رویدادهای سیستم را دارد. این شامل ایجاد مکانیزمی است که رویدادهای مهم سیستم (مثلاً ثبت‌نام جدید، تکمیل تمرین، خطا) را به کاربر از طریق تلگرام ارسال کند. باید یک سرویس NotificationService بسازد که از طریق TelegramService پیام ارسال کند. خارج از این مرحله: تعیین اینکه چه رویدادهایی باید نوتیفیکیشن بفرستند (بعداً مشخص می‌شود). نکته حیاتی: باید از queue یا event bus استفاده شود تا ارسال نوتیفیکیشن async باشد. کاربر تأکید کرده: 'میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته'. کلیدواژه‌ها: NotificationService, TelegramService, event bus, async notification, message queue. در کد فعلی backend/server.js (خطوط 1-1432) هیچ سرویس نوتیفیکیشن یا تلگرامی وجود ندارد. پروژه از Express.js (خط 1)، WebSocket (خط 68)، multer برای آپلود فایل (خط 44)، و Gemini API (خطوط 79-452) استفاده می‌کند. فایل backend/package.json شامل وابستگی‌های express, cors, dotenv, ws است اما هیچ کتابخانه‌ای برای تلگرام (مثل node-telegram-bot-api) یا message queue (مثل bull, amqplib) ندارد. frontend/index.html شامل Firebase config (خطوط 11-18) و Inspector Bridge (خطوط 32-201) است. ساختار پروژه شامل backend/server.js به عنوان entry point اصلی بک‌اند است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/services/telegramService.js: ساخت کلاس TelegramService با متدهای sendMessage(chatId, text) و متدهای دریافت پیام (برای پشتیبانی از تلگرام دوطرفه). استفاده از کتابخانه node-telegram-bot-api. 2. ایجاد فایل جدید backend/services/notificationService.js: ساخت کلاس NotificationService که از EventEmitter (یا bull queue) برای مدیریت async notifications استفاده کند. متدهای subscribe(event, handler) و emit(event, payload). 3. ایجاد فایل جدید backend/services/eventBus.js: پیاده‌سازی event bus ساده با استفاده از EventEmitter داخلی Node.js (یا bull برای production). 4. ایجاد فایل جدید backend/config/telegram.js: تنظیمات تلگرام شامل BOT_TOKEN از environment variables. 5. به‌روزرسانی backend/server.js: import و راه‌اندازی NotificationService در خطوط ابتدایی (بعد از خط 14). اضافه کردن middleware برای ثبت رویدادهای مهم (ثبت‌نام، تمرین، خطا). 6. به‌روزرسانی backend/package.json: اضافه کردن وابستگی node-telegram-bot-api و bull (اختیاری برای queue). 7. ایجاد فایل جدید backend/.env: اضافه کردن TELEGRAM_BOT_TOKEN و TELEGRAM_CHAT_ID. 8. ایجاد فایل جدید backend/tests/notification.test.js: تست‌های واحد برای NotificationService و TelegramService.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 3: پیاده‌سازی دستورات پایه کنترل سیستم از طریق تلگرام (Command Handlers)

**Scope:** این مرحله شامل تعریف و پیاده‌سازی دستورات پایه تلگرام مانند /status (وضعیت سیستم)، /help (راهنما)، /courses (لیست دروس) است. باید یک CommandHandler برای هر دستور ایجاد شود که منطق مربوطه را اجرا کند. خارج از این مرحله: دستورات پیچیده مانند آپلود فایل یا مدیریت تمرین با استاد. نکته حیاتی: دستورات باید با decorator یا registration ثبت شوند و پاسخ‌ها به صورت Markdown یا HTML فرمت شوند.
**Key terms:** CommandHandler, /status, /help, /courses, telegram commands, message formatting

**بخش مربوط از متن کاربر:**
```
بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی CommandHandler تلگرام برای /status، /help، /courses

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:1-68` — `import/require section + server creation` — محل افزودن import برای کتابخانه تلگرام (مثلاً import { Telegraf } from 'telegraf') و محل راه‌اندازی bot بعد از WebSocket server (بعد از خط 68)
  ```jsx
  import express from 'express';
  import cors from 'cors';
  import { fileURLToPath } from 'url';
  import { dirname, join } from 'path';
  import dotenv from 'dotenv';
  import { createServer } from 'http';
  import { WebSocketServer, WebSocket } from 'ws';
  import multer from 'multer';
  import fs from 'fs';
  import os from 'os';
  import ffmpeg from 'fluent-ffmpeg';
  import ffmpegStatic from 'ffmpeg-static';
  ```
- `backend/server.js:190-192` — `GET /api/health` — این endpoint برای دستور /status استفاده می‌شود تا وضعیت سیستم را برگرداند
  ```jsx
  app.get('/api/health', (req, res) => {
    res.json({ status: 'ok', timestamp: new Date().toISOString() });
  });
  ```
- `backend/.env.example:1-10` — `environment variables` — محل افزودن متغیر TELEGRAM_BOT_TOKEN برای توکن ربات تلگرام
  ```
  # Gemini API Key - Get from https://aistudio.google.com/apikey
  GEMINI_API_KEY=your_gemini_api_key_here
  
  # Port (optional, default 3001)
  PORT=3001
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/package.json` (سطر 10) — نیاز به افزودن وابستگی 'telegraf' یا 'node-telegram-bot-api' به dependencies
- `backend/package-lock.json` (سطر 1) — بعد از npm install به‌روزرسانی می‌شود
- `backend/.env.example` — افزودن متغیر TE

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی دستورات پایه کنترل سیستم از طریق تلگرام (Command Handlers) را داده است. این مرحله شامل تعریف و پیاده‌سازی دستورات پایه تلگرام مانند /status (وضعیت سیستم)، /help (راهنما)، /courses (لیست دروس) است. باید یک CommandHandler برای هر دستور ایجاد شود که منطق مربوطه را اجرا کند. خارج از این مرحله: دستورات پیچیده مانند آپلود فایل یا مدیریت تمرین با استاد. نکته حیاتی: دستورات باید با decorator یا registration ثبت شوند و پاسخ‌ها به صورت Markdown یا HTML فرمت شوند. بخش مربوط از درخواست اصلی کاربر: 'بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم'. کلیدواژه‌ها: CommandHandler, /status, /help, /courses, telegram commands, message formatting. در کد فعلی پروژه (backend/server.js) هیچ اثری از Webhook تلگرام، bot token، یا CommandHandler وجود ندارد. پروژه در backend/server.js از Express و WebSocket استفاده می‌کند (خطوط 1-68) و APIهای Gemini را ارائه می‌دهد (خطوط 79-263). فایل backend/package.json (از package-lock.json) وابستگی‌های فعلی شامل express, cors, dotenv, ws است و هیچ کتابخانه‌ای برای Telegram Bot API (مثل node-telegram-bot-api یا telegraf) وجود ندارد. فایل .github/workflows/claude-auto-task.yml نشان می‌دهد که پروژه از Claude Code برای اجرای خودکار تسک‌ها استفاده می‌کند. frontend/index.html حاوی Inspector Bridge برای ارتباط با iframe والد است (خطوط 32-201).

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/services/telegramBot.js با ساختار پایه: import Telegraf یا node-telegram-bot-api، تعریف CommandHandler برای /status (وضعیت سیستم با فراخوانی endpoint /api/health در backend/server.js خط 190-192)، /help (راهنما با لیست دستورات)، /courses (لیست دروس با placeholder). 2. ثبت دستورات با decorator یا registration pattern (مثلاً یک Map از command به handler). 3. فرمت پاسخ‌ها با Markdown یا HTML (مثلاً با parse_mode: 'MarkdownV2'). 4. افزودن وابستگی 'telegraf' یا 'node-telegram-bot-api' به backend/package.json. 5. افزودن متغیر محیطی TELEGRAM_BOT_TOKEN به backend/.env.example. 6. راه‌اندازی bot در backend/server.js بعد از خط 68 (بعد از ایجاد WebSocket server) با import و start bot. 7. اطمینان از graceful shutdown bot در صورت خطا.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 4: ایجاد سیستم احراز هویت و اتصال کاربر تلگرام به حساب کاربری

**Scope:** این مرحله شامل ایجاد مکانیزمی است که کاربر تلگرام را به حساب کاربری موجود در سیستم متصل کند. باید یک دستور /login یا /verify ایجاد شود که کاربر یک کد یکتا دریافت کند و در وبسایت وارد کند. همچنین باید mapping بین chat_id تلگرام و user_id سیستم ذخیره شود. خارج از این مرحله: احراز هویت دو مرحله‌ای یا OAuth. نکته حیاتی: امنیت بالا لازم است و کدها باید زمان‌دار (expire) باشند.
**Key terms:** /login, verification code, chat_id mapping, user authentication, token expiry

**بخش مربوط از متن کاربر:**
```
بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد سیستم احراز هویت تلگرام با کد یکتا و mapping chat_id

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:61-75` — `app setup and middleware` — محل فعلی تنظیم middlewareهای Express. باید route جدید auth.js بعد از این خطوط اضافه شود.
  ```jsx
  const app = express();
  const PORT = process.env.PORT || 3001;
  
  // Create HTTP server
  const server = createServer(app);
  
  // Create WebSocket server
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  
  app.use(cors());
  app.use(express.json({ limit: '10mb' }));
  
  // Serve static files from frontend build
  app.use(express.static(join(__dirname, '../frontend/dist')));
  ```
- `backend/server.js:189-192` — `GET /api/health` — الگوی endpoint ساده برای مرجع. endpointهای احراز هویت مشابه این ساختار خواهند بود.
  ```jsx
  // Health check
  app.get('/api/health', (req, res) => {
    res.json({ status: 'ok', timestamp: new Date().toISOString() });
  });
  ```
- `frontend/index.html:11-18` — `Firebase Configuration`
  ```
  var __firebase_config = JSON.stringify({
          apiKey: "AIzaSyAMFtV0zIMKKe0bIr68Z1wTORQ1jQpsv9Q",
          authDomain: "labaneseapp.firebaseapp.com",
          projectId: "labaneseapp",
          storageBucket: "labaneseapp.firebasestorage.app",
          messagingSenderId: "951874597795",
          appId: "1:951874597795:web:00745327993
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد سیستم احراز هویت و اتصال کاربر تلگرام به حساب کاربری موجود در سیستم را دارد. این سیستم باید شامل یک دستور /login یا /verify در تلگرام باشد که کاربر یک کد یکتا دریافت کند و در وبسایت وارد کند. همچنین باید mapping بین chat_id تلگرام و user_id سیستم ذخیره شود. خارج از این مرحله: احراز هویت دو مرحله‌ای یا OAuth. نکته حیاتی: امنیت بالا لازم است و کدها باید زمان‌دار (expire) باشند. بخش مربوط از درخواست اصلی کاربر: 'بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم'. کلیدواژه‌ها: /login, verification code, chat_id mapping, user authentication, token expiry.

شواهد در کد واقعی پروژه: پروژه فعلی یک اپلیکیشن آموزش لهجه لبنانی با React (frontend) و Express.js (backend) است. فایل backend/server.js شامل APIهای مختلف از جمله /api/gemini/chat (خط 79)، /api/gemini/tts (خط 140)، /api/analyze-files (خط 727) و WebSocket در /ws/live (خط 68) است. هیچ سیستم احراز هویتی در کد فعلی وجود ندارد. فایل frontend/index.html شامل Firebase config (خطوط 11-18) و Inspector Bridge Script (خطوط 32-201) است. backend/.env.example و frontend/.env.example هر دو شامل Firebase config به صورت اختیاری هستند. backend/package.json شامل dependencies: express, cors, dotenv, ws, multer, fluent-ffmpeg, ffmpeg-static, pdf-parse است. frontend/package.json شامل react, react-dom, firebase, lucide-react است. هیچ endpoint یا middleware احراز هویتی در backend/server.js وجود ندارد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/services/authService.js برای مدیریت منطق احراز هویت شامل: تولید کد یکتا (مثلاً 6 رقمی)، ذخیره موقت کدها با زمان انقضا (مثلاً 5 دقیقه) در حافظه (یا دیتابیس در آینده)، و mapping chat_id به user_id.
2. ایجاد فایل جدید backend/routes/auth.js با دو endpoint:
   - POST /api/auth/verify-code: دریافت کد از وبسایت و تأیید آن
   - POST /api/auth/link-telegram: دریافت chat_id و user_id و ذخیره mapping
3. ایجاد فایل جدید backend/services/telegramBot.js برای مدیریت Webhook تلگرام و پردازش دستور /login که یک کد یکتا تولید کرده و به کاربر ارسال می‌کند.
4. به‌روزرسانی backend/server.js برای import و استفاده از routes/auth.js.
5. ایجاد فایل جدید frontend/src/components/TelegramLogin.jsx برای نمایش فرم ورود کد تأیید در وبسایت.
6. به‌روزرسانی frontend/src/App.jsx برای اضافه کردن مسیر /verify به React Router.
7. استفاده از کتابخانه node-telegram-bot-api برای ارتباط با Telegram Bot API.
8. ذخیره کدها در حافظه با ساختار Map<code, {chatId, userId, expiresAt}> و پاکسازی خودکار کدهای منقضی شده با setInterval.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 5: ایجاد دستور /courses برای نمایش لیست دروس و وضعیت آنها

**Scope:** این مرحله شامل توسعه دستور /courses است که لیست تمام دروس موجود در سیستم را با وضعیت هر کدام (فعال/غیرفعال، تعداد جلسات، آخرین به‌روزرسانی) نمایش دهد. باید از دیتابیس خوانده شود و به صورت فرمت‌شده در تلگرام نمایش داده شود. خارج از این مرحله: امکان تغییر وضعیت دروس از تلگرام. نکته حیاتی: پاسخ باید خلاصه و قابل اسکن باشد (حداکثر ۱۰ درس در هر پیام).
**Key terms:** /courses, course list, course status, database query, formatted message

**بخش مربوط از متن کاربر:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد دستور /courses برای نمایش لیست دروس و وضعیت آنها

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:1-12` — `imports` — محل اضافه کردن import برای دیتابیس (Firebase Admin SDK یا sqlite3) و کتابخانه تلگرام بات
  ```jsx
  import express from 'express';
  import cors from 'cors';
  import { fileURLToPath } from 'url';
  import { dirname, join } from 'path';
  import dotenv from 'dotenv';
  import { createServer } from 'http';
  import { WebSocketServer, WebSocket } from 'ws';
  import multer from 'multer';
  import fs from 'fs';
  import os from 'os';
  import ffmpeg from 'fluent-ffmpeg';
  import ffmpegStatic from 'ffmpeg-static';
  ```
- `backend/server.js:61-68` — `app initialization` — محل اضافه کردن middleware و route جدید برای /api/courses
  ```jsx
  const app = express();
  const PORT = process.env.PORT || 3001;
  
  // Create HTTP server
  const server = createServer(app);
  
  // Create WebSocket server
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/server.js:189-192` — `health check endpoint` — الگوی مناسب برای اضافه کردن endpoint جدید /api/courses — مشابه همین ساختار
  ```jsx
  // Health check
  app.get('/api/health', (req, res) => {
    res.json({ status: 'ok', timestamp: new Date().toISOString() });
  });
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/.env.example` (سطر 1) — شامل Firebase config که می‌تواند برای دیتابیس استفاده شود
- `frontend/index.html` (سطر 11) — شامل Firebase config سخت‌کد شده (lines 11-18) که می‌تواند برای backend نیز استفاده شود
- `backend/package.json` — نیاز به اضافه کردن وابستگی‌های جدید: firebase-admin یا sqlite3،

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد دستور /courses در تلگرام را دارد که لیست تمام دروس موجود در سیستم را با وضعیت هر کدام (فعال/غیرفعال، تعداد جلسات، آخرین به‌روزرسانی) نمایش دهد. این دستور باید از دیتابیس خوانده شود و به صورت فرمت‌شده در تلگرام نمایش داده شود. خارج از این مرحله: امکان تغییر وضعیت دروس از تلگرام. نکته حیاتی: پاسخ باید خلاصه و قابل اسکن باشد (حداکثر ۱۰ درس در هر پیام). کاربر همچنین اشاره کرده: "و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ..." که نشان‌دهنده نیاز به مدیریت محتوای دروس از طریق تلگرام است. کلیدواژه‌ها: /courses, course list, course status, database query, formatted message. در کد فعلی پروژه (backend/server.js) هیچ endpoint یا مسیری برای مدیریت دروس وجود ندارد. فایل‌های مرتبط با دیتابیس و مدل‌ها در deep context موجود نیستند. ساختار پروژه شامل backend با Express.js و frontend با React/Vite است. فایل backend/server.js شامل APIهای مربوط به Gemini (Chat، TTS، File Analysis) و WebSocket است. هیچ فایل مدل یا دیتابیسی در ساختار پروژه دیده نمی‌شود. فایل backend/.env.example شامل Firebase config است که می‌تواند برای persistence داده استفاده شود. فایل frontend/index.html شامل Firebase config سخت‌کد شده است. برای پیاده‌سازی این قابلیت، نیاز به ایجاد مدل Course در دیتابیس (احتمالاً Firebase Firestore یا SQLite)، ایجاد endpoint جدید در backend، و ایجاد هندلر دستور /courses در یک bot تلگرام (که در پروژه وجود ندارد) است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد مدل Course در دیتابیس (Firebase Firestore یا SQLite) با فیلدهای: id, title, description, status (active/inactive), sessionCount, lastUpdated, createdAt. 2. ایجاد endpoint GET /api/courses در backend/server.js که لیست دروس را از دیتابیس خوانده و به صورت JSON برگرداند. 3. ایجاد هندلر دستور /courses در یک bot تلگرام (نیاز به نصب کتابخانه node-telegram-bot-api یا مشابه). 4. فرمت‌دهی خروجی به صورت خلاصه و قابل اسکن با حداکثر ۱۰ درس در هر پیام. 5. اضافه کردن pagination برای دروس بیشتر از ۱۰ عدد. 6. نمایش وضعیت هر درس با آیکون‌های مناسب (✅ فعال / ❌ غیرفعال). 7. نمایش تعداد جلسات و آخرین به‌روزرسانی برای هر درس.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 6: ایجاد دستور /course_content برای مشاهده محتوای یک درس خاص

**Scope:** این مرحله شامل ایجاد دستور /course_content <course_id> است که محتوای یک درس خاص (لیست جلسات، فایل‌ها، تمرین‌ها) را نمایش دهد. باید از دیتابیس یا storage محتوای درس را بخواند و به صورت دسته‌بندی‌شده نمایش دهد. خارج از این مرحله: آپلود محتوا. نکته حیاتی: محتوا ممکن است حجیم باشد، بنابراین باید صفحه‌بندی (pagination) شود.
**Key terms:** /course_content, course_id, lesson list, content display, pagination

**بخش مربوط از متن کاربر:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد دستور /course_content برای نمایش محتوای درس با صفحه‌بندی

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:79-137` — `app.post('/api/gemini/chat', ...)` — الگوی موجود برای ایجاد endpoint جدید — مشابه این ساختار برای /api/course-content استفاده شود
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
  ```
- `backend/server.js:68-68` — `const wss = new WebSocketServer({ server, path: '/ws/live' })` — WebSocket موجود — در صورت نیاز به real-time notification برای تغییرات محتوا می‌توان از آن استفاده کرد
  ```jsx
  const wss = new WebSocketServer({ server, path: '/ws/live' })
  ```
- `backend/server.js`

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد دستور /course_content <course_id> را داده است که محتوای یک درس خاص شامل لیست جلسات، فایل‌ها و تمرین‌ها را از دیتابیس یا storage بخواند و به صورت دسته‌بندی‌شده نمایش دهد. کاربر تأکید کرده که آپلود محتوا خارج از این مرحله است و نکته حیاتی این است که محتوا ممکن است حجیم باشد، بنابراین باید صفحه‌بندی (pagination) شود. بخش مربوط از درخواست اصلی کاربر: "و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...". کلیدواژه‌ها: /course_content, course_id, lesson list, content display, pagination. در کد فعلی پروژه (backend/server.js) هیچ endpoint یا مسیری برای مدیریت محتوای دروس وجود ندارد. تنها endpointهای موجود عبارتند از: /api/gemini/chat (خط 79)، /api/gemini/tts (خط 140)، /api/health (خط 190)، /api/list-models (خط 195)، /api/test-gemini (خط 218)، /api/analyze-files (خط 727). همچنین WebSocket در /ws/live (خط 68) تعریف شده است. هیچ دیتابیس یا storageای برای ذخیره محتوای دروس در کد فعلی دیده نمی‌شود. فایل backend/.env.example فقط شامل GEMINI_API_KEY و VITE_FIREBASE_CONFIG است. frontend/index.html شامل Inspector Bridge است اما هیچ کامپوننت مدیریت درس ندارد. بنابراین این تسک نیازمند ایجاد زیرساخت کامل backend برای ذخیره و بازیابی محتوای دروس و همچنین ایجاد endpoint جدید /api/course-content است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک فایل جدید backend/services/courseService.js برای مدیریت عملیات مربوط به محتوای دروس شامل توابع readCourseContent(courseId, page, pageSize) که از یک storage (فایل JSON محلی یا Firebase) محتوا را می‌خواند و با صفحه‌بندی برمی‌گرداند. 2. ایجاد endpoint جدید POST /api/course-content در backend/server.js که course_id را از body دریافت کند و با پارامترهای page و pageSize (پیش‌فرض 10) محتوای درس را برگرداند. 3. ایجاد ساختار داده برای ذخیره محتوای دروس شامل فیلدهای: courseId, title, sessions (آرایه‌ای از session objects شامل sessionId, title, files, exercises), createdAt, updatedAt. 4. پیاده‌سازی صفحه‌بندی در سمت سرور با استفاده از پارامترهای page و pageSize و برگرداندن metadata شامل totalItems, totalPages, currentPage. 5. ایجاد یک فایل JSON نمونه در backend/data/courses.json برای ذخیره اولیه محتوای دروس (تا زمان اتصال به Firebase). 6. افزودن validation برای course_id که وجود داشته باشد و در صورت عدم وجود خطای 404 برگرداند. 7. افزودن middleware برای مدیریت خطاهای مربوط به خواندن محتوا.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 7: ایجاد دستور /upload برای آپلود محتوای دروس از طریق تلگرام

**Scope:** این مرحله شامل ایجاد دستور /upload <course_id> است که کاربر می‌تواند یک فایل (PDF، تصویر، ویدئو) را به عنوان محتوای درس آپلود کند. باید فایل را از تلگرام دریافت کرده، در storage ذخیره کند و متادیتا را در دیتابیس ثبت کند. خارج از این مرحله: پردازش فایل (OCR، تبدیل). نکته حیاتی: محدودیت حجم فایل تلگرام (۵۰ مگابایت) باید مدیریت شود و فایل‌های بزرگتر رد شوند.
**Key terms:** /upload, file upload, telegram file, storage, course_id, file size limit

**بخش مربوط از متن کاربر:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد دستور /upload تلگرام برای آپلود محتوای دروس

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:20-28` — `uploadDir` — مسیر فعلی آپلود فایل‌ها - باید مسیر جداگانه‌ای برای فایل‌های دروس تلگرام ایجاد شود
  ```jsx
  const uploadDir = join(os.tmpdir(), 'lebanese-uploads');
  if (!fs.existsSync(uploadDir)) {
    fs.mkdirSync(uploadDir, { recursive: true });
  }
  ```
- `backend/server.js:44-56` — `upload` — پیکربندی multer فعلی - می‌توان از الگوی مشابه برای آپلود تلگرام استفاده کرد اما با محدودیت 50MB
  ```jsx
  const upload = multer({
    storage: storage,
    limits: { fileSize: MAX_FILE_SIZE, files: MAX_FILES },
    fileFilter: (req, file, cb) => {
      if (isAllowedMimeType(file.mimetype)) {
        cb(null, true);
      } else {
        const err = new multer.MulterError('LIMIT_UNEXPECTED_FILE', file.fieldname);
        err.message = `نوع فایل پشتیبانی نمی‌شود: ${file.mimetype}`;
        cb(err);
      }
    }
  });
  ```
- `backend/server.js:727-730` — `app.post('/api/analyze-files')` — الگوی endpoint موجود برای دریافت فایل - مشابه برای endpoint تلگرام استفاده شود
  ```jsx
  app.post('/api/analyze-files', upload.array('files', MAX_FILES), handleMulterError, async (req, res) => {
    console.log('Received file analysis request');
    if (!GEMINI_API_KEY) {
      console.error('API key not configured');
      return res.status(500).json({ error: 'API key not configured' });
    }
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `frontend/index.html` (سطر 11) — حاوی Firebase config (خطوط 11-18) که برای ذخیره متادیتا استفاده می‌شود
- `backend/package.json`

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد دستور /upload <course_id> در تلگرام را دارد که از طریق آن کاربر بتواند فایل‌های PDF، تصویر یا ویدئو را به عنوان محتوای درس آپلود کند. فایل باید از تلگرام دریافت شده، در storage ذخیره شود و متادیتا در دیتابیس ثبت گردد. پردازش فایل (OCR، تبدیل) خارج از این مرحله است. نکته حیاتی: محدودیت حجم فایل تلگرام (۵۰ مگابایت) باید مدیریت شود و فایل‌های بزرگتر رد شوند. کاربر همچنین اشاره کرده: "و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا آپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم". کلیدواژه‌ها: /upload, file upload, telegram file, storage, course_id, file size limit. در کد فعلی پروژه (backend/server.js) زیرساخت آپلود فایل با multer وجود دارد (خطوط 25-56) که از disk storage استفاده می‌کند و محدودیت 500MB و فیلتر MIME type دارد. endpoint /api/analyze-files در خط 727 فایل‌ها را دریافت می‌کند. اما هیچ endpoint یا handler ای برای تلگرام وجود ندارد. پروژه از Firebase استفاده می‌کند (frontend/index.html خطوط 11-18) و WebSocket server در خط 68 backend/server.js تعریف شده. ساختار پروژه شامل backend/server.js به عنوان entry point اصلی بک‌اند است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک فایل جدید backend/services/telegramBot.js که یک bot تلگرام با کتابخانه node-telegram-bot-api راه‌اندازی کند. 2. در این فایل، handler برای دستور /upload <course_id> ایجاد شود که: الف) course_id را از متن دستور استخراج کند. ب) از کاربر بخواهد فایل را ارسال کند (با پیام راهنما). ج) فایل دریافتی را بررسی کند: اگر حجم > 50MB باشد، پیام خطا برگرداند. د) فایل را با استفاده از multer یا دانلود مستقیم در مسیر uploadDir (backend/server.js خط 20) ذخیره کند. ه) متادیتا (course_id, filename, mimeType, size, uploadDate) را در یک فایل JSON یا Firebase ذخیره کند. 3. endpoint جدید در backend/server.js اضافه شود: POST /api/telegram/upload که اطلاعات آپلود را دریافت و در دیتابیس ثبت کند. 4. محدودیت حجم 50MB تلگرام در validation چک شود. 5. مسیر storage برای فایل‌های دروس: join(uploadDir, 'courses', courseId). 6. متادیتا در فایل courses.json در backend/data/ ذخیره شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 8: ایجاد دستور /delete_content برای حذف محتوای دروس از تلگرام

**Scope:** این مرحله شامل ایجاد دستور /delete_content <course_id> <content_id> است که کاربر می‌تواند یک محتوای خاص را از درس حذف کند. باید از دیتابیس و storage حذف شود. خارج از این مرحله: حذف دسته‌جمعی. نکته حیاتی: باید تأییدیه (confirmation) از کاربر گرفته شود قبل از حذف.
**Key terms:** /delete_content, content_id, confirmation, delete from storage, delete from database

**بخش مربوط از متن کاربر:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد دستور /delete_content برای حذف محتوای دروس از تلگرام

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:1-12` — `import section` — نیاز به اضافه کردن import برای Firebase Admin SDK (firebase-admin) برای دسترسی به Firestore و Storage
  ```jsx
  import express from 'express';
  import cors from 'cors';
  import { fileURLToPath } from 'url';
  import { dirname, join } from 'path';
  import dotenv from 'dotenv';
  import { createServer } from 'http';
  import { WebSocketServer, WebSocket } from 'ws';
  import multer from 'multer';
  import fs from 'fs';
  import os from 'os';
  import ffmpeg from 'fluent-ffmpeg';
  import ffmpegStatic from 'ffmpeg-static';
  ```
- `backend/server.js:76-77` — `GEMINI_API_KEY constant` — پس از این خط، باید Firebase Admin SDK را مقداردهی اولیه کرد: const firebaseAdmin = await import('firebase-admin'); و firebaseAdmin.initializeApp(...)
  ```jsx
  const GEMINI_API_KEY = process.env.GEMINI_API_KEY;
  ```
- `backend/server.js:189-192` — `Health check endpoint` — پس از این endpoint، endpointهای جدید برای مدیریت محتوا اضافه شوند
  ```jsx
  app.get('/api/health', (req, res) => {
    res.json({ status: 'ok', timestamp: new Date().toISOString() });
  });
  ```
- `frontend/index.html:11-18` — `Firebase Configuration`
  ```
  var __firebase_config = JSON.stringify({
    apiKey: "AIzaSyAMFtV0zIMKKe0bIr68Z1wTORQ1jQpsv9Q",
    authDomain: "labaneseapp.firebaseapp.com",
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد دستور /delete_content <course_id> <content_id> را دارد که از طریق تلگرام (احتمالاً یک ربات تلگرامی) یک محتوای خاص را از یک درس حذف کند. این عملیات باید هم از دیتابیس و هم از storage (احتمالاً Firebase Storage یا فضای ذخیره‌سازی مشابه) حذف انجام دهد. خارج از این مرحله: حذف دسته‌جمعی. نکته حیاتی: باید تأییدیه (confirmation) از کاربر گرفته شود قبل از حذف. کاربر در بخش مربوط از درخواست اصلی خود اشاره کرده: 'و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...' کلیدواژه‌ها: /delete_content, content_id, confirmation, delete from storage, delete from database. در کد فعلی پروژه (backend/server.js) هیچ endpoint یا مسیری برای حذف محتوا وجود ندارد. فایل‌های مرتبط با مدیریت محتوا و دیتابیس در deep context موجود نیستند، اما با توجه به ساختار پروژه که از Firebase استفاده می‌کند (frontend/index.html خط 11-18: __firebase_config شامل apiKey, authDomain, projectId, storageBucket)، احتمالاً دیتابیس Firestore و storage Firebase Storage است. همچنین backend/server.js خط 76: GEMINI_API_KEY و خط 79-137: /api/gemini/chat و خط 140-187: /api/gemini/tts و خط 727: /api/analyze-files نشان می‌دهد که backend فعلاً فقط APIهای مربوط به Gemini را دارد و هیچ API برای مدیریت محتوا (CRUD) پیاده‌سازی نشده است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. مرحله 1: ایجاد یک endpoint جدید در backend/server.js با مسیر DELETE /api/courses/:courseId/contents/:contentId که عملیات حذف را انجام دهد. مرحله 2: در این endpoint، ابتدا باید محتوا را از دیتابیس (Firestore) پیدا کند، سپس فایل مربوطه را از storage (Firebase Storage) حذف کند، و در نهایت سند دیتابیس را حذف کند. مرحله 3: پیاده‌سازی منطق تأییدیه (confirmation) — endpoint باید ابتدا یک پاسخ با اطلاعات محتوا برگرداند و منتظر تأیید صریح کاربر بماند (مثلاً با یک فیلد confirmed: true در درخواست دوم). مرحله 4: ایجاد یک endpoint کمکی GET /api/courses/:courseId/contents/:contentId برای نمایش اطلاعات محتوا قبل از حذف. مرحله 5: در سمت تلگرام (که احتمالاً یک سرویس جداگانه است)، دستور /delete_content <course_id> <content_id> را پیاده‌سازی کنید که ابتدا اطلاعات محتوا را نمایش دهد، سپس تأییدیه بگیرد و در نهایت درخواست حذف را ارسال کند. مرحله 6: اضافه کردن middleware احراز هویت (با توجه به Firebase Auth در frontend/index.html). مرحله 7: اضافه کردن validation برای course_id و content_id. مرحله 8: اضافه کردن logging برای عملیات حذف.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 9: ایجاد دستور /edit_course برای ویرایش متادیتای دروس از تلگرام

**Scope:** این مرحله شامل ایجاد دستور /edit_course <course_id> است که کاربر می‌تواند متادیتای درس (نام، توضیحات، وضعیت) را ویرایش کند. باید از طریق مراحل (step-by-step) با کاربر تعامل کند. خارج از این مرحله: ویرایش محتوای دروس. نکته حیاتی: باید validation روی ورودی‌ها انجام شود.
**Key terms:** /edit_course, course metadata, step-by-step, validation, update database

**بخش مربوط از متن کاربر:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد دستور /edit_course برای ویرایش متادیتای دروس از تلگرام

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:1-12` — `imports` — محل افزودن import برای ماژول Telegram Bot (مثلاً import { TelegramBotHandler } from './telegram-bot.js')
  ```jsx
  import express from 'express';
  import cors from 'cors';
  import { fileURLToPath } from 'url';
  import { dirname, join } from 'path';
  import dotenv from 'dotenv';
  import { createServer } from 'http';
  import { WebSocketServer, WebSocket } from 'ws';
  import multer from 'multer';
  import fs from 'fs';
  import os from 'os';
  import ffmpeg from 'fluent-ffmpeg';
  import ffmpegStatic from 'ffmpeg-static';
  ```
- `backend/server.js:61-`

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد دستور /edit_course <course_id> را داده است که از طریق تلگرام (Telegram Bot) قابل اجرا باشد. این دستور باید به کاربر امکان ویرایش متادیتای دروس (metadata) شامل: نام درس (course name)، توضیحات (description)، و وضعیت (status) را بدهد. تعامل با کاربر باید به صورت گام‌به‌گام (step-by-step) انجام شود، یعنی ربات ابتدا course_id را دریافت کند، سپس فیلد مورد نظر برای ویرایش را بپرسد، مقدار جدید را بگیرد و در نهایت تأیید نهایی را انجام دهد. کاربر تأکید کرده که ویرایش محتوای دروس (مثل فایل‌ها، ویدیوها، متن درس) خارج از scope این مرحله است. یک نکته حیاتی که کاربر ذکر کرده: 'باید validation روی ورودی‌ها انجام شود' — یعنی هر ورودی (course_id معتبر، نام غیرخالی، توضیحات با محدودیت طول، وضعیت مجاز) باید قبل از اعمال تغییرات اعتبارسنجی شود. همچنین کاربر در بخش دیگری از درخواست اصلی اشاره کرده: 'و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا آپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم' که نشان می‌دهد این قابلیت بخشی از یک سیستم مدیریت محتوای گسترده‌تر از طریق تلگرام است. کلیدواژه‌های اصلی: /edit_course, course metadata, step-by-step, validation, update database. در کد فعلی پروژه (backend/server.js) هیچ endpoint یا handler مربوط به Telegram Bot وجود ندارد. پروژه در حال حاضر شامل یک backend مبتنی بر Express با WebSocket و APIهای مربوط به Gemini (Chat, TTS, File Analysis) است. frontend/index.html شامل Inspector Bridge برای ارتباط با iframe parent است. ساختار پروژه شامل فایل‌های پیکربندی GitHub Actions (claude-auto-task.yml) برای اجرای خودکار تسک‌هاست. هیچ فایل یا ماژول مرتبط با Telegram Bot API (مثل node-telegram-bot-api یا telegraf) در package.json یا package-lock.json دیده نمی‌شود. بنابراین این تسک نیازمند ایجاد یک ماژول جدید برای Telegram Bot است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/telegram-bot.js که حاوی کلاس/ماژول TelegramBotHandler باشد. 2. در این فایل، یک Webhook یا Long Polling handler برای دریافت پیام‌های تلگرام پیاده‌سازی شود. 3. پیاده‌سازی تابع handleEditCourseCommand(message, args) که: a. course_id را از args استخراج کند و اعتبارسنجی کند (عددی یا ObjectId معتبر). b. اگر course_id معتبر نبود، پیام خطای مناسب برگرداند. c. اگر معتبر بود، وضعیت کاربر را در یک session ذخیره کند (مثلاً با استفاده از Map یا Redis) تا مرحله بعدی مشخص شود. d. از کاربر بپرسد کدام فیلد را می‌خواهد ویرایش کند (name, description, status). e. پس از دریافت فیلد، مقدار جدید را درخواست کند و validation انجام دهد (مثلاً name نباید خالی باشد، description حداکثر 500 کاراکتر، status یکی از 'active', 'draft', 'archived'). f. پس از validation، یک پیام تأیید نهایی با نمایش تغییرات بفرستد و منتظر تأیید (yes/no) بماند. g. پس از تأیید، با استفاده از یک تابع updateCourseInDatabase(courseId, updates) که در فایل backend/db.js (که باید ایجاد شود) تعریف شده، دیتابیس را به‌روز کند. 4. ایجاد فایل backend/db.js شامل توابع: getCourseById(courseId), updateCourse(courseId, updates). 5. ایجاد فایل backend/validators.js شامل توابع: validateCourseId(id), validateCourseName(name), validateCourseDescription(desc), validateCourseStatus(status). 6. نصب پکیج node-telegram-bot-api یا telegraf (بسته به انتخاب) و افزودن به backend/package.json. 7. افزودن route جدید در backend/server.js برای Webhook تلگرام (اختیاری — اگر از Long Polling استفاده شود نیازی نیست). 8. افزودن متغیر محیطی TELEGRAM_BOT_TOKEN به backend/.env.example.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 10: ایجاد دستور /add_course برای افزودن درس جدید از تلگرام

**Scope:** این مرحله شامل ایجاد دستور /add_course است که کاربر می‌تواند یک درس جدید با نام، توضیحات و سایر فیلدها ایجاد کند. باید از طریق مراحل با کاربر تعامل کند و در دیتابیس ذخیره کند. خارج از این مرحله: افزودن محتوا به درس جدید. نکته حیاتی: باید بررسی شود که درس تکراری نباشد.
**Key terms:** /add_course, new course, step-by-step, duplicate check, create course

**بخش مربوط از متن کاربر:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد دستور /add_course برای افزودن درس جدید از تلگرام

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:1-75` — `server setup` — فایل اصلی بک‌اند — باید Webhook تلگرام یا WebSocket Bot در اینجا اضافه شود. خط 68 WebSocket موجود را نشان می‌دهد.
  ```jsx
  import express from 'express';
  import cors from 'cors';
  import { fileURLToPath } from 'url';
  import { dirname, join } from 'path';
  import dotenv from 'dotenv';
  import { createServer } from 'http';
  import { WebSocketServer, WebSocket } from 'ws';
  ```
- `frontend/index.html:11-18` — `Firebase config` — Firebase پیکربندی شده — می‌توان از Firestore برای ذخیره درس‌ها استفاده کرد. این فایل deep-read شده است.
  ```
  var __firebase_config = JSON.stringify({
          apiKey: "AIzaSyAMFtV0zIMKKe0bIr68Z1wTORQ1jQpsv9Q",
          authDomain: "labaneseapp.firebaseapp.com",
          projectId: "labaneseapp",
          storageBucket: "labaneseapp.firebasestorage.app",
          messagingSenderId: "951874597795",
          appId: "1:951874597795:web:00745327993adad760a016"
        });
  ```
- `backend/package.json:1-15` — `dependencies` — نیاز به اضافه کردن وابستگی‌های تلگرام (node-telegram-bot-api یا telegraf) و Firebase Admin SDK. این فایل deep-read شده است.
  ```json
  {
    "name": "lebanese-dialect-backend",
    "version": "1.0.0",
    "dependencies": {
      "cors": "^2.8.5",
      "dotenv": "^16.3.1",
      "express": "^4.18.2",
      "ws": "^8.19.0"
    }
  }
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/.env.example` (سطر 1) — نیاز به اضافه کردن TELEGRAM_BOT_TOKEN و Firebase Admin credentials
- `frontend/src/App.jsx` (سطر 1) — احتمالاً کامپوننت‌های مدیریت درس در فرانت‌اند — باید با دیتابیس همگام شود
- `prompt/_index.json` (سطر 1) — برای ثبت تسک جدید در سیستم auto-runner

## 🌐 نقشهٔ وابستگی‌ها
این قابلیت به فایل‌های backend

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد دستور /add_course برای افزودن درس جدید از تلگرام را دارد. این دستور باید با کاربر به صورت مرحله‌ای (step-by-step) تعامل کند و یک درس جدید با فیلدهای نام، توضیحات و سایر فیلدها ایجاد کند. داده‌ها باید در دیتابیس ذخیره شوند. نکته حیاتی: باید بررسی شود که درس تکراری نباشد (duplicate check). خارج از این مرحله: افزودن محتوا به درس جدید. کاربر همچنین اشاره کرده: 'و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا آپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم' که نشان‌دهنده نیاز به مدیریت کامل دروس از تلگرام است. کلیدواژه‌ها: /add_course, new course, step-by-step, duplicate check, create course. در کد فعلی پروژه (backend/server.js) هیچ endpoint یا handler مربوط به Telegram Bot وجود ندارد. پروژه از Express.js (خط 1-65) با WebSocket (خط 68) و APIهای Gemini (خطوط 79-263) استفاده می‌کند. هیچ فایل bot یا Telegram integration در ساختار پروژه دیده نمی‌شود. فایل‌های مرتبط احتمالی: backend/server.js (ورودی اصلی بک‌اند)، backend/package.json (وابستگی‌ها)، frontend/src/App.jsx (احتمالاً کامپوننت‌های مدیریت درس).

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/bot.js یا backend/services/telegramBot.js برای مدیریت Webhook/WebSocket تلگرام. 2. پیاده‌سازی دستور /add_course با مراحل: دریافت نام درس از کاربر، دریافت توضیحات، تأیید نهایی. 3. ایجاد مدل/ساختار داده برای درس‌ها (مثلاً backend/models/Course.js یا استفاده از Firebase که در frontend/index.html خط 11-18 پیکربندی شده). 4. پیاده‌سازی duplicate check با جستجوی نام درس در دیتابیس قبل از ذخیره. 5. ذخیره درس جدید در دیتابیس (Firebase یا فایل JSON). 6. ثبت Webhook تلگرام در backend/server.js (خط 68-74). 7. نصب کتابخانه node-telegram-bot-api یا telegraf در backend/package.json.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 11: ایجاد دستور /delete_course برای حذف درس از تلگرام

**Scope:** این مرحله شامل ایجاد دستور /delete_course <course_id> است که کاربر می‌تواند یک درس و تمام محتوای آن را حذف کند. باید تأییدیه دو مرحله‌ای بگیرد و سپس درس و محتواهای مرتبط را از دیتابیس و storage حذف کند. خارج از این مرحله: حذف انتخابی محتوا. نکته حیاتی: این عملیات غیرقابل بازگشت است و باید هشدار داده شود.
**Key terms:** /delete_course, course_id, double confirmation, cascade delete, irreversible

**بخش مربوط از متن کاربر:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد دستور /delete_course برای حذف درس از تلگرام

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:20-23` — `uploadDir` — این مسیر برای ذخیره فایل‌های آپلود شده استفاده می‌شود. هنگام حذف cascade درس، فایل‌های مرتبط در این مسیر (یا مسیر مشابه) باید حذف شوند.
  ```jsx
  const uploadDir = join(os.tmpdir(), 'lebanese-uploads');
  if (!fs.existsSync(uploadDir)) {
    fs.mkdirSync(uploadDir, { recursive: true });
  }
  ```
- `backend/server.js:68` — `WebSocketServer` — WebSocket موجود برای Inspector Bridge است. برای ربات تلگرام نیازی به WebSocket نیست، اما می‌توان از همین سرور HTTP استفاده کرد.
  ```jsx
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `frontend/index.html:11-18` — `__firebase_config` — Firebase در frontend پیکربندی شده. می‌توان از Firebase Firestore برای ذخیره درس‌ها و محتواها و از Firebase Storage برای فایل‌ها استفاده کرد.
  ```
  var __firebase_config = JSON.stringify({
    apiKey: "AIzaSyAMFtV0zIMKKe0bIr68Z1wTORQ1jQpsv9Q",
    authDomain: "labaneseapp.firebaseapp.com",
    projectId: "labaneseapp",
    storageBucket: "labaneseapp.firebasestorage.app",
    messagingSenderId: "951874597795",
    appId: "1:951874597795:web:00745327993adad760a016"
  });
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/package.json` — برای افزودن وابستگی‌های جدید مانند telegraf یا node-telegram-bot-api و better-sqlite3 یا firebase-admin باید این فایل به‌روز شود.

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد دستور /delete_course <course_id> را دارد که از طریق تلگرام (احتمالاً یک ربات تلگرامی) یک درس و تمام محتوای آن را حذف کند. این دستور باید تأییدیه دو مرحله‌ای (double confirmation) بگیرد و سپس درس و محتواهای مرتبط را از دیتابیس و storage حذف کند (cascade delete). عملیات غیرقابل بازگشت (irreversible) است و باید هشدار داده شود. خارج از این مرحله: حذف انتخابی محتوا. کاربر همچنین اشاره کرده که بتواند محتوای دروس را ببیند یا آپلود کند و کار را از همانجا (تلگرام) پیش ببرد. در کد فعلی پروژه (backend/server.js) هیچ endpoint یا handler مرتبط با تلگرام یا مدیریت درس (course) وجود ندارد. پروژه یک اپلیکیشن آموزش لهجه لبنانی با React frontend و Express backend است که از Gemini API برای تحلیل فایل و چت استفاده می‌کند. فایل‌های مرتبط با تلگرام در ساختار پروژه دیده نمی‌شود — این قابلیت باید از صفر ساخته شود. کلیدواژه‌های کاربر: /delete_course, course_id, double confirmation, cascade delete, irreversible.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. با توجه به عدم وجود زیرساخت تلگرام و مدیریت درس در پروژه فعلی، مراحل زیر پیشنهاد می‌شود:

1. ایجاد یک ربات تلگرام جدید (یا افزودن به ربات موجود) با استفاده از کتابخانه‌ای مانند node-telegram-bot-api یا telegraf.
2. ایجاد مدل داده برای درس (Course) شامل فیلدهای: course_id (شناسه یکتا)، title, description, created_at, owner_id (شناسه کاربر تلگرام).
3. ایجاد مدل داده برای محتوای درس (CourseContent) شامل: content_id, course_id (FK), type (text/audio/video/image), file_url, created_at.
4. پیاده‌سازی دستور /delete_course در ربات:
   - دریافت course_id از کاربر
   - بررسی وجود درس و مالکیت کاربر
   - نمایش پیام تأیید دو مرحله‌ای (مثلاً: «آیا مطمئن هستید؟ این عملیات غیرقابل بازگشت است. برای تأیید دوباره /confirm_delete <course_id> را بزنید»)
   - پس از تأیید نهایی: حذف cascade همه محتواهای مرتبط از دیتابیس و فایل‌های ذخیره شده در storage (احتمالاً از مسیر uploadDir در backend/server.js خط 20)
   - نمایش پیام موفقیت یا خطا
5. ذخیره‌سازی اطلاعات در دیتابیس (پیشنهاد: SQLite با better-sqlite3 یا Firebase که در frontend/index.html خط 11-18 پیکربندی شده).
6. افزودن middleware احراز هویت برای اطمینان از اینکه فقط مالک درس می‌تواند آن را حذف کند.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 12: ایجاد دستور /practice برای اتصال به بخش 'تمرین با استاد هوش مصنوعی'

**Scope:** این مرحله شامل ایجاد دستور /practice است که کاربر را به بخش تمرین با استاد هوش مصنوعی متصل می‌کند. باید وضعیت فعلی جلسات تمرین را نمایش دهد و گزینه‌هایی برای شروع جلسه جدید یا ادامه جلسه قبلی ارائه دهد. خارج از این مرحله: خود مکالمه با هوش مصنوعی. نکته حیاتی: باید از API موجود در بخش 'تمرین با استاد هوش مصنوعی' استفاده کند.
**Key terms:** /practice, AI tutor, session management, start session, continue session

**بخش مربوط از متن کاربر:**
```
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد دستور /practice برای اتصال به بخش تمرین با استاد هوش مصنوعی

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:79-137` — `app.post('/api/gemini/chat', ...)`
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
      if (payload.contents) {
        payload.contents = payload.contents.map(content => ({
          role: content.role,
          parts: content.parts.map(part => {
            if (part.inline_data && includeAudio) {
              return { inline_data: part.inline_data };
            }
            if (part.text !== undefined) {
              return { text: part.text };
            }
            return null;
          }).filter(p => p !== null)
        }));
      }
      const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${GEMINI_API_KEY}`;
      const response = await fetch(apiUrl, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(p
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد دستور /practice را داده است که کاربر را به بخش 'تمرین با استاد هوش مصنوعی' متصل کند. این دستور باید وضعیت فعلی جلسات تمرین را نمایش دهد و گزینه‌هایی برای شروع جلسه جدید یا ادامه جلسه قبلی ارائه دهد. نکته حیاتی: باید از API موجود در بخش 'تمرین با استاد هوش مصنوعی' استفاده کند. کاربر همچنین اشاره کرده: 'و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم'. کلیدواژه‌ها: /practice, AI tutor, session management, start session, continue session. خارج از این مرحله: خود مکالمه با هوش مصنوعی. در کد فعلی پروژه، backend/server.js شامل APIهای مربوط به Gemini است: endpoint /api/gemini/chat (خط 79) برای مکالمه، /api/gemini/tts (خط 140) برای تبدیل متن به گفتار، و /api/analyze-files (خط 727) برای تحلیل فایل. همچنین frontend/index.html شامل اسکریپت Inspector Bridge (خط 32) است. frontend/src/App.jsx احتمالاً کامپوننت اصلی React را شامل می‌شود. backend/package.json وابستگی‌های express, cors, dotenv, ws را نشان می‌دهد. frontend/package.json شامل react, firebase, lucide-react است. برای پیاده‌سازی /practice، نیاز به یک endpoint جدید در backend (مثلاً /api/practice/session) و یک کامپوننت جدید در frontend (مثلاً PracticePage.jsx) است که از API موجود Gemini برای شبیه‌سازی استاد هوش مصنوعی استفاده کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد endpoint جدید در backend/server.js با مسیر /api/practice/session که وضعیت جلسات تمرین را مدیریت کند. این endpoint باید از API Gemini (خطوط 79-137 در backend/server.js) برای ارتباط با هوش مصنوعی استفاده کند. 2. ایجاد یک کامپوننت React جدید در frontend/src/components/PracticePage.jsx که صفحه تمرین با استاد هوش مصنوعی را نمایش دهد. 3. افزودن مسیر /practice به روتر frontend (احتمالاً در frontend/src/App.jsx). 4. پیاده‌سازی منطق شروع جلسه جدید و ادامه جلسه قبلی با ذخیره‌سازی وضعیت در localStorage یا Firebase. 5. اطمینان از استفاده از API موجود در بخش 'تمرین با استاد هوش مصنوعی' (همان /api/gemini/chat).

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 13: ایجاد دستور /practice_topic برای انتخاب موضوع تمرین از تلگرام

**Scope:** این مرحله شامل ایجاد دستور /practice_topic <session_id> است که کاربر می‌تواند موضوع تمرین را برای یک جلسه انتخاب کند. باید لیست موضوعات موجود را از بخش AI دریافت کرده و به کاربر نمایش دهد. کاربر با انتخاب یک موضوع، آن را برای جلسه تنظیم می‌کند. خارج از این مرحله: ایجاد موضوع جدید. نکته حیاتی: موضوعات باید از API بخش AI دریافت شوند.
**Key terms:** /practice_topic, session_id, topic list, select topic, AI API

**بخش مربوط از متن کاربر:**
```
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد دستور /practice_topic برای انتخاب موضوع تمرین از تلگرام با دریافت لیست از API بخش AI

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:79-137` — `app.post('/api/gemini/chat', ...)`
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
      if (payload.contents) {
        payload.contents = payload.contents.map(content => ({
          role: content.role,
          parts: content.parts.map(part => {
            if (part.inline_data && includeAudio) {
              return { inline_data: part.inline_data };
            }
            if (part.text !== undefined) {
              return { text: part.text };
            }
            return null;
          }).filter(p => p !== null)
        }));
      }
      const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${GEMINI_API_KEY}`;
      const response = await fetch(apiUrl, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(payload)
      });
      if (!response.ok) {
        const errorData = await response.text();
        console.error('Gemini API error:', errorData);
        return res.status(response.status).json({ error: errorData });
      }
      const result = await response.json();
      if (includeAudio) {
        res.json(result);
      } else {
        const text = result.candidates?.[0]?.content?.parts?.[0]?.text;
        if (!text) {
          return res.status(
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد دستور /practice_topic <session_id> در تلگرام را دارد. این دستور باید به کاربر امکان دهد موضوع تمرین را برای یک جلسه مشخص انتخاب کند. لیست موضوعات موجود باید از API بخش AI دریافت شود و به کاربر نمایش داده شود. کاربر با انتخاب یک موضوع، آن را برای جلسه تنظیم می‌کند. ایجاد موضوع جدید خارج از این مرحله است. نکته حیاتی: موضوعات باید از API بخش AI دریافت شوند. بخش مربوط از درخواست اصلی کاربر: «و اگر به قسمت تمرین با استاد هوش مصنوعی وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم». کلیدواژه‌ها: /practice_topic, session_id, topic list, select topic, AI API. در کد فعلی پروژه (backend/server.js) هیچ endpoint یا مسیری برای دریافت لیست موضوعات از AI وجود ندارد. فایل‌های backend/server.js شامل endpointهای /api/gemini/chat (خط 79)، /api/gemini/tts (خط 140)، /api/analyze-files (خط 727) و /api/list-models (خط 195) است. همچنین frontend/index.html شامل اسکریپت Inspector Bridge است. هیچ فایل تلگرام بات یا مدیریت session در ساختار پروژه دیده نمی‌شود. بنابراین این تسک نیازمند ایجاد زیرساخت جدید برای تلگرام بات، ارتباط با API بخش AI برای دریافت موضوعات، و مدیریت session است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک فایل جدید backend/services/telegramBot.js برای مدیریت دستورات تلگرام. 2. در این فایل، یک هندلر برای دستور /practice_topic پیاده‌سازی شود که session_id را از آرگومان دریافت کند. 3. ایجاد یک endpoint جدید در backend/server.js مانند POST /api/ai/topics که لیست موضوعات را از API بخش AI (مثلاً با فراخوانی مدل Gemini) دریافت کند. 4. در هندلر /practice_topic، ابتدا با فراخوانی endpoint /api/ai/topics لیست موضوعات را دریافت کند. 5. لیست موضوعات را به صورت دکمه‌های شیشه‌ای (Inline Keyboard) در تلگرام به کاربر نمایش دهد. 6. پس از انتخاب کاربر، موضوع انتخاب‌شده را به session مرتبط ذخیره کند (مثلاً در یک فایل JSON یا دیتابیس موقت). 7. پیام تأیید به کاربر نمایش دهد. 8. مستندات و راهنمای استفاده از دستور در فایل README یا docs اضافه شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 14: ایجاد دستور /practice_chat برای شروع مکالمه با هوش مصنوعی از طریق تلگرام

**Scope:** این مرحله شامل ایجاد دستور /practice_chat <session_id> است که کاربر را وارد حالت مکالمه با هوش مصنوعی می‌کند. در این حالت، تمام پیام‌های کاربر به API هوش مصنوعی ارسال شده و پاسخ دریافت می‌شود. خارج از این مرحله: پردازش پیام صوتی. نکته حیاتی: باید state management داشته باشد تا بداند کاربر در کدام جلسه است.
**Key terms:** /practice_chat, session_id, chat mode, AI API, state management, conversation

**بخش مربوط از متن کاربر:**
```
و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد دستور /practice_chat برای مکالمه AI در تلگرام

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:79-137` — `app.post('/api/gemini/chat')` — این endpoint اصلی برای ارسال پیام به Gemini API است. ربات تلگرام باید از این endpoint برای ارسال پیام‌های کاربر و دریافت پاسخ استفاده کند.
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
      if (payload.contents) {
        payload.contents = payload.contents.map(content => ({
          role: content.role,
          parts: content.parts.map(part => {
            if (part.inline_data && includeAudio) {
              return { inline_data: part.inline_data };
            }
            if (part.text !== undefined) {
              return { text: part.text };
            }
            return null;
          }).filter(p => p !== null)
        }));
      }
  ```
- `backend/server.js:68-68` — `WebSocketServer` — WebSocket موجود برای ارتباط بلادرنگ. ربات تلگرام می‌تواند از WebSocket برای ارسال پاسخ‌های بلادرنگ به کاربر استفاده کند.
  ```jsx
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `frontend/index.html:11-18` — `Firebase Configuration` — Firebase برای احراز هویت استفاده می‌شود. ربات تلگرام می‌تواند از Firebase برای ذخیره session_id و وضعیت کاربران استفاده کند.
  ```
  var __firebase_config = JSON.stringify({
    apiKey: "AIzaSyAMFtV0zIMKKe0bIr68Z1wTORQ1jQpsv9Q",
    authDomain: "labaneseapp.firebaseapp.com",
    projectId: "labaneseapp",
    storageBucket: "labaneseapp.firebasestorage.app",
    messagingSenderId: "951874597795",
    appId: "1:951874597795:web:00745327993adad760a016"
  });
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/package.json` (سطر 1) — برای اضافه کردن وابستگی‌های جدید مانند node-telegram-bot-api یا telegraf
- `backend/.env.example` (سطر 1) — برای اضافه کردن متغیر محیطی TELEGRAM_BOT_TOKEN
- `frontend/src/App.jsx` (سطر 1) — کامپوننت اصلی فرانت‌اند که ممکن است نیاز به نمایش وضعیت مکالمه داشته باشد
- `prompt/_index.json` (سطر 1) — برای ثبت تسک جدید در سیستم oversight

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد دستور /practice_chat <session_id> را دارد که کاربر را وارد حالت مکالمه با هوش مصنوعی از طریق تلگرام می‌کند. در این حالت، تمام پیام‌های کاربر به API هوش مصنوعی ارسال شده و پاسخ دریافت می‌شود. خارج از این مرحله: پردازش پیام صوتی. نکته حیاتی: باید state management داشته باشد تا بداند کاربر در کدام جلسه است. کلیدواژه‌ها: /practice_chat, session_id, chat mode, AI API, state management, conversation. در پروژه فعلی، backend/server.js شامل endpointهای مربوط به Gemini API است: /api/gemini/chat (خط 79) برای چت، /api/gemini/tts (خط 140) برای تبدیل متن به گفتار، و /api/analyze-files (خط 727) برای تحلیل فایل. همچنین WebSocket در خط 68 با مسیر /ws/live راه‌اندازی شده. frontend/index.html شامل Inspector Bridge برای ارسال رویدادها به والد iframe است. پروژه از Firebase برای احراز هویت استفاده می‌کند (frontend/index.html خط 11-18).

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک فایل جدید backend/services/telegramBot.js برای مدیریت ربات تلگرام. 2. پیاده‌سازی دستور /practice_chat با پارامتر session_id. 3. ایجاد state management با استفاده از Map یا Redis برای ذخیره وضعیت جلسات کاربران. 4. اتصال به endpoint /api/gemini/chat در backend/server.js (خط 79) برای ارسال پیام‌های کاربر و دریافت پاسخ. 5. مدیریت پیام‌های صوتی با استفاده از endpoint /api/analyze-files (خط 727) برای تبدیل گفتار به متن. 6. ذخیره session_id و وضعیت مکالمه در state management. 7. پیاده‌سازی خروج از حالت مکالمه با دستور /exit یا timeout. 8. اضافه کردن middleware برای بررسی وضعیت کاربر قبل از پردازش هر پیام.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 15: پیاده‌سازی قابلیت ارسال پیام صوتی به هوش مصنوعی از تلگرام

**Scope:** این مرحله شامل اضافه کردن قابلیت دریافت پیام صوتی از کاربر در حالت مکالمه با هوش مصنوعی است. باید فایل صوتی را از تلگرام دریافت کرده، به سرویس Speech-to-Text ارسال کند، متن استخراج‌شده را به API هوش مصنوعی بفرستد و پاسخ را دریافت کند. خارج از این مرحله: پاسخ صوتی. نکته حیاتی: باید از میکروفون تلگرام استفاده شود و فایل صوتی به فرمت OGG دریافت می‌شود.
**Key terms:** voice message, speech-to-text, STT, OGG format, microphone, AI API

**بخش مربوط از متن کاربر:**
```
و از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن قابلیت دریافت و پردازش پیام صوتی OGG از تلگرام برای تحلیل با هوش مصنوعی

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server`

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی قابلیت ارسال پیام صوتی به هوش مصنوعی از تلگرام را دارد. این قابلیت شامل دریافت فایل صوتی از کاربر در حالت مکالمه با هوش مصنوعی است. فایل صوتی باید از تلگرام دریافت شود (با فرمت OGG که از میکروفون تلگرام می‌آید)، سپس به سرویس Speech-to-Text (STT) ارسال شود تا متن استخراج شود. متن استخراج‌شده باید به API هوش مصنوعی (که در این پروژه از طریق endpoint /api/gemini/chat در backend/server.js خط 79-137 پیاده‌سازی شده) فرستاده شود و پاسخ دریافت شود. پاسخ صوتی خارج از این مرحله است. نکته حیاتی: باید از میکروفون تلگرام استفاده شود و فایل صوتی به فرمت OGG دریافت می‌شود. کلیدواژه‌های کاربر: voice message, speech-to-text, STT, OGG format, microphone, AI API. در کد فعلی پروژه، backend/server.js شامل endpointهای /api/gemini/chat (خط 79) و /api/gemini/tts (خط 140) و /api/analyze-files (خط 727) است. endpoint تحلیل فایل‌ها از multer برای آپلود فایل استفاده می‌کند (خط 44-56) و فایل‌های صوتی را با mimeType 'audio/' پشتیبانی می‌کند (خط 32-38). تابع analyzeWithGemini (خط 296-317) محتوای صوتی را به صورت inline_data به Gemini API می‌فرستد. تابع uploadToGeminiFileAPI (خط 320-415) فایل‌های بزرگ را از طریق File API آپلود می‌کند. همچنین تابع extractAudioFromVideo (خط 527-551) برای استخراج صدا از ویدیو وجود دارد. frontend/index.html شامل اسکریپت Inspector Bridge (خط 32-201) است که رویدادهای کلیک، اسکرول، تایپ و فوکوس را به parent ارسال می‌کند. پروژه از React (frontend/src/App.jsx) و Vite (frontend/vite.config.js) استفاده می‌کند. برای پیاده‌سازی این قابلیت، نیاز به یک endpoint جدید در backend برای دریافت فایل OGG از تلگرام (احتمالاً از طریق webhook یا polling)، تبدیل OGG به فرمت قابل قبول Gemini (مثلاً WAV یا MP3 با ffmpeg که در backend/server.js خط 11-17 پیکربندی شده)، ارسال به سرویس STT (می‌توان از Gemini API خود پروژه استفاده کرد چون از audio پشتیبانی می‌کند)، و سپس ارسال متن به /api/gemini/chat است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک endpoint جدید در backend/server.js مثلاً POST /api/telegram/voice که فایل OGG را از تلگرام دریافت کند. 2. استفاده از multer (که قبلاً در خط 44-56 پیکربندی شده) برای دریافت فایل صوتی. 3. تبدیل فایل OGG به WAV یا MP3 با استفاده از ffmpeg (که در خط 17 تنظیم شده) چون Gemini API ممکن است OGG را مستقیماً پشتیبانی نکند. 4. ارسال فایل صوتی تبدیل‌شده به Gemini API برای Speech-to-Text: می‌توان از تابع analyzeWithGemini (خط 296) با inline_data حاوی base64 فایل صوتی استفاده کرد، یا برای فایل‌های بزرگ از uploadToGeminiFileAPI (خط 320). 5. متن استخراج‌شده را به endpoint /api/gemini/chat (خط 79) بفرستد تا پاسخ هوش مصنوعی دریافت شود. 6. پاسخ را به تلگرام برگرداند (به صورت متن). 7. اضافه کردن مسیر جدید به frontend برای نمایش وضعیت پردازش صوتی (اختیاری). 8. به‌روزرسانی frontend/src/App.jsx برای پشتیبانی از دکمه ارسال صدا.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 16: پیاده‌سازی قابلیت دریافت پاسخ صوتی از هوش مصنوعی در تلگرام

**Scope:** این مرحله شامل اضافه کردن قابلیت ارسال پاسخ صوتی از هوش مصنوعی به کاربر است. باید متن پاسخ هوش مصنوعی را به سرویس Text-to-Speech بفرستد، فایل صوتی حاصل را در تلگرام آپلود کند و برای کاربر ارسال کند. خارج از این مرحله: انتخاب صدای مختلف. نکته حیاتی: باید هم پاسخ صوتی و هم متن پاسخ به صورت همزمان ارسال شود.
**Key terms:** text-to-speech, TTS, voice response, audio file, dual response

**بخش مربوط از متن کاربر:**
```
و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد
```

## 🎯 هدف (خلاصه ساختاریافته)
اضافه کردن پاسخ صوتی TTS به تلگرام

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:140-187` — `app.post('/api/gemini/tts')` — این endpoint TTS موجود است که باید توسط سرویس جدید فراخوانی شود. خروجی آن base64 audioData و mimeType است.
  ```jsx
  app.post('/api/gemini/tts', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
  
    try {
      const { prompt, voice = 'Kore' } = req.body;
      const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-preview-tts:generateContent?key=${GEMINI_API_KEY}`;
  ```
- `backend/server.js:79-137` — `app.post('/api/gemini/chat')` — این endpoint چت موجود است که باید برای دریافت پاسخ متنی از هوش مصنوعی استفاده شود.
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
  
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
  ```
- `backend/server.js:61-68` — `server و wss` — سرور HTTP موجود باید برای Webhook تلگرام استفاده شود. Webhook به مسیر /api/telegram/webhook اضافه می‌شود.
  ```jsx
  const app = express();
  const PORT = process.env.PORT || 3001;
  
  // Create HTTP server
  const server = createServer(app);
  
  // Create WebSocket server
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/.env.example` (سطر 1) — باید متغیر TELEGRAM_BOT_TOKEN به این فایل اضافه شود تا کاربر بتواند توکن ربات تلگرام را تنظیم کند.
- `backend/package.json` (سطر 1) — باید وابستگی node-telegram-bot-api به package.json اضافه شود.
- `frontend/src/App.jsx` — این فایل deep-read نشده، اما احتمالاً کامپون

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی قابلیت دریافت پاسخ صوتی از هوش مصنوعی در تلگرام را دارد. این مرحله شامل اضافه کردن قابلیت ارسال پاسخ صوتی از هوش مصنوعی به کاربر است. باید متن پاسخ هوش مصنوعی را به سرویس Text-to-Speech بفرستد، فایل صوتی حاصل را در تلگرام آپلود کند و برای کاربر ارسال کند. خارج از این مرحله: انتخاب صدای مختلف. نکته حیاتی: باید هم پاسخ صوتی و هم متن پاسخ به صورت همزمان ارسال شود. بخش مربوط از درخواست اصلی کاربر: 'و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد'. کلیدواژه‌ها: text-to-speech, TTS, voice response, audio file, dual response. در کد فعلی backend/server.js از خط 140 تا 187 endpoint /api/gemini/tts وجود دارد که از مدل gemini-2.5-flash-preview-tts استفاده می‌کند و خروجی base64 audioData و mimeType برمی‌گرداند. همچنین endpoint /api/gemini/chat در خط 79-137 وجود دارد که برای چت استفاده می‌شود. پروژه فاقد هرگونه کد مرتبط با تلگرام (bot API) است و باید از صفر پیاده‌سازی شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/services/telegramBot.js برای مدیریت Webhook و ارسال پیام‌های تلگرام. 2. ایجاد فایل جدید backend/services/ttsService.js برای سرویس TTS با استفاده از endpoint موجود /api/gemini/tts در backend/server.js خط 140-187. 3. ایجاد فایل جدید backend/services/chatService.js برای مدیریت چت و ترکیب پاسخ صوتی و متنی. 4. اضافه کردن endpoint جدید /api/telegram/webhook در backend/server.js برای دریافت Webhook از تلگرام. 5. پیاده‌سازی منطق ارسال همزمان پاسخ صوتی و متنی: پس از دریافت پیام از تلگرام، متن را به /api/gemini/chat ارسال کند، سپس پاسخ متنی را به /api/gemini/tts بدهد تا فایل صوتی تولید شود، سپس فایل صوتی را در تلگرام آپلود کند و هم متن و هم صدا را برای کاربر ارسال کند. 6. استفاده از کتابخانه node-telegram-bot-api برای ارتباط با Telegram Bot API. 7. اضافه کردن متغیر محیطی TELEGRAM_BOT_TOKEN به backend/.env.example.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 17: ایجاد دستور /practice_end برای پایان جلسه تمرین از تلگرام

**Scope:** این مرحله شامل ایجاد دستور /practice_end <session_id> است که کاربر می‌تواند جلسه تمرین را پایان دهد. باید خلاصه جلسه (تعداد پیام‌ها، موضوع، مدت زمان) را از API هوش مصنوعی دریافت کرده و برای کاربر ارسال کند. خارج از این مرحله: ذخیره تاریخچه. نکته حیاتی: باید state کاربر را به حالت عادی برگرداند.
**Key terms:** /practice_end, session_id, session summary, end session, reset state

**بخش مربوط از متن کاربر:**
```
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد دستور /practice_end برای پایان جلسه تمرین تلگرام

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:79-137` — `app.post('/api/gemini/chat')`
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
      // Clean payload - preserve audio data if includeAudio flag is set
      if (payload.contents) {
        payload.contents = payload.contents.map(content => ({
          role: content.role,
          parts: content.parts.map(part => {
            if (part.inline_data && includeAudio) {
              return { inline_data: part.inline_data };
            }
            if (part.text !== undefined) {
              return { text: part.text };
            }
            return null;
          }).filter(p => p !== null)
        }));
      }
      const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${GEMINI_API_KEY}`;
      const response = await fetch(apiUrl, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(payload)
      });
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد دستور /practice_end <session_id> برای پایان جلسه تمرین از تلگرام را داده است. این دستور باید جلسه تمرین را پایان دهد، خلاصه جلسه (تعداد پیام‌ها، موضوع، مدت زمان) را از API هوش مصنوعی دریافت کرده و برای کاربر ارسال کند. خارج از این مرحله: ذخیره تاریخچه. نکته حیاتی: باید state کاربر را به حالت عادی برگرداند. بخش مربوط از درخواست اصلی کاربر: 'و اگر به قسمت تمرین با استاد هوش مصنوعی وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم'. کلیدواژه‌ها: /practice_end, session_id, session summary, end session, reset state. در کد فعلی پروژه (backend/server.js) هیچ endpoint یا handler مربوط به تلگرام وجود ندارد. فایل‌های deep-read شده شامل backend/server.js (خطوط 1-1432) است که شامل APIهای Gemini Chat (خط 79), TTS (خط 140), File Analysis (خط 727) و WebSocket (خط 68) است. همچنین frontend/index.html شامل Inspector Bridge (خطوط 32-201) است. هیچ فایل تلگرام بات یا handler برای دستورات تلگرام در ساختار پروژه دیده نمی‌شود. بنابراین این تسک نیاز به ایجاد یک سرویس تلگرام بات جدید (احتمالاً در backend) دارد که دستور /practice_end را پردازش کند. session_id باید از کاربر دریافت شود، سپس با API هوش مصنوعی (مثلاً Gemini Chat API در خط 79 backend/server.js) برای تولید خلاصه جلسه ارتباط برقرار کند. state کاربر باید پس از پایان جلسه به حالت عادی (idle) برگردد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/services/telegramBot.js که شامل یک تلگرام بات با استفاده از کتابخانه node-telegram-bot-api (نیاز به نصب) است. 2. در این فایل، هندلر برای دستور /practice_end <session_id> ایجاد شود که: الف) session_id را از متن پیام استخراج کند. ب) با استفاده از تابع analyzeWithGemini در backend/server.js (خط 296) یا یک endpoint جدید، خلاصه جلسه (تعداد پیام‌ها، موضوع، مدت زمان) را از API هوش مصنوعی دریافت کند. ج) خلاصه را به کاربر در تلگرام ارسال کند. د) state کاربر را در یک state manager (مثلاً یک Map در حافظه یا دیتابیس) به حالت عادی (idle) برگرداند. 3. ایجاد endpoint جدید در backend/server.js مانند POST /api/practice/end که session_id را دریافت کرده و خلاصه را از Gemini API بگیرد. 4. ایجاد یک state manager ساده (مثلاً backend/services/sessionState.js) که state کاربران را مدیریت کند (states: idle, in_practice). 5. اطمینان از اینکه ذخیره تاریخچه خارج از این مرحله است (کاربر گفته خارج از این مرحله). 6. تست دستور با ارسال /practice_end <session_id> به بات و بررسی پاسخ.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 18: ایجاد دستور /practice_history برای مشاهده تاریخچه جلسات تمرین از تلگرام

**Scope:** این مرحله شامل ایجاد دستور /practice_history است که کاربر می‌تواند تاریخچه جلسات تمرین خود را مشاهده کند. باید از دیتابیس یا API هوش مصنوعی تاریخچه را دریافت کرده و به صورت لیست نمایش دهد. خارج از این مرحله: مشاهده جزئیات هر جلسه. نکته حیاتی: باید صفحه‌بندی شود.
**Key terms:** /practice_history, session history, pagination, list sessions

**بخش مربوط از متن کاربر:**
```
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد دستور /practice_history برای مشاهده تاریخچه جلسات تمرین از تلگرام با صفحه‌بندی

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:79-137` — `app.post('/api/gemini/chat')` — این endpoint الگوی مناسبی برای ایجاد endpoint جدید /api/practice-history است. باید GET endpoint با پارامترهای page و limit ایجاد شود.
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
  ```
- `backend/server.js:727-800` — `app.post('/api/analyze-files')` — این endpoint نشان می‌دهد که backend از middleware و error handling استاندارد استفاده می‌کند. endpoint جدید باید از همین الگو پیروی کند.
  ```jsx
  app.post('/api/analyze-files', upload.array('files', MAX_FILES), handleMulterError, async (req, res) => {
    console.log('Received file analysis request');
    if (!GEMINI_API_KEY) {
      console.error('API key not configured');
      return res.status(500).json({ error: 'API key not configured' });
    }
  ```
- `frontend/index.html:9-20` — `Firebase Configuration` — Firebase از قبل پیکربندی شده و می‌توان از Firestore برای ذخیره تاریخچه جلسات تمرین استفاده کرد. این فایل deep-read شده است.
  ```
  var __firebase_config = JSON.stringify({
    apiKey: "AIzaSyAMFtV0zIMKKe0bIr68Z1wTORQ1jQpsv9Q",
    authDomain: "labaneseapp.firebaseapp.com",
    projectId: "labaneseapp",
    storageBucket: "labaneseapp.firebasestorage.app",
    messagingSenderId: "951874597795",
    appId: "1:951874597795:web:00745327993adad760a016"
  });
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد دستور /practice_history برای مشاهده تاریخچه جلسات تمرین از تلگرام را دارد. این دستور باید تاریخچه جلسات تمرین کاربر را از دیتابیس یا API هوش مصنوعی دریافت کرده و به صورت لیست نمایش دهد. نکته حیاتی: باید صفحه‌بندی شود. خارج از این مرحله: مشاهده جزئیات هر جلسه. بخش مربوط از درخواست اصلی کاربر: 'و اگر به قسمت تمرین با استاد هوش مصنوعی وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم'. کلیدواژه‌ها: /practice_history, session history, pagination, list sessions. در کد فعلی پروژه (backend/server.js) هیچ endpoint یا مسیری برای دریافت تاریخچه جلسات تمرین وجود ندارد. فایل‌های مرتبط با تلگرام در ساختار پروژه دیده نمی‌شوند و این قابلیت باید از صفر ساخته شود. فایل‌های backend/server.js (خطوط 79-137 برای API chat، خطوط 727-800 برای analyze-files) نشان می‌دهند که backend از Express.js با ساختار REST استفاده می‌کند. frontend/index.html (خطوط 9-20) حاوی Firebase config است که می‌تواند برای ذخیره تاریخچه استفاده شود. backend/.env.example نشان می‌دهد که Firebase config اختیاری است. package.json (root) حاوی firebase dependency است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک endpoint جدید در backend/server.js با مسیر GET /api/practice-history که تاریخچه جلسات تمرین کاربر را با صفحه‌بندی برمی‌گرداند. 2. طراحی ساختار دیتابیس (Firebase Firestore یا فایل JSON) برای ذخیره جلسات تمرین با فیلدهای: sessionId, userId, topic, date, summary. 3. پیاده‌سازی منطق صفحه‌بندی با پارامترهای page و limit در endpoint. 4. ایجاد یک سرویس تلگرام (فایل جدید backend/services/telegram.js) که دستور /practice_history را پردازش کند. 5. اضافه کردن هندلر برای دستور /practice_history در سرویس تلگرام که endpoint را فراخوانی کرده و نتیجه را به صورت لیست صفحه‌بندی شده نمایش دهد. 6. استفاده از Firebase (که در frontend/index.html خط 11-18 پیکربندی شده) برای ذخیره و بازیابی تاریخچه جلسات. 7. پیاده‌سازی دکمه‌های صفحه‌بندی (قبلی/بعدی) در پاسخ تلگرام با استفاده از Inline Keyboard.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 19: ایجاد دستور /practice_details برای مشاهده جزئیات یک جلسه تمرین خاص

**Scope:** این مرحله شامل ایجاد دستور /practice_details <session_id> است که کاربر می‌تواند جزئیات یک جلسه خاص (موضوع، تعداد پیام‌ها، خلاصه) را مشاهده کند. باید از API هوش مصنوعی یا دیتابیس اطلاعات را دریافت کند. خارج از این مرحله: مشاهده متن کامل مکالمه. نکته حیاتی: باید خلاصه‌ای از مکالمه ارائه دهد.
**Key terms:** /practice_details, session_id, session details, summary

**بخش مربوط از متن کاربر:**
```
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد دستور /practice_details برای مشاهده جزئیات جلسه تمرین

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:79-137` — `app.post('/api/gemini/chat')` — این endpoint نمونه‌ای از نحوه ارتباط با Gemini API است. برای generateSummary می‌توان از همین الگو استفاده کرد.
  ```jsx
  app.post('/api/gemini/chat', async (req, res) => {
    if (!GEMINI_API_KEY) {
      return res.status(500).json({ error: 'API key not configured' });
    }
    try {
      const payload = req.body;
      const includeAudio = payload.includeAudio;
      delete payload.includeAudio;
  ```
- `backend/server.js:296-317` — `async function analyzeWithGemini(parts, systemPrompt)` — این تابع برای تحلیل محتوا با Gemini استفاده می‌شود. می‌توان از آن برای تولید خلاصه session استفاده کرد.
  ```jsx
  async function analyzeWithGemini(parts, systemPrompt) {
    const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${GEMINI_API_KEY}`;
    const payload = {
      contents: [{ role: 'user', parts }],
      systemInstruction: { parts: [{ text: systemPrompt }] }
    };
  ```
- `frontend/src/App.jsx:1-50` — `App component` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. احتمالاً نیاز به اضافه کردن state و handler برای نمایش جزئیات session دارد.
  ```jsx
  فایل deep-read نشده — بر اساس ساختار سطحی، احتمالاً کامپوننت اصلی React است
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/package.json` (سطر 1) — برای اضافه کردن وابستگی‌های جدید (مثلاً sqlite3 یا firebase-admin) نیاز به تغییر دارد
- `frontend/src/App.jsx` (سطر 1) — کامپوننت اصلی فرانت‌اند که باید دستور /practice_details را مدیریت کند
- `frontend/index.html` (سطر 1) — فایل HTML اصلی که ممکن است نیاز به اضافه کردن routing یا state management داشته باشد
- `backend/.env.example` (سطر 1) — برای اضافه کردن متغیرهای محیطی جدید (مثلاً DATABASE_URL) نیاز به تغییر دارد

## 🌐 نقشهٔ وابستگی‌ها
این قابلیت به backend/server.js وابسته است (برای endpoint‌های جدید و ارتباط با Gemini API). همچنین به یک دیتابیس (SQLite یا Firebase) برای ذخیره session

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد دستور /practice_details <session_id> را دارد تا بتواند جزئیات یک جلسه تمرین خاص (موضوع، تعداد پیام‌ها، خلاصه) را مشاهده کند. این دستور باید از API هوش مصنوعی یا دیتابیس اطلاعات را دریافت کند. خارج از این مرحله: مشاهده متن کامل مکالمه. نکته حیاتی: باید خلاصه‌ای از مکالمه ارائه دهد. بخش مربوط از درخواست اصلی کاربر: 'و اگر به قسمت تمرین با استاد هوش مصنوعی وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم'. کلیدواژه‌ها: /practice_details, session_id, session details, summary. در کد فعلی پروژه (backend/server.js) هیچ endpoint یا مسیری برای مدیریت جلسات تمرین (practice sessions) وجود ندارد. فایل‌های مرتبط با مدیریت session یا ذخیره‌سازی مکالمه در deep context موجود نیستند. ساختار پروژه شامل backend/server.js با Express.js و WebSocket است اما هیچ دیتابیس یا مدل Session تعریف نشده. فایل frontend/src/App.jsx در deep context موجود است اما محتوای آن نشان‌دهنده یک اپ React ساده است. برای پیاده‌سازی این قابلیت نیاز به ایجاد ساختار دیتابیس (احتمالاً SQLite یا Firebase) و endpoint جدید در backend داریم.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد مدل Session در backend با فیلدهای: session_id (UUID), topic (string), message_count (integer), summary (text), created_at (timestamp), updated_at (timestamp). 2. ایجاد endpoint GET /api/practice/sessions/:session_id در backend/server.js که جزئیات جلسه را برگرداند. 3. ایجاد endpoint GET /api/practice/sessions برای لیست جلسات کاربر. 4. ایجاد middleware احراز هویت (با Firebase یا token ساده) برای دسترسی به session‌ها. 5. پیاده‌سازی تابع generateSummary(session_id) که با استفاده از Gemini API (موجود در backend/server.js خطوط 296-317) خلاصه‌ای از مکالمه تولید کند. 6. ذخیره‌سازی session‌ها در فایل JSON یا SQLite (بسته به پیچیدگی). 7. ایجاد دستور /practice_details در فرانت‌اند (احتمالاً در frontend/src/App.jsx) که با کلیک روی session جزئیات را نمایش دهد.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 20: ایجاد دستور /settings برای مدیریت تنظیمات نوتیفیکیشن و تلگرام

**Scope:** این مرحله شامل ایجاد دستور /settings است که کاربر می‌تواند تنظیمات نوتیفیکیشن (فعال/غیرفعال کردن انواع نوتیفیکیشن‌ها) و تنظیمات عمومی تلگرام را مدیریت کند. باید از طریق دکمه‌های inline keyboard تعامل انجام شود. خارج از این مرحله: تنظیمات پیشرفته. نکته حیاتی: تنظیمات باید در دیتابیس ذخیره شود.
**Key terms:** /settings, notification settings, inline keyboard, user preferences, database

**بخش مربوط از متن کاربر:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد دستور /settings برای مدیریت تنظیمات نوتیفیکیشن و تلگرام

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:68-68` — `WebSocketServer` — WebSocket موجود می‌تواند برای ارتباط دوطرفه تلگرام استفاده شود. نیاز به افزودن هندلر برای پیام‌های تلگرام.
  ```jsx
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/server.js:76-77` — `GEMINI_API_KEY` — الگوی مشابه برای خواندن TELEGRAM_BOT_TOKEN از environment variables نیاز است.
  ```jsx
  const GEMINI_API_KEY = process.env.GEMINI_API_KEY;
  ```
- `frontend/index.html:9-20` — `__firebase_config` — Firebase از قبل پیکربندی شده و می‌توان از Firestore برای ذخیره تنظیمات کاربر استفاده کرد.
  ```
  var __firebase_config = JSON.stringify({
          apiKey: "AIzaSyAMFtV0zIMKKe0bIr68Z1wTORQ1jQpsv9Q",
          authDomain: "labaneseapp.firebaseapp.com",
          projectId: "labaneseapp",
          storageBucket: "labaneseapp.firebasestorage.app",
          messagingSenderId: "951874597795",
          appId: "1:951874597795:web:00745327993adad760a016"
        });
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد دستور /settings را داده است که از طریق آن کاربر بتواند تنظیمات نوتیفیکیشن (فعال/غیرفعال کردن انواع نوتیفیکیشن‌ها) و تنظیمات عمومی تلگرام را مدیریت کند. تعامل باید از طریق دکمه‌های inline keyboard انجام شود. نکته حیاتی: تنظیمات باید در دیتابیس ذخیره شود. این درخواست برگرفته از بخش 'میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه' است. کلیدواژه‌های اصلی: /settings, notification settings, inline keyboard, user preferences, database. خارج از این مرحله: تنظیمات پیشرفته. پروژه فعلی یک اپلیکیشن آموزش لهجه لبنانی با هوش مصنوعی است که از React (frontend) و Express.js (backend) استفاده می‌کند. در backend/server.js خطوط 1-12 مشاهده می‌شود که از express, cors, ws, multer, ffmpeg استفاده شده و WebSocket در خط 68 با مسیر '/ws/live' راه‌اندازی شده است. در frontend/index.html خطوط 9-20 تنظیمات Firebase (apiKey: AIzaSyAMFtV0zIMKKe0bIr68Z1wTORQ1jQpsv9Q, authDomain: labaneseapp.firebaseapp.com, projectId: labaneseapp) وجود دارد. در frontend/src/App.jsx (فایل deep-read نشده) احتمالاً کامپوننت‌های اصلی UI قرار دارند. backend/.env.example و frontend/.env.example هر دو حاوی تنظیمات Firebase و WebSocket هستند. backend/package.json وابستگی‌های express, cors, dotenv, ws را نشان می‌دهد. frontend/package.json وابستگی‌های react, react-dom, firebase, lucide-react را نشان می‌دهد. در حال حاضر هیچ endpoint یا دستوری برای مدیریت تنظیمات نوتیفیکیشن تلگرام وجود ندارد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک مدل دیتابیس جدید (مثلاً UserSettings) در backend برای ذخیره تنظیمات نوتیفیکیشن و تلگرام هر کاربر. 2. ایجاد endpoint جدید در backend/server.js (مثلاً POST /api/settings و GET /api/settings) برای ذخیره و بازیابی تنظیمات. 3. ایجاد یک سرویس تلگرام (مثلاً backend/services/telegramService.js) که شامل هندلر دستور /settings باشد. 4. پیاده‌سازی inline keyboard برای نمایش گزینه‌های فعال/غیرفعال کردن انواع نوتیفیکیشن‌ها. 5. اتصال به Firebase (که در frontend/index.html خط 11-18 پیکربندی شده) یا یک دیتابیس دیگر برای ذخیره تنظیمات. 6. ایجاد کامپوننت React جدید (مثلاً frontend/src/components/SettingsPanel.jsx) برای نمایش تنظیمات در UI وب. 7. اضافه کردن دکمه/لینک به تنظیمات در frontend/src/App.jsx. 8. تست ارتباط دوطرفه تلگرام با استفاده از WebSocket موجود در backend/server.js خط 68.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 21: ایجاد دستور /help برای نمایش راهنمای کامل دستورات تلگرام

**Scope:** این مرحله شامل به‌روزرسانی دستور /help است تا تمام دستورات جدید (courses, upload, practice, settings و ...) را به صورت دسته‌بندی‌شده نمایش دهد. باید با inline keyboard یا Markdown فرمت شود. خارج از این مرحله: راهنمای پیشرفته. نکته حیاتی: باید بر اساس سطح دسترسی کاربر، دستورات متفاوت نمایش دهد.
**Key terms:** /help, command list, categorized, access level, inline keyboard

**بخش مربوط از متن کاربر:**
```
بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد دستور /help برای نمایش راهنمای دستورات تلگرام با سطح دسترسی

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:68-68` — `WebSocketServer` — این خط WebSocket سرور را روی path /ws/live راه‌اندازی می‌کند. برای ربات تلگرام، می‌توانیم از همین WebSocket استفاده کنیم یا یک endpoint مجزا ایجاد کنیم. فایل deep-read شده است.
  ```jsx
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/server.js:70-71` — `app.use(cors()) و app.use(express.json())` — این middlewareها برای پردازش درخواست‌های JSON و CORS تنظیم شده‌اند. برای webhook تلگرام، نیاز به تنظیمات اضافی نیست چون تلگرام مستقیماً POST می‌فرستد. فایل deep-read شده است.
  ```jsx
  app.use(cors());
  app.use(express.json({ limit: '10mb' }));
  ```
- `backend/server.js:76-76` — `GEMINI_API_KEY` — متغیر محیطی GEMINI_API_KEY در خط 76 تعریف شده. برای ربات تلگرام، یک متغیر محیطی جدید مثل TELEGRAM_BOT_TOKEN نیاز است. فایل deep-read شده است.

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/.env.example` (سطر 1) — این فایل حاوی نمونه متغیرهای محیطی است. باید TELEGRAM_BOT_TOKEN را به آن اضافه کرد تا کاربران بدانند چه متغیری را تنظیم کنند.
- `frontend/src/App.jsx` — اگر ربات تلگرام قرار است با فرانت‌اند ارتباط داشته

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد دستور /help برای نمایش راهنمای کامل دستورات تلگرام را داده است. این دستور باید تمام دستورات جدید (courses, upload, practice, settings و ...) را به صورت دسته‌بندی‌شده نمایش دهد. فرمت خروجی باید با inline keyboard یا Markdown باشد. نکته حیاتی: باید بر اساس سطح دسترسی کاربر، دستورات متفاوت نمایش دهد. خارج از این مرحله: راهنمای پیشرفته. کاربر در بخش مربوط از درخواست اصلی خود گفته: 'بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم'. کلیدواژه‌ها: /help, command list, categorized, access level, inline keyboard. در کد فعلی پروژه (backend/server.js) هیچ endpoint یا handler مربوط به تلگرام وجود ندارد. فایل‌های frontend/index.html و frontend/src/App.jsx نیز مربوط به فرانت‌اند وب هستند و ارتباطی با تلگرام ندارند. فایل .github/workflows/claude-auto-task.yml مربوط به CI/CD است. بنابراین این تسک نیاز به ایجاد یک ماژول جدید برای ربات تلگرام دارد. با توجه به ساختار پروژه که از Express.js و WebSocket استفاده می‌کند، ربات تلگرام باید به عنوان یک سرویس جدید در backend اضافه شود. فایل‌های prompt/archive/... حاوی تسک‌های قبلی هستند که برخی به ساختار بک‌اند و پیکربندی Inspector Bridge اشاره دارند اما هیچکدام به تلگرام مرتبط نیستند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/services/telegramBot.js که حاوی کلاس TelegramBotHandler باشد. 2. در این فایل، متد handleHelpCommand(userId, accessLevel) را پیاده‌سازی کن که بر اساس سطح دسترسی کاربر (admin/user/guest) لیست دستورات را فیلتر کند. 3. دستورات را به دسته‌های General (start, help), Learning (courses, practice, upload), Settings (settings, profile) تقسیم کن. 4. خروجی را با Markdown فرمت کن و یک inline keyboard با دکمه‌های دسته‌بندی شده اضافه کن. 5. در backend/server.js، یک WebSocket endpoint جدید یا یک middleware برای اتصال به Telegram Bot API اضافه کن (با استفاده از کتابخانه node-telegram-bot-api یا telegraf). 6. متد handleHelpCommand را به webhook handler متصل کن. 7. یک فایل کانفیگ backend/config/commands.js ایجاد کن که لیست کامل دستورات و سطوح دسترسی مجاز را تعریف کند.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 22: پیاده‌سازی سیستم لاگینگ و مانیتورینگ برای ربات تلگرام

**Scope:** این مرحله شامل اضافه کردن لاگینگ برای تمام تعاملات ربات تلگرام (دستورات، خطاها، زمان پاسخ) است. باید لاگ‌ها در فایل یا دیتابیس ذخیره شوند و یک دستور /admin_logs برای مدیران ایجاد شود. خارج از این مرحله: آنالیز لاگ. نکته حیاتی: لاگ‌ها نباید شامل اطلاعات حساس (پسورد) باشند.
**Key terms:** logging, monitoring, /admin_logs, error tracking, audit trail

**بخش مربوط از متن کاربر:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی سیستم لاگینگ و مانیتورینگ برای ربات تلگرام

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:1-12` — `imports` — محل اضافه کردن import برای ربات تلگرام و لاگر
  ```jsx
  import express from 'express';
  import cors from 'cors';
  import { fileURLToPath } from 'url';
  import { dirname, join } from 'path';
  import dotenv from 'dotenv';
  import { createServer } from 'http';
  import { WebSocketServer, WebSocket } from 'ws';
  import multer from 'multer';
  import fs from 'fs';
  import os from 'os';
  import ffmpeg from 'fluent-ffmpeg';
  import ffmpegStatic from 'ffmpeg-static';
  ```
- `backend/server.js:68-68` — `WebSocketServer` — WebSocket موجود که می‌تواند برای مانیتورینگ real-time استفاده شود
  ```jsx
  const wss = new WebSocketServer({ server, path: '/ws/live' });
  ```
- `backend/server.js:76-76` — `GEMINI_API_KEY` — الگوی خواندن API key از env - مشابه برای TELEGRAM_BOT_TOKEN
  ```jsx
  const GEMINI_API_KEY = process.env.GEMINI_API_KEY;
  ```
- `backend/server.js:189-192` — `health check` — محل اضافه کردن endpoint برای وضعیت ربات تلگرام
  ```jsx
  app.get('/api/health', (req, res) => {
    res.json({ status: 'ok', timestamp: new Date().toISOString() });
  });
  ```
- `backend/server.js:727-727` — `file analysis endpoint`

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی سیستم لاگینگ و مانیتورینگ برای ربات تلگرام را دارد. این شامل اضافه کردن لاگینگ برای تمام تعاملات ربات تلگرام (دستورات، خطاها، زمان پاسخ) است. لاگ‌ها باید در فایل یا دیتابیس ذخیره شوند و یک دستور /admin_logs برای مدیران ایجاد شود. خارج از این مرحله: آنالیز لاگ. نکته حیاتی: لاگ‌ها نباید شامل اطلاعات حساس (پسورد) باشند. کاربر همچنین اشاره کرده: 'میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه'. کلیدواژه‌ها: logging, monitoring, /admin_logs, error tracking, audit trail. در کد فعلی پروژه (backend/server.js) هیچ ربات تلگرامی وجود ندارد. فایل‌های مرتبط با WebSocket در backend/server.js خط 68 (const wss = new WebSocketServer({ server, path: '/ws/live' })) و frontend/index.html خط 32-201 (Inspector Bridge Script) وجود دارند که می‌توانند برای مانیتورینگ استفاده شوند. همچنین فایل .github/workflows/claude-auto-task.yml شامل یک workflow خودکار است که می‌تواند برای اجرای تسک‌ها استفاده شود. backend/package.json شامل dependencies: express, cors, dotenv, ws است. frontend/package.json شامل react, firebase, lucide-react است. backend/.env.example و frontend/.env.example شامل متغیرهای محیطی برای Firebase و WebSocket هستند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. با توجه به اینکه در کد فعلی پروژه (backend/server.js) هیچ ربات تلگرامی وجود ندارد و کاربر درخواست 'نوتیفیکشن از طریق تلگرام' و 'تلگرام دو طرفه' را دارد، مراحل زیر پیشنهاد می‌شود:

1. ایجاد فایل جدید backend/services/telegramBot.js برای پیاده‌سازی ربات تلگرام با استفاده از کتابخانه node-telegram-bot-api.
2. ایجاد فایل جدید backend/services/logger.js برای سیستم لاگینگ با قابلیت ذخیره در فایل و دیتابیس.
3. ایجاد فایل جدید backend/services/adminHandler.js برای مدیریت دستورات ادمین مانند /admin_logs.
4. ایجاد فایل جدید backend/models/Log.js برای مدل لاگ در دیتابیس (اگر از MongoDB استفاده شود).
5. ایجاد فایل جدید backend/routes/telegram.js برای مدیریت Webhook تلگرام.
6. به‌روزرسانی backend/server.js برای اضافه کردن مسیر Webhook تلگرام و راه‌اندازی ربات.
7. ایجاد فایل جدید backend/config/telegram.js برای تنظیمات ربات تلگرام.
8. به‌روزرسانی backend/.env.example برای اضافه کردن متغیرهای محیطی مربوط به تلگرام.
9. به‌روزرسانی backend/package.json برای اضافه کردن وابستگی node-telegram-bot-api.
10. ایجاد فایل جدید backend/middleware/errorLogger.js برای لاگ کردن خطاها.
11. ایجاد فایل جدید backend/utils/sanitizeLog.js برای پاکسازی اطلاعات حساس از لاگ‌ها.
12. ایجاد فایل جدید backend/tests/telegram.test.js برای تست ربات تلگرام.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 23: ایجاد تست‌های واحد برای سرویس تلگرام

**Scope:** این مرحله شامل نوشتن تست‌های واحد برای TelegramService شامل ارسال پیام، دریافت پیام، مدیریت state و هندل کردن خطاها است. باید با mock کردن API تلگرام تست شود. خارج از این مرحله: تست‌های integration. نکته حیاتی: باید حداقل ۸۰% کد سرویس تلگرام تحت پوشش تست باشد.
**Key terms:** unit tests, TelegramService, mock, pytest, coverage

**بخش مربوط از متن کاربر:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد تست‌های واحد برای TelegramService با mock

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:1-1432` — `کل فایل` — فایل اصلی بک‌اند — سرویس تلگرام باید به این فایل اضافه شود یا به‌عنوان ماژول جدا import شود
  ```jsx
  import express from 'express';
  import cors from 'cors';
  import { fileURLToPath } from 'url';
  import { dirname, join } from 'path';
  import dotenv from 'dotenv';
  import { createServer } from 'http';
  import { WebSocketServer, WebSocket } from 'ws';
  import multer from 'multer';
  import fs from 'fs';
  import os from 'os';
  import ffmpeg from 'fluent-ffmpeg';
  import ffmpegStatic from 'ffmpeg-static';
  ```
- `backend/package.json:1-16` — `dependencies` — باید وابستگی‌های مربوط به تلگرام (مثلاً node-telegram-bot-api) به این فایل اضافه شود
  ```json
  {
    "name": "lebanese-dialect-backend",
    "version": "1.0.0",
    "dependencies": {
      "cors": "^2.8.5",
      "dotenv": "^16.3.1",
      "express": "^4.18.2",
      "ws": "^8.19.0"
    }
  }
  ```
- `backend/.env.example:1-12` — `کل فایل` — باید متغیر محیطی TELEGRAM_BOT_TOKEN به این فایل اضافه شود
  ```
  # Gemini API Key - Get from https://aistudio.google.com/apikey
  GEMINI_API_KEY=your_gemini_api_key_here
  
  # Port (optional, default 3001)
  PORT=3001
  
  # Firebase config (optional - for data persistence)
  # VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}
  
  # Inspector Bridge WebSocket URL (frontend build-time var, optional).
  # Configured in frontend/.env (see frontend/.env.example). If unset, the
  # Inspector Bridge WebSocket is gracefully disabled. No third-party URL is
  # hardcoded. Example: VITE_WS_URL=ws://localhost:3001/ws/live
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/server.js` (سطر 1) — فایل اصلی بک‌اند — سرویس تلگرام باید در این فایل import و استفاده شود
- `backend/package.json` (سطر 8) — وابستگی‌های پروژه — باید کتابخانه تلگرام به dependencies اضافه شود
- `backend/.env.example` (سطر 1) — متغیرهای محیطی — باید TELEGRAM_BOT_TOKEN به این فایل اضافه شود
- `frontend/src/App.jsx` — احتمالاً نیاز به اضافه

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد تست‌های واحد برای TelegramService را داده است. این سرویس باید شامل ارسال پیام، دریافت پیام، مدیریت state و هندل کردن خطاها باشد. تست‌ها باید با mock کردن API تلگرام انجام شوند. خارج از این مرحله: تست‌های integration. نکته حیاتی: باید حداقل ۸۰% کد سرویس تلگرام تحت پوشش تست باشد. کاربر همچنین اشاره کرده: 'میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه'. کلیدواژه‌ها: unit tests, TelegramService, mock, pytest, coverage. در کد فعلی پروژه (backend/server.js) هیچ سرویس تلگرامی وجود ندارد. فایل‌های مرتبط شامل backend/server.js (خطوط 1-1432) است که سرویس‌های Express، WebSocket، Gemini API و تحلیل فایل را دارد. همچنین frontend/index.html (خطوط 1-208) شامل Inspector Bridge است. backend/package.json وابستگی‌های فعلی را نشان می‌دهد (express, cors, dotenv, ws, multer, fluent-ffmpeg). برای پیاده‌سازی این درخواست، ابتدا باید سرویس TelegramService ایجاد شود و سپس تست‌های واحد برای آن نوشته شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/services/telegram_service.py با کلاس TelegramService شامل متدهای send_message, receive_message, handle_error, manage_state. 2. ایجاد فایل backend/tests/test_telegram_service.py با تست‌های واحد. 3. استفاده از کتابخانه python-telegram-bot یا requests برای ارتباط با API تلگرام. 4. mock کردن API تلگرام با unittest.mock یا pytest-mock. 5. اطمینان از پوشش 80% کد با pytest-cov. 6. اضافه کردن وابستگی‌ها به backend/requirements.txt یا backend/package.json بسته به زبان.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 24: ایجاد تست‌های integration برای دستورات تلگرام

**Scope:** این مرحله شامل نوشتن تست‌های integration برای تمام دستورات تلگرام (courses, upload, practice و ...) است. باید با شبیه‌سازی پیام‌های تلگرام و بررسی پاسخ‌ها انجام شود. خارج از این مرحله: تست با ربات واقعی. نکته حیاتی: باید از دیتابیس تستی استفاده شود.
**Key terms:** integration tests, command handlers, test database, simulated messages

**بخش مربوط از متن کاربر:**
```
بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد تست‌های integration برای دستورات تلگرام با دیتابیس تستی

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/server.js:1-12` — `imports` — فایل اصلی بک‌اند — هیچ handler تلگرام در آن وجود ندارد. باید پکیج جدید backend/src/telegram/ ایجاد شود و در این فایل import و mount شود (مثلاً در خط 70 بعد از app.use(cors())).
  ```jsx
  import express from 'express';
  import cors from 'cors';
  import { fileURLToPath } from 'url';
  import { dirname, join } from 'path';
  import dotenv from 'dotenv';
  import { createServer } from 'http';
  import { WebSocketServer, WebSocket } from 'ws';
  import multer from 'multer';
  import fs from 'fs';
  import os from 'os';
  import ffmpeg from 'fluent-ffmpeg';
  import ffmpegStatic from 'ffmpeg-static';
  ```
- `backend/package.json:1-15` — `dependencies` — فایل package.json بک‌اند — باید وابستگی‌های جدید (node-telegram-bot-api یا telegraf برای ربات تلگرام، و jest/mocha برای تست) اضافه شود. همچنین اسکریپت test
  ```json
  {
    "name": "lebanese-dialect-backend",
    "version": "1.0.0",
    "dependencies": {
      "cors": "^2.8.5",
      "dotenv": "^16.3.1",
      "express": "^4.18.2",
      "ws": "^8.19.0"
    }
  }
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد تست‌های integration برای تمام دستورات تلگرام (courses, upload, practice و ...) را دارد. این تست‌ها باید با شبیه‌سازی پیام‌های تلگرام و بررسی پاسخ‌ها انجام شوند. خارج از این مرحله: تست با ربات واقعی. نکته حیاتی: باید از دیتابیس تستی استفاده شود. بخش مربوط از درخواست اصلی کاربر: 'بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم'. کلیدواژه‌ها: integration tests, command handlers, test database, simulated messages. در کد فعلی پروژه (backend/server.js) هیچ handler یا endpoint مرتبط با تلگرام وجود ندارد. فایل‌های deep-read شده شامل backend/server.js (خطوط 1-800)، frontend/index.html، و چندین فایل prompt archive هستند. ساختار پروژه شامل backend با Express.js و WebSocket (ws) و frontend با React/Vite است. هیچ فایل handler تلگرام یا تستی در ساختار پروژه دیده نمی‌شود. بنابراین این تسک نیازمند ایجاد ساختار جدید برای handlerهای تلگرام و تست‌های integration مربوطه است. کاربر صراحتاً به دستورات courses, upload, practice اشاره کرده و خواسته که از دیتابیس تستی استفاده شود. همچنین تأکید کرده که تست با ربات واقعی خارج از این مرحله است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد پکیج جدید backend/src/telegram/ شامل فایل‌های: handlers.js (برای مدیریت دستورات courses, upload, practice و ...)، bot.js (برای راه‌اندازی ربات تلگرام با کتابخانه node-telegram-bot-api یا telegraf)، و utils.js (برای توابع کمکی). 2. ایجاد فایل backend/tests/telegram.integration.test.js با استفاده از کتابخانه Jest یا Mocha. 3. در تست‌ها، از یک mock برای Telegram Bot API استفاده شود (مثلاً با sinon یا nock) تا پیام‌های تلگرام شبیه‌سازی شوند. 4. دیتابیس تستی: از SQLite در حافظه (better-sqlite3 با :memory:) یا MongoDB در حال اجرا با docker استفاده شود. 5. برای هر دستور (courses, upload, practice) یک تست بنویس که: پیام شبیه‌سازی شده ارسال کند، پاسخ handler را بررسی کند، و وضعیت دیتابیس تستی را بعد از اجرا تأیید کند. 6. فایل backend/package.json باید اسکریپت test:integration اضافه کند. 7. مستندات تست در backend/tests/README.md نوشته شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 25: ایجاد مستندات کاربری برای ربات تلگرام

**Scope:** این مرحله شامل نوشتن مستندات کامل برای کاربران ربات تلگرام است. شامل توضیح تمام دستورات، نحوه استفاده، مثال‌ها و troubleshooting. باید به صورت فایل Markdown در repository ذخیره شود. خارج از این مرحله: مستندات فنی. نکته حیاتی: باید به زبان فارسی باشد.
**Key terms:** documentation, user guide, README, Markdown, Persian

**بخش مربوط از متن کاربر:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد مستندات کاربری ربات تلگرام به زبان فارسی

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `docs/telegram-user-guide.md:1-1` — `N/A (فایل جدید)` — این فایل باید ایجاد شود. مسیر docs/telegram-user-guide.md در ساختار پروژه وجود ندارد و باید ساخته شود.
  ```markdown
  فایل جدید — هنوز وجود ندارد
  ```
- `README.md:1-1` — `N/A (فایل جدید یا موجود)` — بر اساس ساختار پروژه، فایل README.md در ریشه وجود ندارد. اگر بعداً ایجاد شود، لینک به docs/telegram-user-guide.md باید اضافه گردد.
  ```markdown
  فایل README.md در ریشه پروژه موجود نیست (در ساختار فایل‌ها دیده نشد). اگر وجود داشته باشد، باید لینک به مستندات تلگرام اضافه شود.
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/server.js` (سطر 1) — در آینده، endpointهای تلگرام باید به این فایل اضافه شوند. مستندات باید با معماری این فایل (Express.js, WebSocket) سازگار باشد.
- `backend/package.json` (سطر 1) — برای اضافه کردن وابستگی‌های تلگرام (مانند node-telegram-bot-api) باید این فایل به‌روزرسانی شود. مستندات باید به این موضوع اشاره کند.
- `frontend/src/App.jsx` (سطر 1) — در آینده، ممکن است نوتیفیکیشن‌های تلگرام در فرانت‌اند نمایش داده شوند. مستندات باید به این قابلیت اشاره کند.

## 🌐 نقشهٔ وابستگی‌ها
این تسک وابستگی به کد موجود ندارد زیرا صرفاً یک فایل مستندات جدید ایجاد می‌کند. با این حال، در آینده برای پیاده‌سازی واقعی ربات تلگرام، فایل‌های backend/server.js (برای اضافه کردن endpointهای تلگرام)، backend/package.json (برای اضافه

## 🔍 Context و وضعیت فعلی
ایجاد مستندات کامل برای کاربران ربات تلگرام این پروژه. این مستندات شامل توضیح تمام دستورات، نحوه استفاده، مثال‌ها و troubleshooting است. باید به صورت فایل Markdown در repository ذخیره شود. خارج از این مرحله: مستندات فنی. نکته حیاتی: باید به زبان فارسی باشد. بخش مربوط از درخواست اصلی کاربر: 'میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه'. کلیدواژه‌ها: documentation, user guide, README, Markdown, Persian. در کد فعلی پروژه (backend/server.js) هیچ endpoint یا سرویس تلگرامی وجود ندارد. فایل‌های backend/package.json و backend/package-lock.json نیز هیچ وابستگی به کتابخانه‌های تلگرام (مانند node-telegram-bot-api یا telegraf) ندارند. frontend/index.html و frontend/src/App.jsx نیز هیچ اشاره‌ای به تلگرام ندارند. بنابراین این تسک صرفاً ایجاد مستندات کاربری برای ربات تلگرام است که باید در آینده ساخته شود. فایل مستندات باید در مسیر docs/telegram-user-guide.md یا مشابه آن ایجاد شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل docs/telegram-user-guide.md در ریشه پروژه. 2. نوشتن مستندات به زبان فارسی با ساختار زیر: - مقدمه و معرفی ربات تلگرام - نحوه شروع به کار (شروع مکالمه با ربات) - لیست کامل دستورات با توضیح هر کدام (مانند /start, /help, /analyze, /settings, /feedback) - مثال‌های کاربردی برای هر دستور - راهنمای عیب‌یابی (troubleshooting) برای مشکلات رایج - اطلاعات تماس و پشتیبانی 3. اطمینان از اینکه مستندات در فهرست (Table of Contents) قابل جستجو هستند. 4. اضافه کردن لینک به این مستندات در فایل README اصلی پروژه (در صورت وجود). 5. از آنجایی که در حال حاضر هیچ کد تلگرامی در پروژه وجود ندارد، مستندات باید به صورت generic و قابل تطبیق با هر کتابخانه تلگرامی (مانند node-telegram-bot-api یا telegraf) نوشته شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: docs
- اولویت: medium
- تخمین زمان: small

---

## ✅ معیارهای پذیرش کلی (همهٔ مراحل)
- [ ] همهٔ مراحل بالا با موفقیت پیاده‌سازی شده‌اند
- [ ] تست‌های موجود pass می‌شوند
- [ ] هیچ regression رخ نداده

## Acceptance Criteria

1. فایل backend/services/telegramService.js ایجاد شده و تابع sendVoiceMessage را export کند _(verify: static)_

## Task Steps

### Step 1: ایجاد ساختار پایه برای اتصال تلگرام دوطرفه (Bot API + Webhook)
**Status:** `done` (100%)
**Scope:** این مرحله شامل ایجاد یک سرویس تلگرام با استفاده از python-telegram-bot یا aiogram است که قابلیت دریافت و ارسال پیام را دارد. باید webhook یا polling برای دریافت پیام‌های کاربر تنظیم شود. این مرحله فقط زیرساخت ارتباطی را فراهم می‌کند و شامل هیچ منطق تجاری یا دستور خاصی نیست. نکته حیاتی: حتماً از async استفاده شود تا با سایر بخش‌های پروژه تداخل نداشته باشد.
**Excerpt:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته، بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

### Step 2: پیاده‌سازی سیستم نوتیفیکیشن تلگرام برای رویدادهای سیستم
**Status:** `done` (100%)
**Scope:** این مرحله شامل ایجاد مکانیزمی است که رویدادهای مهم سیستم (مثلاً ثبت‌نام جدید، تکمیل تمرین، خطا) را به کاربر از طریق تلگرام ارسال کند. باید یک سرویس NotificationService بسازد که از طریق TelegramService پیام ارسال کند. خارج از این مرحله: تعیین اینکه چه رویدادهایی باید نوتیفیکیشن بفرستند (بعداً مشخص می‌شود). نکته حیاتی: باید از queue یا event bus استفاده شود تا ارسال نوتیفیکیشن async باشد.
**Excerpt:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه یعنی علاوه بر اینکه اطلاعات از تعیین شده رو از سیستم میگیره و میفرسته
```

### Step 3: پیاده‌سازی دستورات پایه کنترل سیستم از طریق تلگرام (Command Handlers)
**Status:** `done` (100%)
**Scope:** این مرحله شامل تعریف و پیاده‌سازی دستورات پایه تلگرام مانند /status (وضعیت سیستم)، /help (راهنما)، /courses (لیست دروس) است. باید یک CommandHandler برای هر دستور ایجاد شود که منطق مربوطه را اجرا کند. خارج از این مرحله: دستورات پیچیده مانند آپلود فایل یا مدیریت تمرین با استاد. نکته حیاتی: دستورات باید با decorator یا registration ثبت شوند و پاسخ‌ها به صورت Markdown یا HTML فرمت شوند.
**Excerpt:**
```
بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

### Step 4: ایجاد سیستم احراز هویت و اتصال کاربر تلگرام به حساب کاربری
**Status:** `done` (100%)
**Scope:** این مرحله شامل ایجاد مکانیزمی است که کاربر تلگرام را به حساب کاربری موجود در سیستم متصل کند. باید یک دستور /login یا /verify ایجاد شود که کاربر یک کد یکتا دریافت کند و در وبسایت وارد کند. همچنین باید mapping بین chat_id تلگرام و user_id سیستم ذخیره شود. خارج از این مرحله: احراز هویت دو مرحله‌ای یا OAuth. نکته حیاتی: امنیت بالا لازم است و کدها باید زمان‌دار (expire) باشند.
**Excerpt:**
```
بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

### Step 5: ایجاد دستور /courses برای نمایش لیست دروس و وضعیت آنها
**Status:** `done` (100%)
**Scope:** این مرحله شامل توسعه دستور /courses است که لیست تمام دروس موجود در سیستم را با وضعیت هر کدام (فعال/غیرفعال، تعداد جلسات، آخرین به‌روزرسانی) نمایش دهد. باید از دیتابیس خوانده شود و به صورت فرمت‌شده در تلگرام نمایش داده شود. خارج از این مرحله: امکان تغییر وضعیت دروس از تلگرام. نکته حیاتی: پاسخ باید خلاصه و قابل اسکن باشد (حداکثر ۱۰ درس در هر پیام).
**Excerpt:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

### Step 6: ایجاد دستور /course_content برای مشاهده محتوای یک درس خاص
**Status:** `done` (100%)
**Scope:** این مرحله شامل ایجاد دستور /course_content <course_id> است که محتوای یک درس خاص (لیست جلسات، فایل‌ها، تمرین‌ها) را نمایش دهد. باید از دیتابیس یا storage محتوای درس را بخواند و به صورت دسته‌بندی‌شده نمایش دهد. خارج از این مرحله: آپلود محتوا. نکته حیاتی: محتوا ممکن است حجیم باشد، بنابراین باید صفحه‌بندی (pagination) شود.
**Excerpt:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

### Step 7: ایجاد دستور /upload برای آپلود محتوای دروس از طریق تلگرام
**Status:** `done` (100%)
**Scope:** این مرحله شامل ایجاد دستور /upload <course_id> است که کاربر می‌تواند یک فایل (PDF، تصویر، ویدئو) را به عنوان محتوای درس آپلود کند. باید فایل را از تلگرام دریافت کرده، در storage ذخیره کند و متادیتا را در دیتابیس ثبت کند. خارج از این مرحله: پردازش فایل (OCR، تبدیل). نکته حیاتی: محدودیت حجم فایل تلگرام (۵۰ مگابایت) باید مدیریت شود و فایل‌های بزرگتر رد شوند.
**Excerpt:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

### Step 8: ایجاد دستور /delete_content برای حذف محتوای دروس از تلگرام
**Status:** `done` (100%)
**Scope:** این مرحله شامل ایجاد دستور /delete_content <course_id> <content_id> است که کاربر می‌تواند یک محتوای خاص را از درس حذف کند. باید از دیتابیس و storage حذف شود. خارج از این مرحله: حذف دسته‌جمعی. نکته حیاتی: باید تأییدیه (confirmation) از کاربر گرفته شود قبل از حذف.
**Excerpt:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

### Step 9: ایجاد دستور /edit_course برای ویرایش متادیتای دروس از تلگرام
**Status:** `done` (100%)
**Scope:** این مرحله شامل ایجاد دستور /edit_course <course_id> است که کاربر می‌تواند متادیتای درس (نام، توضیحات، وضعیت) را ویرایش کند. باید از طریق مراحل (step-by-step) با کاربر تعامل کند. خارج از این مرحله: ویرایش محتوای دروس. نکته حیاتی: باید validation روی ورودی‌ها انجام شود.
**Excerpt:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

### Step 10: ایجاد دستور /add_course برای افزودن درس جدید از تلگرام
**Status:** `done` (100%)
**Scope:** این مرحله شامل ایجاد دستور /add_course است که کاربر می‌تواند یک درس جدید با نام، توضیحات و سایر فیلدها ایجاد کند. باید از طریق مراحل با کاربر تعامل کند و در دیتابیس ذخیره کند. خارج از این مرحله: افزودن محتوا به درس جدید. نکته حیاتی: باید بررسی شود که درس تکراری نباشد.
**Excerpt:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

### Step 11: ایجاد دستور /delete_course برای حذف درس از تلگرام
**Status:** `done` (100%)
**Scope:** این مرحله شامل ایجاد دستور /delete_course <course_id> است که کاربر می‌تواند یک درس و تمام محتوای آن را حذف کند. باید تأییدیه دو مرحله‌ای بگیرد و سپس درس و محتواهای مرتبط را از دیتابیس و storage حذف کند. خارج از این مرحله: حذف انتخابی محتوا. نکته حیاتی: این عملیات غیرقابل بازگشت است و باید هشدار داده شود.
**Excerpt:**
```
و اینکه بتونم محتوای دروس رو هم از اینجا ببینم یا اپلود کنم و تا حد زیادی کار رو از اینجا پیش ببرم ...
```

### Step 12: ایجاد دستور /practice برای اتصال به بخش 'تمرین با استاد هوش مصنوعی'
**Status:** `done` (100%)
**Scope:** این مرحله شامل ایجاد دستور /practice است که کاربر را به بخش تمرین با استاد هوش مصنوعی متصل می‌کند. باید وضعیت فعلی جلسات تمرین را نمایش دهد و گزینه‌هایی برای شروع جلسه جدید یا ادامه جلسه قبلی ارائه دهد. خارج از این مرحله: خود مکالمه با هوش مصنوعی. نکته حیاتی: باید از API موجود در بخش 'تمرین با استاد هوش مصنوعی' استفاده کند.
**Excerpt:**
```
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم
```

### Step 13: ایجاد دستور /practice_topic برای انتخاب موضوع تمرین از تلگرام
**Status:** `done` (100%)
**Scope:** این مرحله شامل ایجاد دستور /practice_topic <session_id> است که کاربر می‌تواند موضوع تمرین را برای یک جلسه انتخاب کند. باید لیست موضوعات موجود را از بخش AI دریافت کرده و به کاربر نمایش دهد. کاربر با انتخاب یک موضوع، آن را برای جلسه تنظیم می‌کند. خارج از این مرحله: ایجاد موضوع جدید. نکته حیاتی: موضوعات باید از API بخش AI دریافت شوند.
**Excerpt:**
```
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم، و انتخاب کنم
```

### Step 14: ایجاد دستور /practice_chat برای شروع مکالمه با هوش مصنوعی از طریق تلگرام
**Status:** `done` (100%)
**Scope:** این مرحله شامل ایجاد دستور /practice_chat <session_id> است که کاربر را وارد حالت مکالمه با هوش مصنوعی می‌کند. در این حالت، تمام پیام‌های کاربر به API هوش مصنوعی ارسال شده و پاسخ دریافت می‌شود. خارج از این مرحله: پردازش پیام صوتی. نکته حیاتی: باید state management داشته باشد تا بداند کاربر در کدام جلسه است.
**Excerpt:**
```
و از طریق همین چت با هوش مصنوع موجود در اون چت صحبت کنم
```

### Step 15: پیاده‌سازی قابلیت ارسال پیام صوتی به هوش مصنوعی از تلگرام
**Status:** `done` (100%)
**Scope:** این مرحله شامل اضافه کردن قابلیت دریافت پیام صوتی از کاربر در حالت مکالمه با هوش مصنوعی است. باید فایل صوتی را از تلگرام دریافت کرده، به سرویس Speech-to-Text ارسال کند، متن استخراج‌شده را به API هوش مصنوعی بفرستد و پاسخ را دریافت کند. خارج از این مرحله: پاسخ صوتی. نکته حیاتی: باید از میکروفون تلگرام استفاده شود و فایل صوتی به فرمت OGG دریافت می‌شود.
**Excerpt:**
```
و از همین میکروفون استفاده کنم برای پیام صوتی و همین صوت اونجا بره و انالیز و استخراج بشه
```

### Step 16: پیاده‌سازی قابلیت دریافت پاسخ صوتی از هوش مصنوعی در تلگرام
**Status:** `done` (100%)
**Scope:** این مرحله شامل اضافه کردن قابلیت ارسال پاسخ صوتی از هوش مصنوعی به کاربر است. باید متن پاسخ هوش مصنوعی را به سرویس Text-to-Speech بفرستد، فایل صوتی حاصل را در تلگرام آپلود کند و برای کاربر ارسال کند. خارج از این مرحله: انتخاب صدای مختلف. نکته حیاتی: باید هم پاسخ صوتی و هم متن پاسخ به صورت همزمان ارسال شود.
**Excerpt:**
```
و پاسخ صوتی استاد و متن و هر چی اونجا تو چت یره سریع تو تلگرام هم بیاد
```

### Step 17: ایجاد دستور /practice_end برای پایان جلسه تمرین از تلگرام
**Status:** `done` (100%)
**Scope:** این مرحله شامل ایجاد دستور /practice_end <session_id> است که کاربر می‌تواند جلسه تمرین را پایان دهد. باید خلاصه جلسه (تعداد پیام‌ها، موضوع، مدت زمان) را از API هوش مصنوعی دریافت کرده و برای کاربر ارسال کند. خارج از این مرحله: ذخیره تاریخچه. نکته حیاتی: باید state کاربر را به حالت عادی برگرداند.
**Excerpt:**
```
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم
```

### Step 18: ایجاد دستور /practice_history برای مشاهده تاریخچه جلسات تمرین از تلگرام
**Status:** `done` (100%)
**Scope:** این مرحله شامل ایجاد دستور /practice_history است که کاربر می‌تواند تاریخچه جلسات تمرین خود را مشاهده کند. باید از دیتابیس یا API هوش مصنوعی تاریخچه را دریافت کرده و به صورت لیست نمایش دهد. خارج از این مرحله: مشاهده جزئیات هر جلسه. نکته حیاتی: باید صفحه‌بندی شود.
**Excerpt:**
```
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم
```

### Step 19: ایجاد دستور /practice_details برای مشاهده جزئیات یک جلسه تمرین خاص
**Status:** `done` (100%)
**Scope:** این مرحله شامل ایجاد دستور /practice_details <session_id> است که کاربر می‌تواند جزئیات یک جلسه خاص (موضوع، تعداد پیام‌ها، خلاصه) را مشاهده کند. باید از API هوش مصنوعی یا دیتابیس اطلاعات را دریافت کند. خارج از این مرحله: مشاهده متن کامل مکالمه. نکته حیاتی: باید خلاصه‌ای از مکالمه ارائه دهد.
**Excerpt:**
```
و اگر به قسمت "تمرین با استاد هوش مصنوعی" وصل شدم بتونم موضوعات رو از همین تلگرام هم مدیریتش کنم
```

### Step 20: ایجاد دستور /settings برای مدیریت تنظیمات نوتیفیکیشن و تلگرام
**Status:** `done` (100%)
**Scope:** این مرحله شامل ایجاد دستور /settings است که کاربر می‌تواند تنظیمات نوتیفیکیشن (فعال/غیرفعال کردن انواع نوتیفیکیشن‌ها) و تنظیمات عمومی تلگرام را مدیریت کند. باید از طریق دکمه‌های inline keyboard تعامل انجام شود. خارج از این مرحله: تنظیمات پیشرفته. نکته حیاتی: تنظیمات باید در دیتابیس ذخیره شود.
**Excerpt:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه
```

### Step 21: ایجاد دستور /help برای نمایش راهنمای کامل دستورات تلگرام
**Status:** `done` (100%)
**Scope:** این مرحله شامل به‌روزرسانی دستور /help است تا تمام دستورات جدید (courses, upload, practice, settings و ...) را به صورت دسته‌بندی‌شده نمایش دهد. باید با inline keyboard یا Markdown فرمت شود. خارج از این مرحله: راهنمای پیشرفته. نکته حیاتی: باید بر اساس سطح دسترسی کاربر، دستورات متفاوت نمایش دهد.
**Excerpt:**
```
بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

### Step 22: پیاده‌سازی سیستم لاگینگ و مانیتورینگ برای ربات تلگرام
**Status:** `done` (100%)
**Scope:** این مرحله شامل اضافه کردن لاگینگ برای تمام تعاملات ربات تلگرام (دستورات، خطاها، زمان پاسخ) است. باید لاگ‌ها در فایل یا دیتابیس ذخیره شوند و یک دستور /admin_logs برای مدیران ایجاد شود. خارج از این مرحله: آنالیز لاگ. نکته حیاتی: لاگ‌ها نباید شامل اطلاعات حساس (پسورد) باشند.
**Excerpt:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه
```

### Step 23: ایجاد تست‌های واحد برای سرویس تلگرام
**Status:** `done` (100%)
**Scope:** این مرحله شامل نوشتن تست‌های واحد برای TelegramService شامل ارسال پیام، دریافت پیام، مدیریت state و هندل کردن خطاها است. باید با mock کردن API تلگرام تست شود. خارج از این مرحله: تست‌های integration. نکته حیاتی: باید حداقل ۸۰% کد سرویس تلگرام تحت پوشش تست باشد.
**Excerpt:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه
```

### Step 24: ایجاد تست‌های integration برای دستورات تلگرام
**Status:** `done` (100%)
**Scope:** این مرحله شامل نوشتن تست‌های integration برای تمام دستورات تلگرام (courses, upload, practice و ...) است. باید با شبیه‌سازی پیام‌های تلگرام و بررسی پاسخ‌ها انجام شود. خارج از این مرحله: تست با ربات واقعی. نکته حیاتی: باید از دیتابیس تستی استفاده شود.
**Excerpt:**
```
بتونم از طریق تلگرام هم کنترل و انجام برخی کارها روی سیستم هم داشته باشم
```

### Step 25: ایجاد مستندات کاربری برای ربات تلگرام
**Status:** `done` (100%)
**Scope:** این مرحله شامل نوشتن مستندات کامل برای کاربران ربات تلگرام است. شامل توضیح تمام دستورات، نحوه استفاده، مثال‌ها و troubleshooting. باید به صورت فایل Markdown در repository ذخیره شود. خارج از این مرحله: مستندات فنی. نکته حیاتی: باید به زبان فارسی باشد.
**Excerpt:**
```
میخوام برای این پروژه هم نوتیفیکشن از طریق تلگرام فراهم بشه و تلگرام دو طرفه کار کنه
```
